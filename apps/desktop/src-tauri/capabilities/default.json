{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Phase 3C desktop dev MVP capability with minimal dialog permissions.",
  "windows": ["main"],
  "permissions": ["dialog:allow-open", "dialog:allow-save"]
}
