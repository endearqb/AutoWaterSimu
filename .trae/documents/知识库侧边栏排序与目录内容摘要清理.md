## 目标
- 将 `目录内容摘要.md` 置于知识库侧边栏首位，将 `废水处理技术分类总结报告.md` 置于第二位。
- 清理 `目录内容摘要.md` 中的条目，移除每行末尾的文件大小与说明（如 `(52KB) - …`），保留可点击的文件名链接，并确保可跳转到知识库对应内容。

## 技术方案
- 侧边栏排序依据：按 `frontmatter.order` 排序，未设定时为最大值，随后按标题排序（`frontend/src/knowledge/build-tree.ts:86-91`）。
- 默认首页：`KNOWLEDGE_DEFAULT_SLUG = "目录内容摘要"`（`frontend/src/knowledge/index.ts:4`），无需变更。
- 链接跳转：MDX/Markdown 中以 `/knowledge/...` 的绝对路径链接，组件将使用 Router 进行前端路由跳转（`frontend/src/components/MDX/CustomMDX.tsx:47-55` 与路由 `frontend/src/routes/_layout/knowledge/$...slug.tsx:4-10`）。

## 实施步骤
1. 为两份根目录文档添加 YAML frontmatter 并设定排序：
   - `frontend/content/knowledge/目录内容摘要.md` 顶部加入：
     ```yaml
     ---
     title: 目录内容摘要
     order: 1
     ---
     ```
   - `frontend/content/knowledge/废水处理技术分类总结报告.md` 顶部加入：
     ```yaml
     ---
     title: 废水处理技术分类总结报告
     order: 2
     ---
     ```
   - 排序将通过 `frontmatter.order` 生效（`frontend/src/knowledge/mdx-manifest.ts:79-87` 与排序 `frontend/src/knowledge/mdx-manifest.ts:94-99`）。
2. 清理 `frontend/content/knowledge/目录内容摘要.md` 条目内容：
   - 将所有列表项末尾的 `(<大小>KB) - <说明>` 统一删除，仅保留 `[文件名](/knowledge/…/…/…)` 格式。
   - 修正不存在的链接：将 `MBR污水处理技术详解` 链接改为实际存在的 `MBBR污水处理技术详解`（对应文件 `frontend/content/knowledge/2. 废水处理技术/01 生物处理技术/MBBR污水处理技术详解.md`）。
   - 特别处理需求行：移除 `/d:/MyProject/dataAnalysis/my-full-stack/frontend/content/knowledge/目录内容摘要.md#L8-16` 中每行的大小与说明，保留链接文字与路径。

## 验证步骤
- 本地预览：启动前端开发服务器，打开 `/knowledge` 验证侧边栏排序与默认首页。
- 跳转检查：在“目录内容摘要”页面点击每个链接，确认能进入对应知识页；特别验证中文和空格的路径正常路由（`$...slug` 支持整段路径）。
- 类型检查：执行 `cd frontend; npx tsc --noEmit` 确认类型无误（项目规范 1.1）。

## 影响范围
- 仅修改两份 Markdown 的 frontmatter 与目录摘要的内容行；不涉及前端组件或路由逻辑变更。
- 侧边栏的排序将稳定为：第一“目录内容摘要”、第二“废水处理技术分类总结报告”，其他条目按默认规则排序。

## 交付物
- 更新后的 `frontend/content/knowledge/目录内容摘要.md`（去除条目末尾大小与说明、修正错误链接、添加 frontmatter）。
- 更新后的 `frontend/content/knowledge/废水处理技术分类总结报告.md`（添加 frontmatter）。
