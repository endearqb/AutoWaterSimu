## 目标
- 运行时间按钮固定在中部行最右侧（flex-end），视觉更明确。
- 增加面板中部与底部区块之间的垂直间距，层次更清晰。

## 修改
1. 中部行（参数按钮行）
- 在中部 `HStack` 上增加 `mb={4}`，与下层拉开距离。
- 保持右侧容器 `justifyContent="flex-end"`，确保运行时间按钮在最右侧。

2. 底部区块（分析结果/小地图行）
- 在底部包裹 `Box`（带 `borderTopWidth`）上增加 `mt={4}`，与中部进一步拉开距离。

## 验证
- 类型检查：`cd frontend; npx tsc --noEmit`。
- 运行预览：确认运行时间按钮位于最右侧；中部与底部间距明显增加，其他交互不受影响。