## 目标
- 将面板圆角改为 `full` 以查看视觉效果。
- 保持“运行时间”按钮位于面板上层中部行的最右侧（整体行的右端），不做中列定位。

## 实施
1. 圆角调整
- 在 `SimulationActionPlate.tsx` 的 `panelStyles` 中，将 `borderRadius: GLASS_PANEL_RADIUS` 修改为 `borderRadius: 'full'`。

2. 位置确认
- 当前“运行时间”按钮容器为右列 `Box flex={1} display="flex" justifyContent="flex-end"`（位于中部行整体最右侧），无需调整结构，仅保留现状。

## 验证
- 类型检查：`cd frontend; npx tsc --noEmit`。
- 预览：面板呈满圆角；运行时间按钮处于面板上层中部行的最右端，交互与弹层正常。