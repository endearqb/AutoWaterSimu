## 目标
- 将面板左下主菜单按钮改为与“小地图/每小时步数/采样间隔”相同的样式与动效（使用现有 `getToggleButtonStyles`）。
- 点击交互与其他弹出按钮一致：互斥展开，点击面板或画布其他区域时收起。

## 具体修改
- 在 `frontend/src/components/Flow/inspectorbar/SimulationActionPlate.tsx`：
  - 用 `Button size="xs" {...getToggleButtonStyles(false)}` 替换当前 `IconButton` 主菜单触发器；内容使用 `HStack` 包含 `FiMenu` 图标与“保存与导入”文字，悬停通过现有 `showMenuLabel` 控制 `Text` 的 `maxWidth/opacity` 过渡（保持与“小地图”一致）。
  - 保留 `aria-label="menu"`、`ref={bubbleBtnRef}`、指针事件与现有点击逻辑；`onMouseEnter/onMouseLeave` 不变。
  - 删除或不再使用 `bubbleTriggerStyles`，按钮样式统一走 `getToggleButtonStyles`。
  - 互斥处理：主菜单按钮展开时继续调用 `closeSteps/closeResample/closeHours`（已有）；全局点击外部关闭逻辑中补充依赖项 `isBubbleOpen`，确保监听在气泡开关变化时也更新（扩展 effect 依赖：`[isStepsOpen, isResampleOpen, isHoursOpen, isBubbleOpen]`）。

## 已满足点（确认保留）
- 打开主菜单时关闭其他弹出（已实现）。
- 点击面板或画布其他区域收起（已实现：包含 `bubbleOverlayRef`/`bubbleBtnRef` 判断）。

## 验证
- 类型检查：`cd frontend; npx tsc --noEmit`。
- 运行预览：悬停主菜单按钮表现与“小地图”等一致；点击互斥、外部点击收起正常。