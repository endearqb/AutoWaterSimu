[project]
name = "autowatersimu-contracts"
version = "0.1.0+phase2b"
description = "Python contract transform helpers for AutoWaterSimu Next"
requires-python = ">=3.10,<4.0"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["autowatersimu_contracts"]
