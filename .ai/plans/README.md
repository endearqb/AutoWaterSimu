# 目录说明：.ai/plans

## 1. 目录职责

本目录保存可选复杂任务计划。

## 2. 核心文件

后续按任务新增 Markdown 文件。

## 3. 维护约定

只保存需要跨会话延续的计划。

## 4. 对外接口

对 Agent 和开发者提供执行前上下文。

## 5. 依赖边界

不替代 `tasks/todo.md` 的当前任务追踪。

## 6. 测试与验证

修改后运行 `git diff --check -- .ai/plans`。

## 7. AI 操作提示

计划完成后应在 `.ai/changes/` 记录执行结果。
