## 12:21 - 新增 macOS 本地启动文档 [L1]

- 目标与修改：新增 `docs/architecture/next-startup-macos.md`，记录 macOS/zsh 下数据库、Go API、Python worker 和 standalone 前端的四终端启动、验证、停止及排障流程；同步更新架构文档索引。
- 原因：现有启动文档默认 Windows PowerShell，且 `Justfile` 与 Windows 虚拟环境路径不能直接用于 macOS。
- 假设 / 剩余不确定性：采用当前 Next 主线、no-auth 本机源码调试和 `docker-compose.dev.yml` PostgreSQL；前端默认端口可能被其他本地项目占用，因此提供 `5174` 备用命令。
- 验证证据：`docker compose ... ps compute-postgres` → PostgreSQL healthy；`curl http://127.0.0.1:8088/readyz` → `{"status":"ready"}`；worker `--run-api-once` → 成功注册且空队列返回 `idle`；文档修改后运行格式与链接检查。
- 文档更新：新增 macOS 启动文档并更新 `docs/architecture/README.md`。

## 20:36 - 对齐 UDM-v2 画布 UI、交互与国际化 [L2]

- 用户目标：保持 UDM-v2 的业务数据与 v1 独立，同时复用一致的画布 UI/交互；修复工作台过大稀疏、中英文混排、选中边交互不一致和默认流量数字消失。
- 涉及目录：`frontend/src/features/udm-v2/`、`frontend/src/components/Flow/`、`frontend/tests/`、`scripts/`、`.ai/decisions/`、`.ai/plans/`、`tasks/`。
- 修改内容：将浮动工作台压缩为桌面端 360px、节点面板改为紧凑双列；新增可白名单复用的无状态 `EdgeQuickToolbar`；hydraulic/pump 生成流量标签改为常驻 `Q <value> m³/d`，标签可点击选中，选中后显示连线上方类型快捷栏并继续打开右侧参数抽屉；新增经端口兼容校验的 `changeEdgeKind`；补齐 UDM-v2 工作台、节点、连线、状态和属性编辑器的中英文/ARIA 文案；补充 Vitest 与 Playwright 回归。
- 修改原因：原 v2 在展示层重复实现并偏离 v1 的画布交互语言，且边渲染代码只在 `selected` 时展示生成标签，导致实时流量计算虽存在但默认不可见。
- 本次已消除的不确定性：确认实时流量仍由 `NetworkV2Canvas` 注入 render copy，问题只在标签显隐；确认选边已有右侧抽屉状态链路；确认原 hard-isolation ADR 可以在不共享 store/合同/服务的前提下放行无状态展示原语。
- 本次采用的关键假设：自定义边标签与生成流量并列显示；边类型切换保留 `data.ui`，类型专有字段重置为目标类型默认值；技术枚举值可保持英文，但所有用户可见 UI/ARIA 随语言切换。
- 剩余不确定性：既有导入夹具 `valid-network-v2-canvas.json` 与当前页面导入格式不一致会触发通用操作失败，本次未扩展到导入合同修复；后端依赖的保存/加载 Playwright 用例在无后端环境下按既有条件跳过。
- 影响范围 / 风险控制方式：未修改 schema、序列化 payload、solver、后端 API、数据库或 v1 store；共享组件新增能力保留 legacy 默认值；边类型切换必须通过现有连接校验；boundary allowlist 仅新增 `EdgeQuickToolbar`。
- 验证证据：`npm run typecheck` → exit 0；`npm run test:unit -- --run` → 17 files / 53 tests passed（仅既有 jsdom/Chakra CSS parse 警告）；`npm run check:udm-v2-boundary` → passed；聚焦 Playwright → 14 passed / 1 backend-dependent skipped；浏览器 1280x720 实测工作台约 358px 且中文紧凑布局正确；`git diff --check` → exit 0。README PowerShell 门禁因本机无 `pwsh`/`powershell` 未运行，已人工核对相关 README。
- 是否更新 README：是，更新 Flow shared 边界以及 UDM-v2 canvas/edges/palette/state 维护契约，并新增 `components/Flow/shared/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：是，修订 ADR-0028，明确独立的是业务与数据边界，而不是展示层 UI/交互语言。
