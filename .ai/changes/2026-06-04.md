## 20:31 - Simulation input workflow file split

- 用户目标：根据 Certainty/Elegance PRD 与开发计划继续完善 AutoWaterSimu Next，完成一个可验证的大阶段并适时提交推送。
- 涉及目录：`apps/api/internal/compute/`、`apps/api/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`。
- 修改内容：将 `SimulationInputService` workflow 方法按同 package 文件拆分到 `simulation_inputs_registry.go`、`simulation_inputs_process_graphs.go` 和 `simulation_inputs_resolution.go`；`simulation_inputs.go` 仅保留 struct、constructor、replay store interface 与 store wiring；同步更新 API/compute README、architecture current-state、compute-api audit summary、Certainty/Elegance checklist 和任务计划。
- 修改原因：继续落实 Certainty/Elegance Phase 1 的 handler/package surface reduction 与 Go API large-file cleanup；`simulation_inputs.go` 是 artifact lifecycle、model governance、evidence governance workflow split 后 remaining compute workflow large-file target。
- 本次已消除的不确定性：确认本轮只做同 package workflow 文件拆分；确认 `SimulationInputService` public 方法、HTTP routes、OpenAPI、contracts、migrations、generated clients、auth scopes、store interfaces、public `Service` delegates、process graph validation/projection、simulation input/process graph record mapping、idempotency 和 model-run replay 行为不需要改变；确认完整 simulation input/process graph domain package migration 需要未来 DTO/store/HTTP 边界准备，不在本阶段执行。
- 本次采用的关键假设：当前未跟踪的 `docs/rebuild/AutoWaterSimu_95分优雅度完整计划.md` 不属于本阶段目标文件；simulation input/process graph workflow 暂留 compute compatibility package，不在本阶段迁入 `domain/simulation`，避免同时改变 metadata persistence、store implementations、HTTP mapping 和 schema/store orchestration 边界。
- 剩余不确定性：完整 simulation input/process graph metadata service domain package migration 尚未完成；schema validation、input_ref resolution orchestration、metadata persistence、Memory/Postgres implementations、evidence-ref lookup、HTTP mapping、public `NewService` / `NewServiceWithArchive` 构造签名收窄和 broader production-security work 仍是后续阶段。
- 影响范围 / 风险控制方式：不改变 API 行为、持久化语义、schema 或公共签名；新增文件与原文件保持同 package、同 service receiver、同 helper/callback 调用；通过 focused Go tests、full Go tests、architecture boundary audit、dependency checks 和 pr-fast 验证拆分无行为/边界回归。
- 验证方式：`cd apps\api; go test ./internal/compute -run "TestSimulationCheckEndpointCreatesComputeJob|TestProcessGraphEvidenceReference|TestBenchmarkCaseScheduleRunEndpoint" -count=1` 通过；`cd apps\api; go test ./...` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` 通过；`rg -n "schema_version|P0|P1|P2" docs\rebuild --glob "AutoWaterSimu_Next_*.md"` 已检查；`git diff --check -- apps\api docs scripts tasks .ai` 通过，仅有 LF/CRLF warning。
- 是否更新 README：是，更新 `apps/api/README.md` 与 `apps/api/internal/compute/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本次是既有 modular monolith / same-package file split 路线的延续，没有新增长期架构决策。
