## 22:24 - Model governance MemoryStore persistence file split

- 用户目标：根据 Certainty/Elegance PRD 与开发计划继续完善 AutoWaterSimu Next，完成一个可验证的大阶段并适时提交推送。
- 涉及目录：`apps/api/internal/compute/`、`apps/api/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`。
- 修改内容：删除宽泛的 `memory_models.go`，将原有模型治理 MemoryStore metadata persistence 按 `ModelRunStore`、`BenchmarkRunStore` 和 `ModelCatalogStore` 责任拆分为 `memory_model_runs.go`、`memory_benchmark_runs.go` 和 `memory_model_catalog.go`；同步更新 API/compute README、architecture current-state、compute-api audit summary、Certainty/Elegance checklist 和任务计划。
- 修改原因：继续落实 Certainty/Elegance Phase 1 的 Store / repository 边界收敛和 package surface reduction；模型治理 workflow、HTTP mapping 与 PostgreSQL persistence 已经细分，MemoryStore 层仍由单个 `memory_models.go` 聚合 model_run、benchmark_run、model_catalog 三组持久化。
- 本次已消除的不确定性：确认本轮只做同 package MemoryStore persistence 文件拆分；确认 Store interfaces、PostgresStore behavior、service/HTTP/OpenAPI/contracts/generated clients、DTOs、pagination/order、idempotency/conflict、clone semantics、locking、maps 和 error mapping 不需要改变；确认完整 model governance domain package migration 或 store schema/API 变更不在本阶段执行。
- 本次采用的关键假设：当前未跟踪的 `docs/rebuild/AutoWaterSimu_95分优雅度完整计划.md` 不属于本阶段目标文件；模型治理 MemoryStore persistence 暂留 compute compatibility package，不在本阶段抽取独立 repository adapter，避免同时改变 MemoryStore wiring、store interface 和 service constructor 边界。
- 剩余不确定性：完整 model governance domain package migration、future in-memory adapter extraction、public `NewService` / `NewServiceWithArchive` 构造签名收窄、hosted integration/browser/release evidence、full object-level data scope 和 all-mutation audit 仍是后续阶段。
- 影响范围 / 风险控制方式：不改变 API 行为、持久化语义、schema 或公共签名；新增文件与原文件保持同 package、同 `MemoryStore` receiver、同锁与 clone 语义；通过 focused Go tests、full Go tests、architecture boundary audit、dependency checks 和 pr-fast 验证拆分无行为/边界回归。
- 验证方式：`go test ./internal/compute -run "Test(ValidatedCompletePersistsModelRun|ModelCatalogEndpoint|DefaultParameterSetPromotionPlanEndpoint|BenchmarkCaseScheduleRunEndpoint)$" -count=1` 通过；`go test ./...` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` 通过；`rg -n "schema_version|P0|P1|P2" docs\rebuild --glob "AutoWaterSimu_Next_*.md"` 已检查；`git diff --check -- apps\api docs scripts tasks .ai` 通过，仅有 LF/CRLF warning。
- 是否更新 README：是，更新 `apps/api/README.md` 与 `apps/api/internal/compute/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本次是既有 modular monolith / same-package persistence split 路线的延续，没有新增长期架构决策。

## 22:09 - Model governance PostgreSQL persistence file split

- 用户目标：根据 Certainty/Elegance PRD 与开发计划继续完善 AutoWaterSimu Next，完成一个可验证的大阶段并适时提交推送。
- 涉及目录：`apps/api/internal/compute/`、`apps/api/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`。
- 修改内容：删除宽泛的 `postgres_models.go`，将原有模型治理 PostgreSQL metadata persistence 按 `ModelRunStore`、`BenchmarkRunStore` 和 `ModelCatalogStore` 责任拆分为 `postgres_model_runs.go`、`postgres_benchmark_runs.go` 和 `postgres_model_catalog.go`；同步更新 API/compute README、architecture current-state、compute-api audit summary、Certainty/Elegance checklist 和任务计划。
- 修改原因：继续落实 Certainty/Elegance Phase 1 的 Store / repository 边界收敛和 package surface reduction；模型治理 workflow 与 HTTP mapping 已经细分，PostgreSQL 层仍由单个 `postgres_models.go` 聚合 model_run、benchmark_run、model_catalog 三组持久化。
- 本次已消除的不确定性：确认本轮只做同 package PostgreSQL persistence 文件拆分；确认 SQL、migrations、Store interfaces、MemoryStore behavior、scan field order、list filters、pagination/order、idempotency/conflict behavior、service/HTTP/OpenAPI/contracts/generated clients、DTOs 和 error mapping 不需要改变；确认完整 model governance domain package migration 或 schema migration 不在本阶段执行。
- 本次采用的关键假设：当前未跟踪的 `docs/rebuild/AutoWaterSimu_95分优雅度完整计划.md` 不属于本阶段目标文件；模型治理 PostgreSQL persistence 暂留 compute compatibility package，不在本阶段抽取独立 repository adapter，避免同时改变 PostgresStore wiring、migration smoke、store interface 和 service constructor 边界。
- 剩余不确定性：完整 model governance domain package migration、future PostgreSQL adapter extraction、public `NewService` / `NewServiceWithArchive` 构造签名收窄、hosted integration/browser/release evidence、full object-level data scope 和 all-mutation audit 仍是后续阶段。
- 影响范围 / 风险控制方式：不改变 API 行为、持久化语义、schema、SQL 或公共签名；新增文件与原文件保持同 package、同 `PostgresStore` receiver、同 helper 语义；通过 focused Go tests、full Go tests、architecture boundary audit、dependency checks 和 pr-fast 验证拆分无行为/边界回归。
- 验证方式：`cd apps\api; go test ./internal/compute -run "Test(ValidatedCompletePersistsModelRun|ModelCatalogEndpoint|DefaultParameterSetPromotionPlanEndpoint|BenchmarkCaseScheduleRunEndpoint)$" -count=1` 通过；`cd apps\api; go test ./...` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` 通过；`rg -n "schema_version|P0|P1|P2" docs\rebuild --glob "AutoWaterSimu_Next_*.md"` 已检查；`git diff --check -- apps\api docs scripts tasks .ai` 通过，仅有 LF/CRLF warning。
- 是否更新 README：是，更新 `apps/api/README.md` 与 `apps/api/internal/compute/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本次是既有 modular monolith / same-package persistence split 路线的延续，没有新增长期架构决策。

## 21:47 - Model governance HTTP handler file split

- 用户目标：根据 Certainty/Elegance PRD 与开发计划继续完善 AutoWaterSimu Next，完成一个可验证的大阶段并适时提交推送。
- 涉及目录：`apps/api/internal/compute/`、`apps/api/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`。
- 修改内容：删除宽泛的 `http_models.go`，将原有模型治理 HTTP handler/helper 按同 package 文件拆分为 `http_model_catalog.go`、`http_model_parameters.go`、`http_model_benchmark_cases.go`、`http_benchmark_runs.go` 和 `http_model_runs.go`；同步更新 API/compute README、architecture current-state、compute-api audit summary、Certainty/Elegance checklist 和任务计划。
- 修改原因：继续落实 Certainty/Elegance Phase 1 的 HTTP handler 只做 request/response/error mapping 与 handler/package surface reduction；模型治理 service/workflow 已经按 catalog、parameter promotion、benchmark case、benchmark run、model run 拆分，HTTP 层仍由单个 `http_models.go` 聚合多条子路径，不利于后续迁移和审查。
- 本次已消除的不确定性：确认本轮只做同 package HTTP 文件拆分；确认 `http.go` route registration、HTTP paths/methods、auth scopes、request parsing、error text、audit principal propagation、service method calls、OpenAPI、contracts、migrations、generated clients、DTOs、store/service behavior 和 persistence 不需要改变；确认完整 model governance HTTP/domain migration 需要未来 app/domain boundary 准备，不在本阶段执行。
- 本次采用的关键假设：当前未跟踪的 `docs/rebuild/AutoWaterSimu_95分优雅度完整计划.md` 不属于本阶段目标文件；模型治理 HTTP handler 暂留 compute compatibility package，不在本阶段迁入独立 app/domain package，避免同时改变 route wiring、response DTO、auth/scope/error mapping 和 service 构造边界。
- 剩余不确定性：完整 model governance HTTP mapping migration、future PostgreSQL adapter extraction、full model governance domain package migration、public `NewService` / `NewServiceWithArchive` 构造签名收窄、hosted integration/browser/release evidence、full object-level data scope 和 all-mutation audit 仍是后续阶段。
- 影响范围 / 风险控制方式：不改变 API 行为、持久化语义、schema 或公共签名；新增文件与原文件保持同 package、同 handler receiver、同 service 调用和同 helper 语义；通过 focused Go tests、full Go tests、architecture boundary audit、dependency checks 和 pr-fast 验证拆分无行为/边界回归。
- 验证方式：`cd apps\api; go test ./internal/compute -run "Test(ValidatedCompletePersistsModelRun|ModelCatalogEndpoint|DefaultParameterSetPromotionPlanEndpoint|BenchmarkCaseScheduleRunEndpoint)$" -count=1` 通过；`cd apps\api; go test ./...` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` 通过；`rg -n "schema_version|P0|P1|P2" docs\rebuild --glob "AutoWaterSimu_Next_*.md"` 已检查；`git diff --check -- apps\api docs scripts tasks .ai` 通过，仅有 LF/CRLF warning。
- 是否更新 README：是，更新 `apps/api/README.md` 与 `apps/api/internal/compute/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本次是既有 modular monolith / same-package handler split 路线的延续，没有新增长期架构决策。

## 21:38 - Job lifecycle workflow file split

- 用户目标：根据 Certainty/Elegance PRD 与开发计划继续完善 AutoWaterSimu Next，完成一个可验证的大阶段并适时提交推送。
- 涉及目录：`apps/api/internal/compute/`、`apps/api/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`。
- 修改内容：将 `JobLifecycleService` workflow 方法按同 package 文件拆分到 `job_lifecycle_create.go`、`job_lifecycle_read.go`、`job_lifecycle_state.go` 和 `job_lifecycle_completion.go`；`job_lifecycle.go` 仅保留 struct、constructor、validator、clock、job/model-run store dependency 与 artifact-listing callback wiring；同步更新 API/compute README、architecture current-state、compute-api audit summary、Certainty/Elegance checklist 和任务计划。
- 修改原因：继续落实 Certainty/Elegance Phase 1 的 handler/package surface reduction 与 Go API large-file cleanup；`job_lifecycle.go` 是 result explanation workflow split 后 remaining core compute workflow file-split target。
- 本次已消除的不确定性：确认本轮只做同 package workflow 文件拆分；确认 `JobLifecycleService` public 方法、HTTP routes、OpenAPI、contracts、migrations、generated clients、auth scopes、store interfaces、public `Service` delegates、job idempotency/audit events、worker stale checks、result/model-run persistence、timeout sweep 和 snapshot assembly 不需要改变；确认完整 job lifecycle domain package migration 需要未来 DTO/store adapter 与 HTTP boundary 准备，不在本阶段执行。
- 本次采用的关键假设：当前未跟踪的 `docs/rebuild/AutoWaterSimu_95分优雅度完整计划.md` 不属于本阶段目标文件；job lifecycle workflow 暂留 compute compatibility package，不在本阶段迁入 `domain/jobs`，避免同时改变 job record/store adapter、HTTP mapping、audit persistence、model-run persistence 和 result schema validation 边界。
- 剩余不确定性：完整 job lifecycle domain package migration 尚未完成；job DTO/store adapter extraction、HTTP mapping migration、public `NewService` / `NewServiceWithArchive` 构造签名收窄、full object-level data scope、all-mutation audit、hosted integration/release evidence 仍是后续阶段。
- 影响范围 / 风险控制方式：不改变 API 行为、持久化语义、schema 或公共签名；新增文件与原文件保持同 package、同 service receiver、同 helper/callback 调用；通过 focused Go tests、full Go tests、architecture boundary audit、dependency checks 和 pr-fast 验证拆分无行为/边界回归。
- 验证方式：`cd apps\api; go test ./internal/domain/jobs ./internal/domain/models ./internal/domain/evidence ./internal/compute -run "Test(CreateJobIdempotencyDuplicateAndConflict|WorkerLifecycleArtifactSucceedAndDownload|CancelRejectsLateResult|ValidatedWorkerFailPersistsTerminalResult|ValidatedCompletePersistsModelRun|TimeoutSweepAndPagination|HTTPMutationAuditEventEnvelopeForJobCreate)$" -count=1` 通过；`cd apps\api; go test ./...` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` 通过；`rg -n "schema_version|P0|P1|P2" docs\rebuild --glob "AutoWaterSimu_Next_*.md"` 已检查；`git diff --check -- apps\api docs scripts tasks .ai` 通过，仅有 LF/CRLF warning。
- 是否更新 README：是，更新 `apps/api/README.md` 与 `apps/api/internal/compute/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本次是既有 modular monolith / same-package file split 路线的延续，没有新增长期架构决策。

## 21:16 - Result explanation workflow file split

- 用户目标：根据 Certainty/Elegance PRD 与开发计划继续完善 AutoWaterSimu Next，完成一个可验证的大阶段并适时提交推送。
- 涉及目录：`apps/api/internal/compute/`、`apps/api/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`。
- 修改内容：将 `ResultExplanationService` workflow 方法按同 package 文件拆分到 `result_explanations_submit.go`、`result_explanations_read.go` 和 `result_explanations_review.go`；`result_explanations.go` 仅保留 struct、constructor、validator、clock、result/job store dependency 与 job-scoped evidence resolver callback wiring；同步更新 API/compute README、architecture current-state、compute-api audit summary、Certainty/Elegance checklist 和任务计划。
- 修改原因：继续落实 Certainty/Elegance Phase 1 的 handler/package surface reduction 与 Go API large-file cleanup；`result_explanations.go` 是 draft workflow split 后 remaining compute workflow file-split target。
- 本次已消除的不确定性：确认本轮只做同 package workflow 文件拆分；确认 `ResultExplanationService` public 方法、HTTP routes、OpenAPI、contracts、migrations、generated clients、auth scopes、store interfaces、public `Service` delegates、result explanation persistence/idempotency、review/publish 行为和 job-scoped evidence ref validation 行为不需要改变；确认完整 result explanation workflow domain package migration 需要未来 DTO/store/HTTP 边界准备，不在本阶段执行。
- 本次采用的关键假设：当前未跟踪的 `docs/rebuild/AutoWaterSimu_95分优雅度完整计划.md` 不属于本阶段目标文件；result explanation workflow 暂留 compute compatibility package，不在本阶段迁入 `domain/evidence`，避免同时改变 schema validation、job/result availability checks、evidence resolver orchestration、persistence/review/publish、HTTP mapping 和 store implementation 边界。
- 剩余不确定性：完整 result explanation workflow domain package migration 尚未完成；schema validation/job availability/evidence resolver orchestration 迁移、Memory/Postgres implementations、HTTP mapping、public `NewService` / `NewServiceWithArchive` 构造签名收窄和 broader production-security work 仍是后续阶段。
- 影响范围 / 风险控制方式：不改变 API 行为、持久化语义、schema 或公共签名；新增文件与原文件保持同 package、同 service receiver、同 helper/callback 调用；通过 focused Go tests、full Go tests、architecture boundary audit、dependency checks 和 pr-fast 验证拆分无行为/边界回归。
- 验证方式：`cd apps\api; go test ./internal/domain/evidence ./internal/compute -run "TestValidatedCompletePersistsModelRun|TestNewSystemEvidenceReferenceE2E" -count=1` 通过；`cd apps\api; go test ./...` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` 通过；`rg -n "schema_version|P0|P1|P2" docs\rebuild --glob "AutoWaterSimu_Next_*.md"` 已检查；`git diff --check -- apps\api docs scripts tasks .ai` 通过，仅有 LF/CRLF warning。
- 是否更新 README：是，更新 `apps/api/README.md` 与 `apps/api/internal/compute/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本次是既有 modular monolith / same-package file split 路线的延续，没有新增长期架构决策。

## 20:47 - Draft workflow file split

- 用户目标：根据 Certainty/Elegance PRD 与开发计划继续完善 AutoWaterSimu Next，完成一个可验证的大阶段并适时提交推送。
- 涉及目录：`apps/api/internal/compute/`、`apps/api/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`。
- 修改内容：将 `DraftWorkflowService` workflow 方法按同 package 文件拆分到 `draft_workflows_confirmations.go`、`draft_workflows_constraints.go` 和 `draft_workflows_promotion.go`；`draft_workflows.go` 仅保留 struct、constructor、validator、clock、store dependency 与 simulation-check callback wiring；同步更新 API/compute README、architecture current-state、compute-api audit summary、Certainty/Elegance checklist 和任务计划。
- 修改原因：继续落实 Certainty/Elegance Phase 1 的 handler/package surface reduction 与 Go API large-file cleanup；`draft_workflows.go` 是 simulation input workflow split 后 remaining compute workflow large-file target。
- 本次已消除的不确定性：确认本轮只做同 package workflow 文件拆分；确认 `DraftWorkflowService` public 方法、HTTP routes、OpenAPI、contracts、migrations、generated clients、auth scopes、store interfaces、public `Service` delegates、draft confirmation persistence/idempotency、advisory constraint plan 行为和 explicit simulation-check promotion 行为不需要改变；确认完整 agent draft workflow domain package migration 需要未来 DTO/store/HTTP 边界准备，不在本阶段执行。
- 本次采用的关键假设：当前未跟踪的 `docs/rebuild/AutoWaterSimu_95分优雅度完整计划.md` 不属于本阶段目标文件；draft workflow 暂留 compute compatibility package，不在本阶段迁入 `domain/agent`，避免同时改变 schema file lookup、JSON Schema validation、confirmation store read/write、response attachment、HTTP mapping 和 promotion orchestration 边界。
- 剩余不确定性：完整 agent draft workflow domain package migration 尚未完成；schema validation、confirmation persistence/readback、promotion orchestration、HTTP mapping、Memory/Postgres implementations、public `NewService` / `NewServiceWithArchive` 构造签名收窄和 broader production-security work 仍是后续阶段。
- 影响范围 / 风险控制方式：不改变 API 行为、持久化语义、schema 或公共签名；新增文件与原文件保持同 package、同 service receiver、同 helper/callback 调用；通过 focused Go tests、full Go tests、architecture boundary audit、dependency checks 和 pr-fast 验证拆分无行为/边界回归。
- 验证方式：`cd apps\api; go test ./internal/domain/agent ./internal/compute -run "TestContractValidationEndpoint|TestSimulationCheckEndpointCreatesComputeJob" -count=1` 通过；`cd apps\api; go test ./...` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` 通过；`rg -n "schema_version|P0|P1|P2" docs\rebuild --glob "AutoWaterSimu_Next_*.md"` 已检查；`git diff --check -- apps\api docs scripts tasks .ai` 通过，仅有 LF/CRLF warning。
- 是否更新 README：是，更新 `apps/api/README.md` 与 `apps/api/internal/compute/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本次是既有 modular monolith / same-package file split 路线的延续，没有新增长期架构决策。

## 20:31 - Simulation input workflow file split

- 用户目标：根据 Certainty/Elegance PRD 与开发计划继续完善 AutoWaterSimu Next，完成一个可验证的大阶段并适时提交推送。
- 涉及目录：`apps/api/internal/compute/`、`apps/api/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`。
- 修改内容：将 `SimulationInputService` workflow 方法按同 package 文件拆分到 `simulation_inputs_registry.go`、`simulation_inputs_process_graphs.go` 和 `simulation_inputs_resolution.go`；`simulation_inputs.go` 仅保留 struct、constructor、replay store interface 与 store wiring；同步更新 API/compute README、architecture current-state、compute-api audit summary、Certainty/Elegance checklist 和任务计划。
- 修改原因：继续落实 Certainty/Elegance Phase 1 的 handler/package surface reduction 与 Go API large-file cleanup；`simulation_inputs.go` 是 artifact lifecycle、model governance、evidence governance workflow split 后 remaining compute workflow large-file target。
- 本次已消除的不确定性：确认本轮只做同 package workflow 文件拆分；确认 `SimulationInputService` public 方法、HTTP routes、OpenAPI、contracts、migrations、generated clients、auth scopes、store interfaces、public `Service` delegates、process graph validation/projection、simulation input/process graph record mapping、idempotency 和 model-run replay 行为不需要改变；确认完整 simulation input/process graph domain package migration 需要未来 DTO/store/HTTP 边界准备，不在本阶段执行。
- 本次采用的关键假设：当前未跟踪的 `docs/rebuild/AutoWaterSimu_95分优雅度完整计划.md` 不属于本阶段目标文件；simulation input/process graph workflow 暂留 compute compatibility package，不在本阶段迁入 `domain/simulation`，避免同时改变 metadata persistence、store implementations、HTTP mapping 和 schema/store orchestration 边界。
- 剩余不确定性：完整 simulation input/process graph metadata service domain package migration 尚未完成；schema validation、input_ref resolution orchestration、metadata persistence、Memory/Postgres implementations、evidence-ref lookup、HTTP mapping、public `NewService` / `NewServiceWithArchive` 构造签名收窄和 broader production-security work 仍是后续阶段。
- 影响范围 / 风险控制方式：不改变 API 行为、持久化语义、schema 或公共签名；新增文件与原文件保持同 package、同 service receiver、同 helper/callback 调用；通过 focused Go tests、full Go tests、architecture boundary audit、dependency checks 和 pr-fast 验证拆分无行为/边界回归。
- 验证方式：`cd apps\api; go test ./internal/compute -run "TestSimulationCheckEndpointCreatesComputeJob|TestProcessGraphEvidenceReference|TestBenchmarkCaseScheduleRunEndpoint" -count=1` 通过；`cd apps\api; go test ./...` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` 通过；`rg -n "schema_version|P0|P1|P2" docs\rebuild --glob "AutoWaterSimu_Next_*.md"` 已检查；`git diff --check -- apps\api docs scripts tasks .ai` 通过，仅有 LF/CRLF warning。
- 是否更新 README：是，更新 `apps/api/README.md` 与 `apps/api/internal/compute/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本次是既有 modular monolith / same-package file split 路线的延续，没有新增长期架构决策。
