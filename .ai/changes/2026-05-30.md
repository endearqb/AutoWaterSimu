## 10:22 - README First protocol refinement

- 用户目标：先完善 README First 协作协议。
- 涉及目录：仓库根目录、`tasks/`、`.ai/changes/`。
- 修改内容：统一 `AGENTS.md`、`README_First.md`、根 `README.md` 中的协议文件分工和读取顺序；修正 `README First.md` 文件名漂移为实际存在的 `README_First.md`；补充无明确目标文件时的目标定位流程；在 `tasks/todo.md` 记录本次计划。
- 修改原因：现有根 README 已建议读取 `README_First.md`，但 `AGENTS.md` 的正式顺序没有列入该文件，且 `README_First.md` 文内使用了不存在的带空格文件名，后续 Agent 容易按不同入口执行。
- 本次已消除的不确定性：确认仓库实际存在 `AGENTS.md`、`README_First.md`、根 `README.md`、`.ai/` 和 `tasks/`；确认 README First 初始化记录已存在；确认本次无需改业务代码。
- 本次采用的关键假设：用户的“完善 readme first”指完善 README First 协作协议本身，而非为某个业务目录补 README；因此本次只做协议文档最小更新。
- 剩余不确定性：是否还需要为更多现有业务目录补齐目录级 README，需要后续按具体目标目录继续审计。
- 影响范围 / 风险控制方式：仅修改 Markdown 文档，不改变代码、构建、公共接口、运行方式或依赖。
- 验证方式：运行 `rg` 检查核心协议文档中的 README First 文件名和读取顺序；运行 `git diff --check -- AGENTS.md README.md README_First.md tasks/todo.md .ai/changes/2026-05-30.md`。
- 是否更新 README：是，更新 `AGENTS.md`、根 `README.md` 和 `README_First.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本次是既有 README First 决策下的文档一致性修正。

## 10:26 - Directory README coverage audit

- 用户目标：继续审计并补齐更多目录级 README。
- 涉及目录：`backend/app/`、`backend/scripts/`、`frontend/src/`、`frontend/tests/`、`apps/api/`、`apps/desktop/`、`contracts/`、`docs/`、`hooks/`、`updatenote/`、`tasks/`。
- 修改内容：新增 P0/P1 目录 README，覆盖 legacy backend app/API/core/material balance/tests/scripts、legacy frontend source/components/Flow/UDM/routes/services/stores/utils/hooks/i18n/data/config/types/theme/tests、Go Compute API command/internal/migrations/openapi、Desktop Rust/React helper sub目录、contracts Python package/fixtures、Petersen/tutorial/UDM docs、template hook 和 update notes；修正根 README/README_zh 中不存在的根 `scripts/` 目录引用；在 AGENTS/frontend README 中说明当前已跟踪工作区没有 `scripts/generate-client.sh`。
- 修改原因：审计显示多个高频维护目录缺少目录级上下文，后续 Agent 在修改 route、store、Flow/UDM、compute API、Desktop runtime 或合同转换时容易只靠上级 README 推断；同时发现 README/命令说明引用了当前工作区不存在的根脚本目录。
- 本次已消除的不确定性：确认新增 README 的目标目录均存在；确认未为 `node_modules/`、`.venv/`、`dist/`、`target/`、`tmp/`、`.pytest_cache/` 等依赖、构建产物或缓存目录补 README；确认 Go Compute API 相关 `.go` 修改已在工作区存在但本轮未触碰。
- 本次采用的关键假设：本轮优先补 P0/P1 维护入口，不追求一次覆盖所有历史资料、资产和低频子目录；README 只记录长期目录契约，不记录流水账。
- 剩余不确定性：是否需要继续为 `frontend/src/components/Flow/*` 更细子目录、`frontend/public/` 静态资产、`.github/` 工作流和历史任务归档子目录补 README，可在后续按实际维护频率继续审计。
- 影响范围 / 风险控制方式：仅新增/更新 Markdown 文档，不修改业务代码、API 行为、依赖或构建配置；保留工作区中已有的非 README Go 文件改动不处理。
- 验证方式：检查目标目录均存在 `README.md`；运行 `git diff --check -- '**/README.md' tasks/todo.md .ai/changes/2026-05-30.md AGENTS.md README.md README_First.md`，仅出现既有 LF-to-CRLF normalization warning。
- 是否更新 README：是，新增多个目录 README，并更新根 README、README_zh、frontend README 与 AGENTS 中相关文档说明。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本次是既有 README First 决策下的覆盖扩展。

## 10:32 - AutoWaterSimu Next goal review and Phase 4A hardening

- 用户目标：按既有 PRD、Technical Spec、Development Plan 审查当前完成情况，制定完整开发规划，并继续完成开发目标。
- 涉及目录：`apps/api/`、`tasks/`、`.ai/changes/`。
- 修改内容：在 `tasks/todo.md` 记录 Phase 0-6 当前完成度、后续完整开发规划和本轮实现复盘；修补 Go Compute API，使 worker claim 按注册 worker 的 `capabilities` 与 `supported_contract_versions` 过滤兼容 job；修补 worker fail 路径，使其生成 schema-valid `compute_result.v1` 并稳定持久化失败状态；修补 Go contract validator 的本地 schema file URL 编译方式；新增 Go 回归测试。
- 修改原因：Phase 4A skeleton 已存在，但 claim 未执行 Spec 要求的 capability / contract-version 匹配，且 fail 路径在生产 validator 开启时会构造不符合 `compute_result.v1` 的结果，可能阻断 worker 失败落库。
- 本次已消除的不确定性：确认 Phase 0、Phase 1、Phase 2、Phase 3C、Phase 4A 已有代码与验证记录；确认 Phase 4B Web UI、Phase 5 ProcessGraph runtime integration / ASM/UDM worker migration、Phase 6 governance 仍未开始；确认 Go API schema validator 生产路径此前未被测试覆盖。
- 本次采用的关键假设：用户要求基于已写 PRD/Spec/Development Plan 继续推进，因此把三份文档视为当前批准输入；本轮优先处理可测的 Phase 4A P0 硬化缺口，再进入 Web UI P0B；现有未提交 README First 文档变更应保留。
- 剩余不确定性：PostgreSQL integration smoke 仍依赖 `COMPUTE_API_DATABASE_URL`；Desktop packaged sidecar、NSIS installer smoke、Web jobs UI、ProcessGraph UI 接入、ASM/UDM worker handler、evidence/model governance 仍待后续阶段实现。
- 影响范围 / 风险控制方式：仅修改 Go Compute API lifecycle 与测试，不改 contracts schema、不改 legacy FastAPI/React 行为、不改 generated client；通过内存 store 与 schema validator 测试覆盖兼容 claim 和失败落库。
- 验证方式：`cd apps\api; go test ./...` 通过；`backend\.venv\Scripts\python -m pytest contracts\tests -q` 通过（`44 passed`）；`cd frontend; npx tsc --noEmit` 通过；`git diff --check -- apps\api tasks\todo.md` 通过，仅 LF-to-CRLF normalization warnings。
- 是否更新 README：否，本轮实现既有 Spec/README 已声明的 Go API worker lifecycle 约束，未改变目录职责或公共文档契约。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，沿用 `0002-go-compute-api-p0-foundation.md`。

## 10:50 - AutoWaterSimu Next Phase 4B Web Jobs UI

- 用户目标：继续按既有 PRD、Technical Spec、Development Plan 完成 AutoWaterSimu Next 开发目标，本轮推进 Web Compute Jobs UI P0B。
- 涉及目录：`frontend/`、`frontend/src/routes/`、`frontend/src/services/`、`frontend/src/client/compute/`、`frontend/src/i18n/`、`tasks/`、`.ai/changes/`。
- 修改内容：新增 `computeJobsService.ts` 封装 Go Compute API jobs/health/artifact 调用；在 `main.tsx` 独立配置 Compute API base URL/token；新增 authenticated `/compute-jobs` 页面，提供 health、status filter、jobs list、job detail、result summary、events、artifact download、cancel 和 demo material-balance job submission；新增 sidebar 入口与中英文 nav key；同步 `routeTree.gen.ts`；更新 frontend/service/route README 说明。
- 修改原因：Phase 4A 已有 Go Compute API 与 generated client，但 legacy frontend 尚未提供 Web jobs list/detail/result/events/artifact UI，Development Plan Phase 4B 仍未开始。
- 本次已消除的不确定性：确认 generated Compute API client 已隔离在 `frontend/src/client/compute`；确认 route 新增需要同步 `routeTree.gen.ts`；确认页面可在 legacy 登录布局内渲染；确认 Compute client 可与 legacy FastAPI client 并行初始化。
- 本次采用的关键假设：P0B Web UI 先面向开发/运维可视化，不引入新的业务权限模型；本地开发默认使用 Go API 内置 `dev-public-token`，生产/共享环境需用 `VITE_COMPUTE_API_TOKEN` 或 `localStorage.compute_access_token` 覆盖；demo job 使用现有 material-balance 最小合同结构。
- 剩余不确定性：未在本轮启动 PostgreSQL-backed Go Compute API，因此未完成浏览器内真实 create/list/cancel/download E2E；worker health 目前以 `/healthz`/`/readyz` 表示，尚无 worker list endpoint；Phase 5 ProcessGraph UI 接入、ASM/UDM worker migration 与 governance 仍待实现。
- 影响范围 / 风险控制方式：仅新增 legacy frontend 中的独立 Compute Jobs 页面和服务封装，不改 generated compute client、不覆盖 legacy FastAPI client、不修改 legacy 仿真页面、不改变 contracts schema；README 更新限于新运行配置和服务/route 说明。
- 验证方式：`cd frontend; npx tsc --noEmit` 通过；`cd frontend; npm run build` 通过（仅既有 Vite warnings：gray-matter eval、toaster chunking、large bundle）；`cd frontend; npx biome check src/routes/_layout/compute-jobs.tsx src/services/computeJobsService.ts src/main.tsx src/components/Common/SidebarItems.tsx src/i18n/messages/en/common.ts src/i18n/messages/zh/common.ts` 通过；`git diff --check -- frontend tasks .ai\changes\2026-05-30.md` 通过，仅 LF-to-CRLF normalization warnings；Headless Playwright 验证 `http://127.0.0.1:5173/compute-jobs` 可渲染 heading、Demo job、Refresh 和 status filter 控件；Codex in-app browser 对 localhost/127.0.0.1 导航被环境拦截。
- 是否更新 README：是，更新 `frontend/README.md`、`frontend/src/services/README.md`、`frontend/src/routes/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，沿用 `0002-go-compute-api-p0-foundation.md`，本轮未产生新的长期架构决策。

## 10:59 - AutoWaterSimu Next Phase 5A ProcessGraph UI bridge

- 用户目标：继续按既有 PRD、Technical Spec、Development Plan 完成 AutoWaterSimu Next 开发目标，本轮推进 ProcessGraph Integration 的前端最小闭环。
- 涉及目录：`frontend/src/contracts/`、`frontend/src/stores/`、`frontend/src/services/`、`frontend/src/routes/_layout/`、`tasks/`、`.ai/changes/`。
- 修改内容：将 material balance `flowStore.exportFlowData()` 输出升级为 legacy-compatible + CanvasGraph-compatible shape；保留 legacy `nodes` / `edges` / `customParameters` / `calculationParameters` 字段，同时新增 `schema_version=canvas_graph.v1`、`graph_id`、`name`、`component_schema`、`timeSegments`、`exported_at` 和 `metadata`；新增 `buildComputeJobFromFlowExport()` / `createJobFromFlowExport()`，把当前 flow 通过现有 TS transforms 组装为 `process_graph.v1`、`simulation_input.v1` 和 `compute_job.v1`；在 `/compute-jobs` 页面新增 `Current flow` 提交入口和 transform validation issues 展示；更新 contracts/services/stores README。
- 修改原因：Phase 4B 已能查看和创建 demo jobs，但 Phase 5 要求现有流程图能进入 `CanvasGraph -> ProcessGraph -> SimulationInput -> ComputeJob` 合同链路，本轮先提供 opt-in UI bridge，不替换 legacy `/calculate` 主路径。
- 本次已消除的不确定性：确认 TS transform prototype 已存在且导出；确认 material balance route 使用 `flowStore.ts`；确认 legacy 后端转换读取的是原有字段，新增 CanvasGraph 元数据不会移除 legacy 字段；确认 Compute Jobs 页面可访问同一个 Zustand flow store 状态。
- 本次采用的关键假设：Phase 5A 先只支持 material balance P0 job type `simulation.material_balance.v1`；当前 flow 提交流程作为 opt-in bridge 放在 Compute Jobs 页面，不直接改变 Flow 中央播放按钮的 legacy 行为；无节点时禁用提交，避免构造空 graph。
- 剩余不确定性：未完成真实 Go API/PostgreSQL/worker E2E current-flow submission；还未把 validation issues 放回画布 inspector；结果 overlay 回写节点/边仍未实现；ASM1/ASM1Slim/ASM3/UDM job types 仍未迁移到 worker。
- 影响范围 / 风险控制方式：不修改 generated client、不改 contracts schema、不改 legacy FastAPI 计算入口；`exportFlowData()` 只新增字段、不删除 legacy 字段；Compute Jobs 的 current-flow submission 失败时显示 transform details，不阻断 legacy 页面使用。
- 验证方式：`cd frontend; npx tsc --noEmit` 通过；`cd frontend; npm run build` 通过（仅既有 Vite warnings：gray-matter eval、toaster chunking、large bundle）；`cd frontend; npx biome check src/routes/_layout/compute-jobs.tsx src/services/computeJobsService.ts` 通过；`git diff --check -- frontend tasks .ai\changes\2026-05-30.md` 通过，仅 LF-to-CRLF normalization warnings；Headless Playwright 验证 `/compute-jobs` 渲染 `Demo job` 和 `Current flow`，且空 flow store 下 `Current flow` 禁用；更广的 Biome 覆盖 `flowStore.ts` / `materialBalanceTransforms.ts` 时会命中既有 `forEach` / format debt，本轮未做无关大文件重排。
- 是否更新 README：是，更新 `frontend/src/contracts/README.md`、`frontend/src/services/README.md`、`frontend/src/stores/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮是既有 Phase 5 ProcessGraph Integration 规划下的 opt-in UI bridge，未新增长期架构决策。

## 11:11 - AutoWaterSimu Next local Compute API smoke

- 用户目标：继续按既有 PRD、Technical Spec、Development Plan 完成 AutoWaterSimu Next 开发目标，本轮补齐 Web Compute Jobs UI 到本地 Go Compute API 的真实 create/list smoke。
- 涉及目录：`apps/api/cmd/compute-api/`、`apps/api/internal/compute/`、`apps/api/README.md`、`frontend/src/routes/_layout/`、`tasks/`、`.ai/changes/`。
- 修改内容：`cmd/compute-api` 在 `COMPUTE_API_DATABASE_URL` 未配置时启用非持久 memory metadata store，并保留 PostgreSQL/migration 路径；HTTP routes 增加 loopback-only CORS/OPTIONS；service 层把空 artifact/event 集合规范为数组；Compute Jobs 详情页容忍旧响应中的 `artifacts: null`；新增 Go regression tests；更新相关 README 与任务复盘。
- 修改原因：Phase 4B/5A UI 已能渲染，但本地没有 PostgreSQL 时无法启动 Go API 完成真实 Web create/list；同时真实 smoke 暴露出空 artifact list 以 JSON `null` 返回，偏离 OpenAPI 中 `JobSnapshot.artifacts` 的数组契约并导致前端详情崩溃。
- 本次已消除的不确定性：确认无 `COMPUTE_API_DATABASE_URL` 时 Go API 可用 memory store 启动；确认 PostgreSQL URL 存在时仍执行 migrations；确认本地 Vite origin 可通过 CORS preflight 调用 8088；确认 Demo job 可从 Web UI 创建、列表显示为 `queued`，详情显示空 artifacts 状态。
- 本次采用的关键假设：memory store 只用于本地 smoke，不作为正式 Web/Platform 持久路径；本地 CORS 只放行 loopback browser origin，不扩展到生产跨域策略；legacy FastAPI 后端不属于本轮启动范围。
- 剩余不确定性：PostgreSQL-backed Go API + worker 完整 E2E 仍需配置 `COMPUTE_API_DATABASE_URL` 后验证；legacy backend 未启动导致 `/api/v1/users/me` 在浏览器 smoke 中连接失败，但不影响 Compute API create/list 验收；current-flow 有效图提交、worker 执行结果回流、ASM/UDM worker migration 仍待后续阶段。
- 影响范围 / 风险控制方式：不改 OpenAPI schema、不改 generated compute client、不改 legacy FastAPI；memory fallback 与 CORS 行为均明确限制在本地开发场景；前端只做 nullable 防御，不扩大页面功能。
- 验证方式：`cd apps\api; go test ./...` 通过；`cd frontend; npx tsc --noEmit` 通过；`cd frontend; npm run build` 通过（仅既有 Vite warnings：gray-matter eval、toaster chunking、large bundle）；`cd frontend; npx biome check src/routes/_layout/compute-jobs.tsx src/services/computeJobsService.ts` 通过；PowerShell OPTIONS smoke 返回 204 与本地 CORS headers；Headless Playwright 验证 `/compute-jobs` 可创建并列出 queued Demo job，且详情不再因空 artifacts 崩溃。
- 是否更新 README：是，更新 `apps/api/README.md`、`apps/api/cmd/compute-api/README.md`、`apps/api/internal/compute/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮是本地开发 smoke 能力与 OpenAPI 合同一致性修复，不改变长期架构路线。

## 11:17 - AutoWaterSimu Next worker HTTP E2E bridge

- 用户目标：继续按既有 PRD、Technical Spec、Development Plan 完成 AutoWaterSimu Next 开发目标，本轮把 queued Compute job 接到已有 Python simulation worker，形成本地 register/claim/run/upload/succeed 闭环。
- 涉及目录：`services/simulation-worker/`、`services/simulation-worker/simulation_worker/`、`services/simulation-worker/tests/`、`tasks/`、`.ai/changes/`。
- 修改内容：新增 `simulation_worker/api_client.py`，实现 stdlib-only one-shot Compute API client；CLI 新增 `--run-api-once`、`--api-base-url`、`--api-token`、`--worker-id`；worker 可注册能力、claim 一个 job、复用既有 material balance runner、上传 artifact，并调用 succeed/fail；新增 fake Compute API server 测试；更新 worker README 职责与验证命令。
- 修改原因：Phase 4 Web UI 已能创建 queued job，但没有 worker HTTP 客户端时仍不能完成 Development Plan Phase 4 验收中的 “worker 可 claim 并写回 result”；已有 worker runner 和 Go worker endpoints 之间缺少最小集成层。
- 本次已消除的不确定性：确认 worker 能通过 Go API HTTP contract 处理 Web demo job；确认 artifact 可通过 multipart 上传并被 Go API 记录；确认 succeeded job 可被 Web UI 读取到 artifact；确认实现未依赖 legacy FastAPI、PostgreSQL 直连或额外 Python 依赖。
- 本次采用的关键假设：本轮只做本地/CI one-shot bridge，不实现长驻调度器、后台 heartbeat loop、重试策略或生产部署 worker manager；短耗时 material balance job 在 P0 smoke 中不需要单独 heartbeat。
- 剩余不确定性：PostgreSQL-backed worker E2E、长耗时 job heartbeat/cancel、worker retry/requeue、ASM/UDM job types、model_run records 和 evidence package 仍待后续阶段实现；legacy backend 未启动导致 Web smoke 中 `/api/v1/users/me` 连接失败，但与 Compute API worker 闭环无关。
- 影响范围 / 风险控制方式：不改 contracts schema、不改 Go API public endpoints、不引入新依赖、不改变 `--run-job` / `--stdio-jsonrpc` 既有行为；stdout 仍只输出 JSON，stderr 只输出脱敏错误摘要。
- 验证方式：`backend\.venv\Scripts\python -m pytest services\simulation-worker\tests -q` 通过（`7 passed`）；本地 `--run-api-once` 对运行中的 Go API memory store 处理 `job_web_demo_8bac722d08c5` 并返回 `status: succeeded` 与一个 time-series artifact；PowerShell API check 确认该 job 为 `succeeded`、`event_count=5`、`artifact_count=1`；Headless Playwright 确认 `/compute-jobs` 显示 succeeded job 和 artifact id。
- 是否更新 README：是，更新 `services/simulation-worker/README.md`、`services/simulation-worker/simulation_worker/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮是既有 worker/API contract 下的最小集成实现，长期长驻 worker 调度策略仍未确定。

## 11:21 - AutoWaterSimu Next worker model run audit

- 用户目标：继续按既有 PRD、Technical Spec、Development Plan 完成 AutoWaterSimu Next 开发目标，本轮补齐 material balance worker 成功结果中的 `model_run.v1` runtime audit。
- 涉及目录：`services/simulation-worker/simulation_worker/`、`services/simulation-worker/tests/`、`services/simulation-worker/README.md`、`tasks/`、`.ai/changes/`。
- 修改内容：成功的 material balance worker run 现在在 `compute_result.runtime_audit.model_runs` 输出一个 `model_run.v1`；模型记录包含 `model_key`、`model_version`、canonical `parameter_hash`、`input_hash`、质量指标和 artifact evidence refs；HTTP worker bridge 上传 artifact 后会把 `evidence_refs` 改写为 Go API 返回的 artifact id；新增 worker 测试校验 `model_run.v1`。
- 修改原因：`model_run.v1` 合同已存在，但 worker 成功结果此前返回空 `model_runs`，无法支撑后续 evidence package、model governance 与审计链路。
- 本次已消除的不确定性：确认 worker 能生成 schema-valid model run；确认 hash 字段满足 `sha256:<64hex>`；确认 artifact evidence ref 在本地 `--run-job` 和 HTTP worker bridge 两种路径下均可用；确认 Go API schema validator 接受包含 model run 的 compute result。
- 本次采用的关键假设：本轮只把 model run 放入 `compute_result.runtime_audit`，不新增 Go API 持久化查询端点或 `model_runs` 表写入；material balance P0 的 `model_key` 固定为 `material_balance`，`model_version` 固定为 `material_balance.v1`。
- 剩余不确定性：持久化 `model_runs`、按 model_run 查询、evidence package 导出、参数集生命周期和 ASM/UDM model run 内容仍待 Phase 6/Phase 5 后续工作。
- 影响范围 / 风险控制方式：不改 contracts schema、不改变 worker CLI 既有输出结构外层字段、不引入依赖；model run 添加在已有 `runtime_audit.model_runs` 数组中，保持向后兼容。
- 验证方式：`backend\.venv\Scripts\python -m pytest services\simulation-worker\tests -q` 通过（`7 passed`）；本地 `--run-api-once` 处理 `job_web_demo_fc29eff86a39` 并返回 `status: succeeded`、一个 time-series artifact 和一个 valid `model_run.v1`。
- 是否更新 README：是，更新 `services/simulation-worker/README.md`、`services/simulation-worker/simulation_worker/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮实现既有合同字段，未决定 model run 持久化策略。

## 11:28 - AutoWaterSimu Next Go API model run persistence

- 用户目标：继续按既有 PRD、Technical Spec、Development Plan 完成 AutoWaterSimu Next 开发目标，本轮把 worker 返回的 `model_run.v1` 从 transient runtime audit 推进到 Go API metadata 持久化与只读查询。
- 涉及目录：`apps/api/internal/compute/`、`apps/api/openapi/`、`frontend/src/client/compute/`、`apps/api/README.md`、`tasks/`、`.ai/changes/`。
- 修改内容：Go contract validator 增加 `model_run.v1`；`Complete` 从 `compute_result.runtime_audit.model_runs` 提取并校验 model run；memory/PostgreSQL store 持久化完整 model run JSON 并索引 id/job/model key/version；新增 `GET /api/v1/model-runs/{model_run_id}`；job result response 增加 `model_runs`；Compute Jobs 详情页新增 `Model runs` JSON 面板；更新 OpenAPI 并重新生成 isolated compute client；新增 Go regression test。
- 修改原因：worker 已能产生 valid `model_run.v1`，但 Go API 只保存 job summary/result hash/artifacts，无法支撑 PRD/Spec 中后续 evidence package 和 model governance 的 read path。
- 本次已消除的不确定性：确认现有 `model_runs` migration 表可支持最小持久化，不需要新增 migration；确认 P0 可用 `job:read` 暂代只读 model run 查询权限；确认 generated compute client 可包含 `getModelRun` 且不影响前端 typecheck；确认本地 memory-store smoke 可写入、读取并在 job result/Web detail 中展示 model run。
- 本次采用的关键假设：本轮只做按 id 只读查询，不做 model_run list/search、evidence package、retention、`evidence:read` scope 或 frontend 展示；PostgreSQL `runtime_audit` 字段暂存完整 `model_run.v1` JSON。
- 剩余不确定性：model run 查询过滤、evidence package 导出、NewSystem/milp evidence read、参数集状态机、retention policy 和更完整的治理 UI 仍待 Phase 6 设计与实现。
- 影响范围 / 风险控制方式：不改合同 schema、不新增 migration、不改 legacy FastAPI/React；API surface 变化已同步 OpenAPI 和 generated compute client；只读 endpoint 使用既有 P0 token scope，避免新增未完整实现的 auth 模型。
- 验证方式：`cd apps\api; go test ./...` 通过；`cd frontend; npm run generate-compute-client` 完成；`cd frontend; npx tsc --noEmit` 通过；`cd frontend; npx biome check src/routes/_layout/compute-jobs.tsx src/services/computeJobsService.ts` 通过；`cd frontend; npm run build` 通过（仅既有 Vite warnings：gray-matter eval、toaster chunking、large bundle）；`backend\.venv\Scripts\python -m pytest services\simulation-worker\tests -q` 通过（`7 passed`）；本地 memory-store smoke 创建 `job_material_balance_minimal`，运行 `--run-api-once` 后通过 `/api/v1/model-runs/mr_job_material_balance_minimal_material_balance` 读取到 `model_key=material_balance` 与 artifact evidence ref，并确认 `/api/v1/compute/jobs/{job_id}/result` 返回一个 model run；Headless Playwright 确认 Compute Jobs 详情页展示 `Model runs` 与 model run id；`git diff --check -- apps/api frontend/src/client/compute services/simulation-worker tasks/todo.md .ai/changes/2026-05-30.md` 通过，仅 LF-to-CRLF warnings。
- 是否更新 README：是，更新 `apps/api/README.md`、`apps/api/internal/compute/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮沿用现有 P0 Go API foundation 和既有 migration 表，不新增长期策略。

## 11:39 - AutoWaterSimu Next evidence package export

- 用户目标：继续按既有 PRD、Technical Spec、Development Plan 完成 AutoWaterSimu Next 开发目标，本轮推进 Phase 6 governance/integration 的最小 evidence package export。
- 涉及目录：`apps/api/internal/compute/`、`apps/api/openapi/`、`frontend/src/client/compute/`、`apps/api/README.md`、`tasks/`、`.ai/changes/`。
- 修改内容：Go service 新增按 job 生成 `evidence_package.v1`；证据包引用 job metadata、events、artifact refs、model_run refs 和 runtime audit，不内联 artifact bytes；新增 `GET /api/v1/compute/jobs/{job_id}/evidence`；默认 dev public token 增加 `evidence:read` scope；响应返回 `X-Evidence-Checksum`；OpenAPI 与 isolated compute client 已同步；新增 Go regression test 覆盖 schema validation、checksum header 和 worker token scope denial。
- 修改原因：Spec/Development Plan 要求 evidence package 可导出并校验 checksum，且 NewSystem/milp/Agent 后续只读集成依赖 evidence refs；前一轮已经持久化 model runs，本轮补齐最小 job-level evidence export。
- 本次已消除的不确定性：确认现有 job/events/artifacts/model_runs 足以生成 schema-valid `evidence_package.v1`；确认未完成 job 因缺少 result hash 不能导出 evidence；确认 `evidence:read` scope 可以在 P0 静态 token 模型中生效；确认 worker token 无 evidence 读取权限。
- 本次采用的关键假设：P0 evidence export 以 job id 为入口，不做 list/search、project/tenant/model_run 过滤或持久化 evidence package；checksum 使用导出 JSON 的 SHA-256 响应头；artifact 大文件仍通过 artifact endpoint 单独下载。
- 剩余不确定性：Evidence retention、evidence package 存储、按 project/tenant/model_run 导出、NewSystem/milp 专用 API、service token rotate/revoke、Agent DSL validation 和用户确认 gate 仍待后续 Phase 6 实现。
- 影响范围 / 风险控制方式：不改合同 schema、不改 legacy FastAPI/React、不新增 migration；只增加只读 API 和 generated compute client；证据包只包含引用和审计摘要，避免把大时间序列写入主响应。
- 验证方式：`cd apps\api; go test ./...` 通过；`cd frontend; npm run generate-compute-client` 完成；`cd frontend; npx tsc --noEmit` 通过；`backend\.venv\Scripts\python -m pytest services\simulation-worker\tests -q` 通过（`7 passed`）；本地 memory-store smoke 创建并完成 `job_material_balance_minimal`，导出 `evidence_job_material_balance_minimal`，返回 model run/artifact refs 和 `X-Evidence-Checksum`，并确认 `dev-worker-token` 访问 evidence 返回 HTTP 403。
- 是否更新 README：是，更新 `apps/api/README.md`、`apps/api/internal/compute/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮是既有 Phase 6 evidence 合同下的最小只读导出，没有确定长期 evidence 存储/retention 策略。

## 11:48 - AutoWaterSimu Next Desktop model_run persistence

- 用户目标：继续按既有 PRD、Technical Spec、Development Plan 完成 AutoWaterSimu Next 开发目标，本轮补齐 Desktop 本地审计链中的 model_run 持久化与展示。
- 涉及目录：`apps/desktop/`、`apps/desktop/src-tauri/`、`apps/desktop/src/`、`tasks/`、`.ai/changes/`。
- 修改内容：Desktop Rust store 新增 `model_run.v1` 最小校验和 SQLite 持久化；成功 worker result 的 `runtime_audit.model_runs` 写入既有 `model_runs` 表；job snapshot 与 support bundle 返回 `model_runs`；Desktop React wrapper 类型和 workbench 页面新增 `Model Runs` 展示；更新 Desktop README 上下文。
- 修改原因：worker 与 Go API 已具备 model_run 审计链路，但 Desktop 虽有 `model_runs` 表却未写入或展示 worker 产出的 `model_run.v1`，不满足 PRD/Spec 中 Desktop 本地可追溯要求。
- 本次已消除的不确定性：确认 Desktop migration 已有可复用 `model_runs` 表，无需新增 migration；确认 worker source-mode JSON-RPC 已返回 `model_run.v1`；确认 job snapshot/support bundle 是当前 Desktop UI 的最小读路径；确认 support bundle 仍不包含 artifact contents。
- 本次采用的关键假设：本轮只处理成功 material balance run 的 model_run 记录；Desktop 先通过 job snapshot 暴露 model_runs，不新增独立 Tauri command 或查询 UI；校验采用 Rust 本地最小 invariants，不引入 JSON Schema 依赖。
- 剩余不确定性：完整 Desktop MVP 仍缺 project create/open/export/import、backup/restore smoke、canvas graph save/load、process graph validate、result CSV export、packaged sidecar/NSIS installer smoke 和更完整项目 UI。
- 影响范围 / 风险控制方式：复用既有 SQLite 表和 command response shape，未新增 migration、未改变 worker CLI、未改变 Go API 或 legacy FastAPI；support bundle 只增加 model_run 审计 JSON 与 artifact refs，不内联大 artifact。
- 验证方式：`cargo test --manifest-path apps\desktop\src-tauri\Cargo.toml` 通过（`12 passed`）；`cd apps\desktop; npm run typecheck` 通过；`cd apps\desktop; npm run build` 通过；Browser 检查 `http://127.0.0.1:1420/` 渲染 `Desktop Runtime`、`Model Runs` 和 browser-mode Tauri unavailable notice；`git diff --check -- apps\desktop tasks\todo.md .ai\changes\2026-05-30.md` 通过，仅 LF-to-CRLF warnings。
- 是否更新 README：是，更新 `apps/desktop/README.md`、`apps/desktop/src-tauri/README.md`、`apps/desktop/src-tauri/src/README.md`、`apps/desktop/src/lib/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮复用既有 Desktop SQLite schema 和 Phase 3/6 审计规划，未产生新的长期架构决策。

## 11:56 - AutoWaterSimu Next Desktop CSV export

- 用户目标：继续按既有 PRD、Technical Spec、Development Plan 完成 AutoWaterSimu Next 开发目标，本轮补齐 Desktop MVP 验收中的 result CSV export。
- 涉及目录：`apps/desktop/`、`apps/desktop/src-tauri/`、`apps/desktop/src/`、`tasks/`、`.ai/changes/`。
- 修改内容：新增 `artifact_export_csv` Tauri command 和 `DesktopRuntime::artifact_export_csv()`；从 `material_balance_time_series_artifact.v1` 展平 CSV columns；React wrapper/UI 增加 `Export CSV` action 与导出路径/行数显示；补 Rust regression test；更新 Desktop README 上下文。
- 修改原因：Desktop 已有 artifact JSON copy，但 Development Plan Phase 3 验收要求 result JSON / CSV 可导出；当前最小可实现路径是从 worker time-series artifact 派生 CSV，而不改变原有 JSON artifact export 语义。
- 本次已消除的不确定性：确认 worker artifact 的结构包含 `timestamps`、`node_data` 和 `edge_data`；确认 `artifact_type=material_balance.time_series` 可作为 Desktop CSV 支持边界；确认导出目录继续使用现有 `exports/<target_dir>` sandbox。
- 本次采用的关键假设：P0 CSV 只支持 material balance time-series artifact，不尝试为任意 artifact 通用转换；CSV 列采用稳定命名 `time`、`node.<node_id>.<metric>`、`edge.<edge_id>.<metric>`；现阶段仍不接入系统文件选择对话框。
- 剩余不确定性：Desktop project lifecycle、backup/restore、canvas/process graph commands、packaged sidecar/NSIS installer smoke、生产级文件对话框 allowlist 仍未完成。
- 影响范围 / 风险控制方式：新增独立 command，不改变现有 `artifact_export` JSON copy；CSV 导出仍受相同 path traversal 检查；不改 contracts schema、worker 输出、Go API 或 legacy FastAPI。
- 验证方式：`cargo test --manifest-path apps\desktop\src-tauri\Cargo.toml` 通过（`13 passed`）；`cd apps\desktop; npm run typecheck` 通过；`cd apps\desktop; npm run build` 通过；Browser 检查 `http://127.0.0.1:1420/` 渲染 `Export CSV`、`No CSV export yet.` 和 `Model Runs`；`git diff --check -- apps\desktop tasks\todo.md .ai\changes\2026-05-30.md` 通过，仅 LF-to-CRLF warnings。
- 是否更新 README：是，更新 `apps/desktop/README.md`、`apps/desktop/src-tauri/README.md`、`apps/desktop/src-tauri/src/README.md`、`apps/desktop/src/README.md`、`apps/desktop/src/lib/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮是既有 Desktop artifact export 能力的格式扩展，未新增长期架构决策。

## 12:00 - AutoWaterSimu Next Desktop queued job cancel

- 用户目标：继续按既有 PRD、Technical Spec、Development Plan 完成 AutoWaterSimu Next 开发目标，本轮补齐 Desktop Rust API 中 compute job cancel 的最小安全语义。
- 涉及目录：`apps/desktop/`、`apps/desktop/src-tauri/`、`apps/desktop/src/`、`tasks/`、`.ai/changes/`。
- 修改内容：Rust store/runtime 新增 queued job cancellation；注册 `compute_job_cancel` Tauri command；React wrapper/UI 新增 `cancelComputeJob()` 与 `Cancel Queued Job` 控制；新增 Rust regression test；更新 Desktop README 上下文。
- 修改原因：Technical Spec Desktop Rust API 列出 `compute_job_cancel`，当前 Desktop 只能 create/list/get/run；在 source-mode worker 尚未支持异步中断时，最小可靠能力是取消尚未运行的 queued job。
- 本次已消除的不确定性：确认 Desktop source-mode worker 是同步执行，不能安全宣称 running job 可被中断；确认 job 状态机可保守扩展为 `queued -> cancelled`，terminal job 继续不可重复运行或取消。
- 本次采用的关键假设：P0 cancellation 只覆盖 queued job；running-job cooperative cancellation 留给后续长驻 worker/heartbeat 设计；UI 只在 queued 状态启用取消按钮。
- 剩余不确定性：running job cancel/requeue、worker heartbeat、long-running cancel token、UI progress event 仍待后续 worker manager 阶段实现。
- 影响范围 / 风险控制方式：不改变现有 run/success/fail 路径；新增 terminal 状态只由 queued job 触发；running/terminal 状态返回明确错误；不改 contracts schema、worker CLI、Go API 或 legacy FastAPI。
- 验证方式：`cargo test --manifest-path apps\desktop\src-tauri\Cargo.toml` 通过（`14 passed`）；`cd apps\desktop; npm run typecheck` 通过；`cd apps\desktop; npm run build` 通过；Browser 检查 `http://127.0.0.1:1420/` 渲染 `Cancel Queued Job`、`Export CSV` 和 `Model Runs`；`git diff --check -- apps\desktop tasks\todo.md .ai\changes\2026-05-30.md` 通过，仅 LF-to-CRLF warnings。
- 是否更新 README：是，更新 `apps/desktop/README.md`、`apps/desktop/src-tauri/README.md`、`apps/desktop/src-tauri/src/README.md`、`apps/desktop/src/README.md`、`apps/desktop/src/lib/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮是既有 Desktop command list 下的保守实现，没有确定 running job cancellation 架构。

## 12:09 - AutoWaterSimu Next Web evidence download

- 用户目标：继续按既有 PRD、Technical Spec、Development Plan 完成 AutoWaterSimu Next 开发目标，本轮把 Go API evidence package export 接入 Web Compute Jobs UI。
- 涉及目录：`frontend/src/services/`、`frontend/src/routes/`、`tasks/`、`.ai/changes/`。
- 修改内容：`computeJobsService` 新增 `downloadEvidencePackage()`，使用 generated Compute API client 获取 evidence JSON 并下载；Compute Jobs job detail 新增 `Evidence` 按钮；更新 routes/services README 边界说明。
- 修改原因：Go API 已提供 `GET /api/v1/compute/jobs/{job_id}/evidence`，但 Web UI 还没有最小导出入口，Phase 6 governance 的 evidence export 缺少前端操作面。
- 本次已消除的不确定性：确认 generated client 已包含 `getComputeJobEvidence()`；确认 evidence package 类型目前是 loose object；确认 route 应通过 service helper 触发下载，不在页面内拼 payload；确认 generated client 当前不暴露 `X-Evidence-Checksum`。
- 本次采用的关键假设：前端先只下载 evidence JSON 正文；按钮在 job 有 `result_hash` 后可用；checksum 展示/校验等到 client 支持响应头或后续自定义 fetch 实现。
- 剩余不确定性：前端仍无法显示 `X-Evidence-Checksum`；evidence package list/search、project/model_run filters、retention UI 和 NewSystem/milp evidence read 仍待后续 Phase 6。
- 影响范围 / 风险控制方式：不修改 generated client、不改变 API schema、不改 legacy simulation pages；新增下载入口仅在 Compute Jobs detail 内生效。
- 验证方式：`cd frontend; npx tsc --noEmit` 通过；`cd frontend; npm run build` 通过（仅既有 Vite warnings：gray-matter eval、toaster chunking、large bundle）；`cd frontend; npx biome check src\routes\_layout\compute-jobs.tsx src\services\computeJobsService.ts` 通过；Browser 插件可达 login 但其只读 evaluate 无法写 `localStorage`，因此 fallback headless Playwright seeded local auth tokens、创建 demo job，并确认 `Compute Jobs`、`Demo job`、`Evidence` 渲染；`git diff --check -- frontend\src\services\computeJobsService.ts frontend\src\routes\_layout\compute-jobs.tsx frontend\src\routes\README.md frontend\src\services\README.md tasks\todo.md .ai\changes\2026-05-30.md` 通过，仅 LF-to-CRLF warnings。
- 是否更新 README：是，更新 `frontend/src/services/README.md`、`frontend/src/routes/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮是既有 evidence export API 的 UI 接入，没有新增架构决策。

## 12:17 - AutoWaterSimu Next Desktop backup restore smoke

- 用户目标：继续按既有 PRD、Technical Spec、Development Plan 完成 AutoWaterSimu Next 开发目标，本轮补齐 Desktop MVP 验收中的 backup/restore smoke。
- 涉及目录：`apps/desktop/`、`apps/desktop/src-tauri/`、`apps/desktop/src/`、`tasks/`、`.ai/changes/`。
- 修改内容：Desktop runtime 新增 `project_backup()` / `project_restore()`；backup 写入 runtime-local `backups/<backup_id>/manifest.json`，包含 SQLite、artifacts、support_bundles、manifest checksum 信息；restore 校验 manifest 和文件 checksum 后恢复 SQLite/artifacts/support bundles；注册 Tauri commands；React wrapper/UI 新增 Backup/Restore 控制；补 Rust round-trip test；更新 Desktop README 上下文。
- 修改原因：Development Plan Phase 3 明确要求 backup/restore smoke；当前 Desktop 已有 SQLite/artifact/support bundle 基础，但没有备份恢复入口。
- 本次已消除的不确定性：确认 source-mode worker 是 one-shot 调用，没有长驻进程需要关闭；确认 P0 可先把 backup/restore 限制在 runtime-local sandbox；确认 exports 是派生输出，不纳入备份内容；确认 restore 可以复用既有 migration runner 在恢复后补迁移。
- 本次采用的关键假设：P0 backup/restore 只覆盖本地 runtime 数据（SQLite、artifact files、support bundle files）；生产级文件对话框、recent project allowlist、用户外部项目目录和长驻 worker shutdown 留给后续 Desktop 项目管理阶段。
- 剩余不确定性：完整 project create/open/export/import、canvas graph save/load、process graph validate、packaged sidecar/NSIS installer smoke 仍未完成。
- 影响范围 / 风险控制方式：restore 只接受 `backups/` 下的相对 manifest object key；所有 manifest 文件先校验 checksum；只替换允许目录 `artifacts`、`support_bundles` 和 SQLite；不改 worker CLI、Go API 或 legacy FastAPI。
- 验证方式：`cargo test --manifest-path apps\desktop\src-tauri\Cargo.toml` 通过（`15 passed`）；`cd apps\desktop; npm run typecheck` 通过；`cd apps\desktop; npm run build` 通过；Browser 检查 `http://127.0.0.1:1420/` 渲染 `Backup`、`Restore`、`No backup yet.`、`No restore yet.`；`git diff --check -- apps\desktop tasks\todo.md .ai\changes\2026-05-30.md` 通过，仅 LF-to-CRLF warnings。
- 是否更新 README：是，更新 `apps/desktop/README.md`、`apps/desktop/src-tauri/README.md`、`apps/desktop/src-tauri/src/README.md`、`apps/desktop/src/README.md`、`apps/desktop/src/lib/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮是 Phase 3 Desktop MVP 既有要求的 runtime-local smoke，没有确定生产项目文件/外部目录策略。

## 12:34 - AutoWaterSimu Next Web evidence checksum visibility

- 用户目标：继续按既有 PRD、Technical Spec、Development Plan 完成 AutoWaterSimu Next 开发目标，本轮让 Web Compute Jobs UI 下载 evidence 后可见后端返回的 checksum。
- 涉及目录：`apps/api/`、`apps/api/internal/compute/`、`frontend/src/services/`、`frontend/src/routes/`、`tasks/`、`.ai/changes/`。
- 修改内容：Go Compute API loopback CORS 增加 `Access-Control-Expose-Headers: X-Artifact-Checksum, X-Evidence-Checksum`；CORS regression test 增加 evidence checksum header 断言；`computeJobsService.downloadEvidencePackage()` 改为复用 generated Compute API base URL/token 的 service-local `fetch`，读取 `X-Evidence-Checksum` 后下载 evidence JSON 并返回下载结果；Compute Jobs detail 显示当前 job 的 evidence 文件名和 checksum；更新 API/internal compute/services/routes README。
- 修改原因：前一轮 Web evidence 下载只能拿到 JSON 正文，浏览器因 generated client/header 暴露限制无法展示 Spec 要求的 evidence checksum。
- 本次已消除的不确定性：确认 Go API evidence endpoint 已返回 checksum；确认本地 loopback CORS 可以安全暴露 checksum header；确认 generated client 仍不暴露 response headers，因此当前需要在 service wrapper 内走自定义 fetch；确认页面能在下载后展示 `sha256:` checksum。
- 本次采用的关键假设：P0 只展示后端返回的 evidence checksum，不在前端重新计算或校验文件内容；custom fetch 只用于 evidence 下载，其他 API 调用继续使用 generated client；CORS 仍只放行 loopback browser origins。
- 剩余不确定性：generated Compute API client 何时支持响应头访问仍未确定；evidence package list/search、retention、project/model_run filters 和生产 token/跨域策略仍待后续 Phase 6；Browser 插件无法写入本地 auth storage，因此认证态页面 smoke 仍需 fallback headless Playwright。
- 影响范围 / 风险控制方式：不修改 contracts schema、不改变 evidence JSON 内容、不扩大非本地 CORS、不改 legacy FastAPI/legacy simulation pages；UI 下载状态按 selected job 过滤，避免跨 job 显示旧 checksum。
- 验证方式：`cd apps\api; go test ./...` 通过；`cd frontend; npx tsc --noEmit` 通过；`cd frontend; npx biome check src\routes\_layout\compute-jobs.tsx src\services\computeJobsService.ts` 通过；`cd frontend; npm run build` 通过（仅既有 Vite warnings：`gray-matter` eval、toaster chunking、large bundle）；PowerShell OPTIONS smoke 返回 `Access-Control-Expose-Headers: X-Artifact-Checksum, X-Evidence-Checksum`；headless Playwright seeded local auth tokens 后下载 `evidence_job_material_balance_minimal.json`，并确认页面显示 `Evidence checksum: sha256:a7dfa7...`；复跑 one-shot worker smoke 成功完成 `job_worker_retry_123434`。
- 是否更新 README：是，更新 `apps/api/README.md`、`apps/api/internal/compute/README.md`、`frontend/src/services/README.md`、`frontend/src/routes/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮是既有 evidence export 能力的客户端可见性补齐，不确定长期 evidence retention 或生产跨域策略。

## 12:45 - AutoWaterSimu Next Desktop graph commands

- 用户目标：继续按既有 PRD、Technical Spec、Development Plan 完成 AutoWaterSimu Next 开发目标，本轮补齐 Desktop command list 中的 CanvasGraph save/load 与 ProcessGraph validation 最小闭环。
- 涉及目录：`apps/desktop/`、`apps/desktop/src-tauri/`、`apps/desktop/src/`、`apps/desktop/src/fixtures/`、`tasks/`、`.ai/changes/`。
- 修改内容：新增 `canvas_graph_save`、`canvas_graph_load`、`process_graph_validate` Tauri commands；`DesktopStore` 使用既有 `canvas_graphs` 表保存/读取 CanvasGraph，并在写入前校验 graph id、节点、边和引用；`DesktopRuntime` 新增只读 ProcessGraph validation，返回 `process_graph_validation.v1`、结构化 errors 与 warnings；React wrapper/UI 增加 graph command 控制和状态展示；新增 Desktop graph demo fixtures；补 Rust tests；更新 Desktop README 上下文。
- 修改原因：Technical Spec Desktop Rust API 列出 `canvas_graph_save/load` 与 `process_graph_validate`，但当前 Desktop 只覆盖 job/artifact/model_run/support/backup 路径，尚缺本地流程图合同命令面。
- 本次已消除的不确定性：确认 SQLite migration 已包含 `canvas_graphs` / `process_graphs` 表，本轮无需新增 migration；确认 CanvasGraph 可先保存完整 JSON 而不进入 worker；确认 ProcessGraph validation 可先只读返回错误，不隐式创建 compute job；确认 contract examples 可作为 Desktop demo fixture 的镜像来源。
- 本次采用的关键假设：P0 Desktop graph 命令先服务本地 smoke 和后续 UI wiring，不实现完整 project file allowlist、process graph persistence UI 或 CanvasGraph -> ProcessGraph conversion；ProcessGraph validation 使用 Rust 本地最小结构检查，不引入 JSON Schema validator 依赖。
- 剩余不确定性：完整 project create/open/export/import、外部项目目录 allowlist、ProcessGraph 持久化/加载 UI、packaged sidecar、NSIS installer smoke 仍待后续；更完整的 ProcessGraph schema validation 和 UI issue mapping 仍属于 Phase 5 后续工作。
- 影响范围 / 风险控制方式：不改 contracts schema、不改 worker/job lifecycle、不改 Go API 或 legacy FastAPI；CanvasGraph 写入只进入 SQLite `canvas_graphs`；ProcessGraph validation 不写 job 状态；新增 UI 控制在 browser-only 模式仍为禁用展示。
- 验证方式：`cargo test --manifest-path apps\desktop\src-tauri\Cargo.toml` 通过（`17 passed`）；`cd apps\desktop; npm run typecheck` 通过；`cd apps\desktop; npm run build` 通过；Browser 检查 `http://127.0.0.1:1420/` 渲染 `Save Canvas Graph`、`Load Canvas Graph`、`Validate ProcessGraph`、`Canvas Graph` 和 `ProcessGraph`。
- 是否更新 README：是，更新 `apps/desktop/README.md`、`apps/desktop/src-tauri/README.md`、`apps/desktop/src-tauri/src/README.md`、`apps/desktop/src/README.md`、`apps/desktop/src/lib/README.md`、`apps/desktop/src/fixtures/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮使用既有 SQLite 表和 Spec command list，没有确定新的项目文件/ProcessGraph 持久化策略。

## 12:54 - AutoWaterSimu Next model_run list search

- 用户目标：继续按既有 PRD、Technical Spec、Development Plan 完成 AutoWaterSimu Next 开发目标，本轮补齐 Phase 6 中按模型维度查询历史 model_run 的最小 API 能力。
- 涉及目录：`apps/api/`、`apps/api/internal/compute/`、`apps/api/openapi/`、`frontend/src/client/compute/`、`tasks/`、`.ai/changes/`。
- 修改内容：新增 `ModelRunFilter` 与 `ListModelRunsResponse`；memory/PostgreSQL store 实现 `ListModelRuns()`；新增只读 `GET /api/v1/model-runs`，支持 `job_id`、`model_key`、`model_version`、`limit`、`cursor`；更新 OpenAPI source 并重新生成 isolated compute client；新增 Go regression 覆盖 service filter 和 HTTP list endpoint；更新 API/internal compute/openapi README。
- 修改原因：前一轮只支持 `GET /api/v1/model-runs/{id}`，但 PRD/Spec 的治理目标要求能按 `model_key/model_version` 查询历史 model run，支撑后续 model governance/evidence 检索。
- 本次已消除的不确定性：确认现有 `model_runs` 表字段足以支持最小 `job_id` / `model_key` / `model_version` 查询；确认无需新增 migration；确认 generated compute client 可生成 `listModelRuns` 且前端类型检查通过。
- 本次采用的关键假设：P0/P1 先复用 `job:read` scope；列表接口不引入参数集状态机、model catalog、advanced query DSL 或 UI 搜索页；cursor 复用现有 offset cursor 编码。
- 剩余不确定性：参数集状态、model catalog/version API、model_run UI 搜索、retention policy、production approved gate 和 NewSystem/milp 专用检索仍待后续 Phase 6。
- 影响范围 / 风险控制方式：只新增只读 endpoint 和 store 查询，不改变 existing get-by-id、job result、evidence export、worker complete 行为；OpenAPI 与 generated client 同步；不改 contracts schema。
- 验证方式：`cd apps\api; go test ./...` 通过；`cd frontend; npm run generate-compute-client` 完成；`cd frontend; npx tsc --noEmit` 通过；`cd frontend; npm run build` 通过（仅既有 Vite warnings）；本地 API smoke 重启 Go API 后创建并完成 `job_model_run_list_125322`，`GET /api/v1/model-runs?model_key=material_balance&model_version=material_balance.v1&limit=5` 返回 `total=1` 和 `mr_job_model_run_list_125322_material_balance`。
- 是否更新 README：是，更新 `apps/api/README.md`、`apps/api/internal/compute/README.md`、`apps/api/openapi/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮是既有 model_run/evidence governance 路线下的只读查询补齐，没有确定参数治理状态机。

## 13:02 - AutoWaterSimu Next Web model_run history

- 用户目标：继续按既有 PRD、Technical Spec、Development Plan 完成 AutoWaterSimu Next 开发目标，本轮把已实现的 model_run list/search API 接入 Web Compute Jobs 页面。
- 涉及目录：`frontend/src/services/`、`frontend/src/routes/`、`tasks/`、`.ai/changes/`。
- 修改内容：`computeJobsService` 新增 `listModelRuns()` 薄封装；Compute Jobs 页面新增 `Model run history` 面板，支持 `job_id`、`model_key`、`model_version` 只读筛选，并展示 model run id、job id、model key/version 和 parameter hash；更新 frontend services/routes README。
- 修改原因：Phase 6 验收要求能按 `model_key/model_version` 查询历史；上一轮已补齐 Go API 和 generated client，但 Web UI 还没有对应操作面。
- 本次已消除的不确定性：确认 generated Compute API client 已提供 `listModelRuns()`；确认 route 层可保持为筛选和展示编排，不需要引入 model catalog/parameter set 状态机；确认受保护页面在开发态注入 auth storage 后可渲染新面板。
- 本次采用的关键假设：本轮仅提供只读历史查询入口；默认使用既有 `job:read` API 权限；不新增独立 route、导航项、model catalog、参数集审批、retention 或 NewSystem/milp 专用检索。
- 剩余不确定性：model catalog/version endpoints、parameter set lifecycle、production approved gate、retention policy、NewSystem/milp-specific evidence/model_run 查询和治理 UI 仍待后续 Phase 6。
- 影响范围 / 风险控制方式：不修改 generated client、不改 API schema、不影响 legacy simulation pages；route 只调用 service wrapper，service 只传递后端已支持的只读过滤字段。
- 验证方式：`cd frontend; npx tsc --noEmit` 通过；`cd frontend; npx biome check src\routes\_layout\compute-jobs.tsx src\services\computeJobsService.ts` 通过；`cd frontend; npm run build` 通过（仅既有 Vite warnings：`gray-matter` eval、toaster chunking、large bundle）；Browser 插件访问 `/compute-jobs` 被 auth guard 重定向到 `/login`，fallback headless Playwright seeded local auth tokens 后打开 `/compute-jobs`、填写 model filters，并确认 `Model run history`、三个 filter inputs、`Parameter hash` 和 result/empty-state 渲染。
- 是否更新 README：是，更新 `frontend/src/services/README.md`、`frontend/src/routes/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮是既有 model_run list/search API 的 UI 接入，不确定新的治理状态机或长期架构策略。

## 13:16 - AutoWaterSimu Next contract validation API

- 用户目标：继续按既有 PRD、Technical Spec、Development Plan 完成 AutoWaterSimu Next 开发目标，本轮推进 Phase 6 Agent DSL 中的只读 draft/contract validation API。
- 涉及目录：`apps/api/`、`apps/api/internal/compute/`、`apps/api/openapi/`、`frontend/src/client/compute/`、`tasks/`、`.ai/changes/`。
- 修改内容：`ContractValidator` 增加编译 `simulation_request.v1`、`agent_scenario_draft.v1`、`process_graph.v1`、`simulation_input.v1`；新增 `ContractValidationResponse` 与 `Service.ValidateContractDocument()`；新增 `POST /api/v1/contracts/validate`，使用现有 `job:create` scope，只返回 valid/errors/warnings，不创建 job；更新 OpenAPI 并重新生成 isolated compute client；新增 Go regression test。
- 修改原因：Spec 要求 `agent_scenario_draft.v1` 必须经 API 校验和用户确认后才能转为 simulation request 或 compute job；当前仓库已有 Agent/Simulation 合同 schema，但 API 还没有只读校验入口。
- 本次已消除的不确定性：确认 `contracts/` 已存在 `agent_scenario_draft.v1` 与 `simulation_request.v1` schema；确认没有 `constraint_draft.v1` schema，因此不能宣称支持；确认 generated compute client 可生成 `validateContract` 且前端 typecheck/build 通过；确认 endpoint 对 worker token 返回 scope denial。
- 本次采用的关键假设：P0/P1 使用通用 `POST /api/v1/contracts/validate` 而不是 Agent 专属 job-producing endpoint；只复用现有 `job:create` scope；schema validation 失败返回 HTTP 200 + `valid=false`，JSON 解析失败仍返回 contract error；不做用户确认 gate 或 draft-to-job promotion。
- 剩余不确定性：`constraint_draft.v1` schema、用户确认 gate、result explanation refs、Agent UI、生产相关草案转 job 规则和 NewSystem/milp 专用校验仍待后续 Phase 6。
- 影响范围 / 风险控制方式：不新增数据库表、不写 metadata、不改变 job lifecycle、worker lifecycle 或 evidence/model_run 行为；未知 future schema 显式返回 invalid，避免把未定义合同固化为项目事实。
- 验证方式：`cd apps\api; go test ./...` 通过；`cd frontend; npm run generate-compute-client` 完成；`cd frontend; npx tsc --noEmit` 通过；`cd frontend; npm run build` 通过（仅既有 Vite warnings：`gray-matter` eval、toaster chunking、large bundle）；临时本地 API smoke 在 8090 端口返回 `simulation_request.v1` 为 `valid=true`，`constraint_draft.v1` 为 `valid=false` 且错误为 `unsupported schema_version: constraint_draft.v1`。
- 是否更新 README：是，更新 `apps/api/README.md`、`apps/api/internal/compute/README.md`、`apps/api/openapi/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮只是既有合同 validator 的只读 HTTP 接入，没有确定用户确认或生产发布架构。

## 13:19 - AutoWaterSimu Next static token revocation

- 用户目标：继续按既有 PRD、Technical Spec、Development Plan 完成 AutoWaterSimu Next 开发目标，本轮补齐 Phase 6 service token revoke 的最小配置级语义。
- 涉及目录：`apps/api/internal/compute/`、`apps/api/README.md`、`tasks/`、`.ai/changes/`。
- 修改内容：`TokenRecord` 增加 `revoked` 字段；`Authenticator` 启动时拒绝重复 token 值；认证时在 scope 检查前拒绝 revoked bearer token；新增 regression test 覆盖 active、revoked 和 duplicate token config；更新 API/internal compute README。
- 修改原因：Development Plan 要求 revoked token 后续请求被拒绝；当前 P0 auth 只有静态 token + scopes，没有表达旧 token 已撤销的配置语义。
- 本次已消除的不确定性：确认可以在不新增数据库和动态管理 API 的前提下，先用静态 config 支持 rotate/revoke smoke；确认默认 dev token 不变；确认 revoked token 即使 scope 匹配也会被拒绝。
- 本次采用的关键假设：P0 轮换方式是“新旧 token 在配置中短暂重叠，旧 token 标记 `revoked=true` 后拒绝”；本轮不做 token CRUD endpoint、审计日志、过期时间、not-before 或生产 secret 管理。
- 剩余不确定性：动态 token rotate/revoke API、token 变更审计、生产密钥存储、token expiry/not-before 和多租户 service account 边界仍待 Phase 6/security hardening。
- 影响范围 / 风险控制方式：只修改认证配置解析和认证拒绝路径；不改变默认 token、HTTP route、OpenAPI、job/worker/evidence/model_run lifecycle；重复 token 值显式失败，避免配置顺序覆盖。
- 验证方式：`cd apps\api; gofmt -w internal\compute\auth.go internal\compute\service_test.go internal\compute\types.go; go test ./...` 通过。
- 是否更新 README：是，更新 `apps/api/README.md`、`apps/api/internal/compute/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮是 P0 静态 token 语义补齐，没有确定生产 token 管理架构。

## 13:25 - AutoWaterSimu Next Desktop project registry

- 用户目标：继续按既有 PRD、Technical Spec、Development Plan 完成 AutoWaterSimu Next 开发目标，本轮推进 Desktop MVP 的本地项目生命周期基础。
- 涉及目录：`apps/desktop/`、`apps/desktop/src-tauri/`、`apps/desktop/src/`、`tasks/`、`.ai/changes/`。
- 修改内容：`DesktopStore` 新增 SQLite `projects` create/get/list；`DesktopRuntime` 和 Tauri commands 新增 `project_create`、`project_get`、`project_list`；React wrapper 与 workbench 新增 typed project registry、`Create Project`/`Load Project` 控制和 `Projects` 面板；新增 Rust regression test；更新 Desktop README 上下文。
- 修改原因：Desktop migration 已有 `projects` 表，但 runtime/UI 仍没有项目 registry 命令；Development Plan Phase 3 Desktop MVP 后续项目 open/export/import 需要先有本地 project metadata 读写闭环。
- 本次已消除的不确定性：确认无需新增 migration；确认项目 registry 可先独立于 jobs/canvas graphs，继续保持现有 `project_id=NULL` 行为；确认 browser-only preview 能展示项目命令面但不执行 Tauri side effects。
- 本次采用的关键假设：本轮只实现本地 SQLite project create/get/list；不做外部 project file import/export、recent file allowlist、project_id wiring for jobs/graphs 或生产文件对话框。
- 剩余不确定性：项目文件格式、open/save dialogs、recent_files 安全 allowlist、jobs/graphs 归属迁移、packaged sidecar 和 NSIS installer smoke 仍待 Desktop MVP 后续。
- 影响范围 / 风险控制方式：复用既有 `projects` 表；新增独立 commands，不改变 job lifecycle、backup/restore、canvas/process graph 或 worker 行为；React 只通过 typed Tauri wrappers 调用 Rust。
- 验证方式：`cargo test --manifest-path apps\desktop\src-tauri\Cargo.toml` 通过（`18 passed`）；`cd apps\desktop; npm run typecheck` 通过；`cd apps\desktop; npm run build` 通过；Browser 检查 `http://127.0.0.1:1420/` 渲染 `Create Project`、`Load Project`、`Projects` 和 `No local projects yet.`。
- 是否更新 README：是，更新 `apps/desktop/README.md`、`apps/desktop/src-tauri/README.md`、`apps/desktop/src-tauri/src/README.md`、`apps/desktop/src/README.md`、`apps/desktop/src/lib/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮只是既有 migration 表的本地 registry smoke，没有确定外部 project file 架构。

## 13:31 - AutoWaterSimu Next Desktop project export import

- 用户目标：继续按既有 PRD、Technical Spec、Development Plan 完成 AutoWaterSimu Next 开发目标，本轮继续推进 Desktop MVP 的项目导出/导入闭环。
- 涉及目录：`apps/desktop/`、`apps/desktop/src-tauri/`、`apps/desktop/src/`、`tasks/`、`.ai/changes/`。
- 修改内容：新增 `project_export` / `project_import` runtime 与 Tauri commands；导出 `desktop_project_export.v1` JSON 到 runtime-local `exports/<target_dir>/<project_id>.autowatersimu-project.json`；导入只接受 `exports/` 下 sandbox-relative object key 并 upsert project row；React wrapper/UI 增加导出导入控制与状态块；新增 Rust regression test；更新 Desktop README。
- 修改原因：Project registry 已能 create/get/list，但 Desktop MVP 还需要项目生命周期的导出/导入 smoke；当前最小安全路径是先限制在 runtime-local sandbox。
- 本次已消除的不确定性：确认 project export/import 可复用现有 `exports/` path sandbox；确认导入可以先只恢复项目元数据，不迁移 jobs/graphs；确认 path traversal 和绝对路径会被拒绝。
- 本次采用的关键假设：本轮只导出/导入 project metadata；不接系统文件对话框、不写 recent_files、不把现有 jobs/canvas graphs 重新绑定 project_id、不设计最终 `.autowatersimu` 项目包格式。
- 剩余不确定性：外部 file dialog allowlist、recent_files tracking、jobs/graphs project_id wiring、完整项目包内容、packaged sidecar 和 NSIS installer smoke 仍待 Desktop MVP 后续。
- 影响范围 / 风险控制方式：所有文件读写都限制在 runtime-local `exports/`；新增命令不改变 job lifecycle、backup/restore、worker 或 graph validation 行为；导入前校验 export schema 和必需 project 字段。
- 验证方式：`cargo test --manifest-path apps\desktop\src-tauri\Cargo.toml` 通过（`19 passed`）；`cd apps\desktop; npm run typecheck` 通过；`cd apps\desktop; npm run build` 通过；Browser 检查 `http://127.0.0.1:1420/` 渲染 `Export Project`、`Import Project`、`Project Export` 和 `Project Import`。
- 是否更新 README：是，更新 `apps/desktop/README.md`、`apps/desktop/src-tauri/README.md`、`apps/desktop/src-tauri/src/README.md`、`apps/desktop/src/README.md`、`apps/desktop/src/lib/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮是 runtime-local smoke，不确定最终外部项目文件/allowlist 架构。

## 13:38 - AutoWaterSimu Next Desktop project_id wiring

- 用户目标：继续按既有 PRD、Technical Spec、Development Plan 完成 AutoWaterSimu Next 开发目标，本轮把 Desktop 本地项目与 jobs/canvas graphs 的可选归属关系接起来。
- 涉及目录：`apps/desktop/`、`apps/desktop/src-tauri/`、`apps/desktop/src/`、`tasks/`、`.ai/changes/`。
- 修改内容：`compute_job_create` 与 `canvas_graph_save` 支持可选 `project_id`；Rust store 在写入前校验项目存在，并把有效项目 id 写入 `compute_jobs.project_id` / `canvas_graphs.project_id`；React wrapper 接受 optional `projectId`；Desktop workbench 在创建 demo job 和保存 CanvasGraph 时传入当前选中项目，并在 job detail 展示 Project；新增 Rust regression test；更新 Desktop README。
- 修改原因：上一轮项目 registry/export/import 仍保持 jobs/canvas graphs 为 `project_id=NULL`，Desktop MVP 需要至少能把本地项目作为 job 和画布图的归属元数据，后续才能扩展完整 project package。
- 本次已消除的不确定性：确认 SQLite migrations 已包含 `compute_jobs.project_id` 和 `canvas_graphs.project_id`；确认现有无项目场景仍应保持 NULL 兼容；确认 project id 校验应放在 Rust store/runtime，而不是 React。
- 本次采用的关键假设：项目归属在本轮只是可选本地 metadata link；不新增 migration、不改变 job lifecycle、不设计外部 `.autowatersimu` 项目包格式、不自动迁移既有 NULL jobs/graphs。
- 剩余不确定性：外部 file dialog allowlist、`recent_files`、完整项目包内容、ProcessGraph persistence UI、packaged sidecar 和 NSIS installer smoke 仍待 Desktop MVP 后续。
- 影响范围 / 风险控制方式：保留不传 `project_id` 的旧调用路径；未知项目 id 明确失败，避免 SQLite 中出现 dangling references；React 只负责传递当前选中项目，实际校验和写入仍由 Rust 执行。
- 验证方式：`cargo test --manifest-path apps\desktop\src-tauri\Cargo.toml` 通过（`20 passed`）；`cd apps\desktop; npm run typecheck` 通过；`cd apps\desktop; npm run build` 通过；Browser 检查 `http://127.0.0.1:1420/` 渲染 `Project`、`Create Project`、`Create Demo Job` 和 `Save Canvas Graph`。
- 是否更新 README：是，更新 `apps/desktop/README.md`、`apps/desktop/src-tauri/README.md`、`apps/desktop/src-tauri/src/README.md`、`apps/desktop/src/README.md`、`apps/desktop/src/lib/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮只是既有 local project registry 的 optional association wiring，没有确定外部项目文件架构。

## 13:48 - AutoWaterSimu Next model catalog API

- 用户目标：继续按既有 PRD、Technical Spec、Development Plan 完成 AutoWaterSimu Next 开发目标，本轮推进 Phase 6 model governance 的最小 model catalog 能力。
- 涉及目录：`contracts/`、`apps/api/`、`apps/api/internal/compute/`、`apps/api/openapi/`、`frontend/src/client/compute/`、`tasks/`、`.ai/changes/`。
- 修改内容：新增 `model_catalog.v1` JSON Schema 与 valid/invalid examples；Go `ContractValidator` 支持校验 `model_catalog.v1`；Go Compute API 新增 built-in material_balance model catalog，并暴露 `GET /api/v1/model-catalog` 与 `GET /api/v1/model-catalog/{model_key}`；新增 regression test；更新 OpenAPI 并重新生成 isolated compute client。
- 修改原因：PRD/Spec/Development Plan 要求 Phase 6 引入 model catalog、model versions、parameter templates/sets，但仓库此前没有 `model_catalog.v1` 合同或对应只读 API；需要先建立最小可验证 wire shape，避免后续 UI/API 基于隐式结构开发。
- 本次已消除的不确定性：确认当前合同目录缺少 `model_catalog.v1`；确认 P0/P1 运行时只稳定支持 material_balance；确认最小 catalog 可先 built-in 返回，不需要新增数据库 migration；确认 generated compute client 可生成 `listModelCatalog` / `getModelCatalogModel` 并通过前端类型检查。
- 本次采用的关键假设：本轮只落地只读 built-in catalog 和默认 approved parameter set metadata；不实现 persistent model catalog tables、benchmark cases、parameter set lifecycle transition API、生产 evidence enforcement 或治理 UI。
- 剩余不确定性：ASM/UDM catalog entries、benchmark cases、参数集状态迁移、production allowed gate、Web model governance UI、NewSystem/milp 风险结果结构仍待 Phase 6 后续。
- 影响范围 / 风险控制方式：只新增合同、只读 API 和 generated client；不改变 job lifecycle、worker dispatch、model_run persistence、evidence package 内容或数据库 schema；未知模型按 `MODEL_NOT_FOUND` 返回 404。
- 验证方式：`backend\.venv\Scripts\python -m pytest contracts\tests -q` 通过（`48 passed`）；`cd apps\api; go test ./...` 通过；`cd frontend; npm run generate-compute-client` 完成；`cd frontend; npx tsc --noEmit` 通过；`cd frontend; npm run build` 通过（仅既有 Vite warnings）；临时本地 Compute API 在 8092 端口返回 `model_catalog.v1`、一个 `material_balance` entry 和 `approved` default parameter set。
- 是否更新 README：是，更新 `contracts/README.md`、`apps/api/README.md`、`apps/api/internal/compute/README.md`、`apps/api/openapi/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮没有确定持久化 catalog 或参数审批状态机，只补齐 Phase 6 最小只读合同/API。

## 13:54 - AutoWaterSimu Next artifact retention metadata

- 用户目标：继续按既有 PRD、Technical Spec、Development Plan 完成 AutoWaterSimu Next 开发目标，本轮补齐 Phase 6 lifecycle 中 artifact retention metadata 的第一阶段记录能力。
- 涉及目录：`contracts/`、`apps/api/`、`apps/api/internal/compute/`、`apps/api/migrations/`、`apps/api/openapi/`、`frontend/src/client/compute/`、`tasks/`、`.ai/changes/`。
- 修改内容：`artifact.v1` 新增可选 `retention_policy` / `retain_until`；新增 invalid retention fixture；新增 PostgreSQL `0002_artifact_retention` up/down migration；Go `ArtifactRecord`、artifact upload、PostgreSQL insert/select 支持 retention 字段，缺省为 `retain_forever`；OpenAPI 与 generated compute client 同步。
- 修改原因：Technical Spec 要求 Web object storage lifecycle 第一阶段不自动删除 artifact，但必须记录 `retention_policy` 和 `retain_until` 字段；当前 artifact 合同和 metadata 表缺少这两个字段。
- 本次已消除的不确定性：确认 artifact contract、Go DTO、PostgreSQL metadata schema 和 generated client 均未包含 retention 字段；确认可先以 optional metadata 扩展而不改变 artifact 内容存储或下载权限。
- 本次采用的关键假设：本轮只记录 lifecycle metadata；不实现自动删除、归档、引用计数检查、后台清理 worker 或管理 UI；缺省策略为 `retain_forever`，保持现有 worker artifact metadata 兼容。
- 剩余不确定性：实际 retention 执行策略、artifact 删除前 evidence/model_run 引用保护、对象存储 lifecycle、admin UI 和审计事件仍待 Phase 6 后续。
- 影响范围 / 风险控制方式：新增字段为可选合同字段且 API 缺省填充，不要求现有 worker 立即发送；新增 migration 有 down script；不改变 artifact bytes、checksum 校验、download 权限或 evidence package 内容。
- 验证方式：`backend\.venv\Scripts\python -m pytest contracts\tests -q` 通过（`49 passed`）；`cd apps\api; go test ./...` 通过；`cd frontend; npm run generate-compute-client` 完成；`cd frontend; npx tsc --noEmit` 通过；`cd frontend; npm run build` 通过（仅既有 Vite warnings）；临时本地 Compute API 在 8093 端口接受带 `retention_policy=ttl` 的 artifact 上传并返回 `retain_until=2026-06-30T00:00:00Z`。
- 是否更新 README：是，更新 `contracts/README.md`、`apps/api/README.md`、`apps/api/internal/compute/README.md`、`apps/api/migrations/README.md`、`apps/api/openapi/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮只落地 Spec 已有的 metadata 字段，不确定实际 retention 执行架构。

## 14:03 - AutoWaterSimu Next Web model catalog panel

- 用户目标：继续按既有 PRD、Technical Spec、Development Plan 完成 AutoWaterSimu Next 开发目标，本轮把只读 model catalog API 接入 Web Compute Jobs 页面。
- 涉及目录：`frontend/src/services/`、`frontend/src/routes/`、`tasks/`、`.ai/changes/`。
- 修改内容：`computeJobsService` 新增 `listModelCatalog()` wrapper；Compute Jobs 页面新增 `Model catalog` 面板，展示 model key/name、version、status、默认 parameter set、parameter set status、template count 和 parameter hash；route error handling 纳入 model catalog query；更新 frontend services/routes README。
- 修改原因：上一轮已补齐 Go API model catalog 只读 endpoint 和 generated client，但 Web UI 还没有任何 catalog 可见性；Phase 6 governance 需要至少能看到当前可用模型版本和默认参数集状态。
- 本次已消除的不确定性：确认 generated Compute client 已提供 `listModelCatalog()`；确认现有 Compute Jobs 页面可承载只读治理上下文；确认 legacy auth guard 会拦截未登录浏览器访问，因此渲染 smoke 需要注入本地 storage。
- 本次采用的关键假设：本轮仅展示 built-in catalog metadata；不新增独立 route/nav、不实现 parameter set edit/approve workflow、不引入 persistent model catalog UI 或业务审批规则。
- 剩余不确定性：独立 governance 页面、catalog persistence、parameter set lifecycle transition、approval workflow、ASM/UDM catalog entries 和 production evidence enforcement 仍待 Phase 6 后续。
- 影响范围 / 风险控制方式：只修改现有 Compute Jobs route 和 service wrapper；不改 generated client、不改 API schema、不影响 job create/list/result/evidence/model_run 行为。
- 验证方式：`cd frontend; npx biome check src\routes\_layout\compute-jobs.tsx src\services\computeJobsService.ts` 通过；`cd frontend; npx tsc --noEmit` 通过；`cd frontend; npm run build` 通过（仅既有 Vite warnings）；Browser 插件访问临时 5174 `/compute-jobs` 被 auth guard 重定向到 `/login`；fallback headless Playwright seeded local auth tokens 后确认页面显示 `Compute Jobs`、`Model catalog`、`Material Balance`、`approved` 和 `Parameter hash`。
- 是否更新 README：是，更新 `frontend/src/services/README.md`、`frontend/src/routes/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮只是只读 catalog API 的 Web 可见性接入，不确定治理工作流架构。

## 14:08 - AutoWaterSimu Next Web contract validation panel

- 用户目标：继续按既有 PRD、Technical Spec、Development Plan 完成 AutoWaterSimu Next 开发目标，本轮把 Agent DSL / simulation request 的只读合同校验能力接入 Web。
- 涉及目录：`frontend/src/services/`、`frontend/src/routes/`、`tasks/`、`.ai/changes/`。
- 修改内容：`computeJobsService` 新增 `validateContractDocument()` wrapper；Compute Jobs 页面新增 `Contract validation` 面板，支持 Agent draft 与 Simulation request 示例、编辑 JSON、调用后端合同校验并展示 valid/invalid、document schema、contract schema 和 errors；更新 frontend services/routes README。
- 修改原因：Phase 6 Agent DSL 要求草案必须先经 API 校验，上一轮已提供 `POST /api/v1/contracts/validate` 和 generated client，但 Web 端还没有可见的校验入口。
- 本次已消除的不确定性：确认 generated client 已提供 `validateContract`；确认已有 valid examples 可作为样例；确认现阶段还没有 user confirmation gate，因此不能自动把 valid draft 转为 job。
- 本次采用的关键假设：本轮只做只读 validation workbench；不新增独立 route/nav、不实现 draft promotion、用户确认、生产 job 创建规则或 result explanation refs。
- 剩余不确定性：用户确认 gate、draft-to-simulation-request promotion、production-related job creation policy、Agent UI 和解释引用 evidence_refs 仍待 Phase 6 后续。
- 影响范围 / 风险控制方式：只修改现有 Compute Jobs route 和 service wrapper；不改 generated client、API schema、job create/list/result/evidence 行为；valid 结果只展示，不触发 side effect。
- 验证方式：`cd frontend; npx biome check src\routes\_layout\compute-jobs.tsx src\services\computeJobsService.ts` 通过；`cd frontend; npx tsc --noEmit` 通过；`cd frontend; npm run build` 通过（仅既有 Vite warnings）；Browser 插件访问临时 5175 `/compute-jobs` 被 auth guard 重定向到 `/login`；fallback headless Playwright seeded local auth tokens 后点击 `Validate`，确认显示 `Contract validation`、`valid`、`agent_scenario_draft.v1`、`agent_scenario_draft.v1.json` 和 `No validation errors.`。
- 是否更新 README：是，更新 `frontend/src/services/README.md`、`frontend/src/routes/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮只是已存在合同校验 API 的 Web 可见性接入，不确定 Agent confirmation 架构。

## 14:21 - AutoWaterSimu Next constraint draft contract

- 用户目标：继续按既有 PRD、Technical Spec、Development Plan 完成 AutoWaterSimu Next 开发目标，本轮补齐 Phase 6 Agent DSL 中 `constraint_draft.v1` 的最小可校验合同和 Web 验证入口。
- 涉及目录：`contracts/`、`apps/api/`、`apps/api/internal/compute/`、`frontend/src/routes/`、`frontend/src/services/`、`tasks/`、`.ai/changes/`。
- 修改内容：新增 `constraint_draft.v1` JSON Schema；新增 valid/invalid examples；Go `ContractValidator` 注册 `constraint_draft.v1` 并在 endpoint test 中覆盖 valid constraint draft 和未知 future schema；Compute Jobs Contract validation 面板新增 `Constraint draft` 示例按钮；更新相关 README 与任务复盘。
- 修改原因：Development Plan Phase 8.3 明确列出 `constraint_draft.v1`、draft validation API、user confirmation gate；此前 API/README 明确把 `constraint_draft.v1` 作为未知 schema 返回 invalid，Web 也没有 constraint draft 示例。
- 本次已消除的不确定性：确认 PRD/Spec/Development Plan 只要求草案合同和确认 gate 方向，没有给出生产约束执行语义；确认可以通过现有只读 validation endpoint 接入而不新增 job lifecycle side effect；确认 unknown future schema 仍会 invalid。
- 本次采用的关键假设：`constraint_draft.v1` 只表达 Agent/外部系统提出的约束草案，字段包括 target ref、constraints、operator/value/severity 和 `requires_confirmation`；本轮不实现 draft promotion、用户确认 gate、生产约束执行或 result explanation refs。
- 剩余不确定性：用户确认 gate、constraint draft 到 simulation request / compute job 的转换规则、生产约束执行策略、result explanation refs 和 NewSystem/milp 专用约束语义仍待 Phase 6 后续。
- 影响范围 / 风险控制方式：只新增合同、fixture、Go schema 注册和 Web 示例按钮；不新增数据库表、不改变 OpenAPI response 类型、不创建 job、不修改 worker/evidence/model_run lifecycle；未知 future schema 继续显式 invalid。
- 验证方式：`backend\.venv\Scripts\python -m pytest contracts\tests -q` 通过（`53 passed`）；`cd apps\api; go test ./...` 通过；`cd frontend; npx biome check src\routes\_layout\compute-jobs.tsx src\services\computeJobsService.ts` 通过；`cd frontend; npx tsc --noEmit` 通过；`cd frontend; npm run build` 通过（仅既有 Vite warnings）；Browser 插件访问临时 5176 `/compute-jobs` 被 auth guard 重定向到 `/login`；fallback headless Playwright seeded local auth tokens 后点击 `Constraint draft` 和 `Validate`，确认显示 `constraint_draft.v1`、`constraint_draft.v1.json` 和 `No validation errors.`。
- 是否更新 README：是，更新 `contracts/README.md`、`apps/api/README.md`、`apps/api/internal/compute/README.md`、`frontend/src/routes/README.md`、`frontend/src/services/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮只补齐 PRD/Plan 已列出的 draft 合同，不确定确认 gate 和生产约束执行架构。

## 14:28 - AutoWaterSimu Next result explanation refs contract

- 用户目标：继续按既有 PRD、Technical Spec、Development Plan 完成 AutoWaterSimu Next 开发目标，本轮补齐 Agent 结果解释必须引用 `evidence_refs` 的最小可校验合同。
- 涉及目录：`contracts/`、`apps/api/`、`apps/api/internal/compute/`、`frontend/src/routes/`、`frontend/src/services/`、`tasks/`、`.ai/changes/`。
- 修改内容：新增 `result_explanation.v1` JSON Schema，要求 top-level `evidence_refs` 和每条 statement 的 `evidence_refs`；新增 valid/invalid examples；Go `ContractValidator` 注册 `result_explanation.v1` 并在 endpoint test 中覆盖 valid result explanation；Compute Jobs Contract validation 面板新增 `Result explanation` 示例按钮；更新相关 README 与任务复盘。
- 修改原因：PRD 要求 Agent 解释结果时引用 `evidence_refs`，Development Plan Phase 8.3 验收要求“解释必须引用 evidence_refs”；此前仓库只有 model_run/evidence_package 引用字段，没有可校验的 Agent 解释输出合同。
- 本次已消除的不确定性：确认当前 authoritative PRD/Spec/Plan 未定义具体解释 schema 名称或发布流程；确认可先用只读 validation endpoint 验证解释引用证据，而不引入 Agent 生成、审批或 job lifecycle side effect。
- 本次采用的关键假设：`result_explanation.v1` 是 Phase 6 的最小结构化解释输出合同；每条可展示 statement 必须携带至少一个 evidence ref；本轮不实现 evidence ref dereference、Agent 自动解释生成、review/publish workflow 或生产审批集成。
- 剩余不确定性：最终 Agent 解释 UI、解释发布/审核流程、evidence ref dereference 详情页、NewSystem 审批引用方式和多系统 ref 命名规范仍待 Phase 6 后续。
- 影响范围 / 风险控制方式：只新增合同、fixture、Go schema 注册和 Web 示例按钮；不新增数据库表、不改变 OpenAPI response 类型、不创建或修改 job、不改变 worker/evidence/model_run lifecycle；未知 future schema 继续显式 invalid。
- 验证方式：`backend\.venv\Scripts\python -m pytest contracts\tests -q` 通过（`57 passed`）；`cd apps\api; go test ./...` 通过；`cd frontend; npx biome check src\routes\_layout\compute-jobs.tsx src\services\computeJobsService.ts` 通过；`cd frontend; npx tsc --noEmit` 通过；`cd frontend; npm run build` 通过（仅既有 Vite warnings）；Browser 插件访问临时 5177 `/compute-jobs` 被 auth guard 重定向到 `/login`；fallback headless Playwright seeded local auth tokens 后点击 `Result explanation` 和 `Validate`，确认显示 `result_explanation.v1`、`result_explanation.v1.json` 和 `No validation errors.`。
- 是否更新 README：是，更新 `contracts/README.md`、`apps/api/README.md`、`apps/api/internal/compute/README.md`、`frontend/src/routes/README.md`、`frontend/src/services/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮只确定最小解释输出合同，不确定解释发布、审批和 evidence dereference 架构。

## 14:39 - AutoWaterSimu Next draft confirmation gate

- 用户目标：继续按既有 PRD、Technical Spec、Development Plan 完成 AutoWaterSimu Next 开发目标，本轮为 Agent DSL 草案补齐 validation-only user confirmation gate。
- 涉及目录：`contracts/`、`apps/api/`、`apps/api/internal/compute/`、`apps/api/openapi/`、`frontend/src/client/compute/`、`frontend/src/routes/`、`frontend/src/services/`、`tasks/`、`.ai/changes/`。
- 修改内容：新增 `draft_confirmation.v1` JSON Schema 与 valid/invalid examples；新增 `POST /api/v1/contracts/confirm-draft`，校验确认记录和嵌入草案 schema/id/`requires_confirmation`；OpenAPI 和 generated compute client 新增 `confirmDraft`；Web Contract validation panel 新增 `Draft confirmation` 示例、`Confirm draft` dry-run action 和 warnings 展示；更新 README 与任务复盘。
- 修改原因：PRD/Spec/Development Plan 要求 Agent 草案必须经 API 校验和用户确认后才能转换为 simulation request 或 compute job，且未确认草案不能创建生产相关任务；此前只有只读 validate，没有显式 confirmation gate。
- 本次已消除的不确定性：确认当前仓库没有 draft-to-job promotion endpoint；确认可先实现不持久化、不创建 job 的 confirmation gate，以验证 wrapper 和嵌入草案，同时避免误触生产动作；确认 OpenAPI client 能生成 `confirmDraft` 并通过前端类型检查。
- 本次采用的关键假设：P0 confirmation gate 是 validation-only dry run；`draft_confirmation.v1` 记录 approve/reject/changes_requested 决策，但不代表持久审批或生产发布；嵌入草案必须显式 `requires_confirmation=true`。
- 剩余不确定性：确认记录持久化、审计事件、draft-to-simulation-request/job promotion、生产相关 job policy、审批流与 NewSystem 集成仍待 Phase 6 后续。
- 影响范围 / 风险控制方式：新增 endpoint 只返回 `ContractValidationResponse`，并在成功时返回 warning `draft confirmation validated; no compute job was created`；不新增数据库表、不写 metadata、不创建 job、不改变 worker/evidence/model_run lifecycle；未知 future schema 继续 invalid。
- 验证方式：`backend\.venv\Scripts\python -m pytest contracts\tests -q` 通过（`61 passed`）；`cd apps\api; go test ./...` 通过；`cd frontend; npm run generate-compute-client` 完成；`cd frontend; npx biome check src\routes\_layout\compute-jobs.tsx src\services\computeJobsService.ts` 通过；`cd frontend; npx tsc --noEmit` 通过；`cd frontend; npm run build` 通过（仅既有 Vite warnings）；Browser 插件访问临时 5178 `/compute-jobs` 被 auth guard 重定向到 `/login`；fallback headless Playwright seeded local auth tokens 后点击 `Draft confirmation` 和 `Confirm draft`，确认显示 `draft_confirmation.v1`、`draft_confirmation.v1.json`、`No validation errors.` 和 `draft confirmation validated; no compute job was created`。
- 是否更新 README：是，更新 `contracts/README.md`、`apps/api/README.md`、`apps/api/internal/compute/README.md`、`apps/api/openapi/README.md`、`frontend/src/routes/README.md`、`frontend/src/services/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮只实现 validation-only gate，不确定最终持久化确认和 promotion 架构。

## 14:43 - AutoWaterSimu Next risk findings in compute result

- 用户目标：继续按既有 PRD、Technical Spec、Development Plan 完成 AutoWaterSimu Next 开发目标，本轮补齐 NewSystem/milp 集成验收中的 result risk findings 最小结构。
- 涉及目录：`contracts/`、`apps/api/`、`apps/api/internal/compute/`、`tasks/`、`.ai/changes/`。
- 修改内容：`compute_result.v1` 新增可选 top-level `risk_findings`，每条 finding 要求 `risk_code`、`severity`、`title`、`description` 和 `evidence_refs`；更新 valid compute_result fixture 并新增 invalid missing risk code fixture；Go API `Complete` 在写入 summary 时把 top-level `risk_findings` 同步进去；新增 regression assertion；更新 README 与任务复盘。
- 修改原因：Development Plan Phase 8.2 要求 milp/NewSystem simulation check 结果含 risk findings，PRD 要求审批用户能判断调度建议或方案发布风险；此前 compute result 没有标准 risk findings 字段，API result summary 也不会保留 top-level 风险结论。
- 本次已消除的不确定性：确认现有 metadata store 只保存 summary/result hash/artifact/model_run refs，不应为了 P0 risk findings 存完整 result payload；确认可以把标准化 risk findings 同步进 summary，让现有 result read API 可见。
- 本次采用的关键假设：risk findings 属于 `compute_result.v1` 的可选结构；每条 finding 必须引用 evidence refs；本轮只保存/展示风险结论，不定义风险分类算法、审批规则或生产发布策略。
- 剩余不确定性：风险分类规则、NewSystem 审批 UI、evidence ref dereference、生产 release policy 和不同模型的 risk finding taxonomy 仍待 Phase 6 后续。
- 影响范围 / 风险控制方式：字段为可选；不新增数据库表；不改变 job lifecycle、worker claim、artifact/evidence/model_run 行为；只在 worker 返回 top-level `risk_findings` 时复制到 stored summary。
- 验证方式：`backend\.venv\Scripts\python -m pytest contracts\tests -q` 通过（`62 passed`）；`cd apps\api; go test ./...` 通过。
- 是否更新 README：是，更新 `contracts/README.md`、`apps/api/README.md`、`apps/api/internal/compute/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮只是 PRD/Plan 要求的最小结果结构，不确定审批和风险分类架构。

## 14:47 - AutoWaterSimu Next evidence governance summary

- 用户目标：继续按既有 PRD、Technical Spec、Development Plan 完成 AutoWaterSimu Next 开发目标，本轮补齐 evidence package 对模型版本、参数集状态和生产/审批可用性的只读说明。
- 涉及目录：`contracts/`、`apps/api/`、`apps/api/internal/compute/`、`tasks/`、`.ai/changes/`。
- 修改内容：`evidence_package.v1` 新增可选 `governance`，包含 `production_allowed` 与 model version refs；新增 invalid governance fixture；更新 valid evidence fixture；Go evidence export 基于 persisted model runs 和 built-in model catalog 生成 governance summary；新增 regression assertion；更新 README 与任务复盘。
- 修改原因：PRD 要求 evidence package 说明模型版本、参数集状态、是否允许进入生产/审批场景；Development Plan Phase 8.1 要求 only approved parameter sets can be used for production/evidence scenarios。此前 evidence package 只有 model_run_refs，没有可读治理摘要。
- 本次已消除的不确定性：确认当前 model catalog 仍是 built-in material_balance catalog；确认 P0 可先把 production readiness 作为 evidence/read metadata，而不是阻止 job 创建或实现生产审批。
- 本次采用的关键假设：`production_allowed=true` 当前要求 model version 为 `active` 且 model_run parameter_hash 匹配 built-in approved default parameter set；该判断只用于 evidence/read 集成，不代表 AutoWaterSimu 发布生产指令。
- 剩余不确定性：持久化 model catalog、非默认 parameter set lifecycle、benchmark cases、审批 UI、production enforcement 和 NewSystem 最终引用方式仍待 Phase 6 后续。
- 影响范围 / 风险控制方式：`governance` 是 optional contract field；不新增数据库表；不改变 job create/worker dispatch/artifact/model_run 行为；只在 evidence export 中生成只读摘要。
- 验证方式：`backend\.venv\Scripts\python -m pytest contracts\tests -q` 通过（`63 passed`）；`cd apps\api; go test ./...` 通过。
- 是否更新 README：是，更新 `contracts/README.md`、`apps/api/README.md`、`apps/api/internal/compute/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮只补 evidence metadata，不确定生产 enforcement 架构。

## 14:55 - AutoWaterSimu Next benchmark cases in model catalog

- 用户目标：继续按既有 PRD、Technical Spec、Development Plan 完成 AutoWaterSimu Next 开发目标，本轮补齐模型治理中 benchmark case 的最小只读能力。
- 涉及目录：`contracts/`、`apps/api/`、`apps/api/internal/compute/`、`apps/api/openapi/`、`frontend/src/client/compute/`、`frontend/src/routes/`、`frontend/src/services/`、`tasks/`、`.ai/changes/`。
- 修改内容：`model_catalog.v1` 的 model version 新增 required `benchmark_cases`；新增 built-in material balance minimal validated benchmark case；新增 invalid benchmark case fixture；Go built-in model catalog 返回 benchmark metadata；OpenAPI 与 generated compute client 增加 `ModelBenchmarkCase`；Web Model catalog 面板展示 benchmark case 数量；更新 README 与任务复盘。
- 修改原因：PRD 要求模型工程师维护 benchmark case，Development Plan Phase 8.1 明确列出 benchmark cases；此前 model catalog 只有版本、参数模板和默认参数集，没有 benchmark case 可见性。
- 本次已消除的不确定性：确认当前阶段适合把 benchmark cases 作为 model catalog 只读 metadata，而不是先实现 benchmark 执行、run history 或审批状态机；确认现有 material balance minimal fixture 可作为 P0 validated benchmark reference。
- 本次采用的关键假设：本轮只定义 benchmark case metadata 和 Web 可见性；不运行 benchmark、不存 benchmark run、不引入新数据库表、不做参数集审批流。
- 剩余不确定性：benchmark execution API、benchmark run history、persistent model catalog、非默认参数集 lifecycle、curve matching 输出和审批流集成仍待 Phase 6 后续。
- 影响范围 / 风险控制方式：只扩展 model catalog 合同和 built-in read API；不改变 job lifecycle、worker dispatch、model_run/evidence 行为；Web 只显示数量，不触发 benchmark。
- 验证方式：`backend\.venv\Scripts\python -m pytest contracts\tests -q` 通过（`64 passed`）；`cd apps\api; go test ./...` 通过；`cd frontend; npm run generate-compute-client` 完成；`cd frontend; npx biome check src\routes\_layout\compute-jobs.tsx src\services\computeJobsService.ts` 通过；`cd frontend; npx tsc --noEmit` 通过；`cd frontend; npm run build` 通过（仅既有 Vite warnings）；Browser 插件访问临时 5179 `/compute-jobs` 被 auth guard 重定向到 `/login`；fallback headless Playwright seeded local auth tokens 后确认 `Model catalog`、`Benchmarks` 和 benchmark count `1` 渲染。
- 是否更新 README：是，更新 `contracts/README.md`、`apps/api/README.md`、`apps/api/internal/compute/README.md`、`apps/api/openapi/README.md`、`frontend/src/routes/README.md`、`frontend/src/services/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮只是只读 benchmark metadata，不确定 benchmark execution/run history 架构。

## 14:58 - AutoWaterSimu Next completion audit and roadmap

- 用户目标：继续按既有 PRD、Technical Spec、Development Plan 完成 AutoWaterSimu Next 开发目标，本轮补齐“review 当前开发完成情况并制定完整开发规划”的跨会话计划交付物。
- 涉及目录：`.ai/plans/`、`tasks/`、`.ai/changes/`。
- 修改内容：新增 `.ai/plans/autowatersimu_next_completion_audit_2026-05-30.md`，按 Phase 0-6 和 release governance 汇总当前状态、证据、剩余工作、非目标与下一批实现候选；更新 `tasks/todo.md` 记录审计任务。
- 修改原因：此前进展分散在 `tasks/todo.md` 和 `.ai/changes/`，缺少一个可跨会话延续的当前完成度审计与后续路线图，容易把阶段性实现误判为整体目标完成。
- 本次已消除的不确定性：确认当前不能标记 `/goal` 完成；确认 Phase 6 Agent DSL、governance、benchmark metadata 已有较强局部证据；确认 Desktop、worker、legacy baseline、PostgreSQL integration、persistent model catalog、benchmark run history、approval integration 等仍缺当前强证据或实现。
- 本次采用的关键假设：审计文档是计划和状态记录，不替代测试、源码或 PRD/Spec/Plan；只有当前直接验证过的命令才作为 current-turn evidence，历史记录单独标注为 latest recorded evidence。
- 剩余不确定性：完整 verification matrix 尚未在本轮全部重跑；各 Phase 的最终完成度还需逐项用当前命令、runtime smoke 和代码证据证明。
- 影响范围 / 风险控制方式：只新增 `.ai/plans` 文档并追加任务记录；不修改源码、不改变公共接口；明确写明 goal remains active，避免缩小目标。
- 验证方式：计划文档为 Markdown；待运行 `git diff --check -- .ai/plans tasks .ai/changes`。
- 是否更新 README：否，`.ai/plans/README.md` 已覆盖计划文档规则，本轮未改变目录职责。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮是完成度审计和路线图，不做新的架构选择。

## 21:20 - AutoWaterSimu Next simulation check API

- 用户目标：继续按既有 PRD、Technical Spec、Development Plan 完成 AutoWaterSimu Next 开发目标，本轮补齐 NewSystem/milp Phase 6 中 “milp plan 可创建 simulation check job” 的最小 API 能力。
- 涉及目录：`contracts/`、`contracts/examples/valid/`、`apps/api/`、`apps/api/internal/compute/`、`apps/api/openapi/`、`frontend/src/client/compute/`、`tasks/`、`.ai/changes/`。
- 修改内容：新增 `POST /api/v1/simulation-checks` handler/service，将 `simulation_request.v1` 校验后转换为 `compute_job.v1` 并入队；新增 milp embedded simulation request fixture；扩展 `simulation_request.v1.input_ref` / `external_refs` / `metadata` schema 描述；更新 OpenAPI 与 generated compute client；更新 README 和任务复盘。
- 修改原因：Development Plan Phase 8.2 要求 simulation check API，验收项明确 “milp plan 可创建 simulation check job”；此前只能直接提交 `compute_job.v1` 或验证合同，缺少外部系统按 `simulation_request.v1` 创建 job 的入口。
- 本次已消除的不确定性：确认当前 Go API 仍只负责编排，不执行 Python 科学计算；确认 `simulation_request.v1` 已是权威外部请求合同；确认没有已实现的持久化 `simulation_input_id` lookup，因此本轮必须要求内嵌 `input_ref.simulation_input` 才能安全创建 job。
- 本次采用的关键假设：simulation check P0 采用 deterministic `job_simcheck_<request_id>` 和 `simcheck:<request_id>` idempotency；`external_refs` 只进入 job context/metadata，不触发生产发布或审批动作；引用式 `simulation_input_id` 当前只保留为 metadata，不解析。
- 剩余不确定性：持久化 simulation input / process graph lookup、NewSystem service integration test、milp plan payload adapter、审批 UI、生产发布 workflow 和 service-token 运行环境配置仍待后续实现。
- 影响范围 / 风险控制方式：新增独立 endpoint，不改变现有 `/api/v1/compute/jobs` 行为；只接受 schema-valid request 和 schema-valid embedded simulation input；继续使用既有 job:create scope、idempotency、payload hash 与 worker dispatch。
- 验证方式：`backend\.venv\Scripts\python -m pytest contracts\tests -q` 通过（`65 passed`）；`cd apps\api; go test ./...` 通过；`cd frontend; npm run generate-compute-client` 完成；`cd frontend; npx tsc --noEmit` 通过；`cd frontend; npm run build` 通过（仅既有 Vite warnings）；`git diff --check -- ...` 通过（仅 LF/CRLF 提示）。
- 是否更新 README：是，更新 `contracts/README.md`、`apps/api/README.md`、`apps/api/internal/compute/README.md`、`apps/api/openapi/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮沿用既有 contracts-first / API orchestration 边界，不新增长期架构决策。

## 21:35 - AutoWaterSimu Next simulation input registry

- 用户目标：继续按既有 PRD、Technical Spec、Development Plan 完成 AutoWaterSimu Next 开发目标，本轮补齐 `simulation_request.v1` 通过 `simulation_input_id` 引用已登记仿真输入来创建 simulation check job 的能力。
- 涉及目录：`apps/api/`、`apps/api/internal/compute/`、`apps/api/migrations/`、`apps/api/openapi/`、`contracts/`、`frontend/src/client/compute/`、`tasks/`、`.ai/changes/`。
- 修改内容：新增 simulation input registry 的内存与 PostgreSQL store；新增 `POST /api/v1/simulation-inputs` 和 `GET /api/v1/simulation-inputs/{simulation_input_id}`；`POST /api/v1/simulation-checks` 支持解析已登记 `input_ref.simulation_input_id`；新增 `0003_simulation_inputs` migration/down；更新 OpenAPI 与 generated compute client；更新 README 与任务复盘。
- 修改原因：上一轮 simulation check 只能接受内嵌 `simulation_input`；Technical Spec 明确 `simulation_request.v1` 可引用 `simulation_input.v1`，Development Plan Phase 8.2 要求 NewSystem/milp 可创建 simulation check job，因此需要让 reference-only request 具备可执行路径。
- 本次已消除的不确定性：确认可在 Go API metadata store 中保存 schema-valid simulation input payload 和 hash；确认 reference-only request 可以通过 registry 转成完整 `compute_job.v1` payload；确认当前仍不解析 `process_graph_id` 或 `model_run_id`。
- 本次采用的关键假设：registry 只保存输入 payload、hash 和来源 metadata，不保存结果 artifact、大 time-series、审批状态或生产发布状态；重复登记同一 `simulation_input_id` 且 payload hash 相同视为幂等，payload 不同视为冲突。
- 剩余不确定性：`process_graph_id` lookup、`model_run_id` replay/derivation、NewSystem service integration test、milp plan payload adapter、审批 UI、生产发布 workflow 和持久化 model catalog/parameter set lifecycle 仍待后续实现。
- 影响范围 / 风险控制方式：新增独立 registry endpoint 和独立 `simulation_inputs` 表；不改变既有 job lifecycle、worker dispatch、artifact/model_run/evidence 行为；通过 schema validation、payload hash 和 idempotent insert 控制引用输入风险。
- 验证方式：`backend\.venv\Scripts\python -m pytest contracts\tests -q` 通过（`65 passed`）；`cd apps\api; go test ./...` 通过；`cd frontend; npm run generate-compute-client` 完成；`cd frontend; npx tsc --noEmit` 通过；`cd frontend; npm run build` 通过（仅既有 Vite warnings）；`git diff --check -- ...` 通过（仅 LF/CRLF 提示）。
- 是否更新 README：是，更新 `contracts/README.md`、`apps/api/README.md`、`apps/api/internal/compute/README.md`、`apps/api/migrations/README.md`、`apps/api/openapi/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮是既有 contracts-first 编排边界内的 metadata registry，不引入新的长期架构选择。

## 21:49 - AutoWaterSimu Next verification refresh

- 用户目标：按 PRD、Technical Spec、Development Plan review 当前开发完成情况并持续推进完整开发目标，本轮把完成度审计中的历史证据刷新为当前直接验证。
- 涉及目录：`.ai/plans/`、`tasks/`、`.ai/changes/`。
- 修改内容：更新 `.ai/plans/autowatersimu_next_completion_audit_2026-05-30.md` 的验证快照、Phase 状态、剩余路线图和下一批实现候选；更新 `tasks/todo.md` 中 Completion Audit 的验证复盘；追加本变更记录。
- 修改原因：此前 completion audit 明确“完整 verification matrix 尚未全部重跑”；本轮已重新验证合同、Go API、worker、simulation core、Desktop、legacy backend、frontend 和 PostgreSQL migration up/down。
- 本次已消除的不确定性：确认当前合同测试、Go API、worker CLI/API smoke、Desktop Rust/React、legacy backend targeted tests、frontend type/build 和 PostgreSQL migration up/down 均可在本机通过；确认 simulation check API 与 simulation input registry 后，Phase 6.2 状态应从 partial 提升为 strong partial。
- 本次采用的关键假设：当前验证刷新不等于 release 完成；browser render refresh、packaged sidecar、NSIS installer、CI gate 和完整 release checklist 仍需要后续单独执行。
- 剩余不确定性：ASM/UDM worker 迁移、persistent model catalog、parameter set lifecycle、draft confirmation persistence、evidence dereference UI/API、artifact retention executor、Desktop installer/package smoke 和生产部署 auth/secrets review 仍未完成。
- 影响范围 / 风险控制方式：只更新计划和任务记录，不修改运行时代码；明确区分“当前直接通过的验证”和“仍缺的 release 级验证”。
- 验证方式：`backend\.venv\Scripts\python -m pytest contracts\tests -q` 通过（65 passed）；`cd apps\api; go test ./...` 通过；worker self-check/pytest/minimal run-job/API once smoke 通过；`backend\.venv\Scripts\python -m pytest simulation_core\tests -q` 通过（4 passed）；legacy backend targeted pytest 通过（17 passed，既有 warnings）；Desktop cargo/typecheck/build 通过；frontend tsc/build 通过（既有 Vite warnings）；临时 Docker PostgreSQL migration up/down smoke 通过。
- 是否更新 README：否，本轮未改变目录职责或公共接口。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮是验证刷新和路线图校准，不做新的架构选择。

## 22:00 - AutoWaterSimu Next draft confirmation persistence

- 用户目标：继续按 PRD、Technical Spec、Development Plan 完成 AutoWaterSimu Next 开发目标，本轮推进 Agent DSL user confirmation gate 的可审计持久化。
- 涉及目录：`apps/api/`、`apps/api/internal/compute/`、`apps/api/migrations/`、`apps/api/openapi/`、`contracts/`、`frontend/src/client/compute/`、`frontend/src/routes/`、`frontend/src/services/`、`tasks/`、`.ai/plans/`、`.ai/changes/`。
- 修改内容：新增 `draft_confirmations` PostgreSQL migration/up-down；Go store/service/http 支持持久化 valid `draft_confirmation.v1`、重复同 hash 幂等、不同 hash 冲突、按 `confirmation_id` 回读；`ContractValidationResponse` 增加 optional `confirmation_record`；OpenAPI 和 generated compute client 同步；Web Contract validation panel 显示持久化 confirmation metadata；更新相关 README、任务记录和 completion audit。
- 修改原因：Phase 6 Agent DSL 剩余路线图要求确认记录可持久化并形成 audit trail；此前 `confirm-draft` 只做 validation-only dry run，无法回读用户确认记录。
- 本次已消除的不确定性：确认可以在不创建 job、不执行审批、不发布生产动作的前提下持久化确认记录；确认同一 confirmation payload 可幂等提交；确认 generated client、前端展示和 PostgreSQL migration up/down 均可通过验证。
- 本次采用的关键假设：`draft_confirmation.v1` 的持久化是审计元数据，不代表生产审批完成；当前继续使用既有 `job:create` scope 写入、`job:read` scope 回读；不在本轮实现 draft-to-job promotion、审批 UI、生产策略或 explanation publish workflow。
- 剩余不确定性：确认后的 promotion 规则、生产相关 job policy、NewSystem approval UI、Agent result explanation 生成/审核/发布和 evidence dereference 仍待后续 Phase 6。
- 影响范围 / 风险控制方式：新增独立表和独立读接口；不改变 job lifecycle、worker dispatch、artifact/evidence/model_run 行为；README 明确 `confirm-draft` 仍不创建 job 或生产动作。
- 验证方式：`cd apps\api; go test ./...` 通过；`backend\.venv\Scripts\python -m pytest contracts\tests -q` 通过（65 passed）；`cd frontend; npx biome check src\routes\_layout\compute-jobs.tsx src\services\computeJobsService.ts` 通过；`cd frontend; npx tsc --noEmit` 通过；`cd frontend; npm run build` 通过（既有 Vite warnings）；临时 in-memory Compute API smoke 确认 `confirm-draft` 持久化并可回读；临时 Docker PostgreSQL migration smoke 确认 `0001`-`0004` up 和 reverse down 通过；`COMPUTE_API_MIGRATION_DOWN_SMOKE=true` opt-in Go rollback smoke 通过。
- 是否更新 README：是，更新 `contracts/README.md`、`apps/api/README.md`、`apps/api/internal/compute/README.md`、`apps/api/migrations/README.md`、`apps/api/openapi/README.md`、`frontend/src/routes/README.md`、`frontend/src/services/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮沿用既有 contracts-first 与 confirmation gate 边界，没有确定 promotion 或生产审批架构。

## 22:20 - AutoWaterSimu Next process graph registry

- 用户目标：继续按 PRD、Technical Spec、Development Plan 完成 AutoWaterSimu Next 开发目标，本轮推进 Phase 5/6.2 的 `process_graph_id` 引用提交能力。
- 涉及目录：`apps/api/`、`apps/api/internal/compute/`、`apps/api/migrations/`、`apps/api/openapi/`、`contracts/`、`contracts/examples/valid/`、`frontend/src/client/compute/`、`frontend/src/services/`、`tasks/`、`.ai/changes/`。
- 修改内容：新增 ProcessGraph registry 的内存与 PostgreSQL store；新增 `POST /api/v1/process-graphs` 和 `GET /api/v1/process-graphs/{process_graph_id}?version=1`；`POST /api/v1/simulation-checks` 支持解析已登记 `input_ref.process_graph_id` / `process_graph_version`，生成 `simulation_input.v1` 后入队；新增 `0005_process_graphs` migration/down；新增 ProcessGraph reference simulation request fixture；更新 OpenAPI、generated compute client、frontend service wrapper、README 和任务复盘。
- 修改原因：Technical Spec 明确 `simulation_request.v1` 可引用 `process_graph.v1`，Development Plan Phase 5 要求 ProcessGraph 到 SimulationInput，再到 worker payload；此前 API 只能解析内嵌或已登记 `simulation_input_id`。
- 本次已消除的不确定性：确认可在 Go API metadata store 中保存 schema-valid ProcessGraph payload 和 hash；确认 registered ProcessGraph request 可被转换为 schema-valid `simulation_input.v1` 并创建 queued `compute_job.v1`；确认 generated frontend client 与服务封装可通过类型检查。
- 本次采用的关键假设：省略 `process_graph_version` 时默认读取 version 1；ProcessGraph-to-SimulationInput 目前只支持 `simulation.material_balance.v1`；registry 只保存输入 payload metadata，不保存结果、artifact、审批或生产发布状态。
- 剩余不确定性：`model_run_id` replay/derivation、persistent model catalog、draft promotion、NewSystem service integration tests、approval UI、production release workflow 和 ASM/UDM worker migration 仍待后续实现。
- 影响范围 / 风险控制方式：新增独立 registry endpoint 和独立 `process_graphs` 表；不改变现有 job lifecycle、worker dispatch、artifact/model_run/evidence 行为；转换逻辑复用现有 material-balance 合同边界并再次校验生成的 `simulation_input.v1`。
- 验证方式：`cd apps\api; go test ./...` 通过；`backend\.venv\Scripts\python -m pytest contracts\tests -q` 通过（66 passed）；`cd frontend; npm run generate-compute-client` 完成；`cd frontend; npx tsc --noEmit` 通过；`cd frontend; npm run build` 通过（既有 Vite warnings）；临时 Docker PostgreSQL migration up/down smoke 覆盖 `0001`-`0005` 通过；`git diff --check` 通过（仅 LF/CRLF 提示）。
- 是否更新 README：是，更新 `contracts/README.md`、`apps/api/README.md`、`apps/api/internal/compute/README.md`、`apps/api/migrations/README.md`、`apps/api/openapi/README.md`、`frontend/src/services/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮是既有 ProcessGraph->SimulationInput 编排边界内的 metadata registry，不确定 model replay 或生产审批架构。

## 22:27 - AutoWaterSimu Next model run replay

- 用户目标：继续按 PRD、Technical Spec、Development Plan 完成 AutoWaterSimu Next 开发目标，本轮推进 Phase 6.2 的 `model_run_id` replay/derivation 能力。
- 涉及目录：`apps/api/`、`apps/api/internal/compute/`、`contracts/`、`contracts/examples/valid/`、`tasks/`、`.ai/plans/`、`.ai/changes/`。
- 修改内容：`POST /api/v1/simulation-checks` 支持解析已持久化的 `input_ref.model_run_id`；实现从 `model_run.v1.job_id` 找回源 job，并复用源 job `compute_job.v1.payload` 中的 `simulation_input.v1`；新增 model-run replay simulation request fixture；新增 Go missing/persisted replay regression；更新 README、任务记录和 completion audit。
- 修改原因：Technical Spec 明确 `simulation_request.v1` 可引用已登记的 `model_run.v1`；此前 completion audit 中 Phase 6.2 仍缺 `model_run_id` replay/derivation。
- 本次已消除的不确定性：确认当前已持久化的 `model_run.v1` 含 `job_id`，可通过 metadata store 找回源 job；确认保守 replay 不需要根据 `input_hash` 或 evidence refs 反推 payload；确认缺失 model_run 会返回结构化 not-found。
- 本次采用的关键假设：replay 只复用源 job 的原始 `simulation_input.v1` payload，不支持修改参数、替换 simulation_input_id、从 artifact/evidence 反推输入，且不代表生产审批。
- 剩余不确定性：Evidence/risk dereference UI/API、NewSystem service-level E2E、persistent model catalog、draft promotion、approval UI 和 production release workflow 仍待后续实现。
- 影响范围 / 风险控制方式：不新增数据库表、不改 OpenAPI schema（`simulation_request.v1.input_ref.model_run_id` 已存在）、不改变 worker/model_run/evidence 写入行为；replay 前再次校验源 payload 是 `simulation_input.v1`。
- 验证方式：`cd apps\api; go test ./...` 通过；`backend\.venv\Scripts\python -m pytest contracts\tests -q` 通过（67 passed）；`git diff --check` 通过（仅 LF/CRLF 提示）。
- 是否更新 README：是，更新 `contracts/README.md`、`apps/api/README.md`、`apps/api/internal/compute/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮采用最小 replay 语义，未确定更广泛的 derivation 或审批架构。

## 22:34 - AutoWaterSimu Next draft promotion

- 用户目标：继续按 PRD、Technical Spec、Development Plan 完成 AutoWaterSimu Next 开发目标，本轮推进 Agent DSL 中确认后 promotion 的最小安全能力。
- 涉及目录：`apps/api/`、`apps/api/internal/compute/`、`apps/api/openapi/`、`contracts/`、`contracts/examples/valid/`、`frontend/src/client/compute/`、`frontend/src/services/`、`tasks/`、`.ai/changes/`。
- 修改内容：新增 `POST /api/v1/contracts/confirmations/{confirmation_id}/promote-simulation-check`；promotion 读取已持久化 confirmation，要求 `decision=approved`、`draft_schema_version=agent_scenario_draft.v1`，并要求 `draft.proposed_request` 是完整 schema-valid `simulation_request.v1` 后复用 simulation-check 入队；新增 promotable draft confirmation fixture；更新 OpenAPI、generated compute client、frontend service wrapper、README 和任务记录。
- 修改原因：Technical Spec 要求 Agent 草案经过 API 校验和用户确认后才能转换为 `simulation_request.v1` 或 `compute_job.v1`；此前已有确认持久化，但没有显式 promotion 路径。
- 本次已消除的不确定性：确认现有旧 Agent draft 示例并不包含完整 simulation request，不能隐式补字段；确认可用独立 endpoint 保持 `confirm-draft` 不创建 job，同时让 approved + complete proposed request 进入既有 simulation-check 路径。
- 本次采用的关键假设：promotion 只支持 `agent_scenario_draft.v1`，不处理 `constraint_draft.v1`；不从 confirmation metadata、约束草案、evidence 或用户上下文推断缺失请求字段；promotion 创建的是 simulation check job，不代表生产审批。
- 剩余不确定性：Constraint draft application semantics、production approval policy、Agent explanation generation/review/publish、evidence/risk dereference UI/API、persistent model catalog 和 release workflow 仍待后续实现。
- 影响范围 / 风险控制方式：新增显式 endpoint；保留 `confirm-draft` 的 no-job 语义；复用 `CreateSimulationCheck` 的 schema validation、idempotency 和 job lifecycle；拒绝未 approved 或非 Agent draft 的 confirmation。
- 验证方式：`cd apps\api; go test ./...` 通过；`backend\.venv\Scripts\python -m pytest contracts\tests -q` 通过（68 passed）；`cd frontend; npm run generate-compute-client` 完成；`cd frontend; npx tsc --noEmit` 通过；`cd frontend; npm run build` 通过（既有 Vite warnings）；`git diff --check` 通过（仅 LF/CRLF 提示）。
- 是否更新 README：是，更新 `contracts/README.md`、`apps/api/README.md`、`apps/api/internal/compute/README.md`、`apps/api/openapi/README.md`、`frontend/src/services/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮只实现最小显式 promotion 语义，仍未确定生产审批或 constraint application 架构。

## 22:50 - AutoWaterSimu Next persistent model catalog snapshot

- 用户目标：继续按 PRD、Technical Spec、Development Plan 完成 AutoWaterSimu Next 开发目标，本轮推进 Phase 6.1 model governance 的持久化 catalog 边界，并按用户要求完成一段后先提交。
- 涉及目录：`.ai/decisions/`、`apps/api/`、`apps/api/internal/compute/`、`apps/api/migrations/`、`apps/api/openapi/`、`contracts/`、`frontend/src/client/compute/`、`frontend/src/services/`、`tasks/`、`.ai/plans/`、`.ai/changes/`。
- 修改内容：新增 ADR `0003-persistent-model-catalog-scope.md`；新增 `model_catalogs` PostgreSQL migration/up-down；Go store/service/http 支持注册 schema-valid `model_catalog.v1` snapshot、同 catalog/hash 幂等、读取最新 `default` snapshot；`GET /api/v1/model-catalog` 和 evidence governance 优先使用持久化 catalog，未登记时回退 built-in catalog；新增 `model:write` scope；更新 OpenAPI、generated compute client、frontend service wrapper、README、任务记录和 completion audit。
- 修改原因：PRD/Development Plan 要求 model catalog、parameter sets、benchmark cases、production allowed status；此前 API 只有 built-in read-only catalog，无法形成 durable model governance 输入，也让 parameter set lifecycle 缺少持久化边界。
- 本次已消除的不确定性：确认 `model_catalog.v1` 合同已经能表达版本、参数模板、默认参数集和 benchmark case；确认当前应先持久化完整 catalog snapshot，而不是提前规范化 parameter set 状态机；确认 evidence governance 可切换到 latest persisted catalog 且保留 built-in fallback。
- 本次采用的关键假设：catalog snapshot 以 `metadata.catalog_id` 作为 catalog id，缺省为 `default`；本轮只登记/读取完整快照，不提供参数集状态迁移、benchmark run history 或生产审批 enforcement；`model:write` 是 P0 静态 token scope，不代表完整 RBAC。
- 剩余不确定性：参数集 lifecycle endpoint、benchmark run contract/execution history、独立 governance UI、生产审批策略、evidence/risk dereference UI/API 和 ASM/UDM catalog 扩展仍待后续。
- 影响范围 / 风险控制方式：新增独立表和独立 POST endpoint；GET API 保持兼容且提供 built-in fallback；所有 catalog payload 先经 JSON Schema validation；evidence governance 只读生产可用性，不创建 job、不发布生产动作。
- 验证方式：`cd apps\api; go test ./...` 通过；`backend\.venv\Scripts\python -m pytest contracts\tests -q` 通过（68 passed）；`cd frontend; npm run generate-compute-client` 完成；`cd frontend; npx tsc --noEmit` 通过；`cd frontend; npm run build` 通过（既有 Vite warnings）；临时 Docker PostgreSQL migration smoke 覆盖 `0001`-`0006` up/down 通过；`git diff --check` 通过（仅 LF/CRLF 提示）。
- 是否更新 README：是，更新 `contracts/README.md`、`apps/api/README.md`、`apps/api/internal/compute/README.md`、`apps/api/migrations/README.md`、`apps/api/openapi/README.md`、`frontend/src/services/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：是，新增 `0003-persistent-model-catalog-scope.md`，明确持久化 catalog snapshot 与参数集 lifecycle/benchmark run 的边界。

## 23:05 - AutoWaterSimu Next default parameter set lifecycle

- 用户目标：继续按 PRD、Technical Spec、Development Plan 完成 AutoWaterSimu Next 开发目标，本轮在已持久化 model catalog 边界上推进最小 parameter set lifecycle。
- 涉及目录：`.ai/decisions/`、`apps/api/`、`apps/api/internal/compute/`、`apps/api/openapi/`、`contracts/`、`frontend/src/client/compute/`、`frontend/src/services/`、`tasks/`、`.ai/plans/`、`.ai/changes/`。
- 修改内容：新增 ADR `0004-parameter-set-lifecycle-minimal-scope.md`；新增 `POST /api/v1/model-catalog/{model_key}/versions/{model_version}/default-parameter-set/status`；Go service 校验 `draft -> candidate -> validated -> approved` 前向迁移和任意非 retired 到 retired，拒绝 backward、retired 后续迁移、`from_status` 不匹配和缺失 parameter set；成功迁移会更新 latest catalog 中已有 `default_parameter_set.status` 并写入新 catalog snapshot；更新 OpenAPI、generated compute client、frontend service wrapper、README、任务记录和 completion audit。
- 修改原因：PRD 要求参数集状态清晰，Development Plan Phase 8.1 要求 approved parameter set 才能用于 production/evidence 场景；此前已有 catalog snapshot 和 evidence governance，但没有任何受控状态迁移入口。
- 本次已消除的不确定性：确认最小 lifecycle 可限定在 default parameter set，不需要先实现多参数集表；确认 status 迁移可以通过生成新的 `model_catalog.v1` snapshot 保留审计性；确认 endpoint 不需要也不应该创建生产审批或 benchmark run。
- 本次采用的关键假设：状态迁移只修改已有 default parameter set；`retired` 是终态；重复请求当前状态视为 no-op；`model:write` 是 P0 静态 scope；production enforcement 和 benchmark-backed approval 仍需后续设计。
- 剩余不确定性：多参数集 lifecycle、benchmark run contract/history、独立 governance UI、生产审批策略、evidence/risk dereference UI/API、ASM/UDM catalog 扩展仍待后续。
- 影响范围 / 风险控制方式：不新增数据库表；复用 catalog snapshot persistence；所有结果仍经 `model_catalog.v1` schema validation；evidence governance 只读取状态，不创建 job 或生产动作。
- 验证方式：`cd apps\api; go test ./...` 通过；`backend\.venv\Scripts\python -m pytest contracts\tests -q` 通过（68 passed）；`cd frontend; npm run generate-compute-client` 完成；`cd frontend; npx tsc --noEmit` 通过；`cd frontend; npm run build` 通过（既有 Vite warnings）；`git diff --check` 通过（仅 LF/CRLF 提示）。
- 是否更新 README：是，更新 `contracts/README.md`、`apps/api/README.md`、`apps/api/internal/compute/README.md`、`apps/api/openapi/README.md`、`frontend/src/services/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：是，新增 `0004-parameter-set-lifecycle-minimal-scope.md`，明确最小 default parameter set lifecycle 与后续多参数集/benchmark/生产审批的边界。

## 23:15 - AutoWaterSimu Next evidence ref dereference API

- 用户目标：继续按 PRD、Technical Spec、Development Plan 完成 AutoWaterSimu Next 开发目标，本轮推进 NewSystem/milp/Agent 审批读取所需的 evidence/risk finding dereference 能力。
- 涉及目录：`apps/api/`、`apps/api/internal/compute/`、`apps/api/openapi/`、`contracts/`、`frontend/src/client/compute/`、`frontend/src/services/`、`tasks/`、`.ai/plans/`、`.ai/changes/`。
- 修改内容：新增 `EvidenceReferenceResolution` DTO 和 `GET /api/v1/compute/jobs/{job_id}/evidence-ref?ref=...`；支持在 job 边界内解析 `model_run:<id>`、`artifact:<id>`、`job:<id>` 和嵌入 payload 中的 `simulation_input:<id>`；新增 Go regression 覆盖 model_run ref 成功、缺失和 worker token 拒绝；更新 OpenAPI、generated compute client、frontend service wrapper、README、任务记录和 completion audit。
- 修改原因：`compute_result.v1.risk_findings` 和 `result_explanation.v1` 都要求 `evidence_refs`，但此前 API 只能导出整个 evidence package 或读取 model_run/artifact，审批页无法通过统一 ref 解析入口定位证据。
- 本次已消除的不确定性：确认最小 dereference 可以作为 evidence-read API，不需要改变合同 schema；确认 ref 必须受 route job_id 约束，避免跨 job 拼装证据；确认 artifact dereference 先返回 metadata，不内联 artifact bytes。
- 本次采用的关键假设：当前只支持明确的 ref 类型和 job 内嵌 simulation_input；不支持任意 DSL、跨 job ref、process graph registry dereference、artifact content inline 或审批动作。
- 剩余不确定性：审批页 UI、process graph ref 解析、result explanation 生成/审核/发布、NewSystem service-level E2E 和生产审批策略仍待后续。
- 影响范围 / 风险控制方式：新增只读 endpoint，使用 `evidence:read`；不新增数据库表；不改变 job lifecycle、worker dispatch、artifact 下载或 evidence package 内容；未解析 refs 返回结构化 404。
- 验证方式：`cd apps\api; go test ./...` 通过；`backend\.venv\Scripts\python -m pytest contracts\tests -q` 通过（68 passed）；`cd frontend; npm run generate-compute-client` 完成；`cd frontend; npx tsc --noEmit` 通过；`cd frontend; npm run build` 通过（既有 Vite warnings）；`git diff --check` 通过（仅 LF/CRLF 提示）。
- 是否更新 README：是，更新 `contracts/README.md`、`apps/api/README.md`、`apps/api/internal/compute/README.md`、`apps/api/openapi/README.md`、`frontend/src/services/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮是既有 evidence/read 集成边界下的只读 ref 解析，不新增生产审批架构。

## 23:25 - AutoWaterSimu Next constraint application plan

- 用户目标：继续按 PRD、Technical Spec、Development Plan 完成 AutoWaterSimu Next 开发目标，本轮收敛 constraint draft application semantics 和生产审批边界。
- 涉及目录：`.ai/decisions/`、`contracts/`、`contracts/examples/valid/`、`apps/api/`、`apps/api/internal/compute/`、`apps/api/openapi/`、`frontend/src/client/compute/`、`frontend/src/services/`、`tasks/`、`.ai/plans/`、`.ai/changes/`。
- 修改内容：新增 ADR `0005-constraint-draft-application-policy.md`；新增 approved `constraint_draft.v1` confirmation fixture；新增 `GET /api/v1/contracts/confirmations/{confirmation_id}/constraint-application-plan`，只对 approved constraint confirmation 返回 `constraint_application_plan.v1`，并固定 `application_mode=advisory_only`、`would_create_job=false`、`would_modify_target=false`、`production_approval_required=true`；更新 OpenAPI、generated compute client、frontend service wrapper、README、任务记录和 completion audit。
- 修改原因：completion audit 中 Phase 6.3 仍缺 constraint draft application semantics；直接把 constraint draft 转成 job、目标对象 mutation 或生产审批动作会引入未确认业务规则。
- 本次已消除的不确定性：确认已确认的 constraint draft 可以安全生成只读 application plan；确认该 plan 不创建 job、不修改 simulation request / process graph / simulation input / model run / evidence review，不代表生产审批完成；确认 wrong draft type 和 worker token 会被拒绝。
- 本次采用的关键假设：constraint application plan 当前只是 Agent/NewSystem 审批读取 metadata；实际 constraint enforcement、violation evaluation、生产发布或外部审批完成标记必须由后续独立 contract/endpoint 承载。
- 剩余不确定性：Agent result explanation 生成/审核/发布流程、approval-page UI、NewSystem service-level E2E、constraint enforcement contract、release gate/packaging smoke 仍待后续。
- 影响范围 / 风险控制方式：新增只读 endpoint，使用 `job:read`；不新增数据库表；不改变 `confirm-draft` 的 no-job 语义，不改变 `promote-simulation-check` 的 Agent-only 语义，不改变 worker/job/artifact/model_run lifecycle。
- 验证方式：`cd apps\api; go test ./...` 通过；`backend\.venv\Scripts\python -m pytest contracts\tests -q` 通过（69 passed）；`cd frontend; npm run generate-compute-client` 完成；`cd frontend; npx tsc --noEmit` 通过；`cd frontend; npm run build` 通过（既有 Vite warnings）；`git diff --check` 通过（仅 LF/CRLF 提示）。
- 是否更新 README：是，更新 `contracts/README.md`、`apps/api/README.md`、`apps/api/internal/compute/README.md`、`apps/api/openapi/README.md`、`frontend/src/services/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：是，新增 `0005-constraint-draft-application-policy.md`，明确 constraint application 只读/advisory 和生产审批外部边界。

## 23:43 - AutoWaterSimu Next result explanation workflow

- 用户目标：继续按 PRD、Technical Spec、Development Plan 完成 AutoWaterSimu Next 开发目标，本轮推进 Agent result explanation 的生成后审核/发布 workflow。
- 涉及目录：`.ai/decisions/`、`apps/api/`、`apps/api/internal/compute/`、`apps/api/migrations/`、`apps/api/openapi/`、`contracts/`、`frontend/src/client/compute/`、`frontend/src/services/`、`tasks/`、`.ai/plans/`、`.ai/changes/`。
- 修改内容：新增 ADR `0006-result-explanation-workflow-scope.md`；新增 `result_explanations` PostgreSQL metadata table migration；新增 `POST /api/v1/compute/jobs/{job_id}/result-explanations`、`GET /api/v1/compute/jobs/{job_id}/result-explanations/{explanation_id}`、`review` 和 `publish` endpoints；提交时校验 `result_explanation.v1`、路径 job_id、job result 可用性和 job-scoped evidence refs；`evidence-ref` 新增 `evidence_package:<id>` 支持；新增 `explanation:write` scope；更新 OpenAPI、generated compute client、frontend service wrapper、README、任务记录和 completion audit。
- 修改原因：PRD/Development Plan 要求 Agent 解释结果时引用 `evidence_refs`，且 Agent 不能绕过合同校验和用户确认；此前只有 schema validation，没有可审计的 review/publish 状态。
- 本次已消除的不确定性：确认当前不应实现内部 LLM 生成；确认可把 external Agent generated explanation 作为 schema-valid payload 持久化，再由显式 review/publish 改变审计状态；确认发布解释不创建 job、不修改 result、不代表生产审批完成。
- 本次采用的关键假设：`result_explanation.v1` 内容由外部 Agent/系统生成；AutoWaterSimu 只负责校验、证据引用解析、状态审计和 API 暴露；review 决策最小支持 `approved` / `rejected`。
- 剩余不确定性：内部 LLM provider、reviewer assignment UI、approval-page 集成、NewSystem service-level E2E、解释展示 UI、生产审批策略仍待后续。
- 影响范围 / 风险控制方式：新增独立表和独立 endpoints；使用 `explanation:write` mutation scope；所有 evidence refs 必须在 route job 边界内解析；publish 需要 prior approved review；不执行 Agent code、不发布生产指令、不改变 worker/job/artifact/model_run lifecycle。
- 验证方式：`cd apps\api; go test ./...` 通过；`backend\.venv\Scripts\python -m pytest contracts\tests -q` 通过（69 passed）；`cd frontend; npm run generate-compute-client` 完成；`cd frontend; npx tsc --noEmit` 通过；`cd frontend; npm run build` 通过（既有 Vite warnings）；临时 Docker PostgreSQL migration smoke 覆盖 `0001`-`0007` up/down 通过；`git diff --check` 通过（仅 LF/CRLF 提示）。
- 是否更新 README：是，更新 `contracts/README.md`、`apps/api/README.md`、`apps/api/internal/compute/README.md`、`apps/api/migrations/README.md`、`apps/api/openapi/README.md`、`frontend/src/services/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：是，新增 `0006-result-explanation-workflow-scope.md`，明确 externally generated explanation、review/publish audit 状态和无生产副作用边界。

## 23:55 - AutoWaterSimu Next process graph evidence ref

- 用户目标：继续按 PRD、Technical Spec、Development Plan 完成 AutoWaterSimu Next 开发目标，本轮推进 NewSystem/milp/Agent 审批读取链路中 ProcessGraph 证据引用解析。
- 涉及目录：`apps/api/`、`apps/api/internal/compute/`、`tasks/`、`.ai/changes/`。
- 修改内容：`GET /api/v1/compute/jobs/{job_id}/evidence-ref?ref=...` 新增 `process_graph:<id>` 支持；只有当 job payload 的 `process_graph_id` 匹配且 metadata store 中存在对应 `ProcessGraphRecord` 时才返回记录；新增 Go regression 覆盖成功和 missing 404；更新 API README。
- 修改原因：evidence package 已导出 `process_graph_ref`，但统一 evidence-ref dereference 只支持 model_run/artifact/job/simulation_input/evidence_package，审批或解释引用无法直接定位注册过的流程图证据。
- 本次已消除的不确定性：确认最小安全实现应只读解析 registered ProcessGraph，不反推未登记 graph、不跨 job dereference、不修改 evidence package。
- 本次采用的关键假设：`process_graph:<id>` 解析使用 job payload 中的 `process_graph_id` / `process_graph_version` 作为边界；未显式 version 时按现有 ProcessGraph 规则默认 version 1。
- 剩余不确定性：approval-page UI、NewSystem service-level E2E、risk finding detail UI、未登记 ProcessGraph 的展示策略仍待后续。
- 影响范围 / 风险控制方式：只扩展只读 evidence-ref endpoint；不新增数据库表、不改变 ProcessGraph registry、job lifecycle、result explanation workflow 或生产审批状态。
- 验证方式：`cd apps\api; go test ./...` 通过；`git diff --check` 通过（仅 LF/CRLF 提示）。
- 是否更新 README：是，更新 `apps/api/README.md`、`apps/api/internal/compute/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮是既有 evidence-read 边界内的 ref 类型补齐。

## 00:04 - AutoWaterSimu Next Web evidence ref lookup

- 用户目标：继续按 PRD、Technical Spec、Development Plan 完成 AutoWaterSimu Next 开发目标，本轮把 evidence ref dereference 接入 Web Compute Jobs 详情页，服务 NewSystem/milp/Agent 审批读取链路。
- 涉及目录：`frontend/src/routes/`、`tasks/`、`.ai/plans/`、`.ai/changes/`。
- 修改内容：Compute Jobs job detail 新增只读 `Evidence ref lookup` 面板；输入 `model_run:<id>`、`artifact:<id>`、`job:<id>`、`simulation_input:<id>`、`process_graph:<id>` 或 `evidence_package:<id>` 后调用 `computeJobsService.resolveEvidenceReference()`；展示后端返回的 ref type、ref id、resolved 状态和 payload；切换 selected job 时清空 lookup 状态；更新 route README、任务记录和 completion audit。
- 修改原因：后端已有 job-scoped evidence-ref endpoint，且 result summary 已包含 risk findings/evidence refs，但 Web 审批读取面仍缺少最小统一 ref 解析入口。
- 本次已消除的不确定性：确认 generated compute client 已包含 `EvidenceReferenceResolution`；确认 service wrapper 已提供 `resolveEvidenceReference()`；确认 route README 要求 route 层只调用 service helper，不在前端拼装 evidence payload。
- 本次采用的关键假设：本轮只做只读 ref lookup，不实现独立审批页面、risk finding 专用表格、result explanation UI、生产审批或跨 job evidence 组合；用户可从 result summary/risk finding JSON 复制 evidence ref 进行解析。
- 剩余不确定性：NewSystem service-level E2E、risk finding 专用交互、reviewer assignment UI、内部 LLM generation、生产审批策略仍待后续。
- 影响范围 / 风险控制方式：仅修改 Compute Jobs route 编排与 route README；不改后端、contracts schema、generated client、service wrapper public shape 或 legacy simulation pages；结果按 selected job 过滤显示，避免跨 job stale resolution。
- 验证方式：`cd frontend; npx tsc --noEmit` 通过；`cd frontend; npm run build` 通过（仅既有 Vite warnings：gray-matter eval、toaster chunking、large bundle）；`cd frontend; npx biome check src\routes\_layout\compute-jobs.tsx` 通过；`git diff --check` 通过（仅 LF/CRLF 提示）。
- 是否更新 README：是，更新 `frontend/src/routes/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮是既有 evidence-read API 的 UI 接入，没有新增长期架构决策。

## 00:16 - AutoWaterSimu Next NewSystem evidence E2E

- 用户目标：继续按 PRD、Technical Spec、Development Plan 完成 AutoWaterSimu Next 开发目标，本轮补齐 NewSystem/milp 审批读取链路的 service-level E2E 覆盖。
- 涉及目录：`apps/api/internal/compute/`、`tasks/`、`.ai/plans/`、`.ai/changes/`。
- 修改内容：新增 `TestNewSystemEvidenceReferenceE2E`，串联 `process_graph.v1` 注册、NewSystem-style `simulation_request.v1` 创建 simulation-check job、worker claim/complete、risk finding summary、`simulation_input:<id>` / `process_graph:<id>` / `model_run:<id>` / `evidence_package:<id>` dereference 和 result explanation submit。
- 修改原因：此前各能力已有分散测试，但缺少一个同 job 边界内覆盖 NewSystem 只读审批所需 ref 类型和 result explanation 引用的端到端回归。
- 本次已消除的不确定性：确认 process graph simulation check job 的生成 payload 可作为 `simulation_input` evidence ref；确认完成后的 model run、process graph record、evidence package 可通过统一 evidence-ref endpoint 解析；确认 result explanation submit 会接受这些 refs 并记录 resolved refs。
- 本次采用的关键假设：本轮只补后端 regression，不新增外部 NewSystem SDK、独立审批页面、生产审批策略或跨 job dereference；`NewSystem` source_system 仅用于测试外部集成 metadata 传递。
- 剩余不确定性：真实外部 NewSystem/milp acceptance smoke、生产审批 policy、benchmark run contract、release checklist automation、packaged sidecar/installer smoke 仍待后续。
- 影响范围 / 风险控制方式：仅新增 Go 测试，不改 API 行为、contracts schema、generated client、数据库 migration 或前端；测试使用 in-memory store 与现有 fixtures，避免引入新依赖。
- 验证方式：`cd apps\api; go test ./internal/compute -run TestNewSystemEvidenceReferenceE2E -count=1` 通过；`cd apps\api; go test ./...` 通过；`git diff --check` 通过（仅 LF/CRLF 提示）。
- 是否更新 README：否，本轮不改变目录职责、公共接口或长期约定。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮是既有 evidence/read 和 result explanation 边界下的回归覆盖。
