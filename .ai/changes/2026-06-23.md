## 22:26 - AutoWaterSimu Next standalone RC blocker closeout review

- 用户目标：全面 review 独立开发实施计划完成情况和质量，确认是否还有未完成和需要完善的部分，并优先补齐本地可关闭的 P0 缺口。
- 涉及目录：`docs/rebuild/`、`frontend/src/routes`、`frontend/src/services`、`frontend/src/features/udm`、`frontend/tests`、`apps/api/internal/compute`、`apps/api/internal/platform/httpx`、`apps/api/internal/legacyimport`、`services/simulation-worker`、`scripts/ci`、`scripts/release`、`tasks`。
- 修改内容：补齐 standalone route tree；将 ASM1Slim/ASM1/ASM3/UDM standalone Flowchart CRUD 切到 Go Workspace CanvasGraph/Scenario API；新增 UDM Hybrid strict validation HTTP endpoint 与 frontend preflight/config 复用；修复 legacy canonical compute job migration hash/history；新增并通过五模型 live UI/API/PostgreSQL/MinIO/worker/UI 回读 gate；增强 release gate 对 skipped evidence 的失败判定；修复 worker claim 响应丢失后的 same-worker running job reclaim 与 claim-only retry；补充相关 README、实施计划和任务复盘。
- 修改原因：原实施计划存在 P0 运行路径缺口和证据缺口，尤其是 standalone 下部分 CRUD/Hybrid validation 仍依赖 legacy 或本地假通过，五模型缺少真实 live worker/backend 证据，release gate 容易把 skip 当作通过。
- 本次已消除的不确定性：确认 standalone 五模型 live 闭环在当前 HEAD 可通过；确认 frontend standalone boundary audit 不再允许五模型 legacy calculation 静态回流；确认 UDM Hybrid standalone validation 走 Go endpoint；确认 migration smoke 在缺少 DSN 时只可作为 skipped evidence；确认 release gate `-FailOnSkip` 会因 skipped migration/backup evidence 失败。
- 本次采用的关键假设：本地 `COMPUTE_API_AUTH_MODE=disabled` 是 standalone smoke 的正确模式；legacy FastAPI 源码和 generated client 在最终 no-skip RC 前保留为 oracle/compat 材料；缺少外部 DSN/对象存储凭据时不得声称生产迁移或 artifact profile 已完成。
- 剩余不确定性：生产 legacy PostgreSQL read-only/full migration rehearsal 未执行；MinIO/S3 full artifact profile live validation 未执行；hosted/full standalone RC gate no-skip evidence 未生成；backup/restore 仍只有 skipped/fixture 证据，缺少内容级 live restore 验收。
- 影响范围 / 风险控制方式：变更集中在 standalone runtime、Go Compute API、worker HTTP bridge、CI/release evidence；legacy runtime 保持动态 FastAPI client 路径；不删除 FastAPI/legacy client，不改变生产数据；worker retry 仅限 `/claim` 底层连接失败，artifact upload/succeed/fail 不自动重放。
- 验证方式：`cd frontend; npx tsc --noEmit`；`cd apps\api; go test ./internal/platform/httpx ./internal/compute ./internal/legacyimport ./cmd/migrate-legacy`；`backend\.venv\Scripts\python -m pytest services\simulation-worker\tests -q`；`scripts/audit-frontend-standalone-compute-boundary.ps1`；`scripts/ci/standalone-five-model-smoke.ps1`；`scripts/ci/standalone-five-model-live.ps1`；`scripts/ci/standalone-migration-smoke.ps1`；`scripts/release/standalone-release-gate.ps1 -SkipLong -FailOnSkip`；`git diff --check -- frontend/src apps/api scripts docs README.md Justfile docker-compose.dev.yml services/simulation-worker tasks .ai`。
- 是否更新 README：是，更新根 README、docs/rebuild、frontend routes/services/UDM/tests、apps/api/compute/httpx、services/simulation-worker、scripts/release/ci 等相关上下文。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次是既有 standalone boundary 决策下的实现和 gate 收敛，没有新增长期架构方向。

## 22:30 - Standalone external acceptance evidence gate

- 用户目标：继续确认 standalone RC 是否仍有未完成项，并避免把外部验收缺口误判为本地完成。
- 涉及目录：`scripts/release`、`.ai/changes`。
- 修改内容：`standalone-release-gate.ps1` 新增必需外部验收记录检查，要求 `tmp/ci-evidence/standalone-legacy-production-rehearsal.json` 与 `tmp/ci-evidence/standalone-s3-artifact-profile-live.json` 均存在、JSON 合法、顶层 `status` 为 `passed` 且 `commit_sha` 匹配当前 HEAD；补充 `scripts/release/README.md` 说明。
- 修改原因：生产 legacy 只读/完整迁移演练与 MinIO/S3 artifact profile live 验收无法由本地 fixture 代替，必须在 release evidence 中显式暴露。
- 本次已消除的不确定性：确认 release gate 可机器化识别两类外部验收记录缺失或 commit 不匹配，不再只依赖口头说明。
- 本次采用的关键假设：外部验收记录由实际运行环境生成或人工汇总，但最小 schema 只要求 `status`、`message`、`generated_at`、`commit_sha`，避免把外部流程写死到本地脚本。
- 剩余不确定性：两份真实外部验收记录尚未生成；完整 no-skip RC gate 仍需外部 DSN、S3/MinIO profile 和 hosted current HEAD 环境。
- 影响范围 / 风险控制方式：只增强 release evidence 判定；不修改业务代码、不删除现有 evidence、不把缺失外部资源自动视为失败，除非调用完整 RC 或 `-FailOnSkip`。
- 验证方式：PowerShell parse 通过；`scripts/release/standalone-release-gate.ps1 -SkipLong -FailOnSkip` 按预期失败并生成 4 个 `evidence_gaps`，其中包含两份外部验收记录缺失；`git diff --check -- scripts/release .ai` 通过，仅提示 LF/CRLF 工作区换行警告。
- 是否更新 README：是，更新 `scripts/release/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否。

## 22:37 - Standalone full gate DSN safety

- 用户目标：继续收口 Standalone RC 前阻断项，避免 full gate 命令把未准备好的本地库误当作完整 RC 验收环境。
- 涉及目录：根 `Justfile`、`scripts/release`、`docs/rebuild`、`tasks`、根 README。
- 修改内容：`standalone-release-gate-full` 不再硬编码 `localhost:5434`，改为要求调用方显式提供 `COMPUTE_API_DATABASE_URL` 与 `AUTOWATERSIMU_RESTORE_DATABASE_URL` 两个临时 PostgreSQL DSN；`standalone-release-gate.ps1` 记录 GitHub Actions 运行上下文字段，并在非 Windows 上递归调用 `pwsh`。
- 修改原因：migration rollback 和 restore 类检查不能默认指向 dev 库、生产 legacy 库或正在运行的 standalone 元数据库；hosted/current HEAD evidence 也需要在 release evidence 中可识别。
- 本次已消除的不确定性：确认 full gate 入口不再隐式依赖 `docker-compose.dev.yml` 的 `5434` 端口或本地残留数据库。
- 本次采用的关键假设：完整 RC 的 migration/restore 验证应由调用方准备显式临时库；脚本只检查和记录 evidence，不负责创建外部验收环境。
- 剩余不确定性：真实 hosted/full no-skip run、production legacy 演练和 MinIO/S3 profile live 验收仍未生成。
- 影响范围 / 风险控制方式：只改 gate 命令和 evidence 元数据；不改业务运行路径，不创建/删除数据库，不生成伪外部验收记录。
- 验证方式：PowerShell parse 通过；`just --list` 和 `just --dry-run standalone-release-gate-full` 通过；`scripts/release/standalone-release-gate.ps1 -SkipLong -FailOnSkip` 按预期失败，evidence 记录 `hosted_workflow_run=not_covered` 且仍有 4 个缺口；外部 evidence 检查已要求 `commit_sha` 匹配当前 HEAD；`git diff --check -- Justfile README.md scripts/release docs/rebuild tasks .ai` 通过，仅提示 LF/CRLF 工作区换行警告。
- 是否更新 README：是，更新根 README、`scripts/release/README.md`、standalone 实施计划和 `tasks/todo.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否。

## 22:45 - Standalone hosted full gate workflow entry

- 用户目标：继续收口 Standalone RC 前阻断项，补齐“当前 HEAD hosted/full gate 无 skip 通过”的可执行入口。
- 涉及目录：`.github/workflows`、`.github`、根 README、`docs/rebuild`、`tasks`。
- 修改内容：新增 `.github/workflows/next-standalone-release-gate.yml`，用 Ubuntu runner 编排已有 standalone full gate：安装依赖、准备临时 PostgreSQL `compute_rc`/`restore_rc`、写入调用方提供的外部验收 JSON、构建 standalone worker image、运行 five-model live、启动 standalone compose、执行 `standalone-release-gate.ps1 -RunComposeSmoke -RunBackupRestoreLive -RunPostgresMigrationSmoke -RunReleaseImageSmoke` 并上传 `tmp/ci-evidence` / `tmp/release-evidence`。
- 修改原因：此前 hosted/full gate 只是退出条件，缺少 GitHub Actions 手动入口，无法生成 current HEAD hosted evidence。
- 本次已消除的不确定性：确认仓库现在有 standalone full RC hosted/manual workflow 入口；缺少当前 commit 的 legacy rehearsal / S3 profile JSON 时，该入口应失败并上传 evidence。
- 本次采用的关键假设：外部验收记录由生产 legacy 演练和 S3 profile live validation 真实生成，workflow 只接收 JSON 输入，不负责伪造或替代外部执行。
- 剩余不确定性：尚未触发真实 GitHub Actions run；两份外部验收 JSON 仍未生成。
- 影响范围 / 风险控制方式：只新增 workflow 编排和 README 说明；不改业务代码，不把 workflow 存在解释为 hosted green。
- 验证方式：PyYAML 解析 `.github/workflows/*.yml` 通过；`just --dry-run standalone-release-gate-full` 通过；`scripts/release/standalone-release-gate.ps1 -SkipLong -FailOnSkip` 按预期失败，evidence 为 `hosted_workflow_run=not_covered` 且 4 个缺口仍是 skipped/missing；`git diff --check -- .github README.md docs/rebuild tasks .ai` 通过，仅提示 LF/CRLF 工作区换行警告。
- 是否更新 README：是，更新 `.github/README.md`、`.github/workflows/README.md`、根 README、standalone 实施计划和 `tasks/todo.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否。

## 22:50 - Standalone full gate evidence coverage tightening

- 用户目标：继续收口 Standalone RC 前阻断项，避免 hosted/full gate 在外部 JSON 齐备后仍因为本地 evidence 配置缺口产生 skip。
- 涉及目录：`scripts/release`、`.github/workflows`、`.github`、`docs/rebuild`、`tasks`。
- 修改内容：`standalone-release-gate.ps1` 现在要求 `tmp/ci-evidence/standalone-five-model-live.json` 为当前 HEAD 的 `passed` evidence；`next-standalone-release-gate.yml` 在启动 standalone compose 前创建 `tmp/standalone-artifacts`，并把它作为 `COMPUTE_API_ARTIFACT_DIR` 传入 full gate，避免 backup/restore manifest step 因未设置 artifact dir 而 skip；workflow 还会把 `AUTOWATERSIMU_LEGACY_DATABASE_URL` GitHub Secret 注入 release gate，使 standalone migration smoke 在 secret 存在时执行 live dry-run。
- 修改原因：完整 RC 不能只依赖 mock-backed five-model smoke；backup/restore live gate 也不应因 workflow 未传 artifact dir 产生可避免的 skip。
- 本次已消除的不确定性：确认 release gate 会机器化要求 five-model live evidence；hosted workflow 具备 artifact manifest 输入路径，并且不再因为缺少 legacy DSN wiring 而必然产生 migration skip。
- 本次采用的关键假设：`tmp/standalone-artifacts` 是 standalone compose 中 Compute API 的 host-side artifact directory，适合作为 backup/restore manifest 输入；内容级 restore 仍列为 P1。
- 剩余不确定性：真实 hosted/full no-skip run、production legacy 演练和 MinIO/S3 profile live 验收仍未生成；GitHub Secret `AUTOWATERSIMU_LEGACY_DATABASE_URL` 是否已配置仍需外部确认。
- 影响范围 / 风险控制方式：只增强 gate evidence 检查和 workflow env；不改业务代码，不伪造 live evidence。
- 验证方式：PyYAML 解析 `.github/workflows/*.yml` 通过；PowerShell parse 通过；`scripts/release/standalone-release-gate.ps1 -SkipLong -FailOnSkip` 按预期失败，`standalone-five-model-live.json` 无 gap，剩余 4 个缺口仍是 skipped/missing；`git diff --check -- .github README.md docs/rebuild tasks .ai` 通过，仅提示 LF/CRLF 工作区换行警告。
- 是否更新 README：是，更新 `scripts/release/README.md`、`.github/workflows/README.md`、standalone 实施计划和 `tasks/todo.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否。

## 22:59 - Standalone release gate current HEAD evidence check

- 用户目标：继续收口 Standalone RC 前阻断项，避免 full gate 复用旧 commit 的 passed evidence。
- 涉及目录：`scripts/release`、`docs/rebuild`、`tasks`。
- 修改内容：`standalone-release-gate.ps1` 的 `Mark-EvidenceGaps` 增加 `HeadCommit` 校验；migration、backup/restore 和 golden summary evidence 在非 skipped/partial 时也必须 `commit_sha` 匹配当前 HEAD，否则进入 `evidence_gaps=commit_mismatch`。
- 修改原因：完整 RC evidence 必须证明当前 HEAD，而不是复用旧本地或旧 CI 的通过记录。
- 本次已消除的不确定性：确认 release gate 不再只对 five-model live 和外部验收记录检查 commit；主要聚合 evidence 都会绑定当前 HEAD。
- 本次采用的关键假设：这些 evidence 文件都已有顶层 `commit_sha` 字段；缺失 `commit_sha` 应按不满足完整 RC 证据处理。
- 剩余不确定性：真实 hosted/full no-skip run、production legacy 演练和 MinIO/S3 profile live 验收仍未生成。
- 影响范围 / 风险控制方式：只增强 release gate evidence 判定；不改业务代码，不删除旧 evidence 文件。
- 验证方式：PowerShell parse 通过；`scripts/release/standalone-release-gate.ps1 -SkipLong -FailOnSkip` 按预期失败，`commit_mismatch_count=0`，剩余 4 个缺口仍是 skipped/missing；`git diff --check -- scripts/release docs/rebuild tasks .ai` 通过，仅提示 LF/CRLF 工作区换行警告。
- 是否更新 README：是，更新 `scripts/release/README.md`、standalone 实施计划和 `tasks/todo.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否。

## 23:05 - Standalone required evidence schema check

- 用户目标：继续收口 Standalone RC 前阻断项，避免错误类型的 passed JSON 满足 full gate 外部证据要求。
- 涉及目录：`scripts/release`、`.github/workflows`、根 README、`docs/rebuild`、`tasks`。
- 修改内容：`standalone-release-gate.ps1` 的 `Mark-RequiredEvidencePassed` 增加 `schema_version` 校验；five-model live、production legacy rehearsal 和 MinIO/S3 artifact profile live evidence 分别绑定 `autowatersimu_next_standalone_five_model_live_smoke.v1`、`autowatersimu_next_standalone_legacy_production_rehearsal.v1` 和 `autowatersimu_next_standalone_s3_artifact_profile_live.v1`；同步更新 release README、workflow README、根 README、standalone 实施计划和 `tasks/todo.md`。
- 修改原因：完整 RC 证据需要证明验收类别正确，不能只凭 `status=passed` 和当前 commit 误收任意 JSON。
- 本次已消除的不确定性：确认 release gate 会对三类必需 evidence 做轻量类型判定；schema 不匹配会进入 `evidence_gaps=schema_mismatch`。
- 本次采用的关键假设：当前阶段先校验顶层 `schema_version` 作为证据类别门槛，完整外部记录字段细则由真实生产 legacy rehearsal 与 S3 profile live validation 脚本/流程产生。
- 剩余不确定性：真实 hosted/full no-skip run、production legacy 演练和 MinIO/S3 profile live 验收仍未生成；GitHub Secret `AUTOWATERSIMU_LEGACY_DATABASE_URL` 是否已配置仍需外部确认。
- 影响范围 / 风险控制方式：只增强 release gate evidence 判定和文档记录；不改业务代码，不伪造外部证据。
- 验证方式：PowerShell parse 通过；PyYAML 解析 `.github/workflows/*.yml` 通过；`scripts/release/standalone-release-gate.ps1 -SkipLong -FailOnSkip` 按预期失败，`schema_mismatch_count=0`、`commit_mismatch_count=0`，剩余 4 个缺口为 migration/backup passed_with_skips 和两份外部验收记录 missing；`git diff --check -- scripts/release .github README.md docs/rebuild tasks .ai` 通过，仅提示 LF/CRLF 工作区换行警告。
- 是否更新 README：是，更新 `scripts/release/README.md`、`.github/workflows/README.md`、根 README、standalone 实施计划和 `tasks/todo.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否。
