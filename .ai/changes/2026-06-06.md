## 16:17 - Draft confirmation tenant/project/site scope and selected audit

- 用户目标：根据 Certainty/Elegance PRD 与开发计划继续完成剩余工作，并按近期策略推进明确 object data-scope / selected mutation audit 切片，避免继续 evidence wrapper、hosted workflow entry 或同包整理循环。
- 涉及目录：`apps/api/internal/domain/agent/`、`apps/api/internal/compute/`、`apps/api/migrations/`、`apps/api/openapi/`、`frontend/src/client/compute/`、`scripts/ci/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`。
- 修改内容：为 draft confirmation record 增加 `site_id` projection、compute DTO、PostgreSQL 持久化和 reversible migration `0013_draft_confirmation_site_scope`；`GET /api/v1/contracts/confirmations/{id}`、constraint application plan 和 explicit simulation-check promotion 在读取记录后按 stored tenant/project/site 拒绝跨 scope token；`POST /api/v1/contracts/confirm-draft` 通过 audit context 写 compact `draft_confirmation.recorded` 到 `mutation_audit_events`，Memory/Postgres 仅在首次 idempotent insert 成功时写入；OpenAPI 与 generated Compute client 新增 `DraftConfirmationRecord.site_id`；新增 focused domain/HTTP 回归测试并纳入 security smoke；更新 Compute API、domain agent、migrations、security、architecture、Certainty/Elegance plan 和任务记录。
- 修改原因：近期 `.ai/changes` 显示 hosted/evidence wrapper 与同 package 文件拆分边际收益下降；draft confirmation 已持有 tenant/project metadata，但缺少 site metadata、HTTP read-scope 检查和 selected mutation audit。该切片直接推进全对象 data-scope 与 all-mutation audit 的一个明确对象面。
- 本次已消除的不确定性：确认 `draft_confirmation.v1.metadata` 可承载 tenant/project/site/trace/approval metadata；确认 GET/constraint plan/promotion 都能先读取 stored confirmation 再做 scope authorization；确认 Memory lock 与 Postgres transaction 可让 draft confirmation insert 与 compact audit 同步写入，重复同 hash 确认不会重复写 audit；确认 generated client 只需要 record 字段变化。
- 本次采用的关键假设：本阶段只覆盖 draft confirmation record 的 selected audit 与 confirmation get / constraint plan / promotion read-scope，不声明完整 all-mutation audit 或全对象 data scope 完成；confirm-draft 写授权保持现状，只新增 who/where audit context；audit payload 只保留 compact id/schema/decision/hash/scope/trace/approval 字段，不保存完整 confirmation payload；endpoint paths、contracts schema、draft validation/promotion semantics 和 public `Service` method signatures 保持不变。
- 剩余不确定性：model catalog/global object data scope、OIDC/JWKS、RBAC/ABAC、complete all-mutation audit、ontology-backed policy enforcement、hosted security green evidence 和完整 golden scenarios 仍未完成。
- 影响范围 / 风险控制方式：只修改 draft confirmation projection/persistence/audit/read authorization、OpenAPI/generated client record field、focused tests/security smoke 和长期文档；新增 PostgreSQL up/down migration；Postgres 写入使用同一 transaction，MemoryStore 写入使用同一 lock；不新增审计查询 API，不写入大 payload，不修改 legacy FastAPI client。
- 验证方式：`cd apps\api; go test ./internal/domain/agent ./internal/compute -run "Test(DraftConfirmationRecordDataFromDocument|HTTPDraftConfirmationTenantProjectSiteScope|HTTPDraftConfirmationMutationAuditEvents|ContractValidationEndpoint)$" -count=1` passed；`cd apps\api; go test ./internal/compute -run "TestHTTPDraftConfirmation(TenantProjectSiteScope|MutationAuditEvents)" -count=1` passed；`cd apps\api; go test ./...` passed；`cd frontend; npm run generate-compute-client` completed，生成产物经仓库既有尾随空格/末尾换行规则归一化后只保留 `DraftConfirmationRecord.site_id` 变化；`cd frontend; npx tsc --noEmit` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\security-smoke.ps1` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1` passed（42 Store methods、13 embedded interfaces、13 domain groups、11 constructors、0 violations）；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1` passed；OpenAPI JSON parse passed；PowerShell parse check for `scripts\ci\security-smoke.ps1` passed；docs/rebuild P0/P1/P2/schema scan completed；final diff-check passed with LF/CRLF warnings only。PostgreSQL live migration up/down smoke 未运行，因为 `COMPUTE_API_DATABASE_URL` 未配置；未伪造 hosted security green evidence。
- 是否更新 README：是，更新 `apps/api/README.md`、`apps/api/internal/compute/README.md`、`apps/api/internal/domain/agent/README.md`、`apps/api/migrations/README.md`、`scripts/ci/README.md`、`docs/architecture/current-state.md`、`docs/architecture/local-dev.md`、`docs/architecture/compute-api.md` 与 Certainty/Elegance Development Plan。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次沿用 existing selected mutation audit / static-token scoped read security 边界，不改变长期认证或审计架构决策。

## 15:23 - Simulation registry selected mutation audit

- 用户目标：根据 Certainty/Elegance PRD 与开发计划继续完成剩余工作，并按近期策略推进明确 all-mutation audit 切片，避免继续 evidence wrapper 或同包整理循环。
- 涉及目录：`apps/api/internal/compute/`、`scripts/ci/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`。
- 修改内容：新增 compact `process_graph.registered` / `simulation_input.registered` audit event construction；`ProcessGraphStore` / `SimulationInputStore` upsert 接收可选 `MutationAuditRecord`，Memory/Postgres 只在首次新建成功时写入 `mutation_audit_events`，重复同 hash 幂等 upsert 不重复写 audit；显式 POST registry 与 simulation-check 自动创建 simulation_input record 均传入同一 compact audit；HTTP registry POST 注入 static-token principal 与 route audit context；新增 focused HTTP audit 测试并纳入 security smoke；更新 Compute API、security smoke、architecture、Certainty/Elegance plan 和任务记录。
- 修改原因：上一阶段已完成 simulation registry tenant/project/site read-scope，但 registry 写路径仍没有 selected mutation audit。该切片直接推进 all-mutation audit 的一个明确剩余对象面，同时不引入 OIDC/RBAC、审计查询 API 或 schema/OpenAPI 变更。
- 本次已消除的不确定性：确认现有 `mutation_audit_events` 可复用到非 job-scoped registry mutation；确认 registry upsert 已有 Memory lock / Postgres transaction，可让记录创建与 audit 同步写入；确认 HTTP registry handler 已有 principal，只需接入 `withAuditPrincipal` 即可保留 who/where。
- 本次采用的关键假设：本阶段只覆盖 process_graph / simulation_input registration selected audit，不声明完整 all-mutation audit 完成；audit payload 只保存 compact id/hash/status/scope/ref 字段，不保存完整 process graph、simulation input、catalog、benchmark 或 artifact payload；不修改 contracts、OpenAPI、generated client、migration schema、endpoint path 或 registry read-scope 行为。
- 剩余不确定性：完整 all-mutation audit、draft confirmation/model catalog/global object data scope、OIDC/JWKS、RBAC/ABAC、ontology-backed policy enforcement、hosted security green evidence 和完整 security golden scenario 仍未完成。
- 影响范围 / 风险控制方式：只修改 registry service/store call sites、audit builder、focused tests/security smoke 和长期文档；Postgres 写入使用现有 transaction；MemoryStore 写入使用现有 lock；不新增外部 API 或大 payload metadata。
- 验证方式：`cd apps\api; go test ./internal/compute -run "TestHTTPSimulationRegistry(TenantProjectSiteScope|MutationAuditEvents)" -count=1` passed；`cd apps\api; go test ./...` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\security-smoke.ps1` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1` passed（42 Store methods、13 embedded interfaces、13 domain groups、11 constructors、0 violations）；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1` passed；PowerShell parse check for `scripts\ci\security-smoke.ps1` passed；docs/rebuild P0/P1/P2/schema scan completed；final diff-check passed with LF/CRLF warnings only。PostgreSQL live migration/up-down smoke 未运行，因为本次未改 schema 且 `COMPUTE_API_DATABASE_URL` 未配置；未伪造 hosted security green evidence。
- 是否更新 README：是，更新 `apps/api/README.md`、`apps/api/internal/compute/README.md`、`scripts/ci/README.md`、`docs/architecture/current-state.md`、`docs/architecture/local-dev.md`、`docs/architecture/compute-api.md` 与 Certainty/Elegance Development Plan。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次沿用现有 selected audit envelope / static-token security 边界，不改变长期架构决策。

## 14:59 - Simulation registry tenant/project/site read-scope

- 用户目标：根据 Certainty/Elegance PRD 与开发计划继续完成剩余工作，并按用户最新反馈停止低收益 evidence wrapper / 同包整理循环，推进明确 object data-scope 切片。
- 涉及目录：`apps/api/internal/compute/`、`apps/api/internal/domain/simulation/`、`apps/api/migrations/`、`apps/api/openapi/`、`frontend/src/client/compute/`、`scripts/ci/`、`scripts/`、`docs/architecture/`、`docs/operations/`、`docs/rebuild/`、`tasks/`。
- 修改内容：为 `process_graph.v1.metadata.site_id` 与 `simulation_input.v1.metadata.site_id` 增加 domain record projection、compute record 字段、PostgreSQL `site_id` 持久化和 reversible migration `0012_simulation_registry_site_scope`；`GET /api/v1/process-graphs/{process_graph_id}` 与 `GET /api/v1/simulation-inputs/{simulation_input_id}` 增加 stored tenant/project/site scoped-token read-scope 拒绝；OpenAPI / generated Compute TS client 新增 registry record `site_id`；新增 focused HTTP 测试并纳入 security smoke；更新相关 README、architecture、operations、Certainty/Elegance plan 和任务记录。
- 修改原因：近期记录显示继续新增 hosted/evidence wrapper 边际收益下降；benchmark_run/model_run/artifact/job read-scope 已推进，但 process_graph / simulation_input registry record 仍只有 tenant/project metadata 且 GET 无 data-scope 授权。该切片直接推进全对象 data-scope 的一个明确对象面，同时不引入完整 RBAC/OIDC。
- 本次已消除的不确定性：确认 `process_graph.v1` 与 `simulation_input.v1` 合同允许 metadata 扩展；确认 registry domain projection 已从 metadata 读取 tenant/project，可低风险扩展到 site；确认 GET handler 可在读取单个 record 后基于 stored metadata 做 scope check，global token 保持既有读取行为。
- 本次采用的关键假设：本阶段只补 process_graph / simulation_input GET read-scope，不声明全对象 data-scope 完成；不改变 registry POST 写入授权、payload hash/idempotency、合同 schema、endpoint paths、public `Service` method signatures 或 simulation input/process graph resolution behavior；site-scoped token 读取缺少/不匹配 `site_id` 的 registry record 应返回 403，是安全收紧。
- 剩余不确定性：完整 object-level data scope（draft confirmation、model catalog/global objects 等）、OIDC/JWKS、RBAC/ABAC、all-mutation audit、hosted security workflow green run 和完整 security golden scenario 仍未完成。
- 影响范围 / 风险控制方式：只修改 simulation registry metadata projection/persistence/read authorization、OpenAPI/generated client record fields、focused tests/security smoke 和长期文档；新增 PostgreSQL up/down migration；不修改 worker runtime、simulation execution、frontend route UI、contracts schema、job create behavior、model governance workflows 或 artifact behavior。
- 验证方式：`cd apps\api; go test ./internal/compute -run TestHTTPSimulationRegistryTenantProjectSiteScope -count=1` passed；`cd apps\api; go test ./internal/domain/simulation ./internal/compute -run "Test(ProcessGraphRecordData|SimulationInputRecordData|HTTPSimulationRegistryTenantProjectSiteScope|HTTPJobReadTenantProjectSiteScope)" -count=1` passed；`cd apps\api; go test ./...` passed；`cd frontend; npm run generate-compute-client` completed and generated only record `site_id` changes after whitespace normalization；`cd frontend; npx tsc --noEmit` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\security-smoke.ps1` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1` passed；OpenAPI JSON parse passed；docs/rebuild P0/P1/P2/schema scan completed；PowerShell parse checks for `scripts\ci\security-smoke.ps1` and `scripts\ci\golden-scenarios.ps1` passed；`git diff --check -- apps/api frontend/src/client/compute scripts/ci docs/architecture docs/operations docs/rebuild tasks .ai` passed with LF/CRLF warnings only；PostgreSQL migration up/down smoke 未运行，因为 `COMPUTE_API_DATABASE_URL` 未配置。
- 是否更新 README：是，更新 `apps/api/README.md`、`apps/api/internal/compute/README.md`、`apps/api/internal/domain/simulation/README.md`、`apps/api/migrations/README.md`、`scripts/ci/README.md`、`docs/architecture/current-state.md`、`docs/architecture/local-dev.md`、`docs/architecture/compute-api.md`、`docs/operations/README.md`、`docs/operations/compute_api_tenancy_observability_runbook.md` 与 Certainty/Elegance Development Plan。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次是现有 static-token read-scope coverage extension，不改变长期认证/授权架构边界。

## 14:25 - Benchmark run tenant/project/site read-scope

- 用户目标：根据 Certainty/Elegance PRD 与开发计划继续完成剩余工作，并按近期策略转向明确 object data-scope 切片，避免继续 evidence wrapper 或同包整理循环。
- 涉及目录：`apps/api/internal/compute/`、`apps/api/openapi/`、`frontend/src/client/compute/`、`frontend/src/shared/api/`、`frontend/src/features/model-governance/`、`scripts/ci/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`。
- 修改内容：为 benchmark_run read/list 增加基于关联 `job_id` 的 tenant/project/site read-scope 授权；`BenchmarkRunFilter` 新增 `JobID` 并让 Memory/Postgres list 支持 `job_id` 过滤；scoped token 调用 benchmark_run list 必须提供已授权 `job_id`；OpenAPI list benchmark runs 新增可选 `job_id` query 参数并重新生成 Compute TS client；model-governance feature wrapper 透传可选 `jobId`；新增 focused HTTP 测试并纳入 security smoke；更新相关 README、architecture/current-state/local-dev/compute-api、Certainty/Elegance plan 和任务记录。
- 修改原因：上一阶段已完成 model governance selected mutation audit，但全对象 data scope 仍是明确剩余缺口。benchmark_run 是 model governance 对象，同时记录已包含 `job_id`，可以复用现有 job scope 授权而不引入新业务模型或 schema。
- 本次已消除的不确定性：确认 benchmark_run get 可通过记录内 `JobID` 反查 job scope；确认 benchmark_run list 如果不提供 `job_id` 就无法在 HTTP 层可靠过滤 scoped 数据，因此 scoped token 应像 model_run list 一样被要求提供已授权 `job_id`；确认 global token 仍可保留未过滤 list 行为。
- 本次采用的关键假设：本阶段只补 benchmark_run read-scope，不声明全对象 data scope 完成；scoped token 无 `job_id` 的 benchmark_run list 从 200 变为 403 是安全收紧；不改变 benchmark_run 写入、promotion plan、contracts、数据库 schema、model governance workflow 或 public `Service` method signature。
- 剩余不确定性：完整 object-level data scope（process_graph、simulation_input、draft confirmation、model catalog/global objects 等）、OIDC/JWKS、RBAC/ABAC、all-mutation audit、hosted security workflow green run 和完整 security golden scenario 仍未完成。
- 影响范围 / 风险控制方式：只修改 benchmark_run read/list HTTP 授权、list filter、OpenAPI/generated compute client、feature wrapper、focused tests、security smoke 和长期文档；不修改 token schema、benchmark_run persistence schema、promotion gate policy、generated legacy FastAPI client 或 frontend route UI。
- 验证方式：`cd apps\api; go test ./internal/compute -run TestHTTPBenchmarkRunTenantProjectSiteScope -count=1` passed；`cd apps\api; go test ./...` passed；`cd frontend; npm run generate-compute-client` completed and generated only compute client `jobId` changes after whitespace normalization；`cd frontend; npx tsc --noEmit` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\security-smoke.ps1` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1` passed；OpenAPI JSON parse passed；docs/rebuild P0/P1/P2/schema scan completed；`git diff --check -- apps/api frontend/src/client/compute frontend/src/shared/api frontend/src/features/model-governance scripts/ci docs/architecture docs/rebuild tasks .ai` passed with LF/CRLF warnings only。
- 是否更新 README：是，更新 `apps/api/README.md`、`apps/api/internal/compute/README.md`、`frontend/src/features/model-governance/README.md`、`scripts/ci/README.md`、`docs/architecture/current-state.md`、`docs/architecture/local-dev.md`、`docs/architecture/compute-api.md` 与 Certainty/Elegance Development Plan。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次是现有 static-token data-scope coverage extension，不改变长期安全架构边界。

## 14:00 - Model governance selected mutation audit

- 用户目标：根据 Certainty/Elegance PRD 与开发计划继续完成剩余工作，并按用户最新判断停止低收益 evidence wrapper / 同包整理循环，转向明确的 all-mutation audit 切片。
- 涉及目录：`apps/api/internal/compute/`、`apps/api/migrations/`、`apps/api/`、`scripts/`、`scripts/ci/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`。
- 修改内容：新增 compact `MutationAuditRecord` / `MutationAuditStore`、Memory/Postgres mutation audit persistence 和 `0011_mutation_audit_events` migration；为 model catalog registration、default parameter set status/promote、benchmark_run registration 写入非 job-scoped selected mutation audit events；为 model governance HTTP write paths 注入 audit principal/route；新增 draft promotion 与 benchmark schedule-run job audit context 断言；更新 security smoke coverage summary、boundary audit script 和长期文档。
- 修改原因：近期记录显示继续扩展 hosted/evidence wrapper 边际收益下降；result explanation 已完成 job-scoped audit，但 model governance mutation 仍缺 audit 记录。该切片直接推进 all-mutation audit 的明确范围，同时避免一次性引入完整 RBAC/OIDC 或全局审计查询 API。
- 本次已消除的不确定性：确认 draft promotion 与 benchmark schedule-run 通过 createJob 路径写入 job-scoped `job.created` / `job.queued` audit，并补充回归断言；确认 model catalog registration、parameter set status/promote 和 benchmark_run registration 不是 job-scoped mutation，需要独立 metadata audit surface；确认可通过 upsert transaction / MemoryStore lock 让 mutation 与 audit 共同写入，重复幂等请求不重复写 audit。
- 本次采用的关键假设：本阶段只做 selected mutation audit，不声明完整 all-mutation audit 完成；不新增 HTTP/OpenAPI audit read endpoint，不改变 endpoint behavior、contracts、generated clients、auth scopes 或 public `Service` method signature；audit event 只保存 compact before/after、hash、status、job/model_run 关联和 route/principal，不保存完整 catalog、benchmark_run、大结果或 artifact payload。
- 剩余不确定性：完整 all-mutation audit、全对象 tenant/project/site data scope、OIDC/JWKS、RBAC/ABAC、ontology-backed policy enforcement 和 hosted security green evidence 仍未完成。
- 影响范围 / 风险控制方式：新增独立 PostgreSQL migration 且提供 down script；global audit persistence 不暴露新外部 API；model governance upsert 在同一 transaction/lock 下写 audit；focused tests 覆盖 mutation audit、route/principal、幂等不重复；boundary audit 增加 `MutationAuditStore` 分类而非放宽规则。
- 验证方式：`cd apps\api; go test ./internal/compute -run "Test(ModelCatalogEndpoint|DefaultParameterSetPromotionPlanEndpoint|BenchmarkCaseScheduleRunEndpoint|ContractValidationEndpoint)$" -count=1` passed；`cd apps\api; go test ./...` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\security-smoke.ps1` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1` passed（42 Store methods、13 embedded interfaces、13 domain groups、11 constructors、0 violations）；docs/rebuild P0/P1/P2/schema scan completed；`git diff --check -- apps/api scripts docs/architecture docs/rebuild tasks .ai` passed with LF/CRLF warnings only。PostgreSQL migration up/down smoke 未运行，因为 `COMPUTE_API_DATABASE_URL` 未配置；未伪造 hosted 或 live database green evidence。
- 是否更新 README：是，更新 `apps/api/README.md`、`apps/api/internal/compute/README.md`、`apps/api/migrations/README.md`、`scripts/README.md`、`scripts/ci/README.md`、`docs/architecture/current-state.md`、`docs/architecture/local-dev.md`、`docs/architecture/compute-api.md` 与 Certainty/Elegance Development Plan。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次是 selected audit coverage extension，不改变长期认证/审计架构决策。

## 13:03 - Jobs state lifecycle domain package split

- 用户目标：根据 Certainty/Elegance PRD 与开发计划继续完成剩余工作，并按用户最新判断停止低收益 evidence wrapper / 同包文件拆分循环，转向真实 Go domain package movement。
- 涉及目录：`apps/api/internal/domain/jobs/`、`apps/api/internal/compute/`、`apps/api/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`。
- 修改内容：新增 `domain/jobs.JobStateService`、`StateMutation`、cancel/timeout mutation plan、事件 payload 与 timeout error 语义；`JobLifecycleService.CancelJob` / `TimeoutSweep` 改为通过 compute adapter 调用 domain service；MemoryStore/PostgresStore 改为应用 domain mutation plan；新增 direct domain tests；更新 Compute API/domain README、architecture/current-state、compute-api 文档、Certainty/Elegance Development Plan 和任务记录。
- 修改原因：近期 `.ai/changes` 显示 evidence/hosted wrapper 与同 package 文件拆分边际收益下降；`workers` 已完成 domain service split，`jobs` 仍主要停留在 compute compatibility package。本次选取 cancel/timeout state lifecycle 作为低风险但真实的 package movement 切片。
- 本次已消除的不确定性：确认 `JobLifecycleService` constructor 已收窄且可挂接 domain state service；确认 cancel/timeout 的稳定状态、事件 payload、timeout error code/message 可作为 DTO-neutral domain mutation plan；确认 compute 仍需保留 `JobRecord` / `JobSnapshot`、HTTP mapping、Memory/Postgres persistence 和 AppError 映射。
- 本次采用的关键假设：本阶段只迁移 jobs cancel/timeout state lifecycle，不声明完整 jobs lifecycle package movement 完成；不修改 endpoint behavior、OpenAPI、contracts、generated clients、数据库 schema、auth scope 或 public `Service` method signature；hosted green evidence 不在本阶段声明完成。
- 剩余不确定性：完整 jobs lifecycle package movement、artifact/models/evidence/simulation/agent workflow 迁出、hosted integration/browser/security/release green evidence、all-mutation audit、all-object tenant/project/site data scope 和完整 8 golden scenarios 仍未完成。
- 影响范围 / 风险控制方式：保留 compute compatibility wiring 和具体持久化；domain package 不 import compute；通过 boundary audit 验证 `NewJobStateService` 不接收 aggregate `Store` 且无 constructor boundary violation；通过 focused cancel/timeout regression 和全量 Go tests 验证行为不变。
- 验证方式：`cd apps\api; go test ./internal/domain/jobs ./internal/compute -run "TestJobStateService|TestTimeoutSweepAndPagination|TestCancelRejectsLateResult" -count=1` passed；`cd apps\api; go test ./...` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1` passed（41 Store methods、12 embedded interfaces、11 constructors、0 violations）；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` passed；`git diff --check -- apps/api docs/architecture docs/rebuild tasks .ai` passed with LF/CRLF warnings only。
- 是否更新 README：是，更新 `apps/api/README.md`、`apps/api/internal/compute/README.md`、`apps/api/internal/domain/README.md`、`apps/api/internal/domain/jobs/README.md`，并同步 `docs/architecture/current-state.md`、`docs/architecture/compute-api.md` 与 Certainty/Elegance Development Plan。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次遵循既有 modular monolith / domain package movement 决策，不新增长期架构取舍。

## 12:26 - Model run tenant/project/site read-scope

- 用户目标：根据 Certainty/Elegance PRD 与开发计划继续完成剩余工作，并在大阶段完成后提交推送。
- 涉及目录：`apps/api/internal/compute/`、`scripts/ci/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`。
- 修改内容：为 `GET /api/v1/model-runs/{model_run_id}` 增加基于 stored `model_run.job_id` 的 tenant/project/site read-scope 授权；scoped token 调用 `GET /api/v1/model-runs` 必须提供已授权 `job_id`，避免跨 job 枚举；新增 focused HTTP 测试；扩展 security smoke 测试选择与 coverage summary；更新相关 README、architecture/current-state/local-dev、Certainty/Elegance plan 和任务记录。
- 修改原因：上一阶段已覆盖 job list/get 与 artifact download 的 tenant/project/site read-scope，但 model_run 只读 endpoint 仍只检查 `job:read` scope，可能让 scoped token 读取跨 tenant/project/site job 的 model_run。
- 本次已消除的不确定性：确认 stored `model_run.v1` 包含 `job_id`，可通过既有 `domain/models.RunFieldsFromRaw` 解析；确认 model_run list 在没有 job_id 时无法在 HTTP 层可靠分页过滤 scoped 数据，因此 scoped token 的无 `job_id` 列表应被拒绝而不是返回可能泄露的跨 job 页面。
- 本次采用的关键假设：本阶段只补 model_run read-scope，不声明全对象 data scope 完成；global token 保留既有 unfiltered model_run list；HTTP/OpenAPI shape 不变，scoped token 的无 `job_id` list 变为 403 是安全收紧。
- 剩余不确定性：完整 object-level data scope、model catalog/global object policy、OIDC/JWKS、RBAC/ABAC、all-mutation audit、hosted security workflow green run 和完整 security golden scenario 仍未完成。
- 影响范围 / 风险控制方式：只修改 HTTP authorization helper/handler、focused tests、security smoke 和长期文档；不改变 model_run persistence、store pagination、service API、OpenAPI、contracts、generated client 或数据库 schema。
- 验证方式：`cd apps\api; go test ./internal/compute -run TestHTTPModelRunTenantProjectSiteScope -count=1` passed；`cd apps\api; go test ./...` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\security-smoke.ps1` passed and wrote `tenant_project_site_data_scope=tenant_project_site_read_scope_covered_for_job_list_get_model_run_get_job_filtered_list_and_artifact_download`；`scripts\check-deps.ps1` passed；docs/rebuild P0/P1/P2/schema scan completed；`git diff --check -- apps/api scripts/ci docs/architecture docs/rebuild tasks .ai` passed with LF/CRLF warnings only。
- 是否更新 README：是，更新 `apps/api/README.md`、`apps/api/internal/compute/README.md`、`scripts/ci/README.md`、`docs/architecture/current-state.md`、`docs/architecture/local-dev.md` 与 Certainty/Elegance Development Plan。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次是现有 static-token read-scope coverage 扩展，不改变长期安全架构边界。

## 12:12 - Result explanation selected audit events

- 用户目标：根据 Certainty/Elegance PRD 与开发计划继续完成剩余工作，并在大阶段完成后提交推送。
- 涉及目录：`apps/api/internal/compute/`、`scripts/ci/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`。
- 修改内容：为 result explanation submit/review/publish 新增 compact selected mutation audit events；HTTP mutation paths 注入 static-token principal 与 route audit context；Memory/Postgres result explanation store mutation 接收 audit event，PostgreSQL 在同一事务内写记录变更和 job event；新增 focused HTTP audit 测试；扩展 security smoke 测试选择与 coverage summary；更新相关 README、architecture/current-state/local-dev/compute-api、Certainty/Elegance plan 和任务记录。
- 修改原因：Certainty/Elegance 剩余安全缺口仍包含 all-mutation audit；当前 `compute_job_events` 是 job-scoped audit/event store，result explanation submit/review/publish 是低风险且 job-scoped 的下一步覆盖范围。
- 本次已消除的不确定性：确认 result explanation HTTP mutation paths 已有 `job:read` route boundary 和 `explanation:write` mutation scope，但缺少 audit context 注入；确认 submit/review/publish 均可写入 compact `ResultExplanation` audit envelope；确认重复 submit 与重复 publish 应保持幂等且不重复写审计事件。
- 本次采用的关键假设：本阶段只扩展 job-scoped selected mutation audit，不声明完整 all-mutation audit 完成；审计事件只记录 explanation id、状态、payload hash、resolved ref count、decision 和 before/after 摘要，不记录完整 `result_explanation.v1` payload；model catalog 等全局 mutation 需要后续全局 audit 存储/策略支持。
- 剩余不确定性：完整 all-mutation audit、model catalog/global mutation audit、全对象 tenant/project/site data scope、OIDC/JWKS、RBAC/ABAC、hosted security workflow green run 和完整 security golden scenario 仍未完成。
- 影响范围 / 风险控制方式：只修改 result explanation mutation audit 写入、focused tests、security smoke 和长期文档；不改变 HTTP route shape、OpenAPI、contracts、generated client、数据库 schema、result explanation idempotency、review/publish 状态流或 token schema。
- 验证方式：`cd apps\api; go test ./internal/compute -run TestHTTPResultExplanationAuditEvents -count=1` passed；`cd apps\api; go test ./...` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\security-smoke.ps1` passed and wrote `mutation_audit_events=covered_for_job_create_artifact_retention_and_result_explanation_events`；`scripts\check-deps.ps1` passed；docs/rebuild P0/P1/P2/schema scan completed；`git diff --check -- apps/api scripts/ci docs/architecture docs/rebuild tasks .ai` passed with LF/CRLF warnings only。
- 是否更新 README：是，更新 `apps/api/README.md`、`apps/api/internal/compute/README.md`、`scripts/ci/README.md`、`docs/architecture/current-state.md`、`docs/architecture/local-dev.md`、`docs/architecture/compute-api.md` 与 Certainty/Elegance Development Plan。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次是现有 selected audit coverage 扩展，不改变长期安全架构边界。

## 11:49 - Production token secret file source

- 用户目标：根据 Certainty/Elegance PRD 与开发计划继续完成剩余工作，并在大阶段完成后提交推送。
- 涉及目录：`apps/api/cmd/compute-api/`、`apps/api/internal/platform/config/`、`apps/api/internal/platform/auth/`、`scripts/ci/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`。
- 修改内容：为 Compute API 启动配置新增 `COMPUTE_API_TOKENS_FILE`；生产 token config 可从 inline JSON 或 file-mounted JSON secret 二选一读取；新增 ambiguous source、empty file、file source production guard 测试；扩展 security smoke 的 token file source 覆盖；更新相关 README、architecture/current-state/local-dev/compute-api、Certainty/Elegance plan 和任务记录。
- 修改原因：Certainty/Elegance 剩余安全缺口包含 service-token secret manager / issuer-JWKS 方向；在不引入 OIDC/RBAC 大改的前提下，先补齐低风险的 mounted secret file source，减少生产 token 必须以内联环境变量提供的限制。
- 本次已消除的不确定性：确认现有 static token JSON shape 可以复用；确认文件读取应位于 `cmd/compute-api` wiring，而不是 `internal/platform/auth`；确认 production guard 可对 file source 的有效 JSON 执行同样的默认 dev token 拒绝。
- 本次采用的关键假设：本阶段只推进 static-token P0 hardening，不声明 OIDC/JWKS、RBAC/ABAC、动态 token 管理、全对象 data scope 或 all-mutation audit 已完成；`COMPUTE_API_TOKENS_JSON` 与 `COMPUTE_API_TOKENS_FILE` 同时设置是配置错误。
- 剩余不确定性：完整 issuer/JWKS、外部 secret manager provider、全对象 tenant/project/site data scope、all-mutation audit、hosted security workflow green run 和完整 security golden scenario 仍未完成。
- 影响范围 / 风险控制方式：只修改启动配置读取、测试、security smoke 覆盖和长期文档；不改变 token JSON schema、HTTP auth principal/scope 行为、数据库 schema、OpenAPI、generated client 或业务 endpoint。
- 验证方式：`cd apps\api; go test ./cmd/compute-api ./internal/platform/... ./internal/compute` passed；PowerShell parser for `scripts\ci\security-smoke.ps1` passed；docs/rebuild P0/P1/P2/schema scan completed；`cd apps\api; go test ./...` passed；`scripts\ci\security-smoke.ps1` passed and wrote `security-smoke.json` with `production_service_token_file_source` coverage；`scripts\check-deps.ps1` passed；`git diff --check -- apps\api scripts docs tasks .ai` passed with LF/CRLF warnings only。
- 是否更新 README：是，更新 Compute API、cmd/config/auth、scripts/ci 与 architecture/Certainty-Elegance 文档。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次是 static-token P0 hardening slice，不改变长期认证策略边界。

## 10:51 - Desktop release artifacts local evidence lane

- 用户目标：根据 Certainty/Elegance PRD 与开发计划继续完成剩余工作，并在大阶段完成后提交推送。
- 涉及目录：`scripts/ci/`、`scripts/release/`、`scripts/`、`apps/desktop/packaging/`、`apps/desktop/scripts/`、`docs/architecture/`、`docs/rebuild/`、根 `README.md`、`Justfile`、`tasks/`。
- 修改内容：新增 `scripts/ci/desktop-release-artifacts-smoke.ps1`；新增 `just desktop-release-artifacts-smoke` 与 `just golden-scenarios-refresh-desktop-release`；更新根 README、scripts README、Desktop packaging/scripts README、release README、current-state/local-dev/desktop-runtime 与 Certainty/Elegance plan。
- 修改原因：Desktop package smoke 已覆盖 source-mode package/support bundle，但 Certainty/Elegance 剩余缺口仍包含 packaged worker、NSIS installer 和 release gate with real artifacts；现有 packaging scripts 可单独通过，需要一个根级本地 evidence lane 串联真实 unsigned sidecar、installer、release gate 与本地 artifact verifier。
- 本次已消除的不确定性：确认现有 `build-packaged-sidecar.ps1` 可构建 PyInstaller one-folder sidecar 并通过 sidecar smoke；确认 `build-nsis-installer.ps1` 可构建 unsigned NSIS installer 并通过 installer smoke；确认 `next-release-gates.ps1 -Mode release` 可使用真实 sidecar/installer paths 通过，不再依赖 missing-artifact dry run。
- 本次采用的关键假设：本阶段以本地真实 unsigned Desktop release artifact evidence 为低风险交付；wrapper 只编排既有脚本，不改变 Desktop runtime、worker、installer 或 release gate 业务逻辑；nested release gate 默认 `-SkipLong`，完整 build gate 可通过 `-RunLongReleaseGate` 显式开启；`-ReuseExistingArtifacts` 只用于长构建被外部超时中断后的恢复验证。
- 剩余不确定性：GitHub hosted artifact upload/download round trip 仍受 workflow 不在默认分支限制而无法触发；本轮 release gate evidence 在 dirty tracked worktree 下生成，提交后 clean-HEAD rerun 仍需后续补；signing、auto update、GitHub Release publication、full legacy auth、all-object data scope、all-mutation audit 和完整 8 条金标场景仍未完成。
- 影响范围 / 风险控制方式：只新增仓库级编排和长期文档，不修改业务 API、Desktop Rust runtime、Python worker、installer build logic 或 release gate 内部逻辑；所有生成 artifact/evidence 留在 `tmp/`，不提交二进制或运行产物。
- 验证方式：PowerShell parse check passed；第一次完整默认 `scripts\ci\desktop-release-artifacts-smoke.ps1` 在外部 20 分钟工具超时前已产出 passing packaged sidecar build/smoke 和 NSIS installer build/smoke evidence；随后 `scripts\ci\desktop-release-artifacts-smoke.ps1 -OutputDir tmp\desktop-packaging\local-release-20260606110553 -ReuseExistingArtifacts` passed，完成 Rust packaged-worker runtime smoke、release gate `Mode=release`、本地 unsigned artifact bundle verifier，并写出 `desktop-release-artifacts-smoke.json`；`scripts\ci\golden-scenarios.ps1` completed；`scripts\check-deps.ps1` passed；docs/rebuild P0/P1/P2/schema scan completed；`git diff --check -- README.md Justfile scripts apps docs tasks .ai` passed with LF/CRLF warnings only。
- 是否更新 README：是，更新根 README、`scripts/README.md`、`scripts/ci/README.md`、`scripts/release/README.md`、`apps/desktop/packaging/README.md`、`apps/desktop/scripts/README.md`、`docs/architecture/current-state.md`、`docs/architecture/local-dev.md`、`docs/architecture/desktop-runtime.md` 与 Certainty/Elegance Development Plan。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次是 release evidence 编排入口增强，不改变 signing/auto update 边界。

## 10:03 - Live backend browser hosted workflow entry

- 用户目标：根据 Certainty/Elegance PRD 与开发计划继续完成剩余工作，并在大阶段完成后提交推送。
- 涉及目录：`.github/`、`.github/workflows/`、`docs/architecture/`、`docs/rebuild/`、`scripts/ci/`、`tasks/`。
- 修改内容：新增 `.github/workflows/next-live-backend-browser-smoke.yml` manual/reusable workflow；将 live backend browser smoke 接入 `next-nightly.yml` 和 nightly summary；更新 `.github` / workflow README、`scripts/ci` README、current-state、local-dev 与 Certainty/Elegance plan。
- 修改原因：live backend browser lane 已有本地脚本和 evidence，但缺少可触发 GitHub Actions hosted evidence 的专用入口；上一阶段已完成 current-flow live hosted entry，本阶段补齐另一条 live browser lane 的 hosted/manual 入口。
- 本次已消除的不确定性：确认该 hosted lane 可复用现有 `scripts/ci/live-backend-browser-smoke.ps1`，workflow 只负责依赖安装、Docker Compose availability check、脚本调用和 evidence artifact 上传；确认 nightly 可通过 `workflow_call` 聚合该 lane。
- 本次采用的关键假设：Ubuntu runner 是 live backend browser hosted lane 的保守选择，因为它提供 Docker Compose 与 PowerShell Core；本阶段只增加 hosted workflow entry，不声称已有 hosted green run；完整 legacy authenticated browser session 与 UI current-flow submit 到 live worker 仍由其他 lane 或后续工作覆盖。
- 剩余不确定性：验证尚未运行；新 workflow 尚未在 GitHub Actions 实际执行；完整 legacy authenticated backend browser session、release artifact 真实上传/下载、签名/安装器、全对象 data scope、all-mutation audit 和完整 8 条金标场景仍未完成。
- 影响范围 / 风险控制方式：只新增/修改 workflow 与长期文档，不修改业务 API、数据库 schema、worker runtime、frontend route UI、generated client 或 smoke 脚本；workflow 调用既有仓库脚本，复杂逻辑不内联到 YAML。
- 验证方式：YAML parser checks passed，确认新 workflow 与 nightly `workflow_call` / summary wiring；`scripts\ci\live-backend-browser-smoke.ps1` passed；`scripts\check-deps.ps1` passed；docs/rebuild P0/P1/P2/schema scan completed；`git diff --check -- .github docs scripts tasks .ai` passed with LF/CRLF warnings only；本阶段未改 frontend 源码，未重复运行 `cd frontend; npx tsc --noEmit`。
- 是否更新 README：是，更新 `.github/README.md`、`.github/workflows/README.md`、`scripts/ci/README.md`、`docs/architecture/current-state.md`、`docs/architecture/local-dev.md` 与 Certainty/Elegance Development Plan。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次是 hosted evidence orchestration entry，不改变长期架构决策。

## 09:43 - Current-flow live hosted workflow entry

- 用户目标：根据 Certainty/Elegance PRD 与开发计划继续完成剩余工作，并在大阶段完成后提交推送。
- 涉及目录：`.github/`、`.github/workflows/`、`docs/architecture/`、`docs/rebuild/`、`scripts/ci/`、`tasks/`。
- 修改内容：新增 `.github/workflows/next-current-flow-live-smoke.yml` manual/reusable workflow；将 current-flow live smoke 接入 `next-nightly.yml` 和 nightly summary；更新 `.github` / workflow README、`scripts/ci` README、current-state、local-dev 与 Certainty/Elegance plan。
- 修改原因：上一阶段已完成本地 current-flow live smoke，但缺少可触发 GitHub Actions hosted evidence 的专用入口；Certainty/Elegance 计划要求把 local lanes 逐步提升为可追溯 hosted/manual evidence。
- 本次已消除的不确定性：确认该 hosted lane 可复用现有 `scripts/ci/current-flow-live-smoke.ps1`，workflow 只负责依赖安装、Docker Compose availability check、脚本调用和 evidence artifact 上传；确认 nightly 可通过 `workflow_call` 聚合该 lane。
- 本次采用的关键假设：Ubuntu runner 是 current-flow live hosted lane 的保守选择，因为它提供 Docker Compose 与 PowerShell Core；本阶段只增加 hosted workflow entry，不声称已有 hosted green run；完整 legacy authenticated browser session 仍是单独缺口。
- 剩余不确定性：验证尚未运行；新 workflow 尚未在 GitHub Actions 实际执行；live backend browser read lane 仍缺 dedicated hosted workflow；完整 legacy authenticated backend browser session、release artifact 真实上传/下载、签名/安装器、全对象 data scope、all-mutation audit 和完整 8 条金标场景仍未完成。
- 影响范围 / 风险控制方式：只新增/修改 workflow 与长期文档，不修改业务 API、数据库 schema、worker runtime、frontend route UI、generated client 或 smoke 脚本；workflow 调用既有仓库脚本，复杂逻辑不内联到 YAML。
- 验证方式：YAML parser checks passed，确认新 workflow 与 nightly `workflow_call` / summary wiring；`scripts\ci\current-flow-live-smoke.ps1` passed；`scripts\check-deps.ps1` passed；docs/rebuild P0/P1/P2/schema scan completed；`git diff --check -- .github docs scripts tasks .ai` passed with LF/CRLF warnings only；本阶段未改 frontend 源码，未重复运行 `cd frontend; npx tsc --noEmit`。
- 是否更新 README：是，更新 `.github/README.md`、`.github/workflows/README.md`、`scripts/ci/README.md`、`docs/architecture/current-state.md`、`docs/architecture/local-dev.md` 与 Certainty/Elegance Development Plan。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次是 hosted evidence orchestration entry，不改变长期架构决策。

## 08:32 - AutoWaterSimu Next current-flow live smoke

- 用户目标：根据 Certainty/Elegance PRD 与开发计划继续完成剩余工作，并在大阶段完成后提交推送。
- 涉及目录：`frontend/tests/`、`scripts/ci/`、`scripts/`、`docs/architecture/`、`docs/rebuild/`、根 README、`Justfile`、`tasks/`。
- 修改内容：新增 current-flow live Playwright spec；新增 `scripts/ci/current-flow-live-smoke.ps1`；新增 `just current-flow-live-smoke` 与 `just golden-scenarios-refresh-current-flow-live`；将 `current_flow_live` lane 纳入 golden scenario summary；更新相关 README、current-state、local-dev 与 Certainty/Elegance plan。
- 修改原因：上一阶段 live backend browser lane 证明了前端能读取真实 PostgreSQL/MinIO/worker-backed job/result/evidence/ref，但它从 integration-prepared job 开始；Certainty/Elegance 剩余缺口仍需要本地 UI current-flow submit -> live worker -> evidence 的单场景证明。
- 本次已消除的不确定性：确认该缺口可通过独立 opt-in smoke 收敛，不需要修改 API、worker runtime、generated client、route UI 或 feature query wrappers；确认 UI current-flow live smoke 需要完整覆盖 simulation hours 的 time segment，修正后 worker 可完成 job 并生成 artifact/model_run；确认 golden scenario summary 可把 `current_flow_live` 作为 current-flow scenario 的独立 evidence source。
- 本次采用的关键假设：本阶段以本地 opt-in live lane 作为低风险交付；legacy `/api/v1/users/me` 继续 mock，因为完整 legacy authenticated backend session 是单独缺口；hosted workflow evidence 不在本地阶段内声称完成。
- 剩余不确定性：hosted workflow green run 未触发；完整 legacy authenticated backend browser session、release artifact 真实上传/下载、签名/安装器、全对象 data scope、all-mutation audit 和完整 8 条金标场景仍未完成。
- 影响范围 / 风险控制方式：只新增测试与仓库级 evidence 编排，不修改业务 API、数据库 schema、worker 实现、frontend route UI、feature wrappers 或 generated client；live script 使用隔离 Compose project 并默认清理容器。
- 验证方式：PowerShell parse checks for `scripts/ci/current-flow-live-smoke.ps1` and `scripts/ci/golden-scenarios.ps1` passed；`cd frontend; npx tsc --noEmit` passed；`scripts\ci\current-flow-live-smoke.ps1` passed；`scripts\ci\golden-scenarios.ps1` passed；`scripts\ci\golden-scenarios.ps1 -RunCurrentFlowLiveSmoke` passed；`scripts\ci\golden-scenarios.ps1 -RefreshLocalEvidence -RunCurrentFlowLiveSmoke` passed with 7 refresh steps and summary `partial=7` / `missing=1` / `blocked=0`；`scripts\check-deps.ps1` passed；`scripts\ci\pr-fast.ps1` passed；docs/rebuild P0/P1/P2/schema search completed；`git diff --check -- frontend scripts docs README.md Justfile tasks .ai` passed with LF/CRLF warnings only。
- 是否更新 README：是，更新根 README、`scripts/README.md`、`scripts/ci/README.md`、`frontend/tests/README.md`、`docs/architecture/current-state.md`、`docs/architecture/local-dev.md` 与 Certainty/Elegance Development Plan。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次是 evidence lane 增量，不改变长期架构决策。

## 08:14 - AutoWaterSimu Next live backend browser evidence

- 用户目标：根据 Certainty/Elegance PRD 与开发计划继续完善剩余工作，并在阶段完成后提交推送。
- 涉及目录：`frontend/tests/`、`scripts/ci/`、`scripts/`、`docs/architecture/`、`docs/rebuild/`、根 README、`Justfile`、`tasks/`。
- 修改内容：新增 live backend browser Playwright spec；新增 `scripts/ci/live-backend-browser-smoke.ps1`；新增 `just live-backend-browser-smoke` 与 `just golden-scenarios-refresh-live`；将 `live_backend_browser` lane 纳入 golden scenario summary；更新相关 README、current-state、local-dev 和 Certainty/Elegance plan。
- 修改原因：现有 integration smoke 能证明真实 PostgreSQL/MinIO/worker-backed API 链路，mock-backed browser smoke 能证明 UI 编排，但缺少前端在不 mock Compute API 时读取真实 job/result/evidence/ref 的 evidence。
- 本次已消除的不确定性：确认 Compute Jobs route 可在真实 Compute API 上读取 integration-prepared succeeded job、result、artifact evidence package 和 model_run evidence ref；确认新增 golden summary flag 可刷新并读取该 lane；确认本阶段不需要修改 API、worker、generated client 或 route UI。
- 本次采用的关键假设：live backend browser read lane 可以先从 integration-prepared succeeded job 开始，作为 current-flow golden scenario 的 partial evidence；legacy `/api/v1/users/me` 仍可 mock，因为完整 legacy authenticated browser session 是单独缺口。
- 剩余不确定性：hosted workflow green run 未触发；尚未完成 UI current-flow submit -> live worker -> evidence 的单场景闭环；release artifact 真实上传/下载、签名/安装器、全对象 data scope、all-mutation audit 和完整 8 条金标场景仍未完成。
- 影响范围 / 风险控制方式：只新增测试与仓库级 evidence 编排，不修改业务 API、数据库 schema、worker runtime、route UI、feature wrappers 或 generated client；live script 使用隔离 Compose project 并在默认路径清理容器和 volume。
- 验证方式：PowerShell parse checks for `scripts/ci/live-backend-browser-smoke.ps1` and `scripts/ci/golden-scenarios.ps1` passed；`cd frontend; npx tsc --noEmit` passed；`scripts\ci\live-backend-browser-smoke.ps1` passed；`scripts\ci\golden-scenarios.ps1` passed；`scripts\ci\golden-scenarios.ps1 -RunLiveBackendBrowserSmoke` passed；`scripts\check-deps.ps1` passed；`scripts\ci\pr-fast.ps1` passed；docs/rebuild P0/P1/P2/schema search completed；`git diff --check -- frontend scripts docs README.md Justfile tasks .ai` passed with LF/CRLF warnings only。
- 是否更新 README：是，更新根 README、`scripts/README.md`、`scripts/ci/README.md`、`frontend/tests/README.md`、`docs/architecture/current-state.md`、`docs/architecture/local-dev.md` 与 Certainty/Elegance Development Plan。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次是 evidence lane 增量，不改变长期架构决策。

## 07:18 - Golden scenario local evidence refresh

- 用户目标：根据 Certainty/Elegance PRD 与开发计划继续完善和优化 AutoWaterSimu Next，完成一个合理的大阶段并适时提交推送。
- 涉及目录：`scripts/ci/`、`scripts/`、`docs/architecture/`、`docs/rebuild/`、根 `README.md`、`Justfile`、`tasks/`、`.ai/changes/`。
- 修改内容：为 `scripts/ci/golden-scenarios.ps1` 新增 `-RefreshLocalEvidence` 和独立 lane refresh 开关；新增 `just golden-scenarios-refresh`；更新本地开发、脚本、current-state 与 Certainty/Elegance 计划文档；记录本轮任务计划。
- 修改原因：现有 golden scenario 脚本只能读取已有 evidence，开发者仍需手工按顺序刷新多个本地 lanes，容易让 scenario summary 使用 stale evidence。显式刷新模式把非 Docker 本地 lanes 编排成一个可复用入口，同时保留默认 summary-only 行为。
- 本次已消除的不确定性：确认 Phase 0/1 入口、依赖边界、frontend feature query、Go API domain split、security/browser/Desktop package/local release smoke 等基础已存在；确认当前对齐缺口是 scenario-level evidence 的刷新与解释成本，而不是继续横向新增 endpoint。
- 本次采用的关键假设：本阶段以本地非 Docker evidence refresh 作为低风险大阶段；Docker integration smoke 仍需显式 `-RunIntegrationSmoke`，避免默认命令启动 Compose；不把 partial scenario evidence 解释为 8 条金标场景完成。
- 剩余不确定性：真实 GitHub hosted integration/browser/security/Desktop/release green evidence 仍需实际 workflow run；live PostgreSQL/MinIO/worker backend browser reads、真实 release artifact upload/download、OIDC/RBAC/all-object scope/all-mutation audit 仍未完成。
- 影响范围 / 风险控制方式：只改仓库级编排脚本和长期文档；不修改 API、contracts、generated client、frontend route、worker、desktop runtime 或数据库 schema；不提交 `tmp/` evidence；保留未跟踪的旧 95 分文档不动。
- 验证方式：已运行 PowerShell parse check for `scripts\ci\golden-scenarios.ps1`；`scripts\ci\golden-scenarios.ps1` summary-only 通过并写出 `refresh_requested=false`、`partial=7`、`missing=1`；`scripts\ci\golden-scenarios.ps1 -RefreshLocalEvidence` 通过，6 个 refresh steps 全部 passed；`rg -n "schema_version|P0|P1|P2" docs\rebuild --glob "AutoWaterSimu_Next_*.md"` 已检查；`git diff --check -- scripts docs README.md Justfile tasks .ai` 通过（仅 LF/CRLF warning）。
- 是否更新 README：是，更新根 README、`scripts/README.md`、`scripts/ci/README.md`、`docs/architecture/local-dev.md`、`docs/architecture/current-state.md` 与 Certainty/Elegance 开发计划。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本次是 evidence 编排入口增强，不改变长期架构决策。

## 07:39 - Integration-backed golden scenario refresh entry

- 用户目标：继续依据 Certainty/Elegance PRD 与开发计划推进质量提升，做完一个阶段并提交推送。
- 涉及目录：根 `Justfile`、根 `README.md`、`docs/architecture/`、`docs/rebuild/`、`scripts/`、`scripts/ci/`、`tasks/`、`.ai/changes/`。
- 修改内容：新增 `just golden-scenarios-refresh-integration`，调用 `scripts\ci\golden-scenarios.ps1 -RefreshLocalEvidence -RunIntegrationSmoke`；更新本地开发、脚本、current-state 和 Certainty/Elegance 计划文档；记录本轮任务计划。
- 修改原因：上一阶段已提供非 Docker 本地 evidence refresh，但 Docker integration 仍只能通过记忆脚本参数触发；金标场景中的 PostgreSQL/worker/artifact evidence 需要一个明确根级入口。
- 本次已消除的不确定性：确认 `-RunIntegrationSmoke` 已存在且可以复用 `integration-smoke.ps1 -StartCompose`；确认此入口只提升本地 integration-backed scenario evidence，不替代 hosted workflow、live browser 或 release artifact evidence。
- 本次采用的关键假设：Docker integration smoke 可作为本地当前 HEAD integration evidence；该入口应保持显式，不并入默认非 Docker refresh；仍不宣称 8 条金标场景完成。
- 剩余不确定性：真实 hosted integration green run、live frontend/backend browser reads、PostgreSQL migration up/down release-gate step、真实 unsigned release artifact round trip、full production data scope / all-mutation audit 仍未完成。
- 影响范围 / 风险控制方式：仅新增根级任务入口与文档；不修改业务代码、API、schema、generated client、数据库 migration、worker 或 Desktop runtime；不提交 `tmp/` evidence。
- 验证方式：已运行 `scripts\ci\golden-scenarios.ps1 -RefreshLocalEvidence -RunIntegrationSmoke`，7 个 refresh step 全部 passed，summary 为 `partial=8` / `missing=0` / `blocked=0`；`tmp\ci-evidence\integration-smoke.json` 显示 integration smoke passed、9 steps、0 failed；Docker Compose project cleanup 后无 `autowatersimu-next-integration-smoke` 容器残留；`git diff --check -- README.md Justfile docs scripts tasks .ai` 通过（仅 LF/CRLF warning）；docs/rebuild P0/P1/P2/schema search 已运行。
- 是否更新 README：是，更新根 README、`docs/architecture/local-dev.md`、`docs/architecture/current-state.md`、`scripts/README.md`、`scripts/ci/README.md` 与 Certainty/Elegance 开发计划。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本次是现有 evidence lane 的 root entry 增强，不改变架构决策。
