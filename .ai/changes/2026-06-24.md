## 22:14 - Restore standalone legacy landing and sidebar shell

- 用户目标：恢复旧 FastAPI 前端的 landing page 和左侧 Sidebar 风格，同时保持 standalone 无登录、无鉴权、无 `/users/me`。
- 涉及目录：`frontend/src`、`frontend/src/routes`、`frontend/src/components`、`tasks`。
- 修改内容：standalone 根路由 `/` 重新使用 legacy landing；standalone route tree 增加 landing 支撑页面、`/dashboard` 和 Petersen 教程；工作台改用 standalone-safe legacy Sidebar shell；landing CTA 和 embed click redirect 在 standalone 下进入 `/dashboard`；更新相关 README。
- 修改原因：当前 standalone 顶部导航与 legacy FastAPI 前端视觉差异较大，但直接恢复旧登录/用户/管理页面会违反 standalone no-auth 边界。
- 本次已消除的不确定性：确认旧 landing 是 `PosthogLanding`；确认管理页不恢复；确认 `useAuth`、login/users/admin/items/settings 未进入 standalone 静态依赖图。
- 本次采用的关键假设：landing 的登录/注册 CTA 在 standalone 下应进入 `/dashboard`；legacy 模式继续保留 `/login` 行为。
- 剩余不确定性：未做完整人工跨页面视觉走查；只做了根页到 dashboard 的最小浏览器验收。
- 影响范围 / 风险控制方式：仅前端 route/shell/landing 跳转和文档更新；不改后端 API、OpenAPI client、数据库 schema 或认证模式；不加入 admin/items/settings/super-dashboard。
- 验证方式：`cd frontend; npx tsc --noEmit` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-frontend-standalone-compute-boundary.ps1` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\standalone-five-model-smoke.ps1` 通过；Playwright 最小视觉验收通过并生成 `tmp/standalone-landing-dashboard-smoke.png`。
- 是否更新 README：是，更新 `frontend/README.md`、`frontend/src/README.md`、`frontend/src/routes/README.md`、`frontend/src/components/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否。

## 22:23 - Make standalone sidebar logo return to landing

- 用户目标：点击 sidebar 的 logo 回到 landing page。
- 涉及目录：`frontend/src/components/Common`。
- 修改内容：将 standalone Sidebar logo 链接从 `/dashboard` 改为 `/`。
- 修改原因：standalone 根路由已恢复为 legacy landing，logo 应返回 public landing 而不是工作台首页。
- 本次已消除的不确定性：确认目标链接位于 `StandaloneSidebar.tsx`，不涉及 legacy `Sidebar.tsx`。
- 本次采用的关键假设：用户指的是 standalone 工作台左侧栏 logo。
- 剩余不确定性：无。
- 影响范围 / 风险控制方式：单点链接目标变更，不改 route tree、认证、API 或菜单项。
- 验证方式：`cd frontend; npx tsc --noEmit` 通过；`scripts\audit-frontend-standalone-compute-boundary.ps1` 通过；Playwright 最小点击验收通过（`/dashboard` 点击 sidebar logo 返回 `/`，且无 login/users 请求）。
- 是否更新 README：否。
- 是否需要新增或更新架构决策（.ai/decisions/）：否。

## 23:17 - Localize compute operation pages and widen model governance layout

- 用户目标：计算任务、模型治理、计算生命周期页面补中文；模型治理页面横向平铺，不居中收窄。
- 涉及目录：`frontend/src/routes/_layout`。
- 修改内容：三个 Compute operation route 使用当前 i18n language 切换页面内中文 UI 文案；模型治理 `Container` 从 `maxW="7xl"` 改为 `maxW="full"`，与相邻 Compute 页面一致。
- 修改原因：这些页面此前主要使用英文硬编码，中文环境下页面正文仍显示英文；模型治理页宽度与计算任务、计算生命周期不一致。
- 本次已消除的不确定性：确认目标 route 为 `compute-jobs.tsx`、`model-governance.tsx`、`compute-lifecycle.tsx`；确认 sidebar 导航已有中文，缺失集中在页面正文硬编码。
- 本次采用的关键假设：后端状态值、schema 示例、`model_run:...` 这类契约/引用格式示例不翻译，以免影响排查和复制使用。
- 剩余不确定性：未做浏览器截图人工验收；仅通过静态扫描确认主要可见英文标签已替换。
- 影响范围 / 风险控制方式：仅 route 层文案和容器宽度；不改 API wrapper、generated client、后端、数据结构或 route tree。
- 验证方式：`cd frontend; npx tsc --noEmit` 通过；`rg` 扫描三页剩余可见英文，仅保留 `model_run:...` 示例占位符。
- 是否更新 README：否。
- 是否需要新增或更新架构决策（.ai/decisions/）：否。
