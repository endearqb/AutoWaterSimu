## 00:20 - Release artifact download verifier smoke

- 用户目标：继续根据 Certainty/Elegance PRD 与 Development Plan 完善 Next 重构，并推进阶段性可验证 evidence。
- 涉及目录：`scripts/release/`、`.github/workflows/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`、根 README / Justfile。
- 修改内容：新增 `scripts/release/smoke-release-artifact-download.ps1`，用临时 fixture 覆盖 release artifact download verifier 的成功路径与缺失 installer 的失败路径；新增 `just release-artifact-download-smoke`；将 smoke 接入 `next-release-gates.yml`；更新相关 README、current-state、Certainty/Elegance checklist 和任务复盘。
- 修改原因：`next-release-gates.yml` 已支持真实 unsigned artifact 上传/下载校验，但缺少一个不依赖真实二进制 artifact 的本地/CI 可重复 verifier smoke。
- 本次已消除的不确定性：确认 release workflow 已有真实 artifact upload/download/verify 编排；确认新增 fixture smoke 能验证 `verify-release-artifact-download.ps1` 的通过与失败行为；确认 release gate dry run 和 pr-fast 不受影响。
- 本次采用的关键假设：fixture smoke 只证明校验器逻辑，不等价于真实 GitHub hosted release-evidence green；真实 artifact round trip 仍需要 `workflow_dispatch build_release_artifacts=true`。
- 剩余不确定性：尚未触发 hosted GitHub Actions release-evidence run；packaged worker exe、NSIS installer、签名、auto update 和 GitHub Release publication 仍未完成生产发布验证。
- 影响范围 / 风险控制方式：只新增 release automation smoke 和文档记录，不修改业务接口、OpenAPI、contracts、迁移、生成客户端或发布策略；fixture 文件写入 `tmp/`。
- 验证方式：`scripts\release\smoke-release-artifact-download.ps1` passed；PyYAML workflow parser check passed；`scripts\release\next-release-gates.ps1 -Mode merge -SkipLong` passed；`scripts\ci\pr-fast.ps1` passed；`git diff --check -- .github Justfile README.md docs scripts tasks .ai` passed with LF/CRLF warnings only。
- 是否更新 README：是，更新 root README、`.github` README、`.github/workflows` README、`scripts` README、`scripts/release` README。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；仍沿用 `0008-release-gate-artifact-boundary.md` 与 `0011-desktop-release-signing-auto-update-boundary.md`。

## 00:37 - Nightly evidence workflow

- 用户目标：继续根据 Certainty/Elegance PRD 与 Development Plan 完善 Next 重构，并推进 CI lanes 产品化。
- 涉及目录：`.github/workflows/`、`.github/`、`scripts/ci/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`。
- 修改内容：为 `next-pr-fast.yml` 增加 `workflow_call`；新增 `next-security-smoke.yml`；新增 scheduled/manual `next-nightly.yml`，组合 pr-fast、integration、browser、security 和 Desktop package smoke，并上传 nightly summary evidence；更新相关 README、current-state、Certainty/Elegance checklist 和任务复盘。
- 修改原因：Development Plan 的 Testing / CI 清单仍缺 `nightly`，且 security smoke 只有本地/Justfile 入口，没有 hosted reusable workflow 入口。
- 本次已消除的不确定性：确认 integration/browser/Desktop package workflow 已有 `workflow_call`；确认 PR fast 可安全增加 reusable 入口；确认 nightly summary 能引用所有 called workflow 的结果。
- 本次采用的关键假设：nightly 是 scheduled evidence orchestrator，不替代 release-evidence；新增 workflow 文件和本地 parser 通过不等于 hosted green run 已完成。
- 剩余不确定性：尚未触发 GitHub Actions nightly run；integration/browser/Desktop/security 的 hosted green evidence 仍需实际 workflow run；release artifact build/download 仍由 manual release gate path 证明。
- 影响范围 / 风险控制方式：只改 GitHub workflow 编排和文档，不修改业务代码、OpenAPI、contracts、生成客户端或发布策略；复用已有 smoke 脚本，避免在 YAML 内联业务逻辑。
- 验证方式：PyYAML workflow parser check passed；`scripts\ci\security-smoke.ps1` passed；`scripts\release\smoke-release-artifact-download.ps1` passed；`scripts\ci\pr-fast.ps1` passed。
- 是否更新 README：是，更新 `.github` README、`.github/workflows` README、`scripts/ci` README 和 `docs/architecture/current-state.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次是 CI 编排补齐，不改变长期发布/安全策略。

## 01:00 - Tenant/project read-scope enforcement

- 用户目标：继续根据 Certainty/Elegance PRD 与 Development Plan 完善 Next 重构，并推进 production security 中的数据隔离证据。
- 涉及目录：`apps/api/`、`apps/api/internal/compute/`、`scripts/ci/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`。
- 修改内容：为静态 token record/principal 增加可选 `tenant_id`、`project_id` 和前向兼容 `site_id`；对 HTTP job list/get 与 artifact download read path 增加 tenant/project scope 过滤或拒绝；为 memory/Postgres list 查询补 tenant/project filter；新增 scoped job read 与 artifact download Go 测试；扩展 `security-smoke.ps1` 覆盖摘要；同步更新 API/compute、scripts/ci、current-state、Certainty/Elegance plan 和任务复盘。
- 修改原因：此前 security smoke 只覆盖 token guard、scope denial、revocation、selected mutation audit 与 admin scope，尚未对真实数据对象 read path 做 tenant/project 隔离验证。
- 本次已消除的不确定性：确认 job metadata 已有 tenant/project 字段，artifact metadata 可回溯 job_id，因此无需变更 OpenAPI、contracts 或数据库 schema 即可在 job list/get 和 artifact download 上落地最小读隔离切片；确认空 token scope 可保留 existing dev/admin token 的全局访问兼容性。
- 本次采用的关键假设：本次只做 tenant/project read scope；`site_id` 只进入 token/principal 作为后续兼容字段，不在缺少 job/site metadata enforcement 之前声明已覆盖；空 tenant/project 代表 global principal。
- 剩余不确定性：site scope、全对象 data scope、all-mutation audit、OIDC/JWKS、service-token secret manager、ontology-backed runtime policy enforcement 和所有 mutation path 的对象级授权仍未完成。
- 影响范围 / 风险控制方式：保持变更在 API internal compute 与 security smoke 内，不修改公开响应 schema、OpenAPI、contracts、迁移或生成客户端；新增测试锁定允许/拒绝行为；Development Plan 继续保留 security checklist 未完成。
- 验证方式：`cd apps\api; go test ./internal/compute -run "Test(HTTPJobReadTenantProjectScope|HTTPArtifactDownloadTenantProjectScope)" -count=1` passed；`cd apps\api; go test ./...` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\security-smoke.ps1` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` passed；`git diff --check -- apps\api scripts docs tasks .ai` passed with LF/CRLF warnings only。
- 是否更新 README：是，更新 `apps/api/README.md`、`apps/api/internal/compute/README.md`、`scripts/ci/README.md` 和 `docs/architecture/current-state.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次是既有 static-token security 机制上的最小 read-scope enforcement slice，未改变长期认证/授权架构决策。

## 01:25 - Site read-scope enforcement

- 用户目标：继续根据 Certainty/Elegance PRD 与 Development Plan 完善 Next 重构，并把 production security 的数据隔离从 tenant/project read-scope 推进到 tenant/project/site read-scope。
- 涉及目录：`contracts/`、`apps/api/`、`apps/api/internal/compute/`、`apps/api/migrations/`、`frontend/src/client/compute/`、`scripts/ci/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`。
- 修改内容：为 `compute_job.v1.context` 和 `simulation_request.v1.metadata` 增加可选 `site_id`；新增 site-scoped compute job fixture 并登记 registry；新增 `0010_job_site_scope` migration；Go API 持久化 `JobRecord.SiteID`，从 simulation request metadata 或 `external_refs.site_id` 传递到 compute job context，并在 job list/get 与 artifact download read path 上执行 tenant/project/site scope 过滤；生成 Compute TS client 的 `JobRecord.site_id`；扩展 security smoke；同步更新 README、current-state、Certainty/Elegance plan 和任务记录。
- 修改原因：上一阶段 token config 已接受 `site_id` 但未有 job/site metadata enforcement；Development Plan 的 read-scope 验收要求 tenant/project/site list/get 过滤或明确豁免。
- 本次已消除的不确定性：确认 `site_id` 可以作为 v1 可选合同字段添加，不破坏现有 required 字段；确认 `simulation_request.external_refs.site_id` 可作为已有 NewSystem/milp 语义来源；确认 artifact download 可继续通过 artifact job_id 回溯 job scope。
- 本次采用的关键假设：`site_id` 仍只作为 job metadata read-scope，不代表完整 WaterStation/Site domain model；空 tenant/project/site token scope 保持 global access；本次不覆盖全对象授权或 mutation authorization。
- 剩余不确定性：完整 OIDC/JWKS、RBAC/ABAC、全对象 tenant/project/site data scope、ontology-backed policy enforcement 和 all-mutation audit 仍未完成；hosted security workflow green run 仍需 GitHub Actions 实际执行。
- 影响范围 / 风险控制方式：`site_id` 是可选 additive v1 字段；新增 forward migration 和 rollback；保持 API response backward-compatible；用 focused Go 测试锁定 cross-tenant 与 cross-site deny 行为；不修改 worker execution payload semantics。
- 验证方式：`cd apps\api; go test ./internal/compute -run "Test(HTTPJobReadTenantProjectSiteScope|HTTPArtifactDownloadTenantProjectSiteScope|NewSystemEvidenceReferenceE2E|SimulationCheckEndpointCreatesComputeJob|BenchmarkCaseScheduleRunEndpoint)" -count=1` passed；`cd apps\api; go test ./...` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\security-smoke.ps1` passed；`backend\.venv\Scripts\python -m pytest contracts\tests -q` passed；`cd frontend; npx tsc --noEmit` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-contracts.ps1` passed after commit；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` passed；`git diff --check -- contracts apps\api frontend\src\client\compute scripts docs tasks .ai` passed with LF/CRLF warnings only。
- 是否更新 README：是，更新 `contracts/README.md`、`apps/api/README.md`、`apps/api/internal/compute/README.md`、`apps/api/migrations/README.md`、`scripts/ci/README.md` 和 `docs/architecture/current-state.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次沿用现有 static-token/read-scope 与 contracts additive v1 policy。

## 01:43 - Service constructor boundary audit

- 用户目标：继续根据 Certainty/Elegance PRD 与 Development Plan 完善 Next 重构，并推进 Go API modular monolith / Store constructor narrowing。
- 涉及目录：`apps/api/`、`apps/api/internal/compute/`、`scripts/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`。
- 修改内容：将 `ArtifactLifecycleService`、`SimulationInputService`、`ModelGovernanceService` 和 `EvidenceGovernanceService` 的内部构造函数参数收敛为明确的领域 repository bundle / object-store bundle；保持 service 内部字段仍为最小接口；扩展 `scripts/audit-compute-api-boundary.ps1`，记录 `service_constructor_boundaries` 并在内部 domain service constructor 接收 aggregate `Store` 或超过 3 个 store-like 参数时失败；同步更新 API/compute、scripts、compute-api architecture、current-state、Certainty/Elegance checklist 和任务记录。
- 修改原因：各 domain service 行为已委派到窄边界，但若审计不覆盖构造函数，后续重构容易重新把巨型 `Store` 泄漏回内部 service constructor。
- 本次已消除的不确定性：确认当前内部 domain service constructors 可在不改变 HTTP/API 行为的前提下收敛到 1-3 个 store-like 参数；确认 audit 能枚举 10 个 service constructor 并对聚合 Store / 过宽参数列表做失败门禁。
- 本次采用的关键假设：公共 `NewService` / `NewServiceWithArchive` 在 `internal/compute` 未拆包前继续作为兼容 wiring 接收 aggregate `Store`；本次只收窄内部 domain service constructor，不做 package movement、不改变 OpenAPI/contracts/migrations。
- 剩余不确定性：Go API domain package split、handler/package surface reduction、公共 `Service` 构造签名收窄和 `service.go` / `http.go` / `store.go` / `postgres.go` 大文件拆分仍未完成。
- 影响范围 / 风险控制方式：仅修改内部 constructor wiring 与只读审计脚本；保持公开 HTTP contract、auth scope、schema、migration、generated client 不变；用 audit 和 focused Go tests 验证无行为回归。
- 验证方式：`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1` passed（41 Store methods、12 embedded interfaces、12 domain groups、10 service constructors audited）；`cd apps\api; go test ./internal/compute -run "Test(NewSystemEvidenceReferenceE2E|SimulationCheckEndpointCreatesComputeJob|BenchmarkCaseScheduleRunEndpoint|HTTPArtifactDownloadTenantProjectSiteScope)" -count=1` passed；`cd apps\api; go test ./...` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` passed，evidence status `passed`；`git diff --check -- apps\api scripts docs tasks .ai` passed with LF/CRLF warnings only。
- 是否更新 README：是，更新 `apps/api/README.md`、`apps/api/internal/compute/README.md`、`scripts/README.md` 和 `docs/architecture/current-state.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次是既有 modular monolith / Store split 路线下的内部边界门禁，没有新增长期架构决策。

## 02:00 - Platform HTTP package split

- 用户目标：继续根据 Certainty/Elegance PRD 与 Development Plan 完善 Next 重构，并开始 Go API package movement。
- 涉及目录：`apps/api/internal/platform/`、`apps/api/internal/compute/`、`apps/api/`、`scripts/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`。
- 修改内容：新增 `apps/api/internal/platform/httpx`，承接 JSON response 和本地 loopback CORS；新增 platform/httpx 目录 README 与 helper 直接测试；`compute/http.go` 改用 `httpx.WithLocalCORS`，`compute/errors.go` 通过 `httpx.WriteJSON` 保持原有错误输出路径；扩展 `scripts/audit-compute-api-boundary.ps1` 记录 `package_boundaries` 并校验 `compute` 与 `platform/httpx` package dirs；同步更新 API/internal/compute/scripts README、compute-api architecture、current-state、Certainty/Elegance plan 和任务复盘。Prometheus label escaping 后续在同一工作集中移动到 `platform/metrics`。
- 修改原因：服务构造器边界已经收窄，但 `internal/compute` 仍未开始 Go package movement。先抽出无领域状态的 HTTP platform helper，可以在不改变 API 行为的前提下建立第一条 package boundary。
- 本次已消除的不确定性：确认 CORS 与 JSON response 不需要 compute domain types；确认平台 helper package 可以被直接测试；确认 audit 能把第一块 package split 作为可验证 evidence，而不是只靠文档声明。
- 本次采用的关键假设：本次只移动 platform HTTP helper，不把 `Go API domain package split` 视为完成；domain packages（jobs、artifacts、models、evidence、simulation、agent、workers）仍需后续逐步迁移。
- 剩余不确定性：`apps/api/internal/compute` 仍是主要大包；`service.go`、`http.go`、`store.go`、`postgres.go` 仍是大文件拆分目标；handler/package surface reduction、公共 `Service` 构造签名收窄和真正 domain package movement 尚未完成。
- 影响范围 / 风险控制方式：不修改 routes、OpenAPI、contracts、auth scopes、数据库迁移、生成客户端或 HTTP response shape；只移动平台 helper 并保留 compute 的 `WriteError` / `AppError` 映射；新增直接测试和 audit guardrail。
- 验证方式：`cd apps\api; go test ./internal/platform/httpx ./internal/compute` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1` passed（package dirs 为 `compute`、`platform/httpx`）；`cd apps\api; go test ./...` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` passed，evidence status `passed`；`git diff --check -- apps\api docs scripts tasks .ai` passed with LF/CRLF warnings only。
- 是否更新 README：是，新增 `apps/api/internal/platform/README.md`、`apps/api/internal/platform/httpx/README.md`，并更新 `apps/api/README.md`、`apps/api/internal/README.md`、`apps/api/internal/compute/README.md`、`scripts/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次是既有 modular monolith package split 路线的首个低耦合 helper movement，不引入新的长期架构取舍。

## 02:12 - Platform dependency guard

- 用户目标：继续根据 Certainty/Elegance PRD 与 Development Plan 完善 Next 重构，并让首个 Go API package movement 具备自动化依赖护栏。
- 涉及目录：`scripts/`、`docs/architecture/`、`docs/rebuild/`、`apps/api/internal/platform/`、`tasks/`。
- 修改内容：在 `scripts/check-deps.ps1` 中新增 `apps-api-platform-must-not-import-compute-domain` 规则，禁止 `apps/api/internal/platform` source files import `apps/api/internal/compute`；同步更新 dependency graph、scripts README、platform README、compute-api architecture、current-state、Certainty/Elegance checklist 和任务复盘。
- 修改原因：上一阶段已抽出 `platform/httpx`，但若没有依赖检查，后续 helper 可能重新反向依赖 compute domain，削弱 package movement 的长期价值。
- 本次已消除的不确定性：确认现有 `platform/httpx` 不依赖 compute；确认新增规则可在当前仓库通过；确认文档中的 platform 不反向依赖 compute 约定已有自动化验证。
- 本次采用的关键假设：compute domain 继续允许依赖 platform helper；本次只阻止 platform-to-compute reverse dependency，不为尚未存在的 domain packages 编写过早规则。
- 剩余不确定性：jobs/artifacts/models/evidence/simulation 等 domain packages 尚未拆出；未来 domain package 之间的依赖方向仍需在 package 存在后补充 check-deps 规则。
- 影响范围 / 风险控制方式：只修改仓库级依赖检查和文档记录，不改变业务代码、HTTP 行为、OpenAPI、contracts、migrations 或 generated client；规则扫描沿用既有非 Markdown source file 范围。
- 验证方式：`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1` passed；`cd apps\api; go test ./internal/platform/... ./internal/compute` passed；`cd apps\api; go test ./...` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` passed，evidence status `passed`；`git diff --check -- apps\api docs scripts tasks .ai` passed with LF/CRLF warnings only。
- 是否更新 README：是，更新 `scripts/README.md` 与 `apps/api/internal/platform/README.md`；同时更新 architecture 文档。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；这是对既有 dependency boundary 策略的自动化补强。

## 02:15 - Platform config package split

- 用户目标：继续根据 Certainty/Elegance PRD 与 Development Plan 完善 Next 重构，并推进 Go API `platform/config` package movement。
- 涉及目录：`apps/api/cmd/compute-api/`、`apps/api/internal/platform/config/`、`apps/api/internal/compute/`、`scripts/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`。
- 修改内容：新增 `apps/api/internal/platform/config`，把 runtime `Config` 从 `compute/types.go` 移到 domain-free platform package；`cmd/compute-api` 与 command tests 改用 `platformconfig.Config`；扩展 compute boundary audit 的 expected package dirs 为 `compute`、`platform/config`、`platform/httpx`；同步更新 API/internal/platform/compute README、compute-api architecture、current-state、Certainty/Elegance checklist 和任务复盘。
- 修改原因：`Config` 只描述 command/runtime 环境组装和部署 wiring，不属于 compute job/artifact/model/evidence domain；将其留在 `compute` 会继续扩大 compute domain package 的横切职责。
- 本次已消除的不确定性：确认 `Config` 只被 `cmd/compute-api` 和 command tests 使用；确认移动后 command tests、compute tests、platform tests、dependency guard 和 package boundary audit 均可通过；确认 platform/config 不依赖 compute domain。
- 本次采用的关键假设：本次只移动 runtime config shape；auth `TokenConfig` / `Principal` 暂留 compute，因为它们仍参与 handler、audit context 和 tenant/project/site data-scope 判断。
- 剩余不确定性：`platform/auth` 尚未拆分；jobs/artifacts/models/evidence/simulation 等 domain packages 尚未移动；公共 `Service` 构造签名、handler/package surface、store/postgres 大文件拆分仍未完成。
- 影响范围 / 风险控制方式：不修改环境变量名、启动逻辑、OpenAPI、contracts、migrations、generated client、routes 或 storage behavior；用 command tests 与 full Go tests 覆盖编译和生产 token guard / archive wiring 行为。
- 验证方式：`cd apps\api; go test ./cmd/compute-api ./internal/platform/... ./internal/compute` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1` passed（package dirs 为 `compute`、`platform/config`、`platform/httpx`）；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1` passed；`cd apps\api; go test ./...` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` passed，evidence status `passed`；`git diff --check -- apps\api docs scripts tasks .ai` passed with LF/CRLF warnings only。
- 是否更新 README：是，新增 `apps/api/internal/platform/config/README.md`，并更新 `apps/api/README.md`、`apps/api/internal/README.md`、`apps/api/internal/platform/README.md`、`apps/api/internal/compute/README.md` 和 architecture 文档。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次是既有 modular monolith package split 路线下的低风险 platform config movement。

## 02:26 - Platform auth package split

- 用户目标：继续根据 Certainty/Elegance PRD 与 Development Plan 完善 Next 重构，并推进 Go API `platform/auth` package movement。
- 涉及目录：`apps/api/internal/platform/auth/`、`apps/api/internal/compute/`、`apps/api/cmd/compute-api/`、`scripts/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`。
- 修改内容：新增 `apps/api/internal/platform/auth`，移动 `Authenticator`、`TokenConfig`、`TokenRecord`、`Principal` 和平台 auth error；新增 platform auth 直接测试；compute 保留兼容 alias 和 job-specific data-scope helper；`ToAppError` 将 platform auth error 映射回现有 `contract_error.v1` response；`cmd/compute-api` 改用 `platformauth.NewAuthenticator` 和 `platformauth.TokenConfig`；扩展 boundary audit expected package dirs 至 `compute`、`platform/auth`、`platform/config`、`platform/httpx`；同步更新 API/platform/compute README、architecture current-state、dependency graph、Certainty/Elegance checklist 和任务复盘。
- 修改原因：计划中的 modular monolith 拆分明确包含 `platform/auth`。静态 bearer token parsing、scope/revocation check 和 principal metadata 是平台能力，不应继续扩大 compute domain package。
- 本次已消除的不确定性：确认 Authenticator 可独立于 compute domain 实现；确认 auth error 可通过 compute `ToAppError` 保持原 HTTP error shape；确认 production token guard 可以直接使用 platform auth config 类型；确认 platform/auth 不依赖 compute，且现有 focused auth/compute/command tests 通过。
- 本次采用的关键假设：compute 继续拥有 tenant/project/site 对具体 `JobRecord` 的 data-scope authorization，因为该判断依赖 compute domain metadata；本次不改变 token JSON shape、scope 字符串或生产 token guard policy。
- 剩余不确定性：OIDC/JWKS、RBAC/ABAC、all-mutation audit、全对象 data scope 和 ontology-backed runtime policy enforcement 仍未完成；jobs/artifacts/models/evidence/simulation 等 domain packages 尚未拆出。
- 影响范围 / 风险控制方式：不修改 routes、OpenAPI、contracts、migrations、generated client、env var name、auth scope 字符串或 HTTP response shape；保留 compute compatibility aliases 降低机械改动风险；用 platform auth 直接测试和 compute/server 现有测试验证行为。
- 验证方式：`cd apps\api; go test ./internal/platform/auth ./internal/platform/... ./internal/compute ./cmd/compute-api` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1` passed（package dirs 为 `compute`、`platform/auth`、`platform/config`、`platform/httpx`）；`cd apps\api; go test ./...` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` passed，evidence status `passed`；`git diff --check -- apps\api docs scripts tasks .ai` passed with LF/CRLF warnings only。
- 是否更新 README：是，新增 `apps/api/internal/platform/auth/README.md`，并更新 `apps/api/README.md`、`apps/api/internal/README.md`、`apps/api/internal/platform/README.md`、`apps/api/internal/compute/README.md` 和 architecture 文档。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次仍是既有 modular monolith package split 路线下的低风险 platform auth movement，不改变长期安全模型。

## 02:34 - Platform metrics package split

- 用户目标：继续根据 Certainty/Elegance PRD 与 Development Plan 完善 Next 重构，并推进 Go API `platform/metrics` package movement。
- 涉及目录：`apps/api/internal/platform/metrics/`、`apps/api/internal/platform/httpx/`、`apps/api/internal/compute/`、`scripts/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`。
- 修改内容：新增 `apps/api/internal/platform/metrics`，移动 metrics snapshot shape 与 Prometheus text renderer；新增 renderer 直接测试，覆盖 status 排序和 label escaping；compute `MetricsSnapshot` 改为 `platformmetrics.Snapshot` alias，`/metrics` handler 通过 `platformmetrics.RenderPrometheus` 输出；`platform/httpx` 去掉 Prometheus label escaping，保持只负责 JSON response 与本地 loopback CORS；扩展 boundary audit expected package dirs 至 `compute`、`platform/auth`、`platform/config`、`platform/httpx`、`platform/metrics`；同步更新 README、architecture current-state、dependency graph、Certainty/Elegance checklist 和任务复盘。
- 修改原因：Development Plan 的 Go API modular monolith 候选包包含 `platform/metrics`。Prometheus 文本渲染和 metrics snapshot shape 是平台观测能力，不应继续放在 compute HTTP handler 或 HTTP helper package 中。
- 本次已消除的不确定性：确认 compute `MetricsService` 可以继续只负责 metadata-store count collection；确认 renderer 可独立于 compute domain 测试；确认 `/metrics` 行为可通过现有 compute HTTP tests 和新增 platform metrics tests 覆盖。
- 本次采用的关键假设：metrics store 查询仍属于 compute metadata boundary，因为它依赖 `MetricsStore` 和 metadata store implementation；本次只移动 snapshot shape 与 renderer，不改变 metric names、labels、HTTP route 或 output semantics。
- 剩余不确定性：domain packages（jobs、artifacts、models、evidence、simulation 等）尚未拆出；future 指标新增仍需同步 compute store 查询、platform metrics renderer 和 smoke checks。
- 影响范围 / 风险控制方式：不修改 routes、auth、OpenAPI、contracts、migrations、generated client 或 metadata queries；保留 compute `MetricsSnapshot` alias 降低迁移面；用 focused platform/compute tests 验证文本输出。
- 验证方式：`cd apps\api; go test ./internal/platform/metrics ./internal/platform/httpx ./internal/platform/... ./internal/compute` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1` passed（package dirs 为 `compute`、`platform/auth`、`platform/config`、`platform/httpx`、`platform/metrics`）；`cd apps\api; go test ./...` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` passed，evidence status `passed`；`git diff --check -- apps\api docs scripts tasks .ai` passed with LF/CRLF warnings only。
- 是否更新 README：是，新增 `apps/api/internal/platform/metrics/README.md`，并更新 `apps/api/README.md`、`apps/api/internal/README.md`、`apps/api/internal/platform/README.md`、`apps/api/internal/platform/httpx/README.md`、`apps/api/internal/compute/README.md` 和 architecture 文档。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次仍是既有 modular monolith package split 路线下的平台观测 helper movement。

## 02:49 - Workers domain package split

- 用户目标：继续根据 Certainty/Elegance PRD 与 Development Plan 完善 Next 重构，并开始真正的 Go API domain package movement。
- 涉及目录：`apps/api/internal/domain/workers/`、`apps/api/internal/compute/`、`apps/api/`、`scripts/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`。
- 修改内容：新增 `apps/api/internal/domain` 与 `apps/api/internal/domain/workers` README；新增 `domain/workers` package，承接 worker register / claim / heartbeat 的领域行为、最小 `WorkerStore`、worker record、claim/heartbeat job 投影和领域 validation error；`compute/worker_lifecycle.go` 改为 `workerStoreAdapter`，把现有 `WorkerStore` / `JobRecord` 投影给 domain package；`WorkerRecord` 改为 `domain/workers.Record` alias；`ToAppError` 映射 workers domain error 回 `contract_error.v1`；扩展 `check-deps` 和 compute boundary audit 记录/防止 `internal/domain` 反向依赖 compute；同步更新 API/internal/compute/scripts README、compute-api architecture、current-state、dependency graph、Certainty/Elegance checklist 和任务复盘。
- 修改原因：上一阶段只完成 platform helper package movement，`internal/compute` 仍没有真正 domain package。worker lifecycle 是当前最窄且低风险的领域边界，可在不改变 HTTP/API 行为的前提下建立第一块 `internal/domain/*` package。
- 本次已消除的不确定性：确认 worker register / claim / heartbeat 可以通过最小 job state 投影脱离完整 compute `JobRecord`；确认 compute 适配器可以保留现有 MemoryStore/PostgresStore、HTTP routes 和 public service methods；确认 audit 能记录 `domain/workers` package dir 且 constructor violations 为 0；确认 `check-deps` 能禁止 domain package import compute。
- 本次采用的关键假设：本次只移动 worker register / claim / heartbeat 领域行为；artifact upload、job complete/fail、worker artifact 写入权限仍属于 artifact/job lifecycle 边界；不把整体 `Go API domain package split` 视为完成。
- 剩余不确定性：jobs、artifacts、models、evidence、simulation、agent 等 domain packages 尚未拆出；public `NewService` / `NewServiceWithArchive`、HTTP handler surface、`store.go` 和 `postgres.go` 仍是 compatibility / large-file split targets。
- 影响范围 / 风险控制方式：不修改 routes、auth scopes、OpenAPI、contracts、migrations、generated client、metadata queries 或 HTTP response shape；通过 alias 和 adapter 降低迁移面；新增 workers domain 直接测试并保留 compute 全量测试。
- 验证方式：`cd apps\api; go test ./internal/domain/workers ./internal/compute` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1` passed（package dirs 为 `compute`、`domain/workers`、`platform/auth`、`platform/config`、`platform/httpx`、`platform/metrics`，constructor violations 为 0）；`cd apps\api; go test ./...` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` passed，evidence status `passed`；`git diff --check -- apps\api docs scripts tasks .ai` passed with LF/CRLF warnings only。
- 是否更新 README：是，新增 `apps/api/internal/domain/README.md`、`apps/api/internal/domain/workers/README.md`，并更新 `apps/api/README.md`、`apps/api/internal/README.md`、`apps/api/internal/compute/README.md`、`scripts/README.md` 和 architecture 文档。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次沿用既有 modular monolith domain package movement 路线，没有新增长期架构取舍。

## 03:19 - Platform contracts package split

- 用户目标：继续根据 Certainty/Elegance PRD 与 Development Plan 完善 Next 重构，并让 contract schema validation 成为 compute/domain 均可复用的平台能力。
- 涉及目录：`apps/api/internal/platform/contracts/`、`apps/api/internal/compute/`、`apps/api/`、`scripts/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`。
- 修改内容：新增 `apps/api/internal/platform/contracts`，移动 schema file registry、`schema_version` 映射、JSON Schema compiler / validator 和 platform validation error；新增 platform contracts 直接测试；compute `contracts.go` 保留 `ContractValidator` / `NewContractValidator` 兼容 alias 与 compute DTO decode helper；`ToAppError` 映射 platform contract validation error 回现有 `contract_error.v1` response；扩展 compute boundary audit expected package dirs 与 README/architecture/current-state/Certainty-Elegance checklist 和任务复盘。
- 修改原因：schema validation 本身不持有 compute domain 状态，继续留在 `internal/compute` 会让后续 domain package 为了复用 validator 反向依赖 compute compatibility package。
- 本次已消除的不确定性：确认 schema loading / mapping / validation 可独立于 compute DTO 实现；确认 `DecodeComputeJob` 与 `DecodeArtifactMetadata` 应继续留在 compute，因为它们返回 compute 类型和 compute-specific shape errors；确认 `check-deps` 和 boundary audit 可覆盖新增 `platform/contracts` 且不产生 reverse dependency。
- 本次采用的关键假设：本次只移动 validator mechanics，不修改 `contracts/` schema、OpenAPI、migrations、generated client、HTTP routes 或 public response shape；新增 schema 时仍需同步 registry/codegen manifest、platform schema list 和 contract checks。
- 剩余不确定性：multi-language generated contract DTOs 仍未实现；jobs、artifacts、models、evidence、simulation、agent 等 domain packages 尚未拆出；public `NewService` / `NewServiceWithArchive`、HTTP handler surface、`store.go` 和 `postgres.go` 仍是 compatibility / large-file split targets。
- 影响范围 / 风险控制方式：使用 compute alias 与 error mapping 保持调用面稳定；仅新增 platform package 和直接测试；不改变合同文件、数据库 schema、OpenAPI 或生成客户端；保留 `check-deps` platform/domain-to-compute reverse dependency guard。
- 验证方式：`cd apps\api; go test ./internal/platform/contracts ./internal/domain/workers ./internal/compute` passed；`cd apps\api; go test ./...` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1` passed（package dirs 为 `compute`、`domain/workers`、`platform/auth`、`platform/config`、`platform/contracts`、`platform/httpx`、`platform/metrics`，constructor violations 为 0）；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` passed，evidence status `passed`；`git diff --check -- apps\api docs scripts tasks .ai` passed with LF/CRLF warnings only。
- 是否更新 README：是，新增 `apps/api/internal/platform/contracts/README.md`，并更新 `apps/api/README.md`、`apps/api/internal/README.md`、`apps/api/internal/platform/README.md`、`apps/api/internal/compute/README.md` 和 architecture 文档。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次仍是既有 modular monolith package split 路线下的低风险 platform helper movement。

## 03:35 - Platform metrics collector split

- 用户目标：继续根据 Certainty/Elegance PRD 与 Development Plan 完善 Next 重构，并进一步缩小 `internal/compute` 兼容大包的横切职责。
- 涉及目录：`apps/api/internal/platform/metrics/`、`apps/api/internal/compute/`、`apps/api/`、`scripts/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`。
- 修改内容：在 `apps/api/internal/platform/metrics` 中新增 `SnapshotStore` 与 read-only `MetricsService`，保留 snapshot shape 和 Prometheus renderer；compute `metrics.go` 改为 `platform/metrics` 兼容 alias，`Service.Metrics` 与 `/metrics` handler 调用面保持不变；MemoryStore/PostgresStore 的具体 metrics count 查询仍留在 compute store 实现；扩展 `scripts/audit-compute-api-boundary.ps1`，让 constructor/store-call audit 同时扫描 internal platform package；同步更新 API/platform/compute README、compute-api architecture、current-state、Certainty/Elegance checklist、scripts README 和任务复盘。
- 修改原因：metrics snapshot 与 renderer 已在 platform package，但 read-only collector 仍留在 compute，导致 compute 继续承担横切观测 orchestration。该 collector 只依赖抽象 snapshot store 和 clock，可在不改变 HTTP/API 行为的前提下迁出。
- 本次已消除的不确定性：确认 `MetricsService` 可独立于 compute domain types；确认 compute `MetricsStore` 的返回类型是 `platform/metrics.Snapshot` alias，可直接实现 `platform/metrics.SnapshotStore`；确认 boundary audit 扫描 platform files 后仍保持 10 个 service constructors、0 violations。
- 本次采用的关键假设：本次只移动 read-only collector，不移动 MemoryStore/PostgresStore 具体查询，因为它们依赖 metadata storage implementation；不修改 metric names、Prometheus text、HTTP route、OpenAPI、contracts、migrations 或 generated client。
- 剩余不确定性：jobs、artifacts、models、evidence、simulation、agent 等 domain packages 尚未拆出；hosted integration/browser/security/release evidence 仍需真实 GitHub Actions green run 或更强 live-backend evidence。
- 影响范围 / 风险控制方式：使用 compute compatibility alias 保持 `Service.Metrics` 调用面稳定；新增 direct platform metrics service tests；扩展 audit 只读扫描，不放宽 dependency guard；保持 `check-deps` platform-to-compute 禁止规则。
- 验证方式：`cd apps\api; go test ./internal/platform/metrics ./internal/compute` passed；`cd apps\api; go test ./...` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1` passed（service constructors audited: 10，internal Go package dirs: 7）；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` passed，evidence status `passed`，但因本次文件未提交 evidence 记录 dirty worktree；`git diff --check -- apps\api docs scripts tasks .ai` passed with LF/CRLF warnings only。
- 是否更新 README：是，更新 `apps/api/README.md`、`apps/api/internal/platform/README.md`、`apps/api/internal/platform/metrics/README.md`、`apps/api/internal/compute/README.md`、`scripts/README.md` 和 architecture 文档。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次仍是既有 modular monolith / platform helper package movement 路线下的低风险观测边界迁移。

## 03:43 - Platform contract document validation split

- 用户目标：继续根据 Certainty/Elegance PRD 与 Development Plan 完善 Next 重构，并进一步把 contract single source of truth 相关 helper 从 compute 兼容大包中抽离。
- 涉及目录：`apps/api/internal/platform/contracts/`、`apps/api/internal/compute/`、`apps/api/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`、`.ai/changes/`。
- 修改内容：在 `apps/api/internal/platform/contracts` 中新增 `ValidationIssue`、`DocumentValidationResponse` 和 `ValidateDocument`；新增 missing schema、unsupported schema、known invalid schema、valid `contract_error.v1` 的直接测试；compute `validateContractDocument` 改为对 platform helper 的兼容包装；`compute.ContractValidationIssue` 改为 platform type alias，`compute.ContractValidationResponse` 保留以继续附加 `DraftConfirmationRecord`；同步更新 API/platform/contracts/compute README、compute-api architecture、current-state、Certainty/Elegance checklist 和任务复盘。
- 修改原因：`schema_version` 文档验证和基础 validation response 不持有 compute domain 状态，继续留在 compute 会让后续 domain package 复用合同验证时仍绕回兼容大包。
- 本次已消除的不确定性：确认 base document validation 可独立于 compute domain types；确认 compute 只需保留带 `DraftConfirmationRecord` 的 HTTP-facing wrapper；确认 invalid JSON、unsupported schema、known invalid schema 与 valid schema 路径都可由 platform contracts 直接测试覆盖。
- 本次采用的关键假设：本次不移动 draft confirmation persistence 和 confirmation record attachment，因为它依赖 compute metadata type；不修改 routes、response JSON 字段、OpenAPI、schema files、migrations 或 generated client。
- 剩余不确定性：完整多语言 generated contract DTO 仍未实现；jobs、artifacts、models、evidence、simulation、agent 等 domain packages 尚未拆出；hosted integration/browser/security/release evidence 仍需真实 CI 或 live-backend evidence。
- 影响范围 / 风险控制方式：compute 保留 `ContractValidationResponse` 兼容结构以保护现有 HTTP/tests；platform helper 不 import compute；新增 direct platform tests 并保留 full Go/pr-fast 验证。
- 验证方式：`cd apps\api; go test ./internal/platform/contracts ./internal/platform/metrics ./internal/compute` passed；`cd apps\api; go test ./...` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1` passed（service constructors audited: 10，internal Go package dirs: 7）；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` passed，evidence status `passed`，但因本次文件未提交 evidence 记录 dirty worktree；`git diff --check -- apps\api docs scripts tasks .ai` passed with LF/CRLF warnings only。
- 是否更新 README：是，更新 `apps/api/README.md`、`apps/api/internal/platform/README.md`、`apps/api/internal/platform/contracts/README.md`、`apps/api/internal/compute/README.md` 和 architecture 文档。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次仍是既有 modular monolith / contracts platform helper movement 路线下的低风险边界迁移。

## 03:58 - Jobs domain status invariant split

- 用户目标：继续根据 Certainty/Elegance PRD 与 Development Plan 完善 Next 重构，并推进 Go API domain package movement。
- 涉及目录：`apps/api/internal/domain/jobs/`、`apps/api/internal/compute/`、`apps/api/`、`scripts/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`。
- 修改内容：新增 `apps/api/internal/domain/jobs`，承接 compute job status 常量、终态判断和 worker result status 判断；compute 保留 status 常量兼容 alias，并在 worker heartbeat projection 与 worker result validation 中调用 jobs domain helper；扩展 boundary audit expected package dirs；同步更新 API/internal/domain/compute README、compute-api architecture、current-state、Certainty/Elegance checklist、scripts README 和任务复盘。
- 修改原因：job status 与终态规则是稳定领域不变量，继续只放在 compute 兼容大包中会让后续 jobs/workers/artifacts 边界复用这些规则时绕回 `internal/compute`。
- 本次已消除的不确定性：确认 status 常量和 worker-result 可提交状态可以独立于 job record、store、HTTP handler 和 audit envelope；确认 compute alias 可保持现有测试/调用面；确认 `check-deps` 和 boundary audit 纳入 `domain/jobs` 后仍通过。
- 本次采用的关键假设：本次只移动稳定状态不变量，不移动完整 jobs lifecycle；job records、store interfaces、persistence、audit envelopes、HTTP routes、OpenAPI、contracts、migrations 和 generated clients 保持不变。
- 剩余不确定性：完整 jobs lifecycle package movement 尚未完成；artifacts/models/evidence/simulation/agent domain packages 尚未拆出；hosted integration/browser/security/release evidence 仍需真实 CI 或 live-backend evidence。
- 影响范围 / 风险控制方式：通过兼容 alias 降低机械改动风险；新增 direct domain tests；不改变公开 HTTP 行为、schema、auth scope、数据库或生成客户端。
- 验证方式：`cd apps\api; go test ./internal/domain/jobs ./internal/domain/workers ./internal/compute` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1` passed（service constructors audited: 10，internal Go package dirs: 8）；`cd apps\api; go test ./...` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` passed，evidence status `passed`，但因本次文件未提交 evidence 记录 dirty worktree；`git diff --check -- apps\api docs scripts tasks .ai` passed with LF/CRLF warnings only。
- 是否更新 README：是，新增 `apps/api/internal/domain/jobs/README.md`，并更新 `apps/api/README.md`、`apps/api/internal/README.md`、`apps/api/internal/domain/README.md`、`apps/api/internal/compute/README.md`、`scripts/README.md` 和 architecture 文档。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次仍是既有 modular monolith / domain package movement 路线下的低风险不变量迁移。

## 04:06 - Jobs domain worker match split

- 用户目标：继续根据 Certainty/Elegance PRD 与 Development Plan 完善 Next 重构，并把 jobs domain package 从 status 常量推进到真实调度不变量。
- 涉及目录：`apps/api/internal/domain/jobs/`、`apps/api/internal/compute/`、`apps/api/`、`scripts/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`。
- 修改内容：在 `apps/api/internal/domain/jobs` 中新增 `ClaimCandidate`、`WorkerCapabilities`、`MatchesWorker`、`RequiredCapabilities` 和 `ContractVersions`；新增直接测试覆盖 required capability 提取、contract version 提取、匹配成功、缺 capability 拒绝和缺 payload contract 拒绝；MemoryStore/PostgresStore worker claim path 改为把 compute 记录投影给 jobs domain helper；删除 compute-local worker matching helper；同步更新 README、architecture、Certainty/Elegance checklist、audit note 和任务复盘。
- 修改原因：worker claim 的 capability / contract-version 匹配是稳定 job/worker 调度不变量，继续放在 compute store helper 会让 MemoryStore/PostgresStore 各自携带领域规则，也阻碍后续完整 jobs package movement。
- 本次已消除的不确定性：focused test 首次失败暴露 PostgresStore 也依赖原 helper；修复后确认 MemoryStore 与 PostgresStore claim path 均走同一个 jobs domain matcher；确认该 matcher 不依赖 compute types，也不影响 queue selection、state mutation 或 event 写入。
- 本次采用的关键假设：本次只移动匹配规则；队列选择、状态写入、事件记录、Store interfaces、PostgreSQL schema、HTTP behavior、OpenAPI、contracts、migrations 和 generated clients 保持不变。
- 剩余不确定性：完整 jobs lifecycle package movement 尚未完成；artifacts/models/evidence/simulation/agent domain packages 尚未拆出；hosted integration/browser/security/release evidence 仍需真实 CI 或 live-backend evidence。
- 影响范围 / 风险控制方式：compute store 仅做字段投影；domain/jobs 只依赖标准库；新增直接测试并保留 compute focused/full validation；不改公开 API 或存储契约。
- 验证方式：初次 focused test 发现 PostgresStore 仍引用已移除的 compute-local matcher，修复后 `cd apps\api; go test ./internal/domain/jobs ./internal/domain/workers ./internal/compute` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1` passed（service constructors audited: 10，internal Go package dirs: 8）；`cd apps\api; go test ./...` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` passed，evidence status `passed`，但因本次文件未提交 evidence 记录 dirty worktree；`git diff --check -- apps\api docs scripts tasks .ai` passed with LF/CRLF warnings only。
- 是否更新 README：是，更新 `apps/api/internal/domain/jobs/README.md`、`apps/api/README.md`、`apps/api/internal/domain/README.md`、`apps/api/internal/compute/README.md`、`scripts/README.md` 和 architecture 文档。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次仍是既有 jobs domain package movement 路线下的低风险调度不变量迁移。

## 04:20 - Models domain model-run parsing split

- 用户目标：继续根据 Certainty/Elegance PRD 与 Development Plan 完善 Next 重构，并把 Go API domain package movement 从 jobs/workers 推进到 models 的低风险稳定解析边界。
- 涉及目录：`apps/api/internal/domain/models/`、`apps/api/internal/compute/`、`apps/api/`、`scripts/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`。
- 修改内容：新增 `apps/api/internal/domain/models`，承接 `model_run.v1` 原始 JSON 的 id/job/model/version/parameter_set 字段提取、evidence refs 提取和 warnings 提取；新增直接测试；MemoryStore/PostgresStore/evidence governance/simulation input replay 调用改用 domain models helper；删除 compute-local model_run parsing helper；同步更新 README、architecture、current-state、Certainty/Elegance checklist、boundary audit note 和任务复盘。
- 修改原因：`model_run.v1` 原始字段和 refs/warnings 解析是稳定 models 领域规则，继续散落在 compute store/evidence/simulation helper 中会阻碍后续完整 models domain package movement。
- 本次已消除的不确定性：确认 model_run raw parsing 不依赖 compute DTO、HTTP handler、model catalog mutation 或 Postgres implementation；确认 MemoryStore、PostgresStore、evidence-read 和 simulation replay 可以共享同一 domain helper；确认 boundary audit 可记录新增 `domain/models` package dir。
- 本次采用的关键假设：本次只移动稳定解析 helper，不移动 model catalog governance、benchmark run validation、promotion planning、HTTP response DTO、Store interfaces、OpenAPI、contracts、migrations 或 generated clients。
- 剩余不确定性：完整 model governance package movement 尚未完成；full jobs lifecycle、artifacts、evidence、simulation 和 agent domain packages 仍未拆出；hosted integration/browser/security/release evidence 仍需真实 CI 或 live-backend evidence。
- 影响范围 / 风险控制方式：通过只读/解析 helper 迁移控制变更面；domain/models 只依赖 Go standard library；新增直接测试并保留 compute/full validation；不改变公开 API、schema、auth scope、数据库或生成客户端。
- 验证方式：`cd apps\api; go test ./internal/domain/models ./internal/domain/jobs ./internal/domain/workers ./internal/compute` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1` passed（service constructors audited: 10，internal Go package dirs: 9）；`cd apps\api; go test ./...` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` passed，evidence status `passed`，但因本次文件未提交 evidence 记录 dirty worktree；`git diff --check -- apps\api docs scripts tasks .ai` passed with LF/CRLF warnings only。
- 是否更新 README：是，新增 `apps/api/internal/domain/models/README.md`，并更新 `apps/api/README.md`、`apps/api/internal/README.md`、`apps/api/internal/domain/README.md`、`apps/api/internal/compute/README.md`、`scripts/README.md` 和 architecture 文档。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次仍是既有 modular monolith / domain package movement 路线下的低风险 models 解析 helper 迁移。

## 04:29 - Artifacts domain retention policy split

- 用户目标：继续根据 Certainty/Elegance PRD 与 Development Plan 完善 Next 重构，并按推荐顺序推进 artifacts 领域的低风险 package movement。
- 涉及目录：`apps/api/internal/domain/artifacts/`、`apps/api/internal/compute/`、`apps/api/`、`scripts/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`。
- 修改内容：新增 `apps/api/internal/domain/artifacts`，承接 artifact retention policy 常量、`retention_policy` / `retain_until` metadata 解析和 retention candidate policy 判断；新增直接测试；artifact upload 和 MemoryStore retention/metrics candidate check 改用 domain artifacts helper；删除 compute-local retention parsing/candidate helper；同步更新 README、architecture、current-state、Certainty/Elegance checklist、boundary audit note 和任务复盘。
- 修改原因：artifact retention policy 是稳定 artifacts 领域规则，继续留在 compute helper 中会让上传、retention sweep 和 metrics 继续共享 compute-local 规则，阻碍后续完整 artifact lifecycle package movement。
- 本次已消除的不确定性：确认 retention policy parsing 不依赖 compute DTO、object store、archive backend、audit envelope 或 Postgres implementation；确认 upload metadata validation 和 MemoryStore candidate checks 可共享同一 domain helper；确认 boundary audit 可记录新增 `domain/artifacts` package dir。
- 本次采用的关键假设：本次只移动稳定 retention policy helper，不移动 artifact object storage、archive execution、audit envelopes、metadata store interfaces、PostgreSQL implementation、HTTP routes、OpenAPI、contracts、migrations 或 generated clients。
- 剩余不确定性：完整 artifact lifecycle package movement 尚未完成；full jobs lifecycle、full model governance、evidence、simulation 和 agent domain packages 仍未拆出；clean HEAD pr-fast evidence 仍受当前未跟踪 rebuild plan 文件影响。
- 影响范围 / 风险控制方式：通过纯 helper 迁移控制变更面；domain/artifacts 只依赖 Go standard library；新增直接测试并保留 retention focused、compute、full Go 和 pr-fast validation；不改变公开 API、schema、auth scope、数据库或生成客户端。
- 验证方式：`cd apps\api; go test ./internal/domain/artifacts ./internal/compute -run "Test(ArtifactRetention|HTTPArtifactRetention|IsRetention|Retention|UploadArtifactPersistsRetention)" -count=1` passed；`cd apps\api; go test ./internal/domain/artifacts ./internal/domain/models ./internal/domain/jobs ./internal/domain/workers ./internal/compute` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1` passed（service constructors audited: 10，internal Go package dirs: 10）；`cd apps\api; go test ./...` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` passed，evidence status `passed`，但因本次文件和无关未跟踪 rebuild plan 文件存在，evidence 记录 dirty worktree；`git diff --check -- apps\api docs scripts tasks .ai` passed with LF/CRLF warnings only。
- 是否更新 README：是，新增 `apps/api/internal/domain/artifacts/README.md`，并更新 `apps/api/README.md`、`apps/api/internal/README.md`、`apps/api/internal/domain/README.md`、`apps/api/internal/compute/README.md`、`scripts/README.md` 和 architecture 文档。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次仍是既有 modular monolith / domain package movement 路线下的低风险 artifacts retention helper 迁移。

## 04:51 - Simulation domain execution profile split

- 用户目标：继续根据 Certainty/Elegance PRD 与 Development Plan 完善 Next 重构，并执行阶段性 commit/push。
- 涉及目录：`apps/api/internal/domain/simulation/`、`apps/api/internal/compute/`、`apps/api/`、`scripts/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`、`.ai/changes/`。
- 修改内容：新增 `apps/api/internal/domain/simulation`，承接 simulation job type 到 `compute_job.v1.execution` profile / worker capability 的稳定映射；`CreateSimulationCheck` 与 benchmark case scheduling 改用 domain simulation helper；同步更新 boundary audit、README、architecture/current-state、Certainty/Elegance checklist 和任务记录。
- 修改原因：Go API modular monolith 仍需继续减少 `internal/compute` 的稳定领域规则堆积；execution profile 映射是纯规则，适合作为低风险 simulation domain package movement。
- 本次已消除的不确定性：确认 `simulationCheckExecution` 仅被 simulation-check creation 与 benchmark case scheduling 使用；确认迁移后保持 `time_limit_sec=600`、`priority=normal`、`required_capabilities` 数组形状；确认不需要修改 HTTP/OpenAPI/contracts/migrations/generated client。
- 本次采用的关键假设：本次只移动 execution profile helper，不移动 simulation input/process graph persistence、ProcessGraph-to-SimulationInput transformation、model catalog governance、HTTP mapping 或 worker execution。
- 剩余不确定性：完整 simulation domain package movement 尚未完成；simulation input/process graph、full model governance、evidence、full artifact lifecycle 和 full jobs lifecycle 仍在 compute compatibility wiring 中。
- 影响范围 / 风险控制方式：最小 package split；新增 direct domain tests；保持外部 API、payload schema、store interfaces、database schema、auth scope 和 generated client 不变；`domain/simulation` 不反向依赖 compute。
- 验证方式：`cd apps\api; go test ./internal/domain/simulation ./internal/compute -run "Test(RequiredCapabilities|ExecutionProfile|SimulationCheckEndpointCreatesComputeJob|BenchmarkCaseScheduleRunEndpoint)" -count=1` passed；`cd apps\api; go test ./internal/domain/simulation ./internal/domain/artifacts ./internal/domain/models ./internal/domain/jobs ./internal/domain/workers ./internal/compute` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1` passed（service constructors audited: 10，internal Go package dirs: 11）；`cd apps\api; go test ./...` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` passed，evidence status `passed`，但因本次文件和无关未跟踪 rebuild plan 文件存在，evidence 记录 dirty worktree；`git diff --check -- apps\api docs scripts tasks .ai` passed with LF/CRLF warnings only。
- 是否更新 README：是，新增 `apps/api/internal/domain/simulation/README.md`，并更新 `apps/api/README.md`、`apps/api/internal/README.md`、`apps/api/internal/domain/README.md`、`apps/api/internal/compute/README.md`、`scripts/README.md` 和 architecture 文档。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次仍是既有 modular monolith / domain package movement 路线下的低风险 simulation helper 迁移。

## 05:05 - Simulation domain process graph projection split

- 用户目标：继续根据 Certainty/Elegance PRD 与 Development Plan 完善 Next 重构，并执行阶段性 commit/push。
- 涉及目录：`apps/api/internal/domain/simulation/`、`apps/api/internal/compute/`、`apps/api/`、`scripts/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`、`.ai/changes/`。
- 修改内容：在 `apps/api/internal/domain/simulation` 中新增 process graph structural validation 与 material-balance ProcessGraph-to-SimulationInput projection helper；新增直接测试覆盖合法转换、重复 node、缺 concentration transform component、unsupported ASM process graph job type；`SimulationInputService` 保留 schema validation、metadata record、store/idempotency 和 error mapping，并改用 domain helper；删除 compute-local process graph validation/projection helper；同步更新 README、architecture、Certainty/Elegance checklist、audit note 和任务复盘。
- 修改原因：`service.go` 仍承载大块与 HTTP/service wiring 无关的 process graph 纯规则，阻碍 simulation domain package movement 和 compute 大包瘦身。
- 本次已消除的不确定性：确认 process graph structural validation、node/edge projection、time segment collection、default runtime parameters 与 material-balance-only restriction 可脱离 compute DTO/store/HTTP；focused test 首次失败仅暴露新增测试对 fallback segment id 的预期写错，生产逻辑保持原行为。
- 本次采用的关键假设：本次只移动稳定 validation/projection helper，不移动 process graph/simulation input metadata persistence、schema validation、idempotency、store interfaces、PostgreSQL implementation、HTTP routes、OpenAPI、contracts、migrations、generated clients 或 worker execution。
- 剩余不确定性：完整 simulation input/process graph service package movement 尚未完成；full jobs lifecycle、full artifact lifecycle、full model governance、evidence 和 agent domain packages 仍在 compute compatibility wiring 中。
- 影响范围 / 风险控制方式：compute boundary 将 domain helper error 映射回 `ValidationError`，保持 HTTP response shape；domain/simulation 只依赖 Go standard library；新增 direct domain tests 并保留 compute process graph focused tests。
- 验证方式：初次 focused domain test 发现新增测试对 fallback segment id 的预期写错，按原行为修正为 `seg_1` 后，`cd apps\api; go test ./internal/domain/simulation ./internal/compute -run "Test(ValidateProcessGraph|ProcessGraphToSimulationInput|SimulationCheckEndpointCreatesComputeJob|ProcessGraphEvidenceReference)" -count=1` passed；`cd apps\api; go test ./internal/domain/simulation ./internal/domain/artifacts ./internal/domain/models ./internal/domain/jobs ./internal/domain/workers ./internal/compute` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1` passed（service constructors audited: 10，internal Go package dirs: 11）；`cd apps\api; go test ./...` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` passed，evidence status `passed`，但因本次文件和无关未跟踪 rebuild plan 文件存在，evidence 记录 dirty worktree；`git diff --check -- apps\api docs scripts tasks .ai` passed with LF/CRLF warnings only。
- 是否更新 README：是，更新 `apps/api/internal/domain/simulation/README.md`、`apps/api/README.md`、`apps/api/internal/README.md`、`apps/api/internal/domain/README.md`、`apps/api/internal/compute/README.md`、`scripts/README.md` 和 architecture 文档。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次仍是既有 modular monolith / simulation domain package movement 路线下的低风险规则迁移。

## 05:19 - Evidence domain parsing split

- 用户目标：继续根据 Certainty/Elegance PRD 与 Development Plan 完善 Next 重构，并执行阶段性 commit/push。
- 涉及目录：`apps/api/internal/domain/evidence/`、`apps/api/internal/compute/`、`apps/api/`、`scripts/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`、`.ai/changes/`。
- 修改内容：新增 `apps/api/internal/domain/evidence`，承接 evidence package input ref/hash 提取、evidence ref grammar、内嵌 `simulation_input.v1` payload lookup、`risk_findings` 提取和 risk summary；新增直接测试；`EvidenceGovernanceService` 改用 domain helper；删除 compute-local evidence parsing helper；同步更新 README、architecture/current-state、Certainty/Elegance checklist、boundary audit note 和任务记录。
- 修改原因：evidence input/ref/risk parsing 是稳定 evidence 领域规则，继续放在 compute evidence governance helper 中会让 evidence package、production-readiness、result explanation 和 model governance 相关逻辑继续绕回 compute compatibility package。
- 本次已消除的不确定性：确认这些 helper 只依赖 JSON 与标准库，不依赖 store、HTTP、OpenAPI、contracts、PostgreSQL 或 compute DTO；确认迁移后 evidence package input hash、process graph ref、simulation input ref、evidence ref parsing 和 risk summary 行为由 focused tests 覆盖。
- 本次采用的关键假设：本次只移动纯解析/汇总 helper，不移动 store-backed evidence package export、production-readiness response assembly、process graph/model run/artifact lookup callback、HTTP mapping、auth scope、OpenAPI、contracts、migrations 或 generated clients。
- 剩余不确定性：完整 evidence governance package movement 尚未完成；full jobs lifecycle、full artifact lifecycle、full model governance、full simulation input/process graph metadata service handling 和 agent domain packages 仍在 compute compatibility wiring 中；hosted integration/browser/security/release evidence 仍需真实 CI 或 live-backend evidence。
- 影响范围 / 风险控制方式：domain/evidence 只依赖 Go standard library；compute 仍保留 DTO mapping 与 store orchestration；无公开 API/schema/database 变更；新增 direct domain tests 并保留 compute evidence focused tests。
- 验证方式：`cd apps\api; go test ./internal/domain/evidence ./internal/compute -run "Test(InputRefs|ParseRef|SimulationInputPayload|RiskFindings|EvidencePackage|ProductionReadiness|NewSystemEvidenceReferenceE2E|ProcessGraphEvidenceReference)" -count=1` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1` passed（service constructors audited: 10，internal Go package dirs: 12）；`cd apps\api; go test ./...` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` passed，evidence status `passed`，但因本次文件和无关未跟踪 rebuild plan 文件存在，evidence 记录 dirty worktree；`git diff --check -- apps\api docs scripts tasks .ai` passed with LF/CRLF warnings only。
- 是否更新 README：是，新增 `apps/api/internal/domain/evidence/README.md`，并更新 `apps/api/README.md`、`apps/api/internal/README.md`、`apps/api/internal/domain/README.md`、`apps/api/internal/compute/README.md`、`scripts/README.md` 和 architecture 文档。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次仍是既有 modular monolith / domain package movement 路线下的低风险 evidence 解析 helper 迁移。
