## 00:20 - Release artifact download verifier smoke

- 用户目标：继续根据 Certainty/Elegance PRD 与 Development Plan 完善 Next 重构，并推进阶段性可验证 evidence。
- 涉及目录：`scripts/release/`、`.github/workflows/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`、根 README / Justfile。
- 修改内容：新增 `scripts/release/smoke-release-artifact-download.ps1`，用临时 fixture 覆盖 release artifact download verifier 的成功路径与缺失 installer 的失败路径；新增 `just release-artifact-download-smoke`；将 smoke 接入 `next-release-gates.yml`；更新相关 README、current-state、Certainty/Elegance checklist 和任务复盘。
- 修改原因：`next-release-gates.yml` 已支持真实 unsigned artifact 上传/下载校验，但缺少一个不依赖真实二进制 artifact 的本地/CI 可重复 verifier smoke。
- 本次已消除的不确定性：确认 release workflow 已有真实 artifact upload/download/verify 编排；确认新增 fixture smoke 能验证 `verify-release-artifact-download.ps1` 的通过与失败行为；确认 release gate dry run 和 pr-fast 不受影响。
- 本次采用的关键假设：fixture smoke 只证明校验器逻辑，不等价于真实 GitHub hosted release-evidence green；真实 artifact round trip 仍需要 `workflow_dispatch build_release_artifacts=true`。
- 剩余不确定性：尚未触发 hosted GitHub Actions release-evidence run；packaged worker exe、NSIS installer、签名、auto update 和 GitHub Release publication 仍未完成生产发布验证。
- 影响范围 / 风险控制方式：只新增 release automation smoke 和文档记录，不修改业务接口、OpenAPI、contracts、迁移、生成客户端或发布策略；fixture 文件写入 `tmp/`。
- 验证方式：`scripts\release\smoke-release-artifact-download.ps1` passed；PyYAML workflow parser check passed；`scripts\release\next-release-gates.ps1 -Mode merge -SkipLong` passed；`scripts\ci\pr-fast.ps1` passed；`git diff --check -- .github Justfile README.md docs scripts tasks .ai` passed with LF/CRLF warnings only。
- 是否更新 README：是，更新 root README、`.github` README、`.github/workflows` README、`scripts` README、`scripts/release` README。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；仍沿用 `0008-release-gate-artifact-boundary.md` 与 `0011-desktop-release-signing-auto-update-boundary.md`。

## 00:37 - Nightly evidence workflow

- 用户目标：继续根据 Certainty/Elegance PRD 与 Development Plan 完善 Next 重构，并推进 CI lanes 产品化。
- 涉及目录：`.github/workflows/`、`.github/`、`scripts/ci/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`。
- 修改内容：为 `next-pr-fast.yml` 增加 `workflow_call`；新增 `next-security-smoke.yml`；新增 scheduled/manual `next-nightly.yml`，组合 pr-fast、integration、browser、security 和 Desktop package smoke，并上传 nightly summary evidence；更新相关 README、current-state、Certainty/Elegance checklist 和任务复盘。
- 修改原因：Development Plan 的 Testing / CI 清单仍缺 `nightly`，且 security smoke 只有本地/Justfile 入口，没有 hosted reusable workflow 入口。
- 本次已消除的不确定性：确认 integration/browser/Desktop package workflow 已有 `workflow_call`；确认 PR fast 可安全增加 reusable 入口；确认 nightly summary 能引用所有 called workflow 的结果。
- 本次采用的关键假设：nightly 是 scheduled evidence orchestrator，不替代 release-evidence；新增 workflow 文件和本地 parser 通过不等于 hosted green run 已完成。
- 剩余不确定性：尚未触发 GitHub Actions nightly run；integration/browser/Desktop/security 的 hosted green evidence 仍需实际 workflow run；release artifact build/download 仍由 manual release gate path 证明。
- 影响范围 / 风险控制方式：只改 GitHub workflow 编排和文档，不修改业务代码、OpenAPI、contracts、生成客户端或发布策略；复用已有 smoke 脚本，避免在 YAML 内联业务逻辑。
- 验证方式：PyYAML workflow parser check passed；`scripts\ci\security-smoke.ps1` passed；`scripts\release\smoke-release-artifact-download.ps1` passed；`scripts\ci\pr-fast.ps1` passed。
- 是否更新 README：是，更新 `.github` README、`.github/workflows` README、`scripts/ci` README 和 `docs/architecture/current-state.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次是 CI 编排补齐，不改变长期发布/安全策略。

## 01:00 - Tenant/project read-scope enforcement

- 用户目标：继续根据 Certainty/Elegance PRD 与 Development Plan 完善 Next 重构，并推进 production security 中的数据隔离证据。
- 涉及目录：`apps/api/`、`apps/api/internal/compute/`、`scripts/ci/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`。
- 修改内容：为静态 token record/principal 增加可选 `tenant_id`、`project_id` 和前向兼容 `site_id`；对 HTTP job list/get 与 artifact download read path 增加 tenant/project scope 过滤或拒绝；为 memory/Postgres list 查询补 tenant/project filter；新增 scoped job read 与 artifact download Go 测试；扩展 `security-smoke.ps1` 覆盖摘要；同步更新 API/compute、scripts/ci、current-state、Certainty/Elegance plan 和任务复盘。
- 修改原因：此前 security smoke 只覆盖 token guard、scope denial、revocation、selected mutation audit 与 admin scope，尚未对真实数据对象 read path 做 tenant/project 隔离验证。
- 本次已消除的不确定性：确认 job metadata 已有 tenant/project 字段，artifact metadata 可回溯 job_id，因此无需变更 OpenAPI、contracts 或数据库 schema 即可在 job list/get 和 artifact download 上落地最小读隔离切片；确认空 token scope 可保留 existing dev/admin token 的全局访问兼容性。
- 本次采用的关键假设：本次只做 tenant/project read scope；`site_id` 只进入 token/principal 作为后续兼容字段，不在缺少 job/site metadata enforcement 之前声明已覆盖；空 tenant/project 代表 global principal。
- 剩余不确定性：site scope、全对象 data scope、all-mutation audit、OIDC/JWKS、service-token secret manager、ontology-backed runtime policy enforcement 和所有 mutation path 的对象级授权仍未完成。
- 影响范围 / 风险控制方式：保持变更在 API internal compute 与 security smoke 内，不修改公开响应 schema、OpenAPI、contracts、迁移或生成客户端；新增测试锁定允许/拒绝行为；Development Plan 继续保留 security checklist 未完成。
- 验证方式：`cd apps\api; go test ./internal/compute -run "Test(HTTPJobReadTenantProjectScope|HTTPArtifactDownloadTenantProjectScope)" -count=1` passed；`cd apps\api; go test ./...` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\security-smoke.ps1` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` passed；`git diff --check -- apps\api scripts docs tasks .ai` passed with LF/CRLF warnings only。
- 是否更新 README：是，更新 `apps/api/README.md`、`apps/api/internal/compute/README.md`、`scripts/ci/README.md` 和 `docs/architecture/current-state.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次是既有 static-token security 机制上的最小 read-scope enforcement slice，未改变长期认证/授权架构决策。

## 01:25 - Site read-scope enforcement

- 用户目标：继续根据 Certainty/Elegance PRD 与 Development Plan 完善 Next 重构，并把 production security 的数据隔离从 tenant/project read-scope 推进到 tenant/project/site read-scope。
- 涉及目录：`contracts/`、`apps/api/`、`apps/api/internal/compute/`、`apps/api/migrations/`、`frontend/src/client/compute/`、`scripts/ci/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`。
- 修改内容：为 `compute_job.v1.context` 和 `simulation_request.v1.metadata` 增加可选 `site_id`；新增 site-scoped compute job fixture 并登记 registry；新增 `0010_job_site_scope` migration；Go API 持久化 `JobRecord.SiteID`，从 simulation request metadata 或 `external_refs.site_id` 传递到 compute job context，并在 job list/get 与 artifact download read path 上执行 tenant/project/site scope 过滤；生成 Compute TS client 的 `JobRecord.site_id`；扩展 security smoke；同步更新 README、current-state、Certainty/Elegance plan 和任务记录。
- 修改原因：上一阶段 token config 已接受 `site_id` 但未有 job/site metadata enforcement；Development Plan 的 read-scope 验收要求 tenant/project/site list/get 过滤或明确豁免。
- 本次已消除的不确定性：确认 `site_id` 可以作为 v1 可选合同字段添加，不破坏现有 required 字段；确认 `simulation_request.external_refs.site_id` 可作为已有 NewSystem/milp 语义来源；确认 artifact download 可继续通过 artifact job_id 回溯 job scope。
- 本次采用的关键假设：`site_id` 仍只作为 job metadata read-scope，不代表完整 WaterStation/Site domain model；空 tenant/project/site token scope 保持 global access；本次不覆盖全对象授权或 mutation authorization。
- 剩余不确定性：完整 OIDC/JWKS、RBAC/ABAC、全对象 tenant/project/site data scope、ontology-backed policy enforcement 和 all-mutation audit 仍未完成；hosted security workflow green run 仍需 GitHub Actions 实际执行。
- 影响范围 / 风险控制方式：`site_id` 是可选 additive v1 字段；新增 forward migration 和 rollback；保持 API response backward-compatible；用 focused Go 测试锁定 cross-tenant 与 cross-site deny 行为；不修改 worker execution payload semantics。
- 验证方式：`cd apps\api; go test ./internal/compute -run "Test(HTTPJobReadTenantProjectSiteScope|HTTPArtifactDownloadTenantProjectSiteScope|NewSystemEvidenceReferenceE2E|SimulationCheckEndpointCreatesComputeJob|BenchmarkCaseScheduleRunEndpoint)" -count=1` passed；`cd apps\api; go test ./...` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\security-smoke.ps1` passed；`backend\.venv\Scripts\python -m pytest contracts\tests -q` passed；`cd frontend; npx tsc --noEmit` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-contracts.ps1` passed after commit；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` passed；`git diff --check -- contracts apps\api frontend\src\client\compute scripts docs tasks .ai` passed with LF/CRLF warnings only。
- 是否更新 README：是，更新 `contracts/README.md`、`apps/api/README.md`、`apps/api/internal/compute/README.md`、`apps/api/migrations/README.md`、`scripts/ci/README.md` 和 `docs/architecture/current-state.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次沿用现有 static-token/read-scope 与 contracts additive v1 policy。

## 01:43 - Service constructor boundary audit

- 用户目标：继续根据 Certainty/Elegance PRD 与 Development Plan 完善 Next 重构，并推进 Go API modular monolith / Store constructor narrowing。
- 涉及目录：`apps/api/`、`apps/api/internal/compute/`、`scripts/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`。
- 修改内容：将 `ArtifactLifecycleService`、`SimulationInputService`、`ModelGovernanceService` 和 `EvidenceGovernanceService` 的内部构造函数参数收敛为明确的领域 repository bundle / object-store bundle；保持 service 内部字段仍为最小接口；扩展 `scripts/audit-compute-api-boundary.ps1`，记录 `service_constructor_boundaries` 并在内部 domain service constructor 接收 aggregate `Store` 或超过 3 个 store-like 参数时失败；同步更新 API/compute、scripts、compute-api architecture、current-state、Certainty/Elegance checklist 和任务记录。
- 修改原因：各 domain service 行为已委派到窄边界，但若审计不覆盖构造函数，后续重构容易重新把巨型 `Store` 泄漏回内部 service constructor。
- 本次已消除的不确定性：确认当前内部 domain service constructors 可在不改变 HTTP/API 行为的前提下收敛到 1-3 个 store-like 参数；确认 audit 能枚举 10 个 service constructor 并对聚合 Store / 过宽参数列表做失败门禁。
- 本次采用的关键假设：公共 `NewService` / `NewServiceWithArchive` 在 `internal/compute` 未拆包前继续作为兼容 wiring 接收 aggregate `Store`；本次只收窄内部 domain service constructor，不做 package movement、不改变 OpenAPI/contracts/migrations。
- 剩余不确定性：Go API domain package split、handler/package surface reduction、公共 `Service` 构造签名收窄和 `service.go` / `http.go` / `store.go` / `postgres.go` 大文件拆分仍未完成。
- 影响范围 / 风险控制方式：仅修改内部 constructor wiring 与只读审计脚本；保持公开 HTTP contract、auth scope、schema、migration、generated client 不变；用 audit 和 focused Go tests 验证无行为回归。
- 验证方式：`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1` passed（41 Store methods、12 embedded interfaces、12 domain groups、10 service constructors audited）；`cd apps\api; go test ./internal/compute -run "Test(NewSystemEvidenceReferenceE2E|SimulationCheckEndpointCreatesComputeJob|BenchmarkCaseScheduleRunEndpoint|HTTPArtifactDownloadTenantProjectSiteScope)" -count=1` passed；`cd apps\api; go test ./...` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` passed，evidence status `passed`；`git diff --check -- apps\api scripts docs tasks .ai` passed with LF/CRLF warnings only。
- 是否更新 README：是，更新 `apps/api/README.md`、`apps/api/internal/compute/README.md`、`scripts/README.md` 和 `docs/architecture/current-state.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次是既有 modular monolith / Store split 路线下的内部边界门禁，没有新增长期架构决策。
