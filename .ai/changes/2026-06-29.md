## 22:10 - Edge 中文国际化

- 用户目标：完成 Edge 工具条与连接参数面板的中文国际化。
- 涉及目录：`frontend/src/i18n/`、`frontend/src/components/Flow/edges/`、`frontend/src/components/Flow/inspectorbar/`。
- 修改内容：扩展 `flow.edge` 中英文消息；将 Edge 类型、字段、选项、aria/title、Signal 提示和 Pump/Signal 连线 fallback label 接入现有 i18n。
- 修改原因：截图中的 Edge UI 仍有英文硬编码，中文环境下应显示中文文案。
- 本次已消除的不确定性：确认 Edge UI 文案集中在 `NetworkEdgeInspectorFields`、`EdgeModeSelector`、`EditableEdge` 和 `UDMPropertyPanel`；确认 `NETWORK_EDGE_KIND_OPTIONS` 只作为 UI option/fallback 使用，contract 值不应翻译。
- 本次采用的关键假设：仅翻译显示文案；`edge_kind`、`flow_spec.mode`、`component_policy.mode` 等保存值继续保持英文 contract 值。
- 剩余不确定性：中文术语若需要产品侧进一步统一，可后续只调整 i18n 文案。
- 影响范围 / 风险控制方式：最小前端显示层变更；不改数据结构、不改接口、不引入依赖。
- 验证方式：`cd frontend; npx tsc --noEmit` 通过。
- 是否更新 README：否，未改变目录职责、公共接口或长期维护约定。
- 是否需要新增或更新架构决策（.ai/decisions/）：否。
