## 16:34 - AutoWaterSimu Next standalone Phase 0 baseline

- 用户目标：完成 AutoWaterSimu Next 独立开发实施计划，并在每个 phase 完成后提交并推送 `codex/autowatersimu-next-rebuild`。
- 涉及目录：`docs/rebuild/`、`.ai/decisions/`、`.ai/changes/`、`tasks/`。
- 修改内容：新增 standalone route/caller/data migration matrix；新增 standalone boundary/provider-mode ADR；更新 `docs/rebuild` 索引；记录 Phase 0 任务计划与复盘。
- 修改原因：Phase 0 要先冻结基线、明确 FastAPI/login/static-token 差距和后续 phase owner/target，避免直接把 1-7 阶段混成一次高风险改动。
- 本次已消除的不确定性：确认当前分支为 `codex/autowatersimu-next-rebuild`；确认 Go API 当前仍以 static token 为默认 auth；确认 frontend layout/main/tests 仍依赖 `access_token`、`/login`、`/users/me`；确认当前没有 `docker-compose.standalone.yml` 或 `standalone-*` just target。
- 本次采用的关键假设：Phase 0 只交付文档、矩阵、ADR 和证据记录；运行时代码从 Phase 1 开始逐步修改。
- 剩余不确定性：Phase 1 具体实现时需再次检查 Go auth handler call sites、bind address 配置和 worker no-token兼容范围；Phase 2 需决定 standalone route tree 是复用 `_layout` 还是单独拆分。
- 影响范围 / 风险控制方式：只新增/更新长期文档和任务记录，不修改运行时代码、不删除 legacy 文件、不引入依赖。
- 验证方式：`git diff --check -- docs\rebuild .ai\decisions .ai\changes tasks` 通过（仅 LF/CRLF warning）；Phase 0 rg scans 完成；`scripts\ci\pr-fast.ps1` passed；`scripts\ci\integration-smoke.ps1 -StartCompose` passed；`scripts\ci\current-flow-live-smoke.ps1` passed；`scripts\ci\golden-scenarios.ps1` completed with `status=partial` (`6 partial`, `2 missing`)，作为后续 phase 缺口证据。
- 是否更新 README：更新 `docs/rebuild/README.md` 索引。
- 是否需要新增或更新架构决策（.ai/decisions/）：新增 `0017-standalone-boundary-and-provider-modes.md`。

## 17:50 - AutoWaterSimu Next standalone Phase 1 runtime/no-auth foundation

- 用户目标：继续完成 AutoWaterSimu Next 独立开发实施计划 Phase 1，并在 phase 完成后提交推送 `codex/autowatersimu-next-rebuild`。
- 涉及目录：`apps/api/`、`services/simulation-worker/`、`scripts/ci/`、`scripts/`、根 `Justfile`、`docker-compose*.yml`、`README.md`、`tasks/`。
- 修改内容：新增 Compute API auth mode/provider 抽象、disabled provider、loopback/remote no-auth guard、显式 contracts/migrations runtime path；保留 static-token provider 并在旧 dev compose/performance smoke 显式启用；worker token 为空时不发送 Authorization header；新增 standalone compose、worker Dockerfile、standalone Justfile targets 和 no-auth smoke；补充 Go/Python 测试与 README。
- 修改原因：Phase 1 要先让 Go API/worker/compose 在无登录、无 FastAPI backend 的 standalone 边界内可启动和验证，同时不能削弱现有 static-token security lane。
- 本次已消除的不确定性：确认 handler 可通过 `PrincipalProvider` 接口复用现有 scope/data-scope 逻辑；确认 no-auth API 可在 standalone compose API+Postgres 下创建/读取 job；确认 worker 空 token 不再发送空 bearer header；确认 static-token security smoke 仍通过；确认 standalone compose 默认服务不包含 backend。
- 本次采用的关键假设：Phase 1 不修改前端登录路由和 `/users/me` 依赖；standalone frontend route shell 属于 Phase 2；worker Dockerfile build 阶段安装依赖即可满足“运行时不 pip install”，完整 release/offline image gate 仍在后续 release phase 加严。
- 剩余不确定性：本地 worker image build 在大 Python wheel 安装阶段超过 15 分钟超时，尚未完成镜像构建验证；full MinIO artifact profile 当前只提供 profile service skeleton，artifact archive wiring 仍需后续 profile/gate 验证；清理第一次 standalone compose 时曾用默认 project + `--remove-orphans`，误删同 project orphan 容器，随后已将 Justfile standalone targets 固定为 `autowatersimu-standalone` 且移除 orphan 清理。
- 影响范围 / 风险控制方式：不删除 legacy backend/frontend；旧 dev compose 显式保持 `static_token`；production 禁止 disabled auth；disabled auth 非 loopback bind 必须显式 opt-in；新增 smoke 不启动或清理 legacy backend。
- 验证方式：`go test ./cmd/compute-api ./internal/platform/auth ./internal/platform/contracts ./internal/compute -count=1` 通过；`backend\.venv\Scripts\python -m pytest services\simulation-worker\tests\test_worker_cli.py -k "api_once or api_loop" -q` 通过；`docker compose -f docker-compose.standalone.yml config --services` 和 `--profile full config --services` 通过；`just --list` 通过；`docker compose -p autowatersimu-standalone -f docker-compose.standalone.yml up -d compute-api` + `scripts\ci\standalone-smoke.ps1` 通过；`docker compose -p autowatersimu-standalone -f docker-compose.standalone.yml down -v` 通过；`scripts\ci\security-smoke.ps1` 通过；`docker compose ... build simulation-worker` 未完成（超时）。
- 是否更新 README：更新 root README、`apps/api`、`cmd/compute-api`、`internal/platform/auth`、`internal/platform/config`、`services/simulation-worker`、`scripts` 和 `scripts/ci` README。
- 是否需要新增或更新架构决策（.ai/decisions/）：不新增；沿用 Phase 0 `0017-standalone-boundary-and-provider-modes.md`。

## 18:29 - AutoWaterSimu Next standalone Phase 2 frontend no-auth shell

- 用户目标：继续完成 AutoWaterSimu Next 独立开发实施计划 Phase 2，并在 phase 完成后提交推送 `codex/autowatersimu-next-rebuild`。
- 涉及目录：`frontend/`、`frontend/src/shared/`、`frontend/src/routes/`、`frontend/src/components/Common/`、`frontend/tests/`、`scripts/ci/`、根 `Justfile`、`docker-compose*.yml`、`tasks/`。
- 修改内容：新增前端 runtime config 统一读取 `VITE_APP_MODE`、`VITE_AUTH_MODE`、`VITE_CONTEXT_MODE`；standalone shell 跳过 legacy login redirect、`/users/me` session query 和全局 401/403 `/login` 重定向；standalone 下 Compute client 不发送 bearer token；sidebar/user menu 隐藏用户/admin/items 控件；ASM3 standalone 下不再调用 legacy user permission check；live browser specs 移除 `/users/me` mock 和 `access_token` 注入，并断言无 `/login`、`/api/v1/users*` 请求；current-flow live smoke 以 no-auth Compute API 和空 worker token 运行。
- 修改原因：Phase 2 要让清空 localStorage 的浏览器可以进入现有 Compute 工作台，并用真实 Go API + worker 完成 current flow，而不是依赖 legacy FastAPI 登录/session mock。
- 本次已消除的不确定性：确认 `_layout`、`useAuth`、`main.tsx`、`asm3.tsx` 是当前进入 Compute 页面时的主要 legacy auth 触点；确认 no-auth API 在 dev compose 容器内需要显式 `COMPUTE_API_BIND_ADDR=0.0.0.0` 与 `COMPUTE_API_ALLOW_REMOTE_NO_AUTH=true` 才能被宿主机 browser smoke 访问；确认 current-flow live smoke 可在无 browser/worker token 下完成 job create、worker succeed、result/evidence/evidence-ref read。
- 本次采用的关键假设：保留 legacy login/signup/settings/admin route 文件和 generated FastAPI client 供非 standalone 或后续迁移使用；Phase 2 只移除 standalone shell 的运行时登录依赖，不做完整 route tree split 或 FastAPI client 零 import audit；`live-backend-browser-smoke` 仍可用 static-token Compute API 读取 integration 预置 job，但前端 shell 不再依赖 legacy user session。
- 剩余不确定性：完整 standalone route-tree 拆分、Admin/Items/Login route 从 standalone bundle 中移除、Scenario/Graph/UDM CRUD 全部迁到 Go wrappers、runtime import gate 零发现仍在后续 phase；`live-backend-browser-smoke.ps1` 的脚本变更本阶段未完整执行，仅由 TypeScript 编译和 current-flow live gate 间接覆盖。
- 影响范围 / 风险控制方式：不删除 legacy auth 页面、legacy generated client 或旧管理页面；dev compose 默认仍为 `static_token`；standalone/no-auth 只由显式 runtime env/standalone scripts 开启；current-flow no-auth 容器远程绑定只在 smoke env 中显式打开。
- 验证方式：`cd frontend; npx tsc --noEmit` 通过；`docker compose -f docker-compose.dev.yml config --services` 通过；`docker compose -f docker-compose.standalone.yml config --services` 通过；`scripts\ci\current-flow-live-smoke.ps1` 通过（no-auth API、空 worker token、Playwright no legacy auth request assertion、evidence download/ref resolution）；`docker compose -p autowatersimu-standalone -f docker-compose.standalone.yml up -d compute-api` + `scripts\ci\standalone-smoke.ps1` + `docker compose -p autowatersimu-standalone -f docker-compose.standalone.yml down -v` 通过。
- 是否更新 README：更新 `frontend/README.md`、`frontend/src/README.md`、`frontend/src/shared/README.md`、`frontend/src/shared/api/README.md`、`frontend/src/routes/README.md` 和 `scripts/ci/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：不新增；继续沿用 Phase 0 standalone boundary/provider mode ADR。

## 19:11 - AutoWaterSimu Next standalone Phase 3 workspace persistence

- 用户目标：继续完成 AutoWaterSimu Next 独立开发实施计划 Phase 3，并在 phase 完成后提交推送 `codex/autowatersimu-next-rebuild`。
- 涉及目录：`apps/api/`、`apps/api/internal/compute/`、`apps/api/internal/platform/contracts/`、`apps/api/migrations/`、`apps/api/openapi/`、`frontend/src/client/compute/`、`frontend/src/features/`、`frontend/src/services/`、`frontend/src/shared/api/`、`frontend/src/stores/`、`docs/rebuild/`、`tasks/`。
- 修改内容：新增 Scenario、CanvasGraph、ContextSnapshot workspace DTO/store/service/HTTP/OpenAPI/migration；CanvasGraph publish 派生 immutable ProcessGraph version 并可注册 SimulationInput；Scenario simulation-check 走最新 published ProcessGraph 并带 scenario/context refs；evidence package 暴露 context snapshot ref；前端新增 workspace wrapper，standalone `flowStore` 保存/加载/列表/更新/归档改走 Go workspace API；更新 generated Compute client 和相关 README。
- 修改原因：Phase 3 要让独立运行模式拥有不依赖 legacy FastAPI flowcharts 的核心 Scenario/Graph 持久化，并保证 Scenario 运行、ProcessGraph 不可变和 context snapshot evidence 可追溯。
- 本次已消除的不确定性：确认 `canvas_graph.v1` 可通过平台 validator 加载；确认 Memory/Postgres workspace store 能支撑 versioned CanvasGraph 和 context snapshot；确认 Material Balance CanvasGraph publish 后可运行 Scenario simulation-check；确认 standalone `flowStore` 可在不改 legacy runtime 的情况下切到 Go workspace wrapper；确认 unsupported workspace route methods 已返回 405。
- 本次采用的关键假设：Phase 3 只实现 Material Balance CanvasGraph 自动 publish/run；ASM/UDM ProcessGraph builder、Context UI 和完整 standalone route-tree zero-import audit 留到后续 phase；context snapshot 作为 metadata JSON snapshot 保存，不在本阶段建领域化上下文表。
- 剩余不确定性：完整浏览器保存/重启/加载流程图 smoke 尚未单独覆盖 workspace UI；PostgreSQL migration rollback smoke 需要临时数据库和显式 `COMPUTE_API_MIGRATION_DOWN_SMOKE=true`；ASM/UDM workspace publish 仍需 Phase 4/5 明确 builder。
- 影响范围 / 风险控制方式：不删除 legacy FastAPI flowchart API 或 legacy frontend service；frontend `flowStore` 仅在 `isStandaloneRuntime()` 下使用 Go workspace；ProcessGraph 继续走既有 registry/store；CanvasGraph 表只保存 payload metadata，不存结果时间序列或 artifact 内容。
- 验证方式：`cd apps/api; go test ./internal/compute` 通过；`cd apps/api; go test ./...` 通过；`cd frontend; npx tsc --noEmit` 通过；`docker compose -f docker-compose.dev.yml config --services` 通过；`docker compose -f docker-compose.standalone.yml config --services` 通过；`apps/api/openapi/compute.openapi.json` JSON 解析通过；`git diff --check -- apps/api frontend/src/client/compute frontend/src/features frontend/src/services frontend/src/shared/api frontend/src/stores docs/rebuild tasks/todo.md .ai/changes/2026-06-21.md` 通过（仅 LF/CRLF warning）。
- 是否更新 README：更新 `apps/api/README.md`、`apps/api/internal/compute/README.md`、`apps/api/migrations/README.md`、`apps/api/internal/platform/contracts/README.md`、`frontend/src/stores/README.md`、`frontend/src/services/README.md`，并新增 `frontend/src/features/workspace/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：不新增；继续沿用 Phase 0 standalone boundary/provider mode ADR。

## 20:00 - AutoWaterSimu Next standalone Phase 4 UDM model library and hybrid config

- 用户目标：继续完成 AutoWaterSimu Next 独立开发实施计划 Phase 4，并在 phase 完成后提交推送 `codex/autowatersimu-next-rebuild`。
- 涉及目录：`apps/api/`、`apps/api/internal/compute/`、`apps/api/migrations/`、`apps/api/openapi/`、`frontend/src/client/compute/`、`frontend/src/features/`、`frontend/src/services/`、`frontend/src/components/UDM/`、`scripts/`、`docs/rebuild/`、`tasks/`。
- 修改内容：新增 Go Compute API UDM model library 和 hybrid config DTO/store/service/HTTP/OpenAPI/migration；新增 UDM definition validation、seed templates、immutable version/hash、publish metadata 和 hybrid mapping validation；生成 Compute client；新增 `features/udm/api.ts`；`udmService.ts` 在 standalone runtime 下将 model library 与 hybrid config 方法切换到 Go wrappers；更新 boundary audit 分类和相关 README。
- 修改原因：Phase 4 要让无 FastAPI 的 standalone runtime 可以创建、读取、更新、归档 UDM 模型版本和 hybrid config，并保留 legacy runtime 的 FastAPI 兼容路径。
- 本次已消除的不确定性：确认 legacy UDM model/template/validate/hybrid config 的主要路径和数据结构；确认 Go Memory/Postgres store 能保存 UDM metadata 和 version definitions；确认 standalone frontend 可通过 generated Compute client 调用新 UDM endpoints；确认 unsupported UDM model/config route methods 返回 405；确认 compute boundary audit 已覆盖 workspace/UDM store 分类。
- 本次采用的关键假设：Phase 4 只迁移 UDM model library 与 hybrid config persistence/validation；UDM submit/result、mixed Hybrid worker live smoke、五模型 request builders/result/timeseries 切换仍按计划归入 Phase 5；UDM metadata tables 不存大结果或时间序列。
- 剩余不确定性：Go UDM validation 是 legacy 行为的保守子集，仍需 Phase 5/legacy migration 阶段用 old-vs-new fixtures 做更细 parity；PostgreSQL migration rollback smoke 需要临时数据库和显式 `COMPUTE_API_MIGRATION_DOWN_SMOKE=true`；完整浏览器 UDM editor/hybrid config live smoke 尚未在本阶段单独执行。
- 影响范围 / 风险控制方式：不删除 legacy FastAPI UDM routes 或 legacy frontend client；frontend 仅在 `isStandaloneRuntime()` 下切 Go UDM wrapper；UDM version 更新采用 immutable version + hash；hybrid config archive 使用 metadata soft-delete；generated client 只由 OpenAPI 重新生成并做机械空白清理。
- 验证方式：`cd apps/api; go test ./internal/compute -run UDM -count=1` 通过；`cd apps/api; go test ./internal/compute` 通过；`cd apps/api; go test ./...` 通过；`cd frontend; npx tsc --noEmit` 通过；`docker compose -f docker-compose.dev.yml config --services` 通过；`docker compose -f docker-compose.standalone.yml config --services` 通过；`apps/api/openapi/compute.openapi.json` JSON 解析通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1` 通过；`git diff --check -- apps/api frontend/src/client/compute frontend/src/features frontend/src/services frontend/src/components/UDM docs/rebuild tasks/todo.md .ai/changes/2026-06-21.md scripts` 通过（仅 LF/CRLF warning）。
- 是否更新 README：更新 `apps/api/README.md`、`apps/api/internal/compute/README.md`、`apps/api/migrations/README.md`、`frontend/src/features/README.md`、`frontend/src/features/udm/README.md`、`frontend/src/services/README.md`、`frontend/src/components/UDM/README.md` 和 `scripts/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：不新增；继续沿用 Phase 0 standalone boundary/provider mode ADR。

## 20:42 - AutoWaterSimu Next standalone Phase 5 five-model compute/result cutover

- 用户目标：继续完成 AutoWaterSimu Next 独立开发实施计划 Phase 5，并在 phase 完成后提交推送 `codex/autowatersimu-next-rebuild`。
- 涉及目录：`frontend/src/services/`、`frontend/src/stores/`、`frontend/src/features/compute-jobs/`、`frontend/src/features/lifecycle/`、`frontend/src/shared/api/`、`frontend/src/contracts/`、`frontend/src/components/Flow/`、`frontend/tests/`、`scripts/`、`tasks/`。
- 修改内容：新增 standalone five-model compute adapter；Material Balance、ASM1Slim、ASM1、ASM3、UDM standalone calculation submit/status/result/timeseries/final-values/input-data/delete 切到 Go Compute API；legacy FastAPI calculation SDK 改为非 standalone 动态加载；新增 artifact JSON read、generic job create/list filter、前端 standalone compute 边界审计和五模型 Playwright smoke；更新相关 README 与任务记录。
- 修改原因：Phase 5 要把独立运行模式的五类模型计算和结果读取从 legacy FastAPI `/calculate` 路径切到 Go Compute API/worker 合同，同时保留 legacy runtime 兼容。
- 本次已消除的不确定性：确认五类模型前端服务可生成 `compute_job.v1` / `simulation_input.v1`；确认 ASM1Slim/ASM1/ASM3 参数数组长度与 worker fixture contract 对齐；确认 UDM payload 带 model id/version/hash、processes、parameters 和 variable bindings；确认 standalone 前端路径不再静态导入五类 legacy calculation SDK 或运行时 WebSocket helper；确认 worker 可运行五类独立 fixture。
- 本次采用的关键假设：Phase 5 只切 standalone runtime 的计算提交和结果读取路径；legacy runtime 继续保留 FastAPI generated client；UDM standalone hybrid flowchart validation 暂按本地 shape 校验返回成功，完整 old-vs-new validation parity 留给后续迁移/验收阶段。
- 剩余不确定性：完整真实浏览器五模型 live stack smoke 尚未覆盖所有 legacy UI 操作面；UDM hybrid validation 仍需后续与 legacy 规则细化对齐；large artifact pagination 目前由 adapter 在前端按 artifact JSON 切片，后续若 Go API 增加服务端分页应再收敛。
- 影响范围 / 风险控制方式：不删除 legacy FastAPI services、generated client 或 WebSocket helper；standalone branch 通过 runtime config 显式启用；新增审计脚本防止五模型服务重新静态绑定 legacy calculation SDK；新增 Playwright mock smoke 覆盖五类 job type 和无 legacy endpoint 调用。
- 验证方式：`cd frontend; npx tsc --noEmit` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-frontend-standalone-compute-boundary.ps1` 通过；`cd apps/api; go test ./...` 通过；worker CLI `--run-job` fixtures for Material Balance、ASM1Slim、ASM1、ASM3、UDM 全部通过；`cd frontend; npx playwright test tests/standalone-five-model-compute.spec.ts --project=chromium --no-deps --reporter=line` 通过。
- 是否更新 README：更新 `frontend/src/services/README.md`、`frontend/src/stores/README.md`、`frontend/src/contracts/README.md`、`frontend/src/features/compute-jobs/README.md`、`frontend/src/features/lifecycle/README.md`、`frontend/tests/README.md` 和 `scripts/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：不新增；继续沿用 Phase 0 standalone boundary/provider mode ADR。

## 20:56 - AutoWaterSimu Next standalone Phase 6 legacy migration and FastAPI read-only

- 用户目标：继续完成 AutoWaterSimu Next 独立开发实施计划 Phase 6，并在 phase 完成后提交推送 `codex/autowatersimu-next-rebuild`。
- 涉及目录：`apps/api/cmd/`、`apps/api/internal/legacyimport/`、`apps/api/migrations/`、`scripts/ci/`、`scripts/`、根 `Justfile`、`docs/rebuild/`、`tasks/`。
- 修改内容：新增 `cmd/migrate-legacy` 双 DSN CLI；新增 `internal/legacyimport` 映射与 report 包；新增 `0017_imported_legacy_history` migration；新增 standalone migration smoke 和 Justfile target；更新 apps/api、cmd、migrations、scripts、scripts/ci README 和 standalone implementation plan。
- 修改原因：Phase 6 要提供可重复、可验证、保守的 legacy FastAPI 数据迁移路径，同时让不可安全转换的 job history 以只读形式进入 Next，而不是伪装为可重跑标准 job。
- 本次已消除的不确定性：确认现有仓库没有 legacy migration CLI 或 imported history 表；确认 legacy flowchart/UDM/job 表名与目标 workspace/UDM/job metadata 表结构；确认 CLI 可以在无真实 DSN 情况下通过 mapping tests、help 和 smoke skip 路径验证；确认 Compute API boundary audit 未因新增 internal package/command 产生反向依赖违规。
- 本次采用的关键假设：Phase 6 只提供迁移工具与幂等/verify 基础，不擅自修改生产 legacy DB 权限；legacy DB 写保护通过 CLI read-only transaction 加工具约束，实际 DB-level read-only 切换需要运维 DSN/权限窗口；只有 canonical terminal `compute_job.v1` legacy input 可导入 `compute_jobs`，其他 job history 进入 `imported_legacy_history`。
- 剩余不确定性：未提供 legacy/target PostgreSQL DSN，未执行真实 live DB dry-run、resume、verify-only 或 old-vs-new selected golden；实际 legacy DB read-only 权限切换和备份/恢复演练仍需外部环境支持；真实历史 job 可转换比例需等 fixture DB 或生产副本审计。
- 影响范围 / 风险控制方式：不修改 legacy FastAPI runtime 或 DB；新增迁移表为 append-only history；目标已存在且 legacy source hash 不同会报告 conflict 而非覆盖；新增 smoke 在缺少 DSN 时只跑 Go tests 并明确 skipped live dry-run。
- 验证方式：`cd apps/api; go test ./internal/legacyimport ./cmd/migrate-legacy` 通过；`cd apps/api; go test ./...` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\standalone-migration-smoke.ps1` 通过（live DB dry-run 因未设置 DSN 被跳过）；`cd apps/api; go run ./cmd/migrate-legacy --help` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1` 通过；`just --list` 通过。
- 是否更新 README：更新 `apps/api/README.md`、`apps/api/cmd/README.md`、新增 `apps/api/cmd/migrate-legacy/README.md`、新增 `apps/api/internal/legacyimport/README.md`、更新 `apps/api/migrations/README.md`、`scripts/README.md`、`scripts/ci/README.md` 和 standalone implementation plan。
- 是否需要新增或更新架构决策（.ai/decisions/）：不新增；legacy migration 的保守转换策略记录在 README 与实施计划，长期边界继续沿用 Phase 0 standalone boundary/provider mode ADR。

## 21:25 - AutoWaterSimu Next standalone Phase 7 release gate and RC evidence

- 用户目标：继续完成 AutoWaterSimu Next 独立开发实施计划 Phase 7，并在 phase 完成后提交推送 `codex/autowatersimu-next-rebuild`。
- 涉及目录：`scripts/ci/`、`scripts/release/`、根 `Justfile`、根 `README.md`、`scripts/` README、`docs/rebuild/`、`tasks/`。
- 修改内容：新增 standalone five-model smoke、standalone backup/restore smoke、standalone release gate；新增 `just standalone-five-model-smoke`、`standalone-backup-restore-smoke`、`standalone-release-gate`、`standalone-browser-smoke` 和 `standalone-golden`；更新 README 与实施计划记录。
- 修改原因：Phase 7 要把 FastAPI-free standalone release/RC 条件转成可执行 evidence gate，同时把 live DB restore、migration rollback、compose live smoke 和 image build 等外部环境依赖从默认本地路径中显式区分出来。
- 本次已消除的不确定性：确认现有 release gate 是 Desktop/Next 通用 gate，不是 Web standalone RC gate；确认已有 standalone smoke 只覆盖已运行 API no-auth，不覆盖五模型 worker/frontend、backup/restore 或 RC 汇总；确认已有 operations runbook 有手工 backup/restore 指南但没有可执行 standalone smoke。
- 本次采用的关键假设：Phase 7 以运行路径和发布 gate evidence 证明 FastAPI 退出，不删除 legacy `backend/` 源码；默认 release gate 可在无外部 DSN/镜像构建环境时记录 `passed_with_skips`，完整 RC 需要显式补跑 live flags；backup/restore live restore 只能写入显式临时 restore DSN。
- 剩余不确定性：未提供 temporary PostgreSQL restore DSN、真实 legacy/target DB DSN 或可接受的长耗时 image build 窗口时，live restore、migration rollback、full image build 和完整 RC 全绿仍只能记录为 skipped；当前 `standalone-release-gate.ps1 -SkipLong` 的硬检查已通过，但 evidence status 为 `passed_with_skips`。
- 影响范围 / 风险控制方式：不修改 runtime API、worker、frontend 业务逻辑；不删除 legacy 源码；新增脚本默认不连接数据库、不执行 destructive restore、不启动或清理 compose；RC gate 对 hard failure 返回非零，对外部缺口写入 evidence status。
- 验证方式：`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\standalone-backup-restore-smoke.ps1` 通过（evidence `passed_with_skips`，live DB/artifact dir 未提供）；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\standalone-five-model-smoke.ps1` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\release\standalone-release-gate.ps1 -SkipLong` 通过（evidence `passed_with_skips`，hard checks passed，image build / migration rollback / live API smoke skipped）；`just --list` 通过；`git diff --check -- README.md Justfile scripts docs\rebuild tasks\todo.md .ai\changes\2026-06-21.md` 通过（仅 LF/CRLF warning）。
- 是否更新 README：更新 root README、`scripts/README.md`、`scripts/ci/README.md`、`scripts/release/README.md` 和 standalone implementation plan。
- 是否需要新增或更新架构决策（.ai/decisions/）：不新增；沿用 Phase 0 standalone boundary/provider mode ADR。

## 22:10 - Standalone RC Phase A gate skip hardening

- 用户目标：把 standalone release gate 的 full RC 模式从 `passed_with_skips` 收敛到真失败，避免跳过项被误读为完成。
- 涉及目录：`scripts/release/`、根 `README.md`、`tasks/`、`.ai/changes/`。
- 修改内容：`standalone-release-gate.ps1` 新增 `-FailOnSkip`，并在同时传入 `-RunComposeSmoke -RunBackupRestoreLive -RunPostgresMigrationSmoke -RunReleaseImageSmoke` 且未传 `-SkipLong` 时自动启用 fail-on-skip；full RC 下任意 skip 会写出 `status=failed` 并返回非零。
- 修改原因：Phase A 要让 full RC gate 的跳过项变成硬失败，快速本地模式仍可保留 `passed_with_skips` 作为未完成 evidence。
- 本次已消除的不确定性：确认当前脚本只把 skip 降级为 `passed_with_skips`，不会在 full flags 下失败；确认 standalone compose 当前已将容器内 API 绑定为 `0.0.0.0` 并映射宿主 `127.0.0.1:8088`。
- 本次采用的关键假设：完整 RC 命令以用户给出的四个 live flags 为准；`-SkipLong` 不属于完整 RC。
- 剩余不确定性：严格 `-FailOnSkip` expected-fail 尚未单独跑通；真实 full RC 仍需实际 compose、临时 PostgreSQL restore DB、migration rollback、image build 和 golden passed evidence。
- 影响范围 / 风险控制方式：只改变 release gate 状态判定和文档；不改 runtime、compose service 或 DB 行为。
- 验证方式：`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\release\standalone-release-gate.ps1 -SkipLong` 已运行但未通过；hard checks 与 golden summary 已到达，失败点为既有 `standalone five-model smoke` Playwright `AxiosError`。为排除 mock CORS，已给 `frontend/tests/standalone-five-model-compute.spec.ts` 补充 OPTIONS/CORS 响应并重跑 `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\standalone-five-model-smoke.ps1`，仍在同一 Playwright submit step 失败；本次按用户要求提交推送，后续需继续修复该 smoke blocker 后再取得完整 passed evidence。
- 是否更新 README：更新 root README 和 `scripts/release/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否。

## 22:15 - Standalone five-model smoke port isolation

- 用户目标：继续收敛 standalone RC gate，让 Phase A 验证不再被本地已占用的 5173 dev server 干扰。
- 涉及目录：`frontend/`、`frontend/tests/`、`scripts/ci/`、`tasks/`、`.ai/changes/`。
- 修改内容：`frontend/playwright.config.ts` 支持 `PLAYWRIGHT_PORT`、`PLAYWRIGHT_BASE_URL`、`PLAYWRIGHT_DEV_SERVER_COMMAND` 和 `PLAYWRIGHT_REUSE_EXISTING_SERVER` 覆盖；`standalone-five-model-smoke.ps1` 在 5174 启动 isolated Vite server 并禁用已有 server 复用；`frontend/tests/README.md` 记录 focused smoke 可使用 `PLAYWRIGHT_*` 覆盖。
- 修改原因：本地 5173 被 Docker/WSL 占用时，Playwright 会复用错误前端实例，导致 standalone env 未生效并在五模型 submit step 抛出 AxiosError。
- 本次已消除的不确定性：确认 worker fixtures 不是 blocker；确认独立端口后 `standalone-five-model-smoke.ps1` 通过；确认快速 release gate 可走完并保持 `passed_with_skips`；确认 `-FailOnSkip` 会把 skip 转成 `status=failed` 和非零退出。
- 本次采用的关键假设：5174 作为本 smoke 的固定本地端口足够简单；若未来端口冲突，可通过同一 `PLAYWRIGHT_*` 覆盖改端口。
- 剩余不确定性：真实 full RC 仍需 compose live smoke、临时 PostgreSQL migration/restore DB、release image smoke 和 golden scenarios `passed` evidence。
- 影响范围 / 风险控制方式：只调整测试运行隔离；不改 frontend runtime、业务服务、Go API 或 Docker compose。
- 验证方式：`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\standalone-five-model-smoke.ps1` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\release\standalone-release-gate.ps1 -SkipLong` 通过（evidence `passed_with_skips`）；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\release\standalone-release-gate.ps1 -SkipLong -FailOnSkip` 按预期返回 1（evidence `status=failed`、`fail_on_skip=true`）；release gate 内 `frontend standalone typecheck` 和 `frontend standalone compute boundary audit` 通过。
- 是否更新 README：更新 `frontend/tests/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否。

## 23:34 - Standalone full RC live gate support

- 用户目标：继续把 standalone RC 从 `passed_with_skips` 收敛到 full gate `passed`，并推进 FastAPI-free release image 与 golden evidence。
- 涉及目录：根 `.dockerignore` / `.gitignore`、`scripts/ci/`、`scripts/release/`、`frontend/src/features/`、`frontend/src/services/`、`tasks/`、`.ai/changes/`。
- 修改内容：新增可跟踪的根 `.dockerignore`，把 standalone worker build context 限制到 `contracts/`、`simulation_core/` 和 `services/simulation-worker/`；`standalone-backup-restore-smoke.ps1` 在宿主无 `pg_dump`/`pg_restore` 时使用 Docker `postgres:17` client fallback；`standalone-release-gate.ps1` 将 release image step 改为 standalone worker image content/self-check smoke，验证 image 内无 backend 目录；`golden-scenarios.ps1` 支持场景与整体 `passed` 状态；`standaloneComputeService.ts` 不再直接导入 generated Compute client types，改从 feature wrapper re-export 获取。
- 修改原因：full RC 需要 live backup/restore、migration rollback、image content 和 golden evidence 都可在本机稳定验证；原 release image build 会长时间重装 heavy Python 依赖，原 golden summary 没有 `passed` 输出路径，原 service type import 违反 PR fast boundary。
- 本次已消除的不确定性：确认 `localhost:8088/readyz` 在 standalone compose 下返回 200 ready；确认 `standalone-smoke.ps1` no-auth create/read 通过；确认临时 `compute_rc` / `restore_rc` 库可支持 live backup/restore 与 migration up/down；确认刷新 live lanes 后 `golden-scenarios.json` 可达到 `status=passed` 且 8/8 scenarios passed；确认 `scripts\ci\pr-fast.ps1` 通过。
- 本次采用的关键假设：release image smoke 以已准备好的 `autowatersimu-standalone-simulation-worker:latest` 为检查对象；若未来 CI 必须从零构建 heavy worker image，应放到单独可缓存的 build job，而不是每次 full gate 重新安装 torch/scipy。
- 剩余不确定性：需要提交后在新 HEAD 重新刷新 live golden lanes，并重新运行 full standalone release gate；当前通过的 full gate/live golden evidence 生成时 worktree 仍是 dirty。
- 影响范围 / 风险控制方式：只改 gate/smoke/test boundary 和 type import 边界；不改 runtime API、worker计算逻辑、数据库 schema 或 compose service list；临时 restore 只写入 `AUTOWATERSIMU_RESTORE_DATABASE_URL`。
- 验证方式：`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\standalone-backup-restore-smoke.ps1 -RunLive` 通过；`cd apps\api; go test ./internal/compute -run 'TestPostgresMigrations(Up|Down)Smoke' -count=1` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1` 通过；`cd frontend; npx tsc --noEmit` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\golden-scenarios.ps1 -RunIntegrationSmoke -RunLiveBackendBrowserSmoke -RunCurrentFlowLiveSmoke` 通过并生成 `status=passed`。
- 是否更新 README：更新 `scripts/release/README.md`、`frontend/src/features/compute-jobs/README.md`、`frontend/src/features/lifecycle/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否。
