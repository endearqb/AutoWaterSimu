## 22:15 - Standalone RC result and UDM blocker fix docs

- 用户目标：根据 standalone 静态审查结论制定修复目标、需求文档与开发计划文档。
- 涉及目录：`docs/rebuild/`、`.ai/decisions/`、`.ai/changes/`、`tasks/`。
- 修改内容：新增 standalone RC blocker fix PRD、开发计划和 ADR，更新 docs/rebuild 索引与 tasks/todo 记录。
- 修改原因：审查确认 standalone “分析结果”与 UDM 模板为 P0 阻断项，需要在实现前固化修复目标、非目标、PR 拆分和验收门槛。
- 本次已消除的不确定性：确认 result envelope 不应回退为 inline time-series；确认 `standaloneComputeService.ts` 存在 envelope-as-legacy-result、label 被数组化、缺 artifact 返回空时序、final values 全量下载等同源问题；确认 Go `simpleUDMTemplate()` 与 validation 路径存在模板漂移和未定义符号风险。
- 本次采用的关键假设：以 legacy `backend/app/services/udm_seed_templates.py` 作为 canonical UDM catalog 初始 oracle；`material_balance_time_series.v1` 作为新的长期 time-series artifact payload 合同名；本次只制定文档，不修改运行时代码。
- 剩余不确定性：worker 当前所有 job type 输出的 artifact payload 是否完全同构仍需 PR-1 fixture/schema 验证；历史坏模板数量和用户编辑状态需后续审计脚本确认；真实分页/chunked artifact 方案仍为 P2。
- 影响范围 / 风险控制方式：仅新增和更新文档记录，不修改公共 API、schema、runtime 代码或测试脚本；长期边界通过 ADR 记录。
- 验证方式：`git diff --check -- docs\rebuild .ai\decisions .ai\changes tasks`。
- 是否更新 README：是，更新 `docs/rebuild/README.md` 索引和维护约定。
- 是否需要新增或更新架构决策（.ai/decisions/）：是，新增 `0018-standalone-result-artifact-and-udm-catalog-boundary.md`。

## 22:30 - PR-1 time-series artifact payload contract

- 用户目标：根据 Standalone RC blocker fix PRD/开发计划先完成 time-series artifact payload 合同，让 worker 与前端 decoder 后续共享同一事实源。
- 涉及目录：`contracts/`、`services/simulation-worker/`、`apps/api/internal/platform/contracts/`、`frontend/src/client/compute/`、`tasks/`、`.ai/changes/`。
- 修改内容：新增 `material_balance_time_series.v1` schema 与 valid/invalid fixtures；registry/codegen manifest/Go validator 纳入新合同；worker time-series artifact payload schema_version 改为 canonical 名称并在写文件前校验 schema 与序列长度；worker tests 验证 payload schema；generated Compute client 末尾换行由合同 gate 标准化。
- 修改原因：standalone 分析弹窗需要从 artifact bytes 读取真实 `timestamps/node_data/edge_data`，不能继续依赖未注册 schema 或 envelope-as-result shape。
- 本次已消除的不确定性：确认当前 worker 原先输出未注册的 `material_balance_time_series_artifact.v1`；确认 JSON Schema 需配合语义测试覆盖“序列长度等于 timestamps 长度”；确认 `artifact.v1` 仍只表达 metadata。
- 本次采用的关键假设：time-series payload 可先保持 JSON object 形态，不在 P0 引入 chunked/分页 artifact；节点/边字段中数组代表变量序列，`label/source/target` 等标量 metadata 必须保留为标量。
- 剩余不确定性：提交前 `scripts/check-contracts.ps1` 会因 generated Compute client 末尾换行相对 HEAD 产生 drift，需要本 phase commit 后重跑确认；前端 `getAnalysisResult()` 和 UI gate 仍属于 PR-2。
- 影响范围 / 风险控制方式：不改变 `compute_result.v1` envelope，不把大时序写入主表 JSON；worker 遇到坏 artifact payload 会让 job failed，避免上传坏 artifact。
- 验证方式：`backend\.venv\Scripts\python -m pytest contracts\tests -q`（117 passed）；`backend\.venv\Scripts\python -m pytest services\simulation-worker\tests\test_worker_cli.py -q`（20 passed）；`cd apps\api; go test ./internal/platform/contracts ./internal/compute -run Contract -count=1`（passed）；`git diff --check -- contracts services\simulation-worker apps\api\internal\platform\contracts frontend\src\client\compute`（仅 LF/CRLF warning）。
- 是否更新 README：是，更新 `contracts/examples/valid/README.md` fixture 索引。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，沿用 `0018-standalone-result-artifact-and-udm-catalog-boundary.md`。

## 22:39 - PR-3 canonical UDM seed catalog

- 用户目标：建立单一 canonical UDM seed catalog，让 standalone Go API 与 legacy Python 模板服务不再各自维护生产模板事实。
- 涉及目录：`contracts/`、`backend/app/services/`、`apps/api/internal/compute/`、`apps/api/internal/platform/contracts/`、`tasks/`、`.ai/changes/`。
- 修改内容：新增 `udm_seed_catalog.v1` schema、minimal fixture、canonical `contracts/catalogs/udm_seed_catalog.v1.json` 与目录 README；从 legacy 模板提取 7 个模板并写入 schema/revision/content hash/seed_source；Python `udm_seed_templates.py` 改为读取 catalog；Go `udm_workspace.go` 改为读取同一 catalog，from-template 写入 `udm_seed_catalog.v1:<key>` seed source 和 template hash/revision metadata；删除生产路径 `simpleUDMTemplate()`；新增 Go/Python/contract parity tests。
- 修改原因：Go 极简模板与 legacy ASM1/ASM1Slim/ASM3/Petersen 模板漂移，导致 standalone template validate/create 行为无法达到 RC parity。
- 本次已消除的不确定性：确认 canonical catalog 精确包含 `asm1`、`asm1slim`、`asm3`、`petersen-chapter-1/2/3/7`；确认 ASM1 为 11/19/8、ASM1Slim 为 5/7/3、ASM3 为 13/22/7；确认 7 个模板均可通过 Go teaching validation 并 create-from-template。
- 本次采用的关键假设：catalog 初始内容以 legacy `backend/app/services/udm_seed_templates.py` 为 oracle；`template_content_hash` 基于模板定义字段复算，不包含自身 hash。
- 剩余不确定性：历史坏模板审计和未编辑模型追加修正版仍属于 PR-5/P1；backend DB 依赖的 API route 测试在当前本地环境因数据库 503 无法作为本次验证依据。
- 影响范围 / 风险控制方式：保持现有 HTTP route shape，不新增数据库字段或 OpenAPI 字段；只把已有 record metadata 写入模板 hash/revision，历史模型版本不原地改写。
- 验证方式：`backend\.venv\Scripts\python -m pytest contracts\tests -q`（122 passed）；`cd backend; .venv\Scripts\python -m pytest app\tests\udm_tutorial_validation_test.py -q`（11 passed）；`cd apps\api; go test ./internal/compute -run UDM -count=1`（passed）；`cd apps\api; go test ./...`（passed）；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-contracts.ps1`（passed）。另尝试 `cd backend; .venv\Scripts\python -m pytest app\tests\udm_tutorial_validation_test.py app\tests\api\routes\udm_models_validate_test.py -q`，其中 API route 测试因本地数据库登录 503 失败。
- 是否更新 README：是，新增 `contracts/catalogs/README.md` 并更新 `contracts/README.md`、`contracts/examples/valid/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，沿用 `0018-standalone-result-artifact-and-udm-catalog-boundary.md`。

## 23:08 - PR-2 frontend analysis result adapter

- 用户目标：让 standalone 下 ASM1Slim、ASM1、ASM3、UDM 的分析弹窗从真实 time-series artifact 读取 analyzer 可消费的 `timestamps/node_data/edge_data/summary`，并显示 loading/error/cache 行为。
- 涉及目录：`frontend/src/services/`、`frontend/src/stores/`、`frontend/src/components/Flow/legacy-analysis/`、`frontend/src/components/Flow/inspectorbar/`、`frontend/src/components/Flow/toolbar/`、`frontend/src/i18n/`、`frontend/tests/`、`tasks/`、`.ai/changes/`。
- 修改内容：新增 `standaloneComputeService.getAnalysisResult(jobId)`，严格读取 job snapshot/result envelope/time-series artifact 并校验 schema、job_id、序列长度和数值数组；保留 `label/source/target` 等标量 metadata；同 job analysis result Promise/result 缓存，失败时清缓存以支持 retry；`safeGetResult()` 只容忍 404，权限/500/网络错误不再静默吞掉；`getJobInputData()` 不再把 `compute_result.v1` envelope 放入 legacy `result_data`；各模型 service/store 增加 analysis result 状态和 loader 方法；分析按钮改为 job success 后启用，弹窗内懒加载 artifact 并显示 loading/error/retry；standalone compute Playwright smoke 覆盖四类 analyzer 成功、重复打开不重复下载、缺 artifact/job_id mismatch/schema mismatch/series length mismatch 错误；live spec 增加真实 artifact 的 `getAnalysisResult()` 断言。
- 修改原因：原 standalone adapter 将 result envelope 误传给 legacy analyzer，导致 truthy 但无顶层 `timestamps` 的空图；同时原 `sliceSeriesMap()` 会把 label/source/target 转为空数组，破坏节点和边 metadata。
- 本次已消除的不确定性：确认 analyzer 实际需要顶层 legacy shape；确认前端可从 result/snapshot artifacts 精确选择 `material_balance.time_series` artifact；确认 legacy inline shape 仍可在有顶层 `timestamps/node_data` 时被展示；确认 `getJobInputData()` 的 standalone result_data 字段应保持空或 legacy-compatible，而不是 envelope。
- 本次采用的关键假设：P0 不把完整 time-series 写回 `compute_result.v1.data`；旧 `time_series_json`/`time_series` artifact type 仅作为读取兼容 fallback；analysis state 放在现有模型 store 中，不新建 `features/results/` 目录。
- 剩余不确定性：完整 live gate 两次运行均在 worker claim 阶段失败，错误为 `POST /api/v1/workers/.../claim request failed: [WinError 10054]`，浏览器提交五个 job 的步骤已通过但 worker 未完成 job，因此未能覆盖 live result/analysis verify 阶段；该失败点未指向本次前端 adapter，但仍阻塞完整 live evidence。
- 影响范围 / 风险控制方式：不修改 worker/API schema，不内联大时序，不重写 legacy analyzer UI；按钮入口复用原 analyzer，新增 loader 仅负责 lazy load、error 和 retry；store 中用 jobId guard 防止旧请求覆盖新任务。
- 验证方式：`cd frontend; npx tsc --noEmit`（passed）；`cd frontend; $env:VITE_APP_MODE='standalone'; $env:VITE_AUTH_MODE='disabled'; $env:VITE_CONTEXT_MODE='standalone'; npx playwright test tests/standalone-five-model-compute.spec.ts --project=chromium --no-deps --reporter=line`（1 passed）；`git diff --check`（仅 LF/CRLF warning）；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\standalone-five-model-live.ps1`（两次失败，均停在 worker claim 连接重置，evidence 写入 `tmp/ci-evidence/standalone-five-model-live.json`）。
- 是否更新 README：否，本次未改变目录职责、公共文件组织或长期维护约定。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，沿用 `0018-standalone-result-artifact-and-udm-catalog-boundary.md`。

## 23:20 - Standalone five-model live gate closeout

- 用户目标：完成 standalone RC blocker fix P0 开发目标，并把每个大 phase 提交推送到 `codex/autowatersimu-next-build`。
- 涉及目录：`services/simulation-worker/`、`frontend/tests/`、`tasks/`、`.ai/changes/`。
- 修改内容：将 worker HTTP client 的 `/claim` 有限重试窗口从 3 次扩到 8 次并保留递增退避；把 worker claim retry 测试改为覆盖连续 3 次瞬时断连后恢复；在 five-model live verify 模式导入 `standaloneComputeService.ts` 前先进入 Vite 页面，避免空白页无法解析 `/src/...` 模块。
- 修改原因：当前 HEAD 的 live worker loop 可真实完成五类 job，但 Windows 本地 Go API/urllib 组合仍可能在 claim 上出现连续短暂连接重置；verify 模式是独立 Playwright 进程，未先导航到 Vite origin 时动态 import 失败。
- 本次已消除的不确定性：确认 Go API server-side 同 worker reclaim 单测通过；确认 worker loop 成功时五类 job 均生成 model_run 与 time-series artifact；确认失败不是固定 UDM payload 或 artifact adapter 语义问题，而是 claim 瞬断窗口与 verify 测试导航问题。
- 本次采用的关键假设：`/claim` 是唯一允许自动重试的 worker mutation，因为 Go API 已提供同 `worker_id` active running job reclaim；artifact upload、succeed/fail 等副作用请求仍不得自动重放。
- 剩余不确定性：完整 standalone RC no-skip/hosted gate、生产 legacy DB read-only rehearsal、MinIO/S3 外部 profile acceptance 仍需独立凭据和当前 commit 证据；本次只关闭 PRD/开发计划中的 P0 local live blocker。
- 影响范围 / 风险控制方式：不改变公共 HTTP contract、schema、OpenAPI 或 generated client；重试仍只匹配 `/api/v1/workers/*/claim` 的底层 request failed；frontend 改动仅影响 Playwright live spec。
- 验证方式：`backend\.venv\Scripts\python -m pytest services\simulation-worker\tests\test_worker_cli.py -k "claim or loop" -q`（3 passed, 17 deselected）；`cd frontend; npx tsc --noEmit`（passed）；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\standalone-five-model-live.ps1`（passed，五类 job 均 succeeded，且每类 1 个 model_run / 1 个 artifact）。
- 是否更新 README：否，本次实现符合既有 worker README 中 `/claim` 有限重试与同 worker reclaim 约定，未改变目录职责或长期接口。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，沿用 `0018-standalone-result-artifact-and-udm-catalog-boundary.md`。

## 23:31 - Standalone analysis legacy time-series schema compatibility

- 用户目标：修复分析结果读取失败，错误为 `Time-series artifact schema_version must be material_balance_time_series.v1`。
- 涉及目录：`frontend/src/services/`、`frontend/tests/`、`.ai/changes/`。
- 修改内容：`standaloneComputeService.getAnalysisResult()` 继续输出 canonical `material_balance_time_series.v1`，但读取时兼容历史 artifact payload schema 名 `material_balance_time_series_artifact.v1`；mock-backed five-model Playwright smoke 新增旧 schema 成功读取断言，并把真正坏 schema 改为 `not_time_series.v1`。
- 修改原因：worker 当前已写 canonical schema，但 PRD 明确旧非注册 schema 可作为迁移兼容输入处理；用户现场仍可能打开升级前生成的旧 job/artifact。
- 本次已消除的不确定性：确认 worker 当前输出 `material_balance_time_series.v1`；确认报错来自前端 analysis payload strict schema guard；确认只需兼容旧 schema 名，不应放宽 job_id、series length 或字段 shape 校验。
- 本次采用的关键假设：旧 `material_balance_time_series_artifact.v1` payload shape 与新合同 shape 等价，只是 schema_version 名称不同。
- 剩余不确定性：若旧 artifact 的字段 shape 也不符合新合同，仍会按原有字段/长度校验报错。
- 影响范围 / 风险控制方式：仅前端 reader 做单一旧 schema 名兼容，不改 worker、合同 registry、OpenAPI 或 artifact metadata。
- 验证方式：`cd frontend; npx tsc --noEmit`（passed）；`cd frontend; $env:VITE_APP_MODE='standalone'; $env:VITE_AUTH_MODE='disabled'; $env:VITE_CONTEXT_MODE='standalone'; npx playwright test tests/standalone-five-model-compute.spec.ts --project=chromium --no-deps --reporter=line`（1 passed）。
- 是否更新 README：否，本次没有改变长期目录职责或公共接口。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，沿用 `0018-standalone-result-artifact-and-udm-catalog-boundary.md`。

## 23:47 - Standalone analysis chart and node label display fix

- 用户目标：修复 ASM1 分析弹窗中空间剖面折线图显示 `暂无数据`，并让节点筛选、柱状图坐标/图例、边浓度图例显示节点自定义名称而不是内部长 ID。
- 涉及目录：`frontend/src/services/`、`frontend/src/components/Flow/legacy-analysis/`、`frontend/tests/`、`.ai/changes/`。
- 修改内容：`standaloneComputeService.getAnalysisResult()` 从 job snapshot 的原始 flowchart/legacy input metadata 提取节点显示名，并注入到 `node_data[id].label`；`TimeSeriesChart` 移除本地 `ResizeObserver` 尺寸门禁，时序数据存在时直接挂载 `ResponsiveContainer`；standalone Playwright smoke 让 mock artifact 不自带 label，并断言分析结果补回自定义 reactor 名称。
- 修改原因：legacy analyzer 各面板已按 `node_data[id].label || id` 展示，现场显示 ID 表明 standalone artifact 入口缺少 label；折线图图例已存在但图内空态，说明不是无序列，而是容器尺寸门禁误判导致未渲染 Recharts。
- 本次已消除的不确定性：确认空间剖面、T95 节点筛选、边时序图例共用 `node_data.label` 作为显示名；确认 `ResponsiveContainer` 外层已具备稳定高度，无需额外以测量结果决定是否渲染。
- 本次采用的关键假设：节点自定义名称的事实源是提交 job 时保存在 metadata 中的原始 flowchart `node.data.label`，artifact 仍保留节点 ID 作为数据索引。
- 剩余不确定性：若历史 job snapshot 中没有原始 flowchart/legacy input metadata，只能保留 artifact 自带 label 或回退 ID。
- 影响范围 / 风险控制方式：不改变 node_data key、edge_data key、schema_version 或 artifact 字段校验；只在读取展示层补 label，并保持无 label 时原回退行为。
- 验证方式：`cd frontend; npx tsc --noEmit`（passed）；`cd frontend; $env:VITE_APP_MODE='standalone'; $env:VITE_AUTH_MODE='disabled'; $env:VITE_CONTEXT_MODE='standalone'; npx playwright test tests/standalone-five-model-compute.spec.ts --project=chromium --no-deps --reporter=line`（1 passed）。
- 是否更新 README：否，本次没有改变目录职责、公共接口或长期维护约定。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，沿用 `0018-standalone-result-artifact-and-udm-catalog-boundary.md`。

## 23:52 - Standalone local CORS DELETE preflight fix

- 用户目标：修复浏览器从 `http://localhost:5173` 删除 UDM 模型时，Go Compute API 预检响应未允许 `DELETE` 导致请求被 CORS 拦截。
- 涉及目录：`apps/api/internal/platform/httpx/`、`.ai/changes/`。
- 修改内容：本地 loopback CORS helper 的 `Access-Control-Allow-Methods` 从 `GET, POST, OPTIONS` 扩展为 `GET, POST, PATCH, DELETE, OPTIONS`；直接测试断言 `PATCH` 与 `DELETE` 均在预检允许列表中。
- 修改原因：UDM 模型库已有浏览器调用的 `PATCH` / `DELETE` 路由，CORS helper 只放行 GET/POST 会让声明了合法 route 的浏览器 mutation 在 preflight 阶段失败。
- 本次已消除的不确定性：确认 `DELETE /api/v1/udm-models/{model_id}` 是 OpenAPI/generated client 和 Go route 已声明的方法；确认现有运行中 API 旧 header 缺少 DELETE；确认当前本地数据来自 `compute_rc` PostgreSQL。
- 本次采用的关键假设：本地 loopback Web UI smoke 需要允许当前 Compute API 已声明的浏览器方法，但仍不扩大允许 origin。
- 剩余不确定性：生产环境若有独立反向代理 CORS 配置，仍需同步检查代理层 allow-methods。
- 影响范围 / 风险控制方式：只修改本地 loopback CORS method header，不改变 auth、route handler、OpenAPI schema、允许 origin 或暴露 header。
- 验证方式：`cd apps\api; go test ./internal/platform/httpx`（passed）；`cd apps\api; go test ./internal/compute -run TestHTTPLocalCORSPreflight -count=1`（passed）；重启本地 8088 Compute API 后，`OPTIONS /api/v1/udm-models/{model_id}` 返回 `Access-Control-Allow-Methods: GET, POST, PATCH, DELETE, OPTIONS`，且 UDM 模型列表仍保留 3 条记录。
- 是否更新 README：否，本次符合既有“loopback CORS allowlist，不扩大 origin”约定。
- 是否需要新增或更新架构决策（.ai/decisions/）：否。
