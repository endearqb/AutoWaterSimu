## 22:15 - Standalone RC result and UDM blocker fix docs

- 用户目标：根据 standalone 静态审查结论制定修复目标、需求文档与开发计划文档。
- 涉及目录：`docs/rebuild/`、`.ai/decisions/`、`.ai/changes/`、`tasks/`。
- 修改内容：新增 standalone RC blocker fix PRD、开发计划和 ADR，更新 docs/rebuild 索引与 tasks/todo 记录。
- 修改原因：审查确认 standalone “分析结果”与 UDM 模板为 P0 阻断项，需要在实现前固化修复目标、非目标、PR 拆分和验收门槛。
- 本次已消除的不确定性：确认 result envelope 不应回退为 inline time-series；确认 `standaloneComputeService.ts` 存在 envelope-as-legacy-result、label 被数组化、缺 artifact 返回空时序、final values 全量下载等同源问题；确认 Go `simpleUDMTemplate()` 与 validation 路径存在模板漂移和未定义符号风险。
- 本次采用的关键假设：以 legacy `backend/app/services/udm_seed_templates.py` 作为 canonical UDM catalog 初始 oracle；`material_balance_time_series.v1` 作为新的长期 time-series artifact payload 合同名；本次只制定文档，不修改运行时代码。
- 剩余不确定性：worker 当前所有 job type 输出的 artifact payload 是否完全同构仍需 PR-1 fixture/schema 验证；历史坏模板数量和用户编辑状态需后续审计脚本确认；真实分页/chunked artifact 方案仍为 P2。
- 影响范围 / 风险控制方式：仅新增和更新文档记录，不修改公共 API、schema、runtime 代码或测试脚本；长期边界通过 ADR 记录。
- 验证方式：`git diff --check -- docs\rebuild .ai\decisions .ai\changes tasks`。
- 是否更新 README：是，更新 `docs/rebuild/README.md` 索引和维护约定。
- 是否需要新增或更新架构决策（.ai/decisions/）：是，新增 `0018-standalone-result-artifact-and-udm-catalog-boundary.md`。

## 22:30 - PR-1 time-series artifact payload contract

- 用户目标：根据 Standalone RC blocker fix PRD/开发计划先完成 time-series artifact payload 合同，让 worker 与前端 decoder 后续共享同一事实源。
- 涉及目录：`contracts/`、`services/simulation-worker/`、`apps/api/internal/platform/contracts/`、`frontend/src/client/compute/`、`tasks/`、`.ai/changes/`。
- 修改内容：新增 `material_balance_time_series.v1` schema 与 valid/invalid fixtures；registry/codegen manifest/Go validator 纳入新合同；worker time-series artifact payload schema_version 改为 canonical 名称并在写文件前校验 schema 与序列长度；worker tests 验证 payload schema；generated Compute client 末尾换行由合同 gate 标准化。
- 修改原因：standalone 分析弹窗需要从 artifact bytes 读取真实 `timestamps/node_data/edge_data`，不能继续依赖未注册 schema 或 envelope-as-result shape。
- 本次已消除的不确定性：确认当前 worker 原先输出未注册的 `material_balance_time_series_artifact.v1`；确认 JSON Schema 需配合语义测试覆盖“序列长度等于 timestamps 长度”；确认 `artifact.v1` 仍只表达 metadata。
- 本次采用的关键假设：time-series payload 可先保持 JSON object 形态，不在 P0 引入 chunked/分页 artifact；节点/边字段中数组代表变量序列，`label/source/target` 等标量 metadata 必须保留为标量。
- 剩余不确定性：提交前 `scripts/check-contracts.ps1` 会因 generated Compute client 末尾换行相对 HEAD 产生 drift，需要本 phase commit 后重跑确认；前端 `getAnalysisResult()` 和 UI gate 仍属于 PR-2。
- 影响范围 / 风险控制方式：不改变 `compute_result.v1` envelope，不把大时序写入主表 JSON；worker 遇到坏 artifact payload 会让 job failed，避免上传坏 artifact。
- 验证方式：`backend\.venv\Scripts\python -m pytest contracts\tests -q`（117 passed）；`backend\.venv\Scripts\python -m pytest services\simulation-worker\tests\test_worker_cli.py -q`（20 passed）；`cd apps\api; go test ./internal/platform/contracts ./internal/compute -run Contract -count=1`（passed）；`git diff --check -- contracts services\simulation-worker apps\api\internal\platform\contracts frontend\src\client\compute`（仅 LF/CRLF warning）。
- 是否更新 README：是，更新 `contracts/examples/valid/README.md` fixture 索引。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，沿用 `0018-standalone-result-artifact-and-udm-catalog-boundary.md`。

## 22:39 - PR-3 canonical UDM seed catalog

- 用户目标：建立单一 canonical UDM seed catalog，让 standalone Go API 与 legacy Python 模板服务不再各自维护生产模板事实。
- 涉及目录：`contracts/`、`backend/app/services/`、`apps/api/internal/compute/`、`apps/api/internal/platform/contracts/`、`tasks/`、`.ai/changes/`。
- 修改内容：新增 `udm_seed_catalog.v1` schema、minimal fixture、canonical `contracts/catalogs/udm_seed_catalog.v1.json` 与目录 README；从 legacy 模板提取 7 个模板并写入 schema/revision/content hash/seed_source；Python `udm_seed_templates.py` 改为读取 catalog；Go `udm_workspace.go` 改为读取同一 catalog，from-template 写入 `udm_seed_catalog.v1:<key>` seed source 和 template hash/revision metadata；删除生产路径 `simpleUDMTemplate()`；新增 Go/Python/contract parity tests。
- 修改原因：Go 极简模板与 legacy ASM1/ASM1Slim/ASM3/Petersen 模板漂移，导致 standalone template validate/create 行为无法达到 RC parity。
- 本次已消除的不确定性：确认 canonical catalog 精确包含 `asm1`、`asm1slim`、`asm3`、`petersen-chapter-1/2/3/7`；确认 ASM1 为 11/19/8、ASM1Slim 为 5/7/3、ASM3 为 13/22/7；确认 7 个模板均可通过 Go teaching validation 并 create-from-template。
- 本次采用的关键假设：catalog 初始内容以 legacy `backend/app/services/udm_seed_templates.py` 为 oracle；`template_content_hash` 基于模板定义字段复算，不包含自身 hash。
- 剩余不确定性：历史坏模板审计和未编辑模型追加修正版仍属于 PR-5/P1；backend DB 依赖的 API route 测试在当前本地环境因数据库 503 无法作为本次验证依据。
- 影响范围 / 风险控制方式：保持现有 HTTP route shape，不新增数据库字段或 OpenAPI 字段；只把已有 record metadata 写入模板 hash/revision，历史模型版本不原地改写。
- 验证方式：`backend\.venv\Scripts\python -m pytest contracts\tests -q`（122 passed）；`cd backend; .venv\Scripts\python -m pytest app\tests\udm_tutorial_validation_test.py -q`（11 passed）；`cd apps\api; go test ./internal/compute -run UDM -count=1`（passed）；`cd apps\api; go test ./...`（passed）；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-contracts.ps1`（passed）。另尝试 `cd backend; .venv\Scripts\python -m pytest app\tests\udm_tutorial_validation_test.py app\tests\api\routes\udm_models_validate_test.py -q`，其中 API route 测试因本地数据库登录 503 失败。
- 是否更新 README：是，新增 `contracts/catalogs/README.md` 并更新 `contracts/README.md`、`contracts/examples/valid/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，沿用 `0018-standalone-result-artifact-and-udm-catalog-boundary.md`。
