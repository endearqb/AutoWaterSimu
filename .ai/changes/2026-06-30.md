## 22:22 - UDM-v2 前端硬隔离 PF-1

- 用户目标：根据 UDM-v2 前端隔离附件计划开始按 phase 实施，并在 phase 完成后提交、推送分支。
- 涉及目录：`frontend/`、`frontend/src/features/udm-v2/`、`frontend/src/components/Flow/`、`scripts/`、`.ai/decisions/`、`.ai/changes/`、`tasks/`。
- 修改内容：新增 ADR-0028、UDM-v2 feature README、Vitest/jsdom/testing-library 工具链、Vitest config、测试 setup、UDM-v2 import boundary 脚本；更新 Flow README 将 legacy Flow 与 UDM-v2 独立 feature 边界写清；更新 frontend scripts 与 build test exclude。
- 修改原因：PF-1 要先固定前端硬隔离决策和最小测试/边界守卫，避免后续 v2 代码继续污染 legacy Flow 栈。
- 本次已消除的不确定性：确认当前前端未配置 Vitest；确认 plan 要求新分支 `codex/udm-v2-frontend-split`；确认 `frontend/src/components/Flow/README.md` 与新硬隔离文档存在冲突，已按新裁决改为 legacy Flow 边界。
- 本次采用的关键假设：PF-1 只建立工具链与边界，不创建 `/udm-v2` route；已有未提交 i18n/文档删除改动不纳入本 phase commit。
- 剩余不确定性：后续 PF0/PF1 需要基于实际 standalone flowchart wrapper 和 route-tree 生成方式决定最小接入点。
- 影响范围 / 风险控制方式：新增 dev dependencies 和边界脚本；不改运行时页面行为，不提交已有脏改动。
- 验证方式：`cd frontend; npm run test` 通过（Vitest 空基线限定 `src/**/*.test|spec`）；`cd frontend; npm run typecheck` 通过；`cd frontend; npm run check:udm-v2-boundary` 通过；`git diff --check` 通过（仅 LF/CRLF warning）。
- 是否更新 README：是，新增 `frontend/src/features/udm-v2/README.md`，更新 `frontend/src/components/Flow/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：是，新增 `.ai/decisions/0028-udm-v2-frontend-hard-isolation.md`。

## 22:38 - UDM-v2 前端硬隔离 PF0

- 用户目标：继续按附件计划完成 PF0，建立 `/udm-v2` 独立入口和可验证空骨架。
- 涉及目录：`frontend/src/features/udm-v2/`、`frontend/src/routes/`、`frontend/src/components/Common/`、`frontend/tests/`、`tasks/`、`.ai/changes/`。
- 修改内容：新增 `/udm-v2` route、UdmV2Page、NetworkV2FlowLayout、空 NetworkV2Canvas、toolbar、status bar、独立 Zustand store skeleton、route search parser、standalone flowchart adapter skeleton、Vitest store/adapter tests、Playwright route smoke；更新 standalone route tree、sidebar 入口、routeTree generated file 和相关 README。
- 修改原因：PF0 要先建立独立页面和持久化 payload 边界，后续 PF1/PFC-A 才能把 v2 edge 从 legacy Flow 中迁出。
- 本次已消除的不确定性：确认 TanStack Router Vite plugin 可生成 `routeTree.gen.ts`；确认 Vitest 不应扫描 Playwright tests；确认 Zustand v5 不能在 status bar 里直接使用返回新对象的 selector。
- 本次采用的关键假设：PF0 只提供空画布和 adapter skeleton，不实现节点、四类边 renderer 或实际保存/加载按钮；sidebar 文案暂用固定 `UDM Network v2`，避免混入已有未提交 i18n 改动。
- 剩余不确定性：PF1 需要接管 feature-local edge model；PF6 需要把 adapter skeleton 扩展成完整 save/load/list/delete/submit。
- 影响范围 / 风险控制方式：新增独立 route 和 feature 文件；不修改 legacy Flow runtime 行为；未提交已有 Edge i18n 脏改动。
- 验证方式：`cd frontend; npm run test` 通过；`cd frontend; npm run typecheck` 通过；`cd frontend; npm run check:udm-v2-boundary` 通过；`cd frontend; npx playwright test tests/udm-v2-route.smoke.spec.ts --project=chromium --no-deps --reporter=line` 在 standalone/no-auth 环境通过；`git diff --check` 通过（仅 LF/CRLF warning）。
- 是否更新 README：是，更新 `frontend/src/features/README.md` 与 `frontend/src/routes/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，沿用 ADR-0028。

## 22:51 - UDM-v2 前端硬隔离 PF1

- 用户目标：继续按附件计划完成 PF1，把 v2 edge model 和四类 renderer 收进独立 feature。
- 涉及目录：`frontend/src/features/udm-v2/edges/`、`frontend/src/features/udm-v2/state/`、`frontend/src/features/udm-v2/canvas/`、`frontend/src/types/`、`frontend/src/components/Flow/edges/`、`frontend/tests/`。
- 修改内容：新增 feature-local `edgeModel.ts`、四个 v2 renderer、edge shell/path helper、edge type registry、edge mode selector；store `onConnect` 生成 `hydraulic_v2` / `pump_v2` / `settling_v2` / `signal_v2` typed edge；canvas 接入 selector 和 `edgeTypes`；补 edge model/renderers/store tests；将 legacy `networkEdges.ts` 和 Flow edges README 标记为冻结清理债务。
- 修改原因：PF1 是 copy/freeze/clean 枢轴，必须先让 `/udm-v2` 自己拥有 typed edge model，后续 PFC-A 才能停止 v1 污染。
- 本次已消除的不确定性：确认 v2 feature 不再 import 全局 `src/types/networkEdges.ts`；确认 ReactFlow typed `applyEdgeChanges` 需要完整 `Edge<NetworkV2EdgeData>` 泛型；确认 route smoke 在首次 Vite 编译下需 15 秒断言窗口。
- 本次采用的关键假设：PF1 只做 edge 创建/渲染基础，不做 inspector 字段编辑和语义校验；legacy Flow 中已有 v2 helper 暂不删除，留给 PFC-A/PFC-B。
- 剩余不确定性：PFC-A 需要决定 legacy 新建边恢复到何种旧数据 shape；PF3 再补 field-level validation。
- 影响范围 / 风险控制方式：v2 代码全部在 feature 内；legacy 文件只加 deprecation/README 标记，不改现有 legacy runtime 行为。
- 验证方式：`cd frontend; npm run test` 通过；`cd frontend; npm run typecheck` 通过；`cd frontend; npm run check:udm-v2-boundary` 通过；`cd frontend; npx playwright test tests/udm-v2-route.smoke.spec.ts --project=chromium --no-deps --reporter=line` 在 standalone/no-auth 环境通过；`git diff --check` 通过（仅 LF/CRLF warning）。
- 是否更新 README：是，更新 `frontend/src/types/README.md` 与 `frontend/src/components/Flow/edges/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，沿用 ADR-0028。

## 23:03 - UDM-v2 前端硬隔离 PFC-A

- 用户目标：继续按附件计划完成 PFC-A，尽早停止 legacy v1 页面暴露 UDM-v2 edge 功能，并按 phase 提交推送。
- 涉及目录：`frontend/src/stores/`、`frontend/src/components/Flow/`、`frontend/tests/`、`tasks/`、`.ai/changes/`。
- 修改内容：移除 legacy `flowStore` 与 `createModelFlowStore` 的 `activeEdgeKind` / `setActiveEdgeKind` 和 `createNetworkEdgeData` 新建边路径；v1 新连线恢复为 legacy `flow` edge；`Canvas` / `FlowCanvas` 移除 v2 edge selector；`PropertyPanel` / `UDMPropertyPanel` 移除 `NetworkEdgeInspectorFields` 并恢复普通 flow 字段；`EditableEdge` 不再按 `edge_kind` 条件渲染；新增 v1 store 隔离 Vitest 和 v1 no-v2-selector Playwright 回归。
- 修改原因：PFC-A 要在 v2 edge model 已迁入独立 feature 后，停止旧 MaterialBalance/ASM/UDM 页面继续暴露 v2 中间态功能。
- 本次已消除的不确定性：确认 PFC-A 不删除 `networkEdges.ts`、`EdgeModeSelector.tsx`、`NetworkEdgeInspectorFields.tsx`，这些属于 PFC-B；确认 model store 会继续按 legacy 逻辑把固定模型参数同步到边上，因此测试只断言无 v2 edge 字段。
- 本次采用的关键假设：保留 legacy import/export 中对旧污染数据的宽容更新路径，PFC-A 只阻止新建和 UI 暴露；已有未提交 edge/i18n 本地改动不纳入本 phase commit。
- 剩余不确定性：PFC-B 仍需删除或冻结全局 `networkEdges.ts` 与未使用临时 helper；PF2/PF3 将继续在 `/udm-v2` feature 内实现节点和 inspector。
- 影响范围 / 风险控制方式：仅改 legacy Flow 相关止血路径和测试；不删除核心文件；不改后端、合同或 v2 feature runtime。
- 验证方式：`cd frontend; npm run test` 通过；`cd frontend; npm run typecheck` 通过；`cd frontend; npm run check:udm-v2-boundary` 通过；`cd frontend; npx playwright test tests/v1-no-udm-v2-regression.spec.ts --project=chromium --no-deps --reporter=line` 在 standalone/no-auth 环境通过；`git diff --check` 通过（仅 LF/CRLF warning）。
- 是否更新 README：是，更新 `frontend/src/stores/README.md` 与 `frontend/src/components/Flow/edges/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，沿用 ADR-0028。

## 23:12 - UDM-v2 前端硬隔离 PF2

- 用户目标：继续按附件计划完成 PF2，让 `/udm-v2` 具备独立节点与 palette。
- 涉及目录：`frontend/src/features/udm-v2/nodes/`、`frontend/src/features/udm-v2/palette/`、`frontend/src/features/udm-v2/canvas/`、`frontend/src/features/udm-v2/state/`、`frontend/tests/`、`tasks/`、`.ai/changes/`。
- 修改内容：新增 `NetworkV2NodeData`、五类节点默认数据、React Flow node type registry、五个 node renderer、共享 node shell、draggable node palette；store 增加 `addNode`；canvas 接入 `nodeTypes`、drag/drop 创建节点；补 node factory/store tests 和 `/udm-v2` palette drag/drop smoke。
- 修改原因：PF2 要在独立 v2 feature 内完成节点与 palette MVP，为后续 PF3 inspector/validation 和 PF4 SecondaryClarifier composite 提供本地 node data 基础。
- 本次已消除的不确定性：确认 PF2 只实现五类节点默认 data 与 handles/ports，不展开 SecondaryClarifier composite；确认 React Flow node changes 同 edge changes 一样需要显式 v2 node 泛型。
- 本次采用的关键假设：默认 node data 使用 `udm_network_minimal_components.v1` 和 `COD` / `X_TSS` 最小初始条件；UDM Reactor 默认 `reaction_enabled=false`，等 PF3/PF6 绑定模型后再启用。
- 剩余不确定性：PF3 需要补完整语义校验和 inspector 字段；PF4 需要把 SecondaryClarifier10Layer 配置展开为内部 layer graph。
- 影响范围 / 风险控制方式：新增代码全部在 `frontend/src/features/udm-v2/**`，不改 legacy Flow；node factory tests 覆盖 type/data/ports；route smoke 覆盖 palette 可拖入。
- 验证方式：`cd frontend; npm run test` 通过；`cd frontend; npm run typecheck` 通过；`cd frontend; npm run check:udm-v2-boundary` 通过；`cd frontend; npx playwright test tests/udm-v2-route.smoke.spec.ts --project=chromium --no-deps --reporter=line` 在 standalone/no-auth 环境通过；`git diff --check` 通过（仅 LF/CRLF warning）。
- 是否更新 README：是，更新 `frontend/src/features/udm-v2/README.md`，新增 `nodes/README.md` 与 `palette/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，沿用 ADR-0028。
