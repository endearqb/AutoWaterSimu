## 20:27 - Result explanation mutation data-scope

- 用户目标：根据 Certainty/Elegance PRD 与开发计划继续完成剩余工作，并按近期策略推进明确的 mutation data-scope / all-mutation audit 切片，避免继续做低收益 evidence wrapper 或同 package 整理。
- 涉及目录：`apps/api/`、`scripts/ci/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`、`.ai/changes/`。
- 修改内容：新增 `TestHTTPResultExplanationMutationTenantProjectSiteScope`，覆盖 result explanation submit/review/publish 的 HTTP tenant/project/site mutation data-scope；将该回归纳入 `scripts/ci/security-smoke.ps1`；更新 Compute API、security smoke、architecture current-state/local-dev、Certainty/Elegance plan 和任务记录中的安全覆盖描述。
- 修改原因：result explanation mutation route 已经通过 job route data-scope 授权，但 security smoke 与长期上下文只记录 audit coverage，缺少 focused regression 证明 scoped token 无法跨 tenant/project/site 提交、review 或 publish explanation。
- 本次已消除的不确定性：确认 production route 在进入 result explanation mutation 前会按 stored job scope 拒绝跨 scope token；确认跨 scope submit 不落库，跨 scope review/publish 不修改状态也不写 review/publish audit event；确认 same-scope submit/review/publish 保持成功。
- 本次采用的关键假设：本阶段只补 result explanation submit/review/publish mutation data-scope 证据，不修改生产代码、schema、OpenAPI、generated client、migration 或 audit envelope；不声明 full object-level data-scope、remaining mutation data-scope 全部完成、complete all-mutation audit、OIDC/JWKS、RBAC/ABAC、hosted green evidence、release artifact hosted round trip、legacy authenticated session 或 complete golden scenarios 完成。
- 剩余不确定性：full object-level data-scope、其他 remaining mutation data-scope、remaining all-mutation audit、OIDC/JWKS、RBAC/ABAC、hosted green evidence、release artifact hosted round trip、legacy authenticated session 和 complete golden scenarios 仍未完成。
- 影响范围 / 风险控制方式：只新增 HTTP 回归测试、security-smoke test selector 和长期文档/任务记录；保持 result explanation schema validation、evidence ref resolution、HTTP response shape、review/publish semantics、audit envelope shape、migrations、OpenAPI 与 generated clients 不变；未触碰用户未跟踪文档。
- 验证方式：`go test ./internal/compute -run "TestHTTPResultExplanation(MutationTenantProjectSiteScope|AuditEvents)$" -count=1`；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\security-smoke.ps1`；`go test ./...`（`apps/api`）；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1`；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1`；`rg -n "schema_version|P0|P1|P2" docs\rebuild --glob "AutoWaterSimu_Next_*.md"`；`git diff --check -- apps/api docs scripts/ci tasks .ai`，均通过；diff-check 仅报告既有 LF/CRLF 工作区提示。
- 是否更新 README：是，更新 `apps/api/README.md`、`apps/api/internal/compute/README.md`、`scripts/ci/README.md`、`docs/architecture/current-state.md`、`docs/architecture/local-dev.md` 和 Certainty/Elegance Development Plan 中的 result explanation mutation data-scope / security smoke 上下文。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次是既有 static-token job-scoped data-scope 策略在 result explanation mutation 上的测试证据补齐，不新增长期架构决策。

## 20:03 - Service test second split

- 用户目标：先拆 `service_test.go`，解决当前最大代码维护性问题。
- 涉及目录：`apps/api/internal/compute/`、`tasks/`、`.ai/changes/`。
- 修改内容：确认当前仓库已不存在 `apps/api/internal/compute/service_test.go` 后，继续拆分残留偏大的 `service_*_test.go` 文件；新增 `service_job_audit_test.go`、`service_job_scope_test.go`、`service_benchmark_cases_scope_test.go`、`service_artifact_retention_test.go`、`service_worker_audit_test.go`、`service_worker_scope_test.go`、`service_model_catalog_scope_test.go`、`service_model_catalog_promotion_scope_test.go`、`service_result_explanations_test.go`、`service_evidence_references_test.go` 和 `service_simulation_registry_test.go`；将原文件缩小为各自核心主题；更新 compute README 测试文件导航与任务记录。
- 修改原因：前序阶段已删除单体 `service_test.go`，但 jobs/artifacts/workers/model catalog/evidence/simulation 等残留 service 测试文件仍混合 audit、scope、registry、retention、reference 等多类关注点；按完整 `Test...` 函数块继续细分能降低维护、review 和定位成本。
- 本次已消除的不确定性：确认没有现存 `service_test.go` 可拆；确认拆分可按完整 top-level test function 边界完成；确认同 package helper 可继续共享，不需要移动 helper 或改测试名。
- 本次采用的关键假设：用户的“拆 service_test.go”在当前仓库状态下等价于继续拆 service 测试聚合文件；本阶段只做测试文件组织维护，不修改断言、生产代码、HTTP 行为、schema、OpenAPI、generated client、migration 或 security smoke 入口；不声明 package migration、安全缺口或 golden scenario 完成。
- 剩余不确定性：完整 jobs/artifacts/models/evidence/simulation package movement、remaining mutation data-scope、full object-level data-scope、remaining all-mutation audit、OIDC/JWKS、RBAC/ABAC、hosted green evidence、release artifact hosted round trip、legacy authenticated session 和 complete golden scenarios 仍未完成。
- 影响范围 / 风险控制方式：机械移动完整测试函数块并运行 `gofmt`；保留所有测试函数名和断言；只更新测试导航 README 与任务/变更记录；未触碰用户未跟踪文档。
- 验证方式：`go test ./internal/compute`（`apps/api`）；`go test ./...`（`apps/api`）；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1`；`git diff --check -- apps/api/internal/compute tasks .ai`，均通过；diff-check 仅报告既有 LF/CRLF 工作区提示。
- 是否更新 README：是，更新 `apps/api/internal/compute/README.md` 的 `service_*_test.go` 导航。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次是测试组织维护，不产生长期架构决策。

## 19:37 - Artifact retention sweep data-scope

- 用户目标：根据 Certainty/Elegance PRD 与开发计划继续完成剩余工作，并按近期策略推进明确的 mutation data-scope / object data-scope 切片，避免继续做低收益 evidence wrapper 或同 package 整理。
- 涉及目录：`apps/api/`、`scripts/ci/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`、`.ai/changes/`。
- 修改内容：为 `ArtifactRetentionSweepOptions` 增加内部 `DataScope`；HTTP retention sweep 将静态 token principal 的 tenant/project/site 传入候选筛选；MemoryStore/PostgresStore retention candidate 查询按 artifact 所属 job 的 tenant/project/site 过滤后再 report/delete/archive；新增 `TestHTTPArtifactRetentionSweepTenantProjectSiteScope` 并纳入 `scripts/ci/security-smoke.ps1`；更新 Compute API、security smoke、architecture current-state、Certainty/Elegance plan 和任务记录。
- 修改原因：`POST /api/v1/admin/artifacts/retention-sweep` 已有 `artifact:admin` scope 和 delete/archive audit envelope，但 scoped admin token 仍会枚举全局 artifact candidates 并可能处理跨 tenant/project/site artifact；这是明确的 remaining mutation data-scope 缺口。
- 本次已消除的不确定性：确认 retention sweep public request/response shape 不需要变化；确认 scheduler/service path 可继续使用空 `DataScope` 保持全局维护任务行为；确认 candidate filtering 应在 Memory/Postgres store 查询层完成，避免 scoped report 泄漏跨 scope artifact count/id，也避免 scoped `limit` 被跨 scope 候选占用；确认无需 migration、OpenAPI 或 generated client 变更。
- 本次采用的关键假设：本阶段只覆盖 artifact retention sweep candidate/report/delete/archive 的 tenant/project/site data-scope，不声明 full object-level data-scope、remaining mutation data-scope 全部完成、complete all-mutation audit、OIDC/JWKS、RBAC/ABAC、hosted green evidence、release artifact hosted round trip、legacy authenticated session 或 complete golden scenarios 完成；global/admin token 与 scheduler 行为保持不变。
- 剩余不确定性：full object-level data-scope、其他 remaining mutation data-scope、remaining all-mutation audit、OIDC/JWKS、RBAC/ABAC、hosted green evidence、release artifact hosted round trip、legacy authenticated session 和 complete golden scenarios 仍未完成。
- 影响范围 / 风险控制方式：只修改 retention sweep 内部 options、store candidate filtering、HTTP adapter、focused test、security smoke 和长期文档；不改变 endpoint path、request/response JSON、dry-run default、archive backend behavior、audit envelope shape、database schema、OpenAPI、generated client 或 scheduler/global-token 行为；未触碰用户未跟踪文档。
- 验证方式：`go test ./internal/compute -run "Test(HTTPArtifactRetentionSweepRequiresAdminScope|HTTPArtifactRetentionSweepTenantProjectSiteScope|ArtifactRetentionSweepDeletesOnlyUnreferencedExpiredTTL|ArtifactRetentionSweepArchivesCandidateWithConfiguredBackend|ArtifactRetentionSchedulerDeletesExpiredTTLWhenEnabled)$" -count=1`；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\security-smoke.ps1`；`go test ./...`（`apps/api`）；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1`；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1`；`rg -n "schema_version|P0|P1|P2" docs\rebuild --glob "AutoWaterSimu_Next_*.md"`；`git diff --check -- apps/api docs scripts/ci tasks .ai`，均通过；diff-check 仅报告既有 LF/CRLF 工作区提示。
- 是否更新 README：是，更新 `apps/api/README.md`、`apps/api/internal/compute/README.md`、`scripts/ci/README.md`、`docs/architecture/current-state.md` 和 Certainty/Elegance Development Plan 中的 artifact retention sweep data-scope / security smoke 上下文。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次是既有 static-token tenant/project/site data-scope 策略在 artifact retention sweep 上的覆盖扩展，不新增长期架构决策。

## 19:21 - Model governance service test split

- 用户目标：先拆 `service_test.go`，解决当前最大代码维护性问题；在确认单体 `service_test.go` 已于前序阶段删除后，继续细分剩余偏大的 `service_model_governance_test.go`。
- 涉及目录：`apps/api/internal/compute/`、`tasks/`、`.ai/changes/`。
- 修改内容：删除 `service_model_governance_test.go`，按测试职责拆分为 `service_model_runs_test.go`、`service_benchmark_runs_test.go`、`service_model_catalog_test.go`、`service_model_parameters_test.go` 和 `service_benchmark_cases_test.go`；更新 compute README 的测试文件导航；更新任务记录。
- 修改原因：前序 `service_test.go` 拆分已经消除了 230KB+ 单体测试文件，但 `service_model_governance_test.go` 仍聚合 model run、benchmark run、model catalog、parameter promotion 和 benchmark case scheduling 多类测试，继续细分能降低后续定位和 review 成本。
- 本次已消除的不确定性：确认当前仓库已不存在 `apps/api/internal/compute/service_test.go`；确认 `service_model_governance_test.go` 是 service split 后的主要剩余测试聚合点；确认所有移动均可按完整 `Test...` 函数边界完成并保留同 package helper 共享。
- 本次采用的关键假设：本阶段只做测试文件组织拆分，不修改测试断言、测试名、生产代码、HTTP 行为、OpenAPI、migration、generated client 或 security smoke 入口；不声明 package migration、remaining mutation data-scope、all-mutation audit、hosted evidence 或 golden scenarios 完成。
- 剩余不确定性：完整 jobs/artifacts/models/evidence/simulation/agent package movement、remaining mutation data-scope、full object-level data-scope、remaining all-mutation audit、hosted green evidence、release artifact hosted round trip、legacy authenticated session 和 complete golden scenarios 仍未完成。
- 影响范围 / 风险控制方式：只移动测试函数和更新维护导航；保留原测试函数名与断言；未触碰用户未跟踪文档。
- 验证方式：`go test ./internal/compute -run "Test(ValidatedCompletePersistsModelRun|HTTPModelRunTenantProjectSiteScope|HTTPBenchmarkRunTenantProjectSiteScope|HTTPBenchmarkRunMutationTenantProjectSiteScope|ModelCatalogEndpoint|HTTPModelCatalogTenantProjectSiteScope|HTTPModelCatalogMutationTenantProjectSiteScope|HTTPModelCatalogPromotionTenantProjectSiteScope|DefaultParameterSetPromotionPlanEndpoint|BenchmarkCaseScheduleRunEndpoint)$" -count=1`；`go test ./...`（`apps/api`）；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1`；`git diff --check -- apps/api tasks .ai` 均通过；diff-check 仅报告既有 LF/CRLF 工作区提示。
- 是否更新 README：是，更新 `apps/api/internal/compute/README.md` 的测试文件导航。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次是测试组织维护，不产生长期架构决策。
