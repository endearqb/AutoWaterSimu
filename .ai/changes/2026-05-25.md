## 23:19 - README First and AutoWaterSimu Next branch initialization

- 用户目标：在当前仓库新建 `codex/autowatersimu-next-rebuild` 分支，接入附件 README First / AGENTS，并创建 AutoWaterSimu Next 推荐结构。
- 涉及目录：仓库根目录、`.ai/`、`apps/`、`contracts/`、`docs/`、`frontend/`、`backend/`、`services/`、`simulation_core/`、`tasks/`。
- 修改内容：复制 `README_First.md`，将附件 `AGENTS.md` 作为主协议并追加 AutoWaterSimu 专属规则；新增 Next 目录骨架和目录 README；更新根 README 与关键目录 README；新增 README First 记录目录。
- 修改原因：让后续 Agent 在动手前能按稳定路径读取项目上下文，并让 Next 重构在同仓 monorepo 中与 legacy baseline 并行推进。
- 本次已消除的不确定性：确认当前仓库已有 legacy `frontend/` / `backend/`；确认附件要求已有 AGENTS 必须合并补充；确认 `codex/autowatersimu-next-rebuild` 分支原先不存在。
- 本次采用的关键假设：当前未提交的 `docs/rebuild/` 与 `tasks/todo.md` 改动应随工作区进入新分支；附件 AGENTS 是主协议，但 AutoWaterSimu 技术约束必须保留。
- 剩余不确定性：后续具体 schema、Go module、Tauri app 和 worker CLI 仍需按 PRD/Spec/Plan 分阶段实现。
- 影响范围 / 风险控制方式：本次只创建分支、文档、目录和上下文链路，不修改业务代码或运行时行为。
- 验证方式：执行 `git status --short --branch`、结构存在性检查、README First 关键词搜索和 `git diff --check`。
- 是否更新 README：是，更新根 README、backend/frontend README，并新增关键目录 README。
- 是否需要新增或更新架构决策（.ai/decisions/）：是，新增 `0001-readme-first-and-next-monorepo.md`。

## 23:38 - AutoWaterSimu Next Phase 0+1 legacy stabilization and contracts

- 用户目标：按已写 PRD/Spec/Development Plan 启动第一阶段，先修 legacy P0 问题，再建立合同与 schema test 基础。
- 涉及目录：`backend/app/api/routes/`、`backend/app/tests/api/routes/`、`backend/pyproject.toml`、`backend/uv.lock`、`contracts/`、`tasks/`。
- 修改内容：修复 ASM1/UDM validate 响应字段；清理 ASM1/ASM1Slim/ASM3 flowchart route 的 `print(...)`；给 material balance / ASM1 / UDM 现有计算入口补 legacy baseline docstring；新增 P0 与预留 JSON Schema、valid/invalid examples、contract tests；新增后端回归测试；加入 `jsonschema>=4,<5` dev dependency。
- 修改原因：让旧接口在重构前先稳定运行，并让后续 Go API、Desktop、Worker、frontend 类型生成有可测试的合同源头。
- 本次已消除的不确定性：确认 ASM1/UDM validate 字段已与 `MaterialBalanceValidationResponse` 对齐；确认 flowchart route 不再输出完整流程图数据；确认合同 schema 文件名与 `schema_version.const` 可自动校验。
- 本次采用的关键假设：本轮只新增 repo-level contracts，不改变 FastAPI OpenAPI，所以不重新生成 legacy frontend client；route 字段测试不应依赖数据库登录，因此直接调用 route function。
- 剩余不确定性：真实 worker/Go API/Tauri app 尚未启动；artifact 化、job lifecycle、late-result 防护仍在后续 Phase 实现。
- 影响范围 / 风险控制方式：业务 URL、请求体、响应模型保持不变；新增测试覆盖响应字段、no-print 静态检查、schema 合法性和 valid/invalid examples。
- 验证方式：
  - `cd backend; .venv\Scripts\python -m pytest app/tests/api/routes/test_asm_udm_validate_response.py app/tests/api/routes/test_flowchart_routes_no_print.py -q` 通过，`5 passed`。
  - `backend\.venv\Scripts\python -m pytest contracts\tests -q` 通过，`35 passed`。
  - `cd backend; .venv\Scripts\python -m pytest app/tests/time_segment_validation_test.py app/tests/material_balance_segment_overrides_test.py app/tests/hybrid_udm_validation_test.py app/tests/udm_engine_variable_binding_test.py -q` 通过，`17 passed`。
  - `cd frontend; npx tsc --noEmit` 通过。
  - `git diff --check` 通过；Git 仅报告 touched text files 的 LF-to-CRLF normalization warning。
- 是否更新 README：是，更新 `contracts/README.md` 的实际 contract test 命令。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮按既有 README First / same-repo monorepo 决策推进。
