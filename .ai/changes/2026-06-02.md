## 20:56 - Benchmark case run job document split

- 用户目标：根据 Certainty/Elegance PRD 与 Development Plan 继续完善 AutoWaterSimu Next，完成一个低风险 Phase 1 package movement 切片并阶段性 commit/push。
- 涉及目录：`apps/api/internal/domain/models/`、`apps/api/internal/compute/`、`apps/api/`、`scripts/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`、`.ai/changes/`。
- 修改内容：在 `apps/api/internal/domain/models` 中新增 `BuildBenchmarkCaseRunJobDocument`、`BenchmarkCaseRunJobDocumentInput`、`BenchmarkCaseRunJobDocument` 和 direct domain tests，承接 benchmark case schedule-run `compute_job.v1` 文档 shape 与 idempotency key 生成；`ModelGovernanceService.ScheduleBenchmarkCaseRun` 保留 catalog lookup、gate checks、execution profile lookup、simulation input resolution、job_type mismatch validation、JSON marshaling 与 createJob，只把纯 job document assembly 委派给 domain helper；同步更新 API/domain/models/compute READMEs、architecture/current-state、compute-api notes、Certainty/Elegance checklist、boundary audit note 和本阶段 TODO。
- 修改原因：benchmark case schedule-run job document assembly 是稳定 models 领域规则，继续留在 `model_governance.go` 会让 model governance compatibility service 同时承担 workflow 编排和纯 `compute_job.v1` 文档形状。
- 本次已消除的不确定性：确认该 helper 只依赖已校验的 model/version/case identity、job type、source/requested_by、metadata、resolved simulation input、execution profile、default parameter set fields、expected metrics/tolerance/input_ref 和 created_at/timestamp strings，不依赖 compute DTO、store、ContractValidator、HTTP、OpenAPI、contracts、PostgreSQL、generated client、auth scopes、simulation input resolver 或 job creator；确认 endpoint 回归测试继续覆盖 schedule-run 排队、payload、required capabilities、metadata、idempotency、权限和不记录 benchmark_run history。
- 本次采用的关键假设：本次只移动纯 benchmark case schedule-run `compute_job.v1` document construction，不移动 model catalog read/write、benchmark case gate/error mapping、execution profile selection、simulation input resolution、job_type mismatch validation、createJob/idempotency persistence、benchmark run workflow、OpenAPI、contracts、migrations、generated clients、auth scopes 或 database schema。
- 剩余不确定性：完整 model governance package movement 尚未完成；catalog snapshot mutation、typed catalog DTOs、schema validation、benchmark query orchestration、benchmark run persistence workflow、promotion workflow orchestration、HTTP response mapping 和 store writes 仍在 compute compatibility wiring；完整 jobs/artifacts/evidence/simulation/agent package movement 仍是后续工作。
- 影响范围 / 风险控制方式：最小 helper 迁移；`domain/models` 只返回通用 `map[string]any` job document 与 idempotency key，不 import compute；compute 继续负责 validation、resolution、marshal、createJob、store 和 HTTP；无公开 API/schema/database/client 变更；新增 direct domain tests 并保留 focused/full Go/audit/pr-fast validation。
- 验证方式：`cd apps\api; go test ./internal/domain/models ./internal/compute -run "Test(BuildBenchmarkCaseRunJobDocument|BenchmarkCaseScheduleRun|DefaultParameterSetPromotionPlan)" -count=1` passed；`cd apps\api; go test ./...` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1` passed（Store interface methods: 41，Store embedded interfaces: 12，service constructors audited: 10，internal Go package dirs: 13）；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` passed，evidence status `passed`；`rg -n "schema_version|P0|P1|P2" docs\rebuild --glob "AutoWaterSimu_Next_*.md"` completed and returned expected rebuild document references；`git diff --check -- apps\api docs scripts tasks .ai` passed with LF/CRLF warnings only。
- 是否更新 README：是，更新 `apps/api/internal/domain/models/README.md`、`apps/api/README.md`、`apps/api/internal/README.md`、`apps/api/internal/domain/README.md`、`apps/api/internal/compute/README.md`、`docs/architecture/current-state.md`、`docs/architecture/compute-api.md` 和 `scripts/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次仍是既有 modular monolith / models domain package movement 路线下的低风险 benchmark case run job document helper 迁移。

## 19:33 - Jobs failed-worker fallback result split

- 用户目标：根据 Certainty/Elegance PRD 与 Development Plan 继续完善 AutoWaterSimu Next，完成一个低风险 Phase 1 package movement 切片并阶段性 commit/push。
- 涉及目录：`apps/api/internal/domain/jobs/`、`apps/api/internal/compute/`、`apps/api/`、`scripts/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`、`.ai/changes/`。
- 修改内容：在 `apps/api/internal/domain/jobs` 中新增 `FailedWorkerComputeResult`，承接 worker-reported failure fallback `compute_result.v1` 文档构造规则；新增 direct domain tests；`JobLifecycleService.Fail` 改为通过 jobs domain helper 构造 fallback result，再沿用现有 `Complete` 路径；同步更新 API/domain/jobs/compute READMEs、architecture/current-state、compute-api notes、Certainty/Elegance checklist 和 boundary audit note。
- 修改原因：failed-worker fallback result construction 是稳定 jobs 领域规则，继续留在 job lifecycle workflow 内会让 worker failure completion 的一部分合同形状停留在 compute compatibility package。
- 本次已消除的不确定性：确认该 helper 只依赖 job id、job type、error code/message 和 Go standard library，不依赖 job store、ContractValidator、HTTP、OpenAPI、contracts、PostgreSQL、generated client、artifact listing callback 或 model_run persistence；确认 stale worker detection、schema validation、result hashing、stored summary projection、worker result completion parsing、model_run extraction/persistence 和 store writes 仍由 compute 保留。
- 本次采用的关键假设：本次只移动纯 fallback `compute_result.v1` document construction，不移动 job lifecycle service、worker stale checks、compute_result schema validation、result hash、stored summary projection、model_run extraction/persistence、MemoryStore/PostgresStore writes、HTTP routes、OpenAPI、contracts、migrations、generated clients、auth scopes 或 audit boundaries。
- 剩余不确定性：完整 job lifecycle package movement 尚未完成；job records、events、completion persistence、timeout sweep、artifact listing callback、schema validation 和 HTTP mapping 仍在 compute compatibility wiring；完整 artifacts/models/evidence/simulation/agent package movement 仍是后续工作。
- 影响范围 / 风险控制方式：最小 helper 迁移；`domain/jobs` 只依赖 Go standard library；compute 继续负责 validation、hash、persistence、model_run handling、DTO mapping 与 HTTP behavior；无公开 API/schema/database/client 变更；新增 direct domain tests 并保留 focused/full Go/audit/pr-fast validation。
- 验证方式：`cd apps\api; go test ./internal/domain/jobs ./internal/compute -run "Test(FailedWorkerComputeResult|WorkerResultCompletion|Fail|Complete|TimeoutSweep|WorkerStale|ValidatedComplete)" -count=1` passed；`cd apps\api; go test ./...` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1` passed（service constructors audited: 10，internal Go package dirs: 13）；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` passed，evidence status `passed`，但因本次文件和无关未跟踪 rebuild plan 文件存在，evidence 记录 dirty worktree；`git diff --check -- apps\api docs scripts tasks .ai` passed with LF/CRLF warnings only；`rg -n "schema_version|P0|P1|P2" docs\rebuild --glob "AutoWaterSimu_Next_*.md"` completed and returned expected rebuild document references。
- 是否更新 README：是，更新 `apps/api/internal/domain/jobs/README.md`、`apps/api/README.md`、`apps/api/internal/README.md`、`apps/api/internal/domain/README.md`、`apps/api/internal/compute/README.md` 和 architecture/current-state/compute-api 文档。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次仍是既有 modular monolith / jobs domain package movement 路线下的低风险 fallback result helper 迁移。

## 20:10 - Simulation check job document split

- 用户目标：根据 Certainty/Elegance PRD 与 Development Plan 继续完善 AutoWaterSimu Next，完成一个低风险 Phase 1 package movement 切片并阶段性 commit/push。
- 涉及目录：`apps/api/internal/domain/simulation/`、`apps/api/internal/compute/`、`apps/api/`、`scripts/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`、`.ai/changes/`。
- 修改内容：在 `apps/api/internal/domain/simulation` 中新增 `BuildSimulationCheckJobDocument`，承接已解析 `simulation_request.v1` 到 `compute_job.v1` simulation check job document 的纯组装规则；新增 direct domain tests；`Service.CreateSimulationCheck` 改为先保留原有 decode/schema validation/required field/input_ref resolution/job_type mismatch validation，再调用 domain helper 生成 job map 与 idempotency key；同步更新 API/domain/simulation/compute READMEs、architecture/current-state、compute-api notes、Certainty/Elegance checklist 和 boundary audit note。
- 修改原因：simulation check job document assembly 是稳定 simulation 领域规则，继续留在 compute compatibility service 内会让 `compute_job.v1` shell、context、metadata、execution profile 和 idempotency fallback 规则继续扩大 `service.go`。
- 本次已消除的不确定性：确认该 helper 只依赖已解析 request identity、source/requested_by、metadata/external_refs、resolved simulation input、input_ref 和 created_at 字符串，不依赖 store、ContractValidator、HTTP、OpenAPI、contracts、PostgreSQL、generated client、auth scopes 或 worker execution；确认 schema validation、`input_ref` resolution、model_run replay/process_graph lookup、job_type mismatch rejection、JSON marshaling、idempotent `CreateJob` 和 store writes 仍由 compute 保留。
- 本次采用的关键假设：本次只移动纯 `compute_job.v1` document construction，不移动 simulation-check workflow、request schema validation、required field validation、simulation input/process graph/model-run replay resolution、job persistence/idempotency execution、HTTP routes、OpenAPI、contracts、migrations、generated clients、auth scopes 或 audit boundaries。
- 剩余不确定性：完整 simulation workflow package movement 尚未完成；simulation request schema validation、input_ref resolution、simulation input/process graph metadata records、job persistence、store implementation、evidence-ref lookup 和 HTTP mapping 仍在 compute compatibility wiring；完整 jobs/artifacts/models/evidence/agent package movement 仍是后续工作。
- 影响范围 / 风险控制方式：最小 helper 迁移；`domain/simulation` 只依赖 Go standard library；compute 继续负责 validation、resolution、persistence、DTO mapping 与 HTTP behavior；无公开 API/schema/database/client 变更；新增 direct domain tests 并保留 focused/full Go/audit/pr-fast validation。
- 验证方式：`cd apps\api; go test ./internal/domain/simulation ./internal/compute -run "Test(BuildSimulationCheckJobDocument|SimulationCheck|ContractValidationEndpoint|NewSystemEvidenceReferenceE2E)" -count=1` passed；`cd apps\api; go test ./...` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1` passed（service constructors audited: 10，internal Go package dirs: 13）；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` passed，evidence status `passed`，但因本次文件和无关未跟踪 rebuild plan 文件存在，evidence 记录 dirty worktree；`git diff --check -- apps\api docs scripts tasks .ai` passed with LF/CRLF warnings only；`rg -n "schema_version|P0|P1|P2" docs\rebuild --glob "AutoWaterSimu_Next_*.md"` completed and returned expected rebuild document references。
- 是否更新 README：是，更新 `apps/api/internal/domain/simulation/README.md`、`apps/api/README.md`、`apps/api/internal/README.md`、`apps/api/internal/domain/README.md`、`apps/api/internal/compute/README.md` 和 architecture/current-state/compute-api 文档。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次仍是既有 modular monolith / simulation domain package movement 路线下的低风险 job document helper 迁移。

## 20:40 - Built-in model catalog document split

- 用户目标：根据 Certainty/Elegance PRD 与 Development Plan 继续完善 AutoWaterSimu Next，完成一个低风险 Phase 1 package movement 切片并阶段性 commit/push。
- 涉及目录：`apps/api/internal/domain/models/`、`apps/api/internal/compute/`、`apps/api/`、`scripts/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`、`.ai/changes/`。
- 修改内容：在 `apps/api/internal/domain/models` 中新增 `BuiltInModelCatalogDocument` 和 direct domain tests，承接 built-in `model_catalog.v1` fallback 的纯文档 shape；`compute.builtInModelCatalog` 改为生成 material-balance default parameter hash 后调用 domain helper，并通过原有 `ModelCatalogResponse` DTO 适配返回；同步更新 API/domain/models/compute READMEs、architecture/current-state、compute-api notes、Certainty/Elegance checklist、boundary audit note 和本阶段 TODO。
- 修改原因：built-in catalog document shape 是稳定 models 领域规则，继续留在 `service.go` 会让 compute compatibility package 继续承担纯 catalog fallback 文档组装，且不利于后续完整 model governance package movement。
- 本次已消除的不确定性：确认 built-in catalog helper 只依赖 generated_at、material-balance parameter hash 和 Go standard library，不依赖 compute DTO、store、ContractValidator、HTTP、OpenAPI、contracts、PostgreSQL、generated client、auth scopes 或 catalog snapshot mutation；确认 material-balance default parameter hash、typed DTO conversion、schema validation、persisted catalog preference、store writes、benchmark orchestration 和 HTTP mapping 仍由 compute 保留。
- 本次采用的关键假设：本次只移动 built-in `model_catalog.v1` fallback document shape，不移动 model catalog registration/read/list API、default parameter set status/promotion workflow、benchmark run workflow、model_run schema validation/persistence、OpenAPI、contracts、migrations、generated clients、auth scopes 或 database schema。
- 剩余不确定性：完整 model governance package movement 尚未完成；catalog snapshot mutation、benchmark query orchestration、benchmark run persistence workflow、promotion workflow orchestration、typed DTO/HTTP response mapping、schema validation 和 store writes 仍在 compute compatibility wiring；完整 jobs/artifacts/evidence/simulation/agent package movement 仍是后续工作。
- 影响范围 / 风险控制方式：最小 helper 迁移；`domain/models` 只返回通用 `map[string]any` 文档，不 import compute；compute 继续负责 hash、DTO conversion、validation、fallback read behavior、store 和 HTTP；无公开 API/schema/database/client 变更；新增 direct domain tests 并保留 focused/full Go/audit/pr-fast validation。
- 验证方式：`cd apps\api; go test ./internal/domain/models ./internal/compute -run "Test(BuiltInModelCatalogDocument|ModelCatalog|DefaultParameterSetPromotionPlan|BenchmarkCaseRun|ProductionReadiness)" -count=1` passed；`cd apps\api; go test ./...` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1` passed（Store interface methods: 41，Store embedded interfaces: 12，service constructors audited: 10，internal Go package dirs: 13）；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` passed，evidence 写入 `tmp\ci-evidence\pr-fast.json`；`rg -n "schema_version|P0|P1|P2" docs\rebuild --glob "AutoWaterSimu_Next_*.md"` completed and returned expected rebuild document references；`git diff --check -- apps\api docs scripts tasks .ai` passed with LF/CRLF warnings only。
- 是否更新 README：是，更新 `apps/api/internal/domain/models/README.md`、`apps/api/README.md`、`apps/api/internal/README.md`、`apps/api/internal/domain/README.md`、`apps/api/internal/compute/README.md`、`docs/architecture/current-state.md`、`docs/architecture/compute-api.md` 和 `scripts/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次仍是既有 modular monolith / models domain package movement 路线下的低风险 built-in catalog document helper 迁移。

## 22:07 - Compute MemoryStore domain file split

- 用户目标：执行 Go API Domain Package Split 文件级完成计划，先完成低风险 Stage 1：拆分 `store.go` 中的 Store interfaces 与 MemoryStore domain implementation，保持同 package 和零行为变化，并在阶段验证通过后 commit/push。
- 涉及目录：`apps/api/internal/compute/`、`apps/api/`、`scripts/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`、`.ai/changes/`。
- 修改内容：删除 monolithic `store.go`；新增 `store_interfaces.go` 承接 aggregate `Store`、12 个 domain store interfaces、`ListFilter`、`ModelRunFilter`；新增 `memory_store.go` 与 `memory_jobs.go` / `memory_artifacts.go` / `memory_models.go` / `memory_simulation.go` / `memory_agent.go` / `memory_workers.go` / `memory_metrics.go`，按 domain 拆分同一 `MemoryStore` 的原有方法；更新 `scripts/audit-compute-api-boundary.ps1`，从 `store_interfaces.go` 读取 Store 形状，从所有 `memory_*.go` 扫描 MemoryStore implementation coverage，并把 store implementation files 排除出 service-call attribution；同步更新 compute README、architecture/current-state、compute-api architecture notes、scripts README、Certainty/Elegance checklist 和本阶段 TODO。
- 修改原因：降低 `store.go` 巨型文件维护成本，为后续 HTTP route group split 和 PostgreSQL persistence split 提供更清晰的 domain file organization，同时避免在最高风险 PostgreSQL SQL/事务/扫描逻辑前改变行为。
- 本次已消除的不确定性：确认 Stage 1 可保持 `apps/api/internal/compute` 同 package；确认 Store 仍解析为 41 methods / 12 embedded interfaces；确认 MemoryStore 与 PostgresStore 仍覆盖全部 Store methods；确认 endpoint paths、request/response shape、OpenAPI、contracts、migrations、generated client、auth scopes、SQL、PostgreSQL implementation 和 `service.go` signatures 未变更。
- 本次采用的关键假设：本次仅做文件组织迁移；MemoryStore 字段、锁粒度、clone/cursor/filter/idempotency/retention/claim/completion 逻辑不重写；PostgreSQL 和 HTTP handler split 留到 Stage 2/3；无关未跟踪 `docs/rebuild/AutoWaterSimu_95分优雅度完整计划.md` 继续忽略。
- 剩余不确定性：Stage 2 `http.go` route group split 尚未执行；Stage 3 `postgres.go` domain persistence split 尚未执行，SQL/事务/扫描/migration smoke 风险仍在后续阶段；完整 Go API domain package split 仍未完成。
- 影响范围 / 风险控制方式：同 package mechanical top-level declaration movement；不修改方法签名、导出 API、HTTP routes、Store method set、database schema、migration、OpenAPI/client 或 contracts；先运行 full Go tests 和 architecture/dependency/pr-fast gates 再提交。
- 验证方式：`cd apps\api; go test ./...` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1` passed（Store interface methods: 41，Store embedded interfaces: 12，Domain groups: 12，Service constructors audited: 10，Internal Go package dirs: 13）；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` passed，evidence 写入 `tmp\ci-evidence\pr-fast.json`；`git diff --check -- apps\api docs scripts tasks .ai` passed with LF/CRLF warnings only。
- 是否更新 README：是，更新 `apps/api/internal/compute/README.md`、`scripts/README.md`、`docs/architecture/current-state.md`、`docs/architecture/compute-api.md`，并更新 Certainty/Elegance Development Plan checklist 与 `tasks/todo.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次是既有 modular monolith / domain package split 路线下的同包文件组织切片，不新增长期架构决策。

## 22:36 - Compute HTTP route group file split

- 用户目标：执行 Go API Domain Package Split 文件级完成计划，完成 Stage 2：按 route group 拆分 `http.go`，保持 route registration、HTTP 行为、OpenAPI、contracts、migration 和 generated client 不变，并在阶段验证通过后 commit/push。
- 涉及目录：`apps/api/internal/compute/`、`apps/api/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`、`.ai/changes/`。
- 修改内容：保留 `http.go` 作为 `Server` / `NewServer` / `Routes` / health / ready / panic recovery 入口；新增 `http_jobs.go`、`http_artifacts.go`、`http_models.go`、`http_contracts.go`、`http_simulation.go`、`http_workers.go`、`http_metrics.go`，按 route group 承接原 handler 与 request helper declarations；同步更新 compute README、architecture/current-state、compute-api architecture notes、Certainty/Elegance checklist 和本阶段 TODO。
- 修改原因：降低 `http.go` 巨型 handler 文件维护成本，为后续更细的 handler/package surface reduction 提供清晰 route-group 文件边界，同时避免改动 routes、service workflow 或 persistence。
- 本次已消除的不确定性：确认 `Routes` 注册块未重写；确认所有 handler/helper 仍在 `apps/api/internal/compute` 同 package；确认 endpoint path/method/scope/request/response shape、OpenAPI/client、contracts、migrations、Store、MemoryStore/PostgresStore、SQL 和 `service.go` signatures 未变更；确认 boundary audit 仍报告 41 Store methods / 12 embedded interfaces / 12 domain groups。
- 本次采用的关键假设：本次仅移动 top-level HTTP declarations；`numberValue` 作为现有同包 helper 继续可被 workers handler 与 simulation/evidence compatibility code 调用；不拆 service workflow，不新增 HTTP abstraction，不调整 route registration order。
- 剩余不确定性：Stage 3 `postgres.go` domain persistence split 尚未执行；完整 Go API domain package split 和后续 handler package movement 仍未完成。
- 影响范围 / 风险控制方式：同 package mechanical declaration movement；不修改 request parsing、auth checks、data-scope checks、response writing、service calls、route paths、OpenAPI/schema/database/client；运行 full Go tests 和 dependency/audit/pr-fast gates。
- 验证方式：`cd apps\api; go test ./...` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1` passed（Store interface methods: 41，Store embedded interfaces: 12，Domain groups: 12，Service constructors audited: 10，Internal Go package dirs: 13）；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` passed，evidence 写入 `tmp\ci-evidence\pr-fast.json`；`git diff --check -- apps\api docs scripts tasks .ai` passed with LF/CRLF warnings only。
- 是否更新 README：是，更新 `apps/api/internal/compute/README.md`、`docs/architecture/current-state.md`、`docs/architecture/compute-api.md`，并更新 Certainty/Elegance Development Plan checklist 与 `tasks/todo.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次是既有 modular monolith / handler surface reduction 路线下的同包文件组织切片，不新增长期架构决策。

## 22:49 - Compute PostgreSQL persistence domain file split

- 用户目标：执行 Go API Domain Package Split 文件级完成计划，完成 Stage 3：按 domain 拆分 `postgres.go` persistence，保持 SQL、transaction boundaries、scan order、pagination/cursor、NotFound mapping、idempotency、archive delete semantics、OpenAPI、contracts、migration 和 generated client 不变，并在阶段验证通过后 commit/push。
- 涉及目录：`apps/api/internal/compute/`、`apps/api/`、`scripts/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`、`.ai/changes/`。
- 修改内容：保留 `postgres.go` 作为 `PostgresStore` entrypoint、open/close、migration apply/check helper 文件；新增 `postgres_jobs.go`、`postgres_artifacts.go`、`postgres_models.go`、`postgres_simulation.go`、`postgres_agent.go`、`postgres_workers.go`、`postgres_metrics.go`、`postgres_sql_helpers.go`，按 domain 承接原 persistence methods、select SQL 和 scan helpers；更新 `scripts/audit-compute-api-boundary.ps1`，从所有 `postgres*.go` 扫描 PostgresStore implementation coverage，并把 Postgres persistence files 排除出 service-call attribution；同步更新 compute README、scripts README、architecture/current-state、compute-api architecture notes、Certainty/Elegance checklist 和本阶段 TODO。
- 修改原因：降低 `postgres.go` 巨型 persistence 文件维护成本，把 SQL/scan 函数与对应 domain persistence methods 放近，完成用户指定的低风险到高风险文件级拆分序列。
- 本次已消除的不确定性：确认 split 后 full Go tests 通过；确认 boundary audit 仍报告 Store 41 methods、12 embedded interfaces、12 domain groups，MemoryStore/PostgresStore 仍覆盖全部 Store methods；确认 `postgres.go` migration helper 和每个 domain SQL/scanner 仍在同 package，无 public API/schema/client/migration/HTTP/auth scope 变更；确认当前环境未配置 `COMPUTE_API_DATABASE_URL`，因此 migration smoke 按计划显式跳过。
- 本次采用的关键假设：本次仅移动 top-level PostgreSQL declarations；不重写 SQL text、transactions、scan order、where builders、cursor behavior、NotFound mapping、idempotency or archive semantics；`postgres_sql_helpers.go` 只承接确实跨 domain 共享的 `rowScanner` 与 `nullString`。
- 剩余不确定性：未在真实 PostgreSQL 数据库上运行 migration up/down smoke，因为当前环境没有 `COMPUTE_API_DATABASE_URL`；完整 Go API domain package split 仍未完成，`service.go` 仍是后续自然瘦身目标。
- 影响范围 / 风险控制方式：同 package mechanical declaration movement；select SQL 和 scan functions 保持在 domain file 内；无 OpenAPI/contracts/migrations/generated client/auth/HTTP route 变更；运行 full Go tests、dependency boundary、architecture audit、pr-fast 和 diff hygiene gates。
- 验证方式：`cd apps\api; go test ./...` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1` passed（Store interface methods: 41，Store embedded interfaces: 12，Domain groups: 12，Service constructors audited: 10，Internal Go package dirs: 13）；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` passed，evidence 写入 `tmp\ci-evidence\pr-fast.json`；`git diff --check -- apps\api docs scripts tasks .ai` passed with LF/CRLF warnings only；PostgreSQL migration up smoke skipped because `COMPUTE_API_DATABASE_URL` is absent；migration down smoke skipped because `COMPUTE_API_DATABASE_URL` is absent and `COMPUTE_API_MIGRATION_DOWN_SMOKE=true` is not configured。
- 是否更新 README：是，更新 `apps/api/internal/compute/README.md`、`scripts/README.md`、`docs/architecture/current-state.md`、`docs/architecture/compute-api.md`，并更新 Certainty/Elegance Development Plan checklist 与 `tasks/todo.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次是既有 modular monolith / persistence file organization 路线下的同包拆分，不新增长期架构决策。
