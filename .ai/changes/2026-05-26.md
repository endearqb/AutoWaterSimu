## 21:19 - AutoWaterSimu Next Phase 1B contract transform layer

- 用户目标：按 Phase 1B 计划补完合同转换层，让 `CanvasGraph -> ProcessGraph -> SimulationInput -> MaterialBalanceInput -> MaterialBalanceCalculator` 最小链路跑通。
- 涉及目录：`contracts/python/`、`contracts/tests/`、`contracts/examples/valid/`、`backend/app/services/`、`backend/app/tests/services/`、`frontend/src/contracts/`、`tasks/`。
- 修改内容：新增纯 Python contract transform package；新增 backend simulation input adapter；更新最小 compute job fixture 为完整嵌入式 `simulation_input.v1` payload；新增 transform invalid fixtures 与 tests；新增 TypeScript prototype。
- 修改原因：Phase 2 worker CLI 必须消费稳定的 `compute_job.v1.payload`，不能继续直接依赖旧 React Flow JSON。
- 本次已消除的不确定性：确认 material balance 最小合同可以转换为 legacy `MaterialBalanceInput` 并被现有 calculator 执行；确认组件顺序由 `component_schema.components` 决定；确认空值浓度和缺失 `{a,b}` 使用 legacy-compatible 默认值。
- 本次采用的关键假设：本轮只支持 `simulation.material_balance.v1`；失败路径抛出可映射为 `contract_error.v1` 的验证错误；旧 FastAPI route 与前端 UI 暂不接入新转换层。
- 剩余不确定性：worker CLI、artifact 写入、stdout/stderr 分离、version mismatch 与大结果 checksum 仍留到 Phase 2。
- 影响范围 / 风险控制方式：新增合同转换和 adapter 文件，不修改现有 HTTP 路由、OpenAPI schema、legacy store 或计算服务入口。
- 验证方式：
  - `backend\.venv\Scripts\python -m pytest contracts\tests -q` 通过，`42 passed`。
  - `cd backend; .venv\Scripts\python -m pytest app/tests/services/test_simulation_input_adapter.py -q` 通过，`3 passed`。
  - `cd backend; .venv\Scripts\python -m pytest app/tests/time_segment_validation_test.py app/tests/material_balance_segment_overrides_test.py app/tests/hybrid_udm_validation_test.py app/tests/udm_engine_variable_binding_test.py -q` 通过，`17 passed`。
  - `cd frontend; npx tsc --noEmit` 通过。
  - `git diff --check` 通过；Git 仅报告 touched text files 的 LF-to-CRLF normalization warning。
- 是否更新 README：否，本轮目录职责未变化。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮执行既有 Phase 1B 决策。

## 21:49 - Phase 1B review fixes plus Phase 2A worker and Phase 3A desktop scaffold

- 用户目标：先修补 Phase 1B review 问题，再推进 Phase 2A Worker 与 Phase 3A Desktop Scaffold。
- 涉及目录：`contracts/`、`contracts/python/`、`contracts/tests/`、`backend/app/services/`、`backend/app/tests/services/`、`frontend/src/contracts/`、`services/simulation-worker/`、`apps/desktop/`、`tasks/`。
- 修改内容：补齐 legacy top-level `customParameters` / `calculationParameters` / `timeSegments` 读取；对齐 Python/TypeScript edge transform 优先级；为 `simulation_input.v1` 增加 `time_segments`；adapter 保留分段并包装 Pydantic 校验错误；新增 Python worker CLI、自检、run-job、artifact 输出与 JSON-RPC smoke；新增 Desktop Rust scaffold、SQLite migration 草案和 command placeholders；补充新/关键目录 README。
- 修改原因：Phase 2 worker 必须基于修补后的合同链路执行，且 Desktop 后续 sidecar 集成需要最小可编译 Rust 基线。
- 本次已消除的不确定性：确认真实 legacy export 的顶层参数不会丢失；确认 time segment 覆盖可以通过 ProcessGraph/SimulationInput 到达 legacy calculator；确认 worker stdout 可直接 JSON parse；确认 Desktop scaffold 能通过 `cargo test`。
- 本次采用的关键假设：Phase 2A 仍复用 legacy backend adapter/calculator，不在本轮抽离完整 `simulation_core/`；Phase 3A 只做 scaffold，不接入真实 sidecar、SQLite runtime、React UI、installer 或 signing；Web Go API 不进入本轮。
- 剩余不确定性：Phase 2B 的纯 Python core packaging、worker version compatibility、Desktop sidecar spawn/path sandbox、SQLite runtime migration runner、installer smoke 和 support bundle 仍需后续实现。
- 影响范围 / 风险控制方式：未修改 legacy FastAPI route 或现有前端 UI；新增 worker/Desktop scaffold 与合同 adapter 测试隔离；结果大时间序列通过 artifact 文件输出，summary inline 返回。
- 验证方式：
  - `backend\.venv\Scripts\python -m pytest contracts\tests -q` 通过，`44 passed`。
  - `cd backend; .venv\Scripts\python -m pytest app\tests\services\test_simulation_input_adapter.py app\tests\time_segment_validation_test.py app\tests\material_balance_segment_overrides_test.py app\tests\hybrid_udm_validation_test.py app\tests\udm_engine_variable_binding_test.py -q` 通过，`22 passed`，仅既有 warning。
  - `backend\.venv\Scripts\python -m pytest services\simulation-worker\tests -q` 通过，`4 passed`。
  - `cargo test --manifest-path apps\desktop\src-tauri\Cargo.toml` 通过，`3 passed`。
  - `cd frontend; npx tsc --noEmit` 通过。
  - `git diff --check` 通过，仅 LF/CRLF normalization warnings。
- 是否更新 README：是，更新 `contracts/README.md`、`services/simulation-worker/README.md`、`apps/desktop/README.md`，新增 `contracts/python/README.md`、`backend/app/services/README.md`、`frontend/src/contracts/README.md`、`services/simulation-worker/simulation_worker/README.md`、`apps/desktop/src-tauri/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮执行既有 Phase 1B/2A/3A 分阶段决策，没有新增长期架构取舍。

## 22:19 - Phase 2B simulation core extraction and worker decoupling

- 用户目标：按 Phase 2B 计划把 material balance 最小计算链路从 legacy `backend/app` 解耦到纯 Python `simulation_core`，让 worker 不再依赖 FastAPI backend package。
- 涉及目录：`simulation_core/`、`services/simulation-worker/`、`tasks/`、`.ai/changes/`。
- 修改内容：先提交 Phase 1B review fix + Phase 2A + Phase 3A checkpoint；新增 `simulation_core/python/autowatersimu_simulation_core`；抽入 material balance runtime、ASM runtime helper、UDM runtime helper 和 UDM expression compiler；新增 core runtime Pydantic models、core adapter 和 adapter error；worker 改为仅导入 `simulation_core/python` 与 `contracts/python`；扩展 self-check；调整 JSON-RPC `run_job` 为 `params.job` / `result.compute_result` 形态；新增 core/worker boundary 与 parity 测试；更新 README First 上下文。
- 修改原因：Phase 3B Desktop sidecar 和后续 Web worker 都需要 worker 运行时边界独立于 legacy FastAPI backend，避免把 DB、用户、HTTP 框架和 backend package 传递进 worker。
- 本次已消除的不确定性：确认 core package 可在没有 `backend/` 的 `PYTHONPATH` 下导入；确认 core adapter + core calculator 与 legacy backend baseline 在最小 material balance job 上数值一致；确认 worker/core 静态上不再 import `app.*`；确认 JSON-RPC job object 路径能返回合法 `compute_result.v1`。
- 本次采用的关键假设：本轮只支持 `simulation.material_balance.v1`；暂时允许 backend 与 core 存在 material balance runtime 代码重复；legacy backend 不改为 core wrapper；ASM/UDM 独立 job handler、Desktop sidecar spawn、Go Compute API 不进入本轮。
- 剩余不确定性：后续需要决定 backend 何时改为调用 core、core package 如何独立打包/version pin、Desktop sidecar 路径 sandbox 和 worker binary packaging 规则、ASM/UDM job handler 迁移顺序。
- 影响范围 / 风险控制方式：不修改 legacy FastAPI route、OpenAPI client 或现有 frontend UI；worker CLI 路径行为保持兼容；JSON-RPC `params.job_path` 保留；通过 boundary、parity、worker artifact 和 regression tests 控制风险。
- 验证方式：
  - `backend\.venv\Scripts\python -m pytest contracts\tests -q` 通过，`44 passed`。
  - `backend\.venv\Scripts\python -m pytest simulation_core\tests -q` 通过，`4 passed`，仅 parity test 导入 backend baseline 时出现既有 warning。
  - `backend\.venv\Scripts\python -m pytest services\simulation-worker\tests -q` 通过，`6 passed`。
  - `cd backend; .venv\Scripts\python -m pytest app\tests\services\test_simulation_input_adapter.py app\tests\time_segment_validation_test.py app\tests\material_balance_segment_overrides_test.py app\tests\hybrid_udm_validation_test.py app\tests\udm_engine_variable_binding_test.py -q` 通过，`22 passed`，仅既有 warning。
  - `cargo test --manifest-path apps\desktop\src-tauri\Cargo.toml` 通过，`3 passed`。
  - `cd frontend; npx tsc --noEmit` 通过。
  - `rg -n "from app\.|import app\." services\simulation-worker\simulation_worker simulation_core\python\autowatersimu_simulation_core` 无匹配。
  - `git diff --check` 通过，仅 LF/CRLF normalization warnings。
- 是否更新 README：是，更新 `simulation_core/README.md`、`services/simulation-worker/README.md`、`services/simulation-worker/simulation_worker/README.md`，新增 `simulation_core/python/README.md`、`simulation_core/python/autowatersimu_simulation_core/README.md`、`simulation_core/python/autowatersimu_simulation_core/material_balance/README.md`、`simulation_core/python/autowatersimu_simulation_core/adapters/README.md`、`simulation_core/tests/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮执行既有 Phase 2B 拆分方向，未新增长期架构取舍。
