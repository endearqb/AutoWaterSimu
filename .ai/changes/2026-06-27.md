## 20:59 - UDM Network v2 P4 reaction/passive

- 用户目标：继续 UDM Network v2 路线，完成 P4 `passive + reaction_enabled + seed evaluator`，并按 phase 完成后推送当前分支。
- 涉及目录：`simulation_core/python/autowatersimu_simulation_core/udm_network/`、`simulation_core/tests/`、`simulation_core/*README.md`、`tasks/`、`.ai/changes/`。
- 修改内容：新增 `udm_reaction.py`，提供 `UDMReactionModel` / `UDMProcess`、`build_reaction_model()`、`build_node_reaction_model()` 和 `evaluate_reaction()`；新增 P4 reaction tests；更新 README。
- 修改原因：P4 需要在 UDM Network v2 中支持节点内 passive 和 reaction-enabled 两种模式，并能评估 seed catalog 风格的 `rate_expr`、`stoich`、`stoich_expr`、parameters、`t` 与 signals。
- 本次已消除的不确定性：已确认现有 `material_balance.udm_expression.compile_expression` 可复用，避免新增第二套表达式语言；已确认 passive 节点不能跳过 runtime/state shape；已用现有 v1 UDM runtime 做小案例 parity。
- 本次采用的关键假设：P4 仅提供 local node reaction evaluator，不做 ODE/time-step integration、multi-node transport、worker claim 或 artifact/result envelope；文档列出的 `components.py` / `models.py` 暂时收敛进 `udm_reaction.py`，等接口扩展后再拆分。
- 剩余不确定性：P5 Takacs transport、P6 五模型 parity、P7 SecondaryClarifier reference profile 仍未完成；seed catalog 全量 ASM parity 尚未在 P4 展开。
- 影响范围 / 风险控制方式：新增纯 Python core 模块和测试，不修改 legacy material balance runtime、不改合同 wire shape、不改变 worker 可执行能力。
- 验证方式：`backend\.venv\Scripts\python -m pytest simulation_core\tests\test_udm_network_reaction.py -q`。
- 是否更新 README：是，更新 simulation_core package/tests 和 `udm_network` README。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，沿用 UDM-v2 reaction/transport 拆分 ADR 草案。

## 20:44 - UDM Network v2 P3 flow balance

- 用户目标：继续 UDM Network v2 路线，完成 P3 自动流量平衡 `Aq=b`，覆盖 fixed/balanced/split/ratio/residual，并按 phase 完成后推送当前分支。
- 涉及目录：`simulation_core/python/autowatersimu_simulation_core/udm_network/`、`simulation_core/tests/`、`simulation_core/*README.md`、`tasks/`、`.ai/changes/`。
- 修改内容：新增 `flow_balance.py`，提供 `solve_flow_balance()`、`FlowBalanceResult` 和 `FlowBalanceError`；新增 flow balance tests；更新 README。
- 修改原因：P3 需要基于 hydraulic/pump 边构造线性约束并解析 fixed、balanced、split_fraction、ratio_to_edge 和 residual 流量，为后续 transport/reaction runtime 提供 resolved flows。
- 本次已消除的不确定性：已确认 P3 不应让 settling/signal 进入 `Aq=b`；已确认开发计划要求的八类测试案例均可用纯 core 测试覆盖；numpy 已是 simulation_core 依赖，无需新增依赖。
- 本次采用的关键假设：source/sink 节点通过没有双向 hydraulic/pump degree 自动排除节点守恒；P3 不处理 controlled/timeseries 外部信号，遇到该模式先以 `CONTROL_SIGNAL_MISSING` fail fast；pump/flow bounds 均在 strict solve 后检查。
- 剩余不确定性：P4 reaction/passive evaluator、P5 Takacs transport、P6 parity、P7 SecondaryClarifier reference profile 仍未完成；更复杂循环网络和 control signal 绑定会在后续阶段扩展。
- 影响范围 / 风险控制方式：新增纯 Python solver，不修改 material balance legacy runtime、不改合同 wire shape、不接 worker execution。
- 验证方式：`backend\.venv\Scripts\python -m pytest simulation_core\tests\test_udm_network_flow_balance.py -q`；`backend\.venv\Scripts\python -m pytest simulation_core\tests -q`。
- 是否更新 README：是，更新 simulation_core package/tests 和 `udm_network` README。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，沿用 UDM-v2 flow balance ADR 草案。

## 20:44 - UDM Network v2 P2 compiler MVP

- 用户目标：继续 UDM Network v2 路线，完成 P2 UDMNetworkCompiler MVP，并按 phase 完成后推送当前分支。
- 涉及目录：`simulation_core/`、`simulation_core/python/autowatersimu_simulation_core/udm_network/`、`simulation_core/tests/`、`tasks/`、`.ai/changes/`。
- 修改内容：新增 `udm_network` compiler package，提供 `compile_network()`、runtime node/edge dataclasses、state slice 和 edge bundle 结果模型；新增 compiler tests；更新 simulation_core 相关 README。
- 修改原因：P2 要把 `network_process_graph.v1` / `network_simulation_input.v1` 的基元节点和边编译为 runtime system，作为 P3 flow balance、P4 reaction/passive 和 P5 transport 的输入基础。
- 本次已消除的不确定性：已确认当前仓库没有现成 `udm_network` package；P2 文档验收只要求最小 2 节点 hydraulic 图成功、invalid port/edge_kind 明确失败；worker 执行仍是后续阶段。
- 本次采用的关键假设：P2 只实现 compiler，不实现 ODE execution、自动流量平衡、Takacs runtime 或 worker claim 能力；`network_simulation_input.v1` 缺少 ports 时可按 edge kind 推断端口，`network_process_graph.v1` 显式 ports 则严格校验。
- 剩余不确定性：P3 `Aq=b` 求解、P4 reaction/passive evaluator、P5 `takacs_settling.v1`、P6 五模型 parity、P7 SecondaryClarifier reference profile 仍未完成。
- 影响范围 / 风险控制方式：新增纯 Python core package，不修改 legacy material balance runtime、不改 worker capability、不改合同 wire shape；新增测试只覆盖 compiler MVP。
- 验证方式：`backend\.venv\Scripts\python -m pytest simulation_core\tests\test_udm_network_compiler.py -q`；`backend\.venv\Scripts\python -m pytest simulation_core\tests -q`。
- 是否更新 README：是，更新 simulation_core package/tests README 并新增 `udm_network/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，沿用已新增的 UDM-v2 ADR 草案。

## 16:48 - UDM Network v2 P-1/P0/P1 slice

- 用户目标：根据 `docs/rebuild/AutoWaterSimu_UDM_v2_Docs` 中的开发路线和配套文档，规划并执行 P-1 ADR 与边界冻结、P0 新合同与 job type、P1 前端四类连接线。
- 涉及目录：`.ai/decisions/`、`contracts/`、`apps/api/`、`frontend/src/components/Flow/`、`frontend/src/stores/`、`frontend/src/types/`、`tasks/`、`.ai/changes/`。
- 修改内容：新增 UDM-v2 ADR 草案 `0020`-`0027`；新增 `network_process_graph.v1` / `network_simulation_input.v1` schema 与 valid/invalid fixtures；同步 registry/codegen/OpenAPI/compute client；新增 `simulation.udm_network.v1` Go job profile 和 inline network simulation input simulation-check wire path；新增 Flow edge mode selector、四类 edge renderer/inspector、store import/export preservation；更新相关 README。
- 修改原因：UDM-v2 文档明确 v2 不应塞回 `simulation_input.v1`，四类边必须先以合同和 UI 可建模形态冻结，再进入 compiler/solver/runtime 阶段。
- 本次已消除的不确定性：确认现有 ADR `0019` 已被 mainline cutover 占用；确认 P0 只需要 wire path 和 schema/job type，不应宣称 worker 可执行；确认四类边字段采用 `edge_kind`、`flow_spec`、`component_policy`、`pump`、`transport_model`、`signal_spec` 并由 Flow store 保留。
- 本次采用的关键假设：UDM-v2 ADR 先以 Draft 状态落库；编号从文档建议的 `0019`-`0026` 平移为 `0020`-`0027`；`simulation.udm_network.v1` required capability 可提前写入 `udm_network` 以防无 runner 时被错误 claim；P1 不实现 UDMNetworkCompiler、flow balance solver、Takacs runtime 或 signal/pump control execution。
- 剩余不确定性：UDM Network compiler、自动流量平衡、Takacs settling、BSM1 parity、worker execution、v1 退役门仍需后续阶段实现和验收；`docs/rebuild/` 当前被 `.gitignore` 忽略，新投放文档若需提交需显式处理。
- 影响范围 / 风险控制方式：新增并行 v2 contracts/job type，不修改 `simulation_input.v1` 语义；API 仅支持 inline network simulation input 排队路径，不写入 legacy simulation input registry；前端保留 legacy `flow` 字段和旧 import/export 行为；不宣称 worker 支持执行。
- 验证方式：`backend\.venv\Scripts\python -m pytest contracts\tests -q`；`cd apps\api; go test ./internal/platform/contracts ./internal/domain/simulation ./internal/compute`；`cd frontend; npx tsc --noEmit`；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\readme-contract-check.ps1 -FailOnWarnings`；`git diff --check`；`scripts\check-contracts.ps1` 的 schema tests 通过，但 final client drift gate 因本次预期 OpenAPI/client diff 返回非零。
- 是否更新 README：是，更新 contracts、API/domain、Flow、Flow edges、stores、types README。
- 是否需要新增或更新架构决策（.ai/decisions/）：是，新增 UDM-v2 ADR 草案 `0020`-`0027`。

## 16:10 - Ignore docs/rebuild drops

- 用户目标：把 `docs/rebuild` 加到 `.gitignore` 中。
- 涉及目录：根 `.gitignore`、`docs/rebuild/`、`.ai/changes/`。
- 修改内容：新增 `/docs/rebuild/` 忽略规则，并说明已跟踪的 rebuild 文档仍保持跟踪。
- 修改原因：隐藏当前未跟踪的 rebuild 文档投放包和展开目录，减少工作区噪声。
- 本次已消除的不确定性：已确认 `.gitignore` 原先未忽略 `docs/rebuild`；已确认 `docs/rebuild/` 存在已跟踪权威文档，`.gitignore` 不会取消跟踪这些文件。
- 本次采用的关键假设：用户希望忽略整个 `docs/rebuild/` 下的新文件，而不是只忽略当前两个未跟踪 UDM v2 投放项。
- 剩余不确定性：未来若需要新增并提交 `docs/rebuild/` 文档，需要显式 `git add -f` 或调整忽略规则。
- 影响范围 / 风险控制方式：仅修改 ignore 规则；不删除、不移动、不取消跟踪已有文档。
- 验证方式：`git check-ignore -v docs/rebuild`；`git status --short`；`git diff --check -- .gitignore .ai/changes`。
- 是否更新 README：否。
- 是否需要新增或更新架构决策（.ai/decisions/）：否。

## 16:08 - GitHub remote branch cleanup

- 用户目标：除 `legacy/fastapi-frozen`、`main`、`codex/udm-network-v2` 外，删除 GitHub 远端仓库其他分支。
- 涉及目录：GitHub repository refs、`.ai/changes/`。
- 修改内容：删除 24 个远端分支，保留 `main`、`legacy/fastapi-frozen`、`codex/udm-network-v2`。
- 修改原因：用户确认其他远端分支均可删除，需收敛仓库分支列表到当前主线、冻结 legacy 线和 v2 开发线。
- 本次已消除的不确定性：已确认远端仓库为 `endearqb/AutoWaterSimu`；GitHub 默认分支为 `main`；根 README 明确 `legacy/fastapi-frozen` 是 frozen legacy branch；删除后 GitHub API 和 `git ls-remote` 均只剩 3 个分支。
- 本次采用的关键假设：用户给出的 3 个分支为完整保留名单；删除 dependabot 分支并清空对应打开 PR 符合用户本次清理意图。
- 剩余不确定性：GitHub Dependabot 后续可能按配置重新创建升级分支和 PR。
- 影响范围 / 风险控制方式：仅删除远端 Git refs；不修改源码、工作区业务文件、tag、GitHub Actions 配置或默认分支。
- 验证方式：`git ls-remote --heads origin`；`gh api repos/endearqb/AutoWaterSimu/branches --paginate --jq '.[].name'`；`gh pr list --repo endearqb/AutoWaterSimu --state open --limit 100`。
- 是否更新 README：否。
- 是否需要新增或更新架构决策（.ai/decisions/）：否。

## 15:31 - Use root README as GitHub landing

- 用户目标：让 GitHub 仓库首页显示根目录 README，而不是 `.github/README.md`；按建议删除已跟踪 `.github/README.md` 并加入 `.gitignore`。
- 涉及目录：`.github/`、根 `.gitignore`、`.ai/changes/`。
- 修改内容：删除 `.github/README.md`；新增 `.github/WORKFLOWS.md` 保存 GitHub automation 维护说明；在 `.gitignore` 中忽略 `.github/README.md`。
- 修改原因：GitHub 仓库 README 解析优先级为 `.github/README.md` > 根 `README.md` > `docs/README.md`，保留 `.github/README.md` 会遮蔽根 README。
- 本次已消除的不确定性：已通过 GitHub API 确认当前仓库 README path 为 `.github/README.md`；根 `README.md` 已存在于 `main`。
- 本次采用的关键假设：`.github` 目录仍需要维护说明，所以改用不会被 GitHub 首页优先展示的 `WORKFLOWS.md`。
- 剩余不确定性：GitHub 页面可能有短暂缓存；推送后可用 API 再确认 README path。
- 影响范围 / 风险控制方式：文档和 gitignore-only；不修改 workflow、CI 逻辑或源码。
- 验证方式：README contract check；`git diff --check`；推送后 `gh api repos/endearqb/AutoWaterSimu/readme`。
- 是否更新 README：是，删除 `.github/README.md` 并新增 `.github/WORKFLOWS.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否。

## 15:18 - Roadmap UDM-v2 supplement

- 用户目标：根据附件 `AutoWaterSimu_UDM_v2_完整需求_Spec_开发计划_合并版.md`，简要精简补充当前 Roadmap。
- 涉及目录：根 `README.md`、`README_zh.md`、`.github/README.md`、`.ai/changes/`。
- 修改内容：将 Roadmap 中泛化的 UDM Network v2 事项拆成 UDM-v2 ADR/network contracts、四类 typed edge + flow balance、UDM Network compiler/solver + 五模型 parity、SecondaryClarifier10Layer/Takacs/BSM1 evidence、worker/API/catalog/artifact 接入、v1/FastAPI 退役门，并在 `.github/README.md` 增加短版 near-term roadmap。
- 修改原因：附件明确 UDM-v2 是未来统一运行时，需要在公开 Roadmap 中体现关键阶段和退役门，但不应把完整实施计划搬进 README。
- 本次已消除的不确定性：已确认附件要求独立 `simulation.udm_network.v1` 路径、新 network contracts、四类边、严格流量平衡、passive/reaction/transport UDM、五模型 parity、二沉池/BSM1 和 v1 退役门。
- 本次采用的关键假设：Roadmap 保持公开入口级别，只列阶段，不展开 PR-00 到 PR-17 的全部实施清单；Takacs 在 README 中使用 ASCII 拼写。
- 剩余不确定性：ADR 0019-0026 尚未作为独立文件落库；具体工期和负责人未写入 README。
- 影响范围 / 风险控制方式：文档-only；不修改源码、schema、CI workflow、Git refs 或 GitHub metadata。
- 验证方式：README contract check；`git diff --check`；定向 `rg` 检查 Roadmap 关键词。
- 是否更新 README：是，更新根中英文 README 和 `.github/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮只补公开 Roadmap；后续 ADR 应单独落库。

## 15:10 - Public README and GitHub About rewrite

- 用户目标：处理剩余公开叙事不确定性；重写中英文功能简介和 Roadmap，修正 GitHub 当前显示的 `.github/README.md`，并重写仓库 About。
- 涉及目录：根 `README.md`、`README_zh.md`、`.github/README.md`、`.ai/changes/`、GitHub repository metadata。
- 修改内容：将功能简介从 legacy FastAPI 产品叙事改为 contract-first Next 平台叙事；将技术栈改为 Go Compute API + Python worker + React + Tauri + legacy oracle；将 Roadmap 改为 mainline cutover、standalone RC evidence、UDM Network v2、BSM/golden evidence、Desktop packaging 和 legacy retirement gates；将 `.github/README.md` 改为短公开入口并保留 GitHub automation 维护提示；更新 GitHub About description。
- 修改原因：公开入口仍会让读者把旧 FastAPI/React 系统误认为当前主线；`.github/README.md` 被 GitHub 显示时不应只展示目录契约。
- 本次已消除的不确定性：已确认 GitHub About 当前仍是旧 FastAPI 描述；已确认 `.github/README.md` 当前是 GitHub automation 目录 README；已根据 `docs/rebuild` PRD/Spec/Plan 校准 Next 产品定位、架构和路线。
- 本次采用的关键假设：不重写截图、赞助、许可证和历史来源段落；`.github/README.md` 采用短公开入口 + 最小维护提示，避免复制根 README。
- 剩余不确定性：GitHub Pages/homepage URL `https://envdama.top` 是否仍应作为 About website 未在本轮改变；公开站点内容若仍旧，需要单独处理。
- 影响范围 / 风险控制方式：文档和 GitHub metadata-only；不修改源码、CI workflow、schema 或分支。
- 验证方式：README contract check；`git diff --check`；`gh repo view`/`gh repo edit` 验证 description。
- 是否更新 README：是，更新根中英文 README 和 `.github/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，沿用 `0019-mainline-cutover-to-autowatersimu-next.md`。

## 15:02 - README mainline wording sync

- 用户目标：同步中英文 README 顶部、项目结构、Quick Start 和验证口径，使其符合 AutoWaterSimu Next 新主线。
- 涉及目录：根 `README.md`、`README_zh.md`、`.ai/changes/`。
- 修改内容：英文 README 将旧 Docker Compose/legacy 启动描述降级为 legacy/oracle 入口，更新项目结构和测试 gate 口径；中文 README 补齐 AutoWaterSimu Next 主线描述、README First、monorepo 目录、Justfile/standalone 启动和验证入口。
- 修改原因：主线切换后，中文 README 仍把 FastAPI + React 描述为当前主线；英文 README 的项目结构和 Quick Start 中仍有旧主线暗示。
- 本次已消除的不确定性：已确认 `docker-compose.standalone.yml` 当前服务为 `compute-postgres`、`compute-api`、`simulation-worker`、`frontend`；已确认 Justfile 存在 `standalone-up`、`standalone-status`、`standalone-smoke`、`pr-fast`、`readme-check` 等入口。
- 本次采用的关键假设：本轮只同步用户指定范围，不重写截图、Roadmap、许可证和完整功能描述；旧 FastAPI 来源说明仍保留为历史事实。
- 剩余不确定性：中英文 Roadmap 和功能简介仍偏 legacy，后续如要对外发布可单独做产品叙事重写。
- 影响范围 / 风险控制方式：文档-only；不修改运行代码、schema、CI 或分支；保留 legacy 启动方式但明确其用途。
- 验证方式：`docker compose -f docker-compose.standalone.yml config --services`；`rg` 检查旧主线措辞；README contract check；`git diff --check`。
- 是否更新 README：是，更新根英文和中文 README。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，沿用 `0019-mainline-cutover-to-autowatersimu-next.md`。

## 14:54 - AutoWaterSimu Next mainline cutover

- 用户目标：按附件第一选择执行当前仓库换主线；保留 AutoWaterSimu 仓库，将 `codex/autowatersimu-next-rebuild` 提升为 `main`，旧 `main` 冻结为 `legacy/fastapi-frozen`，v2 从新 `main` 开短分支开发。
- 涉及目录：仓库 git refs、根 `README.md`、`tasks/`、`.ai/changes/`、`.ai/decisions/`。
- 修改内容：更新根 README 顶部主线语义；新增 ADR `0019-mainline-cutover-to-autowatersimu-next.md`；记录本次 TODO/变更；创建 `legacy/fastapi-frozen`、`fastapi-legacy-2026-06-27`、新 `main` 和 `codex/udm-network-v2`。
- 修改原因：`codex/autowatersimu-next-rebuild` 已是 Next monorepo 产品基线，继续让旧 FastAPI `main` 作为默认主线会造成 README、CI、PR 目标和用户认知错位。
- 本次已消除的不确定性：已确认附件选择为“当前仓库直接换主线”；已确认当前工作区干净；已确认 `origin/main` 为 `4c7d61e7df2b5c9a1751af3168f5a1d6032d20cf`，`origin/codex/autowatersimu-next-rebuild` 为 `243e2ca4fde7b3897d17bd35f8c033a2faefacd1`，且旧 `origin/main` 是 rebuild 分支祖先，因此主线提升可 fast-forward。
- 本次采用的关键假设：v2 短分支未指定名称，按 Codex 分支前缀约定使用 `codex/udm-network-v2`；旧 FastAPI 冻结同时保留 branch 和 tag，便于回溯。
- 剩余不确定性：GitHub 仓库描述、branch protection、默认 CI 规则和开放 PR retarget 需要在 GitHub 侧继续确认；外部 Hosted full RC/no-skip evidence 仍按原 TODO 跟踪。
- 影响范围 / 风险控制方式：不删除任何分支；旧主线先保存为 branch/tag；新 `main` 使用 fast-forward push，不使用强推；不改运行代码。
- 验证方式：`git fetch origin --prune`；`git status --short --branch`；`git merge-base --is-ancestor origin/main origin/codex/autowatersimu-next-rebuild`；README check；推送后检查 `origin/main`、`origin/legacy/fastapi-frozen`、tag 和 `origin/codex/udm-network-v2` 指向。
- 是否更新 README：是，更新根 README 顶部 mainline 描述。
- 是否需要新增或更新架构决策（.ai/decisions/）：是，新增 `0019-mainline-cutover-to-autowatersimu-next.md`。

## 09:43 - README First framework automation

- 用户目标：根据附件中的 README First 优化框架，优化 README First 体系。
- 涉及目录：`AGENTS.md`、`README_First.md`、根 `README.md`、`.ai/`、`scripts/`、`Justfile`、`tasks/`，以及被检查命中的目录 README。
- 修改内容：新增 `.ai/readme-contracts.json` README 类型/预算/P0/P1 检查配置；新增 `scripts/readme-contract-check.ps1`，枚举维护源 README、分类、检查断链、核心文件表、本机路径、高置信 secret-like 文本、README First 读取链和命令笔误，并输出 `tmp/readme-contract-check.json`；新增 `just readme-check` 并接入 `just check -FailOnWarnings`；更新 README First/AGENTS/scripts 上下文；修复检查暴露的 P0 README 漂移，并精简超预算的 active contract README。
- 修改原因：框架要求 README First 不止人工阅读，还要具备 README 分类、当前事实校验和自动防漂移门禁。
- 本次已消除的不确定性：确认维护源 README 口径为 124 个，排除依赖、缓存、构建产物、临时目录和 `target` 打包产物；确认当前 P0 issue 为 0；确认当前 P1/P2 warning 为 0；确认 `backend/README.md`、`frontend/README.md`、`scripts/ci/README.md` 已收敛到 contract 长度预算内。
- 本次采用的关键假设：用 PowerShell + JSON 复用仓库现有工具链，不引入 Node/YAML 依赖；本轮自动检查先覆盖可稳定判定的漂移类型，不把需要人工语义判断的业务规则硬编码进脚本。
- 剩余不确定性：自动化尚不判断所有业务语义正确性，例如状态机、权限边界和领域规则是否完整；这类问题仍需结合对应目录源码、测试和评审。
- 影响范围 / 风险控制方式：文档和仓库级检查脚本-only；不修改业务代码、schema、API 或运行时行为；`just readme-check` 和 `just check` 对 P0 issue 与 warning 均失败。
- 验证方式：`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\readme-contract-check.ps1 -FailOnWarnings` 通过（124 README，0 issue，0 warning）；`just --list` 显示 `readme-check`；`just readme-check` 通过；`git diff --check` 通过，仅 LF/CRLF 提示。
- 是否更新 README：是，更新 README First、AGENTS、scripts、.ai 和相关目录 README。
- 是否需要新增或更新架构决策（.ai/decisions/）：否。
