## 00:18 - UDM-v2 前端硬隔离 PFC-B

- 用户目标：继续按附件计划完成 PFC-B，删除残留的 legacy/global UDM-v2 edge 污染，并在 phase 完成后提交、推送分支。
- 涉及目录：`frontend/src/components/Flow/edges/`、`frontend/src/types/`、`frontend/src/stores/`、`scripts/`、`tasks/`、`.ai/changes/`。
- 修改内容：删除 `frontend/src/types/networkEdges.ts`、`frontend/src/components/Flow/edges/EdgeModeSelector.tsx`、`frontend/src/components/Flow/edges/NetworkEdgeInspectorFields.tsx`；将 legacy import/load 时剥离 `_a` / `_b` 字段的小兼容函数就地保留在 v1 stores；加严 `scripts/check-udm-v2-import-boundary.mjs`，禁止已删除文件回流并禁止 legacy v1 code 再引用 `edge_kind` / `NetworkEdgeKind` / `createNetworkEdgeData`；更新相关 README。
- 修改原因：PFC-B 要在 UDM-v2 edge model 已迁入 `frontend/src/features/udm-v2/edges/` 后，彻底移除 pre-isolation 期间留在 legacy/global 路径的 v2 edge helper。
- 本次已消除的不确定性：确认两个 legacy Flow v2 UI 文件没有调用方；确认 `networkEdges.ts` 的唯一剩余价值是 legacy imported edge 数据清理；确认残留 `edge_kind` 只存在于 UDM-v2 feature、PF6 Playwright fixture 和 v1 负向测试中。
- 本次采用的关键假设：保留 legacy store 对旧导入数据中 `<param>_a` / `<param>_b` 字段的兼容清理，不改变 v1 `flow` edge 运行时 shape；不纳入已有 i18n 脏改动。
- 剩余不确定性：PF7 仍需执行最终 gate、截图/evidence 与 PR 说明；真实 UDM Network v2 worker runtime 仍是 runtime pending。
- 影响范围 / 风险控制方式：删除对象均为无调用方或已迁移的 v2 helper；v1 store 只保留局部兼容函数；边界脚本加入回流防护；不修改 legacy i18n 脏改动。
- 验证方式：`cd frontend; npm run check:udm-v2-boundary` 通过；`cd frontend; npx tsc --noEmit` 通过；`cd frontend; npm run test` 通过（13 files / 38 tests，Chakra CSS 在 jsdom 下有非失败 stderr 噪声）；`cd frontend; npx playwright test tests/v1-no-udm-v2-regression.spec.ts --project=chromium --no-deps --reporter=line` 在 standalone/no-auth 环境通过；残留 grep 确认 `edge_kind` / `NetworkEdgeKind` / `createNetworkEdgeData` 未出现在 legacy/global runtime 路径；`scripts\readme-contract-check.ps1 -FailOnWarnings` 已执行但因工作区已有未暂存删除 `release-notes.md` 导致根 README/README_zh 断链而失败，本 phase 未纳入该无关删除。
- 是否更新 README：是，更新 `frontend/src/components/Flow/edges/README.md`、`frontend/src/types/README.md` 和 `scripts/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，沿用 ADR-0028。

## 00:52 - UDM-v2 前端硬隔离 PF7

- 用户目标：按附件计划完成 PF7 最终 gate、文档与证据收尾，并在 phase 完成后提交、推送分支。
- 涉及目录：`docs/rebuild/`、`docs/rebuild/AutoWaterSimu_UDM_v2_Docs/`、`frontend/src/features/udm-v2/`、`frontend/tests/`、`scripts/`、`tasks/`、`.ai/changes/`。
- 修改内容：新增 `10_udm_v2_frontend_isolation_pr_notes.md`，补充 tracked `docs/rebuild/README.md` 索引；生成本地 `/udm-v2` 与 legacy v1 截图证据到 `tmp/udm-v2-frontend-split/`（不提交）；对 UDM-v2 feature、相关 Playwright 测试和 boundary script 执行 scoped Biome cleanup；更新任务清单与本变更记录。
- 修改原因：PF7 要把 07/08/09 计划的前端硬隔离开发结果收口为可审查证据，明确 non-goals、runtime pending 合约、验证命令与已知非本 PR 阻塞项。
- 本次已消除的不确定性：确认 `/udm-v2` 页面可渲染并可添加节点；确认 legacy v1 路由不暴露 UDM-v2 edge selector；确认 serializer contract fixtures 可通过；确认 import boundary 检查覆盖 UDM-v2 与 legacy 双向污染；确认全仓 Biome 失败来自既有 generated-client/openapi 范围而非本次 scoped UDM-v2 文件。
- 本次采用的关键假设：PF7 不修复本分支之前已存在的全仓 generated-client/Biome 问题；不把 `UDM_NETWORK_NOT_EXECUTABLE_YET` 当作仿真成功；截图作为本地证据保留在 `tmp/`，不纳入 git；附件原文 `07/08/09` 继续作为用户提供的 ignored 输入，不把整个 ignored 文档包拉进提交；不纳入已有 i18n 脏改动和根目录文档删除。
- 剩余不确定性：真实 UDM Network v2 worker runtime 仍未实现，提交路径只能进入 `runtime_pending`；全仓 Biome 和 README contract gate 需要先处理既有 generated/openapi 与无关 `release-notes.md` 删除后才能成为绿色。
- 影响范围 / 风险控制方式：本阶段没有新增公共运行时能力，只做最终文档、记录、局部格式化/ lint 修正和验证；scoped Biome 限定在 UDM-v2 feature/tests 与 boundary script；不提交 `tmp/` 证据和无关 dirty files。
- 验证方式：`cd frontend; npm run typecheck` 通过；`cd frontend; npm run test` 通过（13 files / 38 tests，Chakra CSS 在 jsdom 下有非失败 stderr 噪声）；`cd frontend; npm run build` 通过（保留既有 gray-matter eval 与 chunk size 警告）；`cd frontend; npm run check:udm-v2-boundary` 通过；`cd frontend; npm run test -- serializer.contract` 通过；`cd frontend; npx biome check src/features/udm-v2 tests/udm-v2-route.smoke.spec.ts tests/udm-v2-network-basic.spec.ts tests/udm-v2-submit-not-executable.spec.ts tests/v1-no-udm-v2-regression.spec.ts ../scripts/check-udm-v2-import-boundary.mjs` 通过；`cd frontend; npx playwright test tests/udm-v2-route.smoke.spec.ts tests/v1-no-udm-v2-regression.spec.ts --project=chromium --no-deps --reporter=line` 通过；`cd frontend; npx biome check .` 已执行但因既有 `openapi_formatted.json` 空文件、generated client 格式与 static-only class 等全仓 577 个诊断失败。
- 是否更新 README：是，更新 tracked `docs/rebuild/README.md`；ignored 附件包 README 仅作为本地输入索引，不纳入本次提交。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，沿用 ADR-0028，本阶段只补 PR notes 与 gate evidence。
