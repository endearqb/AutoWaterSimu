## 22:34 - Next local startup guide

- 用户目标：编写 Go 后端、前端和 Python worker 的启动说明 Markdown。
- 涉及目录：`docs/architecture/`、`tasks/`、`.ai/changes/`。
- 修改内容：新增 `docs/architecture/next-startup.md`，更新 `docs/architecture/README.md` 索引，并在 `tasks/todo.md` 记录本次文档任务。
- 修改原因：现有启动信息分散在根 README、`docs/architecture/local-dev.md`、`Justfile`、`docker-compose.dev.yml`、`apps/api/README.md`、`frontend/README.md` 和 `services/simulation-worker/README.md` 中，需要一个面向三进程本地启动的统一入口。
- 本次已消除的不确定性：确认 Go Compute API 默认端口为 `8088`，前端默认端口为 `5173`，worker 通过 API loop 使用 `dev-worker-token`；确认 `just dev*` recipes 和 `docker-compose.dev.yml` 是当前本地启动入口。
- 本次采用的关键假设：用户所说的 Go 后端指 AutoWaterSimu Next 的 `apps/api` Compute API；前端指当前 `frontend` Vite app；Python worker 指 `services/simulation-worker`。
- 剩余不确定性：未实际拉起本地服务验证端口占用和本机依赖状态；文档基于当前仓库配置和 README。
- 影响范围 / 风险控制方式：仅新增/更新文档和任务记录，不修改运行时代码、脚本、端口、token 或依赖。
- 验证方式：运行 `git diff --check -- docs\architecture tasks\todo.md .ai\changes\2026-06-16.md`；运行 `rg -n "[ \t]+$" docs\architecture\next-startup.md .ai\changes\2026-06-16.md tasks\todo.md docs\architecture\README.md` 检查新增/变更 Markdown 尾随空白。
- 是否更新 README：是，更新 `docs/architecture/README.md` 文档索引。
- 是否需要新增或更新架构决策（.ai/decisions/）：否。
