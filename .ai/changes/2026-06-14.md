## 00:07 - Result explanation audit state domain split

- 用户目标：根据 Certainty/Elegance PRD/Plan 与附件优先级继续收口非 Desktop 剩余质量工作，在 P0 service-test split 和 P1 artifact audit state split 后，继续推进 compute/domain 边界冻结，同时支撑 P2 result explanation audit 维护性。
- 涉及目录：`apps/api/internal/domain/evidence/`、`apps/api/internal/compute/`、`apps/api/`、`docs/architecture/`、`docs/rebuild/`、`scripts/`、`tasks/`、`.ai/changes/`。
- 修改内容：在 `domain/evidence` 新增 DTO-neutral `ResultExplanationAuditStateInput` 与 `ResultExplanationAuditState` compact projection helper；compute 侧 `resultExplanationAuditState` 改为调用 domain helper 并保留原 audit state key shape；新增 focused domain tests；同步 apps/api 与 compute/domain/evidence README、Compute API architecture、architecture current-state、boundary audit note、Certainty/Elegance development plan 和 `tasks/todo.md`。
- 修改原因：`domain/evidence` 已承接 result explanation evidence ref extraction 与 record data projection，现有 `resultExplanationAuditState` 只做 compact map projection，不做 selected audit envelope、call site 或 persistence 决策。将纯投影迁入 domain 可以减少后续 result explanation audit/data-scope 修改时的 compute 边界漂移。
- 本次已消除的不确定性：确认 result explanation submit/review/publish 的 selected audit envelope、job-scoped event persistence、review/publish 状态流、schema/job/evidence checks、HTTP mapping 和 store implementation 仍应留在 compute；确认本次不需要改 HTTP route、schema、OpenAPI、migration、generated client、auth scope、store interface 或 audit envelope shape。
- 本次采用的关键假设：本阶段是 P1 compute-boundary freeze 的小型 package movement，不是完整 result explanation workflow migration，也不是重新设计 audit action taxonomy；保持 `map[string]any` key/value 兼容比重新设计 audit payload 更重要；未跟踪的 `docs/rebuild/AutoWaterSimu_95分优雅度完整计划.md` 与 `docs/rebuild/simulation_core/` 属于用户/本地文件，不纳入本次修改。
- 剩余不确定性：P2 remaining data-scope / object-scope / all-mutation audit、P3 simulation_core/worker/backend 边界稳定、P4 roadmap triage、hosted green evidence、release artifact hosted round trip、legacy authenticated session 和 complete golden scenarios 仍未完成；是否继续迁移更多 result explanation workflow 逻辑，需要先补 store/DTO adapter，避免把 compute record/store 类型反向带入 domain。
- 影响范围 / 风险控制方式：只迁移纯投影并补 focused tests；compute 仍拥有 selected audit envelope assembly/call sites/persistence decisions、result explanation persistence/review/publish mutations、schema/job/evidence checks、HTTP mapping、store orchestration 和 DTO adapters；不改 public service signatures、contracts、OpenAPI、migrations、generated clients 或 auth scopes。
- 验证方式：已通过 `go test ./internal/domain/evidence ./internal/compute -run "Test(ResultExplanationAuditStateProjectsCompactState|ResultExplanationAuditStateOmitsEmptyOptionalFields|HTTPResultExplanationAuditEvents|HTTPResultExplanationReviewPublishAuditEvents|HTTPResultExplanationMutationTenantProjectSiteScope)$" -count=1`（在 `apps/api`）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\security-smoke.ps1`、`go test ./...`（在 `apps/api`）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1`、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1`、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1`、changed-file trailing-whitespace scan、`git diff --check -- .`。
- 是否更新 README：是，更新 `apps/api/README.md`、`apps/api/internal/README.md`、`apps/api/internal/compute/README.md`、`apps/api/internal/domain/README.md` 与 `apps/api/internal/domain/evidence/README.md`，记录 domain/evidence 现在拥有 result explanation compact audit state projection，compute 保留 selected audit envelope/call site/persistence。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次固化的是既有 compute/domain 分层规则下的小型投影迁移，不新增长期架构决策文件。
