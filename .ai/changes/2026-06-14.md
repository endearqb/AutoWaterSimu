## 13:46 - Benchmark timings Phase 0 baseline harness

- 用户目标：继续按 Certainty/Elegance PRD/Plan 收口非 Desktop 剩余工作；在 backend material_balance input/adapter boundary、calculator delegation/thin-shell 和 source-mode worker dependency gate 已完成后，启动且只启动 benchmark/timings Phase 0 baseline，不做热路径优化、不删除 worker fallback、不改 worker default strict mode。
- 涉及目录：`services/simulation-worker/`、`scripts/`、`scripts/ci/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`、`.ai/changes/`。
- 修改内容：worker 成功 `compute_result.v1.runtime_audit.timings_ms` 增加 `schema_validate`、`dependency_import`、`adapter_convert`、`compute`、`artifact_serialize`、`result_envelope` 与 `total` 分段；worker CLI test 增加 timing segment contract 断言；新增 opt-in `scripts/ci/performance-baseline-phase0.ps1`，运行 small material balance、medium ASM1、single UDM fixtures 的 `scipy_solver` / `rk4` / `adaptive_heun` solver matrix，记录 worker wall time、timing segments、artifact size、future Go claim/list latency metric design 和 open gaps；新增 `just performance-baseline-phase0`；同步 root/scripts/worker README、architecture current-state、Certainty/Elegance Development Plan 与 `tasks/todo.md`。
- 修改原因：当前优先级已从 leaf re-export / hosted wrapper 转向可证据化的 P3/性能前置。Phase 0 需要先建立可复跑 baseline 和 timing schema，再决定后续是否做 UDM RHS、dense/sparse、solver grid、Go claim/list 等热路径优化。
- 本次已消除的不确定性：确认 worker 可以对现有三类 tracked fixtures 生成 9 次 baseline run；确认每次成功结果都有 required timing segments；确认 artifact serialization size/cost 和 worker wall time 已进入 evidence；确认默认 `pr-fast` 不依赖该 opt-in baseline 但仍保持 passed；确认 missing `mixed_asm_udm` fixture 被明确记录为 open gap 而不是完成态。
- 本次采用的关键假设：本轮只接受现有 tracked fixtures；缺少 `contracts/examples/valid/mixed_asm_udm.compute_job.v1.json` 时 baseline 状态为 `partial`，不 hard-fail；Go claim/list latency 当前只做指标设计，不启动 Compute API HTTP benchmark；不改 OpenAPI、route schema、worker fallback、worker default validation mode、calculator 数值行为、Desktop packaged sidecar 或任何性能热路径。
- 剩余不确定性：tracked mixed ASM/UDM benchmark fixture / baseline 尚未补齐；Go claim/list latency 仍需要后续 integration 或 dedicated HTTP benchmark lane 实测；old compatibility model/helper cleanup、worker fallback 删除时机、worker default strict mode、hosted green evidence、full issuer/JWKS/RBAC、complete object data-scope 和 all-mutation audit 仍是未来工作。
- 影响范围 / 风险控制方式：baseline script 为 opt-in，不进入默认 `pr-fast`；timing segments 只增加观测字段，`compute_result.v1.runtime_audit.timings_ms` 仍为开放 object，不改变 schema contract；不修改 job execution 分支、adapter mode、OpenAPI/generated clients、数据库 schema 或 route behavior；保留 untracked `docs/rebuild/AutoWaterSimu_95分优雅度完整计划.md` 与 `docs/rebuild/simulation_core/` 不纳入本次提交。
- 验证方式：`backend\.venv\Scripts\python -m pytest services\simulation-worker\tests -q`（26 passed）；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\performance-baseline-phase0.ps1`（status partial，9 runs，0 hard violations，1 open gap：`mixed_asm_udm-baseline-fixture-missing`）；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-worker-dependency-installation.ps1`（passed，`deprecated_repo_path_fallback_used=False`，0 hard violations）；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-simulation-core-boundary.ps1`（passed，0 hard violations，0 open gaps）；`git diff --check -- .` 通过（仅 LF/CRLF notices）；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` 通过并生成 `tmp\ci-evidence\pr-fast.json`（status passed，10 steps）。
- 是否更新 README：是，更新 root `README.md`、`scripts/README.md`、`scripts/ci/README.md`、`services/simulation-worker/README.md`、`services/simulation-worker/simulation_worker/README.md`，并同步 `docs/architecture/current-state.md`、Development Plan 和 `tasks/todo.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次是性能 Phase 0 观测 baseline 和证据 harness，不新增长期架构决策。

## 13:25 - Worker dependency installation gate

- 用户目标：继续按 Certainty/Elegance PRD/Plan 收口非 Desktop 剩余工作；在 backend calculator thin-shell 完成后推进 `worker-dependency-installation-gate`，证明 source-mode worker 优先通过已安装 helper packages 运行，而不是依赖 deprecated repo-path fallback。本轮不删除 fallback、不改 worker 默认 validation mode、不做 Desktop packaged sidecar 证据，也不启动热路径性能优化。
- 涉及目录：`backend/`、`services/simulation-worker/`、`scripts/`、`scripts/ci/`、`.github/workflows/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`、`.ai/changes/`。
- 修改内容：backend `pyproject.toml` / `uv.lock` 新增 editable `autowatersimu-contracts`，与既有 `autowatersimu-simulation-core` 一起作为 source-mode worker helper packages；worker `self_check()` 的 `worker_dependency_imports` 现在报告 required modules、missing-before/after-fallback、module locations 和 `deprecated_repo_path_fallback_used`；新增 `scripts/audit-worker-dependency-installation.ps1`，默认 hard-fail fallback 使用并输出 `tmp/architecture-evidence/worker-dependency-installation.json`；`scripts/ci/pr-fast.ps1` 新增默认 worker dependency installation audit step 并写入 `tmp/ci-evidence/worker-dependency-installation/worker-dependency-installation.json`；同步 Justfile、root/local-dev/scripts/worker/workflow README、current-state、Development Plan 和 `tasks/todo.md`。
- 修改原因：worker runtime 已经优先 import installed packages，但 source-mode 依赖声明和默认 CI gate 还没有证明 fallback 未使用。继续保留 fallback 的同时增加默认 gate，可以先锁定可安装依赖路径，避免在 packaged sidecar 删除 fallback 前破坏本地开发或 Desktop 相邻路径。
- 本次已消除的不确定性：确认 backend source-mode dependency set 可安装 `autowatersimu-contracts` 与 `autowatersimu-simulation-core`；确认 worker self-check 当前从 installed/editable package metadata 解析到两个 helper package，且 `deprecated_repo_path_fallback_used=false`；确认默认 `pr-fast` 会执行 worker dependency installation audit 并记录 evidence；确认 `scripts/audit-simulation-core-boundary.ps1` 仍为 `passed`，0 hard violations、0 open gaps。
- 本次采用的关键假设：本轮只完成 source-mode/default CI gate；deprecated repo-path fallback 仍作为兼容路径和 dedicated fallback test 保留；packaged sidecar fallback 删除、Desktop first-start/round-trip 证据、worker strict mode 和性能热路径优化均后置；未跟踪的 `docs/rebuild/AutoWaterSimu_95分优雅度完整计划.md` 与 `docs/rebuild/simulation_core/` 属于用户/本地文件，不纳入本次修改。
- 剩余不确定性：worker deprecated fallback 的最终删除时机、packaged sidecar 是否完全不需要 fallback、old backend compatibility model/helper cleanup、benchmark/timings Phase 0、worker default strict mode、mixed-model 最终语义、hosted green evidence、full issuer/JWKS/RBAC、complete object data-scope 和 all-mutation audit 仍是未来工作。
- 影响范围 / 风险控制方式：不改 worker job execution、schema validation 默认模式、route schema、OpenAPI/generated clients、calculator behavior、数据库 schema 或 Desktop packaged path；新增 audit 默认拒绝 fallback，但提供 `-AllowDeprecatedRepoPathFallback` 供专门兼容验证使用；boundary audit 只把 `_ensure_worker_dependency_imports` 和 `_worker_dependency_import_status` 作为合法 fallback gate call sites。
- 验证方式：`uv lock --project backend`（resolved 117 packages，新增 `autowatersimu-contracts v0.1.0+phase2b`）；`backend\.venv\Scripts\python services\simulation-worker\simulation_worker\cli.py --self-check`（`worker_dependency_imports.ok=true`，`deprecated_repo_path_fallback_used=false`）；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-worker-dependency-installation.ps1`（passed，0 hard violations）；`backend\.venv\Scripts\python -m pytest services\simulation-worker\tests -q`（26 passed）；`uv lock --project backend --check` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-simulation-core-boundary.ps1`（passed，0 hard violations，0 open gaps）；`git diff --check -- .` 通过（仅 LF/CRLF notices）；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` 通过并生成 `tmp\ci-evidence\pr-fast.json`（status passed，10 steps，worker dependency installation audit passed，Compute boundary/correctness-freeze audits passed）。
- 是否更新 README：是，更新 root `README.md`、`.github/workflows/README.md`、`docs/architecture/local-dev.md`、`scripts/README.md`、`scripts/ci/README.md`、`services/simulation-worker/README.md`、`services/simulation-worker/simulation_worker/README.md`、`services/simulation-worker/tests/README.md`，并同步 `docs/architecture/current-state.md`、Development Plan 和 `tasks/todo.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次是既有 worker dependency fallback 去留策略下的 source-mode gate，不新增长期架构决策。

## 13:09 - Backend calculator thin-shell delegation

- 用户目标：继续按 Certainty/Elegance PRD/Plan 收口非 Desktop 剩余工作；在 input/adapter boundary 与 calculator delegation preflight 已 green 后，完成小切片 `backend-calculator-thin-shell-delegation`，不整文件 re-export `models.py`、不改 legacy route schema、不删除 worker fallback、不改 worker default strict，也不启动热路径性能优化。
- 涉及目录：`backend/app/material_balance/`、`backend/app/tests/`、`scripts/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`、`.ai/changes/`。
- 修改内容：`backend/app/material_balance/core.py` 改为 compatibility entrypoint，直接 re-export `autowatersimu_simulation_core.material_balance.core.MaterialBalanceCalculator`；新增 `backend/app/tests/material_balance_calculator_thin_shell_test.py` 证明 backend/core calculator class identity；`scripts/audit-simulation-core-boundary.ps1` 新增 `backend material_balance calculator thin shell` hard check，并把 calculator thin-shell 完成态纳入 result/input runtime contract 与 dual implementation drift risk 判断；同步 `backend/app/material_balance/__init__.py`、backend material_balance/tests README、scripts README、architecture current-state、Certainty/Elegance Development Plan 与 `tasks/todo.md`。
- 修改原因：上一阶段 preflight 已证明 legacy backend input path 与显式 core-runtime adapter 对 material_balance、ASM1Slim、ASM1、ASM3、UDM fixtures 无未声明差异；继续保留 backend 本地 calculator implementation 会让 PR-31 前的双实现漂移风险继续存在。将 `core.py` 收敛为兼容入口可以让 backend 生产服务路径直接使用 simulation_core calculator，同时保留 legacy import path。
- 本次已消除的不确定性：确认生产调用方只依赖 `MaterialBalanceCalculator.calculate()` 公共入口；确认外部对旧私有方法的唯一命中是 `material_balance_segment_overrides_test.py` 的 `_convert_to_tensors`，该方法仍由 simulation_core calculator 提供且 targeted suite 通过；确认 boundary audit 能机器识别 calculator thin-shell、result contract、input/adapter boundary 与 drift risk 完成态；确认 backend delegation preflight 在 thin-shell 后仍能证明 legacy input compatibility。
- 本次采用的关键假设：本轮只把 calculator implementation 委托到 simulation_core，保留 `backend/app/material_balance/core.py` 文件与 legacy import path；不重导出或删除 `models.py`，不改变 `app.models.MaterialBalanceInput` route schema，ASM/UDM helpers 仍作为后续 cleanup/migration candidate；不改变 worker fallback、worker default validation mode、OpenAPI/generated clients、数据库 schema、Desktop scope 或性能热路径。
- 剩余不确定性：old compatibility input model/helper cleanup、worker dependency installation gate、worker fallback 删除时机、worker default strict mode、benchmark/timings Phase 0、mixed-model 最终语义、hosted green evidence、full issuer/JWKS/RBAC、complete object data-scope 和 all-mutation audit 仍是未来工作。
- 影响范围 / 风险控制方式：以 file-level compatibility re-export 保持 backend import path；新增 focused identity test；保留 delegation preflight 与 backend/core parity drift guard；审计将 calculator thin-shell 缺失升级为 hard violation，并继续检查 production calculate entrypoints 与 backend-local input model import boundary。
- 验证方式：`cd backend; .venv\Scripts\python -m pytest app\tests\material_balance_calculator_thin_shell_test.py app\tests\material_balance_calculator_delegation_preflight_test.py app\tests\material_balance_result_thin_shell_test.py app\tests\material_balance_utils_thin_shell_test.py app\tests\material_balance_exceptions_thin_shell_test.py app\tests\services\simulation_input_adapter_boundary_test.py app\tests\time_segment_validation_test.py app\tests\material_balance_segment_overrides_test.py app\tests\hybrid_udm_validation_test.py app\tests\udm_engine_variable_binding_test.py -q`（30 passed，1 个既有 `python_multipart` warning）；`backend\.venv\Scripts\python -m pytest simulation_core\tests -q`（21 passed）；`scripts\audit-simulation-core-boundary.ps1`、`scripts\audit-simulation-core-input-contract.ps1`、`scripts\audit-simulation-core-correctness-freeze.ps1` 均 passed，hard_violations/open_gaps 均为 0；`uv lock --project backend --check` 通过；`git diff --check -- .` 通过（仅 LF/CRLF 提示）；`scripts\ci\pr-fast.ps1` 通过并生成 `tmp\ci-evidence\pr-fast.json`（status passed，9 steps）。另尝试 `cd backend; .venv\Scripts\python -m pytest app\tests -q`，因本地环境未提供 legacy DB 连接而失败（112 passed，1 skipped，17 failed，37 errors；错误集中为 `DATABASE_CONNECTION_FAILED`、login 503 与 `db` fixture 为 `None`），未观察到 material_balance targeted 失败。
- 是否更新 README：是，更新 `backend/app/material_balance/README.md`、`backend/app/tests/README.md` 与 `scripts/README.md`，并更新 package docstring、architecture current-state、Certainty/Elegance Development Plan 和 `tasks/todo.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次是既有 P3 backend thin-shell 方案与已完成 preflight 后的实现切片，不新增长期架构决策。

## 12:50 - Backend calculator delegation preflight

- 用户目标：在完成 backend material_balance input/adapter boundary 后，继续推进 `backend-calculator-delegation-preflight`；本轮只建立 shadow/parity 保护，不直接把 `backend/app/material_balance/core.py` 改成 thin shell，不删除 legacy calculator，不整文件 re-export `models.py`，不改 worker default strict，也不启动热路径性能优化。
- 涉及目录：`backend/app/material_balance/`、`backend/app/tests/`、`scripts/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`、`.ai/changes/`。
- 修改内容：新增 `backend/app/tests/material_balance_calculator_delegation_preflight_test.py`，用既有 `contracts/examples/valid` fixtures 对 material_balance minimal、ASM1Slim model-bound、独立 ASM1Slim、ASM1、ASM3、UDM 六类输入同时构造 legacy `app.models.MaterialBalanceInput` 和 simulation_core `MaterialBalanceInput`，分别运行 backend legacy calculator 与 simulation_core calculator，并比较 status、total steps、timestamps、node fields 和 numeric series；显式新增空的 `ALLOWED_CALCULATOR_MIGRATION_DIFFERENCES` 策略；`scripts/audit-simulation-core-boundary.ps1` 新增 `backend calculator delegation preflight` hard check，要求 preflight 覆盖必需 case、使用 backend core-runtime adapter、使用 legacy adapter、比较 backend/core calculator 且 allowed differences 为空；同步 README/current-state/Development Plan 与 `tasks/todo.md`。
- 修改原因：已有 `simulation_core/tests/test_material_balance_core.py` 是 core 侧 backend oracle drift guard，但它不通过上一阶段新增的 backend `simulation_input_to_core_material_balance_input()` 入口。PR-31 前必须证明 legacy backend 输入路径与显式 core-runtime adapter 路径可在同一组 fixtures 上产生无差异结果，才能安全做后续 calculator delegation。
- 本次已消除的不确定性：确认六个 delegation preflight fixtures 均存在；确认 legacy adapter 与 backend core-runtime adapter 可分别构造对应输入模型；确认 backend legacy calculator 与 simulation_core calculator 当前对 material_balance、ASM1Slim、ASM1、ASM3、UDM 无需声明 migration differences；确认 P3 boundary audit 可把 preflight 缺失作为 hard violation。
- 本次采用的关键假设：本轮只增加测试、audit 和长期状态记录；不改变 route schema、OpenAPI/generated clients、数据库 schema、calculator 数值实现、worker invocation/default validation mode、Desktop scope、旧 `models.py` 导出兼容性或热路径性能逻辑；未跟踪的 `docs/rebuild/AutoWaterSimu_95分优雅度完整计划.md` 与 `docs/rebuild/simulation_core/` 属于用户/本地文件，不纳入本次修改。
- 剩余不确定性：backend calculator thin-shell delegation implementation、legacy `core.py` 删除时机、old input model cleanup、ASM/UDM helper migration、worker dependency fallback gate、worker default strict mode、mixed-model 最终语义、benchmark/timings Phase 0、hosted green evidence、full issuer/JWKS/RBAC、complete object data-scope 和 all-mutation audit 仍是未来工作。
- 影响范围 / 风险控制方式：保持 legacy calculator implementation 不变；用 focused backend preflight 与 boundary audit 同时固定 delegation 前置条件；空 allowed-differences map 防止未来把 legacy bugfix 或 migration drift 静默混入 thin-shell 主迁移。
- 验证方式：`cd backend; .venv\Scripts\python -m pytest app\tests\material_balance_calculator_delegation_preflight_test.py app\tests\services\simulation_input_adapter_boundary_test.py app\tests\material_balance_utils_thin_shell_test.py app\tests\material_balance_result_thin_shell_test.py app\tests\material_balance_exceptions_thin_shell_test.py app\tests\time_segment_validation_test.py app\tests\material_balance_segment_overrides_test.py app\tests\hybrid_udm_validation_test.py app\tests\udm_engine_variable_binding_test.py -q`（29 passed，1 个既有 `python_multipart` deprecation warning）；`backend\.venv\Scripts\python -m pytest simulation_core\tests -q`（21 passed）；`scripts\audit-simulation-core-boundary.ps1`、`scripts\audit-simulation-core-input-contract.ps1`、`scripts\audit-simulation-core-correctness-freeze.ps1` 均 passed，hard_violations/open_gaps 均为 0；`uv lock --project backend --check` 通过；`git diff --check -- .` 通过（仅 LF/CRLF 提示）；`scripts\ci\pr-fast.ps1` 通过并生成 `tmp\ci-evidence\pr-fast.json`（9 steps passed）。
- 是否更新 README：是，更新 `backend/app/material_balance/README.md`、`backend/app/tests/README.md` 与 `scripts/README.md`，并更新 architecture/rebuild 长期状态。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次是既有 P3 backend thin-shell 方案下的 calculator delegation preflight，不新增长期架构决策。

## 12:34 - Backend material balance input adapter boundary

- 用户目标：按最新优先级先完成 `backend-material-balance-input-adapter-boundary`，回答 legacy backend 输入路径如何显式、可审计地进入 simulation_core runtime model；本轮不直接切 calculator thin shell、不整文件 re-export `models.py`、不改 worker default strict，也不启动热路径性能优化。
- 涉及目录：`backend/app/material_balance/`、`backend/app/services/`、`backend/app/tests/`、`simulation_core/python/autowatersimu_simulation_core/adapters/`、`scripts/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`、`.ai/changes/`。
- 修改内容：`backend/app/material_balance/core.py` 的 calculator input annotations 改向 `autowatersimu_simulation_core.material_balance.models.MaterialBalanceInput` 等 runtime models，calculator implementation 不变；`backend/app/services/simulation_input_adapter.py` 新增 `simulation_input_to_core_material_balance_input()` 委托 core adapter 构造 simulation_core runtime model，并把旧 `simulation_input_to_material_balance_input()` 标记为 legacy `app.models.MaterialBalanceInput` compatibility-only path；`backend/app/material_balance/models.py` 与 `__init__.py` 标记 backend-local input models 只保留兼容导入；tracked focused adapter boundary test 证明 backend 能显式构造 core runtime model；`scripts/audit-simulation-core-boundary.ps1` 新增 backend input/adapter boundary check，列出 production calculate entrypoints、legacy route input hits，并把隐藏的 backend-local input model 依赖作为 hard violation；同步 README/current-state/Development Plan 与 `tasks/todo.md`。
- 修改原因：exceptions/result/utils leaf 已完成，但 PR-31 真正阻塞点是输入路径仍混杂 `app.models`、backend-local `material_balance.models` 和 simulation_core models。本轮先把“API schema compatibility path”和“runtime core contract path”分开并可审计，为下一步 calculator delegation preflight 提供边界证据。
- 本次已消除的不确定性：确认生产 calculator 入口集中在 5 个 service `_run_calculation_sync`；确认 legacy FastAPI route schema 仍来自 `app.models.MaterialBalanceInput`；确认 backend-local `material_balance.models` 不是当前生产 runtime contract；确认 `simulation_input.v1` 已可在 backend 显式构造 simulation_core `MaterialBalanceInput`；确认 P3 boundary audit 可检查并固定这些事实。
- 本次采用的关键假设：本轮只做类型边界、adapter 明确化、审计与文档；不改变 route schema、OpenAPI/generated clients、数据库 schema、calculator 数值实现、worker 调用模式、Desktop scope、`models.py` 导出兼容性或热路径性能逻辑；未跟踪的 `docs/rebuild/AutoWaterSimu_95分优雅度完整计划.md` 与 `docs/rebuild/simulation_core/` 属于用户/本地文件，不纳入本次修改。
- 剩余不确定性：backend calculator delegation preflight、legacy `core.py` 删除时机、old input model cleanup、ASM/UDM helper migration、worker dependency fallback gate、worker default strict mode、mixed-model 最终语义、benchmark/timings Phase 0、hosted green evidence、full issuer/JWKS/RBAC、complete object data-scope 和 all-mutation audit 仍是未来工作。
- 影响范围 / 风险控制方式：保持 legacy API body models 和 calculation behavior 不变；新增 core-runtime adapter 函数而不替换旧函数；audit 用 expected production calculate entrypoints 和 backend-local input model import 扫描防止边界回退；文档明确本轮不是 PR-31 主迁移完成。
- 验证方式：`cd backend; .venv\Scripts\python -m pytest app\tests\services\simulation_input_adapter_boundary_test.py app\tests\material_balance_utils_thin_shell_test.py app\tests\material_balance_result_thin_shell_test.py app\tests\material_balance_exceptions_thin_shell_test.py app\tests\time_segment_validation_test.py app\tests\material_balance_segment_overrides_test.py app\tests\hybrid_udm_validation_test.py app\tests\udm_engine_variable_binding_test.py -q`（22 passed，1 个既有 `python_multipart` deprecation warning）；`backend\.venv\Scripts\python -m pytest simulation_core\tests -q`（21 passed）；`scripts\audit-simulation-core-boundary.ps1`、`scripts\audit-simulation-core-input-contract.ps1`、`scripts\audit-simulation-core-correctness-freeze.ps1` 均 passed，hard_violations/open_gaps 均为 0；`uv lock --project backend --check` 通过；`git diff --check -- .` 通过（仅 LF/CRLF 提示）；`scripts\ci\pr-fast.ps1` 通过并生成 `tmp\ci-evidence\pr-fast.json`（9 steps passed）。
- 是否更新 README：是，更新 `backend/app/material_balance/README.md`、`backend/app/services/README.md`、`backend/app/tests/README.md`、`simulation_core/python/autowatersimu_simulation_core/adapters/README.md` 与 `scripts/README.md`，并更新 architecture/rebuild 长期状态。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次是既有方案 A 下的 input/adapter boundary preflight，不新增长期架构决策。

## 10:53 - Backend material balance utils thin-shell leaf

- 用户目标：继续按 Certainty/Elegance PRD/Plan 和最新优雅度重构优先级收口非 Desktop 剩余工作；在 exceptions/result model leaf 完成后，优先推进 P3 backend thin-shell 的下一个低风险 utility helper leaf，而不是继续低收益 service-test split、泛化 wrapper、worker 默认 strict 或热路径性能优化。
- 涉及目录：`backend/app/material_balance/`、`backend/app/tests/`、`scripts/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`、`.ai/changes/`。
- 修改内容：`backend/app/material_balance/utils.py` 改为 compatibility re-export `autowatersimu_simulation_core.material_balance.utils` 的 7 个稳定 helper；新增 `material_balance_utils_thin_shell_test.py` 证明 backend/core helper object identity；`scripts/audit-simulation-core-boundary.ps1` 新增 `backend material_balance utils thin shell` check，要求 backend dependency、core utils import 和 required exports 同时存在；同步 backend/material_balance/tests README、scripts README、architecture current-state、Certainty/Elegance Development Plan 与 `tasks/todo.md`。
- 修改原因：旧 backend `utils.py` 与 core helper 基本重复且仍使用旧输入字段名，是 backend thin-shell planning 中低风险、可单独审计的 leaf。把它改为 re-export 可减少 backend/core helper 漂移，同时保持 legacy import path 稳定。
- 本次已消除的不确定性：确认 `app.material_balance.utils` 无生产调用方依赖旧本地实现细节；确认 legacy self-test 之外的运行时行为由 core helper identity 保护；确认 P3 boundary audit 可在 utils leaf 纳入后继续 `passed`，0 hard violations，0 open gaps。
- 本次采用的关键假设：本轮只做 utility helper leaf thin-shell，不迁 `models.py` 旧输入模型、不改 `core.py` calculator delegation、不改 ASM/UDM helpers、不改变 routes、schemas、OpenAPI/generated clients、worker default validation mode、Desktop scope 或数值行为；未跟踪的 `docs/rebuild/AutoWaterSimu_95分优雅度完整计划.md` 与 `docs/rebuild/simulation_core/` 属于用户/本地文件，不纳入本次修改。
- 剩余不确定性：backend calculator delegation、old input model cleanup、显式 adapter 校验、剩余 ASM/UDM helper migration、worker/default strict mode、mixed-model 最终语义、benchmark/timings harness、hosted green evidence、complete golden scenarios、full issuer/JWKS/RBAC、complete object data-scope 和 all-mutation audit 仍是未来工作。
- 影响范围 / 风险控制方式：保持 `app.material_balance.utils` import path 和 helper names 不变；新增 focused object identity test；boundary audit 要求 dependency-backed re-export；README/current-state 明确这是第三个 leaf，不代表 PR-31 backend thin-shell 主迁移完成。
- 验证方式：已通过 `cd backend; .venv\Scripts\python -m pytest app\tests\material_balance_utils_thin_shell_test.py app\tests\material_balance_result_thin_shell_test.py app\tests\material_balance_exceptions_thin_shell_test.py app\tests\time_segment_validation_test.py app\tests\material_balance_segment_overrides_test.py app\tests\hybrid_udm_validation_test.py app\tests\udm_engine_variable_binding_test.py -q`（20 passed，既有 `python_multipart` warning）、`backend\.venv\Scripts\python -m pytest simulation_core\tests -q`（21 passed）、`uv lock --project backend --check`、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-simulation-core-boundary.ps1`（`passed`，0 hard violations，0 open gaps）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-simulation-core-input-contract.ps1`（`passed`，0 hard violations，0 open gaps）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-simulation-core-correctness-freeze.ps1`（`passed`，0 hard violations，0 open gaps）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1`（`status=passed`，9 steps，`compute_boundary_audit.status=passed`，`simulation_core_correctness_freeze_audit.status=passed`）和 `git diff --check -- .`（仅 LF-to-CRLF notices，无 whitespace error）。
- 是否更新 README：是，更新 `backend/app/material_balance/README.md`、`backend/app/tests/README.md` 与 `scripts/README.md`，并更新 architecture/rebuild 长期状态。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次是既有 backend thin-shell 方案 A 下的第三个 leaf 实施，不新增长期架构决策。

## 10:36 - Backend material balance result thin-shell leaf

- 用户目标：继续按 Certainty/Elegance PRD/Plan 收口非 Desktop 剩余工作；在 exception leaf 完成后，继续 P3 backend thin-shell planning 的下一个低风险 leaf，而不是直接迁移整份 `models.py`、删除 dead helper 或启动热路径性能优化。
- 涉及目录：`backend/`、`backend/app/material_balance/`、`backend/app/services/`、`backend/app/tests/`、`scripts/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`、`.ai/changes/`。
- 修改内容：`backend/app/material_balance/core.py` 改为构造 `autowatersimu_simulation_core.material_balance.models.MaterialBalanceResult`；ASM1/ASM3/UDM services 的 `_run_calculation_sync` 返回类型注解改向 core result model；新增 `material_balance_result_thin_shell_test.py` 证明 backend calculator 返回值 class identity；`scripts/audit-simulation-core-boundary.ps1` 新增 result model leaf check，并把 full calculator thin-shell 判断收窄为 calculator delegation，避免把 result leaf 误判为整核薄壳完成；同步 backend/material_balance/services/tests README、scripts README、architecture current-state、Certainty/Elegance Development Plan 与 `tasks/todo.md`。
- 修改原因：`MaterialBalanceResult` 是 backend calculator 当前唯一实际运行时构造的 legacy material_balance model；先把该 leaf 指向 simulation_core 可降低双实现输出模型漂移，同时不触碰旧输入 DTO、route schema、OpenAPI 或数值行为。
- 本次已消除的不确定性：确认 backend calculator 可以直接返回 core `MaterialBalanceResult`；确认服务层只把 `MaterialBalanceResult` 用作注解并按属性读取结果；确认旧 `app.material_balance.models` 输入模型仍是旧字段契约，不能整文件 re-export；确认 P3 boundary audit 仍为 `passed`，0 hard violations，0 open gaps。
- 本次采用的关键假设：本轮只做 result model leaf thin-shell，不迁 `MaterialBalanceInput` / `NodeData` / `EdgeData` / `CalculationParameters`，不改 `app.material_balance.__init__` exports，不删除 `models.py` 或 `utils.py`，不改变 worker default validation mode、routes、schemas、OpenAPI/generated clients、Desktop scope 或数值行为；未跟踪的 `docs/rebuild/AutoWaterSimu_95分优雅度完整计划.md` 与 `docs/rebuild/simulation_core/` 属于用户/本地文件，不纳入本次修改。
- 剩余不确定性：backend calculator delegation、old input model / dead `utils.py` 清理、显式 adapter 校验、worker/default strict mode、mixed-model 最终语义、benchmark/timings harness、hosted green evidence、complete golden scenarios、full issuer/JWKS/RBAC、complete object data-scope 和 all-mutation audit 仍是未来工作。
- 影响范围 / 风险控制方式：保持 legacy input DTO 和 public import path 稳定；新增 focused class identity test；boundary audit 同时检查 calculator result import 与 service annotation import；审计里的 full calculator thin-shell detection 不再用泛化 `autowatersimu_simulation_core` 字符串匹配，避免隐藏 backend/core dual implementation drift。
- 验证方式：已通过 `cd backend; .venv\Scripts\python -m pytest app\tests\material_balance_result_thin_shell_test.py app\tests\material_balance_exceptions_thin_shell_test.py app\tests\time_segment_validation_test.py app\tests\material_balance_segment_overrides_test.py app\tests\hybrid_udm_validation_test.py app\tests\udm_engine_variable_binding_test.py -q`（19 passed，既有 `python_multipart` warning）、`backend\.venv\Scripts\python -m pytest simulation_core\tests -q`（21 passed）、`uv lock --project backend --check`、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-simulation-core-boundary.ps1`（`passed`，0 hard violations，0 open gaps）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-simulation-core-input-contract.ps1`（`passed`，0 hard violations，0 open gaps）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-simulation-core-correctness-freeze.ps1`（`passed`，0 hard violations，0 open gaps）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1`（`status=passed`，9 steps，`compute_boundary_audit.status=passed`，`simulation_core_correctness_freeze_audit.status=passed`）和 `git diff --check -- .`（仅 LF-to-CRLF notices，无 whitespace error）。
- 是否更新 README：是，更新 `backend/app/material_balance/README.md`、`backend/app/services/README.md`、`backend/app/tests/README.md` 与 `scripts/README.md`，并更新 architecture/rebuild 长期状态。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次是既有 backend thin-shell 方案 A 下的第二个 leaf 实施，不新增长期架构决策。

## 10:27 - Backend material balance exception thin-shell leaf

- 用户目标：继续按 Certainty/Elegance PRD/Plan 收口非 Desktop 剩余工作；在 P3 green audits、input-contract 和 correctness-freeze 都已建立后，推进 backend thin-shell planning 的第一个低风险 leaf，而不是直接启动 material_balance 主迁移或热路径性能优化。
- 涉及目录：`backend/`、`backend/app/material_balance/`、`backend/app/tests/`、`scripts/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`、`.ai/changes/`。
- 修改内容：`backend/app/material_balance/exceptions.py` 改为 compatibility re-export `autowatersimu_simulation_core.material_balance.exceptions`；`backend/pyproject.toml` 声明本地 `autowatersimu-simulation-core` dependency 并更新 `backend/uv.lock`；新增 `material_balance_exceptions_thin_shell_test.py` 证明 backend/core exception class identity；`scripts/audit-simulation-core-boundary.ps1` 新增 `backend material_balance exceptions thin shell` check，要求 re-export 和 dependency declaration 同时存在；同步 backend/material_balance/tests/scripts README、architecture current-state、Certainty/Elegance Development Plan 与 `tasks/todo.md`。
- 修改原因：backend 与 simulation_core 仍保留重复 material-balance runtime，后续性能优化前需要逐步降低双实现漂移。异常类是低风险 leaf：类定义语义一致、无数值行为、无 OpenAPI schema 变化，但必须通过显式 backend dependency 和 class identity test 防止依赖当前虚拟环境的偶然可导入状态。
- 本次已消除的不确定性：确认 backend `exceptions.py` 与 core `exceptions.py` 类集合一致；确认直接 re-export 不影响现有 segment override 异常测试；确认 backend fresh uv 环境可通过本地 path dependency 获得 `autowatersimu-simulation-core`；确认 P3 boundary audit 仍为 `passed`，0 hard violations，0 open gaps。
- 本次采用的关键假设：本轮只做 exception leaf thin-shell，不迁 `core.py`、`models.py`、`utils.py`、ASM/UDM helpers、routes、schema、OpenAPI/generated clients、worker default validation mode 或数值行为；`exceptions.py` 的 class identity 比保留 backend 本地中文异常 docstring 更重要。
- 剩余不确定性：backend thin-shell 主迁移、`MaterialBalanceResult` import 归属、dead `utils.py` / old input model 清理、app.models 对象是否显式走 simulation_core 校验、worker/default strict mode、mixed-model 最终语义、benchmark/timings harness、hosted green evidence、complete golden scenarios、full issuer/JWKS/RBAC、complete object data-scope 和 all-mutation audit 仍是未来工作。
- 影响范围 / 风险控制方式：保持 `app.material_balance.exceptions` import path 和 exception class names 不变；新增 focused class identity test；boundary audit 同时检查 re-export 与 dependency declaration；README/current-state 明确这只是第一个 leaf，不代表 PR-31 backend thin-shell 主迁移完成。
- 验证方式：已通过 `cd backend; .venv\Scripts\python -m pytest app\tests\material_balance_exceptions_thin_shell_test.py app\tests\time_segment_validation_test.py app\tests\material_balance_segment_overrides_test.py app\tests\hybrid_udm_validation_test.py app\tests\udm_engine_variable_binding_test.py -q`（18 passed，既有 `python_multipart` warning）、`backend\.venv\Scripts\python -m pytest simulation_core\tests -q`（21 passed）、`uv lock --project backend --check`、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-simulation-core-boundary.ps1`（`passed`，0 hard violations，0 open gaps）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-simulation-core-input-contract.ps1`（`passed`，0 hard violations，0 open gaps）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-simulation-core-correctness-freeze.ps1`（`passed`，0 hard violations，0 open gaps）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1`（`status=passed`，9 steps，`compute_boundary_audit.status=passed`，`simulation_core_correctness_freeze_audit.status=passed`）、docs/rebuild scan 和 `git diff --check -- .`（仅 LF-to-CRLF notices，无 whitespace error）。
- 是否更新 README：是，更新 `backend/README.md`、`backend/app/README.md`、`backend/app/material_balance/README.md`、`backend/app/tests/README.md` 与 `scripts/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；方案 A / backend thin-shell 方向已有性能文档与当前状态约束，本轮是第一个 leaf 实施和审计收口。

## 10:11 - Simulation core correctness freeze before performance

- 用户目标：继续按 Certainty/Elegance PRD/Plan 和最新优雅度优先级收口非 Desktop 剩余工作；在 P3 boundary/input-contract audits 已 green 后，先冻结 simulation_core 当前 mixed-model / clamp / compute_mask 语义，避免后续热路径性能优化隐式改变行为。
- 涉及目录：`simulation_core/`、`simulation_core/tests/`、`scripts/`、`scripts/ci/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`、`.ai/decisions/`、`.ai/changes/`。
- 修改内容：`test_material_balance_core_boundary.py` 新增 core-only `_run_hours` branch precedence、clamp policy 和 `_ode_balance` `compute_mask` derivative masking 测试；新增 `scripts/audit-simulation-core-correctness-freeze.ps1` 输出 `tmp/architecture-evidence/simulation-core-correctness-freeze.json`；新增 Justfile 入口并把该 audit 接入 `scripts/ci/pr-fast.ps1` 默认 gate；新增 ADR `0014-simulation-core-correctness-freeze-before-perf.md`；同步 root/scripts/scripts-ci/simulation_core README、architecture current-state、Certainty/Elegance Development Plan 与 `tasks/todo.md`。
- 修改原因：当前 `_run_hours` 以 `asm1slim -> asm1 -> asm3 -> udm -> default` 互斥选择模型分支，ASM/UDM branches 会 clamp solver output，而 default branch 当前保留 commented clamp。该行为是性能优化前必须明确冻结的当前 baseline；否则 UDM RHS、dense/sparse 或其他热路径改写可能在无意中改变 mixed-model 语义。
- 本次已消除的不确定性：确认当前 `_run_hours` branch precedence 和 clamp policy 可通过 monkeypatched ODE solver 在 core-only tests 中证明；确认 `compute_mask=False` 节点不会产生状态或 volume derivative；确认 correctness-freeze audit 可输出 `passed`，0 hard violations，0 open gaps。
- 本次采用的关键假设：本阶段只冻结当前行为，不声明当前互斥分支是最终 mixed ASM/UDM 设计；不改变 runtime 行为、合同、worker invocation、legacy backend implementation、OpenAPI/generated clients、Go Compute API、Desktop scope 或热路径性能逻辑。
- 剩余不确定性：backend thin-shell planning/migration、worker/default strict mode、最终 mixed ASM/UDM 组合语义、worker benchmark/timings harness、hosted green evidence、complete golden scenarios、full issuer/JWKS/RBAC、complete object data-scope 和 all-mutation audit 仍是未来工作。
- 影响范围 / 风险控制方式：只增加 tests、只读 audit、default gate evidence 和长期文档/ADR；不改 calculator 行为；audit 明确该冻结是 pre-performance current-state baseline，未来语义改动必须同步更新测试、audit 和 ADR。
- 验证方式：已通过 `backend\.venv\Scripts\python -m pytest simulation_core\tests\test_material_balance_core_boundary.py -q`（14 passed）、`backend\.venv\Scripts\python -m pytest simulation_core\tests -q`（21 passed）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-simulation-core-boundary.ps1`（`passed`，0 hard violations，0 open gaps）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-simulation-core-input-contract.ps1`（`passed`，0 hard violations，0 open gaps）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-simulation-core-correctness-freeze.ps1`（`passed`，0 hard violations，0 open gaps）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1`（`status=passed`，9 steps，`compute_boundary_audit.status=passed`，`simulation_core_correctness_freeze_audit.status=passed`）和 `git diff --check -- .`（仅 LF-to-CRLF notices，无 whitespace error）。
- 是否更新 README：是，更新 root `README.md`、`scripts/README.md`、`scripts/ci/README.md`、`simulation_core/README.md`、`simulation_core/tests/README.md` 和 material_balance README，并更新 architecture/rebuild 长期状态。
- 是否需要新增或更新架构决策（.ai/decisions/）：是，新增 `0014-simulation-core-correctness-freeze-before-perf.md`。

## 09:52 - Simulation core runtime extra policy

- 用户目标：继续按 Certainty/Elegance PRD/Plan 和最新优雅度优先级收口非 Desktop 剩余工作；在 P3 input-contract audit 只剩 runtime model `extra` 策略后，闭合 `simulation-core-runtime-models-extra-allow`，而不是继续低收益 service-test split、wrapper、hosted entry 或启动热路径性能优化。
- 涉及目录：`simulation_core/`、`simulation_core/tests/`、`scripts/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`、`.ai/decisions/`、`.ai/changes/`。
- 修改内容：`NodeData` 与 `EdgeData` runtime models 从允许未知字段改为 `extra=forbid`；新增 focused test 证明 direct runtime unknown fields 会抛出 `ValidationError`，同时 adapter 默认 `compat` 仍在构造 runtime model 前丢弃未知 payload 字段；input-contract audit 增加 direct runtime rejection probe 判定，避免只靠文本匹配 `extra="allow"`；新增 ADR `0013-simulation-core-runtime-extra-policy.md` 并在 ADR `0012` 中记录 follow-up；同步 simulation_core README、architecture current-state、Certainty/Elegance Development Plan 和 `tasks/todo.md`。
- 修改原因：`simulation_input.v1` schema closure 和 adapter warn/strict 已经让 worker-executable payload 的未知字段策略可验证，但直接构造 `NodeData` / `EdgeData` 仍可能把 typo 或漂移字段留在 Pydantic `model_extra`。兼容策略应停留在 adapter 边界，calculator runtime DTO 不应继续吞 unknown fields。
- 本次已消除的不确定性：确认 adapter 默认兼容路径在构造 runtime model 前已经只传递已知字段；确认 `position` 是 runtime-only 字段，不是 canonical `simulation_input.v1` node field；确认 input-contract audit 可从 `partial` 变为 `passed`，前提是 schema closure、adapter warn/strict 和 runtime direct rejection 都 green。
- 本次采用的关键假设：本阶段不改变 worker 默认 adapter validation mode、legacy backend models、schema requiredness、OpenAPI/generated clients、Go Compute API、Desktop scope、backend thin-shell 状态、mixed-model correctness 或热路径性能行为；未知 payload 字段的迁移兼容继续由 adapter `compat` / `warn` / `strict` 模式承担。
- 剩余不确定性：worker/default strict mode 何时切换仍需后续兼容计划；backend thin-shell、mixed-model correctness freeze、worker benchmark/timings harness、hosted green evidence、complete golden scenarios、full issuer/JWKS/RBAC、complete object data-scope 和 all-mutation audit 仍是未来工作。
- 影响范围 / 风险控制方式：只改变 simulation_core direct runtime model unknown-field behavior，并保持 worker/default adapter behavior 不变；用 focused tests 和 audit probe 同时证明 compatibility 与 direct rejection；长期策略写入 ADR，避免未来把 runtime model extra policy 当作未决兼容豁免。
- 验证方式：已通过 `backend\.venv\Scripts\python -m pytest simulation_core\tests\test_material_balance_core_boundary.py -q`（8 passed）、`backend\.venv\Scripts\python -m pytest simulation_core\tests -q`（15 passed）、`backend\.venv\Scripts\python -m pytest services\simulation-worker\tests -q`（25 passed）、`backend\.venv\Scripts\python services\simulation-worker\simulation_worker\cli.py --run-job contracts\examples\valid\material_balance_minimal.compute_job.v1.json --artifact-dir tmp\worker-artifacts-runtime-extra`（status `succeeded`）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-simulation-core-input-contract.ps1`（`passed`，0 hard violations，0 open gaps）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-simulation-core-boundary.ps1`（`passed`，0 hard violations，0 open gaps）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-contracts.ps1`（101 passed，contract gate passed）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1`（`status=passed`，8 steps，`compute_boundary_audit.status=passed`）和 `git diff --check -- .`（仅 LF-to-CRLF notices，无 whitespace error）。
- 是否更新 README：是，更新 `simulation_core/README.md`、`simulation_core/python/README.md`、`simulation_core/python/autowatersimu_simulation_core/README.md` 和 `simulation_core/python/autowatersimu_simulation_core/material_balance/README.md`，并更新 architecture/rebuild 长期状态。
- 是否需要新增或更新架构决策（.ai/decisions/）：是，新增 `0013-simulation-core-runtime-extra-policy.md`，并更新 `0012-simulation-input-node-edge-schema-closure.md` follow-up。

## 09:42 - Simulation input node-edge schema closure

- 用户目标：继续按 Certainty/Elegance PRD/Plan 和最新优雅度优先级收口非 Desktop 剩余工作；在确认 P0 service-test closeout、P1 `pr-fast` boundary gate 和 P2 selected mutation matrix 已落地后，推进 P3 input-contract audit 中的 node/edge schema gap，而不是继续低收益测试拆分、wrapper、hosted evidence entry 或热路径性能优化。
- 涉及目录：`contracts/`、`simulation_core/`、`scripts/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`、`.ai/decisions/`、`.ai/changes/`。
- 修改内容：`simulation_input.v1` 新增 node/edge item 显式 canonical snake_case 字段 schema，并关闭 item 级 unknown fields；保留组件浓度、concentration transform、UDM process、UDM 参数与 model snapshot 的动态内部 map；新增 invalid fixture `unknown_node_edge_field.simulation_input.v1.json` 并登记到 registry；更新 input-contract audit 后续建议、contracts/simulation_core/architecture/rebuild 文档、`tasks/todo.md`；新增 ADR `0012-simulation-input-node-edge-schema-closure.md`。
- 修改原因：此前 `simulation_input.v1` 顶层已关闭 unknown fields，但 `nodes[]` / `edges[]` 仍是开放 object，导致 typo、camelCase drift 或 adapter 漏映射无法在 worker schema validation 前暴露。关闭 node/edge item unknown fields 能把 P3 input-contract audit 从 2 个 open gaps 收敛到 runtime `extra` 单项。
- 本次已消除的不确定性：确认当前 valid simulation input fixtures 已使用 canonical snake_case 字段；确认 `process_graph_to_simulation_input` 不把 UI-only `position` 写入 `simulation_input.v1`；确认 adapter 的 camelCase 支持属于直接调用兼容路径，不应扩展为合同字段；确认 audit 当前为 `partial`，0 hard violations、1 open gap：`simulation-core-runtime-models-extra-allow`。
- 本次采用的关键假设：本阶段不新增 node/edge item required 字段；不改 `NodeData`/`EdgeData extra="allow"`、worker 默认 `compat` adapter 调用、legacy backend adapter、Go API/OpenAPI/generated clients、Desktop scope、backend thin-shell 状态或热路径性能行为；动态模型内部对象仍需未来模型级合同单独收窄。
- 剩余不确定性：`NodeData`/`EdgeData` runtime `extra="allow"` 是记录为兼容豁免还是替换为 forbid 仍未决；backend thin-shell、mixed model correctness freeze、worker benchmark/timings harness、hosted green evidence、complete golden scenarios、full issuer/JWKS/RBAC、complete object data-scope 和 all-mutation audit 仍是未来工作。
- 影响范围 / 风险控制方式：只改 worker-executable contract、fixture/registry、只读 audit recommendation 和长期文档；不改变 runtime adapter 默认行为；新增 ADR 明确 canonical snake_case 与动态内部 map 边界，避免未来把 camelCase 兼容误当合同事实。
- 验证方式：已通过 `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-simulation-core-input-contract.ps1`（`partial`，0 hard violations，1 open gap）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-contracts.ps1`（101 passed，contract gate passed）、`backend\.venv\Scripts\python -m pytest simulation_core\tests -q`（14 passed）、`backend\.venv\Scripts\python services\simulation-worker\simulation_worker\cli.py --run-job contracts\examples\valid\material_balance_minimal.compute_job.v1.json --artifact-dir tmp\worker-artifacts-schema-closure`（status `succeeded`）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-simulation-core-boundary.ps1`（`passed`，0 hard violations，0 open gaps）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1`（`status=passed`，8 steps，`compute_boundary_audit.status=passed`）和 `git diff --check -- .`（仅 LF-to-CRLF notices，无 whitespace error）。
- 是否更新 README：是，更新 `contracts/README.md`、`simulation_core/README.md`、`simulation_core/python/README.md` 和 adapter README，并更新 architecture/rebuild 长期状态。
- 是否需要新增或更新架构决策（.ai/decisions/）：是，新增 `0012-simulation-input-node-edge-schema-closure.md`。

## 09:24 - Simulation core adapter unknown-field mode

- 用户目标：继续按 Certainty/Elegance PRD/Plan 收口非 Desktop 剩余工作；在 input-contract audit 已证明 adapter 未知字段策略缺口后，优先落地 `simulation_core` adapter opt-in warn/strict 模式，而不是启动 schema strict、runtime extra 移除、worker 默认 strict 或热路径性能优化。
- 涉及目录：`simulation_core/`、`simulation_core/tests/`、`scripts/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`、`.ai/changes/`。
- 修改内容：`simulation_input_to_material_balance_input` 新增 `validation_mode="compat" | "warn" | "strict"`；`MaterialBalanceInput` 新增 `contract_warnings`；core-only adapter tests 覆盖默认兼容静默忽略、warn 结构化告警和 strict 拒绝；`scripts/audit-simulation-core-input-contract.ps1` 改为探测 compat/warn/strict，并仅在 warn 和 strict 均被证明时关闭 adapter strategy gap；同步 simulation_core / adapter / scripts README、architecture current-state、Certainty/Elegance Development Plan 与 `tasks/todo.md`。
- 修改原因：直接切 schema/runtime strict 仍会影响兼容路径；先提供 opt-in warn/strict 可以把未知字段策略从“静默缺口”收口为可验证能力，同时保持 worker 默认行为稳定。
- 本次已消除的不确定性：确认 adapter 默认调用仍接受未知字段且不暴露 warnings；确认 `warn` 模式会对未知 top-level/node/edge 字段写入 `contract_warnings`；确认 `strict` 模式会在计算前抛出 `SimulationCoreAdapterError`；确认 input-contract audit 从 3 个 open gaps 降到 2 个。
- 本次采用的关键假设：本阶段不修改 `contracts/simulation_input.v1.json`、`NodeData`/`EdgeData` `extra="allow"`、worker 调用模式、legacy backend adapter、OpenAPI、generated clients、Go API 或 Desktop scope；顶层 schema 已知字段如 `runtime_options` / `metadata` 不作为 adapter unknown field 告警。
- 剩余不确定性：`simulation_input.v1` node/edge item 是否继续开放仍未决；`NodeData`/`EdgeData` runtime `extra="allow"` 是记录为兼容豁免还是改为 forbid 仍未决；backend thin-shell、mixed model correctness freeze、worker benchmark/timings harness、hosted green evidence、complete golden scenarios、full issuer/JWKS/RBAC、complete object data-scope 和 all-mutation audit 仍是未来工作。
- 影响范围 / 风险控制方式：默认 `compat` 保持既有 behavior；warn/strict 只通过显式参数启用；不触碰未跟踪文档；audit 默认仍只对 hard violation 失败，开放缺口保持显式 `partial`。
- 验证方式：已通过 `backend\.venv\Scripts\python -m pytest simulation_core\tests\test_material_balance_core_boundary.py -q`（7 passed）、`backend\.venv\Scripts\python -m pytest simulation_core\tests -q`（14 passed）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-simulation-core-input-contract.ps1`（`partial`，0 hard violations，2 open gaps）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-simulation-core-boundary.ps1`（`passed`，0 hard violations，0 open gaps）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1`、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1`（`status=passed`，8 steps，`compute_boundary_audit.status=passed`）和 `git diff --check -- .`（仅 LF-to-CRLF notices，无 whitespace error）。
- 是否更新 README：是，更新 `simulation_core/README.md`、`simulation_core/python/README.md`、`simulation_core/python/autowatersimu_simulation_core/adapters/README.md` 和 `scripts/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次是 adapter 输入契约模式实现和审计收口，不新增长期架构决策。

## 09:17 - Simulation core input contract audit

- 用户目标：继续按 Certainty/Elegance PRD/Plan 收口非 Desktop 剩余工作；在 P3 boundary audit 已 green 后，推进 `simulation-core-input-contract-audit`，先形成可复跑证据，而不是直接切 strict 或启动热路径性能优化。
- 涉及目录：`scripts/`、`simulation_core/`、`docs/architecture/`、`docs/rebuild/`、根 README/Justfile、`tasks/`、`.ai/changes/`。
- 修改内容：新增 `scripts/audit-simulation-core-input-contract.ps1`，输出 `tmp/architecture-evidence/simulation-core-input-contract.json`，检查 `simulation_input.v1` 顶层与 node/edge item schema 闭合度、`NodeData`/`EdgeData` runtime `extra` 策略、adapter 对未知字段的接受/保留/丢弃/告警行为；新增 `just audit-simulation-core-input-contract`；同步 root README、local-dev、scripts README、simulation_core README、adapter README、architecture current-state、Certainty/Elegance Development Plan 与 `tasks/todo.md`。
- 修改原因：当前性能优化前置要求先处理输入契约确定性。直接把 runtime model 改成 strict 风险较高，且会破坏兼容行为；先用只读 audit 固定现状和缺口，可以让下一步 warn/strict 设计有明确 evidence。
- 本次已消除的不确定性：确认 `simulation_input.v1` 顶层 `additionalProperties=false`；确认 node/edge item 仍是开放 object；确认 `NodeData` 与 `EdgeData` 仍 `extra="allow"`；确认 adapter 当前接受未知 top-level/node/edge 字段，未暴露 warnings，并静默丢弃未知 node/edge 字段；确认 input-contract audit 当前为 `partial`，0 hard violations、3 open gaps。
- 本次采用的关键假设：本阶段只做审计、入口和文档，不改变 `simulation_input.v1` schema、runtime Pydantic model 行为、adapter 行为、worker 行为、OpenAPI、generated clients、Desktop scope 或 Go Compute API；`partial` 是预期证据状态，不等同于失败。
- 剩余不确定性：需要设计 adapter 未知字段 warn/strict 模式；需要决定 `simulation_input.v1` node/edge item 是否继续开放或逐步 typed；需要在改变 runtime model `extra` 策略前保持 backend/core parity drift guard green；backend thin-shell、mixed model correctness freeze、worker benchmark/timings harness、hosted green evidence、complete golden scenarios、full issuer/JWKS/RBAC、complete object data-scope 和 all-mutation audit 仍是未来工作。
- 影响范围 / 风险控制方式：只新增只读审计脚本和 Justfile 入口；脚本默认仅对 probe/hard violation 失败，开放缺口可用 `-FailOnOpenGaps` 升级；不触碰未跟踪文档。
- 验证方式：已通过 `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-simulation-core-input-contract.ps1`（`partial`，0 hard violations，3 open gaps）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-simulation-core-boundary.ps1`（`passed`，0 hard violations，0 open gaps）、`backend\.venv\Scripts\python -m pytest simulation_core\tests -q`（11 passed）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1`、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1`（`status=passed`，8 steps，`compute_boundary_audit.status=passed`）、`rg -n "schema_version|P0|P1|P2|simulation-core-input-contract" docs\rebuild --glob "AutoWaterSimu_Next_*.md"`、`git diff --check -- .`（仅 LF-to-CRLF notices，无 whitespace error）。`just audit-simulation-core-input-contract` 未运行成功，因为当前环境未安装 `just`，已用底层 PowerShell 入口验证。
- 是否更新 README：是，更新 root `README.md`、`scripts/README.md`、`simulation_core/README.md`、`simulation_core/python/README.md`、`simulation_core/python/autowatersimu_simulation_core/adapters/README.md` 与 `docs/architecture/local-dev.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次是输入契约审计基线，不新增长期架构决策。

## 09:04 - P3 backend core drift guard

- 用户目标：继续按 Certainty/Elegance PRD/Plan 收口非 Desktop 剩余工作；在 P3 audit 只剩 backend/core 双实现漂移风险后，先补可审计的 parity drift guard，而不是直接做 backend thin-shell 或热路径性能优化。
- 涉及目录：`simulation_core/`、`simulation_core/tests/`、`scripts/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`、`.ai/changes/`。
- 修改内容：`simulation_core/tests/test_material_balance_core.py` 新增 `BACKEND_CORE_DRIFT_GUARD_CASES` 与 manifest 完整性测试，覆盖 material-balance minimal、ASM1Slim model-bound、独立 ASM1Slim/ASM1/ASM3/UDM；backend/core parity 断言从单个最终组件值扩展为状态、总步数、时间戳、节点字段和数值序列比较；`scripts/audit-simulation-core-boundary.ps1` 改为只有 backend thin-shell-like 或显式 drift guard 覆盖完整时才关闭 backend/core 双实现风险；同步 simulation_core/script README、architecture current-state、Certainty/Elegance Development Plan 与 `tasks/todo.md`。
- 修改原因：legacy backend 与 `simulation_core` 在 backend thin-shell 迁移前仍保留重复 material-balance runtime 文件。若没有可复跑、可审计的 parity guard，后续 input contract、thin-shell 或性能优化会放大双实现漂移风险。
- 本次已消除的不确定性：确认现有 required fixtures 能同时跑 legacy backend 和 `simulation_core`；确认状态、步数、时间戳、节点字段和完整数值序列在 required guard cases 下保持一致；确认 P3 audit 能识别 explicit drift guard 并从 `partial` 变为 `passed`，0 hard violations、0 open gaps。
- 本次采用的关键假设：本阶段只改测试、只读 audit 和文档，不改变 backend 或 `simulation_core` runtime behavior，不修改 schema、OpenAPI、generated client、worker runtime、Desktop scope 或 Go Compute API；explicit drift guard 是 backend thin-shell 前的冻结策略，不代表 backend thin-shell 已完成。
- 剩余不确定性：backend thin-shell 迁移、`simulation-core-input-contract-audit`、mixed model correctness freeze、worker benchmark/timings harness、hosted green evidence、legacy authenticated session、complete golden scenarios、full issuer/JWKS/RBAC、complete object data-scope 和 all-mutation audit 仍是未来工作。
- 影响范围 / 风险控制方式：只增强 parity tests 和 audit 判定；audit evidence 明确输出 `drift_guard` required/missing cases；文档保留 duplicate implementation 状态，避免误报为 thin-shell；不触碰未跟踪文档。
- 验证方式：已通过 `backend\.venv\Scripts\python -m pytest simulation_core\tests\test_material_balance_core.py -q`（7 passed）、`backend\.venv\Scripts\python -m pytest simulation_core\tests -q`（11 passed）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-simulation-core-boundary.ps1`（`passed`，0 hard violations，0 open gaps）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1`、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1`（`status=passed`，8 steps，`compute_boundary_audit.status=passed`）、`git diff --check -- .`（仅 LF-to-CRLF notices，无 whitespace error）。
- 是否更新 README：是，更新 `simulation_core/README.md`、`simulation_core/tests/README.md` 和 `scripts/README.md`，记录 backend/core drift guard 与 P3 audit 判定。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次是 thin-shell 前的可审计冻结策略，不新增长期架构决策。

## 08:46 - P3 worker backend oracle test isolation

- 用户目标：继续按 Certainty/Elegance PRD/Plan 收口非 Desktop 剩余工作；在 worker installed-package import preference 与 deprecated fallback 策略落地后，拆分 worker CLI/API bridge tests 中的 backend parity/oracle helper，使 runtime boundary tests 不再和 old-vs-worker 数值基线混在同一 collect 单元。
- 涉及目录：`services/simulation-worker/tests/`、`services/simulation-worker/`、`scripts/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`、`.ai/changes/`。
- 修改内容：将 `test_worker_cli.py` 中的 legacy backend path injection、`app.*` imports、old-vs-worker helper 和 backend oracle baseline tests 移到新增 `test_worker_backend_oracle.py`；`test_worker_cli.py` 只保留 worker runtime/CLI/JSON-RPC/API bridge、artifact、model_run 和 dependency-boundary coverage；`scripts/audit-simulation-core-boundary.ps1` 改为只允许 dedicated `test_worker_backend_oracle.py` 出现 backend oracle imports，其他 worker test 文件若静态加入 backend 仍记为 open gap；同步 worker tests README、worker README、scripts README、architecture current-state、Certainty/Elegance Development Plan 与 `tasks/todo.md`。
- 修改原因：上一阶段 P3 audit 已把 worker runtime import fallback 收口，但 `services/simulation-worker/tests/test_worker_cli.py` 仍在模块顶层加入 `backend/app` 并 import legacy `app.*` oracle，导致 runtime/CLI/API bridge tests collect 时也带上 backend oracle 依赖。拆分后能证明 worker boundary tests 可独立 collect，同时保留旧后端数值基线对照。
- 本次已消除的不确定性：确认 runtime/CLI/API bridge tests 不再静态加入 legacy backend；确认 old-vs-worker oracle coverage 仍保留且通过；确认 P3 audit 能识别 backend oracle imports 是否仅在 dedicated 文件中；确认当前 P3 audit open gaps 降为单项 `backend-core-dual-implementation-drift-risk`。
- 本次采用的关键假设：本阶段只拆分测试文件组织和审计规则，不改变 worker runtime behavior、CLI/JSON-RPC/API 输出、fixture 内容、数值 oracle 断言、schema、OpenAPI、generated clients、backend material-balance implementation 或 Desktop scope；`test_worker_backend_oracle.py` 是本目录唯一允许静态加入 `backend/app` 的 worker test 文件。
- 剩余不确定性：legacy backend 与 simulation_core 仍有双实现漂移风险；backend thin-shell 迁移前的 golden/parity 冻结策略仍待实现；hosted green evidence、legacy authenticated session、complete golden scenarios、full issuer/JWKS/RBAC、complete object data-scope 和 all-mutation audit 仍是未来工作。
- 影响范围 / 风险控制方式：只移动测试 helper/tests 并调整只读 audit；保留所有 25 个 worker tests 通过；新增测试文件因根 `.gitignore` 忽略 `test_*.py`，提交时必须用 `git add -f` 纳入跟踪；不触碰用户未跟踪文档。
- 验证方式：已通过 `backend\.venv\Scripts\python -m pytest services\simulation-worker\tests\test_worker_cli.py -q`（16 passed）、`backend\.venv\Scripts\python -m pytest services\simulation-worker\tests\test_worker_backend_oracle.py -q`（9 passed）、`backend\.venv\Scripts\python -m pytest services\simulation-worker\tests -q`（25 passed）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-simulation-core-boundary.ps1`（`partial`，0 hard violations，1 open gap：`backend-core-dual-implementation-drift-risk`）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1`、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1`（`status=passed`，8 steps，`compute_boundary_audit.status=passed`）、`git diff --check -- .`（仅 LF-to-CRLF notices，无 whitespace error）。
- 是否更新 README：是，更新 `services/simulation-worker/README.md`、`services/simulation-worker/tests/README.md` 和 `scripts/README.md`，记录 runtime tests 与 backend oracle tests 的隔离规则。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次是既有 P3 test boundary 收口，不新增长期架构决策。

## 08:35 - P3 worker installed-package import preference

- 用户目标：继续按 Certainty/Elegance PRD/Plan 收口非 Desktop 剩余工作；在 simulation_core/contracts packaging metadata 已落地后，处理 worker repo-path fallback 缺口，不再推进低收益 wrapper、普通 service-test split 或热路径性能优化。
- 涉及目录：`services/simulation-worker/`、`scripts/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`、`.ai/changes/`。
- 修改内容：`services/simulation-worker/simulation_worker/runner.py` 新增 installed-package-first dependency import gate，优先 import `autowatersimu_simulation_core` / `autowatersimu_contracts`，只有缺包时才调用 `_ensure_deprecated_repo_import_paths()`；`self_check()` 新增 `worker_dependency_imports` 结构化状态，避免缺包时破坏 JSON 输出；新增 worker focused tests 覆盖 installed-package path、deprecated fallback path 和 fallback 后仍缺包的失败路径；`scripts/audit-simulation-core-boundary.ps1` 改为区分 unsafe repo-path fallback 与 gated deprecated fallback；同步 worker/script README、architecture current-state、Certainty/Elegance Development Plan 与 `tasks/todo.md`。
- 修改原因：上一阶段 P3 audit 仍把 worker `_ensure_repo_import_paths()` / `sys.path` fallback 作为 open gap。当前 helper packages 已可 editable 安装，worker runtime 应先依赖正式 package import，只把 repo path mutation 留作 source-mode / packaged sidecar 兼容后备，并让 audit 防止无条件 fallback 回归。
- 本次已消除的不确定性：确认 worker 正常路径不需要无条件修改 `sys.path`；确认 deprecated fallback 只在 missing installed package import 后触发；确认 P3 audit 能识别 unsafe fallback/direct `sys.path` mutation 并接受 gated deprecated fallback；确认当前 P3 audit open gaps 降为 backend/core 双实现漂移风险和 worker tests backend oracle 依赖。
- 本次采用的关键假设：本阶段不删除 compatibility fallback，以避免破坏尚未完全验证的 source-mode / packaged sidecar 资源布局；不改变 worker CLI/JSON-RPC/API 既有字段、schema、OpenAPI、generated clients、backend material-balance implementation 或 Desktop packaging；`self_check()` 可以新增依赖状态字段用于诊断；`runtime_audit.fallback_used` 仍保留给模型执行 fallback，不复用来表示 import-path compatibility fallback。
- 剩余不确定性：legacy backend 与 simulation_core 仍有双实现漂移风险；worker tests 仍在同一 collect 单元混合 backend oracle 与 CLI/API bridge coverage；backend thin-shell 迁移前的 golden/parity 冻结策略仍待实现；hosted green evidence、legacy authenticated session、complete golden scenarios、full issuer/JWKS/RBAC、complete object data-scope 和 all-mutation audit 仍是未来工作。
- 影响范围 / 风险控制方式：只改 worker import boundary 和只读 audit 判定；保留 deprecated fallback；focused tests 直接覆盖 fallback gate；P3 audit 默认仍只对 hard runtime violation 失败，开放缺口保持显式 `partial`。
- 验证方式：已通过 `backend\.venv\Scripts\python -m pytest services\simulation-worker\tests\test_worker_cli.py -k "dependency_imports" -q`（3 passed，22 deselected）、`backend\.venv\Scripts\python -m pytest services\simulation-worker\tests -q`（25 passed）、`backend\.venv\Scripts\python services\simulation-worker\simulation_worker\cli.py --self-check`、`backend\.venv\Scripts\python services\simulation-worker\simulation_worker\cli.py --run-job contracts\examples\valid\material_balance_minimal.compute_job.v1.json --artifact-dir tmp\worker-artifacts`、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-simulation-core-boundary.ps1`（`partial`，0 hard violations，2 open gaps）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1`、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1`（`status=passed`，8 steps，`compute_boundary_audit.status=passed`）、`git diff --check -- .`（仅 LF-to-CRLF notices，无 whitespace error）。
- 是否更新 README：是，更新 `services/simulation-worker/README.md`、`services/simulation-worker/simulation_worker/README.md` 和 `scripts/README.md`，记录 installed-package-first 和 deprecated gated fallback 规则。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次是既有 P3 packaging/import 边界收口，不新增长期架构决策。

## 08:20 - P3 simulation_core pyproject and core-only tests

- 用户目标：继续按 Certainty/Elegance PRD/Plan 收口非 Desktop 剩余工作；在 P3 boundary audit 已建立后，闭合 `simulation_core/python` / `contracts/python` packaging metadata 与 simulation_core core-only test collect 缺口，暂不启动热路径性能优化。
- 涉及目录：`simulation_core/python/`、`simulation_core/tests/`、`contracts/python/`、`scripts/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`、`.ai/changes/`。
- 修改内容：新增 `simulation_core/python/pyproject.toml`（package `autowatersimu-simulation-core`）和 `contracts/python/pyproject.toml`（package `autowatersimu-contracts`）；将 `autowatersimu_simulation_core.__version__` 规范为 PEP 440 兼容的 `0.1.0+phase2b`；新增 `simulation_core/tests/test_material_balance_core_boundary.py` 承担 core-only import boundary 与 adapter 行为测试，保留 `test_material_balance_core.py` 作为 backend parity/oracle 测试；调整 `scripts/audit-simulation-core-boundary.ps1`，允许 backend parity/oracle 测试存在，但要求至少有独立 core-only test file；同步相关 README、architecture current-state、Certainty/Elegance Development Plan 与 `tasks/todo.md`。
- 修改原因：上一阶段 P3 audit 显示 packaging metadata 与 core-only tests collect 是性能 Phase 0 前置缺口。若不先让 Python helper 可安装、并把 core-only 测试从 backend oracle collect 中拆出，后续删除 worker `sys.path` fallback、backend thin-shell 或性能优化都会缺少可复跑边界证据。
- 本次已消除的不确定性：确认 `autowatersimu-simulation-core` 与 `autowatersimu-contracts` 都可通过 `uv pip install --python backend\.venv\Scripts\python.exe -e <path> --no-deps` editable 安装；确认安装后 `importlib.metadata.version(...)` 与 `autowatersimu_simulation_core.__version__` 均为 `0.1.0+phase2b`；确认 `simulation_core/tests/test_material_balance_core_boundary.py` 可作为不导入 legacy backend 的 core-only test file 被 P3 audit 识别。
- 本次采用的关键假设：本阶段只增加 packaging metadata、版本规范化和测试组织边界，不移除 worker `_ensure_repo_import_paths()`，不改变 worker runtime behavior、backend material-balance implementation、合同 schema、OpenAPI、generated clients 或 Desktop scope；PEP 440 兼容版本 `0.1.0+phase2b` 是对原 `0.1.0-phase2b` 的 packaging 必要规范化，不表达新的功能版本。
- 剩余不确定性：worker repo-path fallback 仍未退场；legacy backend 与 simulation_core 仍有双实现漂移风险；worker tests 仍在同一 collect 单元混合 backend oracle 与 worker CLI/API bridge coverage；backend thin-shell 迁移前的 golden/parity 冻结策略仍待实现；hosted green evidence、legacy authenticated session、complete golden scenarios、full issuer/JWKS/RBAC、complete object data-scope 和 all-mutation audit 仍是未来工作。
- 影响范围 / 风险控制方式：只新增 pyproject 元数据和 core-only test file，移动已有 boundary/adapter 测试不改断言语义；保留 backend parity/oracle tests 原数值对照；P3 audit 仍默认只对 runtime hard violation 失败，开放缺口保持显式 `partial`。
- 验证方式：已通过 `uv pip install --python backend\.venv\Scripts\python.exe -e simulation_core\python --no-deps`、`uv pip install --python backend\.venv\Scripts\python.exe -e contracts\python --no-deps`、installed package version import smoke、`backend\.venv\Scripts\python -m pytest simulation_core\tests -q`（10 passed）、`backend\.venv\Scripts\python -m pytest services\simulation-worker\tests -q`（22 passed）、`backend\.venv\Scripts\python -m pytest contracts\tests -q`（100 passed）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-simulation-core-boundary.ps1`（`partial`，0 hard violations，3 open gaps）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1`、`rg -n "schema_version|P0|P1|P2" docs\rebuild --glob "AutoWaterSimu_Next_*.md"`、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1`（`status=passed`，8 steps，`compute_boundary_audit.status=passed`）、`git diff --check -- .`。
- 是否更新 README：是，更新 `contracts/python/README.md`、`simulation_core/python/README.md` 和 `simulation_core/tests/README.md`，记录新 packaging 入口与测试文件分工。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次是 P3 前置 packaging/test-boundary 实施，不改变长期架构决策。

## 08:09 - P3 simulation_core boundary audit

- 用户目标：根据 Certainty/Elegance PRD/Plan 与新的优雅度重构优先级，停止继续推进低收益 wrapper、hosted entry、普通 service-test split 或热路径性能优化，先启动 P3 simulation_core / worker / legacy backend packaging 与边界审计。
- 涉及目录：`scripts/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`、`.ai/changes/`；只读检查涉及 `simulation_core/`、`contracts/python/`、`services/simulation-worker/` 与 `backend/app/material_balance/`。
- 修改内容：新增 `scripts/audit-simulation-core-boundary.ps1`，输出 `tmp/architecture-evidence/simulation-core-boundary.json`，检查 `simulation_core/python` 与 `contracts/python` packaging metadata、worker `_ensure_repo_import_paths()` / `sys.path` fallback、worker/core runtime legacy `app.*` imports、backend/core 双实现漂移风险、simulation_core / worker tests 的 backend oracle 依赖和 core-only import smoke；同步 `scripts/README.md`、`docs/architecture/current-state.md`、Certainty/Elegance Development Plan 与 `tasks/todo.md`。
- 修改原因：P0 service-test split 已结构性收口，P1 Compute boundary audit 已进入默认 `pr-fast`，P2 mutation/data-scope matrix red/yellow rows 已闭合；性能优化前最大的剩余阻塞是 simulation_core packaging、worker import path fallback、backend/core drift 和 tests collect 边界，需要先形成可复跑 evidence，而不是直接做 UDM RHS 去循环或 dense/sparse 重写。
- 本次已消除的不确定性：确认当前 worker/core runtime 没有直接 import legacy `app.*` 的硬违规；确认 core-only import smoke 在仅设置 `PYTHONPATH=simulation_core/python` 时通过；确认当前开放缺口是 missing `simulation_core/python` packaging metadata、missing `contracts/python` packaging metadata、worker repo-path fallback、backend/core 双实现漂移、simulation_core tests backend oracle collect 依赖、worker tests backend oracle 依赖。
- 本次采用的关键假设：本阶段只做 read-only audit 与长期状态记录，不改变 runtime behavior、worker import behavior、backend material-balance implementation、Python package contents、tests、schemas、OpenAPI、generated clients、CI default gates 或 Desktop scope；开放缺口暂不让脚本默认失败，但可用 `-FailOnOpenGaps` 在后续 gate 阶段升级；未跟踪的 `docs/rebuild/AutoWaterSimu_95分优雅度完整计划.md` 与 `docs/rebuild/simulation_core/` 属于用户/本地文件，不纳入本次修改。
- 剩余不确定性：最小 pyproject/editable dependency 的具体依赖区间、`contracts/python` 是否独立打包还是先保留 worker fallback、core-only tests 与 backend parity/oracle tests 的拆分粒度、backend thin-shell 迁移前 golden/parity 策略仍待下一阶段实现；hosted green evidence、legacy authenticated session、complete golden scenarios、full issuer/JWKS/RBAC、complete object data-scope 和 all-mutation audit 仍是未来工作。
- 影响范围 / 风险控制方式：新增脚本默认只在 runtime hard violation 时失败，避免把已知 packaging/test 缺口误当作当前门禁失败；文档明确下一步是 pyproject/core-only test split，不启动热路径性能优化；不触碰用户未跟踪文档。
- 验证方式：已通过 `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-simulation-core-boundary.ps1`（`status=partial`，0 hard violations，6 open gaps，core-only import smoke passed）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1`、`git diff --check -- .`、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1`（`status=passed`，8 steps，`compute_boundary_audit.status=passed`）。
- 是否更新 README：是，更新 `scripts/README.md`，登记新的 P3 boundary audit 入口与验证命令。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次是审计基线与后续优先级收口，不改变长期架构决策。

## 07:55 - P2 mutation matrix red/yellow closeout

- 用户目标：沿 Certainty/Elegance PRD/Plan 继续完成非 Desktop 剩余工作；在 P2 mutation/data-scope gap matrix 已生成后，闭合其中 red/yellow rows，而不是继续做低收益 service-test split、wrapper 或 hosted workflow entry。
- 涉及目录：`apps/api/internal/compute/`、`scripts/ci/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`、`.ai/changes/`。
- 修改内容：新增 `TestHTTPJobCollectionRequiresDeclaredMethods`，补 `POST /api/v1/compute/jobs` collection method/no-write 证明；扩展 `TestSimulationCheckEndpointCreatesComputeJob`，断言 direct simulation-check create/queue route-specific audit；新增 HTTP scoped archive cross-scope no-write test；将 job collection、draft promotion、model catalog status/registration、archive cross-scope 与 direct simulation-check audit 纳入 `security-smoke.ps1`；同步 P2 matrix、current-state、scripts/ci README、Certainty/Elegance Development Plan 与 `tasks/todo.md`。
- 修改原因：上一轮矩阵证明 selected mutation slice 覆盖面已广，但 `security-smoke` 选集与部分 route-specific audit 证明未完全对齐，job collection method guard 缺少专名 no-write smoke，artifact archive cross-scope no-write 仍依赖候选过滤推断。闭合这些 red/yellow rows 后，下一步才能转向 P3 simulation_core / worker / legacy backend boundary audit。
- 本次已消除的不确定性：确认 draft promotion 与 model catalog status/registration route-specific audit 断言已存在但未被 `security-smoke` 直接选择；确认 direct simulation-check 测试已在 smoke 中但缺 route-specific create/queue audit 断言；确认 artifact archive 需要 HTTP scoped archive backend 场景才能证明跨 scope candidate 不被归档。
- 本次采用的关键假设：本阶段只补 selected mutation evidence 和 smoke selection，不改变运行时行为、HTTP route、auth scope、schema、OpenAPI、migration、generated client、store interface 或 selected audit envelope shape；P2 matrix red/yellow closeout 不等同于 full issuer/JWKS/RBAC、complete object-level data scope、ontology-backed policy enforcement 或 all-mutation audit 完成；未跟踪的 `docs/rebuild/AutoWaterSimu_95分优雅度完整计划.md` 与 `docs/rebuild/simulation_core/` 属于用户/本地文件，不纳入本次修改。
- 剩余不确定性：full issuer/JWKS/RBAC、complete object-level tenant/project/site data scope、complete all-mutation audit、ontology-backed policy enforcement、P3 simulation_core/worker/backend boundary audit、hosted green evidence、legacy authenticated session 和 complete golden scenarios 仍未完成。
- 影响范围 / 风险控制方式：只新增 focused Go tests、调整 `security-smoke` test regex 与 evidence wording，并同步长期文档；不改生产 Go workflow 或 API behavior；focused tests 先验证新增断言，再跑 security/pr-fast gates。
- 验证方式：已通过 `go test ./internal/compute -run "Test(HTTPJobCollectionRequiresDeclaredMethods|SimulationCheckEndpointCreatesComputeJob|HTTPArtifactRetentionSweepArchiveTenantProjectSiteScope|DraftConfirmationPromotionEndpoint|ModelCatalogStatusTransitionEndpoint)$" -count=1`（在 `apps/api`）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\security-smoke.ps1`（`status=passed`，3 steps，coverage summary 含 `simulation_check`、`job_collection` 和 `artifact_retention_delete_archive_candidate_filtering`）、`go test ./...`（在 `apps/api`）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1`、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1`（0 import boundary violations）、`rg -n "schema_version|P0|P1|P2" docs\rebuild --glob "AutoWaterSimu_Next_*.md"`、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1`（`status=passed`，8 steps，`compute_boundary_audit.status=passed`）、changed-file trailing-whitespace scan 和 `git diff --check -- .`。
- 是否更新 README：是，更新 `scripts/ci/README.md`，记录 `security-smoke` 的 job collection、archive cross-scope 与 direct simulation-check selected audit coverage。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次是既有 selected mutation matrix 的证据闭合，不新增长期架构决策。

## 07:41 - P2 mutation scope gap matrix

- 用户目标：根据新的优雅度重构重新评估，停止继续做低收益 service-test split、wrapper 或 hosted workflow entry；先把 P2 data-scope / mutation audit / method guard 的剩余缺口做成逐 route 的 gap matrix。
- 涉及目录：`docs/architecture/`、`docs/rebuild/`、`tasks/`、`.ai/changes/`。
- 修改内容：新增 `docs/architecture/compute-mutation-scope-matrix.md`，逐 route 记录 method guard、auth scope、tenant/project/site mutation scope、no-write denial、audit envelope、security-smoke selection 和 focused test evidence；同步 `docs/architecture/README.md`、`docs/architecture/current-state.md`、Certainty/Elegance Development Plan 与 `tasks/todo.md`。
- 修改原因：P0 service-test split 已结构性收口，P1 Compute API boundary audit 已进入默认 `pr-fast` gate。继续零散补 wrapper / refresh /同包拆分会降低边际收益；P2 需要先把已覆盖与未覆盖的 mutation 安全证据显式化，再只修 red/yellow rows。
- 本次已消除的不确定性：确认 `security-smoke.ps1` 的实际 regex 覆盖 production token guard、scope denial、worker POST-only method guard、job/API-wide declared-method guard、selected scope/audit tests；确认 focused tests 覆盖 worker non-POST no-write、job cancel no-write、registry/draft/result explanation/model/benchmark scope no-write等；确认部分 route-specific audit proof 与 security-smoke selected test name 并不完全一一对应。
- 本次采用的关键假设：本阶段只产出 gap matrix 与计划状态，不修改 Go production code、测试、OpenAPI、schema、migration、generated client、auth scope、store interface 或 audit envelope；selected mutation audit 仍是 selected slice，不等同于 all-mutation audit；未跟踪的 `docs/rebuild/AutoWaterSimu_95分优雅度完整计划.md` 与 `docs/rebuild/simulation_core/` 属于用户/本地文件，不纳入本次修改。
- 剩余不确定性：P2 red/yellow rows 尚未修复，包括 security-smoke coverage wording/test selection 对齐、collection-route method proof、artifact archive cross-scope no-write proof；full issuer/JWKS/RBAC、complete object-level tenant/project/site data scope、complete all-mutation audit、P3 simulation_core/worker/backend boundary audit、hosted green evidence、legacy authenticated session 和 complete golden scenarios 仍未完成。
- 影响范围 / 风险控制方式：只改长期架构文档、补充开发计划、任务记录和变更记录；不改变运行时行为或验证脚本逻辑；矩阵对 partial/gap 使用保守表述，避免把现有 selected coverage 包装为全量安全完成。
- 验证方式：已通过 `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\security-smoke.ps1`（`status=passed`，3 steps）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1`、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1`（0 import boundary violations）、`rg -n "schema_version|P0|P1|P2" docs\rebuild --glob "AutoWaterSimu_Next_*.md"`、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1`（`status=passed`，8 steps，`compute_boundary_audit.status=passed`）、changed-file trailing-whitespace scan 和 `git diff --check -- .`。
- 是否更新 README：是，更新 `docs/architecture/README.md`，登记新的 Compute mutation scope matrix。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次是证据矩阵和后续优先级收口，不新增长期架构决策。

## 07:26 - PR fast compute boundary gate

- 用户目标：根据新的优雅度重构重新评估，停止优先推进普通 service-test split 或低收益同包整理；先宣布 P0 service-test split 收口，并把 P1 Compute API boundary audit 固化进默认 `pr-fast` gate。
- 涉及目录：`scripts/ci/`、`scripts/`、`.github/workflows/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`、`.ai/changes/`。
- 修改内容：`scripts/ci/pr-fast.ps1` 新增 `compute boundary audit` step，调用 `scripts/audit-compute-api-boundary.ps1` 并把 evidence 写入 `tmp/ci-evidence/compute-boundary/compute-api-boundary.json`；`pr-fast.json` 新增 `compute_boundary_audit` 摘要；同步 scripts/ci、scripts、workflow、architecture current-state、Compute API architecture、local-dev、Certainty/Elegance PRD/Plan 与 `tasks/todo.md`。
- 修改原因：上一阶段已让 boundary audit 能自动发现 domain/platform reverse-import 漂移，但它还没有进入默认 PR fast lane；只有进入默认 gate，P1 boundary freeze 才能防止后续性能优化 PR 把边界打回去。P0 service-test split 已结构性完成，应明确不再按行数机械拆分。
- 本次已消除的不确定性：确认 `pr-fast.ps1` 之前只运行 `check-deps`，没有运行 Compute API boundary audit；确认 `.github/workflows/next-pr-fast.yml` 上传整个 `tmp/ci-evidence`，因此新增 `compute-boundary/` 子目录会随现有 artifact 一起上传；确认本次不需要修改生产 Go/TS/Python 代码、HTTP route、schema、OpenAPI、migration、generated client、auth scope、store interface 或 audit envelope。
- 本次采用的关键假设：P0 service-test split closeout 可以通过 architecture/current-state 与 Compute API architecture 记录，而不需要继续移动测试；`compute_boundary_audit` 作为 `pr-fast.json` 顶层摘要加 step evidence 足以满足默认 gate 固化；未跟踪的 `docs/rebuild/AutoWaterSimu_95分优雅度完整计划.md` 与 `docs/rebuild/simulation_core/` 属于用户/本地文件，不纳入本次修改。
- 剩余不确定性：P2 mutation/data-scope gap matrix 尚未生成；P2 gap matrix 红项尚未修复；P3 simulation_core/worker/backend packaging/import/backend drift audit 尚未开始；hosted green evidence、release artifact hosted round trip、legacy authenticated session 和 complete golden scenarios 仍未完成。
- 影响范围 / 风险控制方式：只改默认 fast gate 编排与长期状态说明；新增 step 调用已存在的 read-only boundary audit；不改运行时行为、public service signatures、contracts、OpenAPI、migrations、generated clients 或 auth scopes。
- 验证方式：已通过 `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1`（0 import boundary violations）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1`、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1`、`rg -n "schema_version|P0|P1|P2" docs\rebuild --glob "AutoWaterSimu_Next_*.md"`、changed-file trailing-whitespace scan 和 `git diff --check -- .`；`pr-fast.json` status 为 `passed`，`compute_boundary_audit.status` 为 `passed`，`tmp\ci-evidence\compute-boundary\compute-api-boundary.json` 已生成，step count 为 8。
- 是否更新 README：是，更新 `scripts/README.md`、`scripts/ci/README.md` 与 `.github/workflows/README.md`，记录 `pr-fast` 现在包含 Compute API boundary audit evidence。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次固化的是已有 boundary audit 进入默认 gate，不新增架构决策。

## 00:47 - Compute boundary import guard audit

- 用户目标：根据附件优先级继续收口非 Desktop 剩余质量工作；在确认 P0 service-test split 已完成后，推进 P1 Compute API 包边界冻结，避免继续低收益同包整理。
- 涉及目录：`scripts/`、`apps/api/internal/compute/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`、`.ai/changes/`。
- 修改内容：扩展 `scripts/audit-compute-api-boundary.ps1`，解析 domain/platform Go imports，并在 `internal/domain/*` import compute 或 `internal/platform/*` import compute/domain 时失败；审计 JSON 增加 import boundary rules/violations；同步 `scripts/README.md`、`apps/api/internal/compute/README.md`、`docs/architecture/compute-api.md`、Certainty/Elegance development plan 与 `tasks/todo.md`。
- 修改原因：附件建议冻结 Compute API package boundary，避免继续做低收益同包整理。现有 `check-deps.ps1` 已有全仓反向依赖 guard，但 Compute API boundary audit 自身没有把 domain/platform reverse-import 漂移列为失败条件，package-boundary evidence 不够自包含。
- 本次已消除的不确定性：确认 `service_job_scope_test.go` 与 `service_worker_audit_test.go` 当前已是单主题 focused tests；确认 P0 测试拆分无需继续机械拆分；确认 import boundary guard 可通过 read-only PowerShell 审计实现，不需要改生产 Go 代码、HTTP route、schema、OpenAPI、migration、generated client、auth scope、store interface 或 audit envelope。
- 本次采用的关键假设：本阶段只强化 P1 boundary freeze 的自动化 guard；`check-deps` 继续作为全仓依赖边界检查，boundary audit 的重复 guard 是为了让 Compute API 边界证据自身可失败；未跟踪的 `docs/rebuild/AutoWaterSimu_95分优雅度完整计划.md` 与 `docs/rebuild/simulation_core/` 属于用户/本地文件，不纳入本次修改。
- 剩余不确定性：P2 remaining data-scope / object-scope / all-mutation audit、P3 simulation_core/worker/backend 边界稳定、P4 roadmap triage、hosted green evidence、release artifact hosted round trip、legacy authenticated session 和 complete golden scenarios 仍未完成。
- 影响范围 / 风险控制方式：只改审计脚本与长期说明；脚本仍是 read-only evidence gate，新增失败条件只针对 domain/platform reverse imports；不改运行时行为、public service signatures、contracts、OpenAPI、migrations、generated clients 或 auth scopes。
- 验证方式：已通过 `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1`（0 import boundary violations）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1`、`go test ./internal/domain/... ./internal/platform/... ./internal/compute -count=1`（在 `apps/api`）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1`、changed-file trailing-whitespace scan 和 `git diff --check -- .`。
- 是否更新 README：是，更新 `scripts/README.md` 与 `apps/api/internal/compute/README.md`，记录 boundary audit 现在也会失败 domain/platform reverse-import 漂移。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次是现有分层约束的自动化 guard，不引入新的架构决策。

## 00:22 - Model governance audit state domain split

- 用户目标：根据 Certainty/Elegance PRD/Plan 与附件优先级继续收口非 Desktop 剩余质量工作，在 service-test split、artifact/result explanation audit state split 后，继续推进 compute/domain 边界冻结，同时支撑 P2 model governance mutation audit 维护性。
- 涉及目录：`apps/api/internal/domain/models/`、`apps/api/internal/compute/`、`apps/api/`、`docs/architecture/`、`docs/rebuild/`、`scripts/`、`tasks/`、`.ai/changes/`。
- 修改内容：在 `domain/models` 新增 DTO-neutral model governance compact audit state projection helpers，覆盖 model catalog registration、default parameter set transition 和 benchmark_run registration；compute 侧 `model_governance_audit.go` 改为调用 domain helpers 并保留原 audit key shape；新增 focused domain tests；同步 apps/api 与 compute/domain/models README、Compute API architecture、architecture current-state、boundary audit note、Certainty/Elegance development plan 和 `tasks/todo.md`。
- 修改原因：model governance 的 before/after/payload maps 已是稳定、DTO-neutral 的 compact projection；selected mutation audit envelope、event type/action/reason/where、call site 与 persistence 决策仍依赖 compute workflow。将纯投影迁入 domain 可以减少后续 model governance audit/data-scope 修改时的 compute 边界漂移。
- 本次已消除的不确定性：确认本次可独立移动 compact audit state projection，不需要改 catalog/parameter/benchmark workflow、HTTP route、schema、OpenAPI、migration、generated client、auth scope、store interface 或 audit envelope shape；确认 `domain/models` 已是 model governance 稳定 helper 的归属地。
- 本次采用的关键假设：本阶段是 P1 compute-boundary freeze 的小型 package movement，不是完整 model governance workflow migration，也不是重新设计 audit taxonomy；保持 existing `map[string]any` key/value 兼容比重新设计 audit payload 更重要；未跟踪的 `docs/rebuild/AutoWaterSimu_95分优雅度完整计划.md` 与 `docs/rebuild/simulation_core/` 属于用户/本地文件，不纳入本次修改。
- 剩余不确定性：P2 remaining data-scope / object-scope / all-mutation audit、P3 simulation_core/worker/backend 边界稳定、P4 roadmap triage、hosted green evidence、release artifact hosted round trip、legacy authenticated session 和 complete golden scenarios 仍未完成；是否继续迁移更多 model governance workflow 逻辑，需要先补 store/DTO adapter，避免把 compute record/store/HTTP DTO 类型反向带入 domain。
- 影响范围 / 风险控制方式：只迁移纯投影并补 focused tests；compute 仍拥有 selected audit envelope assembly/call sites/persistence decisions、model catalog/status/promote/benchmark workflows、schema/catalog/scope gates、HTTP mapping、store orchestration 和 DTO adapters；不改 public service signatures、contracts、OpenAPI、migrations、generated clients 或 auth scopes。
- 验证方式：已通过 `go test ./internal/domain/models ./internal/compute -run "Test(ModelCatalogAuditState|ParameterSetTransitionAuditState|BenchmarkRunAuditState|HTTPModelCatalogMutationTenantProjectSiteScope|DefaultParameterSetPromoteApprovedEndpoint|BenchmarkRunEndpointRecordsCompletedModelRun|HTTPBenchmarkRunMutationTenantProjectSiteScope)$" -count=1`（在 `apps/api`）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\security-smoke.ps1`、`go test ./...`（在 `apps/api`）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1`、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1`、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1`、changed-file trailing-whitespace scan、`git diff --check -- .`。
- 是否更新 README：是，更新 `apps/api/README.md`、`apps/api/internal/README.md`、`apps/api/internal/compute/README.md`、`apps/api/internal/domain/README.md` 与 `apps/api/internal/domain/models/README.md`，记录 domain/models 现在拥有 model governance compact audit state projection，compute 保留 selected audit envelope/call site/persistence。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次固化的是既有 compute/domain 分层规则下的小型投影迁移，不新增长期架构决策文件。

## 00:07 - Result explanation audit state domain split

- 用户目标：根据 Certainty/Elegance PRD/Plan 与附件优先级继续收口非 Desktop 剩余质量工作，在 P0 service-test split 和 P1 artifact audit state split 后，继续推进 compute/domain 边界冻结，同时支撑 P2 result explanation audit 维护性。
- 涉及目录：`apps/api/internal/domain/evidence/`、`apps/api/internal/compute/`、`apps/api/`、`docs/architecture/`、`docs/rebuild/`、`scripts/`、`tasks/`、`.ai/changes/`。
- 修改内容：在 `domain/evidence` 新增 DTO-neutral `ResultExplanationAuditStateInput` 与 `ResultExplanationAuditState` compact projection helper；compute 侧 `resultExplanationAuditState` 改为调用 domain helper 并保留原 audit state key shape；新增 focused domain tests；同步 apps/api 与 compute/domain/evidence README、Compute API architecture、architecture current-state、boundary audit note、Certainty/Elegance development plan 和 `tasks/todo.md`。
- 修改原因：`domain/evidence` 已承接 result explanation evidence ref extraction 与 record data projection，现有 `resultExplanationAuditState` 只做 compact map projection，不做 selected audit envelope、call site 或 persistence 决策。将纯投影迁入 domain 可以减少后续 result explanation audit/data-scope 修改时的 compute 边界漂移。
- 本次已消除的不确定性：确认 result explanation submit/review/publish 的 selected audit envelope、job-scoped event persistence、review/publish 状态流、schema/job/evidence checks、HTTP mapping 和 store implementation 仍应留在 compute；确认本次不需要改 HTTP route、schema、OpenAPI、migration、generated client、auth scope、store interface 或 audit envelope shape。
- 本次采用的关键假设：本阶段是 P1 compute-boundary freeze 的小型 package movement，不是完整 result explanation workflow migration，也不是重新设计 audit action taxonomy；保持 `map[string]any` key/value 兼容比重新设计 audit payload 更重要；未跟踪的 `docs/rebuild/AutoWaterSimu_95分优雅度完整计划.md` 与 `docs/rebuild/simulation_core/` 属于用户/本地文件，不纳入本次修改。
- 剩余不确定性：P2 remaining data-scope / object-scope / all-mutation audit、P3 simulation_core/worker/backend 边界稳定、P4 roadmap triage、hosted green evidence、release artifact hosted round trip、legacy authenticated session 和 complete golden scenarios 仍未完成；是否继续迁移更多 result explanation workflow 逻辑，需要先补 store/DTO adapter，避免把 compute record/store 类型反向带入 domain。
- 影响范围 / 风险控制方式：只迁移纯投影并补 focused tests；compute 仍拥有 selected audit envelope assembly/call sites/persistence decisions、result explanation persistence/review/publish mutations、schema/job/evidence checks、HTTP mapping、store orchestration 和 DTO adapters；不改 public service signatures、contracts、OpenAPI、migrations、generated clients 或 auth scopes。
- 验证方式：已通过 `go test ./internal/domain/evidence ./internal/compute -run "Test(ResultExplanationAuditStateProjectsCompactState|ResultExplanationAuditStateOmitsEmptyOptionalFields|HTTPResultExplanationAuditEvents|HTTPResultExplanationReviewPublishAuditEvents|HTTPResultExplanationMutationTenantProjectSiteScope)$" -count=1`（在 `apps/api`）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\security-smoke.ps1`、`go test ./...`（在 `apps/api`）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1`、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1`、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1`、changed-file trailing-whitespace scan、`git diff --check -- .`。
- 是否更新 README：是，更新 `apps/api/README.md`、`apps/api/internal/README.md`、`apps/api/internal/compute/README.md`、`apps/api/internal/domain/README.md` 与 `apps/api/internal/domain/evidence/README.md`，记录 domain/evidence 现在拥有 result explanation compact audit state projection，compute 保留 selected audit envelope/call site/persistence。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次固化的是既有 compute/domain 分层规则下的小型投影迁移，不新增长期架构决策文件。
