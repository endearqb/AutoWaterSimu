## 23:59 - UDM v2 画布开发方案 v1.0 落地 [L2]

- 用户目标：根据 `.ai/plans/AutoWaterSimu_UDM_v2_Canvas_Development_Plan_v1.0.md` 完成 UDM Network v2 画布开发。
- 涉及目录：`frontend/src/features/udm-v2/**`、`frontend/tests/**`、`frontend/playwright.config.ts`、`.ai/decisions/`、`tasks/`。
- 修改内容：恢复严格 feature isolation；实现全屏 editor、浮动工作台、分层 Inspector、Load Dialog、状态 overlay、响应式与 feature-local i18n；实现四类边视觉、source/target endpoint lane、连续 fan-out/fan-in path、18px 命中区、低噪声标签和 render-only marker；增加 canonical `port_kind`、共享连接规则、store/semantic/serializer 加固、14-node/22-edge 二沉池展开和实时流量平衡；补充 unit、Chromium/Edge E2E、100/300 fixture 与四档截图。
- 修改原因：解决同端点多边不可读、交互与语义校验分叉、图级内容永久堆叠、移动端遮挡，以及 UDM-v2 与 legacy Flow 共享画布造成的回归风险。
- 本次已消除的不确定性：确认 `/udm-v2` 已具备独立 route/store/serializer/service；确认计划要求的 lane、marker、hit width 与实时流量展示均应为 render-only；确认 canonical port kind 可在不升级合同版本的前提下由现有 schema 承载；确认 branded Microsoft Edge 可用并完成同套 E2E。
- 本次采用的关键假设：保留已有 feature-local 实时流量平衡和二沉池展开能力，但不扩展到后端 solver；v1.0 不实现自动避障、物理子 Handle 或全局 UI 抽象；开发态 store bridge 仅服务 Playwright fixture，生产构建不暴露。
- 剩余不确定性：README contract 全局门禁仍被根 `README.md` 与 `README_zh.md` 既有的 `./release-notes.md` 断链阻塞；Vite build 仍报告既有大 chunk 与 `gray-matter` eval warning，本次未扩大为全局性能重构。
- 影响范围 / 风险控制方式：所有运行时代码留在 `features/udm-v2/**`；未新增依赖、未修改合同版本、未修改 legacy Flow 内容；boundary script 与 legacy Playwright regression 双重守卫；render copy 与 stored edges 分离；selection-only change 不触发 dirty。
- 验证证据：
  - `cd frontend; npm run typecheck` → exit 0。
  - `cd frontend; npm run test:unit -- --run` → 17 files / 50 tests passed；仅有既有 Chakra/jsdom CSS 解析 stderr 噪声。
  - `cd frontend; npm run check:udm-v2-boundary` → `UDM-v2 import boundary passed.`
  - `cd frontend; npm run build` → 4908 modules transformed，build completed；保留既有 chunk/eval warning。
  - `cd frontend; npx biome check ...` → 90 files checked，no fixes/errors。
  - Chromium focused Playwright → 16 passed，覆盖持久化、提交 pending、并行边、100/300、四视口和 legacy isolation。
  - Microsoft Edge focused Playwright → 同一 16 tests passed。
  - `scripts\readme-contract-check.ps1 -FailOnWarnings` → 本次 README 链路无新增问题；仅报告根 README 两个既有 `release-notes.md` 断链。
  - `git diff --check` → exit 0；仅显示 Windows CRLF 提示。
- 是否更新 README：是，更新 feature、nodes、serialize、inspector、tests README，并为 canvas、edges、flow、interaction、state、theme 新增目录契约。
- 是否需要新增或更新架构决策（.ai/decisions/）：是，将 ADR 0028 更新为 Accepted，固化严格隔离、render-only 状态与共享端口/连接规则。
