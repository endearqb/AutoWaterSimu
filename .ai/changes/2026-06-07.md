## 12:46 - Job state lifecycle mutation audit

- 用户目标：根据 Certainty/Elegance PRD 与开发计划继续完成剩余工作，并按近期策略停止低收益 evidence wrapper / 同包整理循环，推进明确的 all-mutation/data-scope 切片。
- 涉及目录：`apps/api`、`scripts/ci`、`docs/architecture`、`docs/rebuild`、`tasks`、`.ai/changes`。
- 修改内容：新增 `job_state_audit.go` 构造 compact job cancel/timeout audit event JSON；MemoryStore/PostgresStore 在 `job.cancelled` 与 `job.timed_out` 事件中写入 `event_json.audit`；HTTP cancel path 传入静态 token principal/route context；新增 `TestHTTPJobCancelMutationAuditEvents` 并扩展 `TestTimeoutSweepAndPagination`，纳入 security smoke；更新 Compute API、scripts/ci、architecture current-state、Certainty/Elegance plan 和任务记录。
- 修改原因：job cancel 和 timeout sweep 已通过 `domain/jobs` 生成稳定 state mutation plan 并写入持久 job events，但仍缺少 selected mutation audit envelope，是明确的 all-mutation audit 剩余切片；继续做 hosted/evidence wrapper 或同 package 整理收益较低。
- 本次已消除的不确定性：确认 cancel/timeout 的 domain package 只负责 DTO-neutral mutation plan，audit call site 应保留在 compute；确认 MemoryStore/PostgresStore 都可在应用 mutation 后用 before/after compact state 包装事件 JSON；确认 HTTP cancel 原先只验证 principal 但没有传入 audit context，已修正；确认 timeout sweep 可使用 job `requested_by` 和 trace 作为 service fallback。
- 本次采用的关键假设：本阶段只覆盖 job cancel/timeout selected audit，不声明完整 all-mutation audit、cancel mutation data-scope、完整 job lifecycle package migration、完整 RBAC/ABAC、全对象 data-scope、剩余 mutation data-scope、hosted green evidence、release artifact hosted round trip、legacy authenticated session 或完整 golden scenarios 完成；保留现有 cancel endpoint shape、状态语义、schema、OpenAPI、database schema 和 conflict 行为。
- 剩余不确定性：cancel mutation data-scope、高频 heartbeat audit、其他 remaining all-mutation audit、full object-level data-scope、remaining mutation data-scope、OIDC/JWKS、RBAC/ABAC、hosted green evidence、release artifact hosted round trip、legacy authenticated session 和 complete golden scenarios 仍未完成。
- 影响范围 / 风险控制方式：只修改 job state event JSON 构造、HTTP cancel context、focused tests、security smoke 和长期文档；不让 `apps/api/internal/domain/jobs` import compute/platform audit；不改变 endpoint path、request/response shape、contracts、migrations、generated clients 或 store interface。
- 验证方式：`go test ./internal/compute -run "Test(HTTPJobCancelMutationAuditEvents|TimeoutSweepAndPagination|HTTPMutationAuditEventEnvelopeForJobCreate|HTTPWorkerRegistrationMutationAuditEvents|HTTPWorkerJobMutationAuditEvents|HTTPResultExplanationAuditEvents|HTTPArtifactRetentionSweepRequiresAdminScope)$" -count=1`；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\security-smoke.ps1`；`go test ./...`（`apps/api`）；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1`；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1`；`rg -n "schema_version|P0|P1|P2" docs\rebuild --glob "AutoWaterSimu_Next_*.md"`；`git diff --check -- apps/api docs scripts/ci tasks .ai`，均通过；diff-check 仅报告既有 LF/CRLF 工作区提示。
- 是否更新 README：是，更新 `apps/api/README.md`、`apps/api/internal/compute/README.md`、`scripts/ci/README.md`、`docs/architecture/current-state.md` 和 Certainty/Elegance Development Plan 中的 selected mutation audit / security smoke 上下文。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次是既有 selected mutation audit envelope 策略在 job state lifecycle events 上的覆盖扩展，不新增长期架构决策。

## 12:29 - Worker registration mutation audit

- 用户目标：根据 Certainty/Elegance PRD 与开发计划继续完成剩余工作，并按近期策略停止低收益 evidence wrapper / 同包整理循环，推进明确的 all-mutation/data-scope 切片。
- 涉及目录：`apps/api`、`scripts/ci`、`docs/architecture`、`docs/rebuild`、`tasks`、`.ai/changes`。
- 修改内容：新增 compact `worker.registered` mutation audit 构造；MemoryStore/PostgresStore worker upsert 成功后写入全局 `mutation_audit_events`；新增 `TestHTTPWorkerRegistrationMutationAuditEvents` 并纳入 security smoke；调整一个模型治理测试为按对象过滤审计事件，避免合法 worker registration 背景事件污染全局计数；更新 Compute API、security smoke、architecture current-state、Certainty/Elegance plan 和任务记录。
- 修改原因：上一阶段已经覆盖 worker claim/artifact/completion 的 job-scoped selected audit，但 worker registration/upsert 仍是低频、明确、非 job-scoped 的 mutation audit 缺口；继续扩展 hosted evidence 或 wrapper 的边际收益低于补齐该切片。
- 本次已消除的不确定性：确认 worker registration/upsert 可在 MemoryStore/PostgresStore 成功写入后记录 compact audit；确认注册事件不需要 schema/migration/OpenAPI/client 变更；确认 payload 可以只存 worker id、runtime version、current job id、capability count、supported contract version count，不泄漏完整能力数组；确认新增 worker audit row 会影响依赖全局 mutation audit 计数的旧测试，已改为对象过滤。
- 本次采用的关键假设：本阶段只覆盖 worker registration selected audit，不声明完整 all-mutation audit、高频 heartbeat audit、完整 RBAC/ABAC、全对象 data-scope、剩余 mutation data-scope、hosted green evidence、release artifact hosted round trip、legacy authenticated session 或完整 golden scenarios 完成；worker registration 的重复 upsert 也记录为注册/刷新类生命周期 mutation，但 audit payload 保持 compact。
- 剩余不确定性：高频 heartbeat audit、其他 remaining all-mutation audit、full object-level data-scope、remaining mutation data-scope、OIDC/JWKS、RBAC/ABAC、hosted green evidence、release artifact hosted round trip、legacy authenticated session 和 complete golden scenarios 仍未完成。
- 影响范围 / 风险控制方式：只修改 worker persistence upsert audit、focused test、security smoke 和长期文档；不改变 worker register endpoint shape、token behavior、worker lifecycle invariants、database schema、OpenAPI、generated client 或 domain package boundaries；未触碰用户未跟踪文档。
- 验证方式：`go test ./internal/compute -run "Test(HTTPWorkerRegistrationMutationAuditEvents|HTTPWorkerJobMutationAuditEvents|HTTPMutationAuditEventEnvelopeForJobCreate|HTTPResultExplanationAuditEvents|HTTPArtifactRetentionSweepRequiresAdminScope)$" -count=1`；`go test ./internal/compute -run "Test(ModelCatalogEndpoint|DefaultParameterSetPromotionPlanEndpoint|BenchmarkCaseScheduleRunEndpoint|ContractValidationEndpoint|SimulationCheckEndpointCreatesComputeJob|NewSystemEvidenceReferenceE2E)" -count=1`；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\security-smoke.ps1`；`go test ./...`（`apps/api`）；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1`；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1`；`rg -n "schema_version|P0|P1|P2" docs\rebuild --glob "AutoWaterSimu_Next_*.md"`；`git diff --check -- apps/api docs scripts/ci tasks .ai`，均通过；diff-check 仅报告既有 LF/CRLF 工作区提示。
- 是否更新 README：是，更新 `apps/api/README.md`、`apps/api/internal/compute/README.md`、`scripts/ci/README.md`、`docs/architecture/current-state.md` 和 Certainty/Elegance Development Plan 中的 selected mutation audit / security smoke 上下文。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次是既有 selected mutation audit envelope 策略在 worker registration 上的覆盖扩展，不新增长期架构决策。

## 12:16 - Worker job-event mutation audit

- 用户目标：根据 Certainty/Elegance PRD 与开发计划继续完成剩余工作，并按近期策略停止低收益 evidence wrapper / 同包整理循环，推进明确的 all-mutation/data-scope 切片。
- 涉及目录：`apps/api/`、`scripts/ci/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`、`.ai/changes/`。
- 修改内容：为 worker HTTP register/claim/heartbeat/artifact/succeed/fail paths 传递 audit principal/route context；为已有 `job.running`、`artifact.recorded`、`job.<terminal_status>` 事件添加 selected mutation audit envelope；新增 `worker_job_event_audit.go` 集中生成 compact audit event JSON；新增 `TestHTTPWorkerJobMutationAuditEvents` 并纳入 security smoke；更新 Compute API、scripts/ci、architecture current-state、Certainty/Elegance plan 和任务记录。
- 修改原因：近期 `.ai/changes` 显示继续做 hosted/evidence wrapper 或同 package 文件整理边际收益下降；worker claim、artifact upload 和 worker completion 已经写入持久 job events，但没有 selected audit envelope，是明确的 all-mutation audit 剩余切片。
- 本次已消除的不确定性：确认 worker claim 的 `job.running`、artifact upload 的 `artifact.recorded`、worker result completion 的 `job.<terminal_status>` 是已存在且可审计的持久事件；确认 audit context 可从 worker HTTP handler 传入而不让 `internal/domain/workers` 依赖 compute/platform audit；确认 MemoryStore 和 PostgresStore 可以只替换事件 JSON 而不改变状态机、SQL schema 或 endpoint shape；确认 security smoke 可直接聚合新增 HTTP 测试。
- 本次采用的关键假设：本阶段只覆盖 worker job-scoped existing event audit，不声明完整 all-mutation audit、worker registration audit、高频 heartbeat audit、完整 RBAC/ABAC、全对象 data-scope、剩余 mutation data-scope、hosted green evidence、release artifact hosted round trip、legacy authenticated session 或完整 golden scenarios 完成；heartbeat 不新增事件，避免引入高频审计噪声。
- 剩余不确定性：worker registration audit、高频 heartbeat audit、其他 remaining all-mutation audit、full object-level data-scope、remaining mutation data-scope、OIDC/JWKS、RBAC/ABAC、hosted green evidence、release artifact hosted round trip、legacy authenticated session 和 complete golden scenarios 仍未完成。
- 影响范围 / 风险控制方式：只修改 worker HTTP context propagation、existing event JSON construction、focused tests、security smoke 和长期文档；保留 endpoint path、request/response schema、OpenAPI、migrations、worker domain package boundary、job state transitions、global/dev token behavior 和原事件业务字段；未触碰用户未跟踪文档。
- 验证方式：`go test ./internal/compute -run "Test(HTTPWorkerJobMutationAuditEvents|HTTPMutationAuditEventEnvelopeForJobCreate|HTTPResultExplanationAuditEvents|HTTPArtifactRetentionSweepRequiresAdminScope)$" -count=1`；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\security-smoke.ps1`；`go test ./...`（`apps/api`）；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1`；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1`；`rg -n "schema_version|P0|P1|P2" docs\rebuild --glob "AutoWaterSimu_Next_*.md"`；`git diff --check -- apps/api docs scripts/ci tasks .ai`，均通过；diff-check 仅报告既有 LF/CRLF 工作区提示。
- 是否更新 README：是，更新 `apps/api/README.md`、`apps/api/internal/compute/README.md`、`scripts/ci/README.md`、`docs/architecture/current-state.md` 与 Certainty/Elegance Development Plan。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次是既有 selected mutation audit envelope 策略在 worker job-event 上的覆盖扩展，不新增长期架构决策。

## 11:48 - Scoped promotion evidence filtering

- 用户目标：根据 Certainty/Elegance PRD 与开发计划继续完成剩余工作，并按近期策略停止低收益 evidence wrapper / 同包整理循环，推进明确的 all-mutation/data-scope 切片。
- 涉及目录：`apps/api/`、`frontend/src/client/compute/`、`frontend/src/features/model-governance/`、`scripts/ci/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`、`.ai/changes/`。
- 修改内容：为 model catalog default-parameter-set promotion-plan / promote-approved 增加 scoped-token `job_id` evidence filter；scoped token 缺少 `job_id` 或传入跨 scope job 时返回 403；授权 `job_id` 只使用该 job 的 benchmark_run/model_run evidence；scoped promote-approved 只突变 matching persisted catalog，不回退 built-in/global catalog；更新 OpenAPI 与 generated Compute TypeScript client，并给 model-governance wrapper 增加可选 `jobId`；新增 focused HTTP 覆盖并纳入 security smoke；更新长期 README、architecture current-state、Certainty/Elegance plan 和任务记录。
- 修改原因：近期 `.ai/changes` 显示 promotion evidence / hosted wrapper 与同包拆分已有循环风险；result explanation review/publish 已受 job route data-scope 保护，而 promotion-plan/promote-approved 仍是明确剩余缺口。本阶段直接消除该缺口，而不是继续包装 partial evidence。
- 本次已消除的不确定性：确认 result explanation mutations 已经在 `jobByID` 入口完成 route-level job data-scope 授权；确认 `BenchmarkRunFilter.JobID` 能直接把 promotion evidence 查询收敛到授权 job；确认 scoped promote-approved 可复用 `ModelCatalogForMutation` 避免写 built-in/global catalog；确认 OpenAPI optional query param 能同步到 generated client 且不破坏现有 UI 调用。
- 本次采用的关键假设：本阶段只覆盖 promotion plan / promote-approved job-scoped evidence filtering，不声明完整 RBAC/ABAC、全对象 data-scope、complete all-mutation audit、hosted green evidence、release artifact hosted round trip、legacy authenticated session 或完整 golden scenarios 完成；global token 保持原有无 `job_id` 的全局最新 evidence 行为；scoped token 的单 `job_id` promotion plan 对多 benchmark case 场景会保守地只认可同一 job 的 matching evidence。
- 剩余不确定性：其他对象写入 data-scope、complete all-mutation audit、OIDC/JWKS、RBAC/ABAC、hosted green evidence、release artifact hosted round trip、legacy authenticated session 和 complete golden scenarios 仍未完成。
- 影响范围 / 风险控制方式：只新增 optional query param、服务层 scoped delegates、HTTP 授权分支、focused tests、generated compute client 类型和长期文档；不改变 endpoint path、request body schema、database schema、benchmark admission、audit envelope shape、domain package auth 边界或现有 global-token 行为；未触碰用户未跟踪文档。
- 验证方式：`go test ./internal/compute -run "Test(HTTPModelCatalogPromotionTenantProjectSiteScope|HTTPModelCatalogMutationTenantProjectSiteScope|HTTPModelCatalogTenantProjectSiteScope|DefaultParameterSetPromotionPlanEndpoint)$" -count=1`；`go test ./...`（`apps/api`）；`npx tsc --noEmit`（`frontend`）；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\security-smoke.ps1`；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1`；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1`；`rg -n "schema_version|P0|P1|P2" docs\rebuild --glob "AutoWaterSimu_Next_*.md"`；`git diff --check -- apps/api docs frontend/src scripts/ci tasks .ai`，均通过；diff-check 仅报告既有 LF/CRLF 工作区提示。
- 是否更新 README：是，更新 `apps/api/internal/compute/README.md` 与 `frontend/src/features/model-governance/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本阶段延续既有 scoped token / job-scoped evidence filter 策略，未新增长期架构决策。

## 09:52 - Model catalog mutation scope

- 用户目标：根据 Certainty/Elegance PRD 与开发计划继续完成剩余工作，并按近期策略停止低收益 evidence wrapper / 同包整理循环，推进明确的 mutation data-scope 切片。
- 涉及目录：`apps/api/`、`scripts/ci/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`、`.ai/changes/`。
- 修改内容：为 model catalog registration 增加 scoped payload tenant/project/site 写入前校验；为 default parameter set status mutation 增加 scoped persisted-catalog lookup，scoped token 没有匹配 persisted catalog 时返回 403 而不回退 built-in/global catalog；scoped promote-approved 与 promotion plan 一样在 job-scoped benchmark/model_run evidence filtering 完成前拒绝；新增 focused HTTP 覆盖并纳入 security smoke；更新长期 README、architecture current-state/local-dev、Certainty/Elegance plan 和任务记录。
- 修改原因：上一阶段完成 persisted model catalog root/model/snapshot read-scope 后，model governance writes 的 mutation data-scope 仍是明确剩余缺口。registration/status 是可收敛的非 job-scoped model catalog 写入面；promote-approved 仍依赖跨 benchmark/model_run evidence 聚合，不能在 scoped token 下静默开放。
- 本次已消除的不确定性：确认 `model_catalog.v1.metadata.tenant_id/project_id/site_id` 已足以做 registration/status scope 校验；确认 status mutation 可以复用现有 persisted snapshot filter，且 scoped token 不应从 built-in fallback 创建新 catalog snapshot；确认无需 schema、OpenAPI、migration 或 generated client 变更。
- 本次采用的关键假设：本阶段只覆盖 model catalog registration/status mutation data-scope，不声明完整 RBAC/ABAC、全对象 data-scope、all-mutation audit 或所有 mutation data-scope 完成；scoped promote-approved 暂时拒绝是安全收紧，直到 promotion evidence filtering 可按 job/data scope 收敛；未改变 endpoint path、request/response shape、contracts、database schema 或全局 token 行为。
- 剩余不确定性：promotion plan / promote-approved 的 job-scoped evidence filtering、benchmark_run registration 的 mutation data-scope、其他对象写入 data-scope、complete all-mutation audit、OIDC/JWKS、RBAC/ABAC、hosted green evidence 和完整 golden scenarios 仍未完成。
- 影响范围 / 风险控制方式：只修改 model governance service delegate/HTTP mapping、focused tests、security smoke coverage 和长期文档；在 service Upsert 前拒绝跨 scope registration，避免先写后拒绝；scoped status mutation 只读取匹配 persisted snapshot；未新增外部 API、依赖、schema 或 migration。
- 验证方式：`go test ./internal/compute -run "Test(HTTPModelCatalogMutationTenantProjectSiteScope|HTTPModelCatalogTenantProjectSiteScope|ModelCatalogEndpoint|DefaultParameterSetPromotionPlanEndpoint)$" -count=1`；`go test ./...`（`apps/api`）；`npx tsc --noEmit`（`frontend`）；`rg -n "schema_version|P0|P1|P2" docs\rebuild --glob "AutoWaterSimu_Next_*.md"`；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\security-smoke.ps1`；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1`；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1`；`git diff --check -- apps/api scripts/ci docs/architecture docs/rebuild tasks`，均通过；diff-check 仅报告既有 LF/CRLF 工作区提示。
- 是否更新 README：是，更新 `apps/api/README.md`、`apps/api/internal/compute/README.md`、`scripts/ci/README.md`、`docs/architecture/current-state.md`、`docs/architecture/local-dev.md` 与 Certainty/Elegance Development Plan。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次是既有 static-token data-scope 策略的覆盖扩展，不新增长期架构决策。

## 10:13 - Benchmark run mutation scope

- 用户目标：根据 Certainty/Elegance PRD 与开发计划继续完成剩余工作，并按近期策略停止 evidence wrapper / hosted workflow / 同包整理循环，推进明确的 mutation data-scope 切片。
- 涉及目录：`apps/api/`、`scripts/ci/`、`docs/rebuild/`、`tasks/`、`.ai/changes/`。
- 修改内容：为 `POST /api/v1/model-catalog/{model_key}/versions/{model_version}/benchmark-runs` 增加 scoped token 写入前 data-scope 校验；scoped token 会按 payload `job_id` 查关联 job，并用 job 的 tenant/project/site 与 token scope 比对，跨 scope 请求返回 403 且不写 benchmark_run 记录或 mutation audit；新增 focused HTTP 回归测试并纳入 security smoke；更新 API/security/Certainty-Elegance/task 上下文。
- 修改原因：benchmark_run read-scope 与 selected mutation audit 已完成，但 registration mutation 仍可由 scoped `model:write` token 指向跨 scope job 写入历史记录。本次切片直接减少剩余 mutation data-scope 缺口，避免继续做低收益 evidence/hosted wrapper。
- 本次已消除的不确定性：确认 `benchmark_run.v1.job_id` 是可用于写入 data-scope 的稳定关联；确认校验可在 `Service` adapter 层完成，不需要把 `JobStore` 注入 `ModelGovernanceService`；确认无需 schema、OpenAPI、migration 或 generated client 变更。
- 本次采用的关键假设：本阶段只覆盖 benchmark_run registration mutation data-scope，不声明完整 RBAC/ABAC、全对象 data-scope 或 all-mutation audit 完成；global token 保持原行为；promotion plan / promote-approved 仍因 job-scoped evidence filtering 未完成而对 scoped token 拒绝。
- 剩余不确定性：promotion plan / promote-approved 的 job-scoped evidence filtering、其他对象写入 data-scope、complete all-mutation audit、OIDC/JWKS、RBAC/ABAC、hosted green evidence 和完整 golden scenarios 仍未完成。
- 影响范围 / 风险控制方式：只修改 HTTP/service adapter、auth helper、focused tests、security smoke 和长期文档；跨 scope 写入在 `UpsertBenchmarkRun` 与 mutation audit 构造前拒绝；不改变 endpoint path、request/response shape、contracts、database schema、OpenAPI、generated client 或全局 token 行为。
- 验证方式：`go test ./internal/compute -run "Test(HTTPBenchmarkRunMutationTenantProjectSiteScope|HTTPBenchmarkRunTenantProjectSiteScope|DefaultParameterSetPromotionPlanEndpoint)$" -count=1`；`go test ./...`（`apps/api`）；`npx tsc --noEmit`（`frontend`）；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\security-smoke.ps1`；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1`；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1`；`rg -n "schema_version|P0|P1|P2" docs\rebuild --glob "AutoWaterSimu_Next_*.md"`；`git diff --check -- apps/api scripts/ci docs/rebuild tasks .ai`，均通过；diff-check 仅报告既有 LF/CRLF 工作区提示。
- 是否更新 README：是，更新 `apps/api/README.md`、`apps/api/internal/compute/README.md`、`scripts/ci/README.md` 与 Certainty/Elegance PRD/Development Plan。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次是既有 static-token data-scope 策略在 benchmark_run registration 上的覆盖扩展，不新增长期架构决策。

## 10:40 - Simulation registry mutation scope

- 用户目标：根据 Certainty/Elegance PRD 与开发计划继续完成剩余工作，并按近期策略停止低收益 evidence wrapper / hosted workflow / 同包整理循环，推进明确的 mutation data-scope 切片。
- 涉及目录：`apps/api/`、`scripts/ci/`、`docs/rebuild/`、`tasks/`、`.ai/changes/`。
- 修改内容：为显式 `POST /api/v1/process-graphs` 与 `POST /api/v1/simulation-inputs` 增加 scoped token 写入前 data-scope 校验；scoped token 会按 payload `metadata.tenant_id` / `project_id` / `site_id` 与 token scope 比对，跨 scope 请求返回 403 且不写入 registry record 或 mutation audit；新增 focused HTTP 回归测试并纳入 security smoke；更新 API/security/Certainty-Elegance/task 上下文。
- 修改原因：simulation registry read-scope 与 selected mutation audit 已完成，但显式 registry registration mutation 仍可由 scoped `job:create` token 写入跨 scope payload。本次切片直接减少剩余 mutation data-scope 缺口，且避免继续扩展 evidence/hosted wrapper。
- 本次已消除的不确定性：确认 `process_graph.v1.metadata` 与 `simulation_input.v1.metadata` 中的 tenant/project/site 已足以做显式 registry POST scope 校验；确认校验可在 registry record 构造后、upsert/audit 前完成；确认无需 schema、OpenAPI、migration 或 generated client 变更。
- 本次采用的关键假设：本阶段只覆盖显式 process_graph/simulation_input registry POST mutation data-scope，不声明 job create / simulation-check mutation data-scope、完整 RBAC/ABAC、全对象 data-scope 或 all-mutation audit 完成；global token 保持原行为；simulation-check 自动落库 registry record 仍随 job-create/simulation-check data-scope 作为后续切片处理。
- 剩余不确定性：job create / simulation-check mutation data-scope、promotion plan / promote-approved 的 job-scoped evidence filtering、其他对象写入 data-scope、complete all-mutation audit、OIDC/JWKS、RBAC/ABAC、hosted green evidence 和完整 golden scenarios 仍未完成。
- 影响范围 / 风险控制方式：只修改显式 registry HTTP/service path、focused tests、security smoke 和长期文档；跨 scope 写入在 registry upsert 与 mutation audit 构造前拒绝；不改变 endpoint path、request/response shape、contracts、database schema、OpenAPI、generated client、registry idempotency、read-scope 或全局 token 行为。
- 验证方式：`go test ./internal/compute -run "Test(HTTPSimulationRegistryMutationTenantProjectSiteScope|HTTPSimulationRegistryTenantProjectSiteScope|HTTPSimulationRegistryMutationAuditEvents)$" -count=1`；`go test ./...`（`apps/api`）；`npx tsc --noEmit`（`frontend`）；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\security-smoke.ps1`；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1`；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1`；`rg -n "schema_version|P0|P1|P2" docs\rebuild --glob "AutoWaterSimu_Next_*.md"`；`git diff --check -- apps/api scripts/ci docs/rebuild tasks .ai`，均通过；diff-check 仅报告既有 LF/CRLF 工作区提示。
- 是否更新 README：是，更新 `apps/api/README.md`、`apps/api/internal/compute/README.md`、`scripts/ci/README.md` 与 Certainty/Elegance Development Plan。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次是既有 static-token data-scope 策略在显式 simulation registry registration 上的覆盖扩展，不新增长期架构决策。

## 11:03 - Direct job create mutation scope

- 用户目标：根据 Certainty/Elegance PRD 与开发计划继续完成剩余工作，并按近期策略停止低收益 evidence wrapper / hosted workflow / 同包整理循环，推进明确的 mutation data-scope 切片。
- 涉及目录：`apps/api/`、`scripts/ci/`、`docs/rebuild/`、`tasks/`、`.ai/changes/`。
- 修改内容：为 direct `POST /api/v1/compute/jobs` 增加 scoped token 写入前 data-scope 校验；为 direct `POST /api/v1/simulation-checks` 增加 request metadata 写入前 scope 校验、scoped input-ref resolution 和 scoped job create；embedded `simulation_input` 缺少 scope metadata 时继承已授权的 simulation request scope，显式 metadata 不匹配时拒绝且不写 job、registry record 或 mutation audit；新增 focused HTTP 回归测试并纳入 security smoke；更新 API/security/Certainty-Elegance/task 上下文。
- 修改原因：上一阶段补齐显式 simulation registry POST mutation-scope 后，direct job create 和 direct simulation-check create 仍可由 scoped `job:create` token 创建跨 scope job；simulation-check 还可能在 job create 拒绝前自动落库 `simulation_input`。本次切片把这两个核心 direct HTTP 写入面收紧。
- 本次已消除的不确定性：确认 `compute_job.context` 是 direct job create 的稳定 scope 来源；确认 `simulation_request.metadata.tenant_id/project_id` 与 `metadata.site_id` / `external_refs.site_id` 是 direct simulation-check job context 的稳定 scope 来源；确认 scoped simulation-check 必须在 input-ref resolution 前做 request-level scope 检查，避免先写 registry 后拒绝。
- 本次采用的关键假设：本阶段只覆盖 direct HTTP job create 和 direct HTTP simulation-check create，不声明 draft promotion、benchmark schedule-run 等间接 job-producing workflow 的 mutation data-scope 完成；global token 和内部 unscoped service calls 保持原行为；未改变 endpoint path、request/response shape、contracts、database schema、OpenAPI 或 generated client。
- 剩余不确定性：draft promotion / benchmark schedule-run 等间接 job-producing workflow data-scope、promotion plan / promote-approved 的 job-scoped evidence filtering、其他对象写入 data-scope、complete all-mutation audit、OIDC/JWKS、RBAC/ABAC、hosted green evidence 和完整 golden scenarios 仍未完成。
- 影响范围 / 风险控制方式：只修改 job lifecycle/service delegate、direct HTTP job/simulation-check handlers、simulation input resolution scope checks、focused tests、security smoke 和长期文档；跨 scope direct job create 在 `InsertJob` 与 job events 前拒绝；跨 scope direct simulation-check 在 input-ref registry write 与 job create 前拒绝；不引入新依赖、schema 或 migration。
- 验证方式：`go test ./internal/compute -run "Test(HTTPJobCreateMutationTenantProjectSiteScope|HTTPSimulationCheckMutationTenantProjectSiteScope|CreateJobIdempotencyDuplicateAndConflict|SimulationCheckEndpointCreatesComputeJob|HTTPSimulationRegistryMutationTenantProjectSiteScope|HTTPSimulationRegistryMutationAuditEvents)$" -count=1`；`go test ./...`（`apps/api`）；`npx tsc --noEmit`（`frontend`）；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\security-smoke.ps1`；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1`；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1`；`rg -n "schema_version|P0|P1|P2" docs\rebuild --glob "AutoWaterSimu_Next_*.md"`；`git diff --check -- apps/api scripts/ci docs/rebuild tasks .ai`，均通过；diff-check 仅报告既有 LF/CRLF 工作区提示。
- 是否更新 README：是，更新 `apps/api/README.md`、`apps/api/internal/compute/README.md`、`scripts/ci/README.md` 与 Certainty/Elegance Development Plan。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次是既有 static-token data-scope 策略在 direct job create / simulation-check create 上的覆盖扩展，不新增长期架构决策。

## 11:21 - Indirect job workflow mutation scope

- 用户目标：根据 Certainty/Elegance PRD 与开发计划继续完成剩余工作，并按近期策略停止低收益 evidence wrapper / hosted workflow / 同包整理循环，推进明确的 mutation data-scope 切片。
- 涉及目录：`apps/api/`、`scripts/ci/`、`docs/rebuild/`、`tasks/`、`.ai/changes/`。
- 修改内容：为 draft promotion 增加 scoped promotion delegate，HTTP route 在 stored confirmation scope 通过后继续把 principal-derived filter 传入 proposed simulation_request、input-ref resolution 和最终 job create；为 benchmark schedule-run 增加 scoped schedule delegate，HTTP route 把同一 filter 传入 benchmark input-ref resolution 和最终 benchmark job create；新增 focused HTTP 回归覆盖 cross-scope proposed request、cross-scope benchmark job metadata 与 cross-scope benchmark input_ref，并纳入 security smoke；更新 API/security/Certainty-Elegance/task 上下文。
- 修改原因：direct job/simulation-check 与显式 registry/model-governance mutation scopes 已收紧后，draft promotion 与 benchmark schedule-run 仍在 route auth/read-scope 之后使用 unscoped creation callback，可能由 scoped token 触发跨 scope job 或 registry 写入。本次切片补齐两个 indirect job-producing workflow 的写入前 data-scope。
- 本次已消除的不确定性：确认 draft promotion 的 stored confirmation scope 不能单独证明 proposed simulation_request scope；确认 benchmark schedule-run 的 referenced simulation_input/process_graph/model_run source job scope 与 request metadata-derived job context 都需要在写入前校验；确认该收紧可通过 compute service scoped callback 完成，domain agent/models packages 不需要接触 auth 或 compute DTO。
- 本次采用的关键假设：本阶段只覆盖 draft promotion 与 benchmark schedule-run job creation mutation data-scope，不声明 promotion plan / promote-approved job-scoped evidence filtering、完整 RBAC/ABAC、全对象 data-scope 或 all-mutation audit 完成；global token 和内部 unscoped service calls 保持原行为；未改变 endpoint path、request/response shape、contracts、database schema、OpenAPI 或 generated client。
- 剩余不确定性：promotion plan / promote-approved 的 job-scoped evidence filtering、其他对象写入 data-scope、complete all-mutation audit、OIDC/JWKS、RBAC/ABAC、hosted green evidence 和完整 golden scenarios 仍未完成。
- 影响范围 / 风险控制方式：只修改 compute service callbacks/delegates、direct HTTP draft promotion and benchmark schedule-run handlers、focused tests、security smoke 和长期文档；跨 scope draft promotion / benchmark schedule-run 在 input-ref registry write、InsertJob 和 job events 前拒绝；不引入新依赖、schema 或 migration。
- 验证方式：`go test ./internal/compute -run "Test(HTTPDraftConfirmationTenantProjectSiteScope|HTTPBenchmarkScheduleRunMutationTenantProjectSiteScope|BenchmarkCaseScheduleRunEndpoint|HTTPSimulationCheckMutationTenantProjectSiteScope|HTTPJobCreateMutationTenantProjectSiteScope)$" -count=1`；`go test ./...`（`apps/api`）；`npx tsc --noEmit`（`frontend`）；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\security-smoke.ps1`；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1`；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1`；`rg -n "schema_version|P0|P1|P2" docs\rebuild --glob "AutoWaterSimu_Next_*.md"`；`git diff --check -- apps/api scripts/ci docs/rebuild tasks .ai`，均通过；diff-check 仅报告既有 LF/CRLF 工作区提示。
- 是否更新 README：是，更新 `apps/api/README.md`、`apps/api/internal/compute/README.md`、`scripts/ci/README.md` 与 Certainty/Elegance Development Plan。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次是既有 static-token data-scope 策略在 indirect job-producing workflows 上的覆盖扩展，不新增长期架构决策。
