## 23:49 - AutoWaterSimu Next selected mutation audit

- 用户目标：继续根据 Certainty/Elegance PRD 与 Development Plan 完善和优化，并在阶段性 commit/push 前补齐 security smoke 中 mutation audit 的最小可验证切片。
- 涉及目录：`apps/api/internal/compute/`、`apps/api/`、`scripts/ci/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`、`.ai/changes/`。
- 修改内容：新增 `apps/api/internal/compute/audit.go`；为 `job.created` / `job.queued` 以及 artifact retention delete/archive events 写入 `event_json.audit` envelope；HTTP job create、simulation-check、draft promotion、benchmark schedule 和 retention sweep 会把 static token principal 与 route 注入 audit context；新增/扩展 Go tests 验证 job create 与 artifact retention audit envelope；扩展 `scripts/ci/security-smoke.ps1` coverage summary；同步 API README、scripts/ci README、architecture current-state/local-dev、Certainty/Elegance Development Plan 和任务记录。
- 修改原因：Certainty/Elegance Phase 2 要求 mutation audit，而 security smoke 之前明确记录 `mutation_audit_events=not_covered`。本轮用已有 `compute_job_events.event_json` 落地 selected mutation audit，不引入 migration/OpenAPI/schema 变更，先把最关键的 job create 与 artifact retention/archive mutation 变成可测试 evidence。
- 本次已消除的不确定性：确认 `compute_job_events.event_json` 是可承载 audit envelope 的现有自由 JSON 边界；确认 HTTP auth principal 当前只有 token name/scopes，因此 audit `who` 可先使用 static-token principal name；确认 non-HTTP scheduler/service path 需要 system/service fallback；确认本轮不应声称完整 OIDC/RBAC/data-scope 或 all-mutation audit 已完成。
- 本次采用的关键假设：selected audit envelope 使用 `who/when/where/target_object/action/before/after/reason/trace_id/approval_ref` 字段；job create/queue 和 artifact retention delete/archive 足以把 security smoke 的 mutation audit 从未覆盖推进到部分覆盖；不修改 OpenAPI、contracts、generated client、DB migration 或 auth scope 语义。
- 剩余不确定性：tenant/project/site data-scope 仍未实现；OIDC/JWKS/service-token secret manager 仍未实现；所有 mutation 的完整 audit enforcement 未完成，result explanation/model catalog/worker mutation 等仍需后续补齐。
- 影响范围 / 风险控制方式：变更限定在现有 event JSON payload、HTTP context 注入和测试/evidence 文档；保留原有事件顶层字段（如 `status`、`artifact_id`），因此现有 evidence timeline 消费方仍可读取旧字段；通过 focused Go tests、full Go tests、security smoke、dependency check、PR fast 和 diff check 控制回归。
- 验证方式：`cd apps\api; go test ./internal/compute -run "TestHTTPMutationAuditEventEnvelopeForJobCreate|TestHTTPArtifactRetentionSweepRequiresAdminScope|TestArtifactRetentionSweepArchivesCandidateWithConfiguredBackend" -count=1` 通过；`cd apps\api; go test ./internal/compute -run "TestArtifactRetentionSweepDeletesOnlyUnreferencedExpiredTTL" -count=1` 通过；`cd apps\api; go test ./...` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\security-smoke.ps1` 通过（evidence status `passed`，3 steps passed，0 failed，`mutation_audit_events=covered_for_job_create_and_artifact_retention_events`，`tenant_project_site_data_scope=not_covered`）；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` 通过（evidence status `passed`，7 steps passed，0 failed）；`git diff --check -- apps\api scripts docs\architecture docs\rebuild\AutoWaterSimu_Next_Certainty_Elegance_Development_Plan_v1.0.md tasks\todo.md .ai\changes\2026-05-31.md` 通过（仅 LF/CRLF warning）。
- 是否更新 README：是，更新 `apps/api/README.md`、`apps/api/internal/compute/README.md`、`scripts/ci/README.md`、`docs/architecture/local-dev.md`、`docs/architecture/current-state.md` 和 Certainty/Elegance Development Plan。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本轮复用现有 event store/JSON payload 边界，不新增长期身份模型或安全架构取舍。

## 22:11 - AutoWaterSimu Next desktop package smoke

- 用户目标：继续根据 Certainty/Elegance PRD 与 Development Plan 完善和优化，把 Desktop 离线项目包从合同落地推进到可重复运行的 package smoke evidence。
- 涉及目录：`scripts/ci/`、`.github/`、`docs/architecture/`、`docs/rebuild/`、根 `README.md`、`Justfile`、`tasks/`、`.ai/changes/`。
- 修改内容：新增 `scripts/ci/desktop-package-smoke.ps1`，聚合 Desktop package/support bundle contract fixture tests、Rust clean-runtime project package export/import round-trip、support bundle redaction 和 Desktop typecheck，并写出 `tmp/ci-evidence/desktop-package-smoke.json`；新增 `just desktop-package-smoke`；新增 `.github/workflows/next-desktop-package-smoke.yml` manual/workflow_call lane 并上传 evidence；新增 `docs/architecture/desktop-runtime.md`；同步 root README、scripts README、scripts/ci README、GitHub READMEs、architecture local-dev/current-state、Certainty/Elegance Development Plan 和任务记录。
- 修改原因：上一轮已把 `desktop_project_package.v1` 与 `desktop_support_bundle.v1` 合同落地，但 PRD/Plan 的场景级验收要求是“export package / import clean runtime / checksum verified / metadata restored / artifact/model_run references preserved”。将现有合同测试和 Rust runtime round-trip 包装成独立 evidence lane，可以让 Desktop 离线项目场景从单点测试变成可追溯 smoke。
- 本次已消除的不确定性：确认现有 Rust `project_export_import_is_limited_to_runtime_sandbox` 已覆盖 clean runtime import、checksum-verified artifact/support bundle file restore、metadata/CanvasGraph/job/artifact/model_run/event/support bundle restoration；确认 `support_bundle_excludes_artifact_contents` 覆盖 support bundle 脱敏边界；确认合同 fixture 可用 targeted contract schema test 覆盖；确认该 lane 仍不能证明 packaged worker、NSIS installer 或 hosted green run。
- 本次采用的关键假设：Desktop package smoke 暂时保持 opt-in，不进入默认 PR fast；该 smoke 证明 source-mode runtime package 语义，不替代 release gate、packaged sidecar smoke、installer smoke、artifact download verification 或签名/auto-update policy。
- 剩余不确定性：真实 GitHub Actions hosted run 尚未执行；packaged worker exe、NSIS installer artifact、release artifact download verification、signing/auto-update、browser smoke、ontology 和完整 8 golden scenarios 仍待后续推进。
- 影响范围 / 风险控制方式：新增仓库级 opt-in 脚本和 manual/reusable workflow，复用现有合同/Rust/Desktop typecheck；不改变 runtime 业务逻辑、schema、OpenAPI、数据库、worker sidecar 或 release artifact build。
- 验证方式：`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\desktop-package-smoke.ps1` 通过（evidence status `passed`，4 steps passed，0 failed，`packaged_worker_runtime/nsis_installer_artifact/hosted_workflow_run=not_covered`）；Python/PyYAML parser 检查 `.github/workflows/next-desktop-package-smoke.yml` 通过（`workflow_dispatch` / `workflow_call`、`runs-on=windows-latest`、Rust setup/cache、smoke run、upload artifact 均存在，10 steps）；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` 通过（evidence status `passed`，6 steps passed，0 failed，workflow list 包含 `next-desktop-package-smoke.yml`，dirty worktree recorded）；`git diff --check -- .github scripts Justfile README.md docs\architecture docs\rebuild\AutoWaterSimu_Next_Certainty_Elegance_Development_Plan_v1.0.md tasks\todo.md .ai\changes\2026-05-31.md` 通过（仅 LF/CRLF warning），新增文件行尾扫描仅发现 Certainty/Elegance Development Plan header 中既有 Markdown hard-break 空格。
- 是否更新 README：是，更新 root README、`scripts/README.md`、`scripts/ci/README.md`、`.github/README.md`、`.github/workflows/README.md`、`docs/architecture/README.md`、`docs/architecture/local-dev.md`、`docs/architecture/current-state.md`，并新增 `docs/architecture/desktop-runtime.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本轮遵守既有 Desktop release signing / auto-update boundary ADR，不新增 signing、auto-update 或 GitHub Release publication 策略。

## 21:50 - AutoWaterSimu Next desktop package contracts

- 用户目标：继续根据 Certainty/Elegance PRD 与 Development Plan 完善和优化，补齐 Desktop project package/support bundle 合同标准化缺口。
- 涉及目录：`contracts/`、`apps/desktop/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`、`.ai/changes/`。
- 修改内容：新增 `desktop_project_package.v1` 与 `desktop_support_bundle.v1` JSON Schema；新增 Desktop project package/support bundle valid fixtures 和两个 invalid fixtures；登记 `contracts/registry.json` 与 `contracts/codegen/manifest.json`；Desktop runtime 新导出切换到 `desktop_project_package.v1`，同时继续接受 legacy `desktop_project_export.v1` 导入；同步 contracts、Desktop、architecture、Certainty/Elegance Development Plan 和任务记录。
- 修改原因：Certainty/Elegance PRD/Plan 明确把 Desktop package schema 作为质量缺口。现有 Desktop runtime 已有 file-backed package export/import 与 support bundle smoke，但长期合同层还没有对应 schema，且项目包命名仍停留在旧 `desktop_project_export.v1`。
- 本次已消除的不确定性：确认 Desktop runtime 当前包内容包括 project metadata、compute job snapshots、job events、CanvasGraphs、artifact/support bundle refs 以及 checksum-verified hex file records；确认 support bundle payload 已是 `desktop_support_bundle.v1`，但必须约束 `artifact_contents_included=false`；确认新项目包合同可作为新导出格式，同时保留旧包导入兼容。
- 本次采用的关键假设：本轮只标准化合同和新导出 schema 名，不引入 generated Rust DTOs，不改变 package file suffix、SQLite schema、worker sidecar、artifact 文件恢复语义或 Desktop UI 流程；legacy `desktop_project_export.v1` 只作为导入兼容路径保留。
- 剩余不确定性：Rust generated DTO 生成策略、hosted Desktop package export/import evidence、完整 Desktop offline golden scenario、package 合同与未来 installer/release evidence 的联动仍待后续推进。
- 影响范围 / 风险控制方式：合同新增是 additive；运行时代码仅改新导出 schema_version 与导入校验接受集合；通过 contracts tests、contract drift gate、Desktop Rust round-trip tests 和 PR fast lane 控制回归。
- 验证方式：`backend\.venv\Scripts\python -m pytest contracts\tests -q` 通过（99 passed）；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-contracts.ps1` 通过（19 schemas、38 valid examples、14 invalid examples，generated client drift passed）；`cargo test --manifest-path apps\desktop\src-tauri\Cargo.toml` 通过（22 passed）；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` 通过（evidence status `passed`，6 steps passed，0 failed，dirty worktree recorded）；`git diff --check -- contracts apps\desktop docs\architecture docs\rebuild\AutoWaterSimu_Next_Certainty_Elegance_Development_Plan_v1.0.md tasks\todo.md .ai\changes\2026-05-31.md` 通过（仅 LF/CRLF warning），新增文件行尾扫描仅发现 Certainty/Elegance Development Plan header 中既有 Markdown hard-break 空格。
- 是否更新 README：是，更新 `contracts/README.md`、`contracts/examples/README.md`、`contracts/examples/valid/README.md`、`contracts/examples/invalid/README.md`、`apps/desktop/README.md`、`apps/desktop/src-tauri/README.md`、`docs/architecture/contracts.md`、`docs/architecture/current-state.md` 和 Certainty/Elegance Development Plan。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本轮是计划内合同标准化与 runtime 命名对齐，不新增架构取舍。

## 21:34 - AutoWaterSimu Next security smoke

- 用户目标：继续根据 Certainty/Elegance PRD 与 Development Plan 完善和优化，把 production token guard 与现有 scope/revocation 安全检查收束成可重复运行的 security smoke evidence。
- 涉及目录：`scripts/ci/`、`scripts/`、`docs/architecture/`、`docs/rebuild/`、根 `README.md`、`Justfile`、`tasks/`、`.ai/changes/`。
- 修改内容：新增 `scripts/ci/security-smoke.ps1`，聚合 production auth config guard、static token revocation、scope denial、artifact admin scope 和 governance route scope-denial Go tests，并写出 `tmp/ci-evidence/security-smoke.json`；新增 `just check-security`；同步 root README、scripts README、scripts/ci README、architecture local-dev/current-state、Certainty/Elegance Development Plan 和任务记录。
- 修改原因：Certainty/Elegance PRD/Plan 明确需要 security smoke/report。上轮已完成 production default-token startup guard，但安全验证仍散落在 Go tests 里；新增 opt-in evidence 脚本可让后续 CI lane 或本地审核直接看到哪些安全边界已覆盖、哪些仍未覆盖。
- 本次已消除的不确定性：确认现有 Go tests 已覆盖 production token guard、token revocation、基础 scope denial、artifact admin scope 和多条治理 route worker-token denial；确认当前还没有 tenant/project/site data-scope 和 mutation audit event 覆盖，因此 security smoke evidence 必须显式标注 `not_covered`，不能把 security checklist 标成完成。
- 本次采用的关键假设：本轮只建立 security smoke 入口和 evidence，不改变 HTTP routes、auth scope 语义、OpenAPI、contracts、generated client 或数据库 schema；security smoke 暂不进入默认 PR fast lane。
- 剩余不确定性：真实 OIDC/JWKS 或 service-token secret manager、tenant/project/site data-scope、mutation audit event coverage、hosted security workflow 和完整 security scope/audit scenario 仍未完成。
- 影响范围 / 风险控制方式：新增仓库级 opt-in 脚本与 Justfile 入口，复用现有 Go tests，不引入新依赖；evidence 文件写入 `tmp/ci-evidence/`，不提交生成物。
- 验证方式：`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\security-smoke.ps1` 通过（`tmp/ci-evidence/security-smoke.json` status `passed`，3 steps passed，0 failed，`mutation_audit_events=not_covered`，`tenant_project_site_data_scope=not_covered`）；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` 通过（evidence status `passed`，6 steps passed，0 failed，dirty worktree recorded）；`git diff --check -- README.md Justfile scripts docs\architecture docs\rebuild\AutoWaterSimu_Next_Certainty_Elegance_Development_Plan_v1.0.md tasks\todo.md .ai\changes\2026-05-31.md` 通过（仅 LF/CRLF warning）。
- 是否更新 README：是，更新 root README、`scripts/README.md`、`scripts/ci/README.md`、`docs/architecture/local-dev.md`、`docs/architecture/current-state.md` 和 Certainty/Elegance Development Plan。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本轮是验证入口与 evidence 汇总，不新增安全架构策略。

## 21:27 - AutoWaterSimu Next production dev-token guard

- 用户目标：继续根据 Certainty/Elegance PRD 与 Development Plan 完善和优化，推进 production security 中“生产环境不能使用默认 dev token 启动”的最小硬化项。
- 涉及目录：`apps/api/cmd/compute-api/`、`apps/api/internal/compute/`、`apps/api/`、`.env.example`、`docs/architecture/`、`docs/rebuild/`、`tasks/`、`.ai/changes/`。
- 修改内容：`compute.Config` 新增 `Environment`；`cmd/compute-api` 读取 `APP_ENV`，为空时回退 `ENVIRONMENT`；新增 `validateProductionAuthConfig`，在 production 环境拒绝空 `COMPUTE_API_TOKENS_JSON`、invalid JSON、空 token list 和默认开发 token 值（`dev-public-token`、`dev-worker-token`、`dev-admin-token`）；新增 focused Go tests；同步 `.env.example`、API/cmd README、architecture current-state、Certainty/Elegance Development Plan 和任务记录。
- 修改原因：Certainty/Elegance PRD 把 P0 static bearer token 明确限定为开发路径，Phase 2 要求 production 禁止默认 dev token。当前 Compute API 本地默认 token 对开发友好，但如果 production 静默接受空 token config 或默认 dev token，会破坏生产边界。
- 本次已消除的不确定性：确认默认 token 注入发生在 `compute.NewAuthenticator("")`；确认最小安全改动应放在 `cmd/compute-api` 启动接线层，而不是破坏内部 authenticator 的本地开发默认行为；确认 `.env.example` 目前只有 `ENVIRONMENT`，因此 Compute API 需要支持 `APP_ENV` 优先、`ENVIRONMENT` fallback。
- 本次采用的关键假设：本轮只完成 P0 production startup guard；不实现 OIDC/JWT、JWKS、service-token secret manager、RBAC/data-scope、mutation audit 或 security smoke lane；显式配置的非默认静态 token 仍是当前允许的生产过渡形态。
- 剩余不确定性：真实生产身份提供方、secret manager、tenant/project/site scope、mutation audit 和 dedicated security smoke 仍待后续设计与实现；当前 guard 不能证明完整生产权限模型。
- 影响范围 / 风险控制方式：仅启动时配置校验和文档更新；不改变 HTTP routes、OpenAPI、contracts、generated client、数据库 schema、auth scope 语义或本地 integration smoke 默认 token 行为。
- 验证方式：`cd apps\api; go test ./cmd/compute-api -run "TestValidateProductionAuthConfig|TestOpenArchiveStore|TestOpenStore" -count=1` 通过；`cd apps\api; go test ./...` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` 通过（evidence status `passed`，6 steps passed，0 failed，dirty worktree recorded）；`git diff --check -- .env.example apps\api docs\architecture docs\rebuild\AutoWaterSimu_Next_Certainty_Elegance_Development_Plan_v1.0.md tasks\todo.md .ai\changes\2026-05-31.md` 通过（仅 LF/CRLF warning）。
- 是否更新 README：是，更新 `apps/api/README.md`、`apps/api/cmd/compute-api/README.md`、`docs/architecture/current-state.md` 和 Certainty/Elegance Development Plan。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本轮是 PRD 已明确的 P0 hardening guard，不新增长期架构取舍。

## 21:18 - AutoWaterSimu Next hosted integration workflow

- 用户目标：继续根据 Certainty/Elegance PRD 与 Development Plan 完善和优化，把已通过的本地 integration smoke 推进到可触发的 hosted/manual integration lane。
- 涉及目录：`.github/workflows/`、`scripts/ci/`、`scripts/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`、`.ai/changes/`。
- 修改内容：新增 `.github/workflows/next-integration-smoke.yml`，通过 `workflow_dispatch` / `workflow_call` 在 `ubuntu-latest` 上安装 backend Python dependencies、验证 Docker Compose 可用性、运行 `scripts/ci/integration-smoke.ps1 -StartCompose` 并上传 `next-integration-smoke-evidence`；调整 `scripts/ci/integration-smoke.ps1` 的路径拼接，使 repo root、Python 解析、fixture、smoke 目录和 worker CLI 调用在 Windows 与 Ubuntu `pwsh` 下都使用平台路径；同步 workflow README、scripts/ci README、scripts README、architecture local-dev/current-state、Certainty/Elegance Development Plan 和任务记录。
- 修改原因：上一轮已证明本地 Postgres + MinIO + Compute API + host Python worker smoke 可跑通，但 PRD/Plan 要求 CI evidence lane 产品化。先把同一 smoke 暴露为手动/可复用 hosted workflow，比直接放进默认 PR gate 更符合慢速 integration 与 fast lane 分离原则。
- 本次已消除的不确定性：确认 integration workflow 不应进入默认 PR fast lane；确认 Ubuntu runner 是更合适的 Docker Compose integration 承载环境；确认原脚本中的 Windows 路径字符串会削弱 Ubuntu hosted 运行可信度，因此先改为跨平台路径拼接；确认 workflow 只负责编排依赖安装、脚本调用和 artifact upload，业务验证仍留在 `integration-smoke.ps1`。
- 本次采用的关键假设：GitHub hosted runner 具备 Docker Compose；`uv sync` 在 `backend/` 下会创建 `backend/.venv` 供 worker CLI 使用；本轮只定义 hosted/manual workflow，不从本地触发远端 GitHub Actions run，不把 workflow 文件存在等同于 hosted green evidence。
- 剩余不确定性：真实 GitHub Actions hosted run 尚未执行，`integration` checklist 因此仍不能标记完成；frontend/browser reads、packaged worker first-start timing、release artifact、安全、Desktop package schema、ontology 和 8 golden scenarios 仍待后续推进。
- 影响范围 / 风险控制方式：新增 opt-in workflow 和跨平台路径兼容，不改变 OpenAPI、contracts、generated client、数据库 schema、生产 auth 或运行时业务逻辑；不修改默认 PR fast lane 触发范围。
- 验证方式：`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\integration-smoke.ps1 -StartCompose` 通过（跨平台路径改动后最新 evidence status `passed`，9 steps passed，0 failed；main job `job_integration_20260531131955`，model_run `mr_job_integration_20260531131955_material_balance`，artifact `art_job_integration_20260531131955_time_series`，archive candidate `art_job_archive_20260531131955_archive_candidate`）；Python/PyYAML parser 检查 `.github/workflows/next-integration-smoke.yml` 通过（required keys present，`runs-on=ubuntu-latest`，7 steps，upload-artifact step present）；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` 通过（evidence status `passed`，6 steps passed，0 failed，workflow file list 包含 `next-integration-smoke.yml`，dirty worktree recorded）；`docker compose -p autowatersimu-next-integration-smoke -f docker-compose.dev.yml ps` 确认 smoke 后无运行服务；`git diff --check -- .github\workflows scripts docs\architecture docs\rebuild\AutoWaterSimu_Next_Certainty_Elegance_Development_Plan_v1.0.md tasks\todo.md .ai\changes\2026-05-31.md` 通过（仅 LF/CRLF warning）。
- 是否更新 README：是，更新 `.github/workflows/README.md`、`scripts/ci/README.md`、`scripts/README.md`、`docs/architecture/local-dev.md`、`docs/architecture/current-state.md` 和 Certainty/Elegance Development Plan。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本轮延续现有 fast/integration/release lane 分离策略，不新增 ADR。

## 21:10 - AutoWaterSimu Next integration smoke

- 用户目标：继续根据 Certainty/Elegance PRD 与 Development Plan 完善和优化，补齐 Postgres + MinIO + Worker integration smoke 证据缺口。
- 涉及目录：`scripts/ci/`、`scripts/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`、根 `README.md`、`Justfile`、`docker-compose.dev.yml`、`.ai/changes/`。
- 修改内容：新增 `scripts/ci/integration-smoke.ps1`；新增 `just integration-smoke`；将 `docker-compose.dev.yml` 的 Compute API source-mounted 启动命令调整为 `sh -c "cd apps/api && go run ./cmd/compute-api"`；同步 `scripts/ci/README.md`、`scripts/README.md`、`docs/architecture/local-dev.md`、`docs/architecture/current-state.md`、Certainty/Elegance Development Plan、根 README 和任务记录。
- 修改原因：Certainty/Elegance Phase 1 明确需要真实本地栈 smoke，不应只停留在 compose config、单元测试或 in-memory store。当前最小高价值路径是证明 PostgreSQL + MinIO + Go Compute API + worker HTTP bridge 能跑通关键 job lifecycle、artifact、evidence、retention 和 metrics。
- 本次已消除的不确定性：确认隔离 Compose project 可启动 PostgreSQL、MinIO、MinIO bucket init 和 source-mounted Compute API；确认 material-balance job 可经 host Python worker API once 完成，并持久化 model_run、上传 artifact、导出 evidence package、通过 archive_candidate retention dry-run、反映到 metrics；确认 `golang:1.26` 镜像下 `sh -lc` 会丢失 `go` PATH，`sh -c` 可稳定启动；确认 PowerShell `Invoke-WebRequest` 手写 multipart 对该接口不可靠，改用 .NET `HttpWebRequest` raw multipart 后通过。
- 本次采用的关键假设：本轮 smoke 使用本地 `backend/.venv` Python worker CLI 来验证 worker HTTP bridge，避免把 worker 容器首次 pip 安装耗时混入主验收；它证明的是 API + durable metadata + object-store archive candidate + worker 协议链路，不证明 frontend/browser reads、hosted CI workflow、packaged worker、release artifact、security scope/audit 或 Desktop package。
- 剩余不确定性：`integration` hosted workflow 尚未接入；frontend/browser 读取 job/result/evidence 尚未纳入 smoke；full compose `simulation-worker` 容器 first-start timing、release evidence、nightly、安全、ontology、Desktop package schema 和 8 golden scenarios 仍待后续计划推进。
- 影响范围 / 风险控制方式：新增 opt-in 脚本和文档入口；默认使用独立 compose project `autowatersimu-next-integration-smoke`，结束时执行 `down -v --remove-orphans`；不改变 OpenAPI、contracts、generated client、数据库 schema、生产 auth、worker payload 或业务计算逻辑。
- 验证方式：`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\integration-smoke.ps1 -StartCompose` 通过（最新 `tmp/ci-evidence/integration-smoke.json` status `passed`，9 steps passed，0 failed，compose cleaned down；main job `job_integration_20260531131136`，model_run `mr_job_integration_20260531131136_material_balance`，artifact `art_job_integration_20260531131136_time_series`，archive candidate `art_job_archive_20260531131136_archive_candidate`）；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` 通过（evidence status `passed`，6 steps passed，0 failed，dirty worktree recorded）；`git diff --check -- README.md Justfile docker-compose.dev.yml scripts docs\architecture docs\rebuild\AutoWaterSimu_Next_Certainty_Elegance_Development_Plan_v1.0.md tasks\todo.md .ai\changes\2026-05-31.md` 通过（仅 LF/CRLF warning）。
- 是否更新 README：是，更新 `scripts/ci/README.md`、`scripts/README.md`、`docs/architecture/local-dev.md`、`docs/architecture/current-state.md` 和根 `README.md`；未新增或改变目录职责外的运行时代码 README。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本轮是计划内 evidence lane 落地，不新增长期架构取舍。

## 20:22 - AutoWaterSimu Next metrics service boundary

- 用户目标：继续根据 Certainty/Elegance PRD 与 Development Plan 推进 Go Compute API service constructor narrowing，把 `/metrics` snapshot read 从主 `Service` 收窄到 metrics boundary。
- 涉及目录：`apps/api/`、`apps/api/internal/compute/`、`scripts/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`、`.ai/changes/`。
- 修改内容：新增 `apps/api/internal/compute/metrics.go`；引入 `MetricsService`，构造函数只接收 `MetricsStore` 和 clock；`Service.Metrics` 保持公开入口不变并委派到窄服务；审计脚本补充 `metrics` 窄仓储字段统计；同步 Compute API README、architecture current-state、Certainty/Elegance checklist 和任务记录。
- 修改原因：上一轮完成 job lifecycle constructor narrowing 后，当前架构文档明确 metrics 是剩余最小、只读、低风险的 service-boundary candidate。先收窄它可以完成当前 Compute API Store 领域调用来源集中化，为后续 package movement 和公共构造签名收窄做准备。
- 本次已消除的不确定性：确认 metrics 只需要 `MetricsStore` 和 clock；确认 `/metrics` Prometheus rendering 仍在 HTTP 层，不需要改变 metric names、OpenAPI、auth、contracts、generated client、migrations 或数据库 schema；确认 audit evidence 能统计 `metrics.go` 中的 metrics Store 调用来源。
- 本次采用的关键假设：本轮只收窄内部服务构造函数；不改变 public constructor signatures，不改变 metrics endpoint 行为，不触发 retention sweep、timeout sweep、job mutation、artifact mutation 或 archive mutation。
- 剩余不确定性：`NewService` / `NewServiceWithArchive` 仍从 aggregate `Store` 统一接线；Go package split、handler/package surface reduction、public constructor signature narrowing、integration smoke、browser/release evidence、ontology 和 Desktop package schema 仍未完成。
- 影响范围 / 风险控制方式：移动既有 metrics snapshot read 到新内部服务并保持 `Service.Metrics` 兼容；无数据库、wire shape、auth scope 或 metric name 改动；通过 focused metrics HTTP test 和边界审计验证。
- 验证方式：`cd apps\api; go test ./internal/compute -run "TestHTTPAuthScopeAndMetrics" -count=1` 通过；`cd apps\api; go test ./...` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1` 通过（41 resolved Store methods、12 embedded interfaces、12 domain groups，`service.go` 922 audit lines，`metrics.go` 25 audit lines，metrics Store call 来源为 `metrics.go`）；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` 通过（evidence status `passed`，6 steps passed，0 failed，dirty worktree recorded）；`git diff --check -- apps\api docs scripts tasks\todo.md .ai\changes\2026-05-31.md` 通过（仅 LF/CRLF warning）。
- 是否更新 README：是，更新 `apps/api/README.md`、`apps/api/internal/compute/README.md`，并同步 `docs/architecture/compute-api.md`、`docs/architecture/current-state.md` 与 Certainty/Elegance 开发计划。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本轮延续既有 modular monolith / narrow interface 分阶段路线，不新增 ADR。

## 20:14 - AutoWaterSimu Next job lifecycle service boundary

- 用户目标：继续根据 Certainty/Elegance PRD 与 Development Plan 推进 Go Compute API service constructor narrowing，把 job create/list/read/events/cancel/complete/fail/timeout 从主 `Service` 收窄到 job lifecycle 边界。
- 涉及目录：`apps/api/`、`apps/api/internal/compute/`、`scripts/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`、`.ai/changes/`。
- 修改内容：新增 `apps/api/internal/compute/job_lifecycle.go`；引入 `JobLifecycleService`，构造函数接收 `JobStore`、`ModelRunStore`、contract validator、clock 和 artifact listing callback；`Service.CreateJob`、`GetJob`、`ListJobs`、`Events`、`CancelJob`、`Complete`、`Fail`、`TimeoutSweep` 保持公开入口不变并委派到窄服务；同步 Compute API README、architecture current-state、Certainty/Elegance checklist、审计推荐顺序和任务记录。
- 修改原因：上一轮完成 evidence governance constructor narrowing 后，当前架构文档明确剩余直接留在主 `Service` 的核心路径为 job lifecycle 和 metrics。job lifecycle 是新增 endpoint 和 worker 完成路径最容易继续堆进主服务的复杂来源，先收窄它能更直接满足 Phase 1 “新增 API endpoint 不再需要修改巨型 service 文件”的方向。
- 本次已消除的不确定性：确认 job lifecycle 需要 `JobStore` 管理 create/list/events/cancel/complete/timeout，`ModelRunStore` 持久化 valid worker result 中的 `model_run.v1`，artifact listing callback 组装 snapshot；确认 artifact object write、evidence package generation、model catalog mutation 不应进入 job boundary；确认 public HTTP/API/OpenAPI/auth/migration/contract 行为无需改变。
- 本次采用的关键假设：本轮只收窄内部服务构造函数；不改变 idempotency 语义、list pagination、event records、stale worker handling、compute_result validation、model_run persistence rules、timeout behavior、OpenAPI、generated client、migrations 或 auth scopes。
- 剩余不确定性：下一批 constructor narrowing 应处理 metrics；`NewService` / `NewServiceWithArchive` 仍从 aggregate `Store` 统一接线；Go package split 仍未开始。
- 影响范围 / 风险控制方式：移动既有 job lifecycle 逻辑到新内部服务并保持 `Service` 公开方法兼容；无数据库或 wire shape 改动；通过 focused job/worker/evidence tests 和边界审计验证。
- 验证方式：`cd apps\api; go test ./internal/compute -run "Test(CreateJob|CancelRejectsLateResult|ValidatedWorkerFailPersistsTerminalResult|TimeoutSweep|WorkerLifecycleArtifactSucceedAndDownload|ValidatedCompletePersistsModelRun|NewSystemEvidenceReferenceE2E)" -count=1` 通过；`cd apps\api; go test ./...` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1` 通过（41 resolved Store methods、12 embedded interfaces、12 domain groups，`service.go` 920 audit lines，`job_lifecycle.go` 252 audit lines）；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` 通过（evidence status `passed`，6 steps passed，0 failed，dirty worktree recorded）；`git diff --check -- apps\api docs scripts tasks\todo.md .ai\changes\2026-05-31.md` 通过（仅 LF/CRLF warning）。
- 是否更新 README：是，更新 `apps/api/README.md`、`apps/api/internal/compute/README.md`，并同步 `docs/architecture/compute-api.md`、`docs/architecture/current-state.md` 与 Certainty/Elegance 开发计划。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本轮延续既有 modular monolith / narrow interface 分阶段路线，不新增 ADR。

## 20:06 - AutoWaterSimu Next evidence governance service boundary

- 用户目标：继续根据 Certainty/Elegance PRD 与 Development Plan 推进 Go Compute API service constructor narrowing，把 result read、evidence package、production readiness 和 evidence-ref resolution 从主 `Service` 收窄到 evidence governance 边界。
- 涉及目录：`apps/api/`、`apps/api/internal/compute/`、`scripts/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`、`.ai/changes/`。
- 修改内容：新增 `apps/api/internal/compute/evidence_governance.go`；引入 `EvidenceGovernanceService`，构造函数接收 `JobStore`、`ModelRunStore`、`ProcessGraphStore`、contract validator、clock、model catalog resolver callback、artifact listing callback 和 artifact metadata callback；`Service.Result`、`Service.EvidencePackage`、`Service.ProductionReadiness`、`Service.ResolveEvidenceReference` 保持公开入口不变并委派到窄服务；同步 Compute API README、architecture current-state、Certainty/Elegance checklist、审计推荐顺序和任务记录。
- 修改原因：上一轮完成 worker lifecycle constructor narrowing 后，当前架构文档明确剩余直接留在主 `Service` 的路径为 job lifecycle、evidence governance 和 metrics。evidence governance 是读模型清晰、可通过现有 E2E 测试覆盖的下一批边界，先收窄它能进一步降低主 `Service` 复杂度，同时避免直接触碰 job mutation。
- 本次已消除的不确定性：确认 result/evidence/readiness/evidence-ref 路径需要 job read/events、model run read/list、process graph read、artifact list/metadata 和 model catalog read；确认该服务应保持 read-only，不修改 job、artifact、model catalog snapshot 或 approval state；确认 public HTTP/API/OpenAPI/auth/migration/contract 行为无需改变。
- 本次采用的关键假设：本轮只收窄内部服务构造函数；不改变 evidence package schema、production readiness policy、supported evidence-ref formats、checksum generation、auth scopes、OpenAPI、generated client、migrations、artifact lifecycle 或 model governance mutation 语义。
- 剩余不确定性：下一批 constructor narrowing 应优先 job lifecycle 还是 metrics；`NewService` / `NewServiceWithArchive` 仍从 aggregate `Store` 统一接线。
- 影响范围 / 风险控制方式：移动既有读路径逻辑到新内部服务并保持 `Service` 公开方法兼容；无数据库或 wire shape 改动；通过 focused evidence/readiness tests 和边界审计验证。
- 验证方式：`cd apps\api; go test ./internal/compute -run "Test(NewSystemEvidenceReferenceE2E|ProductionReadiness|ValidatedCompletePersistsModelRun|RiskFindings|HTTPAuthScopeAndMetrics)" -count=1` 通过；`cd apps\api; go test ./...` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1` 通过（41 resolved Store methods、12 embedded interfaces、12 domain groups，`service.go` 1107 audit lines，`evidence_governance.go` 639 audit lines）；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` 通过（evidence status `passed`，6 steps passed，0 failed，dirty worktree recorded）；`git diff --check -- apps\api docs scripts tasks\todo.md .ai\changes\2026-05-31.md` 通过（仅 LF/CRLF warning）。
- 是否更新 README：是，更新 `apps/api/README.md`、`apps/api/internal/compute/README.md`，并同步 `docs/architecture/compute-api.md`、`docs/architecture/current-state.md` 与 Certainty/Elegance 开发计划。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本轮延续既有 modular monolith / narrow interface 分阶段路线，不新增 ADR。

## 19:55 - AutoWaterSimu Next worker lifecycle service boundary

- 用户目标：继续根据 Certainty/Elegance PRD 与 Development Plan 推进 Go Compute API service constructor narrowing，把 worker register / claim / heartbeat 从主 `Service` 收窄到 worker lifecycle 边界。
- 涉及目录：`apps/api/`、`apps/api/internal/compute/`、`scripts/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`、`.ai/changes/`。
- 修改内容：新增 `apps/api/internal/compute/worker_lifecycle.go`；引入 `WorkerLifecycleService`，构造函数只接收 `WorkerStore` 和 clock；`Service.RegisterWorker`、`Service.Claim`、`Service.Heartbeat` 保持公开入口不变并委派到窄服务；审计脚本补充 `workers` 窄仓储字段统计；同步 Compute API README、architecture current-state、Certainty/Elegance checklist 和任务记录。
- 修改原因：上一轮已完成 artifact upload/listing boundary 后，worker register/claim/heartbeat 是推荐拆分顺序中最小、边界清晰的剩余领域之一。先收窄 worker lifecycle 可以继续降低 `service.go` 复杂度，同时避免直接触碰 job completion、artifact upload 或 metrics 行为。
- 本次已消除的不确定性：确认 worker register/claim/heartbeat 只需要 `WorkerStore` 和 clock；确认 artifact upload 与 worker succeed/fail completion 依赖 job/artifact lifecycle，暂不放入 worker boundary；确认 public HTTP/API/OpenAPI/auth/migration/contract 行为无需改变；确认 audit evidence 能统计 `worker_lifecycle.go` 中的 worker Store 调用来源。
- 本次采用的关键假设：本轮只收窄内部服务构造函数；不改变 worker wire payload、capability matching、lease duration、claim response shape、heartbeat cancellation/terminal semantics、OpenAPI、generated client、migrations、auth scopes、artifact upload 或 job completion/failure 语义。
- 剩余不确定性：下一批 constructor narrowing 应优先 jobs + metrics 还是 evidence governance；`NewService` / `NewServiceWithArchive` 仍从 aggregate `Store` 统一接线。
- 影响范围 / 风险控制方式：移动既有逻辑到新内部服务并保持 `Service` 公开方法兼容；审计脚本只读输出 evidence；无数据库或 wire shape 改动；保留 worker artifact upload 与 complete/fail 在现有路径。
- 验证方式：`cd apps\api; go test ./internal/compute -run "Test(WorkerLifecycleArtifactSucceedAndDownload|WorkerClaim|ValidatedWorkerFailPersistsTerminalResult|HTTPArtifactUploadMultipart|NewSystemEvidenceReferenceE2E)" -count=1` 通过；`cd apps\api; go test ./...` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1` 通过（41 resolved Store methods、12 embedded interfaces、12 domain groups，`service.go` 1659 audit lines，`worker_lifecycle.go` 84 audit lines，worker Store calls 4 且来源集中到 `worker_lifecycle.go`）；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` 通过（evidence status `passed`，6 steps passed，0 failed，dirty worktree recorded）；`git diff --check -- apps\api docs scripts tasks\todo.md .ai\changes\2026-05-31.md` 通过（仅 LF/CRLF warning）。
- 是否更新 README：是，更新 `apps/api/README.md`、`apps/api/internal/compute/README.md`，并同步 `docs/architecture/compute-api.md`、`docs/architecture/current-state.md` 与 Certainty/Elegance 开发计划。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本轮延续既有 modular monolith / narrow interface 分阶段路线，不新增 ADR。

## 19:46 - AutoWaterSimu Next artifact upload/listing service boundary

- 用户目标：继续根据 Certainty/Elegance PRD 与 Development Plan 推进 Go Compute API service constructor narrowing，把 artifact upload/listing 纳入已存在的 artifact lifecycle 边界。
- 涉及目录：`apps/api/`、`apps/api/internal/compute/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`、`.ai/changes/`。
- 修改内容：扩展 `ArtifactLifecycleService`，构造函数新增 `JobStore` 与 contract validator；将 artifact upload validation/write/metadata insertion、job artifact listing、artifact metadata lookup、download、retention sweep、archive copy/checksum/delete 集中在 `artifact_lifecycle.go`；`Service.UploadArtifact`、job snapshot/list artifact assembly 和 artifact evidence-ref metadata lookup 改为委派到 artifact boundary；同步 Compute API README、architecture current-state、Certainty/Elegance checklist 和任务记录。
- 修改原因：上一轮完成 model governance constructor narrowing 后，artifact upload/listing 仍是推荐拆分顺序中尚未纳入 artifact 边界的遗留路径。先把 artifact 领域的 Store 访问集中到一个窄服务，比直接拆 worker 或 jobs 更低风险，并为后续 package split 保留稳定边界。
- 本次已消除的不确定性：确认 artifact upload 需要 `JobStore` 校验 running worker/job、`ArtifactMetadataStore` 记录 metadata、hot object store 写入 bytes、contract validator 校验 `artifact.v1`、clock 生成事件时间；确认 job artifact listing 与 artifact evidence-ref metadata lookup 可复用同一 artifact boundary；确认 public HTTP/API/OpenAPI/auth/migration/contract 行为无需改变。
- 本次采用的关键假设：本轮只收窄内部服务构造函数；不改变 multipart upload wire shape、artifact metadata schema、retention/archive 语义、download archive fallback、auth scopes、OpenAPI、generated client 或数据库 schema。
- 剩余不确定性：下一批 constructor narrowing 应先处理 worker lifecycle，还是 jobs + metrics；`NewService` / `NewServiceWithArchive` 仍从 aggregate `Store` 统一接线。
- 影响范围 / 风险控制方式：移动既有逻辑到 `ArtifactLifecycleService` 并保持 `Service` 公开方法兼容；无数据库或 wire shape 改动；通过 focused artifact tests 和边界审计验证。
- 验证方式：`cd apps\api; go test ./internal/compute -run "Test(WorkerLifecycleArtifactSucceedAndDownload|ArtifactRetention|HTTPArtifactUploadMultipart|NewSystemEvidenceReferenceE2E)" -count=1` 通过；`cd apps\api; go test ./...` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1` 通过（41 resolved Store methods、12 embedded interfaces、12 domain groups，`service.go` 1706 lines，artifact metadata Store calls 来源集中到 `artifact_lifecycle.go`）；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` 通过（evidence status `passed`，6 steps passed）；`git diff --check -- apps\api docs scripts tasks\todo.md .ai\changes\2026-05-31.md` 通过（仅 LF/CRLF warning）。
- 是否更新 README：是，更新 `apps/api/README.md`、`apps/api/internal/compute/README.md`，并同步 `docs/architecture/compute-api.md`、`docs/architecture/current-state.md` 与 Certainty/Elegance 开发计划。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本轮延续既有 modular monolith / narrow interface 分阶段路线，不新增 ADR。

## 19:40 - AutoWaterSimu Next model governance service boundary

- 用户目标：继续根据 Certainty/Elegance PRD 与 Development Plan 推进 Go Compute API service constructor narrowing，处理审计推荐的 model catalog + benchmark run + model run 路径。
- 涉及目录：`apps/api/`、`apps/api/internal/compute/`、`scripts/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`、`.ai/changes/`。
- 修改内容：新增 `apps/api/internal/compute/model_governance.go`；引入 `ModelGovernanceService`，构造函数只接收 `ModelCatalogStore`、`BenchmarkRunStore`、`ModelRunStore`、contract validator、clock、simulation input resolver、compute job creation callback 和 evidence resolver callback；`Service` 保持公开入口不变并委派 model catalog、default parameter set status / promotion plan / promote-approved、benchmark case scheduling、benchmark run、model run read/list 方法；审计脚本补充 `catalogs` 与 `benchmarkRuns` 窄仓储字段统计。
- 修改原因：上一轮已完成 draft/result explanation constructor narrowing，但 model governance 路径仍由主 `Service` 直接持有 catalog、benchmark run、model run 的 Store 访问和业务编排。按计划继续收窄小领域服务边界，可以降低 `service.go` 复杂度，并为后续 package split 保留稳定行为边界。
- 本次已消除的不确定性：确认 model governance 只需要 model catalog、benchmark run、model run 三类 store，加上既有 simulation input resolution、job creation、evidence resolver callback；确认 public HTTP/API/OpenAPI/auth/migration/contract 行为无需改变；确认 audit evidence 能统计 `model_governance.go` 中的 `catalogs`、`benchmarkRuns`、`modelRuns` Store 调用来源。
- 本次采用的关键假设：本轮只收窄内部服务构造函数；不改变 contracts、OpenAPI、generated client、migrations、auth scopes、database schema、benchmark case queueing 语义、default parameter set promotion 语义或 benchmark run evidence ref 校验语义。
- 剩余不确定性：下一批 constructor narrowing 应先把 artifact upload/listing 纳入 artifact 边界，还是先处理 worker lifecycle 或 jobs + metrics；`NewService` / `NewServiceWithArchive` 仍从 aggregate `Store` 统一接线。
- 影响范围 / 风险控制方式：移动既有逻辑到新内部服务并保持 `Service` 公开方法兼容；审计脚本只读输出 evidence；无数据库或 wire shape 改动。
- 验证方式：`cd apps\api; go test ./internal/compute -run "Test(ModelCatalog|DefaultParameterSetPromotionPlanEndpoint|BenchmarkCaseScheduleRunEndpoint|ValidatedCompletePersistsModelRun|NewSystemEvidenceReferenceE2E)" -count=1` 通过；`cd apps\api; go test ./...` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1` 通过（41 resolved Store methods、12 embedded interfaces、12 domain groups）；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` 通过（evidence status `passed`，6 steps passed）；`git diff --check -- apps\api docs scripts tasks\todo.md .ai\changes\2026-05-31.md` 通过（仅 LF/CRLF warning）。
- 是否更新 README：是，更新 `apps/api/README.md`、`apps/api/internal/compute/README.md`、`scripts/README.md`，并同步 `docs/architecture/compute-api.md`、`docs/architecture/current-state.md` 与 Certainty/Elegance 开发计划。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本轮延续既有 modular monolith / narrow interface 分阶段路线，不新增 ADR。

## 19:23 - AutoWaterSimu Next draft and explanation service boundary

- 用户目标：继续根据 Certainty/Elegance PRD 与 Development Plan 推进 Go Compute API service constructor narrowing，处理审计推荐的 draft confirmation + result explanation 路径。
- 涉及目录：`apps/api/`、`apps/api/internal/compute/`、`scripts/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`、`.ai/changes/`。
- 修改内容：新增 `apps/api/internal/compute/draft_workflows.go`、`result_explanations.go` 与 `contract_validation.go`；引入 `DraftWorkflowService`，构造函数只接收 `DraftConfirmationStore`、contract validator、clock 和 simulation-check creation callback；引入 `ResultExplanationService`，构造函数只接收 `ResultExplanationStore`、`JobStore`、contract validator、clock 和 evidence resolver callback；`Service` 保持公开入口不变并委派 draft confirmation / constraint plan / promotion / result explanation 方法；审计脚本收紧为只统计 aggregate/narrow repository 字段上的 resolved Store method，避免把公开 service delegate 误算为 Store 调用。
- 修改原因：上一轮已完成 simulation input/process graph constructor narrowing，但 draft confirmation、Agent draft promotion 和 result explanation 仍直接堆在主 `Service` 中。按计划继续收窄小领域服务边界，可以降低 `service.go` 复杂度，并为后续 package split 保留稳定行为边界。
- 本次已消除的不确定性：确认 draft workflow 只需要 draft confirmation store、validator、clock 和既有 simulation-check creation callback；确认 result explanation workflow 只需要 result explanation store、job read、validator、clock 和既有 evidence resolver；确认 public HTTP/API/OpenAPI 行为无需改变；确认 audit evidence 不再把 `server.service.*` 或 `svc.resultExplanations.*` 误计为 Store 调用。
- 本次采用的关键假设：本轮只收窄内部服务构造函数；不改变 contracts、OpenAPI、generated client、migrations、auth scopes、database schema、draft promotion 语义或 result explanation evidence ref 语义。
- 剩余不确定性：下一批 constructor narrowing 应先做 model catalog + benchmark run + model run，还是先把 artifact upload/listing 纳入 artifact 边界；HTTP handler 仍有部分 public service delegate 调用，但不应被审计为 Store 调用。
- 影响范围 / 风险控制方式：移动既有逻辑到新内部服务并保持 `Service` 公开方法兼容；审计脚本只读输出 evidence；无数据库或 wire shape 改动。
- 验证方式：`cd apps\api; go test ./internal/compute -run "Test(Contract|Draft|Constraint|ResultExplanation|NewSystemEvidenceReferenceE2E)" -count=1` 通过；`cd apps\api; go test ./...` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1` 通过（41 resolved Store methods、12 embedded interfaces、12 domain groups）；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` 通过（evidence status `passed`，6 steps passed）；`git diff --check -- apps\api docs scripts tasks\todo.md .ai\changes\2026-05-31.md` 通过（仅 LF/CRLF warning）。
- 是否更新 README：是，更新 `apps/api/README.md`、`apps/api/internal/compute/README.md`、`scripts/README.md`，并同步 `docs/architecture/compute-api.md`、`docs/architecture/current-state.md` 与 Certainty/Elegance 开发计划。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本轮延续既有 modular monolith / narrow interface 分阶段路线，不新增 ADR。

## 19:09 - AutoWaterSimu Next simulation input service boundary

- 用户目标：继续根据 Certainty/Elegance PRD 与 Development Plan 推进 Go Compute API service constructor narrowing，处理审计推荐的 simulation input + process graph 路径。
- 涉及目录：`apps/api/`、`apps/api/internal/compute/`、`scripts/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`、`.ai/changes/`。
- 修改内容：新增 `apps/api/internal/compute/simulation_inputs.go`；引入 `SimulationInputService`，构造函数只接收 `SimulationInputStore`、`ProcessGraphStore`、`ModelRunStore`、`JobStore`、contract validator 和 clock；`Service.RegisterSimulationInput`、`GetSimulationInput`、`RegisterProcessGraph`、`GetProcessGraph` 保持公开入口不变并委派到窄服务；`CreateSimulationCheck` 与 benchmark case scheduling 通过 `SimulationInputService.ResolveSimulationInput` 处理 inline input、registered input id、process graph id 与 model_run replay；审计脚本改为按 resolved Store method 过滤跨服务文件的调用来源，避免窄服务字段名导致漏计。
- 修改原因：上一轮已完成 artifact lifecycle constructor narrowing，但 simulation input / process graph 仍由主 `Service` 直接持有聚合 Store 逻辑；将其抽到窄服务能继续降低主服务复杂度，并让审计证据反映 service-boundary 文件中的 Store 调用来源。
- 本次已消除的不确定性：确认 simulation input / process graph 路径需要 `SimulationInputStore`、`ProcessGraphStore`，model_run replay 还需要 `ModelRunStore` 与 `JobStore`；确认 public HTTP/API/OpenAPI 行为无需改变；确认 audit evidence 现在能记录 `simulation_inputs.go` 与 `artifact_lifecycle.go` 中的 Store 方法来源。
- 本次采用的关键假设：本轮只收窄内部服务构造函数；不改变 contracts、OpenAPI、generated client、migrations、auth scopes、database schema 或 ProcessGraph material-balance-only 语义；model_run replay 仍只复用源 job 的 `simulation_input.v1` payload。
- 剩余不确定性：后续应先拆 draft confirmation + result explanation，还是先把 artifact upload/listing 也纳入 artifact 边界；HTTP handler 中仍有部分直接 Store 调用，是否先服务化需后续切片评估。
- 影响范围 / 风险控制方式：移动既有逻辑到新内部服务并保持 `Service` 公开方法兼容；审计脚本只读输出 evidence；无数据库或 wire shape 改动。
- 验证方式：`cd apps\api; go test ./internal/compute -run "Test(ProcessGraph|SimulationInput|ModelRunReplay|NewSystemEvidenceReferenceE2E|DefaultParameterSetPromotionPlanEndpoint)" -count=1` 通过；`cd apps\api; go test ./...` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1` 通过（41 resolved Store methods、12 embedded interfaces、12 domain groups）；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` 通过（evidence status `passed`，6 steps passed）；`git diff --check -- apps\api docs scripts tasks\todo.md .ai\changes\2026-05-31.md` 通过（仅 LF/CRLF warning）。
- 是否更新 README：是，更新 `apps/api/README.md`、`apps/api/internal/compute/README.md`、`scripts/README.md`，并同步 `docs/architecture/compute-api.md`、`docs/architecture/current-state.md` 与 Certainty/Elegance 开发计划。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本轮延续既有 modular monolith / narrow interface 分阶段路线，不新增 ADR。

## 18:36 - AutoWaterSimu Next Compute API boundary audit

- 用户目标：继续根据 Certainty/Elegance PRD 与 Development Plan 推进 Go Compute API modular monolith / Store interface 拆分前置工作。
- 涉及目录：`apps/api/`、`apps/api/internal/compute/`、`docs/architecture/`、`docs/rebuild/`、`scripts/`、`tasks/`、`.ai/changes/`。
- 修改内容：新增 `scripts/audit-compute-api-boundary.ps1` 和 `just audit-compute-api`；脚本输出 `tmp/architecture-evidence/compute-api-boundary.json`，记录 `Store` 方法数、领域分组、service 调用次数、MemoryStore/PostgresStore 实现覆盖和大文件信号；新增 `docs/architecture/compute-api.md`；更新 apps/api/internal compute/architecture/scripts/current-state README、Certainty/Elegance checklist 和任务记录。
- 修改原因：Development Plan 的 Phase 1 要求拆分 Go API modular monolith 与 Store interface；直接拆运行时代码风险较高，先建立可重复审计基线可以明确第一拆分顺序和验收范围。
- 本次已消除的不确定性：确认当前 `Store` interface 有 41 个方法，分布在 12 个 candidate domains；确认 MemoryStore 和 PostgresStore 均覆盖全部 Store 方法；确认 `service.go`、`postgres.go`、`http.go`、`store.go` 和 `service_test.go` 是当前大文件 split signals；确认第一推荐拆分域是 `artifacts` + `archive_metadata`。
- 本次采用的关键假设：本轮只做只读架构审计和文档化，不移动 Go package、不拆接口、不改变 endpoint/OpenAPI/auth/contract/database 行为。
- 剩余不确定性：真正 Store interface split 的代码迁移顺序、测试切分、PostgreSQL implementation 文件拆分边界和是否新增内部子 package 仍需后续实现验证。
- 影响范围 / 风险控制方式：新增脚本只读扫描 Go 文件并写入 `tmp/` evidence；不修改 runtime Go 代码；文档明确审计不是拆分完成证明。
- 验证方式：`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1` 通过；`cd apps\api; go test ./...` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` 通过，evidence status=`passed`，6 steps passed，0 failed，dirty worktree 已记录。
- 是否更新 README：是，新增 `docs/architecture/compute-api.md`，更新 `apps/api/README.md`、`apps/api/internal/compute/README.md`、`docs/architecture/README.md`、`docs/architecture/local-dev.md`、`docs/architecture/current-state.md`、`scripts/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮是拆分前审计；真正引入新的 Go package / Store interface 边界时再补 ADR 或实现记录。

## 18:26 - AutoWaterSimu Next contracts codegen policy

- 用户目标：继续根据 Certainty/Elegance PRD 与 Development Plan 完善 AutoWaterSimu Next 的 contracts/codegen 确定性，避免多语言类型生成策略停留在隐含状态。
- 涉及目录：`contracts/`、`contracts/codegen/`、`docs/architecture/`、`docs/rebuild/`、`scripts/`、`tasks/`、`.ai/changes/`。
- 修改内容：新增 `contracts/codegen/README.md` 与 `contracts/codegen/manifest.json`；manifest 覆盖 17 个 registry schema，并记录 TypeScript/OpenAPI/Go/Python/Rust 五类 target decision；新增 `docs/architecture/contracts.md`；`scripts/check-contracts.ps1` 新增 codegen manifest 覆盖校验；`scripts/check-deps.ps1` 对 `contracts/codegen/manifest.json` 做 metadata 扫描豁免；更新 contracts/architecture/scripts/current-state README、Certainty/Elegance checklist 和任务记录。
- 修改原因：Development Plan 要求新增或整理 `contracts/codegen/` 并决定 Go / Python / Rust 类型是生成还是继续 runtime schema validation；如果只保留 registry，会让 codegen 策略继续隐含，后续容易误以为多语言生成已经完成。
- 本次已消除的不确定性：确认当前 TypeScript 生成路径是 OpenAPI Compute client；确认 OpenAPI 是 API-facing projection；确认 Go/Python/Rust 暂不提交生成类型，分别继续使用 API-owned DTO/runtime schema validation、transform helpers/worker tests、Desktop-owned Tauri/package DTO 和 Rust tests；确认新 schema 必须同时更新 codegen manifest。
- 本次采用的关键假设：本轮将 `contracts/codegen/` 定义为策略与覆盖清单目录，不提交生成产物，不替换 runtime JSON Schema validation，也不改变现有 Go/Python/Rust DTO。
- 剩余不确定性：未来是否引入具体 Go/Python/Rust generator、输出目录、review 噪音控制、Rust/Desktop package schema 绑定方式仍需后续设计或 ADR。
- 影响范围 / 风险控制方式：不修改合同 schema、OpenAPI、runtime 代码或 generated client 语义；manifest 只作为可校验策略文件；dependency scan 豁免仅限 metadata manifest。
- 验证方式：`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-contracts.ps1` 通过，codegen manifest 覆盖 17 schemas / 5 target decisions，contract tests `91 passed`；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` 通过，evidence status=`passed`，6 steps passed，0 failed，dirty worktree 已记录；`frontend/src/client/compute` 无 generated content diff。
- 是否更新 README：是，新增 `contracts/codegen/README.md` 并更新 `contracts/README.md`、`docs/architecture/README.md`、`docs/architecture/current-state.md`、`scripts/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮记录当前 codegen policy；真正引入多语言生成器前再补 ADR。

## 18:16 - AutoWaterSimu Next contracts registry gate

- 用户目标：继续根据 Certainty/Elegance PRD 与 Development Plan 完善 AutoWaterSimu Next 的合同 single source of truth 与 drift 证据。
- 涉及目录：`contracts/`、`scripts/`、`scripts/ci/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`、`.ai/changes/`。
- 修改内容：新增 `contracts/registry.json`，覆盖当前 17 个 `*.v1.json` schema、consumer list、valid/invalid examples、compatibility notes 和 breaking-change policy；新增 `scripts/check-contracts.ps1`；`Justfile check-contracts` 改为调用该脚本；`scripts/ci/pr-fast.ps1` 改为通过 registry-backed contract gate；`scripts/check-deps.ps1` 对 `contracts/registry.json` 做显式扫描豁免；更新 contracts/scripts/architecture/root README、Certainty/Elegance checklist 和任务记录。
- 修改原因：Certainty/Elegance Phase 1 要求 contracts registry、consumer/example/compatibility/breaking policy 明确化，并把 drift 检查纳入 `just check-contracts`；当前只有 schema tests 与 Compute TS client drift，缺少可审计 registry。
- 本次已消除的不确定性：确认当前 contracts 目录共有 17 个 schema、36 个 valid examples、12 个 invalid examples；确认 registry 能完整覆盖这些文件；确认 registry 文档中的 consumer/drift path 不应被解释为 contracts runtime dependency；确认 `pr-fast` 可复用新的 contract gate。
- 本次采用的关键假设：本轮只实现 registry 和当前可验证的 drift gate；不新增 `contracts/codegen/`，不承诺 Go/Python/Rust generated type pipeline 已完成；当前 drift gate 只证明 registry consistency、Python schema/transform tests、OpenAPI 和 frontend Compute TS client 输出。
- 剩余不确定性：Go/Python/Rust 生成类型是否需要引入、生成目录结构、Rust/Desktop 合同绑定方式、以及 full `contracts/codegen/` breaking-change 工作流仍需后续决策。
- 影响范围 / 风险控制方式：不修改任何 schema 内容、OpenAPI 内容、runtime 代码或 generated client 语义；`check-contracts` 只做校验、既有 client 生成和机械 whitespace 归一化；dependency scan 只豁免 registry metadata 文件，仍扫描 contracts schema/examples。
- 验证方式：`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-contracts.ps1` 通过，contract tests `91 passed`，registry 覆盖 17 schemas / 36 valid examples / 12 invalid examples；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` 通过，evidence status=`passed`，6 steps passed，0 failed，dirty worktree 已记录。
- 是否更新 README：是，更新 root README、`contracts/README.md`、`scripts/README.md`、`scripts/ci/README.md`、`docs/architecture/local-dev.md`、`docs/architecture/current-state.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮是 contract registry/gate 落地；`contracts/codegen/` 是否引入多语言生成仍需后续 ADR 或实现计划。

## 17:57 - AutoWaterSimu Next PR fast evidence lane

- 用户目标：继续根据 Certainty/Elegance PRD 与 Development Plan 完善 AutoWaterSimu Next 的确定性和默认 PR 反馈证据。
- 涉及目录：根目录、`.github/workflows/`、`scripts/`、`scripts/ci/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`、`.ai/changes/`。
- 修改内容：新增 `scripts/ci/pr-fast.ps1` 与 `scripts/ci/README.md`；新增 `.github/workflows/next-pr-fast.yml`；新增 `just pr-fast`；更新 root/scripts/workflow/architecture README 与 Certainty/Elegance plan 的 immediate next step；`pr-fast` 会输出 `tmp/ci-evidence/pr-fast.json`，记录 commit、branch、dirty worktree、workflow catalog、GitHub Actions metadata、每个 fast check 结果和 generated client drift gate。
- 修改原因：Phase 0 要求 current HEAD 或本地等效 fast check evidence，且不能把“有 workflow 文件”误写成“已验证”；需要一个本地与 CI 共用的脚本入口来提高验证确定性。
- 本次已消除的不确定性：确认 `pr-fast` 能在本机按顺序跑通依赖边界、README path、contracts、Go API、Compute client generation/drift、frontend typecheck 和 Desktop typecheck；确认 evidence JSON 会显式标记 dirty worktree；确认 workflow 只负责依赖安装、脚本调用和 artifact upload。
- 本次采用的关键假设：本轮 fast lane 只作为 PR 反馈/本地等效证据，不替代 release gate、不构建 release artifact、不证明生产审批或真实集成栈通过；dirty worktree 证据不能当作 clean HEAD proof。
- 剩余不确定性：真实 GitHub Windows runner 上的 `next-pr-fast.yml` 尚未 live 运行；clean HEAD 证据需在提交后重跑；Postgres + MinIO + Worker 的完整 job lifecycle smoke 仍待后续。
- 影响范围 / 风险控制方式：只新增 CI/脚本入口和文档记录；fast lane 复用已有检查，不改变 runtime、OpenAPI、JSON Schema 或 generated client 内容；generated client 只允许机械生成和 whitespace 归一化，并由 diff gate 拦截 drift。
- 验证方式：`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` 通过，生成 `tmp\ci-evidence\pr-fast.json`，status=`passed`，9 steps passed，0 failed；evidence 显示 `is_dirty_before=true`、`is_dirty_after=true`，commit=`2168671945e31ddfb1bc50820a1ac621c2676f26`，branch=`codex/autowatersimu-next-rebuild`；`backend\.venv\Scripts\python` + PyYAML 成功解析 `.github/workflows/next-pr-fast.yml` 并确认核心 run/upload steps；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\doctor.ps1` 通过 required fast-check tooling（本机 `just` optional-missing）；`git diff --check -- .github scripts docs README.md Justfile docker-compose.dev.yml tasks\todo.md .ai\changes\2026-05-31.md` 通过（仅 LF/CRLF warning）。
- 是否更新 README：是，新增 `scripts/ci/README.md`，更新 root README、`scripts/README.md`、`.github/workflows/README.md`、`docs/architecture/local-dev.md`、`docs/architecture/current-state.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮是 Phase 0 CI evidence 入口，不新增长期架构取舍。

## 17:49 - AutoWaterSimu Next dev compose entry

- 用户目标：继续根据 Certainty/Elegance PRD 与 Development Plan 完善 AutoWaterSimu Next 的确定性和本地可启动性。
- 涉及目录：根目录、`docs/architecture/`、`docs/rebuild/`、`tasks/`、`.ai/changes/`。
- 修改内容：补齐根 `.env.example` 的 Next Compute API、MinIO archive、frontend 和 worker 本地开发变量；新增 `docker-compose.dev.yml`，覆盖 PostgreSQL、MinIO、Compute API、simulation worker loop 和 frontend；更新 `Justfile` 的 `dev` / `dev-detached` / `dev-down`；更新 local-dev/current-state/root README 与 Certainty/Elegance checklist。
- 修改原因：Phase 0 明确要求统一入口之外补齐 `.env.example` 和覆盖 Postgres、MinIO、Compute API、simulation worker、frontend 的真实本地栈候选。
- 本次已消除的不确定性：确认 Go Compute API command 会在配置 `COMPUTE_API_DATABASE_URL` 时自动执行 migrations；确认 worker CLI 支持 `--run-api-loop`；确认 frontend Compute API base URL 由 `VITE_COMPUTE_API_URL` 配置；确认 S3 archive backend 可用 MinIO dev service 表达。
- 本次采用的关键假设：本轮只提供 source-mounted local dev compose candidate；不新增 Dockerfile，不声明生产部署、runtime integration smoke 或 release evidence 完成；worker 容器首次启动允许用 pip cache 安装科学计算依赖。
- 剩余不确定性：完整 compose runtime 首启耗时、真实 job lifecycle smoke、browser smoke、release artifact 下载验证和生产安全边界仍需后续 evidence。
- 影响范围 / 风险控制方式：只新增本地开发入口和 env 示例；不改变 Go API/worker/frontend runtime 代码；compose 使用 dev token 并在文档中明确非生产。
- 验证方式：`docker compose -f docker-compose.dev.yml config --quiet` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\doctor.ps1` 通过 required fast-check tooling（本机 `just` optional-missing）；`git diff --check -- .env.example docker-compose.dev.yml Justfile README.md docs\architecture docs\rebuild\AutoWaterSimu_Next_Certainty_Elegance_Development_Plan_v1.0.md tasks\todo.md .ai\changes\2026-05-31.md` 通过（仅 LF/CRLF warning）。未运行完整 stack startup / job lifecycle smoke，原因是首次拉取镜像和安装 Python 科学计算依赖可能耗时较长，本轮仅验证 compose 配置有效性。
- 是否更新 README：是，更新 root README 和 `docs/architecture/local-dev.md` / `current-state.md`；`.env.example` 作为根级配置示例同步更新。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮是 Phase 0 本地入口落地，不引入新的长期架构取舍。

## 17:34 - AutoWaterSimu Next Phase 0 task graph and dependency boundary

- 用户目标：根据 Certainty/Elegance PRD 与 Development Plan 继续完善和优化 AutoWaterSimu Next，优先落地低风险 Phase 0 质量提升入口。
- 涉及目录：根目录、`scripts/`、`docs/architecture/`、`docs/rebuild/`、`frontend/src/`、`frontend/src/routes/`、`frontend/src/services/`、`tasks/`、`.ai/changes/`。
- 修改内容：新增根 `Justfile`；新增 `scripts/doctor.ps1` 和 `scripts/check-deps.ps1`；新增 `docs/architecture/README.md`、`module-map.md`、`dependency-graph.md`、`local-dev.md`、`current-state.md`；将 Compute Jobs / Lifecycle / Model Governance routes 对 generated Compute client 的直接 import 移到 `computeJobsService` 边界后；更新 root/docs/scripts/frontend README 与 Certainty/Elegance checklist。
- 修改原因：补充计划的 Immediate Next Step 要求先建立根级 task graph、架构入口和最小依赖边界检查，再进入 Go API/Store/production security 等高影响重构。
- 本次已消除的不确定性：确认仓库此前已有 release gate 脚本但缺根 `Justfile`；确认 `docs/architecture` 尚不存在；确认 route 层存在直接 import `@/client/compute` 的 generated client 上浮问题，并已改为通过 service wrapper 获取类型和 API base URL。
- 本次采用的关键假设：本轮只做 Phase 0 低风险切片；不拆 Go API package、不拆 Store interface、不新增生产 OIDC/RBAC、不创建未验证的完整 `docker-compose.dev.yml`。
- 剩余不确定性：完整 Next docker compose dev stack、contracts registry/codegen drift、Go API modular monolith 拆分、生产 token 禁用和金标场景自动化仍需后续切片。
- 影响范围 / 风险控制方式：依赖检查只覆盖当前已满足的硬规则；前端 route 仅调整 import 边界，不改变页面行为或后端 wire shape；新增文档明确区分已落地能力与后续缺口。
- 验证方式：`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\doctor.ps1` 通过 required fast-check tooling（本机 `just` optional-missing）；`cd frontend; npx biome check src\services\computeJobsService.ts src\routes\_layout\compute-jobs.tsx src\routes\_layout\compute-lifecycle.tsx src\routes\_layout\model-governance.tsx` 通过；`cd frontend; npx tsc --noEmit` 通过；`git diff --check -- ...` 通过（仅 LF/CRLF warning）。
- 是否更新 README：是，新增 `docs/architecture/README.md` 并更新 root、docs、scripts、frontend src/routes/services README。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮落实 Certainty/Elegance Phase 0 入口，不引入新的长期架构取舍。

## 11:04 - AutoWaterSimu Next legacy compute read-only guard

- 用户目标：继续按 PRD/Spec/Development Plan 推进 AutoWaterSimu Next，并为 Release checklist 中 legacy FastAPI compute read-only exit 准备可部署启用的本地机制。
- 涉及目录：`backend/app/core/`、`backend/app/api/routes/`、`backend/app/tests/api/routes/`、`docs/rebuild/`、`tasks/`、`.ai/plans/`。
- 修改内容：新增 `Settings.LEGACY_COMPUTE_READ_ONLY`，默认 `false`；新增集中式 `ensure_legacy_compute_writable` guard；将 guard 接入 material balance / ASM1Slim / ASM1 / ASM3 / UDM 的 `/calculate`、`/calculate-from-flowchart` 和 `DELETE /jobs/{job_id}`；只读模式下返回 `409`、code=`LEGACY_COMPUTE_READ_ONLY` 并指向 `/api/v1/simulation-checks`；新增 route dependency coverage 测试；更新 `.env.example`、backend README、legacy migration guide、Development Plan checklist、completion audit 和任务记录。
- 修改原因：Development Plan 仍保留 “Legacy FastAPI compute marked read-only” release 项；在真实 30 天对照和部署验证前不能直接宣称完成，但代码侧需要一个可审计、默认关闭、可部署启用的 read-only guard。
- 本次已消除的不确定性：确认 legacy write surface 至少包括五类 compute 模块的新建计算任务和删除计算 job；确认 validate/status/result/input-data 读取路径应保持可用，用于 legacy comparison；确认 OpenAPI schema/client 不需要因为 route dependency guard 变更而更新。
- 本次采用的关键假设：本轮只实现 opt-in guard，不默认启用、不删除旧 endpoint、不阻断读取或验证 endpoint、不声称生产部署已进入 read-only comparison。
- 剩余不确定性：真实部署何时启用 `LEGACY_COMPUTE_READ_ONLY=true`、30 天 comparison evidence、live Web deploy smoke、外部发布/审批流程仍需环境与人工治理。
- 影响范围 / 风险控制方式：默认配置保持 legacy 行为不变；guard 只覆盖 mutating compute routes；新增测试检查默认可写、显式只读错误 payload 和所有目标 route 的 dependency 覆盖。
- 验证方式：`cd backend; .venv\Scripts\python -m pytest app\tests\api\routes\legacy_compute_read_only_test.py -q` 通过（7 passed，existing `python_multipart` warning only）；`cd backend; .venv\Scripts\python -m pytest app\tests\api\routes\legacy_compute_read_only_test.py app\tests\api\routes\test_asm_udm_validate_response.py app\tests\api\routes\test_flowchart_routes_no_print.py -q` 通过（29 passed，同一 warning）；`cd backend; .venv\Scripts\python -m pytest app\tests\time_segment_validation_test.py app\tests\material_balance_segment_overrides_test.py app\tests\hybrid_udm_validation_test.py app\tests\udm_engine_variable_binding_test.py -q` 通过（17 passed，同一 warning）。
- 是否更新 README：是，更新 `backend/app/core/README.md`、`backend/app/api/routes/README.md` 和 legacy migration guide。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮落实既有 legacy read-only exit，不改变长期架构边界。

## 10:54 - AutoWaterSimu Next NewSystem production readiness E2E

- 用户目标：继续按 PRD/Spec/Development Plan 推进 AutoWaterSimu Next，并把 NewSystem/milp 只读 readiness 能力纳入服务级 E2E 证据链。
- 涉及目录：`apps/api/internal/compute/`、`tasks/`、`.ai/plans/`。
- 修改内容：扩展 `TestNewSystemEvidenceReferenceE2E`，在同一条 NewSystem-style process graph registration -> simulation check -> worker completion -> evidence ref dereference workflow 中读取 `/api/v1/compute/jobs/{job_id}/production-readiness`，并断言 `ready_for_external_approval`、外部审批要求、禁止自动发布、evidence package id、risk summary 和 NewSystem/project metadata；更新任务记录和 completion audit。
- 修改原因：production readiness endpoint 和 Web UI 已存在，但 NewSystem service-level E2E 尚未把 readiness read 纳入同一条 job-scoped 审批证据链，Phase 6.2 的验收证据仍偏分散。
- 本次已消除的不确定性：确认 production readiness 可在 NewSystem-style job 完成后通过 `evidence:read` 读取；确认 info-level risk finding 不阻断 readiness；确认报告仍保留 `external_approval_required=true` 和 `auto_publish_allowed=false`。
- 本次采用的关键假设：本轮只补服务级 regression coverage；不新增 endpoint、不创建生产审批记录、不改变 readiness policy、不声称外部 NewSystem/milp acceptance smoke 已完成。
- 剩余不确定性：真实外部 NewSystem/milp acceptance smoke、部署侧审批流、真实服务 token/secret manager 和 live deployed browser/API E2E 仍需外部环境验证。
- 影响范围 / 风险控制方式：仅修改 Go API 测试与审计记录；无 OpenAPI/schema/client 变更；不触发 README 更新条件。
- 验证方式：`cd apps\api; go test ./internal/compute -run TestNewSystemEvidenceReferenceE2E -count=1` 通过。
- 是否更新 README：否，本轮不改变目录职责、公共接口或长期维护约定。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，沿用既有只读 readiness 与外部审批边界。

## 09:04 - AutoWaterSimu Next parameter promotion plan

- 用户目标：按 PRD/Spec/Development Plan 继续推进 AutoWaterSimu Next，并补齐 Phase 6.1 model governance 中 benchmark-backed parameter promotion 的可审计只读判断面。
- 涉及目录：`contracts/`、`apps/api/`、`apps/api/internal/compute/`、`apps/api/openapi/`、`frontend/src/client/compute/`、`frontend/src/services/`、`frontend/src/routes/`、`frontend/tests/`、`docs/rebuild/`、`tasks/`、`.ai/plans/`。
- 修改内容：新增 `GET /api/v1/model-catalog/{model_key}/versions/{model_version}/default-parameter-set/promotion-plan`；该 endpoint 读取当前 default parameter set、validated benchmark cases、latest benchmark_run 和 model_run parameter hash，返回 `parameter_set_promotion_plan.v1` advisory response；benchmark run list 增加 `parameter_set_id` 过滤；同步 OpenAPI 与 generated Compute client；新增 frontend service wrapper；`/model-governance` 只读展示 promotion readiness；更新 README、Development Plan、completion audit 和任务记录。
- 修改原因：已有 model catalog、benchmark_run history、model_run 与默认参数集状态迁移，但缺少“某个 validated default parameter set 是否已有足够 benchmark 证据可供人工提升到 approved”的可验证只读面；直接自动修改状态或执行 benchmark 会越过当前 Phase 6 边界。
- 本次已消除的不确定性：确认 `benchmark_run.v1` 是审计历史而非执行器；确认记录 benchmark_run 时已校验 benchmark case、parameter set、model_run hash 与 job-scoped evidence refs；确认 promotion plan 可以复用这些持久化证据并保持 advisory-only。
- 本次采用的关键假设：本轮只针对当前 `default_parameter_set` 做只读 readiness；只有 current status 为 `validated`、model version 为 `active`、每个 validated benchmark case 的当前参数集最新 benchmark run 为 `passed` 且 model_run hash 匹配时才返回 `can_promote_to_approved=true`；不新增多参数集存储、自动 benchmark scheduler 或 approval workflow。
- 剩余不确定性：多参数集管理、scheduled benchmark execution、自动状态提升、审批人/审批流语义、ASM/UDM 默认参数集仍需后续设计。
- 影响范围 / 风险控制方式：新增 `job:read` 只读 endpoint 和 frontend display；`would_modify_catalog=false`，不执行 benchmark、不调用 status transition、不改变 production_allowed 规则；OpenAPI/generated client 已同步。
- 验证方式：`cd apps\api; go test ./internal/compute -run TestDefaultParameterSetPromotionPlanEndpoint -count=1` 通过；`cd apps\api; go test ./...` 通过；`cd frontend; npm run generate-compute-client` 通过；OpenAPI/generated client assertion 通过；`cd frontend; npx tsc --noEmit` 通过；`cd frontend; npx playwright test tests/model-governance.spec.ts --project=chromium --no-deps --reporter=line` 通过（1 passed）；`git diff --check -- contracts apps\api frontend docs\rebuild tasks\todo.md .ai\plans .ai\changes\2026-05-31.md` 通过（仅 LF/CRLF 提示）。
- 是否更新 README：是，更新 `contracts/README.md`、`apps/api/README.md`、`apps/api/internal/compute/README.md`、`apps/api/openapi/README.md`、`frontend/src/services/README.md`、`frontend/src/routes/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮遵循现有 benchmark_run/history 和 advisory-only governance 边界。

## 08:43 - AutoWaterSimu Next worker API loop heartbeat coverage

- 用户目标：按 PRD/Spec/Development Plan 继续推进 AutoWaterSimu Next，并补齐 Phase 2/4 worker HTTP bridge 只有一次性 `--run-api-once`、缺少重复 claim 与 heartbeat 覆盖的问题。
- 涉及目录：`services/simulation-worker/`、`services/simulation-worker/simulation_worker/`、`services/simulation-worker/tests/`、`docs/rebuild/`、`tasks/`、`.ai/plans/`。
- 修改内容：新增 `run_api_loop()` 与 CLI `--run-api-loop`；将 `--run-api-once` 改为复用 register/claim/heartbeat/run/upload/complete helper；每个已 claim job 执行前调用 heartbeat 以刷新 lease 并读取 cancel/terminal 状态；loop 模式支持 `--max-jobs`、`--max-idle-polls`、`--idle-sleep-seconds`，输出 compact JSON summary；扩展 fake Compute API 测试覆盖 one-shot heartbeat 顺序和两 job bounded loop；更新 worker README、Development Plan 和 completion audit。
- 修改原因：Go Compute API 已具备 worker register/claim/heartbeat/succeed/fail lifecycle，但 Python worker 只覆盖单次 register -> claim -> run -> upload -> complete，Phase 2 audit 仍记录 long-running/heartbeat coverage 缺口。
- 本次已消除的不确定性：确认 Go heartbeat endpoint 接收 `job_id` 并返回 `cancel_requested` / `job_terminal` / lease metadata；确认 worker HTTP bridge 可以在不直接访问 metadata store 的前提下复用现有 contract；确认 bounded loop 可以由 fake API 测试稳定覆盖。
- 本次采用的关键假设：本轮只实现 dev/CI 和简单长运行 worker loop；不实现异步中断正在运行的求解器、不新增生产 supervisor、不改变 Go API contract、不声明 deployed worker monitoring 完成。
- 剩余不确定性：生产 worker 部署编排、运行中 cooperative cancellation、真实 deployed worker observability 和外部 API/browser E2E 仍待单独实现或环境验证。
- 影响范围 / 风险控制方式：保持 `--run-api-once` CLI 输出 JSON 兼容；loop 输出不内联每个 job 的完整 compute_result，避免 stdout 大 payload；worker 仍只通过 Go HTTP contract 交互，不直接写数据库。
- 验证方式：`backend\.venv\Scripts\python services\simulation-worker\simulation_worker\cli.py --self-check` 通过；`backend\.venv\Scripts\python -m pytest services\simulation-worker\tests -q` 通过（22 passed）。
- 是否更新 README：是，更新 `services/simulation-worker/README.md`、`services/simulation-worker/simulation_worker/README.md`、`services/simulation-worker/tests/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮是既有 worker HTTP contract 的客户端 loop 能力；生产部署/中断策略仍需未来单独决策。

## 08:31 - AutoWaterSimu Next simulation request ASM/UDM entry

- 用户目标：按 PRD/Spec/Development Plan 继续推进 AutoWaterSimu Next，并让已具备 worker/core/backend 证据的独立 ASM/UDM job type 可通过外部 `simulation_request.v1` / Go simulation-check 入口创建 job。
- 涉及目录：`contracts/`、`apps/api/`、`frontend/src/client/compute/`、`docs/rebuild/`、`tasks/`、`.ai/plans/`。
- 修改内容：扩展 `simulation_request.v1.job_type` 为 material_balance + ASM1Slim/ASM1/ASM3/UDM；新增四个 reference-only `*.simulation_request.v1.json` fixtures；Go simulation-check execution capability 映射新增 `asm1slim` / `asm1` / `asm3` / `udm`；测试覆盖四个已登记 simulation input 的 request-to-job 路径，并确认 `process_graph_id` 自动转换仍拒绝 ASM/UDM；同步 OpenAPI、generated Compute TypeScript client、contracts/API README、Development Plan checklist 和 completion audit。
- 修改原因：独立 ASM/UDM worker job type、simulation_input fixtures 和 built-in model catalog 已完成，但外部 NewSystem/milp/Agent simulation request 入口仍只接受 material-balance，形成提交路径缺口。
- 本次已消除的不确定性：确认 `simulation_input.v1`、`compute_job.v1`、worker 和 built-in catalog 已支持四个独立 ASM/UDM job type；确认 Go API 的 embedded / registered simulation_input 和 model_run replay 路径可以按 job type 保守复用；确认 ProcessGraph-to-ASM/UDM 自动转换语义尚未设计，必须继续拒绝。
- 本次采用的关键假设：本轮只开放已登记或内嵌 `simulation_input.v1` 以及 model_run replay 的 request 入口；不新增 ProcessGraph-to-ASM/UDM transform，不修改 worker 执行逻辑，不新增前端提交 UI。
- 剩余不确定性：ASM/UDM process graph authoring/transform semantics、真实外部 NewSystem/milp acceptance smoke、生产 approval policy 和 deployed API/browser E2E 仍待单独实现或外部环境验证。
- 影响范围 / 风险控制方式：公共合同和 OpenAPI 有枚举扩展，已同步 generated client；保留 job_type 与 simulation_input job_type 一致性校验；保留 process_graph material-balance-only guard；不改数据库 schema。
- 验证方式：`backend\.venv\Scripts\python -m pytest contracts\tests -q` 通过（87 passed）；`cd apps\api; go test ./internal/compute -run TestSimulationCheckEndpointCreatesComputeJob -count=1` 通过；`cd apps\api; go test ./...` 通过；`cd frontend; npx tsc --noEmit` 通过；OpenAPI/schema enum assertion 通过；`git diff --check -- contracts apps\api frontend\src\client\compute docs\rebuild tasks\todo.md .ai\plans .ai\changes\2026-05-31.md` 通过（仅 LF/CRLF 提示）。
- 是否更新 README：是，更新 `contracts/README.md`、`contracts/examples/valid/README.md`、`apps/api/README.md`、`apps/api/internal/compute/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮是在既有 job type 和 simulation-check 架构内开放已验证入口；ProcessGraph-to-ASM/UDM 仍需未来单独决策。

## 08:13 - AutoWaterSimu Next built-in model catalog coverage

- 用户目标：按 PRD/Spec/Development Plan 继续推进 AutoWaterSimu Next，并在 ASM/UDM worker 独立 job type 已有测试证据后扩展 model governance 的内置 catalog 覆盖。
- 涉及目录：`apps/api/`、`apps/api/internal/compute/`、`docs/rebuild/`、`tasks/`、`.ai/plans/`。
- 修改内容：内置 fallback model catalog 在 material_balance 之外新增 ASM1Slim、ASM1、ASM3、UDM active model versions；这些版本带 runtime parameter templates 和基于现有 independent simulation input fixtures 的 validated worker-smoke benchmark cases；扩展 `TestModelCatalogEndpoint` 覆盖五个 fallback models 和 built-in `asm1` by-key read；修正 memory store 在 catalog snapshots `created_at` 相同情况下按后写优先排序；更新 Compute API README、Development Plan、completion audit 和任务记录。
- 修改原因：worker 已支持并验证 `simulation.asm1slim.v1`、`simulation.asm1.v1`、`simulation.asm3.v1`、`simulation.udm.v1`，但无持久化 catalog 时 Go API fallback 仍只暴露 material_balance，不利于只读治理和 UI 可见性。
- 本次已消除的不确定性：确认 contracts/worker README 已将 ASM1Slim/ASM1/ASM3/UDM 标为当前独立 model job type；确认这些 entries 可以只读暴露为 model version + benchmark case；确认默认参数集和 promotion 语义尚未定义，不能为 ASM/UDM 静默创建 approved default parameter sets。
- 本次采用的关键假设：本轮只扩展 built-in fallback catalog 的只读覆盖；material_balance 仍是唯一带 approved default parameter set 的内置模型；持久化 `default` catalog 仍优先于 fallback。
- 剩余不确定性：ASM/UDM 默认参数集、参数集生命周期、benchmark-backed promotion、scheduled benchmark execution 和 approval workflow 仍需单独设计。
- 影响范围 / 风险控制方式：只修改 Go API fallback catalog、memory snapshot listing tie-breaker 与测试；不变更 OpenAPI/schema/generated client；不改变 worker 执行逻辑；不改变 persisted catalog snapshot precedence。
- 验证方式：首次 `cd apps\api; go test ./internal/compute -count=1` 暴露 memory snapshot list 在相同 `created_at` 下会按 payload hash 排序的问题；修正为后写优先后，`cd apps\api; go test ./internal/compute -count=1` 通过；`git diff --check -- apps\api docs\rebuild tasks\todo.md .ai\plans .ai\changes\2026-05-31.md` 通过（仅 LF/CRLF 提示）。
- 是否更新 README：是，更新 `apps/api/README.md` 与 `apps/api/internal/compute/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮落实已测试 worker model path 的只读 catalog visibility，不新增治理状态机。

## 08:04 - AutoWaterSimu Next Web model governance UI

- 用户目标：按 PRD/Spec/Development Plan 继续推进 AutoWaterSimu Next，并把已实现的 model catalog / snapshot governance 能力暴露为独立只读 Web 页面。
- 涉及目录：`frontend/src/routes/`、`frontend/src/components/Common/`、`frontend/src/i18n/`、`frontend/tests/`、`docs/rebuild/`、`tasks/`、`.ai/plans/`。
- 修改内容：新增 `/model-governance` authenticated route，展示当前 model catalog versions、default parameter set 状态/hash、benchmark case 数量和 persisted snapshot history；新增 sidebar 入口与中英文导航 key；通过 TanStack Router Vite plugin 更新 `routeTree.gen.ts`；新增 mock-backed Playwright smoke 覆盖 catalog 读取和 snapshot next-page 分页；更新 route/test README 与完成度记录。
- 修改原因：Compute Jobs 页面已承载任务、evidence、retention、catalog 等多类能力；model governance 需要一个独立只读入口承接 persisted catalog snapshot history，而不在 route 层引入状态机或审批规则。
- 本次已消除的不确定性：确认前端已有 `computeJobsService.listModelCatalogSnapshots` wrapper 可直接复用；确认 route README 要求 model catalog UI 只做只读展示、筛选和查询；确认内置 fallback catalog 不应显示为 persisted snapshot history。
- 本次采用的关键假设：本轮只做只读 governance UI，不实现 multi-parameter-set management、scheduled benchmark execution、benchmark-backed promotion、治理审批或生产 job 创建。
- 剩余不确定性：多参数集生命周期、benchmark 调度、promotion 规则、审批流程和生产 approval policy 仍需单独设计。
- 影响范围 / 风险控制方式：仅新增 authenticated route、sidebar entry、i18n key 和 mock-backed smoke；不修改 Compute API schema、不修改 generated client、不改变 Compute Jobs 既有行为。
- 验证方式：`cd frontend; npx vite build --mode development` 通过并更新 route tree（保留既有 `gray-matter` eval / toaster chunking / bundle-size warnings）；`cd frontend; npx tsc --noEmit` 通过；`cd frontend; npx playwright test tests/model-governance.spec.ts --project=chromium --no-deps --reporter=line` 通过。
- 是否更新 README：是，更新 `frontend/src/routes/README.md` 与 `frontend/tests/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮是既有只读治理能力的前端入口，不改变架构边界。

## 07:52 - AutoWaterSimu Next model catalog snapshot listing

- 用户目标：按 PRD/Spec/Development Plan 继续推进 AutoWaterSimu Next，并补齐 model governance 中已持久化 catalog snapshots 缺少只读历史列表的问题。
- 涉及目录：`apps/api/`、`apps/api/internal/compute/`、`apps/api/openapi/`、`frontend/src/client/compute/`、`frontend/src/services/`、`tasks/`、`.ai/plans/`。
- 修改内容：为 store interface 增加 `ListModelCatalogSnapshots`；memory/PostgreSQL store 支持按 `catalog_id` 分页列出 persisted snapshots；新增 service/HTTP `GET /api/v1/model-catalog/snapshots`；更新 OpenAPI source、生成 Compute TypeScript client、frontend `computeJobsService` wrapper 和相关 README；扩展 model catalog endpoint test 覆盖分页。
- 修改原因：当前 `GET /model-catalog` 只返回最新 catalog 或 built-in fallback，无法查看已登记/状态迁移产生的 catalog snapshot history，不利于治理审计和后续 UI。
- 本次已消除的不确定性：确认 built-in fallback 不应伪装成 persisted history；确认现有 cursor/limit 风格可复用；确认 endpoint 使用 `job:read`，写入仍保留 `model:write`。
- 本次采用的关键假设：本轮只做只读 snapshot history，不实现多参数集管理、scheduled benchmark execution、benchmark-backed promotion 或 standalone governance UI。
- 剩余不确定性：未来治理 UI、参数集生命周期扩展、benchmark-backed promotion 和 production approval policy 仍需单独设计。
- 影响范围 / 风险控制方式：现有 `GET /api/v1/model-catalog` 语义不变；新 endpoint 只读取 persisted snapshots；OpenAPI 变更后同步 generated compute client。
- 验证方式：`cd apps\api; go test ./internal/compute -run TestModelCatalogEndpoint -count=1` 通过（修正测试中第二页 JSON unmarshal 前需重置响应结构体的问题）；`cd apps\api; go test ./internal/compute -count=1` 通过；`cd frontend; npm run generate-compute-client` 通过且只生成 `frontend/src/client/compute/sdk.gen.ts` / `frontend/src/client/compute/types.gen.ts`；OpenAPI 断言确认 `/api/v1/model-catalog/snapshots`、`listModelCatalogSnapshots` 与 `ListModelCatalogSnapshotsResponse` 存在；`cd frontend; npx tsc --noEmit` 通过；`git diff --check -- apps\api frontend docs\rebuild tasks\todo.md .ai\plans .ai\changes\2026-05-31.md` 通过（仅 LF/CRLF 提示）。
- 是否更新 README：是，更新 `apps/api/README.md`、`apps/api/internal/compute/README.md`、`apps/api/openapi/README.md`、`frontend/src/services/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮是既有 model catalog snapshot store 的只读 API 扩展。

## 07:43 - AutoWaterSimu Next monitoring receiver policy

- 用户目标：按 PRD/Spec/Development Plan 继续推进 AutoWaterSimu Next，并补齐 monitoring deployment 中真实 receiver/on-call/secret policy 的仓库安全边界。
- 涉及目录：`docs/operations/`、`docs/operations/monitoring/`、`docs/rebuild/`、`tasks/`、`.ai/plans/`。
- 修改内容：新增 `docs/operations/monitoring/receiver_policy_runbook.md`，记录 severity 到 owner 的映射要求、secret handling、approval checklist、synthetic test alert procedure 和 incident notes；从 monitoring README、monitoring deployment runbook、operations README、Development Plan 和 completion audit 链接。
- 修改原因：仓库已有 Alertmanager placeholder route 和 deployment runbook，但真实 receiver/on-call/secret 归属仍只写成外部事项；需要明确哪些策略可提交，哪些必须留在部署环境。
- 本次已消除的不确定性：确认本目录不得存放真实 webhook、token、个人联系方式或私有 channel；确认 receiver policy 可以作为本地文档完成，而 live deployment evidence 仍需外部环境。
- 本次采用的关键假设：本轮只写仓库安全策略，不添加真实 receiver，不修改 alert rules，不声明 production monitoring 已部署。
- 剩余不确定性：真实 receiver owner、on-call rotation、secret manager、Alertmanager rendered config、Grafana datasource 和 live scrape evidence 仍待部署环境确认。
- 影响范围 / 风险控制方式：文档-only；使用 placeholder 和 synthetic alert 示例，不提交秘密或个人信息。
- 验证方式：`git diff --check -- docs\operations docs\rebuild tasks\todo.md .ai\plans .ai\changes\2026-05-31.md` 通过（仅 LF/CRLF 提示）；`backend\.venv\Scripts\python` 成功解析 monitoring YAML/JSON 并扫描 receiver policy 中常见真实 receiver secret marker / URL；`rg -n "https?://|Bearer |xoxb-|webhook|token|secret|phone|email|@" ...` 仅命中 placeholder/policy 语言，未发现真实 receiver 值。
- 是否更新 README：是，更新 `docs/operations/README.md` 与 `docs/operations/monitoring/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，属于 monitoring operations policy，不改变架构边界。

## 07:40 - AutoWaterSimu Next archive directory guard

- 用户目标：按 PRD/Spec/Development Plan 继续推进 AutoWaterSimu Next，并收紧 `local_fs_archive` 的部署安全边界。
- 涉及目录：`apps/api/cmd/compute-api/`、`docs/operations/`、`tasks/`、`.ai/plans/`。
- 修改内容：新增 Compute API entrypoint 的 archive store 打开逻辑；当 `COMPUTE_API_ARCHIVE_DIR` 与 `COMPUTE_API_ARTIFACT_DIR` 相同、互相包含或被包含时拒绝启动 archive store；新增 command tests；更新 cmd README 和 lifecycle runbook。
- 修改原因：ADR 0010 和 README 都要求 archive store 与 hot artifact store 分离；如果两个目录重叠，copy/verify 后删除 hot object 可能破坏 archive fallback 或让备份/恢复边界变得不清楚。
- 本次已消除的不确定性：确认该问题属于环境 wiring 安全校验，不需要改变 retention sweep 业务逻辑；确认 cmd 目录已有 test 可承载 entrypoint 配置测试。
- 本次采用的关键假设：本轮只处理本地 `local_fs_archive` 路径重叠风险；不实现 production object-store provider、不改变 archive metadata schema、不改变 artifact object key。
- 剩余不确定性：生产 object-store provider、bucket/prefix policy、credentials/secret handling 和 live archive restore/read smoke 仍待部署级决策。
- 影响范围 / 风险控制方式：只在 archive store 显式启用时做启动前校验；未设置 `COMPUTE_API_ARCHIVE_DIR` 时行为不变。
- 验证方式：`cd apps\api; go test ./cmd/compute-api -run "TestOpenArchiveStore|TestOpenStoreUsesMemoryStoreWhenDatabaseURLMissing" -count=1` 通过；`git diff --check -- apps\api docs\operations tasks\todo.md .ai\plans .ai\changes\2026-05-31.md` 通过（仅 LF/CRLF 提示）。
- 是否更新 README：是，更新 `apps/api/cmd/compute-api/README.md` 与 `docs/operations/compute_api_lifecycle_runbook.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，落实 ADR 0010 的 separate archive store 要求。

## 07:36 - AutoWaterSimu Next archive metrics

- 用户目标：按 PRD/Spec/Development Plan 继续推进 AutoWaterSimu Next，并补齐 lifecycle/operations 中 `local_fs_archive` 已实现但监控面无法看到已归档 artifact 数量的问题。
- 涉及目录：`apps/api/`、`apps/api/internal/compute/`、`docs/operations/monitoring/`、`docs/rebuild/`、`tasks/`、`.ai/plans/`。
- 修改内容：为 `MetricsSnapshot` 增加 archived artifact count；memory/PostgreSQL store 统计 `status=archived` 的 archive metadata；`/metrics` 渲染 `autowatersimu_compute_artifact_archives_total`；更新 focused Go tests、Compute API README、internal compute README、monitoring README/runbook/Grafana dashboard、Development Plan 和 completion audit。
- 修改原因：archive backend 已能把 `archive_candidate` 移入 `local_fs_archive` 并保留 metadata/download fallback，但监控示例只能看到总 artifact 和 retention backlog，无法区分已归档数量。
- 本次已消除的不确定性：确认当前 archive metadata 表与 memory store 均有 `Status=archived` 可作为只读计数来源；确认 `/metrics` handler 当前是只读路径，新增 gauge 不应触发 sweep/archive mutation；确认 monitoring README 不允许引用尚未暴露的 provider latency/error metrics。
- 本次采用的关键假设：本轮只暴露 archived artifact metadata count，不实现 production object-store backend、provider-specific metrics、Alertmanager real receiver、Grafana deployment 或 external monitoring smoke。
- 剩余不确定性：生产 object-store archive provider、receiver/on-call ownership、live monitoring deployment 和 deployed scrape evidence 仍待环境级决策与实际部署验证。
- 影响范围 / 风险控制方式：新增只读 gauge 与测试；不修改 retention/archive 执行动作、不改 OpenAPI/generated client、不引入新依赖。
- 验证方式：`cd apps\api; go test ./internal/compute -run "TestArtifactRetentionSweepArchivesCandidateWithConfiguredBackend|TestHTTPAuthScopeAndMetrics" -count=1` 通过；`cd apps\api; go test ./internal/compute -count=1` 通过；`backend\.venv\Scripts\python` 成功解析 monitoring YAML/JSON 并确认 Grafana dashboard 引用 `autowatersimu_compute_artifact_archives_total`；`git diff --check -- apps\api docs\operations docs\rebuild tasks\todo.md .ai\plans .ai\changes\2026-05-31.md` 通过（仅 LF/CRLF 提示）；本轮未运行 PostgreSQL live DB query，需 `COMPUTE_API_DATABASE_URL` 指向临时测试库时覆盖。
- 是否更新 README：是，更新 `apps/api/README.md`、`apps/api/internal/compute/README.md`、`docs/operations/monitoring/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，仍遵循 ADR 0010 的 archive copy/verify/metadata-before-delete 边界。

## 07:24 - AutoWaterSimu Next release artifact download verification

- 用户目标：按 PRD/Spec/Development Plan 继续推进 AutoWaterSimu Next，并补齐 release governance 中 unsigned Desktop workflow artifact 上传后缺少下载校验证据的问题。
- 涉及目录：`.github/`、`.github/workflows/`、`scripts/release/`、`docs/rebuild/`、`tasks/`、`.ai/plans/`。
- 修改内容：新增 `scripts/release/verify-release-artifact-download.ps1`；调整 `.github/workflows/next-release-gates.yml` 为 upload unsigned artifacts 后 download 同名 artifact，再调用校验脚本并最后上传 release evidence；校验脚本检查必需 build/smoke JSON、`sidecar_executable` / `installer_path` manifest 字段、sidecar exe 和 NSIS setup exe；更新 `.github` / workflow / release script README、Development Plan checklist、completion audit 和任务记录。
- 修改原因：之前 workflow 可以构建并上传 unsigned Desktop artifacts，但同一 run 内没有验证 artifact 可下载且包含 sidecar、installer 和 smoke evidence；live runner 仍需实际 dispatch，但代码路径应先具备可审计的下载校验。
- 本次已消除的不确定性：确认 `.github` 约定要求复杂校验逻辑放入 `scripts/`；确认 workflow 可用官方 `actions/download-artifact@v4` 下载已上传的 unsigned artifact；确认 release evidence 上传顺序需要放到下载校验之后才能包含 `downloaded-release-artifacts.json`。
- 本次采用的关键假设：本轮只做 unsigned workflow artifact download/content verification；不做 signing、auto-update、GitHub Release publication、Store/MSI、证书、更新密钥或 release token 处理。
- 剩余不确定性：仍未在真实 GitHub Windows runner 上触发 `workflow_dispatch build_release_artifacts=true`，因此实际 runner timing/cache behavior 仍需 live evidence。
- 影响范围 / 风险控制方式：默认 PR gate 不变；download verification 仅在 manual dispatch 且 artifact upload 成功时运行；校验脚本只读取下载目录并写 evidence，不发布或签名任何 artifact。
- 验证方式：`backend\.venv\Scripts\python` + PyYAML 解析 workflow 并断言 upload/download/verify/evidence step 顺序与 artifact name；`scripts/release/verify-release-artifact-download.ps1` valid fixture 产生 `passed` evidence，missing installer fixture 产生 `failed` evidence；`git diff --check -- .github scripts\release docs\rebuild tasks\todo.md .ai\plans .ai\changes\2026-05-31.md` 通过（仅 LF/CRLF 提示）；`.\scripts\release\next-release-gates.ps1 -Mode merge -SkipLong -EvidenceDir tmp\release-evidence\artifact-download-workflow-gate` 通过并产生 `status=passed` / `10` steps。
- 是否更新 README：是，更新 `.github/README.md`、`.github/workflows/README.md`、`scripts/release/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，遵循 ADR 0011 的 post-P0 signing/auto-update/release publication 边界。

## 07:17 - AutoWaterSimu Next job event retention policy

- 用户目标：按 PRD/Spec/Development Plan 继续推进 AutoWaterSimu Next，并补齐 Phase 6.4 lifecycle/operations 中 job event retention / archive policy 文档缺口。
- 涉及目录：`docs/operations/`、`docs/rebuild/`、`tasks/`、`.ai/plans/`。
- 修改内容：新增 `docs/operations/compute_api_job_event_retention_runbook.md`；更新 operations README、Development Plan Phase 6.4、completion audit 和任务记录。
- 修改原因：当前 `compute_job_events` 被 job timeline、evidence package、artifact retention/archive audit 使用，但只有 artifact retention 有明确操作手册；需要记录当前事件不自动裁剪，以及未来事件归档/删除必须 dry-run-first 并保护 evidence/support 用途。
- 本次已消除的不确定性：确认当前 Go API 没有 job event retention endpoint 或后台 worker；确认 `compute_job_events` 通过 `ON DELETE CASCADE` 绑定 job；确认事件 timeline 进入 evidence package。
- 本次采用的关键假设：本轮只补策略文档，不新增 destructive event pruning；未来如实现 endpoint 必须同步 OpenAPI、generated client、测试和 ADR。
- 剩余不确定性：生产事件保留周期、事件压缩 artifact 格式、外部审计保存期限、是否需要单独事件归档存储仍需部署/合规决策。
- 影响范围 / 风险控制方式：只改文档；明确禁止把尚未实现的 event retention worker 写成当前事实。
- 验证方式：`git diff --check -- docs\operations docs\rebuild tasks\todo.md .ai\plans .ai\changes\2026-05-31.md` 通过（仅 LF/CRLF 提示）；`rg -n "job event retention|compute_job_events|dry-run|automated event|evidence package|artifact.archived|artifact.retention_deleted" ...` 确认目标策略文案存在；`cd apps\api; go test ./internal/compute -run "TestEvidencePackage|TestArtifactRetention|TestWorkerLifecycle|TestNewSystemEvidenceEndToEnd" -count=1` 通过。
- 是否更新 README：是，更新 `docs/operations/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮记录当前策略边界；真正实现 destructive pruning 时再新增 ADR。

## 07:15 - AutoWaterSimu Next tenancy observability runbook

- 用户目标：按 PRD/Spec/Development Plan 继续推进 AutoWaterSimu Next，并补齐 Phase 6.4 lifecycle/operations 中 OpenTelemetry adoption trigger 与 multi-tenant deployment boundary 文档缺口。
- 涉及目录：`docs/operations/`、`docs/rebuild/`、`tasks/`、`.ai/plans/`。
- 修改内容：新增 `docs/operations/compute_api_tenancy_observability_runbook.md`；更新 operations README、Development Plan Phase 6.4 链接、completion audit 和任务记录。
- 修改原因：Technical Spec 已定义 OpenTelemetry 触发条件，Go API 已预留 `tenant_id` / `project_id` / `created_by` / `trace_id`，但 operations 文档未明确这些字段当前只是 P0 metadata boundary，也未给出何时引入 OpenTelemetry 的部署判断。
- 本次已消除的不确定性：确认当前 Compute API 没有完整 RBAC/tenant billing/quota/rate limit；确认 tenant/project 字段已在 schema/store 中作为 metadata 预留；确认当前观测能力是 `/metrics`、job events、evidence/model/artifact metadata 与 trace_id。
- 本次采用的关键假设：本轮只补运维边界文档，不新增 tracing dependency、exporter、RBAC 或数据库结构变更。
- 剩余不确定性：生产 OpenTelemetry exporter、trace retention、attribute redaction、multi-tenant auth model、quota/rate-limit policy 仍需外部平台/安全决策。
- 影响范围 / 风险控制方式：只改文档；明确禁止把 P0 tenant/project metadata 当成安全隔离；OpenTelemetry 只写 adoption triggers 和 implementation prerequisites。
- 验证方式：`git diff --check -- docs\operations docs\rebuild tasks\todo.md .ai\plans .ai\changes\2026-05-31.md` 通过（仅 LF/CRLF 提示）；`rg -n "OpenTelemetry|tenant_id|project_id|full RBAC|metadata boundary|trace_id" ...` 确认 runbook 与链接文档包含目标边界；`cd apps\api; go test ./internal/compute -run "TestHTTPAuthScopeAndMetrics|TestStaticTokenRevocation|TestNewSystemEvidenceEndToEnd" -count=1` 通过。
- 是否更新 README：是，更新 `docs/operations/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮记录既有 Spec 边界，不新增架构决策。

## 07:10 - AutoWaterSimu Next deliverable docs closeout

- 用户目标：按 PRD/Spec/Development Plan 继续推进 AutoWaterSimu Next，并把开发计划中的文档交付清单从初始未勾选状态更新为当前可验证状态。
- 涉及目录：`docs/rebuild/`、`docs/operations/`、`tasks/`、`.ai/plans/`。
- 修改内容：新增 COSS source manifest、legacy migration guide 和 support bundle runbook；更新 rebuild/operations README；更新 Development Plan deliverable checklist，区分 active-branch implementation evidence 与仍需外部审批/部署/30 天 read-only comparison 的项目。
- 修改原因：Development Plan 的交付清单仍停留在初始状态，容易误导后续 Agent 把已完成的本地实现和必须外部验证的治理项混在一起。
- 本次已消除的不确定性：确认当前没有 tracked upstream COSS source copy；确认 `frontend/openapi.json` 是 ignored local generator input；确认 support bundle 当前为 Desktop runtime-local `desktop_support_bundle.v1`，Compute API evidence package 是单独的 `evidence_package.v1`。
- 本次采用的关键假设：本轮只补长期文档和 checklist，不新增运行时代码；未获得人类 approval、live deploy smoke 或 30 天 legacy read-only evidence 的项目保持未勾选。
- 剩余不确定性：PRD/Spec/Development Plan 正式 approval、Web deploy smoke、legacy FastAPI compute read-only 标记、生产 object-store/monitoring receiver policy 仍需外部治理或部署证据。
- 影响范围 / 风险控制方式：只改文档；新增文档均引用当前代码事实和已验证边界，不把未实现能力写成当前事实。
- 验证方式：`git diff --check -- docs\rebuild docs\operations tasks\todo.md .ai\plans .ai\changes\2026-05-31.md` 通过（仅 LF/CRLF 提示）；`rg -n "schema_version|P0|P1|P2" docs\rebuild --glob "AutoWaterSimu_Next_*.md"` 完成；`cargo test --manifest-path apps\desktop\src-tauri\Cargo.toml support_bundle` 通过（`1 passed`）；`cargo test --manifest-path apps\desktop\src-tauri\Cargo.toml project_package` 通过（`1 passed`）。
- 是否更新 README：是，更新 `docs/rebuild/README.md` 与 `docs/operations/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮是交付文档闭环，不改变架构决策。

## 07:05 - AutoWaterSimu Next legacy FastAPI client refresh

- 用户目标：按 PRD/Spec/Development Plan 继续推进 AutoWaterSimu Next，并收敛 Phase 0 legacy OpenAPI/generated client drift。
- 涉及目录：`frontend/`、`frontend/src/client/`、`docs/rebuild/`、`tasks/`、`.ai/plans/`。
- 修改内容：从当前 FastAPI app 导出本地 `frontend/openapi.json` 后运行 `npm run generate-client`；更新 legacy generated client，使其包含 `UDMComponentDefinition.note` 字段和当前 legacy 计算 endpoint description 注释；修正 `frontend/README.md` 与 drift audit，明确 `frontend/openapi.json` 是被 Git 忽略的本地输入，`frontend/src/client` 才是 tracked generated output。
- 修改原因：审计记录把 `frontend/openapi.json` 误写为 tracked 文件；实际 Git 规则是忽略 OpenAPI 输入并提交 generated client。重新生成后可消除 legacy client 与当前 FastAPI OpenAPI 的漂移。
- 本次已消除的不确定性：确认 `frontend/openapi.json` 被 `frontend/.gitignore` 忽略；确认当前 FastAPI OpenAPI 与本地生成输入匹配；确认 Compute client 未被本轮生成覆盖。
- 本次采用的关键假设：继续沿用现有 legacy client 生成策略，不把 `frontend/openapi.json` 改为 tracked 文件；本轮只刷新 generated client，不改变 FastAPI route/schema source。
- 剩余不确定性：第三方 `python_multipart` import warning、debug/test-only print noise 和 broader mojibake readability cleanup 仍需单独处理。
- 影响范围 / 风险控制方式：只运行 legacy `npm run generate-client`；生成输出 diff 局限于 `frontend/src/client/sdk.gen.ts` 与 `frontend/src/client/types.gen.ts`；不修改 `frontend/src/client/compute`。
- 验证方式：OpenAPI compare from `backend/` 显示 `current_paths=88`、`local_paths=88`、`missing_paths=[]`、`extra_paths=[]`、`matches=True`；`cd frontend; npx tsc --noEmit` 通过；`git diff --check -- frontend\README.md frontend\src\client docs\rebuild tasks\todo.md .ai\plans .ai\changes\2026-05-31.md` 通过（仅 LF/CRLF 提示）；`git diff --name-only -- frontend\src\client\compute` 确认 Compute client 无变更。
- 是否更新 README：是，更新 `frontend/README.md` 的 legacy client 生成说明。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮修正生成物与 README 记录，不改变架构边界。

## 06:58 - AutoWaterSimu Next FastAPI lifespan cleanup

- 用户目标：按 PRD/Spec/Development Plan 继续推进 AutoWaterSimu Next，并收敛 Phase 0 legacy baseline 中 FastAPI `on_event` deprecation warning。
- 涉及目录：`backend/app/`、`backend/app/tests/`、`docs/rebuild/`、`tasks/`、`.ai/plans/`。
- 修改内容：将 `backend/app/main.py` 的 startup/shutdown `@app.on_event` hooks 替换为 FastAPI `lifespan` context manager，继续启动和停止 `simple_websocket_manager` 后台任务；更新 PRD 当前状态、legacy drift audit、completion audit 和任务记录。
- 修改原因：FastAPI 已将 `on_event` 生命周期钩子标记为 deprecated；该项属于 legacy baseline cleanup，可在不改变 route/schema/client 的前提下局部收敛。
- 本次已消除的不确定性：确认 `import app.main` 不再产生 FastAPI `on_event` deprecation warning；确认 targeted backend route/service tests 通过；确认 OpenAPI path count 与 missing/extra paths 不变。
- 本次采用的关键假设：本轮只迁移应用生命周期 hook，不改变 WebSocket manager 行为、FastAPI route、response model、OpenAPI schema 或 legacy generated client。
- 剩余不确定性：第三方 `python_multipart` import warning、legacy OpenAPI metadata drift、debug/test-only print noise 和 broader mojibake readability cleanup 仍需单独处理。
- 影响范围 / 风险控制方式：只改 legacy FastAPI app entrypoint 生命周期 wiring；保留 `simple_websocket_manager.start_background_tasks()` / `stop_background_tasks()` 调用顺序和异常清理；用 targeted route/service tests 和 OpenAPI path compare 防止行为扩散。
- 验证方式：warnings capture around `import app.main` 显示 `lifespan_warnings=0`；`cd backend; .venv\Scripts\python -m pytest app\tests\pydantic_warning_test.py app\tests\api\routes\test_asm_udm_validate_response.py app\tests\api\routes\test_flowchart_routes_no_print.py app\tests\services -q` 通过（`48 passed`，剩余 warning 为 `python_multipart` import deprecation）；OpenAPI compare from `backend/` 仍为 `current_paths=88`、`tracked_paths=88`、`missing_paths=[]`、`extra_paths=[]` 且只剩 metadata/description drift；`git diff --check -- backend\app\main.py docs\rebuild tasks\todo.md .ai\plans .ai\changes\2026-05-31.md` 通过（仅 LF/CRLF 提示）。
- 是否更新 README：否，未改变目录职责、公共接口或长期约定；更新 PRD 当前状态与 drift audit。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮是 legacy lifecycle warning cleanup，不改变架构边界。

## 06:54 - AutoWaterSimu Next Pydantic validator migration

- 用户目标：按 PRD/Spec/Development Plan 继续推进 AutoWaterSimu Next，并收敛 Phase 0 legacy baseline 中 Pydantic v1 validator deprecation warning。
- 涉及目录：`backend/app/`、`backend/app/tests/`、`docs/rebuild/`、`tasks/`、`.ai/plans/`。
- 修改内容：将 `backend/app/models.py` 中剩余 `@validator` 迁移为 Pydantic v2 `@field_validator`；跨字段校验改用 `ValidationInfo.data`；扩展 `pydantic_warning_test.py` 同时防 protected namespace 与 v1 validator warning 回归；更新 PRD 当前状态、legacy drift audit、completion audit 和任务记录。
- 修改原因：PRD/current audit 中仍将 Pydantic v1 validator warning 作为后续治理项；该项集中在 legacy input model，可通过保持规则不变的 API 迁移本地收敛。
- 本次已消除的不确定性：确认 `app.models` import 不再产生 Pydantic v1 validator warnings 或 protected namespace warnings；确认 targeted model/adapter/service tests 通过；确认 OpenAPI path count 与 missing/extra paths 不变。
- 本次采用的关键假设：本轮只迁移 Pydantic validator API，不改变字段名、validation rule、错误消息或 schema/client 生成策略。
- 剩余不确定性：FastAPI lifespan deprecation、python_multipart import warning、legacy OpenAPI metadata drift 仍需单独处理。
- 影响范围 / 风险控制方式：只改 legacy model validators 和 warning regression test；用 targeted backend tests 覆盖 time segment、material balance segment overrides、Hybrid UDM、UDM bindings、routes 和 services。
- 验证方式：warnings capture around `import app.models` 显示 `v1_validator_warnings=0`、`protected_namespace_warnings=0`；`cd backend; .venv\Scripts\python -m pytest app\tests\pydantic_warning_test.py app\tests\time_segment_validation_test.py app\tests\material_balance_segment_overrides_test.py app\tests\hybrid_udm_validation_test.py app\tests\udm_engine_variable_binding_test.py app\tests\api\routes\test_asm_udm_validate_response.py app\tests\services -q` 通过（`46 passed`，剩余 warnings 为 FastAPI lifespan/python_multipart）；OpenAPI compare from `backend/` 仍为 `current_paths=88`、`tracked_paths=88`、`missing_paths=[]`、`extra_paths=[]` 且只剩 metadata/description drift；`git diff --check -- backend\app\models.py backend\app\tests docs\rebuild tasks\todo.md .ai\plans .ai\changes\2026-05-31.md` 通过（仅 LF/CRLF 提示）。
- 是否更新 README：否，未改变目录职责、公共接口或长期约定；更新 PRD 当前状态与 drift audit。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮是 legacy warning cleanup，不改变架构边界。

## 06:51 - AutoWaterSimu Next Pydantic protected namespace cleanup

- 用户目标：按 PRD/Spec/Development Plan 继续推进 AutoWaterSimu Next，并收敛 Phase 0 legacy baseline 中 Pydantic protected namespace warning。
- 涉及目录：`backend/app/`、`backend/app/tests/`、`docs/rebuild/`、`tasks/`、`.ai/plans/`。
- 修改内容：为 `HybridUDMSelectedModel`、`HybridUDMConfig`、`UDMModelVersion` 和 `UDMModelVersionPublic` 增加 `ConfigDict(protected_namespaces=())`；新增 `backend/app/tests/pydantic_warning_test.py`；更新 PRD 当前状态、legacy drift audit、completion audit 和任务记录。
- 修改原因：这些模型的 `model_id` / `model_pair_mappings` 是 legacy API/DB 真实字段名，重命名会破坏兼容性；显式配置 Pydantic protected namespace 是最小兼容修复。
- 本次已消除的不确定性：确认 `import app.models` 不再产生 protected namespace warnings；确认 targeted backend tests 通过；确认 OpenAPI path count 与 missing/extra paths 不变。
- 本次采用的关键假设：保留现有字段名是兼容性要求；本轮只处理 protected namespace warning，不迁移 Pydantic v1 validators，不重写 FastAPI lifespan。
- 剩余不确定性：是否迁移 legacy Pydantic v1 validators、是否处理 FastAPI lifespan deprecation、是否刷新 legacy OpenAPI metadata drift 仍需单独评估。
- 影响范围 / 风险控制方式：只给四个模型增加 Pydantic config，不改字段名、数据库列、route response model 或 generated client；新增 warning regression test 防止回归。
- 验证方式：warnings capture around `import app.models` 显示 `protected_namespace_warnings=0`；`cd backend; .venv\Scripts\python -m pytest app\tests\pydantic_warning_test.py app\tests\api\routes\test_asm_udm_validate_response.py app\tests\hybrid_udm_validation_test.py app\tests\udm_engine_variable_binding_test.py app\tests\services -q` 通过（`36 passed`，仅剩既有 validator/lifespan warnings）；OpenAPI compare from `backend/` 仍为 `current_paths=88`、`tracked_paths=88`、`missing_paths=[]`、`extra_paths=[]` 且只剩 metadata/description drift；`git diff --check -- backend\app\models.py backend\app\tests docs\rebuild tasks\todo.md .ai\plans .ai\changes\2026-05-31.md` 通过（仅 LF/CRLF 提示）。
- 是否更新 README：否，未改变目录职责、公共接口或长期约定；更新 PRD 当前状态与 drift audit。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮是 legacy warning cleanup，不改变架构边界。

## 06:44 - AutoWaterSimu Next legacy cleanup

- 用户目标：按 PRD/Spec/Development Plan 继续推进 AutoWaterSimu Next，并收敛 Phase 0 legacy audit 中可低风险本地清理的编码/comment drift。
- 涉及目录：`backend/app/api/routes/`、`backend/app/services/`、`docs/rebuild/`、`tasks/`、`.ai/plans/`。
- 修改内容：移除 `backend/app/api/routes/material_balance.py` 的 UTF-8 BOM；删除 `backend/app/services/data_conversion_service.py` 中已注释的 mojibake debug `print` 残留和死的注释异常调试块；更新 legacy drift audit、completion audit 和任务记录。
- 修改原因：Phase 0 audit 已确认这些是无行为意义的编码/可读性 debt；清理后 no-print guard 不再需要依赖该文件的 BOM 事实，转换服务也不再保留误导性的注释 debug 输出。
- 本次已消除的不确定性：确认 BOM 已移除；确认清理对象均为注释或编码层面变更，不改变 route/service runtime 逻辑；确认 Pydantic protected namespace warning 涉及 schema/model config，需单独处理。
- 本次采用的关键假设：本轮不刷新 legacy OpenAPI/client，因为未改变请求/响应 schema；不处理 ad hoc/debug/test-only active prints，因为它们不在 runtime route/service 路径。
- 剩余不确定性：是否清理 Pydantic protected namespace warnings、是否清理更大范围 legacy mojibake 注释/docstring、是否刷新 legacy OpenAPI metadata drift 仍需单独评估。
- 影响范围 / 风险控制方式：仅改一个 route 文件编码和一个 service 文件注释；不改计算逻辑、response model、generated client 或 OpenAPI source。
- 验证方式：BOM check 确认 `material_balance.py` 不再以 `EF BB BF` 开头；`cd backend; .venv\Scripts\python -m pytest app\tests\api\routes\test_flowchart_routes_no_print.py app\tests\api\routes\test_asm_udm_validate_response.py app\tests\services -q` 通过；OpenAPI compare 首次从仓库根目录运行因 backend settings 未加载 `.env` 必需变量失败，改从 `backend/` 运行后确认 `current_paths=88`、`tracked_paths=88`、`missing_paths=[]`、`extra_paths=[]` 且只剩 metadata/description drift；`git diff --check -- backend\app\api\routes\material_balance.py backend\app\services\data_conversion_service.py docs\rebuild tasks\todo.md .ai\plans .ai\changes\2026-05-31.md` 通过（仅 LF/CRLF 提示）。
- 是否更新 README：否，未改变目录职责、公共接口或长期约定；更新了 drift audit 文档。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮是 legacy 编码/注释清理，不改变架构边界。

## 06:39 - AutoWaterSimu Next browser gate expansion

- 用户目标：按 PRD/Spec/Development Plan 继续推进 AutoWaterSimu Next，并补齐 Phase 4 Web Compute API 中浏览器覆盖仍偏窄的问题。
- 涉及目录：`.github/workflows/`、`scripts/release/`、`frontend/tests/`、`tasks/`、`.ai/plans/`。
- 修改内容：`next-release-gates.ps1 -RunBrowserSmoke` 现在同时运行 `compute-jobs-current-flow.spec.ts` 和 `compute-lifecycle.spec.ts`；workflow/input 文案和 release/frontend tests README 同步更新；completion audit 和任务记录更新为当前覆盖面。
- 修改原因：已有 Compute lifecycle 页面和 Playwright smoke，但 release gate 的 opt-in browser smoke 只覆盖 Compute Jobs current-flow，未覆盖 lifecycle metrics/retention 控制。
- 本次已消除的不确定性：确认两个 smoke 都是 mock-backed、可用 `--no-deps` 聚焦运行，不需要 live backend 或部署 Go API；确认默认 PR gate 不会因此变重。
- 本次采用的关键假设：P0 browser gate 继续保持 opt-in；真实 deployed API/browser E2E 仍依赖部署 URL、token/secret policy 和环境稳定性。
- 剩余不确定性：live deployed API/browser E2E、CI runner browser timing、是否把 browser smoke 提升为默认 gate 仍需部署/成本决策。
- 影响范围 / 风险控制方式：只改 release gate 编排和文档；不改 UI runtime；browser smoke 仍由 manual input / `-RunBrowserSmoke` 显式开启。
- 验证方式：`cd frontend; npx playwright test tests/compute-jobs-current-flow.spec.ts tests/compute-lifecycle.spec.ts --project=chromium --no-deps --reporter=line` 通过；`.\scripts\release\next-release-gates.ps1 -Mode merge -SkipLong -RunBrowserSmoke -EvidenceDir tmp\release-evidence\browser-gate-expanded` 通过；`git diff --check -- .github scripts frontend\tests tasks\todo.md .ai\plans .ai\changes\2026-05-31.md` 通过（仅 LF/CRLF 提示）。
- 是否更新 README：是，更新 `.github/workflows/README.md`、`scripts/release/README.md` 和 `frontend/tests/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮是 release/browser gate 覆盖扩展，不改变架构边界。

## 06:27 - AutoWaterSimu Next CI PostgreSQL migration smoke wiring

- 用户目标：按 PRD/Spec/Development Plan 继续推进 AutoWaterSimu Next，并把 PostgreSQL migration up/down smoke 从手动 Docker 证据推进到 CI 可触发入口。
- 涉及目录：`.github/`、`.github/workflows/`、`scripts/`、`scripts/release/`、`frontend/src/client/`、`tasks/`、`.ai/plans/`。
- 修改内容：为 `next-release-gates.yml` 新增 `run_postgres_migration_smoke` manual dispatch input 和 opt-in `postgres-migration-smoke` job；该 job 使用临时 `postgres:16-alpine` service database 并设置 `COMPUTE_API_MIGRATION_DOWN_SMOKE=true` 后运行 Go migration up/down smoke；release gate Compute client whitespace normalization 从 `sdk.gen.ts` 扩展到 `frontend/src/client/compute/**/*.ts`；提交七个 generated Compute client `core/` / `index.ts` 文件的末尾换行归一化结果；同步更新 GitHub/workflow/release/frontend client README、completion audit 和任务记录。
- 修改原因：migration rollback smoke 已在 Go 测试和 release script 中存在，但缺少 CI 级临时数据库入口；继续依赖手动 Docker evidence 不利于 release governance。
- 本次已消除的不确定性：确认 rollback smoke 必须 opt-in，因为 down migration 会删除 metadata tables；确认 GitHub-hosted Windows Desktop gate 不适合混入 service database，因此新增独立 Linux job；确认 CI database URL 指向 workflow 临时服务而非外部环境。
- 本次采用的关键假设：production/shared database 不会用于 migration smoke；真实 live runner evidence 需要后续手动触发 `workflow_dispatch`；本轮不改变默认 PR gate 的耗时或行为。
- 剩余不确定性：GitHub live runner service startup timing、PostgreSQL service health behavior、workflow artifact/evidence retention 仍需真实 dispatch 验证。
- 影响范围 / 风险控制方式：新增 job 只在 manual input 为 `true` 时运行；数据库凭据是临时服务内的测试值；不接触生产 secret；默认 `next-gates` Windows job 保持不变。
- 验证方式：YAML parser 断言新 input、service image、migration test command 和 `COMPUTE_API_MIGRATION_DOWN_SMOKE=true` wiring 通过；首次 `.\scripts\release\next-release-gates.ps1 -Mode merge -SkipLong -EvidenceDir tmp\release-evidence\ci-postgres-wiring-gate` 在 `compute client diff gate` 失败，原因是 codegen 移除了 `types.gen.ts` 末尾换行而旧 normalization 只处理 `sdk.gen.ts`；扩展 normalization 后第二次运行暴露七个旧 generated 文件无末尾换行，已作为机械 generated 输出纳入本次变更；`git diff --check -- .github scripts frontend\src\client tasks\todo.md .ai\plans .ai\changes\2026-05-31.md` 通过（仅 LF/CRLF 提示）。
- 是否更新 README：是，更新 `.github/README.md`、`.github/workflows/README.md`、`scripts/README.md`、`scripts/release/README.md` 与 `frontend/src/client/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮是 CI 编排入口，不改变数据库或 release 架构边界。

## 06:22 - AutoWaterSimu Next monitoring deployment examples

- 用户目标：按 PRD/Spec/Development Plan 继续推进 AutoWaterSimu Next，并补齐 lifecycle/operations 中 Alertmanager/Grafana 监控样例和部署核对手册缺口。
- 涉及目录：`docs/operations/`、`docs/operations/monitoring/`、`tasks/`、`.ai/plans/`。
- 修改内容：新增 `alertmanager_route_example.yml`、`compute_api_grafana_dashboard.json` 与 `monitoring_deployment_runbook.md`；更新 monitoring/operations README、completion audit 和任务记录。
- 修改原因：已有 Prometheus alert rules，但缺少可复用的 routing/dashboard 示例和接入真实监控系统的核对流程；同时需要继续避免把示例文件误写成生产监控已部署。
- 本次已消除的不确定性：确认当前 dashboard 只能引用 `autowatersimu_compute_api_up`、`autowatersimu_compute_jobs_total`、`autowatersimu_compute_workers_registered_total`、`autowatersimu_compute_artifacts_total` 和 `autowatersimu_compute_artifact_retention_candidates_total`；确认 Alertmanager 示例必须使用占位 receiver，不记录真实 URL 或 secret。
- 本次采用的关键假设：生产 Prometheus/Alertmanager/Grafana 托管、真实 receiver/on-call 策略和 live dashboard 验证属于部署环境职责；本轮只提交可复制样例和 runbook，不新增运行时代码或 metrics。
- 剩余不确定性：生产监控部署位置、receiver secret 管理、on-call/escalation 策略、dashboard datasource uid、真实 deployed scrape/alert 验证仍需部署级决策。
- 影响范围 / 风险控制方式：仅新增运维文档和示例配置；示例 webhook 使用 `example.invalid` 占位；dashboard 只引用已由代码暴露的指标；README 明确示例不等于生产部署。
- 验证方式：`backend\.venv\Scripts\python` 解析 alert YAML、Alertmanager YAML 和 Grafana JSON 通过；`git diff --check -- docs\operations tasks\todo.md .ai\plans .ai\changes\2026-05-31.md` 通过（仅 LF/CRLF 提示）。
- 是否更新 README：是，更新 `docs/operations/monitoring/README.md` 与 `docs/operations/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮是运维样例和部署核对手册，不改变架构边界。

## 06:08 - AutoWaterSimu Next legacy Phase 0 drift audit

- 用户目标：按 PRD/Spec/Development Plan 继续推进 AutoWaterSimu Next，并补齐 Phase 0 legacy baseline 中 `print` / schema / client drift 的精确记录。
- 涉及目录：`backend/app/tests/`、`docs/rebuild/`、`tasks/`、`.ai/plans/`。
- 修改内容：新增 `docs/rebuild/AutoWaterSimu_Next_Legacy_Phase0_Drift_Audit_2026-05-31.md`；记录 active `print(...)` 只剩 ad hoc/debug/test-only 文件，route modules 无 active print；记录当前 FastAPI OpenAPI 与 `frontend/openapi.json` 路径数一致且无 missing/extra paths，剩余差异为 title 和六个 legacy 计算入口 docstring description metadata；将 route no-print regression 从 ASM flowchart 子集扩大到所有 `backend/app/api/routes/*.py`；更新 rebuild README、completion audit 和任务记录。
- 修改原因：完成度审计中 Phase 0 剩余项不是继续大改 legacy，而是把真实剩余 drift 精确化，避免后续把测试脚本 print 或 OpenAPI 描述差异误判为运行时 schema drift。
- 本次已消除的不确定性：确认 legacy route modules 当前无 active print；确认 tracked OpenAPI 与当前 app OpenAPI path count 都是 88，且无 path 增删；确认剩余 OpenAPI 差异不涉及 request/response schema shape；确认 Pydantic protected namespace warnings 是 legacy model cleanup debt，不是 client schema drift。
- 本次采用的关键假设：本轮只记录和加固 guard，不重新生成 legacy `frontend/openapi.json` / legacy client，因为剩余 drift 是 metadata/description 级，不改变调用行为。
- 剩余不确定性：是否要为了 byte-for-byte 元数据一致性单独刷新 legacy OpenAPI/client；是否清理 debug/test-only print 和 `data_conversion_service.py` 中 mojibake 注释；是否调整 Pydantic protected namespace model config。
- 影响范围 / 风险控制方式：只新增 drift audit 文档并扩大 route no-print AST guard；不改 legacy runtime、OpenAPI source、generated legacy client 或计算行为。
- 验证方式：首次 targeted pytest 因 `material_balance.py` 带 UTF-8 BOM 导致 AST parse 失败，已将 no-print guard 改为 `utf-8-sig`；`cd backend; .venv\Scripts\python -m pytest app\tests\api\routes\test_flowchart_routes_no_print.py app\tests\api\routes\test_asm_udm_validate_response.py -q` 通过（`21 passed`，保留既有 warnings）；FastAPI OpenAPI compare 确认 `current_paths=88`、`tracked_paths=88`、`missing_paths=[]`、`extra_paths=[]`，`matches=False` 仅因 metadata/description drift；`git diff --check -- backend\app\tests docs\rebuild tasks\todo.md .ai\plans .ai\changes\2026-05-31.md` 通过（仅 LF/CRLF 提示）。
- 是否更新 README：是，更新 `docs/rebuild/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮是 legacy baseline audit，不改变架构边界。

## 05:57 - AutoWaterSimu Next local artifact archive backend

- 用户目标：按 PRD/Spec/Development Plan 继续推进 AutoWaterSimu Next，并补齐 lifecycle/operations 中 archive backend 的本地可实现部分。
- 涉及目录：`apps/api/`、`apps/api/internal/compute/`、`apps/api/cmd/compute-api/`、`apps/api/migrations/`、`apps/api/openapi/`、`frontend/src/client/compute/`、`frontend/src/routes/`、`frontend/tests/`、`docs/rebuild/`、`docs/operations/`、`.ai/decisions/`、`tasks/`、`.ai/plans/`。
- 修改内容：新增 `artifact_archives` PostgreSQL migration 和 memory-store metadata；新增 opt-in `COMPUTE_API_ARCHIVE_DIR` / `local_fs_archive` wiring；retention sweep 对过期未引用 `archive_candidate` 在 archive store 配置时执行 copy、checksum verify、durable metadata/event、hot object delete；普通 artifact download 在 hot object 缺失时可通过 archive metadata 回退读取；未配置 archive store 时继续返回 `archive_executor_not_configured`；OpenAPI/generated compute client/Compute Jobs/Compute lifecycle UI 增加 `archived`、`would_archive`、`archived` action 支持；同步更新 README、runbook、ADR、Spec/Development Plan、审计和任务记录。
- 修改原因：ADR 0010 已定义 archive copy-before-delete 安全模型，但运行时仍只能跳过 `archive_candidate`；需要先实现一个本地、显式启用、可测试的 archive backend，避免把 archive candidates 当成普通 TTL 删除。
- 本次已消除的不确定性：确认可用单独 local artifact store 表达 archive storage；确认 archive metadata 需要独立 PostgreSQL 表而不是只靠 job event；确认 generated Compute client 需要同步 OpenAPI action/count 字段；确认 UI 不能把 `archive_executor_not_configured` blocker 当成可处理项。
- 本次采用的关键假设：`local_fs_archive` 是第一阶段本地 backend；外部对象存储、restore UI、archive-specific metrics 和生产存储策略仍待后续；本轮保留普通 artifact endpoint 下载语义，通过 archive fallback 读取已归档文件。
- 剩余不确定性：生产 object-store archive provider、archive restore/read smoke against deployed storage、archive-specific metrics/dashboard、对象存储权限/生命周期策略仍需部署级设计。
- 影响范围 / 风险控制方式：archive 处理默认关闭，只有设置 `COMPUTE_API_ARCHIVE_DIR` 才启用；归档前仍检查 model_run evidence refs；先复制并校验 archive copy，再写 metadata/event，最后删除 hot object；未配置时行为保持 skip。
- 验证方式：`gofmt -w ...; cd apps\api; go test ./...` 通过；`cd frontend; npm run generate-compute-client` 通过，随后仅清理 generated client 尾随空格以保持最小 diff；`cd frontend; npx tsc --noEmit` 通过；`cd frontend; npx playwright test tests/compute-lifecycle.spec.ts --project=chromium --no-deps --reporter=line` 通过（`1 passed`）；`git diff --check -- apps\api frontend docs\operations docs\rebuild .ai\decisions tasks\todo.md .ai\plans .ai\changes\2026-05-31.md` 通过（仅 LF/CRLF 提示）。
- 是否更新 README：是，更新 Compute API、cmd、internal compute、migrations、frontend routes、operations runbook/README、backup restore runbook 与 rebuild docs。
- 是否需要新增或更新架构决策（.ai/decisions/）：是，更新 ADR 0010，从 future boundary 更新为已实现 `local_fs_archive` 且保留 future object-store 边界。

## 05:48 - AutoWaterSimu Next Compute API token secret runbook

- 用户目标：按 PRD/Spec/Development Plan 继续推进 AutoWaterSimu Next，并补齐 Compute API 生产 token/secret handling 与 rotation 运维文档缺口。
- 涉及目录：`docs/operations/`、`tasks/`、`.ai/plans/`。
- 修改内容：新增 `docs/operations/compute_api_token_secret_runbook.md`，记录 `COMPUTE_API_TOKENS_JSON` 静态 bearer token 模型、scope 边界、secret storage rules、重叠轮换、`revoked:true` 紧急吊销、验证命令和 incident response；更新 operations README、lifecycle runbook、completion audit 和任务记录。
- 修改原因：当前代码已有静态 token scope 与 config-level revocation，但运维侧缺少生产 token 存储、轮换和泄露处置流程；这会影响上线前 auth/secrets review。
- 本次已消除的不确定性：确认 P0 不实现 full RBAC 或动态 token 管理 API；确认 `COMPUTE_API_TOKENS_JSON` 由进程启动读取，配置变更需要重启/部署；确认重复 token 会被拒绝，`revoked:true` token 会返回 unauthorized。
- 本次采用的关键假设：生产 token 由部署 secret manager 或 CI secrets 注入；本轮只补运维文档，不新增 API、secret manager 集成、RBAC、rate limit 或动态轮换服务。
- 剩余不确定性：具体生产 secret manager、token 创建/审批人、部署重启窗口、日志留存/审计平台和真实 deployed API 验证仍需部署级决策。
- 影响范围 / 风险控制方式：只新增 runbook 和索引/审计记录；所有 token 示例使用占位符；不记录真实 secret；不改变 Go API 行为或 OpenAPI。
- 验证方式：`git diff --check -- docs\operations tasks\todo.md .ai\plans .ai\changes\2026-05-31.md` 通过（仅 LF/CRLF 提示）；`cd apps\api; go test ./internal/compute -run "TestHTTPAuthScopeAndMetrics|TestStaticTokenRevocation|TestHTTPArtifactRetentionSweepRequiresAdminScope" -count=1` 通过。
- 是否更新 README：是，更新 `docs/operations/README.md` 与 lifecycle runbook 链接。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮记录当前 P0 static token 运维流程，不改变 auth 架构。

## 05:39 - AutoWaterSimu Next release runner timing hardening

- 用户目标：按 PRD/Spec/Development Plan 继续推进 AutoWaterSimu Next，并收敛 release governance 中 release runner cache/timing hardening 的本地可完成部分。
- 涉及目录：`.github/`、`.github/workflows/`、`tasks/`、`.ai/plans/`。
- 修改内容：为 `next-release-gates.yml` 增加 concurrency cancellation；`setup-node` 启用 frontend/Desktop npm lockfile cache；`setup-go` 显式使用 `apps/api/go.sum` cache dependency path；新增 Rust build output cache；manual dispatch 增加 `skip_long` input 并传入 release gate 脚本；同步更新 GitHub/workflow README、completion audit 和任务记录。
- 修改原因：本地 release gate 与 workflow artifact build 已存在，但 runner 耗时控制还缺少缓存、并发取消和手动轻量验证入口。
- 本次已消除的不确定性：确认默认 PR gate 不传 `-SkipLong`；确认 manual dispatch opt-in 参数可控制长构建步骤；确认缓存只影响依赖/build 输出，不改变 gate 判断逻辑。
- 本次采用的关键假设：允许使用 `Swatinem/rust-cache@v2` 作为 Rust target cache action；workflow artifact upload 仍保持 unsigned artifact/evidence boundary。
- 剩余不确定性：真实 GitHub Windows runner 上的构建耗时、artifact upload/download retention 和缓存命中效果仍需 live `workflow_dispatch` 验证。
- 影响范围 / 风险控制方式：只改 CI workflow 编排和文档；默认 PR gate 行为保持 full gate；release script 本身不改。
- 验证方式：首次尝试使用 Bash heredoc 形式调用 Python 在 PowerShell 下失败，改用 PowerShell here-string 后 `backend\.venv\Scripts\python` + PyYAML 成功解析 `.github/workflows/next-release-gates.yml` 并断言 concurrency/cache steps；`git diff --check -- .github tasks\todo.md .ai\plans .ai\changes\2026-05-31.md` 通过（仅 LF/CRLF 提示）；`.\scripts\release\next-release-gates.ps1 -Mode merge -SkipLong -EvidenceDir tmp\release-evidence\workflow-timing-gate` 通过，evidence status 为 `passed`，共 `10` steps。
- 是否更新 README：是，更新 `.github/README.md` 与 `.github/workflows/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，遵循既有 release boundary ADR。

## 05:34 - AutoWaterSimu Next Desktop file-backed project restore

- 用户目标：按 PRD/Spec/Development Plan 继续推进 AutoWaterSimu Next，并补齐 Desktop 项目包只能恢复 project/CanvasGraphs、job/artifact/support bundle 仍 metadata-only 的缺口。
- 涉及目录：`apps/desktop/`、`apps/desktop/src-tauri/`、`apps/desktop/src-tauri/src/`、`apps/desktop/src/`、`apps/desktop/src/lib/`、`tasks/`、`.ai/plans/`。
- 修改内容：`desktop_project_export.v1` 继续兼容旧格式，但当前导出会包含 job input snapshots、job events、artifact/support bundle refs 及 checksum-verified hex-encoded file records；导入时先校验并写入 artifact/support bundle 文件，再恢复 compute jobs、artifacts、model_runs、job_events 与 support_bundles；旧 metadata-only 包仍可导入但不会创建指向缺失文件的 job/artifact/support bundle rows；Desktop UI/types 展示 restored/file counts。
- 修改原因：完成度审计中 Phase 3 剩余 `File-backed package restore beyond CanvasGraphs`，而之前导入端只能恢复项目元数据和 CanvasGraphs。
- 本次已消除的不确定性：确认当前 SQLite 表已具备 jobs/events/artifacts/model_runs/support_bundles 所需字段；确认 artifact/support bundle 文件都在 runtime-local 目录内，可通过 `safe_relative_path` 和 checksum 恢复；确认不需要新增 DB migration 或 Tauri filesystem plugin 权限。
- 本次采用的关键假设：当前 P0 项目包仍为单个 `.autowatersimu-project.json` 文件；为避免新增依赖，文件内容使用 hex encoding；若旧包缺少文件内容或 job input，则对应 rows 保持 metadata-only，不创建悬挂记录。
- 剩余不确定性：更大规模项目包的体积/压缩格式、目录型 package、持久化 ProcessGraph restore、以及跨版本 package migration 仍需后续设计。
- 影响范围 / 风险控制方式：文件读写仍只在 Rust runtime 内执行；导入前校验 checksum 和 size；若目标文件已存在但 checksum 不同则拒绝覆盖；React 只展示 counts，不解码文件内容。
- 验证方式：`cargo fmt --manifest-path apps\desktop\src-tauri\Cargo.toml` 通过；首次 Rust test 因测试仍假设 JSON 明文包含 `timestamps` 失败，改为校验 hex file record 和导入后恢复的 artifact 文件内容；`cargo test --manifest-path apps\desktop\src-tauri\Cargo.toml` 通过（`22 passed`）；`cd apps\desktop; npm run typecheck` 通过；`cd apps\desktop; npm run build` 通过；Browser preview against `http://127.0.0.1:1420/` 确认 `Desktop Runtime`、`Project Export`、`Project Import`、`No project export yet.`、`No project import yet.`、`No recent project files yet.` 与 browser-only runtime notice 渲染，首次导航超时但页面已加载，复查 snapshot 通过，临时 Vite dev server 已停止；`git diff --check -- apps\desktop tasks\todo.md .ai\plans .ai\changes\2026-05-31.md` 通过（仅 LF/CRLF 提示）。
- 是否更新 README：是，更新 `apps/desktop/README.md`、`apps/desktop/src-tauri/README.md`、`apps/desktop/src-tauri/src/README.md`、`apps/desktop/src/README.md`、`apps/desktop/src/lib/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮实现既有 Desktop project package restore 缺口，不改变长期 release/signing 边界。

## 05:23 - AutoWaterSimu Next Desktop release policy boundary

- 用户目标：按 PRD/Spec/Development Plan 继续推进 AutoWaterSimu Next，并把 Desktop/release 中 signing、auto-update、GitHub Release publication 的剩余不确定性收敛成明确边界。
- 涉及目录：`.ai/decisions/`、`apps/desktop/`、`.github/`、`scripts/release/`、`docs/rebuild/`、`tasks/`、`.ai/plans/`。
- 修改内容：新增 `.ai/decisions/0011-desktop-release-signing-auto-update-boundary.md`；明确 P0 release artifact 是 unsigned sidecar/NSIS installer 加 smoke evidence，GitHub Actions manual dispatch 只上传 unsigned workflow artifacts；signing、auto update、Microsoft Store/MSI 和 GitHub Release publication 均为 post-P0 policy-driven work；同步更新 Desktop、packaging、src-tauri、GitHub、workflow、release README、Development Plan checklist、completion audit 和任务记录。
- 修改原因：PRD/Spec/Development Plan 已把 signing/auto-update/Store 写成 P0 非目标，但完成度审计仍把 signing/auto-update decisions 作为未收敛项；需要用 ADR 固化边界，避免后续把缺少证书/更新策略的实现伪装成 release-ready。
- 本次已消除的不确定性：确认 PRD、Technical Spec、Development Plan 三份权威文档均将 signing、auto-update、Store 放到 P0 之外；确认当前 workflow 只构建/上传 unsigned artifacts；确认 Tauri release config 未启用 updater 或 signing。
- 本次采用的关键假设：当前 P0 不需要生产分发级签名或自动更新；GitHub Release publication 需要 tag/version、release notes、manual approval、token scope 与 artifact download verification 策略后再实现。
- 剩余不确定性：真实 GitHub Windows runner release-build timing/download verification、release runner cache/timing hardening、未来签名证书/时间戳服务、updater channel、GitHub Release 发布策略仍待后续。
- 影响范围 / 风险控制方式：仅新增 ADR 和文档/审计更新，不修改 runtime、workflow behavior 或 secret handling；明确 unsigned artifacts 不能被写成 signed production distribution。
- 验证方式：`git diff --check -- .ai\decisions apps\desktop .github scripts\release docs\rebuild tasks .ai\plans .ai\changes\2026-05-31.md` 通过（仅 LF/CRLF 提示）；`rg -n "schema_version|P0|P1|P2" docs\rebuild --glob "AutoWaterSimu_Next_*.md"` 完成，确认 Development Plan checklist 更新仍与 PRD/Spec 的 P0 非目标一致。
- 是否更新 README：是，更新 Desktop、packaging、src-tauri、GitHub、workflow 和 release README。
- 是否需要新增或更新架构决策（.ai/decisions/）：是，新增 ADR `0011-desktop-release-signing-auto-update-boundary.md`。

## 03:47 - AutoWaterSimu Next Compute API alert rules

- 用户目标：按 PRD/Spec/Development Plan 继续推进 AutoWaterSimu Next，并补齐 Phase 6.4 lifecycle/operations 中 metrics 已有但缺少可执行告警规则的问题。
- 涉及目录：`docs/operations/`、`docs/operations/monitoring/`、`tasks/`、`.ai/plans/`。
- 修改内容：新增 `docs/operations/monitoring/README.md` 与 `compute_api_alerts.yml`；alert rules 覆盖 API down、queued jobs without workers、failed/timed-out jobs 和 persistent artifact retention backlog；runbook 链接该规则文件；更新任务记录和 completion audit。
- 修改原因：`/metrics` 已暴露 Compute API lifecycle gauges，但运维侧还没有可复制到 Prometheus 的规则文件；直接声称 Alertmanager/dashboard 已完成会不符合当前代码事实。
- 本次已消除的不确定性：确认仓库没有现有 Prometheus/alert 配置目录；确认规则只引用当前 `/metrics` 已暴露的指标；确认 YAML 结构可由 PyYAML 解析。
- 本次采用的关键假设：本轮只提供 Prometheus alert rules，不部署 Prometheus/Alertmanager、不定义生产通知渠道、不提供 dashboard provisioning；环境阈值后续可覆盖。
- 剩余不确定性：生产 Alertmanager routing、dashboard provisioning、环境级阈值、archive backend 和 backup/restore policy 仍待后续。
- 影响范围 / 风险控制方式：仅新增运维配置文档，不改运行时行为；规则文件明确不代表生产告警已部署。
- 验证方式：`git diff --check -- docs\operations` 通过（仅 LF/CRLF 提示）；本机无 `promtool`；`backend\.venv\Scripts\python` + PyYAML 成功解析 alert YAML 并校验 group/rule/alert/expr 结构。
- 是否更新 README：是，新增 `docs/operations/monitoring/README.md` 并更新 runbook/operations README。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮是基于现有 metrics 的运维规则文件，不改变架构边界。

## 03:53 - AutoWaterSimu Next GitHub release artifact build wiring

- 用户目标：按 PRD/Spec/Development Plan 继续推进 AutoWaterSimu Next，并补齐 release governance 中 CI artifact build/publish 的明确入口。
- 涉及目录：`.github/`、`.github/workflows/`、`apps/desktop/packaging/`、`scripts/release/`、`tasks/`、`.ai/plans/`。
- 修改内容：为 `.github/workflows/next-release-gates.yml` 增加 `build_release_artifacts` manual dispatch input；当显式开启时构建 PyInstaller sidecar、构建 NSIS installer、从 build manifest 读取 artifact 路径、传入 release gate，并上传 unsigned Desktop artifacts 与 build/smoke evidence；同步更新相关 README、任务记录和 completion audit。
- 修改原因：本地已有 sidecar/installer build 与 smoke 证据，但 GitHub workflow 只能消费外部路径或 dry-run missing artifacts；需要一个显式、可审计的 CI artifact build/upload 入口。
- 本次已消除的不确定性：确认 packaging 脚本会写出 `sidecar_executable` 与 `installer_path` manifest 字段；确认 release gate 参数仍能从显式路径运行；确认 PR/merge 默认 gate 不会被 artifact build 变重。
- 本次采用的关键假设：本轮只上传 unsigned workflow artifacts，不做 code signing、auto-update、Microsoft Store/GitHub Release publication 或证书/更新密钥处理。
- 剩余不确定性：尚未触发真实 GitHub `workflow_dispatch` runner 验证构建耗时、artifact upload/download retention；签名、auto-update 和 GitHub Release 发布策略仍未定义。
- 影响范围 / 风险控制方式：仅修改 manual workflow dispatch 编排和文档；默认 PR gate 行为不变；artifact 路径来自 build manifest，不在 YAML 中猜测生成文件名。
- 验证方式：`backend\.venv\Scripts\python` + PyYAML 成功解析 workflow；`.\scripts\release\next-release-gates.ps1 -Mode merge -SkipLong -EvidenceDir tmp\release-evidence\workflow-artifact-build-gate` 通过并产生 `10` 个 passed steps；`git diff --check -- .github apps\desktop\packaging scripts\release tasks .ai\plans .ai\changes` 通过（仅 LF/CRLF 提示）；本机未安装 `actionlint`。
- 是否更新 README：是，更新 `.github/README.md`、`.github/workflows/README.md`、`apps/desktop/packaging/README.md`、`scripts/release/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮遵循既有 P0 unsigned artifact / no signing / no auto-update 边界，不新增长期架构决策。

## 04:01 - AutoWaterSimu Next Compute API backup restore policy

- 用户目标：按 PRD/Spec/Development Plan 继续推进 AutoWaterSimu Next，并补齐 lifecycle/operations 中启用 retention deletion 前缺少 backup/restore policy 的问题。
- 涉及目录：`docs/operations/`、`tasks/`、`.ai/plans/`。
- 修改内容：新增 `docs/operations/compute_api_backup_restore_runbook.md`；记录 PostgreSQL metadata 与 `COMPUTE_API_ARTIFACT_DIR` 必须作为一致性单元备份/恢复；补充 pre-backup、backup、restore、post-restore verification 和 recovery limits；从 lifecycle runbook 与 operations README 链接该手册。
- 修改原因：当前 retention sweep 可以删除过期未引用 TTL artifact，但 lifecycle runbook 只说明删除不可由 API 自身恢复，缺少操作层面备份/恢复边界。
- 本次已消除的不确定性：确认当前 durable Compute API 事实是 PostgreSQL metadata + local artifact directory；确认 in-memory mode 不具备 durable backup；确认 archive backend/object-store versioning 未实现，不能写成当前事实。
- 本次采用的关键假设：本轮提供 operator runbook，不新增 API、scheduler、restore endpoint 或自动备份服务；备份命令以 PowerShell + `pg_dump`/`pg_restore`/`robocopy` 为参考。
- 剩余不确定性：生产环境的 managed PostgreSQL backup、object-store/versioning 策略、RPO/RTO、archive backend、自动备份调度仍需部署级决策。
- 影响范围 / 风险控制方式：仅新增运维文档，不改变运行时行为；restore 操作明确标记为 destructive，要求停止 API/worker 并校验 manifest/hash。
- 验证方式：`git diff --check -- docs tasks .ai\plans .ai\changes` 通过（仅 LF/CRLF 提示）。
- 是否更新 README：是，更新 `docs/operations/README.md` 并链接 lifecycle runbook。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮是当前部署形态的操作手册，不决定 archive/object-store 长期架构。

## 04:03 - AutoWaterSimu Next artifact archive backend boundary

- 用户目标：按 PRD/Spec/Development Plan 继续推进 AutoWaterSimu Next，并补齐 lifecycle/operations 中 archive backend 缺少长期决策边界的问题。
- 涉及目录：`.ai/decisions/`、`apps/api/`、`apps/api/internal/compute/`、`docs/operations/`、`tasks/`、`.ai/plans/`。
- 修改内容：新增 `.ai/decisions/0010-artifact-archive-backend-boundary.md`；明确当前 `archive_candidate` 继续被 retention sweep 跳过，未来 archive backend 必须先 copy、checksum verify、durably record archive metadata，再允许删除 hot-storage artifact；更新 Compute API README、internal compute README、lifecycle runbook、任务记录和 completion audit。
- 修改原因：`artifact.v1` 已允许 `archive_candidate`，但当前没有 archive executor；直接实现删除或临时复制会破坏 evidence/artifact download 语义。
- 本次已消除的不确定性：确认当前 runtime 只安全支持 expired unreferenced `ttl` 删除；确认 `archive_candidate` backlog 需要显式 blocker 解释；确认本轮不需要 OpenAPI 或 schema 变更。
- 本次采用的关键假设：先记录长期架构边界，不引入 archive store、migration、OpenAPI 字段或 scheduler 行为变化。
- 剩余不确定性：archive storage provider、archive metadata schema/table、archived artifact download UX、restore/read smoke 和生产存储策略仍待后续实现。
- 影响范围 / 风险控制方式：仅更新 ADR 与 README/runbook；运行时行为不变，避免静默扩大 retention deletion 范围。
- 验证方式：`git diff --check -- .ai\decisions apps\api\README.md apps\api\internal\compute\README.md docs\operations tasks .ai\plans .ai\changes` 通过（仅 LF/CRLF 提示）。
- 是否更新 README：是，更新 `apps/api/README.md`、`apps/api/internal/compute/README.md`、`docs/operations/compute_api_lifecycle_runbook.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：是，新增 ADR `0010-artifact-archive-backend-boundary.md`。

## 04:36 - AutoWaterSimu Next Compute lifecycle admin UI

- 用户目标：按 PRD/Spec/Development Plan 继续推进 AutoWaterSimu Next，并补齐 lifecycle/operations 中缺少 dedicated lifecycle admin UI 的问题。
- 涉及目录：`frontend/src/routes/`、`frontend/src/services/`、`frontend/src/components/Common/`、`frontend/src/i18n/`、`frontend/tests/`、`tasks/`、`.ai/plans/`。
- 修改内容：新增 `/compute-lifecycle` route；展示 Compute API health/metrics 解析结果；提供 artifact retention dry-run/delete controls，删除按钮仅在 dry-run 报告含 `would_delete` 后启用；展示 archive blocker count；新增 sidebar nav、i18n key、generated route tree 和 mock-backed Playwright smoke。
- 修改原因：Compute Jobs 页面已有 retention panel，但 lifecycle/operations 缺少独立 admin 视图来集中展示 metrics、retention 报告和 archive blocker。
- 本次已消除的不确定性：确认前端可通过 generated Compute client 读取 `/metrics` 文本；确认 retention 删除仍复用后端 `artifact:admin` 和 dry-run guard；确认 `archive_candidate` 只显示 blocker，不在 UI 端解释成可删除。
- 本次采用的关键假设：本轮只做 lifecycle admin 页面，不新增后端 API，不实现 Alertmanager/dashboard deployment，不实现 archive backend。
- 剩余不确定性：生产权限入口、完整 dashboard provisioning、archive backend implementation 和环境级告警部署仍待后续。
- 影响范围 / 风险控制方式：前端新增路由和 nav；后端行为不变；mock-backed Playwright 覆盖 metrics 展示、dry-run guard、delete call 和 archive blocker。
- 验证方式：`cd frontend; npx vite build` 通过并生成 route tree（保留既有 Vite warnings）；`cd frontend; npx tsc --noEmit` 通过；`cd frontend; npx playwright test tests/compute-lifecycle.spec.ts --project=chromium --no-deps --reporter=line` 通过（`1 passed`）；`git diff --check -- frontend tasks .ai\plans .ai\changes` 通过（仅 LF/CRLF 提示）。第一次 Playwright 使用 Windows 反斜杠路径未匹配到测试；第二次默认 HTML reporter 在失败后留下 report server，已停止对应 Playwright node/Chrome 进程并用 `--reporter=line` 重跑。
- 是否更新 README：是，更新 `frontend/src/routes/README.md` 和 `frontend/src/services/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，遵循既有 artifact retention 和 archive backend boundary。

## 04:47 - AutoWaterSimu Next Desktop project package contents

- 用户目标：按 PRD/Spec/Development Plan 继续推进 AutoWaterSimu Next，并补齐 Desktop completion 中项目包内容只包含 project metadata 的缺口。
- 涉及目录：`apps/desktop/`、`apps/desktop/src-tauri/src/`、`apps/desktop/src/`、`apps/desktop/src/lib/`、`tasks/`、`.ai/plans/`。
- 修改内容：`desktop_project_export.v1` 继续写入 runtime-local `exports/`，但新增 `contents.compute_jobs`、`contents.canvas_graphs`、`contents.artifact_refs`、`contents.support_bundle_refs`、`contents.redaction` 与 `content_counts`；import 兼容旧 project-only export，并对 rich package 恢复 project metadata 与 CanvasGraphs；Desktop wrapper/UI 展示 package counts；更新 Desktop README 与 completion audit。
- 修改原因：完成审计明确剩余 `full project package content`，而当前实现只导出项目元数据，不能体现项目关联作业、图、artifact 和 support bundle 引用。
- 本次已消除的不确定性：确认现有 `project_id` 已写入 compute jobs 和 CanvasGraphs；确认 `job_snapshot` 已包含 artifact/model_run metadata 但不包含 artifact 文件内容；确认 support bundle 文件内容不能在本轮随项目 JSON 静默恢复。
- 本次采用的关键假设：本轮补齐项目包 metadata/ref 层；没有 artifact/support bundle 文件内容时，不重建 compute job/artifact/support bundle SQLite rows，避免产生指向缺失文件的历史记录。
- 剩余不确定性：外部 file dialog、recent files allowlist、项目包携带并校验 artifact/support bundle 文件后的完整 restore、signing/auto-update 仍待后续。
- 影响范围 / 风险控制方式：保持 `desktop_project_export.v1` schema_version 与旧 project-only export import 兼容；路径仍限制在 runtime-local sandbox；导入只恢复可自洽的 project 和 CanvasGraph。
- 验证方式：`cargo fmt --manifest-path apps\desktop\src-tauri\Cargo.toml` 通过；第一次 `cargo test --manifest-path apps\desktop\src-tauri\Cargo.toml` 因测试局部变量遮蔽 `runtime()` helper 失败，修正后重跑通过（`21 passed`）；`cd apps\desktop; npm run typecheck` 通过；`cd apps\desktop; npm run build` 通过；Browser preview against `http://127.0.0.1:1420/` 确认 `Desktop Runtime`、`Project Export`、`Project Import` 与 `No project export yet.` 在 browser-only mode 渲染，第一次导航超时后重试成功，临时 Vite dev server 已停止；`git diff --check -- apps\desktop tasks\todo.md .ai\plans .ai\changes\2026-05-31.md` 通过（仅 LF/CRLF 提示）。
- 是否更新 README：是，更新 `apps/desktop/README.md`、`apps/desktop/src-tauri/src/README.md`、`apps/desktop/src/README.md`、`apps/desktop/src/lib/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮遵循现有 Desktop sandbox 与 metadata-only support bundle/artifact 边界，不新增长期 ADR。

## 05:11 - AutoWaterSimu Next Desktop file dialogs and recent files

- 用户目标：按 PRD/Spec/Development Plan 继续推进 AutoWaterSimu Next，并补齐 Desktop completion 中 external file dialog allowlist 与 recent files 缺口。
- 涉及目录：`apps/desktop/`、`apps/desktop/src-tauri/`、`apps/desktop/src-tauri/src/`、`apps/desktop/src/`、`apps/desktop/src/lib/`、`tasks/`、`.ai/plans/`。
- 修改内容：新增 `@tauri-apps/plugin-dialog` / `tauri-plugin-dialog` v2.7.1；capability 只开放 `dialog:allow-open` 和 `dialog:allow-save`；新增 `project_export_file`、`project_import_file`、`project_import_recent`、`recent_file_list` commands；Rust 校验外部 project package 路径必须是绝对 `.autowatersimu-project.json`，并在成功导入/导出后记录 `recent_files.file_type = project_package`；React 新增 open/save dialog helpers、`Export Project File` / `Import Project File` / `Import Recent Project` / `Recent Project File` UI；更新 Desktop README 与 completion audit。
- 修改原因：审计剩余项要求 project directory/export directory 来自用户选择或 recent allowlist；之前 Desktop 只能使用 runtime-local sandbox，`recent_files` 表存在但没有命令、记录或 UI。
- 本次已消除的不确定性：确认当前 Tauri dialog plugin 最新 npm/crate 版本为 `2.7.1`；确认 capability 可只开放 open/save；确认 Rust 可以在不开放 broad filesystem plugin 权限的情况下继续拥有实际文件读写。
- 本次采用的关键假设：外部 project package 仅支持用户选择的单文件 `.autowatersimu-project.json`；本轮不实现目录级项目工作区、不开放任意 FS plugin 权限、不做 job/artifact/support bundle file-backed restore。
- 剩余不确定性：项目包携带并校验 artifact/support bundle 文件后的完整 restore、签名、auto-update、GitHub Release 发布策略仍待后续。
- 影响范围 / 风险控制方式：只增加 dialog open/save 权限；React 只选择路径，Rust 执行路径后缀/绝对路径校验和所有文件 IO；Windows canonical path 仅用于校验，不写入 recent file UI，避免 `\\?\` 前缀泄露到用户界面。
- 验证方式：`cargo fmt --manifest-path apps\desktop\src-tauri\Cargo.toml` 通过；首次 Rust test 因 Windows canonical path display prefix 与 recent file 断言不一致失败，修正后 `cargo test --manifest-path apps\desktop\src-tauri\Cargo.toml` 通过（`22 passed`）；`cd apps\desktop; npm run typecheck` 通过；`cd apps\desktop; npm run build` 通过；`cd apps\desktop; npm run tauri -- build` 通过并生成 `apps\desktop\src-tauri\target\release\autowatersimu-desktop.exe`；Browser preview against `http://127.0.0.1:1420/` 确认 `Export Project File`、`Import Project File`、`Import Recent Project`、`Recent Project File` 与 `No recent project files yet.` 在 browser-only mode 渲染，临时 Vite dev server 已停止。
- 是否更新 README：是，更新 `apps/desktop/README.md`、`apps/desktop/src-tauri/README.md`、`apps/desktop/src-tauri/src/README.md`、`apps/desktop/src/README.md`、`apps/desktop/src/lib/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮是 Desktop P0 file dialog/recent file 实现，未改变既有项目包 metadata-only restore 边界。

## 09:31 - AutoWaterSimu Next benchmark case schedule-run API

- 用户目标：按 PRD/Spec/Development Plan 继续推进 AutoWaterSimu Next，并补齐 Phase 6.1 model governance 中已有 benchmark case/history 但缺少安全排队执行入口的缺口。
- 涉及目录：`apps/api/`、`apps/api/internal/compute/`、`apps/api/openapi/`、`frontend/src/client/compute/`、`frontend/src/services/`、`docs/rebuild/`、`tasks/`、`.ai/plans/`。
- 修改内容：新增 `POST /api/v1/model-catalog/{model_key}/versions/{model_version}/benchmark-cases/{benchmark_case_id}/schedule-run`；服务层把 active model version 的 validated benchmark case 解析为标准 `compute_job.v1` 并排队；新增 `BenchmarkCaseRunRequest` DTO、focused Go endpoint 测试、OpenAPI path/schema、generated Compute client method 和 `computeJobsService.scheduleBenchmarkCaseRun()`；更新 API/OpenAPI/frontend service README、Development Plan、completion audit 与任务记录。
- 修改原因：promotion plan 已能读取 benchmark_run/model_run 历史，但尚缺从 catalog benchmark case 启动一次 benchmark 计算的受控入口；直接让 benchmark_run endpoint 执行计算会混淆历史记录与运行编排职责。
- 本次已消除的不确定性：确认 Go API 只负责排队，不执行 Python；确认 built-in benchmark case 的 `simulation_input_id` 可复用现有 simulation input registry lookup；确认 benchmark_run 记录仍需要 worker 完成后显式提交；确认 default parameter set 不存在的 ASM/UDM built-ins 不应在此路径静默排队。
- 本次采用的关键假设：本轮只实现手动 schedule-run API，不实现后台 scheduler、自动 result-to-benchmark_run 记录、参数自动 promotion 或 Web governance mutation UI；允许 approved/validated/candidate/draft 的当前 default parameter set 排队复跑，只禁止 retired。
- 剩余不确定性：自动/周期 benchmark runner 的 cadence、retry、worker 完成后自动生成 benchmark_run 的判定规则、多参数集管理和 approval workflow 语义仍需后续设计。
- 影响范围 / 风险控制方式：endpoint 需要 `job:create`；要求 active model version、validated benchmark case、当前 default parameter set 存在且未 retired；只调用既有 `CreateJob` 入队并保留幂等；不修改 catalog、不写 benchmark_run、不改变 parameter set status、不绕过 worker lifecycle。
- 验证方式：`cd apps\api; go test ./internal/compute -run TestBenchmarkCaseScheduleRunEndpoint -count=1` 通过；`cd apps\api; go test ./...` 通过；`cd frontend; npm run generate-compute-client` 通过；OpenAPI/generated client assertion 通过；`cd frontend; npx tsc --noEmit` 通过；`git diff --check -- apps\api frontend\src\client\compute frontend\src\services docs\rebuild tasks\todo.md .ai\plans .ai\changes\2026-05-31.md` 通过（仅 LF/CRLF 提示）。
- 是否更新 README：是，更新 `apps/api/README.md`、`apps/api/internal/compute/README.md`、`apps/api/openapi/README.md`、`frontend/src/services/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮沿用既有 Go API 编排/worker 执行/benchmark_run 历史分离边界，不新增长期 ADR。

## 09:53 - AutoWaterSimu Next benchmark-backed parameter promotion API

- 用户目标：收敛当前 goal、完成已开始的 Phase 6.1 model governance 小切片，并推送当前分支。
- 涉及目录：`contracts/`、`apps/api/`、`apps/api/internal/compute/`、`apps/api/openapi/`、`frontend/src/client/compute/`、`frontend/src/services/`、`docs/rebuild/`、`tasks/`、`.ai/plans/`。
- 修改内容：新增 `POST /api/v1/model-catalog/{model_key}/versions/{model_version}/default-parameter-set/promote-approved`；服务层复用 promotion plan 作为 gate，只在 benchmark_run/model_run 证据满足时把当前 default parameter set 从 `validated` 晋升到 `approved` 并生成 catalog snapshot；同步 OpenAPI、generated Compute client 与 `computeJobsService.promoteDefaultParameterSetToApproved()`；扩展 focused Go endpoint 测试；更新 README、Development Plan、completion audit 与任务记录。
- 修改原因：已有 promotion plan 能判断 benchmark-backed readiness，但缺少明确、受控、可审计的晋升动作；直接复用通用 status endpoint 会绕过 promotion plan 证据门禁。
- 本次已消除的不确定性：确认 promotion 必须使用 latest catalog 的当前 default parameter set；确认 gate 只依赖已记录且已通过的 benchmark_run/model_run 证据；确认 worker token 不能执行 promotion；确认 promotion 不执行 Python、不自动记录 benchmark_run、不完成生产审批。
- 本次采用的关键假设：本轮只补默认参数集的显式 evidence-backed `validated -> approved` 晋升；不设计多参数集管理、不新增后台 scheduler、不自动把 worker result 转成 benchmark_run、不新增 Web mutation UI。
- 剩余不确定性：多参数集 lifecycle、自动/周期 benchmark runner、result-to-benchmark_run-to-promotion 自动编排、生产 approval workflow 语义和 UI 仍待后续设计。
- 影响范围 / 风险控制方式：endpoint 需要 `model:write`；先读取 promotion plan，只有 `CanPromoteToApproved=true` 才调用现有 status transition；失败返回 409 并保留 blocking reasons；不改变 benchmark run 记录接口、不改变 worker lifecycle、不修改 production approval policy。
- 验证方式：`cd apps\api; go test ./internal/compute -run TestDefaultParameterSetPromotionPlanEndpoint -count=1` 通过；`cd apps\api; go test ./...` 通过；`cd frontend; npm run generate-compute-client` 通过；OpenAPI/generated client assertion 通过；`cd frontend; npx tsc --noEmit` 通过；`git diff --check -- contracts apps\api frontend\src\client\compute frontend\src\services docs\rebuild tasks\todo.md .ai\plans .ai\changes\2026-05-31.md` 通过（仅 LF/CRLF 提示）。
- 是否更新 README：是，更新 `contracts/README.md`、`apps/api/README.md`、`apps/api/internal/compute/README.md`、`apps/api/openapi/README.md`、`frontend/src/services/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮沿用既有 benchmark_run 历史、promotion plan gate 与非生产审批边界。

## 10:10 - AutoWaterSimu Next S3 archive backend

- 用户目标：继续按 PRD/Spec/Development Plan 推进 AutoWaterSimu Next，并补齐 Phase 6.4 lifecycle/operations 中 production object-store archive backend 的本地实现缺口。
- 涉及目录：`apps/api/`、`apps/api/cmd/compute-api/`、`apps/api/internal/compute/`、`docs/operations/`、`docs/rebuild/`、`tasks/`、`.ai/decisions/`、`.ai/plans/`。
- 修改内容：新增 path-style S3-compatible `S3ArtifactStore`；支持 SigV4 signing、PUT/GET/DELETE、可选 object key prefix、provider/object key metadata；`archiveArtifact` 通过 `ArtifactArchiveStore` 记录 `local_fs_archive` 或 `s3_archive`；`cmd/compute-api` 增加 `COMPUTE_API_ARCHIVE_S3_*` env wiring 并拒绝同时配置 local 与 S3 archive；更新 README、runbook、ADR、Technical Spec、Development Plan、completion audit 和任务记录。
- 修改原因：当前 archive_candidate 已能通过 `local_fs_archive` 处理，但 Web/Platform 的生产形态需要对象存储归档后端；继续把对象存储写成未来项会让 lifecycle/operations 与 Technical Spec 的 Web object storage 边界不一致。
- 本次已消除的不确定性：确认现有 retention/report/OpenAPI 字段已能表达新 backend；确认安全边界仍是 copy -> checksum verify -> archive metadata/event -> hot delete；确认未配置 archive backend 时仍跳过；确认 local archive 行为未改变。
- 本次采用的关键假设：本轮实现 path-style S3-compatible backend，不引入 AWS SDK 依赖、不实现 hot artifact S3 store、不实现 bucket provisioning、object-store lifecycle policy 或真实云端 smoke；deployment backup/versioning 仍由运维配置证明。
- 剩余不确定性：真实 S3/MinIO 凭据、bucket policy、versioning/backup、网络连通性、生产 monitoring deployment 仍待部署环境验证。
- 影响范围 / 风险控制方式：保留 `COMPUTE_API_ARCHIVE_DIR` 行为；S3 backend 必须显式设置 endpoint/bucket/access key/secret；local 与 S3 archive 互斥；所有归档仍走已有 model_run reference protection、checksum verification 和 download fallback。
- 验证方式：`cd apps\api; go test ./internal/compute -run "TestS3ArtifactStore|TestArtifactRetentionSweepArchivesCandidate" -count=1` 通过；`cd apps\api; go test ./cmd/compute-api -run TestOpenArchiveStore -count=1` 通过；`cd apps\api; go test ./...` 通过；`rg -n "schema_version|P0|P1|P2|s3_archive|archive_candidate" docs\rebuild --glob "AutoWaterSimu_Next_*.md"` 完成；`git diff --check -- apps\api docs\operations docs\rebuild tasks\todo.md .ai\decisions .ai\plans .ai\changes\2026-05-31.md` 通过（仅 LF/CRLF 提示）。
- 是否更新 README：是，更新 `apps/api/README.md`、`apps/api/internal/compute/README.md`、`apps/api/cmd/compute-api/README.md`、`docs/operations/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：是，更新 `.ai/decisions/0010-artifact-archive-backend-boundary.md`，把 `s3_archive` 纳入既有 archive safety model。
## 10:45 - Production readiness UI

- 用户目标：继续按 PRD/Spec/Development Plan 推进 AutoWaterSimu Next，并把只读 production readiness 能力纳入 Web 任务详情体验。
- 涉及目录：`frontend/src/routes/`、`frontend/src/services/`、`frontend/tests/`、`tasks/`、`.ai/plans/`。
- 修改内容：在 `/compute-jobs` 的 Job detail 中调用 `computeJobsService.getProductionReadiness()` 并展示 readiness status、external approval、auto publish boundary、evidence package、blocking reasons、warnings、risk severity counts 和 policy checks；扩展 `compute-jobs-current-flow.spec.ts` mock `/production-readiness` 并断言 readiness 状态可见；更新 frontend route/test README 和 completion audit。
- 修改原因：上一切片已提供后端 readiness API，但 Web 任务详情仍只能下载 evidence 或解析 evidence refs，缺少给 NewSystem/milp/审批用户可视化查看 readiness 的本地闭环。
- 本次已消除的不确定性：确认 readiness UI 可复用现有 generated client service wrapper；确认 route 层只展示后端报告，不需要自行拼装 evidence 或实现审批规则；确认现有 current-flow smoke 可以覆盖新 endpoint 请求与可见状态。
- 本次采用的关键假设：Web 当前只需要只读展示 readiness，不提供生产审批、发布或 catalog/job mutation；真实外部审批仍由 NewSystem/milp/部署侧系统负责。
- 剩余不确定性：真实部署下的 authenticated browser smoke、NewSystem/milp acceptance smoke、审批 UI/角色策略仍需外部环境或业务规则确认。
- 影响范围 / 风险控制方式：仅改现有 Compute Jobs route 和 mock-backed Playwright test；不改 OpenAPI、generated client、后端 API 或生产审批语义。
- 验证方式：`cd frontend; npx tsc --noEmit` 通过；`cd frontend; npx playwright test tests/compute-jobs-current-flow.spec.ts --project=chromium --no-deps --reporter=line` 通过，`1 passed`；In-app Browser 打开 `http://localhost:5173/compute-jobs` 时因无 live legacy auth backend/session 显示登录页，authenticated readiness panel 以 mock-backed Playwright smoke 验证。
- 是否更新 README：是，更新 `frontend/src/routes/README.md` 与 `frontend/tests/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；该切片沿用只读 evidence/readiness 与外部审批边界。

## 10:32 - Production readiness report

- 用户目标：继续按 PRD/Spec/Development Plan 推进 AutoWaterSimu Next，并收敛 NewSystem/milp 审批集成剩余缺口。
- 涉及目录：`contracts/`、`apps/api/`、`frontend/src/client/compute/`、`frontend/src/services/`、`docs/rebuild/`、`tasks/`、`.ai/plans/`。
- 修改内容：新增 `production_readiness.v1` 合同和 valid/invalid fixture；新增 Go Compute API `GET /api/v1/compute/jobs/{job_id}/production-readiness`；根据 job status、evidence package、governance.production_allowed 与 stored risk_findings 生成只读 readiness report；同步 OpenAPI、generated Compute client 和 `computeJobsService.getProductionReadiness()`；更新 README、Development Plan 和 completion audit。
- 修改原因：Phase 6.2 已有 evidence package、risk findings 和 `production_allowed`，但缺少一个明确的只读政策报告来表达“可提交外部审批”与“AutoWaterSimu 不自动发布生产指令”的边界。
- 本次已消除的不确定性：确认 production readiness 可由现有 evidence/read 数据闭环生成；确认 high/critical risk findings 应阻断 readiness，medium risk findings 仅提示外部 reviewer；确认该 API 不需要新增数据库表或审批写操作。
- 本次采用的关键假设：外部系统拥有最终生产审批权；AutoWaterSimu 只能提供证据和 readiness 判断；当前 readiness policy v1 只覆盖 job succeeded、evidence available、governance allowed、risk severity 四类检查。
- 剩余不确定性：真实 NewSystem/milp acceptance smoke、部署侧审批流接入、审批 UI 和组织级审批策略仍需外部集成目标或业务规则确认。
- 影响范围 / 风险控制方式：新增只读 endpoint 和合同，不改变 job 创建、worker 执行、catalog mutation、result persistence 或 production publish 行为；`external_approval_required=true` 和 `auto_publish_allowed=false` 写入合同约束。
- 验证方式：`backend\.venv\Scripts\python -m pytest contracts\tests -q` 通过，`91 passed`；`cd apps\api; go test ./internal/compute -run "Test(ValidatedCompletePersistsModelRun|ProductionReadinessBlocksHighRiskFindings)" -count=1` 通过；`cd apps\api; go test ./...` 通过；`cd frontend; npm run generate-compute-client` 通过；`cd frontend; npx tsc --noEmit` 通过；`git diff --check -- contracts apps\api frontend\src\client\compute frontend\src\services docs\rebuild tasks\todo.md .ai\plans .ai\changes\2026-05-31.md` 通过（仅 LF/CRLF warning）。
- 是否更新 README：是，更新 `contracts/README.md`、`apps/api/README.md`、`apps/api/internal/compute/README.md`、`apps/api/openapi/README.md`、`frontend/src/services/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；该切片沿用既有“不发布生产指令 / 外部审批拥有最终决定权”边界。

## 17:08 - AutoWaterSimu Next certainty and elegance documents

- 用户目标：独立判断粘贴文本中有利于提高项目确定性和优雅性的内容，并整理成 PRD 和计划文档。
- 涉及目录：`docs/rebuild/`、`tasks/`、`.ai/changes/`。
- 修改内容：新增 `AutoWaterSimu_Next_Certainty_Elegance_PRD_v1.0.md` 和 `AutoWaterSimu_Next_Certainty_Elegance_Development_Plan_v1.0.md`；更新 `docs/rebuild/README.md` 登记补充文档职责；在 `tasks/todo.md` 记录本次计划。
- 修改原因：粘贴文本是一组质量提升路线输入，需要从中筛选能提高确定性和优雅性的内容，沉淀为不扰动现有 Next PRD/Spec/Plan 权威范围的补充文档。
- 本次已消除的不确定性：确认目标是文档整理而非立即实施代码重构；确认新增补充 PRD/Plan 比直接改写现有 Next v1.0 三件套更安全；确认现有未跟踪 `AutoWaterSimu_95分优雅度完整计划.md` 只作为上下文，不覆盖、不删除。
- 本次采用的关键假设：将“95 分”定义为工程质量目标，不作为生产 SLA 或一次性功能承诺；只纳入粘贴文本内与 monorepo 入口、依赖边界、contracts、Go API 模块化、Store 拆分、frontend feature architecture、Desktop package schema、Water Ontology、安全边界、CI/release evidence、工程记忆和场景级验收相关的内容；不纳入粘贴文本之外的 Hteinfo/IMS 扩展。
- 剩余不确定性：后续真正实施 `Justfile`、dependency check、Go API 拆包、contract codegen、ontology、安全模型和 release evidence 时，还需按实际代码分别补充 ADR、README 和测试。
- 影响范围 / 风险控制方式：仅新增和更新 Markdown 文档；不修改运行时代码、公共 API、OpenAPI、JSON Schema、数据库 schema、generated client 或 CI workflow；不覆盖现有未跟踪 95 分计划文件。
- 验证方式：`git diff --check -- docs/rebuild tasks/todo.md .ai/changes/2026-05-31.md` 通过（仅 LF/CRLF warning）；`rg -n "P0|P1|P2|95|ontology|release evidence|single source of truth" docs\rebuild` 返回新补充文档和既有 rebuild 上下文中的预期匹配。
- 是否更新 README：是，更新 `docs/rebuild/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次只形成补充 PRD/计划，具体架构变更实施时再补 ADR。

## 18:47 - AutoWaterSimu Next Store interface first split

- 用户目标：根据 Certainty/Elegance PRD 和开发计划继续完善优化，推进 Go Compute API Store/domain 边界从审计进入低风险结构改进。
- 涉及目录：`apps/api/`、`apps/api/internal/compute/`、`scripts/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`、`.ai/changes/`。
- 修改内容：将 `apps/api/internal/compute/store.go` 中的聚合 metadata `Store` 拆成 12 个领域子接口并由 `Store` 嵌入组合；保留 `ArtifactStore` 字节/对象存储抽象，metadata 持久化接口命名为 `ArtifactMetadataStore`；增强 `scripts/audit-compute-api-boundary.ps1`，使其能解析嵌入接口、展开 `Store` 方法、记录 interface summary，并在预期领域接口缺失时失败；更新 Compute API README、architecture compute/current-state 文档、Certainty/Elegance 开发计划 checklist 和任务记录。
- 修改原因：Certainty/Elegance 计划明确当前 Go API 的主要优雅性缺口之一是巨型 Store interface；先拆聚合接口能降低领域边界认知成本，同时不改变运行行为或移动实现文件。
- 本次已消除的不确定性：确认 `Store` 仍解析为 41 个方法；确认 12 个领域接口均被聚合 `Store` 嵌入；确认 `MemoryStore` 和 `PostgresStore` 仍覆盖全部方法；确认现有 object `ArtifactStore` 命名冲突需要保留并用 `ArtifactMetadataStore` 表达 metadata 接口。
- 本次采用的关键假设：本轮只做第一步接口结构拆分；`Service` 继续依赖聚合 `Store`，后续再按 artifacts/archive metadata 等路径收窄构造函数；不改 HTTP 行为、OpenAPI、contracts、migrations、generated client、auth scopes 或 PostgreSQL/MemoryStore 实现。
- 剩余不确定性：service constructor narrowing 的具体切分边界、Go domain package 迁移顺序、以及是否需要为更窄 repository 接口补充 dedicated unit tests 仍待后续切片验证。
- 影响范围 / 风险控制方式：最小 Go 类型结构变更；不移动文件、不删方法、不改 public HTTP/API surface；通过审计脚本和 Go tests 双重确认 Store method coverage。
- 验证方式：`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1` 通过（41 resolved Store methods、12 embedded interfaces、12 domain groups）；`cd apps\api; go test ./...` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` 通过（evidence status `passed`，6 steps passed）；`git diff --check -- apps\api docs scripts tasks\todo.md .ai\changes\2026-05-31.md` 通过（仅 LF/CRLF warning）。
- 是否更新 README：是，更新 `apps/api/README.md`、`apps/api/internal/compute/README.md`、`scripts/README.md`，并同步 `docs/architecture/compute-api.md`、`docs/architecture/current-state.md` 与 Certainty/Elegance 开发计划。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本轮是既有计划内的第一步接口结构收敛，不新增长期架构选择。

## 18:56 - AutoWaterSimu Next artifact lifecycle service boundary

- 用户目标：继续根据 Certainty/Elegance PRD 与 Development Plan 推进 Go Compute API service constructor narrowing，优先处理审计推荐的 artifacts + archive metadata 路径。
- 涉及目录：`apps/api/`、`apps/api/internal/compute/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`、`.ai/changes/`。
- 修改内容：新增 `apps/api/internal/compute/artifact_lifecycle.go`；引入 `ArtifactLifecycleService`，构造函数只接收 `ArtifactMetadataStore`、`ArchiveMetadataStore`、hot artifact object store、optional archive object store 和 clock；`Service.DownloadArtifact` 与 `Service.SweepArtifactRetention` 保持公开入口不变并委派到窄服务；更新 Compute API README、architecture compute/current-state 文档、Certainty/Elegance checklist 和任务记录。
- 修改原因：上一轮已把聚合 `Store` 拆成领域接口，但主 `Service` 仍直接在 artifact retention/download 路径上使用聚合 store。将 artifact lifecycle 逻辑抽到窄构造函数服务可以继续降低领域认知成本，并为后续 package/file split 打基础。
- 本次已消除的不确定性：确认 artifact lifecycle 只需要 artifact metadata、archive metadata、hot object store、archive object store 和 clock；确认现有 `ArtifactStore` 字节/对象存储命名继续保留；确认公开 HTTP/API/OpenAPI 行为无需改变。
- 本次采用的关键假设：本轮只收窄 artifact lifecycle 服务边界；artifact upload、job snapshot artifact listing 和其他 service 路径暂不迁移；不改 contracts、OpenAPI、generated client、migrations、auth scopes 或 object-store implementation。
- 剩余不确定性：下一批 constructor narrowing 应先做 simulation input + process graph，还是 draft confirmation + result explanation；artifact upload 是否最终并入 artifact lifecycle 边界，仍需结合 package split 计划验证。
- 影响范围 / 风险控制方式：移动既有逻辑到新内部服务并保持 `Service` 公开方法兼容；focused retention tests、Go tests 和 audit 先行验证；无数据库或 wire shape 改动。
- 验证方式：`cd apps\api; go test ./internal/compute -run "TestArtifactRetention|TestHTTPArtifactRetention|TestS3ArtifactStore" -count=1` 通过；`cd apps\api; go test ./...` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1` 通过（41 resolved Store methods、12 embedded interfaces、12 domain groups）；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` 通过（evidence status `passed`，6 steps passed）；`git diff --check -- apps\api docs scripts tasks\todo.md .ai\changes\2026-05-31.md` 通过（仅 LF/CRLF warning）。
- 是否更新 README：是，更新 `apps/api/README.md`、`apps/api/internal/compute/README.md`、`docs/architecture/compute-api.md`、`docs/architecture/current-state.md` 与 Certainty/Elegance 开发计划。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本轮延续既有 modular monolith / narrow interface 分阶段路线，不新增 ADR。

## 22:40 - AutoWaterSimu Next Water Ontology registry

- 用户目标：继续根据 Certainty/Elegance PRD 与 Development Plan 完善 AutoWaterSimu Next，把 Water Ontology 首批对象从计划项落成可校验的语义 registry。
- 涉及目录：`ontology/`、`scripts/`、`scripts/ci/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`、`.ai/changes/`。
- 修改内容：新增 `ontology/objects`、`ontology/actions`、`ontology/links`、`ontology/policies` 四类 registry 与目录 README；新增 `docs/architecture/ontology-model.md`；新增 `scripts/check-ontology.ps1`；新增 `just check-ontology` 并把 ontology gate 接入 `just check` 和 `scripts/ci/pr-fast.ps1`；更新 root README、scripts README、scripts/ci README、architecture current-state/local-dev、Certainty/Elegance Development Plan checklist 和任务记录。
- 修改原因：Certainty/Elegance PRD 要求 Water Ontology 首批对象能说明属性、关系、动作、权限和 evidence；原状态只有计划目标，没有落地 schema/registry 或一致性验证。
- 本次已消除的不确定性：确认仓库此前没有 `ontology/` 目录；确认首批对象应覆盖 operational water objects 与平台 evidence objects；确认本轮可通过 registry 引用校验推进确定性，但不能宣称运行时权限已强制执行。
- 本次采用的关键假设：本轮把 ontology 定义为 semantic registry 层，不替代 `contracts/` wire shape，不接入运行时 RBAC/ABAC/data-scope/approval/audit enforcement，不新增 generated ontology types。
- 剩余不确定性：runtime authorization/data-scope/audit 如何消费 registry、是否需要正式 JSON Schema 或 codegen、Ontology UI/object browser、policy smoke 和 production identity provider 集成仍待后续实现。
- 影响范围 / 风险控制方式：只新增 registry、校验脚本和文档；不改 API、数据库、OpenAPI、contracts、generated client 或运行时权限语义；PR fast 中新增低成本 dependency-free registry check。
- 验证方式：`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-ontology.ps1` 通过（18 objects、14 actions、18 links、5 policies）；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` 通过（evidence status `passed`，7 steps passed，0 failed，包含 ontology registry check）；`git diff --check -- ontology scripts Justfile README.md docs\architecture docs\rebuild\AutoWaterSimu_Next_Certainty_Elegance_Development_Plan_v1.0.md tasks\todo.md` 通过（仅 LF/CRLF warning）。
- 是否更新 README：是，新增并更新 `ontology/**/README.md`、`scripts/README.md`、`scripts/ci/README.md`、`docs/architecture/README.md`，并同步 root README 与 architecture docs。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本轮是计划内首版 registry，不产生长期不可逆架构决策，运行时 enforcement 接入时再视情况新增 ADR。

## 23:04 - AutoWaterSimu Next browser smoke lane

- 用户目标：继续根据 Certainty/Elegance PRD 与 Development Plan 完善 AutoWaterSimu Next，并补齐 Testing / CI 中 `browser` lane 的本地 evidence 入口。
- 涉及目录：`frontend/tests/`、`scripts/ci/`、`.github/workflows/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`、`.ai/changes/`。
- 修改内容：新增 `frontend/tests/contract-validation.spec.ts`；新增 `scripts/ci/browser-smoke.ps1`，聚合 Compute Jobs current-flow、contract validation、Model governance、Compute lifecycle 四个 mock-backed Playwright smoke 并写出 `tmp/ci-evidence/browser-smoke.json`；新增 `just browser-smoke`；新增 `.github/workflows/next-browser-smoke.yml` 手动/可复用 workflow；更新 root README、scripts README、scripts/ci README、GitHub READMEs、frontend tests README、architecture current-state/local-dev 与 Certainty/Elegance Development Plan checklist。
- 修改原因：计划中 `browser` lane 尚未产品化；已有几个 focused Playwright smoke 但缺少仓库级 evidence 编排、contract validation 覆盖和 hosted workflow 入口。
- 本次已消除的不确定性：确认现有 browser smoke 可复用 Playwright request mocking，不需要 live backend；确认 Compute Jobs 页面可以覆盖 current-flow submission、job result/readiness read 和 contract validation；确认 browser lane 当前应标记为 mock-backed，不应被解释成 live integration browser evidence。
- 本次采用的关键假设：本轮 browser smoke 以稳定、快速、mock-backed 的 Web 编排验证为目标；不启动 Docker integration stack，不要求 legacy auth backend session，不把 hosted workflow 文件等同于 hosted green run。
- 剩余不确定性：如何把 browser smoke 接到真实 Postgres/MinIO/worker integration stack、是否需要 authenticated legacy backend session、何时把 hosted browser-smoke green run URL 记录为 evidence 仍待后续执行。
- 影响范围 / 风险控制方式：新增测试和 CI 编排，不改生产 UI 代码、后端 API、OpenAPI、contracts、数据库 schema 或 generated client；browser evidence JSON 明确记录 live backend 与 hosted workflow run `not_covered`。
- 验证方式：`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\browser-smoke.ps1` 通过（evidence status `passed`，4 steps passed，0 failed）；`cd frontend; npx tsc --noEmit` 通过；Python/PyYAML parser check for `.github/workflows/next-browser-smoke.yml` 通过（6 steps，`windows-latest`，包含 Node setup、Chromium install、browser-smoke 和 artifact upload）；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` 通过（evidence status `passed`，7 steps passed，workflow list includes `next-browser-smoke.yml`）；`git diff --check -- .github scripts Justfile README.md docs\architecture docs\rebuild\AutoWaterSimu_Next_Certainty_Elegance_Development_Plan_v1.0.md frontend\tests tasks\todo.md` 通过（仅 LF/CRLF warning）。第一次 workflow parser check 使用了错误的 YAML key 假设，调整为兼容 `'on'` / `True` 后通过。
- 是否更新 README：是，更新 root README、`scripts/README.md`、`scripts/ci/README.md`、`.github/README.md`、`.github/workflows/README.md`、`frontend/tests/README.md`、`docs/architecture/current-state.md`、`docs/architecture/local-dev.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本轮只是将计划内 browser smoke lane 产品化，不新增长期架构取舍。
