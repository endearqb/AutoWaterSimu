## 23:24 - Phase 3C Desktop dev MVP and Tauri wiring

- 用户目标：按 Phase 3C 计划把 Desktop 从可测试 Rust runtime 升级为可启动 Tauri dev MVP。
- 涉及目录：`apps/desktop/`、`apps/desktop/src-tauri/`、`tasks/`、`.ai/changes/`、`.gitignore`。
- 修改内容：新增独立 Vite + React + TypeScript Desktop app；接入 Tauri v2 build/runtime；注册 Rust commands；新增 demo material balance workbench；补 fake worker invalid stdout 测试；新增 Tauri icon；更新 README First 上下文。
- 修改原因：Phase 3B 已有本地 runtime，但缺少可启动 Desktop shell 和 Tauri IPC wiring，无法通过窗口验证最小 material balance job。
- 本次已消除的不确定性：确认 Desktop dev app 使用 `127.0.0.1:1420` 避免 legacy frontend 端口冲突；确认 Tauri build 能找到 frontend dist、resource icon 和 registered commands；确认 browser-only Vite fallback 不会执行本地命令。
- 本次采用的关键假设：3C 仍使用 source-mode Python worker；不做 PyInstaller、`externalBin`、NSIS、signing、auto update、long-lived worker、cancel/progress streaming 或 Web Go API。
- 剩余不确定性：尚未进行人工窗口点击 smoke；packaged sidecar、installer、project import/export、backup/restore、完整 graph editor wiring 仍留到后续 Phase。
- 影响范围 / 风险控制方式：不修改 contracts schema、worker JSON-RPC、legacy FastAPI route、legacy frontend client 或 OpenAPI client；React 只通过 Tauri command 调用 Rust。
- 验证方式：
  - `cd apps\desktop; npm run typecheck` 通过。
  - `cd apps\desktop; npm run build` 通过。
  - `cargo test --manifest-path apps\desktop\src-tauri\Cargo.toml` 通过，`12 passed`。
  - `cd apps\desktop; npm run tauri -- build` 通过，生成 `src-tauri\target\release\autowatersimu-desktop.exe`。
  - `backend\.venv\Scripts\python -m pytest contracts\tests -q` 通过，`44 passed`。
  - `backend\.venv\Scripts\python -m pytest simulation_core\tests services\simulation-worker\tests -q` 通过，`10 passed`，仅既有 backend warning。
  - `cd backend; .venv\Scripts\python -m pytest app\tests\services\test_simulation_input_adapter.py app\tests\time_segment_validation_test.py app\tests\material_balance_segment_overrides_test.py app\tests\hybrid_udm_validation_test.py app\tests\udm_engine_variable_binding_test.py -q` 通过，`22 passed`，仅既有 warning。
  - `cd frontend; npx tsc --noEmit` 通过。
  - `git diff --check` 通过，仅 LF/CRLF normalization warnings。
- 是否更新 README：是，更新 `apps/desktop/README.md`、`apps/desktop/src-tauri/README.md`，新增 `apps/desktop/src/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本轮执行既有 Phase 3 Desktop MVP 方向，没有新增长期架构取舍。
