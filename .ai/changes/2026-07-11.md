## 16:53 - README First v2.1 架构迁移 [L2]

- 用户目标：根据 `endearqb/ReadmeFirst` 更新项目中的 README First 架构。
- 涉及目录：根协议文档、`.ai/`、`tasks/`。
- 修改内容与原因：将通用协议升级为 v2.1 L0/L1/L2 分级执行；新增根 `VERSION`、`.ai/architecture/`、glossary、handoff 与计划/归档占位结构；保留 AutoWaterSimu 专属规则和既有 PowerShell README 门禁。
- 已消除的不确定性：确认上游版本为 2.1.0；确认当前仓库是 v1 风格且已有 changes、decisions、plans、reviews 与文档门禁；确认业务架构的权威入口仍是 `docs/architecture/`。
- 关键假设：用户需要将本仓库协议迁移到上游当前 v2.1，而非运行一次可能移动历史记录的 maintainer 压缩流程。
- 剩余不确定性：根 `README.md` 和 `README_zh.md` 仍链接工作区既有删除的 `release-notes.md`；此文件删除不属于本任务，未恢复或修改。
- 影响范围 / 风险控制：仅修改协作文档与记录结构；不改业务代码，不重写既有 changes，不处理无关的工作区变更。
- 验证证据：`ConvertFrom-Json .ai/readme-contracts.json` → PASS；`scripts\readme-contract-check.ps1 -FailOnWarnings` → 新增 `.ai` 链路通过，仅报告上述两个既有 release-notes 断链；`git diff --check -- AGENTS.md README.md README_zh.md README_First.md .ai tasks\todo.md` → 通过（仅 CRLF warning）。
- README 更新：更新根 README、中文 README、README_First、`.ai/README`；新增 `.ai/architecture/README.md`。
- 是否新增/更新 `.ai/decisions/`：新增 `0029-readme-first-v2-1-tiered-protocol.md`。
## 17:30 - UDM Network v2 独立前端实时流量平衡 [L2]

- 用户目标：将 udm-network-v2 overlay 的 10 层二沉池与实时流量平衡落到当前独立 `/udm-v2`，并复用原 UDM ReactFlow 画布。
- 涉及目录：`frontend/src/components/Flow`、`frontend/src/features/udm-v2`、`scripts`、`.ai/decisions`、`tasks`。
- 修改内容：为 `FlowCanvas` 增加通用注入点；v2 接入独立 store adapter、四类边、10 层展开模板、RREF 实时求解与诊断展示。
- 修改原因：避免维护第二套画布交互，同时不把 v2 数据和业务逻辑耦合回 legacy UDM。
- 本次已消除的不确定性：确认当前 v2 已具备独立合同/serializer/服务；overlay 的前端缺口是展开画布与实时流量求解。
- 本次采用的关键假设：“使用原 UDM 画布”指复用 `FlowCanvas` 交互壳，而非复用 legacy UDM store/inspector/service。
- 剩余不确定性：controlled/timeseries 的可编辑实时输入面板尚未迁入；求解器已接受 externalValues API。
- 影响范围 / 风险控制方式：legacy 默认 props 不变；v2 派生流量不进入持久化 source of truth；新增 focused tests。
- 验证证据：`npm run typecheck` → 通过；`npm run test:unit -- --run` → 13 files / 38 tests 全通过（jsdom 仍输出既有 Chakra CSS 解析噪声）；`npm run check:udm-v2-boundary` → 通过；`git diff --check` → 通过；README contract check 被根 README 已存在的 `release-notes.md` 断链阻塞（2 项，与本次改动无关）。
- 是否更新 README：是，更新 v2 与 Flow 目录共享边界。
- 是否需要新增或更新架构决策（.ai/decisions/）：是，更新 ADR 0028 的画布共享裁决。
