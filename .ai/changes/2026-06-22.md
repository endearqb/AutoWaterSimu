## 00:25 - Standalone runtime route-tree audit closeout

- 用户目标：继续收尾 AutoWaterSimu Next standalone RC，把 standalone frontend runtime legacy login/users/items/FastAPI client 清零，并复核 release 发布物不包含 FastAPI。
- 涉及目录：`frontend/src`、`frontend/src/routes`、`frontend/src/stores`、`scripts`、`tasks`。
- 修改内容：新增 `frontend/src/standaloneRouteTree.tsx`；`main.tsx` 按 standalone/legacy runtime 动态加载 route tree；legacy OpenAPI client 配置移到 `legacyApiClient.ts` 并只在 legacy runtime 动态加载；`flowStore.ts` 的 legacy `FlowchartsService` 改为非-standalone 分支动态导入；`audit-frontend-standalone-compute-boundary.ps1` 增加 standalone static import graph 检查；更新相关 README。
- 修改原因：原 `main.tsx` 静态导入完整 `routeTree.gen.ts`，会把 login/signup/reset/admin/items/settings route 纳入 standalone runtime 可达集合；`flowStore.ts` 也静态导入 legacy FastAPI flowchart SDK。
- 本次已消除的不确定性：确认 standalone frontend runtime 不再静态可达完整 generated route tree、legacy auth/user/item/admin/settings route、`useAuth` 或 legacy FastAPI client；确认 standalone compose 服务清单不包含 legacy backend。
- 本次采用的关键假设：legacy runtime 仍允许动态加载 FastAPI client；standalone runtime 的判定继续以 `isStandaloneRuntime()` 为准。
- 剩余不确定性：full standalone release gate 和 golden scenarios 需要在本次新提交后重新刷新，旧 evidence 不能证明新 HEAD。
- 影响范围 / 风险控制方式：只新增 standalone 专用 route tree，不删除 legacy routes；legacy route tree 和 generated `routeTree.gen.ts` 保持原样。
- 验证方式：`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-frontend-standalone-compute-boundary.ps1` 通过；`cd frontend; npx tsc --noEmit` 通过；`docker compose -f docker-compose.standalone.yml config --services` 输出 `compute-postgres`、`compute-api`、`frontend`、`simulation-worker`。
- 是否更新 README：是，更新 `frontend/src/README.md`、`frontend/src/routes/README.md`、`scripts/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否。
