## 22:42 - PR-11 unified reaction RHS dispatch

- 用户目标：继续完成 v1.4 PR-11 统一 RHS 抽取，使 `_run_hours` 不再按单模型 ASM/UDM 分派到重复 RHS 分支，同时保留 default clamp 现状。
- 涉及目录：`simulation_core/python/autowatersimu_simulation_core/material_balance/`、`simulation_core/tests/`、`scripts/`、`docs/rebuild/simulation_core/`、`docs/architecture/`、`tasks/`、`.ai/changes/`。
- 修改内容：`_run_hours` 现在在 `active_model_count > 0` 时统一使用 `_combined_reaction_ode_balance`，单模型 ASM1Slim/ASM1/ASM3/UDM 不再分派到各自独立 RHS；default no-reaction branch 仍使用 `_ode_balance` 且 `clamp_output=False`；correctness-freeze tests 和 audit 改为冻结 unified reaction dispatch、default no-reaction branch 与 clamp policy；同步 material_balance/tests/scripts README、current-state、前置规划、v1.4 PR-11 状态、docs/rebuild README 和任务记录。
- 修改原因：v1.4 PR-11 要求 RHS 抽取不能只是机械去重，必须以 PR-38 mixed support 为前置，把 ASM/UDM 反应项叠加调度统一到同一 RHS，同时显式保留 ASM 氧列、UDM fixed mask 和 default no-clamp 现状。
- 本次已消除的不确定性：确认 mixed 和 single reaction dispatch 都能通过同一个 combined RHS；确认 default no-reaction branch 仍独立且保持 no-clamp；确认 Phase 0 golden 未因统一 dispatch 改变。
- 本次采用的关键假设：本切片完成 `_run_hours` reaction dispatch 的统一 RHS；旧 per-model RHS helper 函数暂留作内部兼容/后续死代码清理，不在本切片删除；不改变 solver 默认值、output projection policy、route schema、worker strict 默认或 Go API。
- 剩余不确定性：legacy `app.models` route schema 迁移、旧 per-model RHS helper dead-code cleanup、PR-12 输出投影决策、worker strict 默认和 Go API 性能项仍待独立处理。
- 影响范围 / 风险控制方式：只改 `_run_hours` dispatch；保持 `_combined_reaction_ode_balance` 既有 ASM oxygen / UDM fixed mask 逻辑；用 focused branch-spy tests、correctness-freeze audit、full simulation_core tests 和 Phase 0 golden 控制数值/行为回归。
- 验证方式：已通过 `backend\.venv\Scripts\python -m pytest simulation_core\tests\test_material_balance_core_boundary.py -q`（75 passed）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-simulation-core-correctness-freeze.ps1`（passed，0 hard violations，0 open gaps）、`backend\.venv\Scripts\python -m pytest simulation_core\tests -q`（82 passed）、`backend\.venv\Scripts\python -m pytest docs\rebuild\simulation_core -q`（6 passed，1 skipped）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\performance-golden-phase0.ps1`（passed，12 full runs，7 micro goldens，0 hard violations，0 open gaps）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1`（`pr-fast.json` status `passed`）和 `git diff --check -- .`（仅 LF-to-CRLF notices，无 whitespace error）。
- 是否更新 README：是，更新 `simulation_core/python/autowatersimu_simulation_core/material_balance/README.md`、`simulation_core/tests/README.md`、`scripts/README.md` 和 `docs/rebuild/simulation_core/README.md`；同时校准前置计划、v1.4 开发计划和 current-state。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；PR-38/ADR 0015 已确定 supported mixed dispatch，本切片是在既定方向内完成 unified reaction dispatch。

## 22:22 - Core ASM schema-driven component reorder

- 用户目标：继续收口 v1.4 PR-39/KPI-019 反应组分契约，把 ASM1Slim/ASM1/ASM3 从“metadata 必须是模型前缀顺序”推进到按组件名 gather/scatter。
- 涉及目录：`simulation_core/python/autowatersimu_simulation_core/material_balance/`、`simulation_core/tests/`、`scripts/`、`docs/rebuild/simulation_core/`、`docs/architecture/`、`tasks/`、`.ai/changes/`。
- 修改内容：`_convert_to_tensors()` 现在按 `customParameters` 名称为每个实际存在的 ASM model 预计算 component index tensor；ASM reaction runtime 携带 component indices 和全局 oxygen index；`_apply_asm_reaction_runtime()` 先把全局 `y` gather 到模型局部列序，再把 reaction rates scatter 回全局列；缺失或重复必需 ASM 组件前置抛出 `InvalidInputError`；metadata-less legacy 输入继续按前缀列执行并保留组件数量 guard；新增/更新 core boundary tests 覆盖 fixture 兼容、named order mapping、missing/duplicate 组件报错、extra 全局组件不被 ASM reaction 污染和 metadata-less 数量 guard；correctness-freeze audit 纳入新测试；同步 material_balance/tests/scripts README、current-state、前置规划、v1.4 开发计划、docs/rebuild README 和任务记录。
- 修改原因：PR-39 要求把硬编码反应输入列序提升为 `component_schema→模型组分` 映射。仅要求 metadata 前缀顺序会拒绝合法的具名 schema，也无法保护额外全局组件不被反应项误写。
- 本次已消除的不确定性：确认现有 ASM fixtures 仍可通过；确认具名 schema 顺序变化可映射而非报错；确认缺失/重复必需组件前置失败；确认 ASM reaction 只写回映射到的模型组件列，额外全局组件保持不变。
- 本次采用的关键假设：`customParameters` 是当前 legacy/core 路径里可用的组件名称来源；metadata 缺失时不能安全推断名称，因此继续保守使用前缀列并只做数量 guard；本切片不改 legacy route schema、contract JSON、RHS 分支结构、solver 默认值、output clamp、worker strict 默认或 Go API。
- 剩余不确定性：legacy `app.models` route schema metadata 迁移、PR-11 统一 RHS、worker strict 默认、Go API 性能项和 route schema/API cleanup 仍待独立处理。
- 影响范围 / 风险控制方式：只改 ASM reaction runtime gather/scatter；标准前缀 fixtures 必须保持 golden；新增 focused tests 和 correctness-freeze audit 防止回归。
- 验证方式：已通过 `backend\.venv\Scripts\python -m pytest simulation_core\tests\test_material_balance_core_boundary.py -q`（75 passed）、`backend\.venv\Scripts\python -m pytest simulation_core\tests -q`（82 passed）、`backend\.venv\Scripts\python -m pytest docs\rebuild\simulation_core -q`（6 passed，1 skipped）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-simulation-core-correctness-freeze.ps1`（passed，0 hard violations，0 open gaps）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\performance-golden-phase0.ps1`（passed，12 full runs，7 micro goldens，0 hard violations，0 open gaps）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1`（`pr-fast.json` status `passed`）和 `git diff --check -- .`（仅 LF-to-CRLF notices，无 whitespace error）。
- 是否更新 README：是，更新 `simulation_core/python/autowatersimu_simulation_core/material_balance/README.md`、`simulation_core/tests/README.md`、`scripts/README.md` 和 `docs/rebuild/simulation_core/README.md`；同时校准前置计划、v1.4 开发计划和 current-state。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次是在 PR-39 既定方向内实现 core runtime component mapping，不新增长期架构决策。

## 22:08 - Backend material_balance package-root model export cleanup

- 用户目标：继续收口 v1.4 PR-30 / PR-31 backend thin-shell cleanup，删除 `app.material_balance` 包根隐藏的 runtime model 兼容导出，同时保留显式 `app.material_balance.models` 兼容路径。
- 涉及目录：`backend/app/material_balance/`、`backend/app/tests/`、`scripts/`、`docs/rebuild/simulation_core/`、`docs/architecture/`、`tasks/`、`.ai/changes/`。
- 修改内容：从 `backend/app/material_balance/__init__.py` 移除 `MaterialBalanceInput` / `NodeData` / `EdgeData` / `CalculationParameters` / `MaterialBalanceResult` 顶层 re-export，只保留 calculator/errors；`material_balance_compat_models_boundary_test.py` 与 boundary audit 改为只允许 `backend/app/material_balance/models.py` 作为 runtime model 兼容路径，并断言包根不导出 runtime models；同步 backend/material_balance/tests/scripts README、current-state、前置规划、v1.4 开发计划、docs/rebuild README 和任务记录。
- 修改原因：v1.4 checklist 已完成 `app.material_balance.models` re-export，但包根仍把 runtime input/result models 重新暴露为隐式兼容路径，容易让新代码绕过“显式 models 子模块或 service 入核校验”的边界。
- 本次已消除的不确定性：确认仓库生产代码不使用 `from app.material_balance import MaterialBalanceInput` 等包根 model 导入；确认 legacy FastAPI route schema 仍来自 `app.models`，本切片无需 OpenAPI/frontend client 生成。
- 本次采用的关键假设：`app.material_balance.models` 是保留的旧导入兼容路径；`app.material_balance` 包根 model 导出不是公共 HTTP API；本切片不改 route request/response schema、不删 `app.models.MaterialBalanceInput`、不改 calculator 数值语义、不改变 worker strict 默认或 PR-11。
- 剩余不确定性：legacy `app.models` route schema 迁移、完整 backend-only re-export 的最终包面约束、完整 PR-39 schema-driven ASM 重排、PR-11 统一 RHS、worker strict 默认和 Go API 性能项仍待独立处理。
- 影响范围 / 风险控制方式：只收窄 Python import surface；保留 `app.material_balance.models` 显式兼容路径；用 focused boundary test、AST production import scan 和 boundary audit 防止包根 model export 回归。
- 验证方式：首次从仓库根运行 `backend\.venv\Scripts\python -m pytest backend\app\tests\material_balance_compat_models_boundary_test.py backend\app\tests\material_balance_result_thin_shell_test.py backend\app\tests\material_balance_calculator_thin_shell_test.py -q` 因 backend Settings 环境变量未加载而在 collection 前失败；按 backend README 改为 `cd backend; .venv\Scripts\python -m pytest app\tests\material_balance_compat_models_boundary_test.py app\tests\material_balance_result_thin_shell_test.py app\tests\material_balance_calculator_thin_shell_test.py -q` 后通过（6 passed，1 warning）。同时已通过 `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-simulation-core-boundary.ps1`（passed，0 hard violations，0 open gaps）、`backend\.venv\Scripts\python -m pytest docs\rebuild\simulation_core -q`（6 passed，1 skipped）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1`（`pr-fast.json` status `passed`）和 `git diff --check -- .`（仅 LF-to-CRLF notices，无 whitespace error）。
- 是否更新 README：是，更新 `backend/app/README.md`、`backend/app/material_balance/README.md`、`backend/app/tests/README.md`、`scripts/README.md` 和 `docs/rebuild/simulation_core/README.md`；同时校准前置计划、v1.4 开发计划和 current-state。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次是 PR-30/PR-31 既定 compatibility surface cleanup，不新增长期架构决策。

## 22:01 - Core ASM component contract guard

- 用户目标：继续根据 v1.4 性能优化开发计划推进 PR-39 反应组分契约，把 ASM1Slim/ASM1/ASM3 的 core runtime 组分错配从静默风险改为前置校验。
- 涉及目录：`simulation_core/python/autowatersimu_simulation_core/material_balance/`、`simulation_core/tests/`、`scripts/`、`docs/rebuild/simulation_core/`、`docs/architecture/`、`tasks/`、`.ai/changes/`。
- 修改内容：新增 `ASM_COMPONENT_CONTRACTS` 声明 ASM1Slim/ASM1/ASM3 预期组分顺序和 oxygen index；`_convert_to_tensors()` 在 metadata 存在时校验 ASM 组分名前缀顺序，在 metadata 缺失的 legacy 输入中至少校验 concentration 数量；ASM oxygen index 调用改为来自契约常量；新增 boundary tests 覆盖 valid contract fixtures、具名顺序错配和 metadata-less 组分数量不足；correctness-freeze audit 纳入这些测试；同步 material_balance/tests/scripts README、current-state、前置规划、v1.4 开发计划、docs/rebuild README 和专项审查报告。
- 修改原因：PR-39/KPI-019 要求反应组分契约错配非静默。当前 ASM kernels 使用固定张量顺序和硬编码 oxygen index，若上游 metadata 顺序偏离，可能让错误组分进入热路径且不报错。
- 本次已消除的不确定性：确认现有 ASM valid contract fixtures 与当前 tensor order 一致；确认 metadata 存在时可以在 `_convert_to_tensors()` 前置发现顺序错配；确认 metadata-less legacy 输入不能安全推断名称，只能做数量下限 guard。
- 本次采用的关键假设：本切片只做 core runtime fail-fast guard 和 oxygen index 常量化，不改变 contract JSON、legacy route schema、schema-driven 重排、RHS 数学、solver 默认值、output clamp、worker strict 默认值、fallback 或 Go API。
- 剩余不确定性：legacy `app.models` route schema metadata 迁移、真正按 component schema 重排 ASM 张量、完整 backend-only re-export、PR-11 统一 RHS、worker strict 默认和 Go API 性能项仍待独立处理。
- 影响范围 / 风险控制方式：只在 tensor conversion 阶段增加 guard；有效 fixtures 必须继续通过；metadata-less legacy 输入保守只查数量，避免凭空推断名称；v1.4 checklist 仍保持 PR-39 未全量完成。
- 验证方式：已通过 `backend\.venv\Scripts\python -m pytest simulation_core\tests\test_material_balance_core_boundary.py -q`（72 passed）、`backend\.venv\Scripts\python -m pytest simulation_core\tests -q`（79 passed）、`backend\.venv\Scripts\python -m pytest docs\rebuild\simulation_core -q`（6 passed，1 skipped）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-simulation-core-correctness-freeze.ps1`（passed，0 hard violations，0 open gaps）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\performance-golden-phase0.ps1`（passed，12 full runs，7 micro goldens，0 hard violations，0 open gaps）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1`（`pr-fast.json` status `passed`）和 `git diff --check -- .`（仅 LF-to-CRLF notices，无 whitespace error）。
- 是否更新 README：是，更新 `simulation_core/python/autowatersimu_simulation_core/material_balance/README.md`、`simulation_core/tests/README.md`、`scripts/README.md` 和 `docs/rebuild/simulation_core/README.md`；同时校准前置计划、v1.4 开发计划、专项审查报告和 current-state。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次是 PR-39 既定 guard 第一阶段，不新增长期架构决策。

## 21:43 - Backend material_balance model re-export

- 用户目标：继续收口 v1.4 PR-30 / PR-31 backend input/model cleanup，把 `app.material_balance.models` 旧导入路径从本地模型副本改为 simulation_core runtime model re-export。
- 涉及目录：`backend/app/material_balance/`、`backend/app/tests/`、`scripts/`、`docs/rebuild/simulation_core/`、`docs/architecture/`、`tasks/`、`.ai/changes/`。
- 修改内容：用 `autowatersimu_simulation_core.material_balance.models` re-export 替换 `backend/app/material_balance/models.py` 的本地 Pydantic model 副本；更新 package docstring；调整 `material_balance_result_thin_shell_test.py` 和 `material_balance_compat_models_boundary_test.py` 断言 backend/core model identity；boundary audit 改为要求 legacy model import path 是 core model re-export；同步 material_balance/tests/scripts/rebuild/current-state 文档。
- 修改原因：v1.4 方案 A 要求 simulation_core 唯一实现。`backend/app/material_balance/models.py` 已是 compatibility-only 旧路径，继续保留本地字段定义会让维护者误以为 backend 还有独立 runtime input contract。
- 本次已消除的不确定性：确认生产代码不依赖旧 `app.material_balance.models` 本地字段名；确认旧 import path 可保留并指向 core model；确认 boundary audit 可防止本地 model 副本回归。
- 本次采用的关键假设：legacy FastAPI route schema 仍保留在 `app.models.MaterialBalanceInput`，其迁移会影响 OpenAPI/generated client，应另开 PR；本切片不删除 `app.material_balance` 顶层兼容导出、不改变 calculator 数值语义、不改变 worker strict 默认。
- 剩余不确定性：完整 backend-only re-export、legacy `app.models` route schema 迁移、完整 ASM 具名组分契约、PR-11 统一 RHS、worker strict 默认和 Go API 性能项仍待独立处理。
- 影响范围 / 风险控制方式：只改旧 compatibility import path、focused tests、边界审计和文档；保留 `app.material_balance.models` import path；用 object identity tests 和 boundary audit 控制回归。
- 验证方式：已通过 `cd backend; .venv\Scripts\python -m pytest app\tests\material_balance_compat_models_boundary_test.py app\tests\material_balance_result_thin_shell_test.py app\tests\material_balance_calculator_delegation_preflight_test.py app\tests\material_balance_calculator_thin_shell_test.py app\tests\services\material_balance_runtime_input_boundary_test.py -q`（15 passed，1 warning；首次 sandbox 启动失败后按规则非沙箱重跑通过）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-simulation-core-boundary.ps1`（passed，0 hard violations，0 open gaps）、`backend\.venv\Scripts\python -m pytest docs\rebuild\simulation_core -q`（6 passed，1 skipped）、`backend\.venv\Scripts\python -m pytest simulation_core\tests -q`（74 passed）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1`（`pr-fast.json` status `passed`）和 `git diff --check -- .`（仅 LF-to-CRLF notices，无 whitespace error）。
- 是否更新 README：是，更新 `backend/app/material_balance/README.md`、`backend/app/tests/README.md`、`scripts/README.md` 和 `docs/rebuild/simulation_core/README.md`；同时校准前置计划、v1.4 开发计划、专项审查报告和 current-state。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次是 PR-30/PR-31 既定 model cleanup，不新增长期架构决策。

## 21:35 - Backend app.models runtime input validation

- 用户目标：继续收口 v1.4 PR-30 / PR-31 backend input boundary，确保 legacy `app.models.MaterialBalanceInput` 在进入 simulation_core calculator 前走 core runtime model 校验。
- 涉及目录：`backend/app/services/`、`backend/app/tests/`、`scripts/`、`docs/rebuild/simulation_core/`、`docs/architecture/`、`tasks/`、`.ai/changes/`。
- 修改内容：新增 `backend/app/services/material_balance_runtime_input.py`，将 legacy `app.models.MaterialBalanceInput`、core `MaterialBalanceInput` 或 dict 统一转换/校验为 `autowatersimu_simulation_core.material_balance.models.MaterialBalanceInput`；material balance、ASM1Slim、ASM1、ASM3、UDM 五个 legacy calculation service 在 `_run_calculation_sync()` 调用 calculator 前先走该 helper；新增 `backend/app/tests/services/material_balance_runtime_input_boundary_test.py`；boundary audit 新增 service runtime input revalidation 检查；同步 services/tests/scripts/rebuild/current-state 文档。
- 修改原因：v1.4 PR-30 要求服务层对 `app.models` SQLModel 对象做一次显式校验/转换，避免 legacy route schema 对象直接作为 runtime contract 绕过 simulation_core `extra="forbid"` 与 runtime validators。
- 本次已消除的不确定性：确认 legacy service 入口可以保持 route schema 不变，同时在 calculator 前构造 core runtime input；确认未知 node runtime 字段会被 core model 拒绝；确认五个 service entrypoints 都有统一 helper 调用并由 audit 检查。
- 本次采用的关键假设：本切片保留 legacy FastAPI route schema、OpenAPI/generated clients 和 `app.models.MaterialBalanceInput`；删除本地 input models 或迁移 route schema 属于后续更高风险 PR-30/PR-31 切片。
- 剩余不确定性：完整 backend-only re-export、legacy route schema 迁移、本地 input models 删除、完整 ASM 具名组分契约、PR-11 统一 RHS、worker strict 默认和 Go API 性能项仍待独立处理。
- 影响范围 / 风险控制方式：只改 service 入核前转换、focused tests、边界审计和文档；不改公共 HTTP schema、不改 runtime 数值实现；用 delegation preflight、service focused tests 和 boundary audit 控制兼容风险。
- 验证方式：已通过 `cd backend; .venv\Scripts\python -m pytest app\tests\services -q`（32 passed，1 warning）、`cd backend; .venv\Scripts\python -m pytest app\tests\material_balance_calculator_delegation_preflight_test.py app\tests\material_balance_calculator_thin_shell_test.py app\tests\material_balance_runtime_helpers_thin_shell_test.py app\tests\material_balance_result_thin_shell_test.py app\tests\material_balance_utils_thin_shell_test.py app\tests\material_balance_segment_overrides_test.py app\tests\udm_engine_variable_binding_test.py -q`（17 passed，1 warning）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-simulation-core-boundary.ps1`（passed，0 hard violations，0 open gaps）、`backend\.venv\Scripts\python -m pytest docs\rebuild\simulation_core -q`（6 passed，1 skipped）、`backend\.venv\Scripts\python -m pytest simulation_core\tests -q`（74 passed）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1`（`pr-fast.json` status `passed`）和 `git diff --check -- .`（仅 LF-to-CRLF notices，无 whitespace error）。
- 是否更新 README：是，更新 `backend/app/services/README.md`、`backend/app/tests/README.md`、`scripts/README.md` 和 `docs/rebuild/simulation_core/README.md`；同时校准前置计划、v1.4 开发计划和 current-state。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次是 PR-30 既定 input-boundary 收口，不新增长期架构决策。

## 21:25 - Backend ASM/UDM runtime helper thin-shell

- 用户目标：继续根据 `docs/rebuild/simulation_core/` 的 v1.4 性能优化开发计划推进 PR-31/backend thin-shell 收口，本次先把 backend ASM/UDM runtime helper 叶子迁到 simulation_core re-export。
- 涉及目录：`backend/app/material_balance/`、`backend/app/tests/`、`scripts/`、`docs/rebuild/simulation_core/`、`docs/architecture/`、`tasks/`、`.ai/changes/`。
- 修改内容：将 `backend/app/material_balance/asm/*`、`udm_engine.py` 与 `udm_ode.py` 改为 re-export `autowatersimu_simulation_core.material_balance` 对应实现；新增 `backend/app/tests/material_balance_runtime_helpers_thin_shell_test.py` 断言 ASM/UDM helper object identity；`scripts/audit-simulation-core-boundary.ps1` 新增 backend runtime helper thin-shell 检查；同步 backend、scripts、rebuild/current-state README 与 v1.4 PR-31 当前状态。
- 修改原因：v1.4 方案 A 要求 simulation_core 成为唯一实现、backend 薄壳化。calculator/exceptions/utils 已薄壳化后，ASM/UDM helper 仍是 backend-local runtime 副本，存在继续漂移风险。
- 本次已消除的不确定性：确认 backend ASM/UDM helper import paths 可以保持兼容且直接指向 simulation_core；确认 production/runtime 静态引用不需要 backend-local helper 副本；确认 boundary audit 可把 helper thin-shell 状态纳入默认边界证据。
- 本次采用的关键假设：保留 legacy backend import paths 是当前最小风险做法；re-export 私有 UDM helper 名称仅服务兼容测试和旧导入，不代表新公共 API；本切片不删除 `backend/app/material_balance/models.py`、不改变 legacy FastAPI route schema、不改变 simulation_core 数值语义、solver、worker strict 默认或 PR-11 RHS 结构。
- 剩余不确定性：完整 PR-31 的“backend 仅 re-export”仍未完成，legacy input models/import path 与 route schema cleanup 需后续 PR-30/PR-31 切片；完整 ASM 具名组分契约、PR-11 统一 RHS、worker strict 默认和 Go API 性能项仍待独立处理。
- 影响范围 / 风险控制方式：只改 backend compatibility re-export 层、focused tests、边界审计和文档；真实 helper 实现保持在 simulation_core；用 object identity tests 和 boundary audit 防止重新复制。
- 验证方式：已通过 `cd backend; .venv\Scripts\python -m pytest app\tests\material_balance_runtime_helpers_thin_shell_test.py app\tests\udm_engine_variable_binding_test.py app\tests\material_balance_exceptions_thin_shell_test.py app\tests\material_balance_result_thin_shell_test.py app\tests\material_balance_utils_thin_shell_test.py app\tests\material_balance_calculator_thin_shell_test.py -q`（7 passed，1 warning）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-simulation-core-boundary.ps1`（passed，0 hard violations，0 open gaps）、`backend\.venv\Scripts\python -m pytest docs\rebuild\simulation_core -q`（6 passed，1 skipped）、`backend\.venv\Scripts\python -m pytest simulation_core\tests -q`（74 passed）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1`（`pr-fast.json` status `passed`）和 `git diff --check -- .`（仅 LF-to-CRLF notices，无 whitespace error）。
- 是否更新 README：是，更新 `backend/app/README.md`、`backend/app/material_balance/README.md`、`backend/app/material_balance/asm/README.md`、`backend/app/tests/README.md`、`scripts/README.md` 和 `docs/rebuild/simulation_core/README.md`；同时校准 `docs/rebuild/simulation_core/00_AutoWaterSimu_性能优化前置规划计划文档_v1.0.md` 与 v1.4 开发计划/current-state。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次是方案 A/PR-31 既定薄壳化切片，不新增长期架构决策。

## 21:09 - PR-37 core-only f64 golden guard

- 用户目标：继续根据 `docs/rebuild/simulation_core/` 的 v1.4 性能优化开发计划收口 PR-37 parity→golden 改造，消除 `simulation_core/tests` 对 legacy backend oracle 的依赖。
- 涉及目录：`simulation_core/tests/`、`scripts/`、`docs/rebuild/simulation_core/`、`docs/architecture/`、`tasks/`、`.ai/changes/`。
- 修改内容：将 `simulation_core/tests/test_material_balance_core.py` 从 backend parity/oracle 测试改为 core-only CPU/f64 committed golden 测试；新增 `CORE_F64_GOLDEN_CASES` 覆盖 material-balance minimal、ASM1Slim model-bound、独立 ASM1Slim/ASM1/ASM3/UDM 六个 fixture，并断言 job type、node type、参数字段/数量、timestamp count、total steps 与稳定结果 hash；`scripts/audit-simulation-core-boundary.ps1` 改为识别 core-only f64 golden guard，同时继续要求 backend-side delegation preflight；同步 tests/scripts/rebuild README、current-state、v1.4 PR-37 状态/checklist 和 task 记录。
- 修改原因：v1.4 PR-37 要求把 6 个 `test_core_calculator_matches_backend_*` 改为对照 f64 golden，且 core-only 测试不得模块顶层导入 `app.*` 或把 `backend/` 加入 `sys.path`。backend legacy-input 兼容性对照应留在 backend-dependent lane，而不是让 simulation_core tests 依赖 backend oracle。
- 本次已消除的不确定性：确认原 6 个 parity fixture 可由 simulation_core adapter 在 CPU/f64 下稳定计算；确认 backend 侧已有 `material_balance_calculator_delegation_preflight_test.py` 覆盖 legacy/backend vs core preflight；确认 boundary audit 可在不保留 backend imports 的情况下继续控制双实现/薄壳化风险。
- 本次采用的关键假设：committed stable result hash 是当前 PR-37 阶段的 core oracle；未来若 fixture、solver matrix、correctness-freeze 行为或结果序列结构改变，必须同步刷新 committed hashes 与 P-03 golden evidence；本切片不改变 runtime 数值行为、public schema/API、worker strict 默认、solver 默认或 backend helper cleanup。
- 剩余不确定性：backend 全 re-export/ASM/UDM helper cleanup、输入模型契约统一与死代码清理、完整 ASM component-schema→model 组分契约、PR-11 统一 RHS、Go keyset/claim 性能项仍待独立切片。
- 影响范围 / 风险控制方式：只改测试 oracle、边界审计识别规则和文档状态；保留同一批六个 fixture 的覆盖；backend 对照保留在 backend tests；用 targeted golden tests 与 boundary audit 验证。
- 验证方式：已通过 `backend\.venv\Scripts\python -m pytest simulation_core\tests\test_material_balance_core.py -q`（7 passed）、`backend\.venv\Scripts\python -m pytest simulation_core\tests -q`（74 passed）、`backend\.venv\Scripts\python -m pytest docs\rebuild\simulation_core -q`（6 passed，1 skipped）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-simulation-core-boundary.ps1`（passed，0 hard violations，0 open gaps）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\performance-golden-phase0.ps1`（passed，12 full runs，7 micro goldens，0 hard violations，0 open gaps）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1`（`pr-fast.json` status `passed`）和 `git diff --check -- .`（仅 LF-to-CRLF notices，无 whitespace error）。
- 是否更新 README：是，更新 `simulation_core/tests/README.md`、`scripts/README.md` 和 `docs/rebuild/simulation_core/README.md`；同时校准 `docs/rebuild/simulation_core/AutoWaterSimu_simulation_core_专项审查报告.md` 的 A3 当前状态。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次是 v1.4 PR-37 既定测试迁移，不新增架构决策。

## 07:30 - Performance flag matrix evidence

- 用户目标：继续根据 `docs/rebuild/simulation_core/` 的性能优化计划收口上线检查清单；本次补齐 “flag 组合矩阵测试；每 flag 有退场条件” 的机器可读 evidence。
- 涉及目录：`scripts/ci/`、`Justfile`、`scripts/`、`docs/rebuild/simulation_core/`、`docs/architecture/`、`tasks/`、`.ai/changes/`。
- 修改内容：新增 `scripts/ci/performance-flag-matrix-phase0.ps1`，输出 `performance-flag-matrix-phase0.json` / `.md`；覆盖 6 个 v1.4 性能 flags 与 supporting rollout flag `AUTOWATERSIMU_WORKER_ADAPTER_VALIDATION_MODE`，验证默认值、目标值、runtime 状态、matrix cases 与退场条件；同步 Justfile、scripts README、v1.4 flag 表/checklist、docs/rebuild README、current-state 和 task 记录。
- 修改原因：v1.4 要求 flag 组合矩阵与每个 flag 的 sunset/退场条件。当前多个性能 flags 尚未实现 runtime toggle，直接声称 runtime 组合已跑会过度；因此先落可复跑的 metadata/evidence gate，明确 planned/blocked 状态。
- 本次已消除的不确定性：确认当前真实 runtime flag 主要是 worker adapter validation mode；确认 `UDM_EXPR_CACHE` 已 always-on 但无 runtime toggle；确认 `SOLVER_DEFAULT` 已由 ADR 0016 决定不切换；确认其余性能 flags 需等 PR-11/12/25 等实现后才能执行 runtime combo。
- 本次采用的关键假设：flag matrix checklist 可用“metadata/evidence gate + planned/blocked runtime status”收口当前规划项；未来真正实现 planned flags 时必须把 blocked matrix case 转为 runtime 测试或更新 evidence。
- 剩余不确定性：`USE_UNIFIED_RHS`、`UDM_EXPRESSION_ENGINE`、`CLAMP_STATE_IN_RHS` 与 `SHADOW_RHS_COMPARE` 的实际 runtime 实现和组合执行仍待对应 PR；backend 全 re-export、输入模型死代码清理、完整 ASM component contract、PR-37 parity→golden 最终覆盖、PR-11 统一 RHS 与 Go API 性能项仍待独立切片。
- 影响范围 / 风险控制方式：只新增 opt-in evidence 脚本与文档/Justfile 入口；不改变 simulation_core runtime、worker default、schema/OpenAPI、solver 默认值、Go API 或发布 gate。
- 验证方式：已通过 `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\performance-flag-matrix-phase0.ps1`（passed，7 flags，8 matrix cases，0 hard violations）、`backend\.venv\Scripts\python -m pytest docs\rebuild\simulation_core -q`（6 passed，1 skipped）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1`（`pr-fast.json` status `passed`）、`rg -n 'UDM_EXPR_CACHE|USE_UNIFIED_RHS|exit_condition|matrix_cases|runtime_combo_execution' tmp\ci-evidence\performance-flag-matrix-phase0.json`（字段存在）和 `git diff --check -- .`（仅 LF-to-CRLF notices，无 whitespace error）。
- 是否更新 README：是，更新 `scripts/README.md`、`scripts/ci/README.md` 和 `docs/rebuild/simulation_core/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次记录的是已知 planned flags 的组合矩阵与退场条件，不改变运行时策略。

## 07:15 - Worker repo-path fallback 删除

- 用户目标：继续根据 `docs/rebuild/simulation_core/` 的性能优化计划收口 PR-29 / Phase 0 installability checklist；本次删除 worker runtime 的 repo-path `sys.path` fallback。
- 涉及目录：`services/simulation-worker/`、`scripts/`、`scripts/ci/`、`docs/rebuild/simulation_core/`、`docs/architecture/`、`tasks/`、`.ai/changes/`。
- 修改内容：删除 worker `_ensure_deprecated_repo_import_paths()` 调用和实现；helper package 缺失时直接返回/抛出缺失依赖错误；保留 self-check 兼容字段 `missing_before_fallback`、`missing_after_fallback`、`deprecated_repo_path_fallback_used=false`；将 worker dependency tests 改为 no-fallback fail-fast/report 语义；同步 worker/scripts README、current-state、v1.4 PR-29/status/checklist、前置计划和 task 记录。
- 修改原因：v1.4 要求 `simulation_core` 可安装并删除 `sys.path` hack；当前 source-mode dependency gate、boundary audit 与 packaged no-fallback evidence 已证明 worker 可通过 installed/editable helper packages 运行。
- 本次已消除的不确定性：确认当前 worker tests 未直接依赖 fallback 成功路径；确认 boundary audit 已有“worker runtime does not mutate sys.path”的通过分支；确认 self-check 字段可兼容保留为 no-fallback evidence。
- 本次采用的关键假设：backend Python environment 与 packaged sidecar build 会继续安装/打包 `autowatersimu-simulation-core` 与 `autowatersimu-contracts`；缺失依赖应作为配置错误 fail-fast，而不是自动从 repo path 补救。
- 剩余不确定性：backend 全面 re-export、输入模型死代码清理、完整 ASM component contract、PR-37 parity→golden 最终覆盖、PR-11 统一 RHS、flag matrix 与 Go API 性能项仍待独立切片。
- 影响范围 / 风险控制方式：只改 worker dependency import fallback 路径和文档；不改变 job 执行、artifact、schema、simulation_core 数值行为、worker strict 默认、solver 默认或 packaged build 脚本。
- 验证方式：已通过 `backend\.venv\Scripts\python -m pytest services\simulation-worker\tests -q`（27 passed）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-worker-dependency-installation.ps1`（passed，`deprecated_repo_path_fallback_used=False`，0 hard violations）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-simulation-core-boundary.ps1`（passed，0 hard violations，0 open gaps）、`backend\.venv\Scripts\python -m pytest docs\rebuild\simulation_core -q`（6 passed，1 skipped）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\worker-packaged-no-fallback-smoke.ps1`（passed，`deprecated_repo_path_fallback_used=False`，0 hard violations）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1`（`pr-fast.json` status `passed`）、stale fallback text / worker `sys.path` runtime searches（no matches）和 `git diff --check -- .`（仅 LF-to-CRLF notices，无 whitespace error）。
- 是否更新 README：是，更新 `services/simulation-worker/README.md`、`services/simulation-worker/simulation_worker/README.md`、`services/simulation-worker/tests/README.md`、`scripts/README.md`、`scripts/ci/README.md` 和 `docs/rebuild/simulation_core/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；这是 PR-29 既定 installability 计划的执行，不新增架构取舍。

## 07:06 - 绝对阈值 KPI baseline 硬件指纹与 nightly 策略

- 用户目标：继续根据 `docs/rebuild/simulation_core/` 的性能优化计划收口上线检查清单；本次补齐“绝对阈值 KPI 仅 nightly 固定 runner，基线含硬件指纹”的规划/证据字段。
- 涉及目录：`services/simulation-worker/`、`scripts/ci/`、`docs/rebuild/simulation_core/`、`docs/architecture/`、`tasks/`、`.ai/changes/`。
- 修改内容：worker `self_check()` 新增 `torch_runtime` 元数据；`performance-baseline-phase0.ps1` 新增 `environment.hardware_fingerprint` 与 `absolute_threshold_policy` 字段；同步 worker/scripts/rebuild README、current-state、v1.4 checklist/status 和 task 记录。
- 修改原因：REQ-P0 baseline 要求记录 torch 线程数和硬件指纹；v1.4 要求绝对延迟/吞吐类 KPI 只在 nightly 固定 runner 判定，merge/local baseline 仅作 smoke 与证据。
- 本次已消除的不确定性：确认当前 baseline 已覆盖 12-run solver matrix 但缺少独立硬件指纹；确认 worker self-check 可作为 baseline 脚本获取 torch 线程配置的稳定入口；确认该清单项可通过 policy+metadata 收口，不需要在本地脚本强制绝对阈值。
- 本次采用的关键假设：`KPI-007`、`KPI-008` 和 `KPI-015` 是当前绝对阈值 KPI 集合；硬件指纹使用 OS/runtime/CPU/thread 元数据与 hash 绑定 runner 环境即可，不记录机器名等身份信息；future nightly lane 会消费该 metadata 或生成同形 evidence。
- 剩余不确定性：真实 hosted/self-hosted nightly workflow、阈值数值、固定 runner 选择、Go keyset/claim 优化、worker 端到端基线、flag matrix、PR-11 统一 RHS、完整 ASM component contract、fallback 删除仍待独立切片。
- 影响范围 / 风险控制方式：只新增 additive self-check/evidence metadata 和文档状态；不改变 worker job 执行、simulation_core 数值行为、公共 schema/OpenAPI、solver 默认值、worker strict 默认或 fallback 行为。
- 验证方式：已通过 `backend\.venv\Scripts\python -m pytest services\simulation-worker\tests -q`（28 passed）、`backend\.venv\Scripts\python -m pytest docs\rebuild\simulation_core -q`（6 passed，1 skipped）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\performance-baseline-phase0.ps1`（passed，12 runs，0 hard violations，0 open gaps）、`rg -n 'torch_runtime|hardware_fingerprint|absolute_threshold_policy|fingerprint_sha256' tmp\ci-evidence\performance-baseline-phase0.json`（字段存在）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1`（`pr-fast.json` status `passed`）和 `git diff --check -- .`（仅 LF-to-CRLF notices，无 whitespace error）。
- 是否更新 README：是，更新 `services/simulation-worker/README.md`、`services/simulation-worker/simulation_worker/README.md`、`scripts/ci/README.md` 与 `docs/rebuild/simulation_core/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本次只是 evidence/policy metadata，不改变架构决策。

## 05:54 - PR-13a expression validator whitelist

- 用户目标：继续根据 `docs/rebuild/simulation_core/` 的性能优化计划完成后续前置/开发切片，本次收口 PR-13a 表达式校验器白名单化，而不混入 solver、RHS、schema 或 worker fallback 改动。
- 涉及目录：`simulation_core/python/autowatersimu_simulation_core/material_balance/`、`simulation_core/tests/`、`docs/rebuild/simulation_core/`、`docs/architecture/`、`tasks/`、`.ai/changes/`。
- 修改内容：将 `_validate_ast()` 从 denylist/fallthrough 改为默认拒绝未知 AST 节点的白名单校验；allowlisted function call 现在在校验期拒绝 keyword arguments；新增 core-only boundary tests 覆盖 `NamedExpr`、`JoinedStr`、`Starred`、`Subscript`/`Slice` 和 keyword arguments 的 fail-early 行为；同步 simulation_core、material_balance、tests、rebuild planning 与 current-state 文档，并修正旧的 “commented clamp” 表述为当前 default no-clamp baseline。
- 修改原因：v1.4 将 PR-13a 定性为 fail-late 一致性问题：部分 AST 节点原先可能通过校验，直到运行时 evaluator 才失败。性能优化前需要让校验白名单与运行时支持范围保持一致，避免错误表达式进入仿真阶段。
- 本次已消除的不确定性：确认合法表达式语法面不需要扩大；确认未知 AST 节点应统一返回 `DISALLOWED_SYNTAX`；确认 keyword call arguments 应作为 `DISALLOWED_CALL` 在校验期失败；确认本次不是 RCE 修复叙事，也不改变 UDM runtime 数值语义。
- 本次采用的关键假设：保持现有 evaluator 合法语法集合不变；不改变表达式缓存策略、UDM runtime 数值语义、solver/output-grid 行为、schema/API、worker strict 默认值、deprecated fallback 或 Go API；KPI-017 build-time 收益和 fuzz-style 扩展不在本切片内声明完成。
- 剩余不确定性：KPI-017 N=100 build-time 证据仍需后续复测；更系统的 fuzz/property-style 表达式校验覆盖仍是上线清单开放项；PR-11 统一 RHS、PR-12 输出投影、PR-36 solver 矩阵、完整 PR-39 组分契约、worker fallback 删除和 Go API 性能项仍待独立切片。
- 影响范围 / 风险控制方式：只改 UDM expression 校验和 focused tests；不改 evaluator 支持语法、不改仿真数值路径、不改公共 schema/API；文档明确这是 fail-late 一致性修复，避免安全叙事扩大。
- 验证方式：已通过 `backend\.venv\Scripts\python -m pytest simulation_core\tests\test_material_balance_core_boundary.py -q`（40 passed）、`backend\.venv\Scripts\python -m pytest simulation_core\tests -q`（47 passed）、`backend\.venv\Scripts\python -m pytest docs\rebuild\simulation_core -q`（6 passed，1 skipped）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-simulation-core-correctness-freeze.ps1`（passed，0 hard violations，0 open gaps）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-simulation-core-boundary.ps1`（passed，0 hard violations，0 open gaps）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-simulation-core-input-contract.ps1`（passed，0 hard violations，0 open gaps）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\performance-golden-phase0.ps1`（passed，12 full runs，6 micro goldens，0 hard violations，0 open gaps）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\performance-hotpath-prereview-phase0.ps1`（passed，0 hard violations，0 open gaps）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1`（`pr-fast.json` status `passed`）和 `git diff --check -- .`（仅 LF-to-CRLF notices，无 whitespace error）。
- 是否更新 README：是，更新 `simulation_core/README.md`、`simulation_core/python/autowatersimu_simulation_core/material_balance/README.md`、`simulation_core/tests/README.md` 和 `docs/rebuild/simulation_core/README.md`，记录 expression validator whitelist 与当前 PR-13a 状态。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次是既有 v1.4 PR-13a 的 fail-late 一致性实现，不新增长期架构决策。

## 06:00 - KPI-017 expression cache build evidence

- 用户目标：继续根据 `docs/rebuild/simulation_core/` 的性能优化计划推进开放项；本次补齐 KPI-017 N=100 expression cache build-time evidence，而不改 runtime 语义。
- 涉及目录：`scripts/ci/`、`docs/rebuild/simulation_core/`、`docs/architecture/`、`tasks/`、`.ai/changes/`。
- 修改内容：`performance_golden_phase0.py` 新增 `udm_expression_cache_build_l1` micro golden，并将 `udm_expression_cache_build_time` 纳入 priority coverage；证据比较 100 次清 cache 编译与 100 次同表达式 cached 编译，要求降低 ≥70%、evaluator identity 共享且 L1 数值等价；同步 scripts/ci README、simulation_core rebuild docs、v1.4 checklist、current-state 和任务记录。
- 修改原因：v1.4 需求明确 KPI-017 为 N=100 同模型 build 耗时降低 ≥70%。`compile_expression()` 缓存已实现，但缺少机器可读 evidence 证明该 KPI 可复跑。
- 本次已消除的不确定性：确认现有 LRU cache 在本地 N=100 same-expression build 场景下通过 evidence gate；确认 cached path 返回共享 evaluator；确认 evidence 可放入 P-03 golden micro coverage，而不需要新增 runtime API 或修改 worker 默认路径。
- 本次采用的关键假设：KPI-017 build-time evidence 使用同一表达式文本模拟同模型重复节点；cold/no-cache baseline 通过每次编译前 `cache_clear()` 模拟无缓存；该 evidence 是本地可复跑门，不等同于端到端 UDM RHS 收益或表达式引擎替换。
- 剩余不确定性：KPI-001 / UDM RHS 收益仍需按 `scipy_solver` 与 torch 原生求解器拆分；更系统的 expression validator fuzz/property-style 覆盖仍未完成；PR-11、PR-12、PR-36、完整 PR-39、worker fallback 删除与 Go API 性能项仍待独立切片。
- 影响范围 / 风险控制方式：只扩展 opt-in Phase 0 golden evidence 和文档；raw timing 保留在 JSON/Markdown evidence，但排除出 micro-golden stable hash；不改变 expression runtime、solver、schema、worker strict mode、fallback 或 Go API。
- 验证方式：已通过 `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\performance-golden-phase0.ps1`（passed，12 full runs，7 micro goldens，0 hard violations，0 open gaps；KPI-017 cold/no-cache 5.542 ms，cached repeated 0.065 ms，observed reduction 98.82%）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\performance-hotpath-prereview-phase0.ps1`（passed，0 hard violations，0 open gaps）、`backend\.venv\Scripts\python -m pytest docs\rebuild\simulation_core -q`（6 passed，1 skipped）、`backend\.venv\Scripts\python -m pytest simulation_core\tests -q`（47 passed）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1`（`pr-fast.json` status `passed`）和 `git diff --check -- .`（仅 LF-to-CRLF notices，无 whitespace error）。
- 是否更新 README：是，更新 `scripts/ci/README.md` 和 `docs/rebuild/simulation_core/README.md`，记录 KPI-017 build-time evidence 已纳入 Phase 0 golden coverage。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次是既有 KPI 的 evidence 扩展，不引入新架构决策。

## 06:04 - PR-13a expression validator deterministic corpus

- 用户目标：继续收口 v1.4 表达式校验器白名单化后的 fuzz/property-style 覆盖开放项，保持切片独立可验证。
- 涉及目录：`simulation_core/tests/`、`docs/rebuild/simulation_core/`、`docs/architecture/`、`tasks/`、`.ai/changes/`。
- 修改内容：在 core-only boundary tests 中新增 allowlisted expression corpus 与 disallowed AST corpus；允许 corpus 覆盖算术、一元运算、简单函数调用、嵌套 allowlisted 函数与 `clip`/`min`/`max` 组合；拒绝 corpus 覆盖 `List`、`Tuple`、`Dict`、`IfExp`、`Compare`、`BoolOp`、`Lambda`、`Attribute`、`Subscript`、`Starred`、`NamedExpr` 和 `JoinedStr`，并同时断言 `compile_expression()` 与 `validate_udm_definition()` 在校验期失败；同步 tests README、current-state、v1.4 checklist 和任务记录。
- 修改原因：PR-13a 主体与 KPI-017 evidence 已完成后，v1.4 checklist 仍保留 fuzz/property-style 覆盖开放项。用确定性 corpus 可在不引入新依赖的情况下覆盖白名单正反两侧契约。
- 本次已消除的不确定性：确认白名单不会误拒当前支持的合法表达式；确认代表性未知/不支持 AST 节点会 fail early 为 `DISALLOWED_SYNTAX`；确认本次不需要引入 Hypothesis 或随机 fuzz 依赖。
- 本次采用的关键假设：deterministic corpus 足以满足当前 v1.4 “fuzz/property-style”收口需求；更重的随机/生成式 fuzz 可作为未来测试增强，但不作为当前性能计划阻塞项；不改变 evaluator 语法、UDM runtime 数值语义、solver/schema/worker/fallback/Go API。
- 剩余不确定性：KPI-001 / UDM RHS 收益仍需按求解器拆分；PR-11 统一 RHS、PR-12 输出投影、PR-36 solver 矩阵、完整 PR-39 组分契约、worker fallback 删除与 Go API 性能项仍待独立切片。
- 影响范围 / 风险控制方式：只新增 tests 和文档，不改 runtime；拒绝 corpus 使用固定表达式列表，避免随机测试造成 flaky；保留 fail-late/RCE 定性边界。
- 验证方式：已通过 `backend\.venv\Scripts\python -m pytest simulation_core\tests\test_material_balance_core_boundary.py -q`（57 passed）、`backend\.venv\Scripts\python -m pytest simulation_core\tests -q`（64 passed）、`backend\.venv\Scripts\python -m pytest docs\rebuild\simulation_core -q`（6 passed，1 skipped）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-simulation-core-correctness-freeze.ps1`（passed，0 hard violations，0 open gaps）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\performance-golden-phase0.ps1`（passed，12 full runs，7 micro goldens，0 hard violations，0 open gaps）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\performance-hotpath-prereview-phase0.ps1`（passed，0 hard violations，0 open gaps）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1`（`pr-fast.json` status `passed`）和 `git diff --check -- .`（仅 LF-to-CRLF notices，无 whitespace error）。
- 是否更新 README：是，更新 `simulation_core/tests/README.md`，记录 expression AST whitelist/corpus 测试职责。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次是测试覆盖扩展，不新增架构决策。

## 06:08 - PR-36 solver matrix policy freeze

- 用户目标：继续推进 v1.4 优化计划中 PR-36 solver 矩阵/白名单对齐的低风险部分，避免直接混入默认求解器行为变更。
- 涉及目录：`simulation_core/tests/`、`docs/rebuild/simulation_core/`、`docs/architecture/`、`tasks/`、`.ai/changes/`。
- 修改内容：新增 core-only boundary test，冻结 `CalculationParameters` 默认 `solver_method="scipy_solver"`、接受 `adaptive_heun`、拒绝 `dopri5`；更新 tests README、current-state、v1.4 PR-36 当前状态和 checklist，将当前矩阵决策记录为使用 `scipy_solver` / `rk4` / `adaptive_heun`，不放开 `dopri5`。
- 修改原因：REQ-P0-013 允许两种路径：`dopri5` 入白名单并补测，或将文档矩阵改为 `adaptive_heun`。当前 Phase 0 baseline/profiling/golden 已稳定覆盖 `scipy_solver` / `rk4` / `adaptive_heun`，直接放开 `dopri5` 会扩大公共输入行为，适合另开 PR。
- 本次已消除的不确定性：确认当前验收矩阵选择 `adaptive_heun` 而非 `dopri5`；确认真实默认仍是 `scipy_solver`；确认默认求解器切换、`max_iterations` / `max_memory_mb` 处理不属于本低风险切片。
- 本次采用的关键假设：保持 `dopri5` 拒绝状态比直接放开更保守；默认求解器变更需要 flag、存量影响评估和单独 release note；当前脚本矩阵中的 `scipy_solver` / `rk4` / `adaptive_heun` 足以满足当前可复跑 evidence。
- 剩余不确定性：默认求解器是否改为 `rk4` 或 `adaptive_heun` 仍未评估；`max_iterations` / `max_memory_mb` 仍需实现或标 deprecated；PR-11、PR-12、完整 PR-39、worker fallback 删除与 Go API 性能项仍待独立切片。
- 影响范围 / 风险控制方式：只新增测试和文档；不改 runtime solver implementation、公开 schema、worker、backend、默认值或 solver 白名单；用测试防止后续无意放开/改变 solver policy。
- 验证方式：已通过 `backend\.venv\Scripts\python -m pytest simulation_core\tests\test_material_balance_core_boundary.py -q`（58 passed）、`backend\.venv\Scripts\python -m pytest simulation_core\tests -q`（65 passed）、`backend\.venv\Scripts\python -m pytest docs\rebuild\simulation_core -q`（6 passed，1 skipped）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-simulation-core-correctness-freeze.ps1`（passed，0 hard violations，0 open gaps）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\performance-golden-phase0.ps1`（passed，12 full runs，7 micro goldens，0 hard violations，0 open gaps）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\performance-hotpath-prereview-phase0.ps1`（passed，0 hard violations，0 open gaps）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1`（`pr-fast.json` status `passed`）和 `git diff --check -- .`（仅 LF-to-CRLF notices，无 whitespace error）。
- 是否更新 README：是，更新 `simulation_core/tests/README.md`，记录 solver matrix policy boundary test。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次是 v1.4 PR-36 的当前矩阵决策与测试冻结，不改变长期架构。

## 06:13 - PR-36 deprecated solver limit fields

- 用户目标：继续推进 v1.4 优化计划中 PR-36 关于 `max_iterations` / `max_memory_mb` “实现或标 deprecated”的开放项，并保持与现有 solver 行为兼容。
- 涉及目录：`simulation_core/python/autowatersimu_simulation_core/material_balance/`、`simulation_core/tests/`、`docs/rebuild/simulation_core/`、`docs/architecture/`、`tasks/`、`.ai/changes/`。
- 修改内容：将 `CalculationParameters.max_iterations` 和 `max_memory_mb` 保留为继续接受并校验的兼容字段，同时添加 `json_schema_extra={"deprecated": True}` 与说明当前 simulation_core solvers 不执行这些限制；新增 focused boundary test 锁定 deprecation metadata；同步 material_balance/tests README、current-state、v1.4 PR-36 状态与 checklist、任务记录。
- 修改原因：当前 solver 代码没有读取或执行这两个字段，直接实现 step/memory enforcement 会改变 runtime 行为并需要 contracts/worker/backend 联动。REQ-P0-013 允许实现或标 deprecated，本切片选择低风险的 deprecated compatibility path。
- 本次已消除的不确定性：确认字段当前只在 Pydantic model 层校验；确认没有现有 solver enforcement 可被补文档声明为已实现；确认保持字段可传入比删除字段更兼容；确认默认 solver 切换和 `dopri5` 放开不是本切片目标。
- 本次采用的关键假设：schema metadata 的 deprecated 标记足以表达当前兼容字段状态；真实 step/memory enforcement 需要未来独立 PR，且需覆盖 contracts、worker、backend/API 文档和行为测试；本次不改变数值路径、solver 默认值、solver 白名单、payload shape 或 public API。
- 剩余不确定性：默认求解器是否切换仍未评估；真实 `max_iterations` / `max_memory_mb` enforcement 是否需要实现仍待产品/架构取舍；PR-11 统一 RHS、PR-12 输出投影、完整 PR-39 组分契约、worker fallback 删除与 Go API 性能项仍待独立切片。
- 影响范围 / 风险控制方式：只改 schema metadata、focused tests 和文档；不删除字段、不改变校验范围、不改变 runtime solver 行为；用测试防止后续误删 deprecated contract。
- 验证方式：已通过 `backend\.venv\Scripts\python -m pytest simulation_core\tests\test_material_balance_core_boundary.py -q`（60 passed）、`backend\.venv\Scripts\python -m pytest simulation_core\tests -q`（67 passed）、`backend\.venv\Scripts\python -m pytest docs\rebuild\simulation_core -q`（6 passed，1 skipped）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-simulation-core-correctness-freeze.ps1`（passed，0 hard violations，0 open gaps）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\performance-golden-phase0.ps1`（passed，12 full runs，7 micro goldens，0 hard violations，0 open gaps）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\performance-hotpath-prereview-phase0.ps1`（passed，0 hard violations，0 open gaps）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1`（`pr-fast.json` status `passed`）和 `git diff --check -- .`（仅 LF-to-CRLF notices，无 whitespace error）。
- 是否更新 README：是，更新 `simulation_core/python/autowatersimu_simulation_core/material_balance/README.md` 与 `simulation_core/tests/README.md`，记录 deprecated solver limit compatibility contract 和测试职责。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次选择是 v1.4 PR-36 的低风险兼容收口，不新增长期架构决策。

## 06:27 - ASM stable reaction runtime precompute

- 用户目标：继续根据 v1.4 性能优化开发计划收口 “ASM 稳定 mask gather 已预解析或有 profiler 证据说明剩余成本” checklist 项，保持热路径切片独立可验证。
- 涉及目录：`simulation_core/python/autowatersimu_simulation_core/material_balance/`、`simulation_core/tests/`、`scripts/ci/`、`docs/rebuild/simulation_core/`、`docs/architecture/`、`tasks/`、`.ai/changes/`。
- 修改内容：新增 ASM active reaction runtime helper，在 `_convert_to_tensors()` 阶段预计算 ASM1Slim/ASM1/ASM3 active compute node indices 与 filtered parameter rows；single-model 与 combined RHS 复用该 runtime，避免每步重新做布尔 mask 参数 gather；新增 focused boundary test 锁定 runtime payload；P-08 prereview 候选表加入 `asm-stable-reaction-runtime-precompute` completed 项；同步 material_balance/tests README、current-state、v1.4 checklist 和任务记录。
- 修改原因：v1.4 把 ASM mask gather 纳入 PR-11/Phase 4 前置风险。当前无需进入全统一 RHS；先把稳定的 ASM 参数/索引预计算，能降低 RHS 准备开销，同时保持混合调度与氧清零语义不变。
- 本次已消除的不确定性：确认 `_run_hours()` 已经只在入口计算 active model，但 RHS 内仍有每步布尔 mask 参数 gather；确认 ASM active compute indices 与 filtered params 可在 tensor 构建阶段稳定预计算；确认这不需要改变 solver、output grid、schema/API、worker strict mode 或 fallback。
- 本次采用的关键假设：ASM 节点 active compute set 在一次 `MaterialBalanceInput` tensor payload 生命周期内稳定；真实状态 `y` 仍必须在 RHS 每步按 indices gather；本切片不声明 PR-11 全统一 RHS 完成，也不改变 ASM oxygen zeroing active compute scope、mixed dispatch semantics、single-model fallback、default no-clamp baseline 或 solver defaults。
- 剩余不确定性：PR-11 全统一 RHS、PR-12 输出投影、完整 PR-39 组分契约/错配报错、默认 solver 切换、真实 solver limit enforcement、worker fallback 删除与 Go API 性能项仍需独立切片；KPI-001/KPI-003 收益仍需按求解器拆分报告。
- 影响范围 / 风险控制方式：只改 core 内部 RHS 准备路径、focused tests、P-08 evidence metadata 和文档；不修改公共 schema/API、不改 worker/legacy backend 行为；通过 correctness-freeze、Phase 0 golden/baseline/profiling/hotpath 和 `pr-fast` 复核。
- 验证方式：已通过 `backend\.venv\Scripts\python -m pytest simulation_core\tests\test_material_balance_core_boundary.py -q`（61 passed）、`backend\.venv\Scripts\python -m pytest simulation_core\tests -q`（68 passed）、`backend\.venv\Scripts\python -m pytest docs\rebuild\simulation_core -q`（6 passed，1 skipped）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-simulation-core-correctness-freeze.ps1`（passed，0 hard violations，0 open gaps）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\performance-baseline-phase0.ps1`（passed，12 runs，0 hard violations，0 open gaps）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\performance-profiling-phase0.ps1`（passed，12 runs，0 hard violations，0 open gaps）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\performance-golden-phase0.ps1`（passed，12 full runs，7 micro goldens，0 hard violations，0 open gaps）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\performance-hotpath-prereview-phase0.ps1`（passed，0 hard violations，0 open gaps，JSON 包含 `asm-stable-reaction-runtime-precompute`）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1`（`pr-fast.json` status `passed`）和 `git diff --check -- .`（仅 LF-to-CRLF notices，无 whitespace error）。
- 是否更新 README：是，更新 `simulation_core/python/autowatersimu_simulation_core/material_balance/README.md` 与 `simulation_core/tests/README.md`，记录 ASM active reaction runtime precompute 维护契约和测试职责；同时更新 `scripts/ci/README.md` 记录 P-08 prereview completed candidate。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次是 v1.4/P-08 热路径低风险实现切片，不改变长期架构或公共契约。

## 06:31 - v1.4 checklist evidence calibration

- 用户目标：继续收口 `AutoWaterSimu_性能优化开发计划文档_v1.4.md` 的上线检查清单，避免已完成 evidence 项继续显示为开放，同时不误标高风险未来 PR。
- 涉及目录：`docs/rebuild/simulation_core/`、`tasks/`、`.ai/changes/`。
- 修改内容：将 v1.4 checklist 中已有机器证据支撑的项目标为完成：core-only/backend-dependent 测试物理拆分、`NodeData`/`EdgeData extra` 策略、default 纯传输分支 clamp 现状 golden、P-06 Go latency baseline、P-05 strict opt-in smoke / 灰度策略；保留 future PR 边界说明。
- 修改原因：current-state、audit 和 CI evidence 已证明这些项完成或进入维护态，继续保留 `[ ]` 会误导后续优化排序；但 backend full re-export、输入模型合同清理、完整 PR-39、solver 默认切换、PR-11/PR-12、Go follow-up、flag matrix 与 nightly fixed runner 仍未完成。
- 本次已消除的不确定性：确认 boundary audit 已覆盖 core-only/backend-dependent split 和 backend/core 现状；确认 input-contract audit 已覆盖 runtime Node/Edge extra=forbid 与 adapter compat/warn/strict；确认 correctness-freeze audit 已覆盖 default no-clamp baseline；确认 P-05/P-06 evidence 脚本当前可复跑通过。
- 本次采用的关键假设：checklist 可用 `[x]` 表示“当前 evidence-backed baseline 已完成，后续行为变更另开 PR”；本次不把未来 work 的存在理解为当前 baseline 未完成。
- 剩余不确定性：backend material_balance 全量 re-export / ASM/UDM helper cleanup、input model contract cleanup、完整反应组分契约、默认 solver 切换、UDM KPI-001/KPI-003 分求解器收益、PR-11 统一 RHS、PR-12 输出投影、Go keyset/claim 优化、flag 组合矩阵与 nightly fixed-runner 硬件指纹仍待独立切片。
- 影响范围 / 风险控制方式：只改 v1.4 checklist 和任务/变更记录；不修改 runtime、scripts、public contracts、worker defaults、Go API、solver defaults、fallback 或 README 长期契约。
- 验证方式：已通过 `backend\.venv\Scripts\python -m pytest docs\rebuild\simulation_core -q`（6 passed，1 skipped）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-simulation-core-boundary.ps1`（passed，0 hard violations，0 open gaps）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-simulation-core-input-contract.ps1`（passed，0 hard violations，0 open gaps）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-simulation-core-correctness-freeze.ps1`（passed，0 hard violations，0 open gaps）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\worker-adapter-strict-smoke.ps1`（passed，8/8 fixtures，0 hard violations，0 open gaps）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\performance-go-api-latency-phase0.ps1`（passed，0 hard violations，0 open gaps）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1`（`pr-fast.json` status `passed`）和 `git diff --check -- .`（仅 LF-to-CRLF notices，无 whitespace error）。
- 是否更新 README：否；本次只校准 v1.4 checklist 状态，不改变目录职责、公共接口或长期维护规则。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次不新增架构决策。

## 06:36 - PR-39 UDM component mismatch guard

- 用户目标：继续根据 v1.4 性能优化开发计划推进 PR-39 反应组分契约的可独立收口部分，先消除 UDM runtime 组分错配静默污染。
- 涉及目录：`simulation_core/python/autowatersimu_simulation_core/material_balance/`、`simulation_core/tests/`、`docs/rebuild/simulation_core/`、`docs/architecture/`、`tasks/`、`.ai/changes/`。
- 修改内容：UDM runtime payload 构建时显式声明的局部组分必须能通过同名或 `udm_variable_bindings` 映射到全局组分，否则抛出 `InvalidInputError`；`stoich` / `stoich_expr` 目标组分必须属于局部组分集，未知目标同样前置失败；新增 core-only boundary tests 覆盖有效 binding、无显式局部组分名的全局 fallback 兼容、缺失 binding 的局部组分错配、未知 `stoich` 目标和 `_convert_to_tensors()` guard 传播；同步 material_balance/tests README、current-state、v1.4 PR-39 状态、docs/rebuild README 和任务记录。
- 修改原因：PR-39 要求反应组分契约和错配非静默。当前 UDM runtime 在局部组分无法映射到全局组分时会按位置回退，`stoich` 中未知目标组分会被忽略为 0，可能让错误模型进入仿真热路径。
- 本次已消除的不确定性：确认 adapter 的全局组分顺序来自 `component_schema.components` 并写入 `original_flowchart_data.customParameters`；确认未显式声明 UDM 局部组分时仍可继续以全局组分名作为兼容 fallback；确认 UDM binding 正路径不受影响。
- 本次采用的关键假设：只把显式 UDM 局部组分/stoichiometry target 视为强契约；完整 ASM 组分契约、硬编码氧索引迁移、PR-4 写侧/索引冲突清理需要后续独立 PR；本次不改变 solver、output grid、schema/API、worker strict 默认值、fallback 或 Go API。
- 剩余不确定性：完整 PR-39 仍需 ASM `component_schema→模型组分` 映射和硬编码氧索引清理；PR-11 全统一 RHS、PR-12 输出投影、默认 solver 切换、UDM KPI-001/KPI-003 分求解器收益、worker fallback 删除与 Go API 性能项仍待独立切片。
- 影响范围 / 风险控制方式：只改 UDM runtime payload 构建期 guard、focused tests 和文档；保持没有显式 `udm_component_names` 的兼容 fallback；不修改公共 contract schema 或 worker/default 行为。
- 验证方式：已通过 `backend\.venv\Scripts\python -m pytest simulation_core\tests\test_material_balance_core_boundary.py -q`（65 passed）、`backend\.venv\Scripts\python -m pytest simulation_core\tests -q`（72 passed）、`backend\.venv\Scripts\python -m pytest docs\rebuild\simulation_core -q`（6 passed，1 skipped）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-simulation-core-boundary.ps1`（passed，0 hard violations，0 open gaps）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-simulation-core-input-contract.ps1`（passed，0 hard violations，0 open gaps）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-simulation-core-correctness-freeze.ps1`（passed，0 hard violations，0 open gaps）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\performance-golden-phase0.ps1`（passed，12 full runs，7 micro goldens，0 hard violations，0 open gaps）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\performance-hotpath-prereview-phase0.ps1`（passed，0 hard violations，0 open gaps）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1`（`pr-fast.json` status `passed`）和 `git diff --check -- .`（仅 LF-to-CRLF notices，无 whitespace error）。
- 是否更新 README：是，更新 `simulation_core/python/autowatersimu_simulation_core/material_balance/README.md`、`simulation_core/tests/README.md` 和 `docs/rebuild/simulation_core/README.md`，记录 UDM component mapping / stoich mismatch guard。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次是 PR-39 的局部 guard 实现，不新增长期架构决策。

## 06:46 - UDM solver bucket hotpath evidence

- 用户目标：继续根据 v1.4 性能优化开发计划收口 PR-7/8、KPI-001 与 KPI-003 的求解器维度 evidence，避免 UDM `.item()` 热路径优化完成后仍缺少分 solver 报告。
- 涉及目录：`scripts/ci/`、`docs/rebuild/simulation_core/`、`docs/architecture/`、`tasks/`、`.ai/changes/`。
- 修改内容：`performance_hotpath_prereview_phase0.py` 新增 `profiling_summary.udm_solver_bucket_breakdown`，按 `udm_single` / `mixed_asm_udm` 和 `scipy_solver` / `rk4` / `adaptive_heun` 汇总 `expression`、`item_device_sync`、`core_compute`、`ode_framework` 与 `expression_plus_item_sync_share_of_compute`；Markdown 报告新增 UDM solver bucket 表；同步 scripts/ci README、docs/rebuild README、current-state、v1.4 PR-7/8 当前状态与 checklist、任务记录。
- 修改原因：v1.4 要求证明 UDM RHS/evaluate_reaction 热路径无逐步 `.item()` 同步点，并按 `scipy_solver` 与 torch 原生求解器拆分 KPI-001/KPI-003 evidence。现有 profiling 已有原始数据，但 hotpath prereview 汇总没有按 UDM/solver 维度输出。
- 本次已消除的不确定性：确认 Phase 0 profiling 覆盖 `udm_single` 和 `mixed_asm_udm` 的三求解器矩阵；确认当前 hotpath prereview evidence 可从 profile categories 和 runtime timings 生成求解器分桶；确认本地生成 evidence 中三个 solver bucket 的 `item_device_sync_ms` 均为 0。
- 本次采用的关键假设：`rk4` 与 `adaptive_heun` 代表当前 torch-native solver buckets；本切片只补 evidence/report split，不声明新的 runtime speedup；真实 post-change 端到端收益、表达式引擎替换和默认 solver 切换仍需后续独立切片。
- 剩余不确定性：PR-9/10/25 表达式引擎替换是否需要进入仍待取舍；PR-11 全统一 RHS、PR-12 输出投影、完整 PR-39 ASM 组分契约、默认 solver 切换、worker fallback 删除、Go keyset/claim 优化、flag matrix 与 nightly fixed-runner policy 仍未完成。
- 影响范围 / 风险控制方式：只改 opt-in P-08 evidence 汇总脚本和文档；不修改 simulation runtime、solver 默认值、public schema/API、worker strict mode、fallback 或 Go API；checklist 仅标记已有 evidence-backed 项。
- 验证方式：已通过 `backend\.venv\Scripts\python -m pytest docs\rebuild\simulation_core -q`（6 passed，1 skipped）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\performance-hotpath-prereview-phase0.ps1`（passed，0 hard violations，0 open gaps）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1`（`pr-fast.json` status `passed`）和 `git diff --check -- .`（仅 LF-to-CRLF notices，无 whitespace error）。
- 是否更新 README：是，更新 `scripts/ci/README.md` 与 `docs/rebuild/simulation_core/README.md`，记录 UDM solver bucket breakdown evidence。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次是既有 P-08/PR-7/8 evidence 报告增强，不引入新架构决策。

## 06:53 - PR-36 default solver no-switch decision

- 用户目标：继续收口 v1.4 PR-36 的“默认求解器切换评估完成”开放项，在不改变运行时默认值的前提下给出可维护决策。
- 涉及目录：`.ai/decisions/`、`docs/rebuild/simulation_core/`、`docs/architecture/`、`tasks/`、`.ai/changes/`。
- 修改内容：新增 ADR 0016，决定当前继续保持 `CalculationParameters.solver_method` 默认值为 `scipy_solver`；`rk4` 与 `adaptive_heun` 保持为验收/性能矩阵求解器，`dopri5` 继续不进入公共白名单；更新 current-state、v1.4 PR-36 状态与 checklist、docs/rebuild README 和任务记录。
- 修改原因：v1.4 checklist 要求默认求解器切换评估完成。现有 Phase 0 evidence 已足够做保守决策：12 个 full-run goldens 覆盖三求解器矩阵并通过，但不同 solver 输出不是 bit-identical，core-only timings 没有稳定默认切换收益，worker baseline wall time 又受启动/import 主导，不能单独作为默认值依据。
- 本次已消除的不确定性：确认当前不应把 `rk4` 或 `adaptive_heun` 提升为默认；确认 `dopri5` 仍需单独 enablement 决策；确认默认值切换属于行为变更，需要 flag/rollout、golden/evidence refresh、兼容说明和 ADR 更新。
- 本次采用的关键假设：当前 Phase 0 golden/baseline evidence 足以支持“不切换”的保守决策；未来若有新的长仿真/生产 workload evidence，可用新 ADR 修订默认 solver 策略；本切片不改变 runtime、schema、solver whitelist、tolerance、worker strict mode 或 fallback。
- 剩余不确定性：真实 solver limit enforcement、完整 PR-39 ASM 组分契约、PR-11 统一 RHS、PR-12 输出投影、PR-37 parity/f64 golden 最终覆盖、worker fallback 删除、Go 性能项、flag matrix 与 nightly fixed-runner policy 仍待独立切片。
- 影响范围 / 风险控制方式：只新增 ADR 和文档状态；不修改代码运行路径、不改变默认值、不扩大 public input whitelist；用 `pr-fast` 和 docs tests 复核。
- 验证方式：已通过 `backend\.venv\Scripts\python -m pytest docs\rebuild\simulation_core -q`（6 passed，1 skipped）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1`（`pr-fast.json` status `passed`）和 `git diff --check -- .`（含新增 ADR，仅 LF-to-CRLF notices，无 whitespace error）。
- 是否更新 README：是，更新 `docs/rebuild/simulation_core/README.md`，记录 ADR 0016 default solver no-switch decision 已完成。
- 是否需要新增或更新架构决策（.ai/decisions/）：是，新增 `.ai/decisions/0016-simulation-core-default-solver-policy.md`。

## 06:58 - PR-4 UDM index-conflict write-side guard

- 用户目标：继续收口 v1.4 / REQ-P0-004 的 UDM 映射 guard，修复 local→global 索引冲突可能污染写侧 scatter 的问题。
- 涉及目录：`simulation_core/python/autowatersimu_simulation_core/material_balance/`、`simulation_core/tests/`、`docs/rebuild/simulation_core/`、`docs/architecture/`、`tasks/`、`.ai/changes/`。
- 修改内容：`_resolve_local_to_global_indices()` 新增同节点 local→global 唯一性校验，两个 local 组分解析到同一 global 组分时抛出 `InvalidInputError`；新增 direct runtime payload 与 `_convert_to_tensors()` focused tests；同步 material_balance/tests README、current-state、v1.4 状态/checklist、docs/rebuild README 和任务记录。
- 修改原因：REQ-P0-004 要求显式 binding 冲突和写侧 `index_add_` 污染不能静默发生。上一切片已移除 positional fallback 并补 stoich/mapping mismatch guard，但仍缺少两个 local component 映射到同一 global index 的冲突检测。
- 本次已消除的不确定性：确认 UDM runtime 的读侧 env、fixed-mask 与写侧 `index_add_` 都使用 `_resolve_local_to_global_indices()` 产出的同一份索引；确认在构建期拒绝冲突比运行时数值修正更保守；确认无显式 `udm_component_names` 的 global fallback 仍保持唯一且兼容。
- 本次采用的关键假设：同一 UDM node 内 local 组分写入同一 global 组分应视为输入契约错误，而不是把反应项相加；本切片只完成 UDM PR-4 index-conflict/write-side guard，不代表完整 PR-39 ASM 组分契约、硬编码氧索引迁移或 PR-11 统一 RHS 完成。
- 剩余不确定性：完整 ASM component-schema→model 组分映射、硬编码氧索引清理、PR-11 统一 RHS、PR-12 输出投影、PR-37 parity/f64 golden 最终覆盖、simulation_core install/sys.path fallback 删除、backend 全 re-export/input cleanup、Go 性能项、flag matrix 与 nightly fixed-runner policy 仍待独立切片。
- 影响范围 / 风险控制方式：只改 UDM runtime payload 构建期 guard、focused tests 和文档；不修改 valid binding 行为、不改变 solver/default/schema/API/worker/fallback；通过 focused/full core tests、docs tests、audit、golden/hotpath 和 `pr-fast` 复核。
- 验证方式：已通过 `backend\.venv\Scripts\python -m pytest simulation_core\tests\test_material_balance_core_boundary.py -q`（67 passed）、`backend\.venv\Scripts\python -m pytest simulation_core\tests -q`（74 passed）、`backend\.venv\Scripts\python -m pytest docs\rebuild\simulation_core -q`（6 passed，1 skipped）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-simulation-core-correctness-freeze.ps1`（passed，0 hard violations，0 open gaps）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\performance-golden-phase0.ps1`（passed，12 full runs，7 micro goldens，0 hard violations，0 open gaps）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\performance-hotpath-prereview-phase0.ps1`（passed，0 hard violations，0 open gaps）、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1`（`pr-fast.json` status `passed`）和 `git diff --check -- .`（仅 LF-to-CRLF notices，无 whitespace error）。
- 是否更新 README：是，更新 `simulation_core/python/autowatersimu_simulation_core/material_balance/README.md`、`simulation_core/tests/README.md` 和 `docs/rebuild/simulation_core/README.md`，记录 UDM local→global uniqueness/write-side guard。
- 是否需要新增或更新架构决策（.ai/decisions/）：否；本次是 REQ-P0-004 的局部 guard 实现，不新增长期架构决策。
