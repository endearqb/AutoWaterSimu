## 14:47 - Benchmark case promotion evidence helper split

- 用户目标：继续完成 Certainty/Elegance 剩余工作，按用户对近期循环风险的判断推进真实 Go domain package movement，避免新增 evidence/hosted wrapper 或同 package 文件整理。
- 涉及目录：`apps/api/`、`apps/api/internal/compute/`、`apps/api/internal/domain/`、`apps/api/internal/domain/models/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`、`.ai/changes/`。
- 修改内容：新增 `domain/models` 的 `BenchmarkCasePromotionBenchmarkRun`、`BenchmarkCasePromotionEvidenceInput`、`BenchmarkCasePromotionEvidence` 和 `EvaluateBenchmarkCasePromotionEvidence`；`ModelGovernanceService.benchmarkCasePromotionResult` 在 compute 完成 benchmark_run 查询与 model_run lookup 后调用 domain helper，再适配回现有 `BenchmarkCasePromotionResult`；补充 domain 直测；同步 API/internal/domain/models/compute README、architecture/current-state、architecture/compute-api、Certainty/Elegance checklist 和任务记录。
- 修改原因：单个 benchmark case 的 latest benchmark_run/model_run attribution、evidence ref count、identity/hash blocker 和 readiness 判定已经稳定且 DTO-neutral，适合从 compute compatibility package 迁入 `domain/models`；compute 仍应保留 benchmark query orchestration、job-scoped evidence filter、model_run store lookup、plan DTO mapping、HTTP mapping 和持久化边界。
- 本次已消除的不确定性：确认 promotion plan 的 store 查询、authorized `job_id` evidence filter、model_run not-found error mapping、plan DTO 和 HTTP surface 仍需要留在 compute；确认可在不改 OpenAPI、contracts、migrations、generated client、auth scope 或 public service signature 的前提下迁移 per-case 判定。
- 本次采用的关键假设：本阶段只迁移单个 benchmark case promotion evidence/result projection，不迁移完整 model governance package、promotion workflow orchestration、catalog selection/scope gate、benchmark_run registration workflow、persistence implementation、OpenAPI、contracts、migrations、generated clients、auth scopes 或 public constructor signatures。
- 剩余不确定性：完整 model governance package movement、完整 jobs/artifacts/evidence/simulation/agent package movement、full all-mutation audit、全对象 data scope、剩余 mutation data-scope、OIDC/RBAC、hosted green evidence、release artifact hosted round trip、legacy authenticated browser session 与 8 golden scenarios complete 仍是后续工作。
- 影响范围 / 风险控制方式：仅改 DTO-neutral per-case promotion evidence helper、compute adapter 调用、domain tests 和长期上下文；不改 promotion plan/promote-approved HTTP 行为、不改存储 schema、不改 OpenAPI；不触碰用户未跟踪 `docs/rebuild/` 文档。
- 验证方式：`go test ./internal/domain/models -run "TestEvaluateBenchmarkCasePromotionEvidence" -count=1`；`go test ./internal/compute -run "TestDefaultParameterSetPromotionPlanEndpoint" -count=1`；`go test ./...`（`apps/api`）；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1`；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1`；精确 stale-text scan 无旧边界短语命中；`rg -n "[ \t]+$" ...` 针对本阶段文件无尾随空白；`git diff --check -- apps/api docs/architecture docs/rebuild tasks .ai` 通过，diff-check 仅报告既有 LF/CRLF 工作区提示。
- 是否更新 README：是，更新 `apps/api/README.md`、`apps/api/internal/README.md`、`apps/api/internal/compute/README.md`、`apps/api/internal/domain/README.md` 和 `apps/api/internal/domain/models/README.md` 以反映新 package boundary。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本次延续既有 modular-monolith/domain package movement，不新增长期架构决策。

## 09:09 - simulation_core 性能优化计划 v1.3 文档补充

- 用户目标：把新增 4 项 simulation_core 审查建议补充进入性能优化开发计划文档，并把文档版本和文件名更新为 v1.3。
- 涉及目录：外部个人文档目录 `D:\BaiduSyncdisk\Skill-workspace\personal`；仓库记录目录 `.ai/changes/`。
- 修改内容：将 `AutoWaterSimu_性能优化开发计划文档_v1.2.md` 重命名为 `AutoWaterSimu_性能优化开发计划文档_v1.3.md`；更新标题、版本、修订日期、阶段路线图、PR-29/37/7/8/11/12 的说明、依赖约束、DoD、Sprint、开发注意事项和上线检查清单。
- 修改原因：此前 4 项审查建议已确认可靠，但计划文档中对 core-only 测试拆分、UDM RHS 同步点、default clamp golden、统一 RHS 与混合调度绑定的验收表达不够硬。
- 本次已消除的不确定性：确认这是计划文档更新而非代码实现；确认 4 项分别归属 PR-29/37、PR-7/8、PR-12、PR-11/38。
- 本次采用的关键假设：用户所说“优化计划文档”指外部 `AutoWaterSimu_性能优化开发计划文档_v1.2.md`，不同时修改需求文档 v1.2。
- 剩余不确定性：需求文档是否也需要升级到 v1.3 需用户另行确认；本次未生成新的 golden 或 CI lane。
- 影响范围 / 风险控制方式：仅修改文档和 README First 变更记录；不修改 simulation_core、backend、worker、Go API 或测试代码。
- 验证方式：文本检查确认 v1.3 文件存在、v1.2 文件已不存在，关键补充项已进入计划；未运行代码测试，因为本次未改代码。
- 是否更新 README：否，未改变仓库目录职责或长期维护契约。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本次未产生新的架构决策，只是把既有审查结论固化到计划 DoD。

## 09:20 - Jobs create idempotency decision package split

- 用户目标：继续完成 Certainty/Elegance 质量计划中的真实 Go domain package movement，避免继续做 evidence wrapper 或同 package 文件拆分循环。
- 涉及目录：`apps/api/`、`apps/api/internal/compute/`、`apps/api/internal/domain/jobs/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`、`.ai/changes/`。
- 修改内容：新增 `domain/jobs` 的 create idempotency DTO-neutral decision；`JobLifecycleService.CreateJobForScope` 改为把 store 查到的 existing job 映射成 domain decision input，再把 domain decision 映射回现有 conflict/reused snapshot 行为；补充 domain 直测与 README/architecture/plan/todo 记录。
- 修改原因：job create duplicate/conflict 判定是稳定业务不变量，适合从 compute compatibility package 迁入 `domain/jobs`；compute 仍应保留 schema validation、payload hash、store lookup/write、HTTP/error mapping、audit envelope 和 snapshot assembly。
- 本次已消除的不确定性：确认 create idempotency 决策可独立于 compute `JobRecord`、HTTP、SQL、OpenAPI、audit envelope 和 schema validation；确认现有 compute regression 覆盖 duplicate reuse 与 conflict behavior。
- 本次采用的关键假设：本阶段只迁移纯 duplicate/conflict decision，不迁移 idempotency store lookup、payload hashing、conflict error code、response status、snapshot assembly 或 concrete persistence。
- 剩余不确定性：完整 jobs lifecycle package migration、public `Service` constructor narrowing、full all-mutation audit、全对象 data scope、OIDC/RBAC、hosted green evidence、release artifact hosted round trip、legacy authenticated browser session 与 8 golden scenarios 仍是后续工作。
- 影响范围 / 风险控制方式：仅改 jobs create path 的纯决策边界和文档；保持 create-job request/response、idempotency semantics、tenant/project/site data-scope、selected audit envelope shape、store interfaces、migrations、OpenAPI、generated clients 和 HTTP routes 不变；未触碰用户未跟踪文档。
- 验证方式：`go test ./internal/domain/jobs ./internal/compute -run "Test(NewQueuedJobProjectsCreateInput|CreateEventPlans|DecideCreateIdempotency|CreateJobIdempotencyDuplicateAndConflict|HTTPMutationAuditEventEnvelopeForJobCreate|HTTPJobCreateMutationTenantProjectSiteScope)$" -count=1`；`go test ./...`（`apps/api`）；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1`；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1`；`rg -n "schema_version|P0|P1|P2" docs\rebuild --glob "AutoWaterSimu_Next_*.md"`；`git diff --check -- apps/api docs tasks .ai`，均通过；diff-check 仅报告既有 LF/CRLF 工作区提示。
- 是否更新 README：是，更新 `apps/api/README.md`、`apps/api/internal/compute/README.md` 和 `apps/api/internal/domain/jobs/README.md` 以反映新 package boundary。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本次延续既有 modular-monolith/domain package movement，不新增长期架构决策。

## 09:35 - Jobs worker claim mutation plan package split

- 用户目标：继续完成 Certainty/Elegance 质量计划中的真实 Go domain package movement，避免继续做 evidence wrapper、hosted workflow entry 或同 package 文件拆分循环。
- 涉及目录：`apps/api/`、`apps/api/internal/compute/`、`apps/api/internal/domain/jobs/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`、`.ai/changes/`。
- 修改内容：新增 `domain/jobs` 的 DTO-neutral worker claim candidate ordering 与 running mutation plan；`MemoryStore.ClaimNext` 和 `PostgresStore.ClaimNext` 改为使用 domain mutation projection 后再应用到现有 concrete persistence / event insert；补充 domain 直测与 README/architecture/plan/todo 记录。
- 修改原因：worker claim 的 job-side running state projection 是稳定 job domain 规则，适合从 compute compatibility package 迁入 `domain/jobs`；compute 仍应保留 queue scanning、tenant/project/site scope filtering、SQL/locks、audit envelope assembly、HTTP behavior 和 concrete MemoryStore/PostgresStore persistence。
- 本次已消除的不确定性：确认 claim candidate ordering 与 running mutation projection 可独立于 compute `JobRecord`、SQL lock、HTTP、OpenAPI、audit envelope 与 store implementation；确认 MemoryStore/PostgresStore 可共用同一个 domain mutation plan。
- 本次采用的关键假设：本阶段只迁移 worker claim 的 candidate ordering 与 job running mutation planning，不迁移 worker lifecycle service、queue scanning/locking SQL、tenant/project/site scope filtering、event JSON audit envelope assembly、store interfaces、HTTP behavior 或 public service signatures。
- 剩余不确定性：完整 jobs lifecycle package migration、public `Service` constructor narrowing、full all-mutation audit、全对象 data scope、OIDC/RBAC、hosted green evidence、release artifact hosted round trip、legacy authenticated browser session 与 8 golden scenarios 仍是后续工作。
- 影响范围 / 风险控制方式：仅改 worker claim path 的纯规则边界和文档；保持 worker claim request/response、queue scan/lock behavior、tenant/project/site data-scope、selected audit envelope shape、store interfaces、migrations、OpenAPI、generated clients 和 HTTP routes 不变；未触碰用户未跟踪文档。
- 验证方式：`go test ./internal/domain/jobs ./internal/compute -run "Test(PreferClaimRecordOrdersByCreatedAtThenJobID|NewClaimMutationProjectsRunningState|WorkerLifecycleArtifactSucceedAndDownload|WorkerClaimSkipsCapabilityMismatch|WorkerClaimSkipsContractVersionMismatch|HTTPWorkerJobMutationAuditEvents|HTTPWorkerJobMutationTenantProjectSiteScope)$" -count=1`；`go test ./...`（`apps/api`）；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1`；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1`；`rg -n "schema_version|P0|P1|P2" docs\rebuild --glob "AutoWaterSimu_Next_*.md"`；`git diff --check -- apps/api docs tasks .ai`，均通过；diff-check 仅报告既有 LF/CRLF 工作区提示。
- 是否更新 README：是，更新 `apps/api/README.md`、`apps/api/internal/compute/README.md` 和 `apps/api/internal/domain/jobs/README.md` 以反映新 package boundary。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本次延续既有 modular-monolith/domain package movement，不新增长期架构决策。

## 09:44 - Jobs worker heartbeat mutation plan package split

- 用户目标：继续完成 Certainty/Elegance 质量计划中的真实 Go domain package movement，避免继续做 evidence wrapper、hosted workflow entry 或同 package 文件拆分循环。
- 涉及目录：`apps/api/`、`apps/api/internal/compute/`、`apps/api/internal/domain/jobs/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`、`.ai/changes/`。
- 修改内容：新增 `domain/jobs` 的 DTO-neutral worker heartbeat lease refresh mutation plan；`MemoryStore.Heartbeat` 和 `PostgresStore.Heartbeat` 改为使用 domain mutation projection 后再应用到现有 concrete persistence / event insert；补充 domain 直测与 README/architecture/plan/todo 记录。
- 修改原因：worker heartbeat 的 job-side lease refresh 判定是稳定 job domain 规则，适合从 compute compatibility package 迁入 `domain/jobs`；compute 仍应保留 worker table heartbeat/current_job_id 更新、row lock、SQL、tenant/project/site scope checks、audit envelope assembly、HTTP behavior 和 concrete MemoryStore/PostgresStore persistence。
- 本次已消除的不确定性：确认 heartbeat lease refresh 可独立于 compute `JobRecord`、SQL lock、HTTP、OpenAPI、audit envelope 与 store implementation 表达为纯 mutation plan；确认 MemoryStore/PostgresStore 可共用同一个 domain heartbeat mutation plan。
- 本次采用的关键假设：本阶段只迁移 worker heartbeat 的 running-job / assigned-worker lease refresh mutation planning，不迁移 worker lifecycle service、worker table update、row locking SQL、tenant/project/site scope filtering、event JSON audit envelope assembly、store interfaces、HTTP behavior 或 public service signatures。
- 剩余不确定性：完整 jobs lifecycle package migration、public `Service` constructor narrowing、full all-mutation audit、全对象 data scope、OIDC/RBAC、hosted green evidence、release artifact hosted round trip、legacy authenticated browser session 与 8 golden scenarios 仍是后续工作。
- 影响范围 / 风险控制方式：仅改 worker heartbeat path 的纯规则边界和文档；保持 worker heartbeat request/response、worker table update behavior、tenant/project/site data-scope、selected audit envelope shape、store interfaces、migrations、OpenAPI、generated clients 和 HTTP routes 不变；未触碰用户未跟踪文档。
- 验证方式：`go test ./internal/domain/jobs ./internal/compute -run "Test(PreferClaimRecordOrdersByCreatedAtThenJobID|NewClaimMutationProjectsRunningState|NewHeartbeatMutationProjectsLeaseRefresh|NewHeartbeatMutationSkipsNonRunningOrMismatchedWorker|WorkerLifecycleArtifactSucceedAndDownload|HTTPWorkerJobMutationAuditEvents|HTTPWorkerJobMutationTenantProjectSiteScope)$" -count=1`；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\security-smoke.ps1`；`go test ./...`（`apps/api`）；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1`；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1`；`rg -n "schema_version|P0|P1|P2" docs\rebuild --glob "AutoWaterSimu_Next_*.md"`；`git diff --check -- apps/api docs tasks .ai`，均通过；diff-check 仅报告既有 LF/CRLF 工作区提示。
- 是否更新 README：是，更新 `apps/api/README.md`、`apps/api/internal/compute/README.md` 和 `apps/api/internal/domain/jobs/README.md` 以反映新 package boundary。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本次延续既有 modular-monolith/domain package movement，不新增长期架构决策。

## 10:04 - simulation_core 性能优化需求与计划 v1.4 统一升级

- 用户目标：将性能优化需求文档与开发计划文档统一升级到 v1.4，并吸收 simulation_core 实跑复审新增结论。
- 涉及目录：外部个人文档目录 `D:\BaiduSyncdisk\Skill-workspace\personal`；仓库记录目录 `.ai/changes/`。
- 修改内容：将 `AutoWaterSimu_性能优化需求文档_v1.2.md` 重命名为 `AutoWaterSimu_性能优化需求文档_v1.4.md`；将 `AutoWaterSimu_性能优化开发计划文档_v1.3.md` 重命名为 `AutoWaterSimu_性能优化开发计划文档_v1.4.md`；补充 `_balance_param` 维度回归、ASM mask gather、表达式 fail-late 定性、`NodeData`/`EdgeData extra` 策略、mojibake 还原归档与 ASM 氧清零 compute_mask 约束。
- 修改原因：代码复审新增项均属于 simulation_core 的正确性、性能或维护性风险，需要进入需求与计划的验收边界，避免后续 PR 漏做。
- 本次已消除的不确定性：确认 v1.4 采用“不新增 PR 编号”的方式落地；确认 `_balance_param` 需要在需求侧独立为 REQ-P2-006，在计划侧并入 PR-33；确认表达式问题应表述为 fail-late 一致性问题而非 RCE。
- 本次采用的关键假设：旧 v1.1 增量稿与 v1 原始稿作为历史文件保留不动；本次只更新外部个人文档与 README First 变更记录，不修改 simulation_core 代码。
- 剩余不确定性：具体 PR 实施时仍需用代码级测试固化现状 repro 与目标 golden；`extra` 策略最终选择 `forbid` 还是 `ignore + log` 需在 PR-30 中结合存量审计决定。
- 影响范围 / 风险控制方式：仅修改文档与变更记录；不改运行时代码、不改公共接口、不删除历史增量稿。
- 验证方式：文本检查确认 v1.4 文件存在、旧 v1.2/v1.3 目标文件已被重命名，关键术语可检索；未运行代码测试，因为本次未修改代码。
- 是否更新 README：否，未改变仓库目录职责或长期维护契约。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本次是把复审结论纳入需求/计划,不新增架构决策。

## 10:04 - Evidence governance job-scoped model catalog selection

- 用户目标：根据用户对近期 `.ai/changes` 的判断，避免继续做 evidence/hosted wrapper 循环，推进一个明确、可测试的 object data-scope 切片。
- 涉及目录：`apps/api/`、`apps/api/internal/compute/`、`scripts/ci/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`、`.ai/changes/`。
- 修改内容：`EvidenceGovernanceService` 的 model catalog resolver 改为接受 `ModelCatalogSnapshotFilter`；`EvidencePackage` / `ProductionReadiness` 在计算 governance 时用 source job 的 tenant/project/site 选择匹配 persisted model catalog，再按现有 fallback 逻辑回退；新增 `TestEvidenceGovernanceUsesJobScopedModelCatalog`；将该回归纳入 `security-smoke.ps1` 与 evidence summary；更新 Compute API/architecture/Certainty-Elegance 文档和任务复盘。
- 修改原因：此前 job 路由本身已有 scoped read guard，但 evidence governance 的 model catalog 依据仍使用全局 latest catalog resolver，存在 scoped job 读取另一个 scope 最新 catalog 作为 production governance 判据的风险。
- 本次已消除的不确定性：确认 `http_jobs.go` 的 result/events/evidence/readiness/evidence-ref 子路由已经先经 `job:read` route-level data-scope guard；确认真正缺口是 evidence governance catalog selection，而不是新增一个 evidence wrapper。
- 本次采用的关键假设：本阶段只收紧 evidence package / production-readiness 的 model catalog selection，不改变 HTTP paths、auth scopes、OpenAPI、schema、migrations、generated clients、public `Service` signatures、model catalog persistence 或 evidence DTO shape。
- 剩余不确定性：完整 object-level data-scope、remaining mutation data-scope、OIDC/RBAC、complete all-mutation audit、hosted green evidence、release artifact hosted round trip、legacy authenticated session 和 8 golden scenarios complete 仍是后续工作。
- 影响范围 / 风险控制方式：仅改 evidence governance read-only catalog resolver wiring、测试、security smoke 与长期上下文；服务仍不 mutate jobs/model catalogs/artifacts/approval state；保留 unscoped job 使用 unscoped latest / built-in fallback 行为；未触碰用户未跟踪文档。
- 验证方式：`go test ./internal/compute -run "TestEvidenceGovernanceUsesJobScopedModelCatalog|TestProductionReadinessBlocksHighRiskFindings" -count=1`；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\security-smoke.ps1`（更新脚本后重跑通过）；`go test ./...`（`apps/api`）；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1`；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1`；`rg -n "schema_version|P0|P1|P2" docs\rebuild --glob "AutoWaterSimu_Next_*.md"`；`git diff --check -- apps/api scripts docs tasks .ai`，均通过；diff-check 仅报告既有 LF/CRLF 工作区提示。
- 是否更新 README：是，更新 `apps/api/README.md` 和 `apps/api/internal/compute/README.md`，明确 evidence governance model catalog selection 的 job-scoped read boundary。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本次是已确立 static-token data-scope 策略下的局部实现收敛，不新增长期架构决策。
- 提交状态：已提交并推送到 `codex/autowatersimu-next-rebuild`。

## 10:16 - Evidence-ref process graph object-scope

- 用户目标：继续避免 evidence wrapper / hosted entry 循环，推进一个明确的 evidence-ref object data-scope 切片。
- 涉及目录：`apps/api/`、`apps/api/internal/compute/`、`scripts/ci/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`、`.ai/changes/`。
- 修改内容：`EvidenceGovernanceService.ResolveEvidenceReference` 在解析 `process_graph:<id>` 时会比较 stored process graph tenant/project/site 与 source job tenant/project/site，不匹配时返回 evidence ref not found；embedded `simulation_input:<id>` payload 若显式 metadata scope 与 source job 冲突也不解析；新增 `TestProcessGraphEvidenceReferenceHonorsJobObjectScope`，并纳入 `security-smoke.ps1` 与 evidence summary；更新 Compute API/architecture/Certainty-Elegance 文档和任务记录。
- 修改原因：HTTP evidence-ref 子路由已先做 job route data-scope guard，但 stored `process_graph:<id>` 再 dereference 到 registry 对象时仍需要按 source job 边界防止跨 scope 对象被包装成同一 job 的证据。
- 本次已消除的不确定性：确认 `ResolveEvidenceReference` 现有 model_run/artifact already job-bound；确认缺口集中在 process graph registry dereference；确认 empty metadata process graph 仍需保留 legacy/global 兼容。
- 本次采用的关键假设：本阶段只收紧 evidence-ref 对象解析边界；不改变 HTTP paths、auth scopes、OpenAPI、schema、migrations、generated clients、public `Service` signatures、process graph registry persistence 或 evidence-ref DTO shape。
- 剩余不确定性：完整 object-level data-scope、remaining mutation data-scope、OIDC/RBAC、complete all-mutation audit、hosted green evidence、release artifact hosted round trip、legacy authenticated session 和 8 golden scenarios complete 仍是后续工作。
- 影响范围 / 风险控制方式：仅改 read-only evidence-ref resolver、测试、security smoke 与长期上下文；跨 scope ref 返回 404 避免暴露对象存在；保留 unscoped stored process graph 兼容；未触碰用户未跟踪文档。
- 验证方式：`go test ./internal/compute -run "Test(ProcessGraphEvidenceReference|NewSystemEvidenceReferenceE2E)" -count=1`；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\security-smoke.ps1`；`go test ./...`（`apps/api`）；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1`；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1`；`rg -n "schema_version|P0|P1|P2" docs\rebuild --glob "AutoWaterSimu_Next_*.md"`；`git diff --check -- apps/api scripts docs tasks .ai`，均通过；diff-check 仅报告既有 LF/CRLF 工作区提示。
- 是否更新 README：是，更新 `apps/api/README.md` 和 `apps/api/internal/compute/README.md`，明确 evidence-ref process graph object-scope 边界。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本次是既有 static-token data-scope 策略下的局部实现收敛，不新增长期架构决策。

## 10:36 - Service simulation test split

- 用户目标：先拆 `service_test.go`，解决当前最大代码维护性问题；在当前仓库已无单体 `service_test.go` 的前提下，继续拆残留偏大的 service 测试聚合点。
- 涉及目录：`apps/api/internal/compute/`、`docs/architecture/`、`tasks/`、`.ai/changes/`。
- 修改内容：将 400 行单函数 `TestSimulationCheckEndpointCreatesComputeJob` 拆成主路径、worker token 拒绝、missing refs、process graph、model-run replay、registered simulation input 和 ASM/UDM registered input 多个 focused tests；新增 simulation 测试 HTTP/fixture helper；更新 `docs/architecture/compute-api.md` 中仍引用已删除 `service_test.go` 的测试文件 evidence 表。
- 修改原因：单体 `service_test.go` 已在前序阶段删除，但 `service_simulation_test.go` 仍把多类 simulation-check 行为串在一个长测试里，定位失败点和 review 成本偏高。
- 本次已消除的不确定性：确认当前工作区没有 `apps/api/internal/compute/service_test.go`；确认最大残留 service 测试聚合点是 `service_simulation_test.go`；确认可在同 package 内拆分测试和 helper，不需要修改生产代码、schema、OpenAPI、generated client、migration、auth scope 或 HTTP route。
- 本次采用的关键假设：用户的“拆 service_test.go”在当前仓库状态下等价于继续拆 service 测试聚合文件；本阶段只做测试组织维护，保留原测试语义和 `TestSimulationCheckEndpointCreatesComputeJob` focused-test 入口；上一轮已存在但未提交的 evidence/domain 工作不纳入本次提交。
- 剩余不确定性：`service_test_helpers_test.go`、`service_model_runs_test.go`、`service_contracts_test.go` 和 `service_simulation_registry_test.go` 仍偏大，后续可继续按 helper/domain 子主题拆；完整 jobs/artifacts/models/evidence/simulation/agent package movement、remaining mutation data-scope、full object-level data-scope、remaining all-mutation audit、OIDC/RBAC、hosted green evidence、release artifact hosted round trip、legacy authenticated session 和 complete golden scenarios 仍未完成。
- 影响范围 / 风险控制方式：仅拆分测试文件和架构文档中过期测试文件清单；不改生产行为、不改公共接口、不改 schema/migration/client；不触碰用户未跟踪文档。
- 验证方式：`go test ./internal/compute -run "TestSimulationCheckEndpoint(CreatesComputeJob|RejectsWorkerToken|RejectsUnregisteredInputRefs|CreatesJobFromProcessGraph|CreatesJobFromModelRunReplay|CreatesJobFromRegisteredSimulationInput|CreatesJobsForRegisteredModelInputs)$" -count=1`；`go test ./...`（`apps/api`）；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1`；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1`；`rg -n "[ \t]+$" ...` 针对本次文件无尾随空白；`git diff --check -- docs\architecture\compute-api.md tasks\todo.md .ai\changes\2026-06-13.md apps\api\internal\compute\service_simulation_test.go`，均通过；diff-check 仅报告既有 LF/CRLF 工作区提示。
- 是否更新 README：否；`apps/api/internal/compute/README.md` 已用 `service_*_test.go` wildcard 描述 service/lifecycle regression tests，本次仍落在该约定内。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本次是测试组织维护，不产生新的架构决策。

## 10:47 - Evidence object-scope matching domain helper split

- 用户目标：继续完成 Certainty/Elegance 剩余工作，在 service test 拆分后推进一个真实 package-boundary movement，避免继续新增 evidence wrapper、hosted workflow entry 或同 package 文件整理。
- 涉及目录：`apps/api/`、`apps/api/internal/compute/`、`apps/api/internal/domain/`、`apps/api/internal/domain/evidence/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`、`.ai/changes/`。
- 修改内容：将 evidence object tenant/project/site matching 的中立规则迁入 `apps/api/internal/domain/evidence`，新增 `ObjectScope`、`ObjectScopeFromMetadata`、`PayloadScopeMatchesSource` 和 `ObjectScopeMatchesSource`；`EvidenceGovernanceService` 改为把 `JobRecord` / `ProcessGraphRecord` 适配成 domain scope 后调用 domain helper；补充 domain 直测与 README/architecture/Certainty-Elegance/todo 记录。
- 修改原因：上一阶段已落地 process graph evidence-ref source-job object-scope 行为，但匹配规则仍留在 compute compatibility package；该规则已稳定且 DTO-neutral，适合迁入 `domain/evidence`，compute 继续负责 store-backed evidence-ref workflow 和记录适配。
- 本次已消除的不确定性：确认 object-scope matching 可独立于 compute `JobRecord`、`ProcessGraphRecord`、store lookup、HTTP/DTO、OpenAPI、schema、migration 和 public `Service` signatures 表达；确认 empty target scope 继续保持 legacy/global compatible。
- 本次采用的关键假设：本阶段只迁移 object-scope matching helper，不改变上一阶段 evidence-ref 404 行为、HTTP paths、auth scopes、schemas、generated clients、migrations、evidence-ref response shape、store interfaces、job-scoped model catalog selection 或 public service signatures。
- 剩余不确定性：完整 evidence governance package movement、完整 jobs/artifacts/models/evidence/simulation/agent package movement、full all-mutation audit、全对象 data scope、剩余 mutation data-scope、OIDC/RBAC、hosted green evidence、release artifact hosted round trip、legacy authenticated browser session 与 8 golden scenarios complete 仍是后续工作。
- 影响范围 / 风险控制方式：仅改 DTO-neutral matching helper、compute adapter 调用、domain tests 和长期上下文；未引入新依赖、不改公共接口、不改持久化结构、不触碰用户未跟踪文档。
- 验证方式：`go test ./internal/domain/evidence ./internal/compute -run "Test(ObjectScopeMatchesSource|PayloadScopeMatchesSource|ProcessGraphEvidenceReferenceHonorsJobObjectScope|ProcessGraphEvidenceReference|NewSystemEvidenceReferenceE2E)$" -count=1`；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\security-smoke.ps1`；`go test ./...`（`apps/api`）；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1`；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1`；`rg -n "schema_version|P0|P1|P2" docs\rebuild --glob "AutoWaterSimu_Next_*.md"`；`rg -n "[ \t]+$" ...` 针对本阶段文件无尾随空白；`git diff --check -- apps/api docs/architecture docs/rebuild tasks .ai`，均通过；diff-check 仅报告既有 LF/CRLF 工作区提示。
- 是否更新 README：是，更新 `apps/api/README.md`、`apps/api/internal/compute/README.md`、`apps/api/internal/domain/README.md` 和 `apps/api/internal/domain/evidence/README.md` 以反映新 package boundary。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本次延续既有 modular-monolith/domain package movement，不新增长期架构决策。

## 11:00 - Artifact archive metadata projection domain helper split

- 用户目标：继续完成 Certainty/Elegance 剩余工作，优先推进真实 Go domain package movement，避免新增 evidence wrapper、hosted workflow entry 或同 package 文件整理。
- 涉及目录：`apps/api/`、`apps/api/internal/compute/`、`apps/api/internal/domain/`、`apps/api/internal/domain/artifacts/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`、`.ai/changes/`。
- 修改内容：新增 `domain/artifacts` 的 `ArchiveRecordInput`、`ArchiveRecord`、`ArchiveStatusArchived` 和 `NewArchiveRecord`；`ArtifactLifecycleService.archiveArtifact` 在完成 copy/checksum verification 后用 domain helper 投影 archive metadata，再适配回现有 `ArtifactArchiveRecord`；补充 domain 直测；同步 API/domain/compute/architecture/Certainty-Elegance 文档；修正 `apps/api/README.md` 中 scoped model catalog promotion plan/promote-approved 仍写“暂时不能调用”的过时说明。
- 修改原因：archive metadata record shape 与 retention policy metadata 已稳定，适合从 compute compatibility package 迁入 `domain/artifacts`；compute 仍应保留 object/archive store execution、checksum verification、metadata persistence、audit envelope assembly、hot object deletion、download fallback 和 HTTP mapping。
- 本次已消除的不确定性：确认 ADR 0010 的安全顺序不需要改变；确认 archive metadata projection 可独立于 compute `ArtifactArchiveRecord`、object store、PostgreSQL/MemoryStore、audit envelope、HTTP response 和 public `Service` signatures 表达；确认 scoped promotion plan/promote-approved 代码和 smoke 已要求 authorized `job_id`，README 后段是过时信息。
- 本次采用的关键假设：本阶段只迁移 archive metadata projection，不迁移完整 artifact lifecycle service、archive backend、checksum verification、archive audit event、metadata store、download fallback、HTTP route、OpenAPI、schema、migration、generated client 或 public constructor signatures。
- 剩余不确定性：完整 artifact lifecycle package migration、完整 jobs/models/evidence/simulation/agent package movement、full all-mutation audit、全对象 data scope、剩余 mutation data-scope、OIDC/RBAC、hosted green evidence、release artifact hosted round trip、legacy authenticated browser session 与 8 golden scenarios complete 仍是后续工作。
- 影响范围 / 风险控制方式：仅改 DTO-neutral archive metadata projection helper、compute adapter 调用、domain tests 和长期上下文；不改 archive safety order、不改 retention sweep response shape、不改 persisted schema、不改 HTTP/OpenAPI；未触碰用户未跟踪文档。
- 验证方式：`go test ./internal/domain/artifacts ./internal/compute -run "Test(NewArchiveRecordProjectsArchivedMetadata|ArtifactRetentionSweepArchivesCandidateWithConfiguredBackend|ArtifactRetentionSweepDeletesOnlyUnreferencedExpiredTTL|ArtifactRetentionSweepSkipsArchiveCandidateWithoutBackend)$" -count=1`；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\security-smoke.ps1`；`go test ./...`（`apps/api`）；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1`；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1`；`rg -n "schema_version|P0|P1|P2" docs\rebuild --glob "AutoWaterSimu_Next_*.md"`；`rg -n "scoped token 暂时不能调用|Artifacts retention action planner|artifact retention policy helpers and sweep action planner|artifact retention policy parsing, candidate checks, and sweep action planning" apps\api docs\architecture docs\rebuild` 确认 stale promotion 文案已清除，唯一命中为计划清单中的 retention action planner 已完成项；`rg -n "[ \t]+$" ...` 针对本阶段文件无尾随空白；`git diff --check -- apps/api docs/architecture docs/rebuild tasks .ai`，均通过；diff-check 仅报告既有 LF/CRLF 工作区提示。
- 是否更新 README：是，更新 `apps/api/README.md`、`apps/api/internal/README.md`、`apps/api/internal/compute/README.md`、`apps/api/internal/domain/README.md` 和 `apps/api/internal/domain/artifacts/README.md` 以反映新 package boundary，并修正 scoped promotion 文档漂移。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本次延续 ADR 0010 和既有 modular-monolith/domain package movement，不新增长期架构决策。

## 11:17 - Artifact upload metadata projection domain helper split

- 用户目标：继续完成 Certainty/Elegance 剩余工作，优先推进真实 Go domain package movement，避免新增 evidence/hosted wrapper 或同 package 文件整理。
- 涉及目录：`apps/api/`、`apps/api/internal/compute/`、`apps/api/internal/domain/`、`apps/api/internal/domain/artifacts/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`、`.ai/changes/`。
- 修改内容：新增 `domain/artifacts` 的 `DefaultArtifactContentType`、`ArtifactRecordInput`、`ArtifactRecord` 和 `NewArtifactRecord`；`ArtifactLifecycleService.UploadArtifact` 在完成 job/worker 校验、metadata decode、checksum comparison、server object key 生成和 object store write 后用 domain helper 投影 upload artifact metadata，再适配回现有 `ArtifactRecord`；补充 domain 直测；同步 API/domain/compute/architecture/Certainty-Elegance 文档。
- 修改原因：artifact upload metadata record shape、默认 content type 与 retention metadata 映射已稳定，适合从 compute compatibility package 迁入 `domain/artifacts`；compute 仍应保留 file read、checksum verification、object key/storage provider selection、object store write、metadata persistence、selected audit event assembly 和 HTTP mapping。
- 本次已消除的不确定性：确认 upload metadata projection 可独立于 compute `ArtifactRecord`、object store、metadata store、selected audit event、HTTP response、OpenAPI/schema/migration/generated client 和 public `Service` signatures 表达；确认默认 content type 行为仍保持 `application/json`。
- 本次采用的关键假设：本阶段只迁移 upload metadata record projection，不迁移完整 artifact lifecycle service、object/archive backends、checksum verification、audit event call site、metadata store、download fallback、HTTP route、OpenAPI、schema、migration、generated client 或 public constructor signatures。
- 剩余不确定性：完整 artifact lifecycle package migration、完整 jobs/models/evidence/simulation/agent package movement、full all-mutation audit、全对象 data scope、剩余 mutation data-scope、OIDC/RBAC、hosted green evidence、release artifact hosted round trip、legacy authenticated browser session 与 8 golden scenarios complete 仍是后续工作。
- 影响范围 / 风险控制方式：仅改 DTO-neutral upload metadata projection helper、compute adapter 调用、domain tests 和长期上下文；不改 worker upload state checks、不改 object key 约定、不改 persisted schema、不改 HTTP/OpenAPI；未触碰用户未跟踪文档或未跟踪 `docs/rebuild/simulation_core/`。
- 验证方式：`go test ./internal/domain/artifacts ./internal/compute -run "Test(NewArtifactRecord|HTTPArtifactUploadMultipart|WorkerLifecycleArtifactSucceedAndDownload|HTTPArtifactDownloadTenantProjectSiteScope)$" -count=1`（compute package 通过，domain regex 未命中后补跑）；`go test ./internal/domain/artifacts -run TestNewArtifactRecord -count=1`；`go test ./internal/compute -run "Test(HTTPArtifact|ArtifactRetention|WorkerLifecycleArtifact|HTTPWorkerJobMutation)" -count=1`；`go test ./...`（`apps/api`）；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\security-smoke.ps1`；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1`；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1`；`rg -n "schema_version|P0|P1|P2" docs\rebuild --glob "AutoWaterSimu_Next_*.md"`；`rg -n "artifact retention policy helpers, sweep action planner, and archive metadata projection|Artifact retention policy parsing, candidate checks, sweep action planning, and archive metadata projection|stable retention policy/action planner and archive metadata projection|artifact retention policy parsing, candidate checks, sweep action planning, and archive metadata projection" apps\api docs\architecture docs\rebuild` 无命中；`rg -n "[ \t]+$" ...` 针对本阶段文件无尾随空白；`git diff --check -- apps/api docs/architecture docs/rebuild tasks .ai` 通过；diff-check 仅报告既有 LF/CRLF 工作区提示。
- 是否更新 README：是，更新 `apps/api/README.md`、`apps/api/internal/README.md`、`apps/api/internal/compute/README.md`、`apps/api/internal/domain/README.md` 和 `apps/api/internal/domain/artifacts/README.md` 以反映新 package boundary。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本次延续既有 modular-monolith/domain package movement，不新增长期架构决策。

## 11:36 - Service test helper split

- 用户目标：先拆 `service_test.go`，解决当前最大代码维护性问题；在当前仓库已无单体 `service_test.go` 的前提下，继续拆残留偏大的 service 测试 helper 聚合点。
- 涉及目录：`apps/api/internal/compute/`、`tasks/`、`.ai/changes/`。
- 修改内容：将 `service_test_helpers_test.go` 中混合的 worker/job/simulation/draft/model/audit/artifact/payload helper 按职责拆到 `service_worker_fixtures_test.go`、`service_job_fixtures_test.go`、`service_simulation_fixtures_test.go`、`service_contracts_fixtures_test.go`、`service_model_catalog_fixtures_test.go`、`service_audit_helpers_test.go`、`service_artifact_helpers_test.go` 和 `service_payload_assertions_test.go`；保留原文件只承载 service 构造、JSON helper、字符串 helper 与 repo root lookup。
- 修改原因：`service_test_helpers_test.go` 已成为剩余最大的混合 service 测试文件，多个领域 fixture 聚在一起会增加后续定位、review 和拆测试成本。
- 本次已消除的不确定性：确认当前工作区没有 `apps/api/internal/compute/service_test.go`；确认最大残留混合 helper 文件是 `service_test_helpers_test.go`；确认可通过移动完整 helper 函数块完成，不需要修改测试断言、生产代码、schema、OpenAPI、generated client、migration、auth scope 或 HTTP route。
- 本次采用的关键假设：用户的“拆 service_test.go”在当前仓库状态下等价于继续拆 service 测试聚合文件；本阶段只做测试 helper 组织维护，保留所有 helper 名、测试名和行为；不触碰未跟踪 `docs/rebuild/` 文档。
- 剩余不确定性：`service_model_runs_test.go` 仍是一个 379 行单测试聚合点，`service_contracts_test.go` 与 `service_simulation_registry_test.go` 也仍偏大；完整 jobs/artifacts/models/evidence/simulation/agent package movement、remaining mutation data-scope、full object-level data-scope、remaining all-mutation audit、OIDC/RBAC、hosted green evidence、release artifact hosted round trip、legacy authenticated session 和 complete golden scenarios 仍未完成。
- 影响范围 / 风险控制方式：仅拆分同 package `_test.go` helper 文件；不改生产行为、不改公共接口、不改持久化结构；新增 helper 文件仍落在 compute README 的 `service_*_test.go` 测试组织约定内，因此不更新 README。
- 验证方式：`go test ./internal/compute -count=1`；`go test ./...`（`apps/api`）；`rg -n "[ \\t]+$" ...` 针对本阶段文件无尾随空白；`git diff --check -- apps\api\internal\compute\service_test_helpers_test.go ...` 通过，diff-check 仅报告既有 LF/CRLF 工作区提示。
- 是否更新 README：否；`apps/api/internal/compute/README.md` 已用 `service_*_test.go` wildcard 描述 service/lifecycle regression tests，本次新增文件仍落在该约定下，且为纯测试 helper 拆分。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本次是测试组织维护，不产生新的架构决策。

## 11:53 - Model catalog snapshot record data projection domain helper split

- 用户目标：继续完成 Certainty/Elegance 剩余工作，在 service test helper 拆分后推进真实 Go domain package movement，避免新增 evidence/hosted wrapper 或同 package 文件整理。
- 涉及目录：`apps/api/`、`apps/api/internal/compute/`、`apps/api/internal/domain/`、`apps/api/internal/domain/models/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`、`.ai/changes/`。
- 修改内容：新增 `domain/models` 的 `ModelCatalogSnapshotRecordDataInput`、`ModelCatalogSnapshotRecordData` 和 `ModelCatalogSnapshotRecordDataFromDocument`；`ModelGovernanceService.modelCatalogRecord` 在 compute schema validation 后使用 domain helper 投影 snapshot record data，再适配回现有 `ModelCatalogRecord`；补充 domain 直测；同步 API/internal/domain/models/compute README、architecture/current-state、architecture/compute-api、Certainty/Elegance checklist 和任务复盘。
- 修改原因：persisted model catalog snapshot 的 record 字段投影、payload hash、metadata scope、source/requested_by 默认值与 required-field error semantics 已稳定且 DTO-neutral，适合从 compute compatibility package 迁入 `domain/models`；compute 仍应保留 catalog registration/status/promote workflow、typed DTO conversion、store writes、audit envelopes 和 HTTP mapping。
- 本次已消除的不确定性：确认该 record projection 可独立于 compute `ModelCatalogRecord`、store implementation、audit envelope、HTTP response、OpenAPI、schema migration、generated client 和 public service signatures 表达；确认 payload hash 仍由同一 JSON marshal bytes 计算，错误语义保持 `model_catalog.schema_version` / `generated_at` 原有文本。
- 本次采用的关键假设：本阶段只迁移 catalog snapshot record data projection，不迁移完整 model governance package、catalog mutation workflow、status/promote orchestration、benchmark workflow、typed DTO conversion、persistence implementation、OpenAPI、contracts、migrations、generated clients、auth scopes 或 public constructor signatures。
- 剩余不确定性：完整 model governance package movement、完整 jobs/artifacts/evidence/simulation/agent package movement、full all-mutation audit、全对象 data scope、剩余 mutation data-scope、OIDC/RBAC、hosted green evidence、release artifact hosted round trip、legacy authenticated browser session 与 8 golden scenarios complete 仍是后续工作。
- 影响范围 / 风险控制方式：仅改 DTO-neutral record projection helper、compute adapter 调用、domain tests 和长期上下文；不改 catalog registration/status/promote 的行为、不改存储 schema、不改 HTTP/OpenAPI；未触碰用户未跟踪 `docs/rebuild/` 文档。
- 验证方式：`go test ./internal/domain/models ./internal/compute -run "Test(ModelCatalogSnapshotRecordData|ModelCatalogEndpoint|HTTPModelCatalogTenantProjectSiteScope|HTTPModelCatalogMutationTenantProjectSiteScope|DefaultParameterSetPromotionPlanEndpoint|HTTPModelCatalogPromotionTenantProjectSiteScope)$" -count=1`（compute 回归通过，domain helper 测试未被该 regex 命中）；`go test ./internal/domain/models -run TestModelCatalogSnapshotRecordData -count=1`；`go test ./...`（`apps/api`）；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1`；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1`；stale-text scan；tailing-whitespace scan；`git diff --check -- apps/api docs/architecture docs/rebuild tasks .ai`，均通过；diff-check 仅报告既有 LF/CRLF 工作区提示。
- 是否更新 README：是，更新 `apps/api/README.md`、`apps/api/internal/README.md`、`apps/api/internal/compute/README.md`、`apps/api/internal/domain/README.md` 和 `apps/api/internal/domain/models/README.md` 以反映新 package boundary。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本次延续既有 modular-monolith/domain package movement，不新增长期架构决策。

## 12:34 - Default parameter set transition document helper split

- 用户目标：继续完成 Certainty/Elegance 剩余工作，在 benchmark_run record projection 后推进真实 Go domain package movement，避免新增 evidence/hosted wrapper 或同 package 文件整理。
- 涉及目录：`apps/api/`、`apps/api/internal/compute/`、`apps/api/internal/domain/`、`apps/api/internal/domain/models/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`、`.ai/changes/`。
- 修改内容：新增 `domain/models` 的 `DefaultParameterSetStatusTransitionInput`、`DefaultParameterSetStatusTransition`、`ParameterSetTransitionError` 和 `ApplyDefaultParameterSetStatusTransition`；`ModelGovernanceService.updateDefaultParameterSetStatus` 在 compute catalog lookup/scope/error mapping 后使用 domain helper 投影 `default_parameter_set.status` 文档变更，再通过现有 catalog schema validation、snapshot record、audit 和 store write path 持久化；补充 domain 直测；同步 API/internal/domain/models/compute README、architecture/current-state、architecture/compute-api、Certainty/Elegance checklist 和任务记录。
- 修改原因：default parameter set status transition 的 document mutation、transition metadata projection 和 reason-coded domain errors 已稳定且 DTO-neutral，适合从 compute compatibility package 迁入 `domain/models`；compute 仍应保留 catalog selection/scope gate、schema validation、typed DTO conversion、store writes、audit envelopes 和 HTTP error mapping。
- 本次已消除的不确定性：确认该 document mutation projection 可独立于 compute `ModelCatalogResponse`、store implementation、audit envelope、HTTP response、OpenAPI、schema migration、generated client 和 public service signatures 表达；确认 no-op、not found、from_status mismatch 和 transition blocked 仍由 compute 映射为既有 response 语义。
- 本次采用的关键假设：本阶段只迁移 default parameter set status transition document mutation projection，不迁移完整 model governance package、catalog selection/scope gate、promotion orchestration、benchmark workflow、typed DTO conversion、persistence implementation、OpenAPI、contracts、migrations、generated clients、auth scopes 或 public constructor signatures。
- 剩余不确定性：完整 model governance package movement、完整 jobs/artifacts/evidence/simulation/agent package movement、full all-mutation audit、全对象 data scope、剩余 mutation data-scope、OIDC/RBAC、hosted green evidence、release artifact hosted round trip、legacy authenticated browser session 与 8 golden scenarios complete 仍是后续工作。
- 影响范围 / 风险控制方式：仅改 DTO-neutral status transition document projection helper、compute adapter 调用、domain tests 和长期上下文；不改 model catalog endpoint behavior、不改存储 schema、不改 HTTP/OpenAPI；未触碰用户未跟踪 `docs/rebuild/` 文档。
- 验证方式：`go test ./internal/domain/models -run "TestApplyDefaultParameterSetStatusTransition" -count=1`；`go test ./internal/compute -run "Test(DefaultParameterSetPromotionPlanEndpoint|HTTPModelCatalogMutationTenantProjectSiteScope|HTTPModelCatalogPromotionTenantProjectSiteScope|ModelCatalogEndpoint)$" -count=1`；`go test ./...`（`apps/api`）；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1`；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1`；stale-text scan 无过时描述命中；`rg -n "[ \t]+$" ...` 针对本阶段文件无尾随空白；`git diff --check -- apps/api docs/architecture docs/rebuild tasks .ai` 通过，diff-check 仅报告既有 LF/CRLF 工作区提示。
- 是否更新 README：是，更新 `apps/api/README.md`、`apps/api/internal/README.md`、`apps/api/internal/compute/README.md`、`apps/api/internal/domain/README.md` 和 `apps/api/internal/domain/models/README.md` 以反映新 package boundary。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本次延续既有 modular-monolith/domain package movement，不新增长期架构决策。

## 12:09 - Model run service test split

- 用户目标：先拆 `service_test.go`，解决当前最大代码维护性问题；在当前仓库已无单体 `service_test.go` 的前提下，继续拆残留偏大的 service 测试聚合点。
- 涉及目录：`apps/api/internal/compute/`、`docs/architecture/`、`tasks/`、`.ai/changes/`。
- 修改内容：将 `service_model_runs_test.go` 中单个 379 行 integration test 拆为核心 model_run 持久化/list/result view 测试、completed model-run scenario helper、model_run HTTP read/list 测试、evidence/readiness 测试、benchmark_run backed-by-model_run 测试、result explanation model_run evidence refs 测试；同步 `docs/architecture/compute-api.md` 的测试文件 evidence 行数表。
- 修改原因：`service_model_runs_test.go` 已成为当前最大的单测试 service 聚合文件，串联 model_run persistence、HTTP、evidence package、production readiness、benchmark_run、result explanation 和 worker-token 拒绝，导致失败定位与 review 成本偏高。
- 本次已消除的不确定性：确认 `apps/api/internal/compute/service_test.go` 仍不存在；确认当前最大残留单测试聚合点是 `service_model_runs_test.go`；确认可通过同 package helper + focused test 拆分保留原覆盖，不需要修改生产代码、schema、OpenAPI、generated client、migration、auth scope 或 HTTP route。
- 本次采用的关键假设：用户的“拆 service_test.go”在当前仓库状态下等价于继续拆 service 测试聚合文件；本阶段只做测试组织维护，保留 `TestValidatedCompletePersistsModelRun` 作为核心 model_run 持久化入口，并把原长链路断言拆到 focused tests。
- 剩余不确定性：`service_contracts_test.go`、`service_simulation_registry_test.go`、`service_model_catalog_test.go`、`service_artifact_retention_test.go` 和 `service_model_parameters_test.go` 仍偏大；完整 jobs/artifacts/models/evidence/simulation/agent package movement、remaining mutation data-scope、full object-level data-scope、remaining all-mutation audit、OIDC/RBAC、hosted green evidence、release artifact hosted round trip、legacy authenticated session 和 complete golden scenarios 仍未完成。
- 影响范围 / 风险控制方式：仅拆分同 package `_test.go` 文件和修正文档 evidence 表；不改生产行为、不改公共接口、不改持久化结构；未触碰未跟踪 `docs/rebuild/` 文档。
- 验证方式：`go test ./internal/compute -run "Test(ValidatedCompletePersistsModelRun|HTTPModelRunReadListEndpoints|ModelRunEvidencePackageAndReadiness|HTTPModelRunEvidenceAndReadinessEndpoints|HTTPModelRunEvidenceEndpointsRejectWorkerToken|BenchmarkRunEndpointRecordsCompletedModelRun|BenchmarkRunEndpointRejectsWorkerToken|ResultExplanationWorkflowResolvesModelRunEvidenceRefs|ResultExplanationRejectsUnresolvedModelRunEvidenceRefs)$" -count=1`；`go test ./...`（`apps/api`）；`rg -n "[ \t]+$" ...` 针对本阶段文件无尾随空白；`git diff --check -- ...` 通过，diff-check 仅报告既有 LF/CRLF 工作区提示。
- 是否更新 README：否；`apps/api/internal/compute/README.md` 已用 `service_*_test.go` wildcard 描述 service/lifecycle regression tests，本次新增文件仍落在该约定下，且为纯测试组织拆分。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本次是测试组织维护，不产生新的架构决策。

## 12:25 - Benchmark run record data projection domain helper split

- 用户目标：继续完成 Certainty/Elegance 剩余工作，在 service test 拆分后推进真实 Go domain package movement，避免新增 evidence/hosted wrapper 或同 package 文件整理。
- 涉及目录：`apps/api/`、`apps/api/internal/compute/`、`apps/api/internal/domain/`、`apps/api/internal/domain/models/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`、`.ai/changes/`。
- 修改内容：新增 `domain/models` 的 `BenchmarkRunRecordDataInput`、`BenchmarkRunRecordData` 和 `BenchmarkRunRecordDataFromDocument`；`ModelGovernanceService.benchmarkRunRecord` 在 compute schema validation、catalog/model_run/evidence 校验和 scoped mutation gate 后使用 domain helper 投影 benchmark_run record data，再适配回现有 `BenchmarkRunRecord`；补充 domain 直测；同步 API/internal/domain/models/compute README、architecture/current-state、architecture/compute-api、Certainty/Elegance checklist 和任务记录。
- 修改原因：`benchmark_run.v1` 的 persisted record 字段投影、payload hash、metadata scope、source/requested_by 默认值与 `executed_at` 解析已稳定且 DTO-neutral，适合从 compute compatibility package 迁入 `domain/models`；compute 仍应保留 benchmark run schema validation、catalog lookup、benchmark case admission、model_run identity/hash checks、evidence resolution、store writes、audit envelopes 和 HTTP mapping。
- 本次已消除的不确定性：确认 benchmark_run record projection 可独立于 compute `BenchmarkRunRecord`、store implementation、audit envelope、HTTP response、OpenAPI、schema migration、generated client 和 public service signatures 表达；确认可在不改变 benchmark run registration workflow 的前提下由 compute adapter 映射回现有 DTO。
- 本次采用的关键假设：本阶段只迁移 benchmark_run record data projection，不迁移完整 model governance package、benchmark validation workflow、catalog mutation workflow、status/promote orchestration、persistence implementation、OpenAPI、contracts、migrations、generated clients、auth scopes 或 public constructor signatures。
- 剩余不确定性：完整 model governance package movement、完整 jobs/artifacts/evidence/simulation/agent package movement、full all-mutation audit、全对象 data scope、剩余 mutation data-scope、OIDC/RBAC、hosted green evidence、release artifact hosted round trip、legacy authenticated browser session 与 8 golden scenarios complete 仍是后续工作。
- 影响范围 / 风险控制方式：仅改 DTO-neutral benchmark run record projection helper、compute adapter 调用、domain tests 和长期上下文；不改 benchmark_run registration behavior、不改存储 schema、不改 HTTP/OpenAPI；未触碰用户未跟踪 `docs/rebuild/` 文档。
- 验证方式：`go test ./internal/domain/models -run "TestBenchmarkRunRecordData" -count=1`；`go test ./internal/compute -run "Test(BenchmarkRunEndpointRecordsCompletedModelRun|BenchmarkRunEndpointRejectsWorkerToken|HTTPBenchmarkRunTenantProjectSiteScope|HTTPBenchmarkRunMutationTenantProjectSiteScope|ValidatedCompletePersistsModelRun|ModelRunEvidencePackageAndReadiness|ResultExplanationWorkflowResolvesModelRunEvidenceRefs)$" -count=1`；`go test ./...`（`apps/api`）；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1`；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1`；stale-text scan 无过时描述命中；`rg -n "[ \t]+$" ...` 针对本阶段文件无尾随空白；`git diff --check -- apps/api docs/architecture docs/rebuild tasks .ai` 通过，diff-check 仅报告既有 LF/CRLF 工作区提示。
- 是否更新 README：是，更新 `apps/api/README.md`、`apps/api/internal/README.md`、`apps/api/internal/compute/README.md`、`apps/api/internal/domain/README.md` 和 `apps/api/internal/domain/models/README.md` 以反映新 package boundary。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本次延续既有 modular-monolith/domain package movement，不新增长期架构决策。
