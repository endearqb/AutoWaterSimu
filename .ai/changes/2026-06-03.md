## 21:40 - Model governance workflow file split

- 用户目标：根据 Certainty/Elegance PRD 与开发计划继续完善 AutoWaterSimu Next，完成一个可验证的大阶段并适时提交推送。
- 涉及目录：`apps/api/internal/compute/`、`apps/api/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`。
- 修改内容：将 `ModelGovernanceService` workflow 方法按同 package 文件拆分到 `model_governance_catalog.go`、`model_governance_parameters.go`、`model_governance_benchmark_cases.go`、`model_governance_benchmark_runs.go` 和 `model_governance_model_runs.go`；`model_governance.go` 仅保留 struct、constructor、store interface 与 callback wiring；同步更新 API/compute README、architecture current-state、compute-api audit summary、Certainty/Elegance checklist 和任务复盘。
- 修改原因：继续落实 Certainty/Elegance Phase 1 的 handler/package surface reduction 与 Go API large-file cleanup；`model_governance.go` 是 service delegate split 后 remaining compute workflow large-file target。
- 本次已消除的不确定性：确认本轮只做同 package workflow 文件拆分；确认 `ModelGovernanceService` public 方法、HTTP routes、OpenAPI、contracts、migrations、generated clients、auth scopes、store interfaces、public `Service` delegates 和 workflow 行为不需要改变；确认 boundary audit 仍通过当前 Store/domain constructor/package 边界。
- 本次采用的关键假设：当前未跟踪的 `docs/rebuild/AutoWaterSimu_95分优雅度完整计划.md` 不属于本阶段目标文件；model governance workflow 暂留 compute compatibility package，不在本阶段迁入 `domain/models`，避免同时改变 DTO/persistence/HTTP mapping 边界。
- 剩余不确定性：完整 models governance domain package migration 尚未完成；catalog snapshot mutation、benchmark workflow orchestration、DTO mapping、persistence implementation、evidence/simulation wiring 和 HTTP mapping 仍在 compute compatibility package；public `NewService` / `NewServiceWithArchive` 构造签名仍保持 aggregate `Store`。
- 影响范围 / 风险控制方式：不改变 API 行为、持久化语义、schema 或公共签名；新增文件与原文件保持同 package、同 service receiver、同 helper/callback 调用；通过 Go tests、architecture boundary audit、dependency checks 和 pr-fast 验证拆分无行为/边界回归。
- 验证方式：`cd apps\api; go test ./internal/compute ./cmd/compute-api` 通过；`cd apps\api; go test ./...` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` 通过；`rg -n "schema_version|P0|P1|P2" docs\rebuild --glob "AutoWaterSimu_Next_*.md"` 已检查；`git diff --check -- apps\api docs scripts tasks .ai` 通过，仅有 LF/CRLF warning。
- 是否更新 README：是，更新 `apps/api/README.md` 与 `apps/api/internal/compute/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本次是既有 modular monolith / same-package file split 路线的延续，没有新增长期架构决策。

## 21:25 - Service compatibility delegate file split

- 用户目标：根据 Certainty/Elegance PRD 与开发计划继续完善 AutoWaterSimu Next，完成一个可验证的大阶段并适时提交推送。
- 涉及目录：`apps/api/internal/compute/`、`apps/api/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`。
- 修改内容：将 `service.go` 收缩为 `Service` struct 与 `NewService` / `NewServiceWithArchive` wiring 入口；新增 `service_jobs.go`、`service_artifacts.go`、`service_models.go`、`service_simulation.go`、`service_contracts.go`、`service_evidence.go`、`service_workers.go`、`service_metrics.go` 承接 public `Service` compatibility delegates；新增 `service_helpers.go` 承接原共享 helper；同步更新 API/compute README、architecture current-state、compute-api audit summary、Certainty/Elegance checklist 和任务复盘。
- 修改原因：继续落实 Certainty/Elegance Phase 1 的 handler/package surface reduction 与 Go API large-file cleanup；`service.go` 已是 current-state 标记的剩余 large-file split target。
- 本次已消除的不确定性：确认本轮只做同 package 文件拆分；确认 `Service` public 方法签名、HTTP routes、OpenAPI、contracts、migrations、generated clients、auth scopes、store interfaces、narrowed internal service constructors 和 workflow 行为不需要改变；确认边界审计会扫描新增 `service_*.go` 文件。
- 本次采用的关键假设：当前未跟踪的 `docs/rebuild/AutoWaterSimu_95分优雅度完整计划.md` 不属于本阶段目标文件；`service_helpers.go` 暂留 compute compatibility package，不在本阶段迁入 domain/platform package，避免把 compute DTO/helper 依赖外溢。
- 剩余不确定性：完整 jobs/artifacts/models/evidence/simulation/agent workflow package migration 尚未完成；public `NewService` / `NewServiceWithArchive` 构造签名仍保持 aggregate `Store`；production security 的 all-mutation audit、OIDC/RBAC 和全对象 data scope 仍需后续阶段。
- 影响范围 / 风险控制方式：不改变 API 行为和持久化语义；新增文件与原文件保持同 package、同 helper 名和同 delegate 调用；通过 Go tests、architecture boundary audit、dependency checks 和 pr-fast 验证拆分无行为/边界回归。
- 验证方式：`cd apps\api; go test ./internal/compute ./cmd/compute-api` 通过；`cd apps\api; go test ./...` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` 通过；`rg -n "schema_version|P0|P1|P2" docs\rebuild --glob "AutoWaterSimu_Next_*.md"` 已检查；`git diff --check -- apps\api docs scripts tasks .ai` 通过，仅有 LF/CRLF warning。
- 是否更新 README：是，更新 `apps/api/README.md` 与 `apps/api/internal/compute/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本次是既有 modular monolith / same-package file split 路线的延续，没有新增长期架构决策。

## 21:03 - Platform audit envelope helper split

- 用户目标：根据 Certainty/Elegance PRD 与开发计划继续完善 AutoWaterSimu Next，完成一个可验证的大阶段并适时提交推送。
- 涉及目录：`apps/api/internal/platform/audit/`、`apps/api/internal/compute/`、`apps/api/`、`docs/architecture/`、`docs/rebuild/`、`scripts/`、`tasks/`。
- 修改内容：新增 `apps/api/internal/platform/audit` package，承接 selected mutation audit envelope 字段形状、request context 投影和 event JSON helper；`compute/audit.go` 改为兼容 wrapper 并委托平台 helper；同步更新 API/platform/compute README、architecture current-state、compute-api 架构说明、Certainty/Elegance checklist、boundary audit expected package list 和任务复盘。
- 修改原因：继续落实 Certainty/Elegance Phase 1 的 Go API modular monolith / platform package movement，降低 `apps/api/internal/compute` compatibility package 中横切 audit envelope 逻辑密度。
- 本次已消除的不确定性：确认本轮只迁移稳定 envelope shape、HTTP principal/route context 投影和 event JSON payload 组装；确认 selected audit call sites、event persistence、HTTP/OpenAPI/contracts/migrations/generated clients、auth scopes、OIDC/RBAC 和 all-mutation audit coverage 不需要改变；确认 `platform/audit` 不反向依赖 compute。
- 本次采用的关键假设：当前未跟踪的 `docs/rebuild/AutoWaterSimu_95分优雅度完整计划.md` 不属于本阶段目标文件；`EventJSON` 继续保持既有 compute `mustJSON` 风格，不在本阶段把 helper 改成 error-returning API。
- 剩余不确定性：完整 production security 仍未完成；all-mutation audit coverage、OIDC/RBAC、full object data scope checks、security smoke expansion 和 audit persistence policy completion 仍是后续阶段。
- 影响范围 / 风险控制方式：不修改 HTTP routes、OpenAPI、contracts、migrations、generated clients、auth scopes、持久化 schema 或 audit 覆盖范围；新增 direct platform tests，并用 compute tests / boundary audit / dependency check / pr-fast 验证行为与边界。
- 验证方式：`cd apps\api; go test ./internal/platform/audit ./internal/compute ./cmd/compute-api` 通过；`cd apps\api; go test ./...` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` 通过；`rg -n "schema_version|P0|P1|P2" docs\rebuild --glob "AutoWaterSimu_Next_*.md"` 已检查；`git diff --check -- apps\api docs scripts tasks .ai` 通过，仅有 LF/CRLF warning。
- 是否更新 README：是，更新 `apps/api/README.md`、`apps/api/internal/README.md`、`apps/api/internal/compute/README.md`、`apps/api/internal/platform/README.md`、`apps/api/internal/platform/audit/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本次延续既有 modular monolith / platform package movement 边界，没有新增长期架构决策。

## 19:45 - Agent draft confirmation envelope validation split

- 用户目标：根据 Certainty/Elegance PRD 与开发计划继续完善 AutoWaterSimu Next，完成一个可验证的大阶段并适时提交推送。
- 涉及目录：`apps/api/internal/domain/agent/`、`apps/api/internal/compute/`、`apps/api/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`。
- 修改内容：新增 `domain/agent.ValidateDraftConfirmationEnvelope` 及直接测试；`DraftWorkflowService.ConfirmDraftDocument` 改为调用该 helper；同步更新 API/domain/compute README、architecture current-state、compute-api 架构说明、Certainty/Elegance checklist 与任务复盘。
- 修改原因：Certainty/Elegance Phase 1 要继续降低 `apps/api/internal/compute` 巨型 compatibility package 的业务规则密度；draft confirmation wrapper 与 embedded draft 的跨字段规则已经稳定，适合迁入 `domain/agent`。
- 本次已消除的不确定性：确认本轮只迁移纯 envelope 校验规则；确认 schema file lookup、JSON Schema validation、record assembly、store persistence、HTTP/OpenAPI/client/migration/auth scope 和 promotion 行为不需要改变；确认 `domain/agent` 不反向依赖 compute。
- 本次采用的关键假设：当前未跟踪的 `docs/rebuild/AutoWaterSimu_95分优雅度完整计划.md` 不属于本阶段目标文件；非 draft confirmation 文档在 `ConfirmDraftDocument` 中仍保持原有 schema_version 早退语义。
- 剩余不确定性：完整 draft workflow package migration 尚未完成；record assembly/persistence、schema file lookup/JSON Schema validation、confirmation readback、promotion orchestration、HTTP mapping 和 store implementations 仍在 compute compatibility package。
- 影响范围 / 风险控制方式：不修改 HTTP routes、OpenAPI、contracts、migrations、generated clients、auth scopes 或持久化 schema；新增 direct domain tests，并用 compute tests / boundary audit / dependency check 验证行为与边界。
- 验证方式：`cd apps\api; go test ./internal/domain/agent ./internal/compute` 通过；`cd apps\api; go test ./...` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` 通过；`rg -n "schema_version|P0|P1|P2" docs\rebuild --glob "AutoWaterSimu_Next_*.md"` 已检查。
- 是否更新 README：是，更新 `apps/api/README.md`、`apps/api/internal/README.md`、`apps/api/internal/compute/README.md`、`apps/api/internal/domain/README.md`、`apps/api/internal/domain/agent/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本次延续既有 modular monolith / domain package movement 边界，没有新增长期架构决策。

## 20:00 - Agent draft confirmation record data projection split

- 用户目标：根据 Certainty/Elegance PRD 与开发计划继续完善 AutoWaterSimu Next，完成一个可验证的大阶段并适时提交推送。
- 涉及目录：`apps/api/internal/domain/agent/`、`apps/api/internal/compute/`、`apps/api/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`。
- 修改内容：新增 `domain/agent.DraftConfirmationRecordDataFromDocument` 及直接测试；`DraftWorkflowService.draftConfirmationRecord` 改为调用该 helper 并只保留 compute `DraftConfirmationRecord` 映射；同步更新 API/domain/compute README、architecture current-state、compute-api 架构说明、Certainty/Elegance checklist 与任务复盘。
- 修改原因：继续落实 Certainty/Elegance Phase 1 的 Go API modular monolith / domain package movement，降低 `apps/api/internal/compute` draft workflow 中的纯领域字段投影规则密度。
- 本次已消除的不确定性：确认本轮只迁移 record data projection；确认 schema file lookup、JSON Schema validation、compute DTO 映射、store persistence、HTTP/OpenAPI/client/migration/auth scope 和 promotion 行为不需要改变；确认 `domain/agent` 仍不反向依赖 compute。
- 本次采用的关键假设：当前未跟踪的 `docs/rebuild/AutoWaterSimu_95分优雅度完整计划.md` 不属于本阶段目标文件；`DraftConfirmationRecordDataFromDocument` 返回中立 data，compute 继续拥有持久化 DTO 与 response attachment。
- 剩余不确定性：完整 draft workflow package migration 尚未完成；schema validation、confirmation read/write、promotion orchestration、HTTP mapping 和 Memory/Postgres implementations 仍在 compute compatibility package。
- 影响范围 / 风险控制方式：不修改 HTTP routes、OpenAPI、contracts、migrations、generated clients、auth scopes 或持久化 schema；新增 direct domain tests，并用 compute tests / boundary audit / dependency check 验证行为与边界。
- 验证方式：`cd apps\api; go test ./internal/domain/agent ./internal/compute` 通过；`cd apps\api; go test ./...` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` 通过；`rg -n "schema_version|P0|P1|P2" docs\rebuild --glob "AutoWaterSimu_Next_*.md"` 已检查。
- 是否更新 README：是，更新 `apps/api/README.md`、`apps/api/internal/README.md`、`apps/api/internal/compute/README.md`、`apps/api/internal/domain/README.md`、`apps/api/internal/domain/agent/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本次延续既有 modular monolith / domain package movement 边界，没有新增长期架构决策。

## 20:21 - Evidence result explanation record data projection split

- 用户目标：根据 Certainty/Elegance PRD 与开发计划继续完善 AutoWaterSimu Next，完成一个可验证的大阶段并适时提交推送。
- 涉及目录：`apps/api/internal/domain/evidence/`、`apps/api/internal/compute/`、`apps/api/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`。
- 修改内容：新增 `domain/evidence.ResultExplanationRecordDataFromDocument` 及直接测试；`ResultExplanationService.resultExplanationRecord` 改为调用该 helper 并只保留 compute `ResultExplanationRecord` 映射；同步更新 API/domain/evidence/compute README、architecture current-state、compute-api 架构说明、Certainty/Elegance checklist 与任务复盘。
- 修改原因：继续落实 Certainty/Elegance Phase 1 的 Go API modular monolith / domain package movement，降低 `apps/api/internal/compute` result explanation workflow 中的纯记录字段投影规则密度。
- 本次已消除的不确定性：确认本轮只迁移 record data projection；确认 schema validation、route `job_id` 校验、job/result availability gate、job-scoped evidence ref resolution、compute DTO 映射、store persistence、review/publish、HTTP/OpenAPI/contracts/migrations/generated clients 和 auth scopes 不需要改变；确认 `domain/evidence` 仍不反向依赖 compute。
- 本次采用的关键假设：当前未跟踪的 `docs/rebuild/AutoWaterSimu_95分优雅度完整计划.md` 不属于本阶段目标文件；`ResultExplanationRecordDataFromDocument` 返回中立 data，compute 继续拥有持久化 DTO、review/publish 状态流和 response mapping。
- 剩余不确定性：完整 result explanation workflow package migration 尚未完成；schema validation、job/result availability checks、evidence resolver callback、persistence/review/publish、HTTP mapping 和 Memory/Postgres implementations 仍在 compute compatibility package。
- 影响范围 / 风险控制方式：不修改 HTTP routes、OpenAPI、contracts、migrations、generated clients、auth scopes 或持久化 schema；新增 direct domain tests，并用 compute tests / boundary audit / dependency check / pr-fast 验证行为与边界。
- 验证方式：`cd apps\api; go test ./internal/domain/evidence ./internal/compute` 通过；`cd apps\api; go test ./...` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` 通过；`rg -n "schema_version|P0|P1|P2" docs\rebuild --glob "AutoWaterSimu_Next_*.md"` 已检查；`git diff --check -- apps\api docs tasks .ai` 通过，仅有 LF/CRLF warning。
- 是否更新 README：是，更新 `apps/api/README.md`、`apps/api/internal/README.md`、`apps/api/internal/compute/README.md`、`apps/api/internal/domain/README.md`、`apps/api/internal/domain/evidence/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本次延续既有 modular monolith / domain package movement 边界，没有新增长期架构决策。

## 20:41 - Simulation registry record data projection split

- 用户目标：根据 Certainty/Elegance PRD 与开发计划继续完善 AutoWaterSimu Next，完成一个可验证的大阶段并适时提交推送。
- 涉及目录：`apps/api/internal/domain/simulation/`、`apps/api/internal/compute/`、`apps/api/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`。
- 修改内容：新增 `domain/simulation.ProcessGraphRecordDataFromDocument`、`domain/simulation.SimulationInputRecordDataFromDocument` 及直接测试；`SimulationInputService` 的 process graph / simulation input record assembly 改为调用这些 helper 并只保留 compute metadata record 映射；同步更新 API/domain/simulation/compute README、architecture current-state、compute-api 架构说明、Certainty/Elegance checklist 与任务复盘。
- 修改原因：继续落实 Certainty/Elegance Phase 1 的 Go API modular monolith / domain package movement，降低 `apps/api/internal/compute` simulation registry 中的纯记录字段投影规则密度。
- 本次已消除的不确定性：确认本轮只迁移 simulation input / process graph record data projection；确认 JSON Schema validation、material-balance process graph structure validation、compute DTO 映射、store persistence/idempotency、input-ref resolution、HTTP/OpenAPI/contracts/migrations/generated clients 和 auth scopes 不需要改变；确认 `domain/simulation` 仍不反向依赖 compute。
- 本次采用的关键假设：当前未跟踪的 `docs/rebuild/AutoWaterSimu_95分优雅度完整计划.md` 不属于本阶段目标文件；`ProcessGraphRecordDataFromDocument` 与 `SimulationInputRecordDataFromDocument` 返回中立 data，compute 继续拥有持久化 DTO、store/idempotency 和 HTTP mapping。
- 剩余不确定性：完整 simulation input/process graph metadata service migration 尚未完成；schema validation、store persistence/idempotency、input-ref resolution、HTTP mapping 和 Memory/Postgres implementations 仍在 compute compatibility package。
- 影响范围 / 风险控制方式：不修改 HTTP routes、OpenAPI、contracts、migrations、generated clients、auth scopes 或持久化 schema；新增 direct domain tests，并用 compute tests / boundary audit / dependency check / pr-fast 验证行为与边界。
- 验证方式：`cd apps\api; go test ./internal/domain/simulation ./internal/compute` 通过；`cd apps\api; go test ./...` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\audit-compute-api-boundary.ps1` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` 通过；`rg -n "schema_version|P0|P1|P2" docs\rebuild --glob "AutoWaterSimu_Next_*.md"` 已检查；`git diff --check -- apps\api docs tasks .ai` 通过，仅有 LF/CRLF warning。
- 是否更新 README：是，更新 `apps/api/README.md`、`apps/api/internal/README.md`、`apps/api/internal/compute/README.md`、`apps/api/internal/domain/README.md`、`apps/api/internal/domain/simulation/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本次延续既有 modular monolith / domain package movement 边界，没有新增长期架构决策。
