## 22:50 - Golden scenario evidence summary

- 用户目标：根据 Certainty/Elegance PRD 与开发计划继续完善 AutoWaterSimu Next，完成一个可验证的大阶段并适时提交推送。
- 涉及目录：`scripts/ci/`、`scripts/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`、根 `README.md` 和 `Justfile`。
- 修改内容：新增 `scripts/ci/golden-scenarios.ps1`，读取现有 CI/release evidence 并生成 `tmp/ci-evidence/golden-scenarios.json`；新增 `just golden-scenarios`；将 pr-fast、browser、security、Desktop package 和 integration smoke 的 dirty-state line collections 显式数组化，避免 clean tracked worktree + 单个 untracked 文件时 `.Count` 在严格模式下失败；同步更新根 README、`scripts/README.md`、`scripts/ci/README.md`、architecture current-state/local-dev、Certainty/Elegance plan 和任务记录。
- 修改原因：PRD 要求 8 条金标场景至少 6 条具备自动或半自动 evidence；现有 evidence 分散在 pr-fast、integration、browser、security、desktop 和 release lanes 中，缺少场景级汇总，也容易把旧 commit evidence 误读为当前 HEAD 证明。
- 本次已消除的不确定性：确认 GitHub workflow dispatch 当前无法作为 hosted integration evidence 使用，`gh workflow run next-integration-smoke.yml --ref codex/autowatersimu-next-rebuild` 返回 `workflow not found on the default branch`；确认可先用本地汇总脚本把已有 lane evidence 映射到 8 条金标场景，并按 commit relation 标记 current/stale/unknown/missing；确认 dirty-state helper 必须显式返回数组，否则 clean tracked worktree 下单个未跟踪文件会让 `.Count` 访问失败。
- 本次采用的关键假设：当前未跟踪的 `docs/rebuild/AutoWaterSimu_95分优雅度完整计划.md` 不属于本阶段目标文件；本轮只新增 evidence 汇总器，不宣称任何金标场景完成，不修改 workflow 触发策略，不启动 Docker integration 栈。
- 剩余不确定性：hosted integration/browser/security/Desktop package workflow green run、live backend browser reads、release artifact hosted round trip、至少 6 条金标场景自动/半自动 evidence、完整生产 data scope 和 all-mutation audit 仍是后续阶段。
- 影响范围 / 风险控制方式：脚本只读取 `tmp/ci-evidence` 与 `tmp/release-evidence` 中的 JSON 并输出新的 summary JSON；不改业务实现、API、generated client、workflow YAML 或 smoke 执行步骤；summary 只把 commit SHA 匹配当前 HEAD 的 passed lane 计入场景来源。
- 验证方式：`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` 通过并刷新 `tmp\ci-evidence\pr-fast.json`；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\browser-smoke.ps1` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\security-smoke.ps1` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\desktop-package-smoke.ps1` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\release\smoke-release-artifact-download.ps1` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\golden-scenarios.ps1` 通过并写出 `tmp\ci-evidence\golden-scenarios.json`；JSON 断言确认 schema version 正确且包含 8 条场景，当前汇总为 `status=partial`、`covered=0`、`partial=7`、`missing=1`、`blocked=0`，其中 `integration` 为 stale、`release_gate` commit relation unknown、`downloaded_release_artifacts` missing；post-commit `pr-fast` 曾暴露 `.Count` strict-mode failure，修复数组化后上述 CI/release/golden 验证全部通过；`git diff --check -- README.md Justfile scripts docs tasks .ai` 通过，仅有 LF/CRLF warning；`rg -n "schema_version|P0|P1|P2" docs\rebuild --glob "AutoWaterSimu_Next_*.md"` 已检查相关重构文档范围。
- 是否更新 README：是，更新根 `README.md`、`scripts/README.md`、`scripts/ci/README.md`；架构长期事实同步更新 `docs/architecture/current-state.md` 与 `docs/architecture/local-dev.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本次是 scenario evidence 汇总入口，不新增长期架构决策。

## 22:25 - CI evidence dirty-state taxonomy

- 用户目标：根据 Certainty/Elegance PRD 与开发计划继续完善 AutoWaterSimu Next，完成一个可验证的大阶段并适时提交推送。
- 涉及目录：`scripts/ci/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`。
- 修改内容：`pr-fast`、integration、security、browser、Desktop package smoke evidence 在保留 `is_dirty_*` 与 `dirty_files_*` 的基础上，新增 `has_tracked_changes_*`、`tracked_changes_*` 和 `untracked_files_*`；同步更新 `scripts/ci/README.md`、architecture current-state/local-dev、Certainty/Elegance plan 和任务记录。
- 修改原因：Certainty/Elegance 计划要求 current HEAD evidence 可追溯；现有 dirty 字段只能说明 worktree 不干净，无法区分未提交 tracked 修改与本地未跟踪文档，容易让 local evidence 的可信边界含糊。
- 本次已消除的不确定性：确认可以在不改变 smoke step、workflow 文件、业务代码、API shape、generated client、route path 或 UI 行为的前提下，给所有当前 CI smoke JSON 增加向后兼容的 dirty-state taxonomy。
- 本次采用的关键假设：当前未跟踪的 `docs/rebuild/AutoWaterSimu_95分优雅度完整计划.md` 不属于本阶段目标文件；本轮只增强 local evidence 元数据解释力，不宣称 hosted workflow 已通过，也不把 dirty local run 解释为 clean HEAD proof。
- 剩余不确定性：hosted integration/browser/security/Desktop package workflow green run、live backend browser reads、release artifact hosted round trip、8 条 golden scenarios 自动化、完整生产 data scope 和 all-mutation audit 仍是后续阶段。
- 影响范围 / 风险控制方式：只新增 evidence report 字段并更新长期文档；保留旧字段；不修改 smoke 执行步骤和 workflow 调度；通过 focused smoke、PowerShell parse、pr-fast、evidence JSON 字段检查和 diff-check 验证。
- 验证方式：`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\browser-smoke.ps1` 通过并写出 `tmp\ci-evidence\browser-smoke.json`；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\security-smoke.ps1` 通过并写出 `tmp\ci-evidence\security-smoke.json`；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\desktop-package-smoke.ps1` 通过并写出 `tmp\ci-evidence\desktop-package-smoke.json`；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` 通过并写出 `tmp\ci-evidence\pr-fast.json`；`scripts\ci\integration-smoke.ps1` 通过 PowerShell parse check，未跑 `-StartCompose`，因为该真实栈 smoke 需要启动 Docker Compose；对 browser/security/Desktop/pr-fast 生成的 JSON 做字段断言，确认六个新增 dirty-state 字段存在且 status 均为 `passed`；`git diff --check -- scripts docs tasks .ai` 通过，仅有 LF/CRLF warning。
- 是否更新 README：是，更新 `scripts/ci/README.md`；架构长期事实同步更新 `docs/architecture/current-state.md` 与 `docs/architecture/local-dev.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本次是既有 CI smoke evidence schema 的向后兼容字段补充，没有新增长期架构决策。

## 22:09 - Browser current-flow evidence coverage

- 用户目标：根据 Certainty/Elegance PRD 与开发计划继续完善 AutoWaterSimu Next，完成一个可验证的大阶段并适时提交推送。
- 涉及目录：`frontend/tests/`、`scripts/ci/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`。
- 修改内容：扩展 `frontend/tests/compute-jobs-current-flow.spec.ts`，在 current-flow browser smoke 中实际点击 evidence package 下载并解析 selected job 的 evidence ref；`scripts/ci/browser-smoke.ps1` 的 evidence summary 增加 evidence package download 与 evidence ref resolution 覆盖项；同步更新 `frontend/tests/README.md`、`scripts/ci/README.md`、architecture current-state、Certainty/Elegance plan 和任务记录。
- 修改原因：Certainty/Elegance immediate direction 要在 deeper route/component decomposition 前补 focused browser coverage；现有 current-flow spec 已 mock evidence endpoint，但未通过 UI 行为证明 route/query/mutation 链路真的读取 evidence package 和 evidence ref。
- 本次已消除的不确定性：确认在不修改 route、feature query wrappers、generated client、OpenAPI、后端 API 或 UI markup 的前提下，可以用现有 mock-backed Playwright smoke 覆盖 current-flow job submission、result/readiness、evidence package download 和 evidence ref lookup。
- 本次采用的关键假设：当前未跟踪的 `docs/rebuild/AutoWaterSimu_95分优雅度完整计划.md` 不属于本阶段目标文件；本轮只增强 mock-backed browser evidence，不宣称覆盖 live PostgreSQL/MinIO/worker backend、hosted browser workflow green run 或 legacy authenticated backend session。
- 剩余不确定性：live backend browser reads、hosted integration/browser/release evidence、进一步 route/component decomposition、8 条 golden scenarios 自动化、完整生产安全 data scope 和 all-mutation audit 仍是后续阶段。
- 影响范围 / 风险控制方式：仅改测试和 evidence 文档；业务实现、API shape、generated client、route path 和 UI markup 均不变；先运行 focused Playwright spec，再运行 browser-smoke、frontend typecheck、dependency check、pr-fast 和 diff-check。
- 验证方式：`cd frontend; npx playwright test tests/compute-jobs-current-flow.spec.ts --project=chromium --no-deps --reporter=line` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\browser-smoke.ps1` 通过并写出 `tmp\ci-evidence\browser-smoke.json`，其中 `coverage_summary` 包含 `evidence_package_download` 与 `evidence_ref_resolution`，且因本阶段未提交前运行而正确记录 `is_dirty_before=true` / `is_dirty_after=true`；`cd frontend; npx tsc --noEmit` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` 通过并写出 `tmp\ci-evidence\pr-fast.json`。
- 是否更新 README：是，更新 `frontend/tests/README.md` 与 `scripts/ci/README.md`；架构长期事实同步更新 `docs/architecture/current-state.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本次是既有 browser smoke lane 的覆盖增强，没有新增长期架构决策。

## 21:51 - Frontend route query boundary check

- 用户目标：根据 Certainty/Elegance PRD 与开发计划继续完善 AutoWaterSimu Next，完成一个可验证的大阶段并适时提交推送。
- 涉及目录：`scripts/`、`frontend/src/routes/`、`frontend/src/features/`、`frontend/src/services/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`。
- 修改内容：在 `scripts/check-deps.ps1` 中新增 `frontend-routes-must-use-feature-query-boundaries-for-compute` 规则，扫描 `frontend/src/routes` 并拒绝 route 通过 `computeJobsService` 或 `features/*/api`、`features/*/builders`、`features/*/processGraphApi` 绕过 feature query/mutation boundary；同步更新 `scripts/README.md`、architecture dependency/current-state/module-map、Certainty/Elegance plan 和任务记录。
- 修改原因：上一阶段已经把 Compute Jobs/Lifecycle/Model Governance routes 迁到 `features/*/queries.ts`，但自动化依赖检查只限制 generated Compute client 直接 import，尚未防止后续 route 回退到 compatibility facade 或 feature 私有 API 层。
- 本次已消除的不确定性：确认当前 Compute route imports 已指向 `features/*/queries.ts`，新增规则可在不修改业务代码、generated client、OpenAPI、route path、UI 或 service facade export 的前提下固化该边界。
- 本次采用的关键假设：当前未跟踪的 `docs/rebuild/AutoWaterSimu_95分优雅度完整计划.md` 不属于本阶段目标文件；本轮只做已满足边界的自动化 gate，不扩展 live browser smoke、不删除 `computeJobsService` compatibility facade、不改变 feature API exports。
- 剩余不确定性：live backend browser reads job/result/evidence、hosted integration/browser/release evidence、进一步 route/component decomposition、8 条 golden scenarios 自动化、完整生产安全 data scope 和 all-mutation audit 仍是后续阶段。
- 影响范围 / 风险控制方式：规则仅扫描 `frontend/src/routes` 的源码引用，且只针对已经迁移完成的 Compute route boundary；不触碰业务实现和生成文件；通过 dependency check、direct scan、frontend typecheck、pr-fast 和 diff-check 验证。
- 验证方式：`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1` 通过；`rg -n "@/services/computeJobsService|services[/\\]computeJobsService|@/features/[^'\"]+/(api|builders|processGraphApi)|features[/\\][^'\"]+[/\\](api|builders|processGraphApi)" frontend\src\routes --glob "*.ts" --glob "*.tsx"` 无结果；generated Compute client direct import scan 仅命中允许的 `frontend/src/features` 与 `frontend/src/shared/api`；`cd frontend; npx tsc --noEmit` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` 通过并写出 `tmp\ci-evidence\pr-fast.json`；`git diff --check -- scripts docs tasks .ai` 通过，仅有 LF/CRLF warning。
- 是否更新 README：是，更新 `scripts/README.md`；架构长期事实同步更新 `docs/architecture/dependency-graph.md`、`docs/architecture/current-state.md`、`docs/architecture/module-map.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本次是既有 feature query boundary 的自动化 enforcement，没有新增长期架构决策。

## 00:15 - Frontend Compute feature query migration

- 用户目标：根据 Certainty/Elegance PRD 与开发计划继续完善 AutoWaterSimu Next，完成一个可验证的大阶段并适时提交推送。
- 涉及目录：`frontend/src/features/`、`frontend/src/routes/_layout/`、`frontend/src/services/`、`docs/architecture/`、`docs/rebuild/`、`tasks/`。
- 修改内容：新增 `features/compute-jobs/queries.ts`、`features/lifecycle/queries.ts`、`features/model-governance/queries.ts` 和 `features/contracts/queries.ts`，把 Compute 相关 TanStack Query query/mutation options 下沉到 feature 层；`compute-jobs.tsx`、`compute-lifecycle.tsx` 和 `model-governance.tsx` 改为通过 feature query/mutation options 调用数据能力，不再 import `computeJobsService`；同步更新 feature/services README、architecture current-state/dependency-graph/module-map、Certainty/Elegance plan 和任务复盘。
- 修改原因：继续落实 Certainty/Elegance Phase 1 的 frontend service 分层目标；上一阶段已经把 generated Compute client 调用迁入 `shared/api` 与 `features/*/api.ts`，但 route 层仍直接围绕 compatibility facade 编写 query key、queryFn 和 mutationFn。
- 本次已消除的不确定性：确认三个 Compute route 可以在不改 UI markup、route path、generated client、OpenAPI、后端 API、service facade export 或服务返回 shape 的前提下迁到 feature query/mutation options；确认 `retry:false`、`refetchInterval`、`staleTime`、production-readiness result-hash gating、query invalidation、local onSuccess/onError state updates 等 route-specific 行为可保留在 route 层。
- 本次采用的关键假设：当前未跟踪的 `docs/rebuild/AutoWaterSimu_95分优雅度完整计划.md` 不属于本阶段目标文件；本轮只做 route data-boundary migration，不做 live backend browser read、不拆 UI panels、不删除 `computeJobsService` compatibility facade。
- 剩余不确定性：live backend browser reads job/result/evidence、hosted integration/browser/release evidence、进一步 route/component decomposition、8 条 golden scenarios 自动化、完整生产安全 data scope 和 all-mutation audit 仍是后续阶段。
- 影响范围 / 风险控制方式：不触碰 generated client、OpenAPI、后端 schema、route path、UI markup 或服务返回 shape；`computeJobsService` 继续保留兼容导出；通过 frontend typecheck、dependency check、direct facade reference scan、pr-fast 和 diff-check 验证。
- 验证方式：`cd frontend; npx tsc --noEmit` 通过；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check-deps.ps1` 通过；`rg -n "computeJobsService|@/services/computeJobsService" frontend\src\routes\_layout\compute-jobs.tsx frontend\src\routes\_layout\compute-lifecycle.tsx frontend\src\routes\_layout\model-governance.tsx frontend\src\features --glob "*.ts" --glob "*.tsx"` 无结果；`rg -n "client/compute|@/client/compute|src/client/compute|\.\/client\/compute|\.\\client\\compute" frontend\src --glob "*.ts" --glob "*.tsx"` 仅发现 `frontend/src/shared/api` 和 `frontend/src/features` 下直接 import；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci\pr-fast.ps1` 通过并写出 `tmp\ci-evidence\pr-fast.json`；`git diff --check -- frontend docs tasks .ai` 通过，仅有 LF/CRLF warning。
- 是否更新 README：是，更新 `frontend/src/features/*/README.md`、`frontend/src/features/README.md` 和 `frontend/src/services/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：否，本次是既有 frontend feature-sliced query boundary 的落地切片，没有新增长期架构决策。
