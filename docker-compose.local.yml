version: '3.8'

services:
  # PostgreSQL 数据库服务
  postgres:
    image: postgres:15
    container_name: autowatersimu_postgres_local
    environment:
      POSTGRES_DB: autowatersimu
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: dataanalysis123
      POSTGRES_INITDB_ARGS: "--encoding=UTF8 --locale=C --auth-host=trust"
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./backend/scripts/init-db.sql:/docker-entrypoint-initdb.d/init-db.sql
    networks:
      - autowatersimu_network
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d autowatersimu"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Redis 缓存服务
  redis:
    image: redis:7-alpine
    container_name: autowatersimu_redis_local
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - autowatersimu_network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # pgAdmin 数据库管理工具
  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: autowatersimu_pgadmin_local
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@autowatersimu.local
      PGADMIN_DEFAULT_PASSWORD: admin123
      PGADMIN_CONFIG_SERVER_MODE: 'False'
    ports:
      - "5050:80"
    volumes:
      - pgadmin_data:/var/lib/pgadmin
    networks:
      - autowatersimu_network
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy

volumes:
  postgres_data:
    name: autowatersimu_postgres_data
  pgadmin_data:
    name: autowatersimu_pgadmin_data
  redis_data:
    name: autowatersimu_redis_data

networks:
  autowatersimu_network:
    name: autowatersimu_network
    driver: bridge
