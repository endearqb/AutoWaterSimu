日期：2026-01-29

内容：
  - 新增功能：
    - `posthog-demo` 桌面新增 4 个 JSON 示例流程图入口（ASM1-SST / asm1slim / ZLD / multi-flow），弹窗内通过 `/openflow?src=...` 直接加载，并提供下载按钮。
    - `posthog-demo` 桌面新增 `Flow Components` 入口：弹窗内提供“路由式”导航，在弹窗内渲染组件预览与使用说明。
  - 改进：
    - 弹窗最大化定位改为距离浏览器窗口顶端 `24px`。
    - `home.mdx` 图标改为 `home`，并在弹窗 iframe 内通过 `embed=1` 隐藏页面内 header。
    - 桌面图标改用 `frontend/public/assets/icons` 下的 PNG 资源；图标图片调整为尽量铺满容器显示。
    - 桌面右下角增加倾斜放置的 `handwriting.jpeg` 装饰图（位于图标下层）。
    - `posthog-demo` header 去掉搜索按钮，增加语言切换按钮（zh/en）。
    - 默认弹窗内容改为默认显示 `home` 图标的内容（不再显示 WIP 内容）。
  - 修复：
    - 处理 `embed` 搜索参数的 number/string 兼容，避免打开 `home` 时出现 TanStack Router 报错页。
    - `AI DeepResearch` / `Changelog` 等 iframe 内页面支持 `embed=1` 隐藏 header，并在页面内跳转时保持 embed 参数。
    - 删除 `CanvasHome` 页面与组件，并保留 `posthog-demo` 使用的 cases（迁移到 `frontend/src/features/posthogDemo/cases.ts`），同时移除 `/canvas-home` 路由。
    - `Flow Components` / `posthog-demo` 新增文案补全 i18n（新增 `posthogDemo.*` keys）。
    - 首页路由 `/` 切换为 `posthog-demo`（PosthogLanding）。
    - i18n 增加 `storage` 事件监听，使 iframe / 多标签页语言切换保持同步。
    - 弹窗内嵌页面（`embed=1`）点击链接/按钮时，改为跳转到顶层登录页（`/login`），避免只在弹窗内导航。

  - 改进：
    - 首页（posthog-demo）：暂时下线 OpenFlow 入口（移除桌面图标）。
    - 首页（posthog-demo）：桌面背景改为 `#FAF9F5`；header 高度降低并增加阴影（不改 header 颜色）。
    - 4 个示例流程图弹窗：移除内层标题栏/JSON 名，仅保留下载按钮；通过 `/openflow?embed=1&ui=preview&src=...` 以预览模式渲染（隐藏节点工具栏/控制按钮）。
    - Flow Components 弹窗：增加「节点工具栏 / 参数设置 / 独立模拟 / 数据分析」面板切换；Canvas 在弹窗内渲染，点击按钮仅渲染对应面板；提供示例数据下载。
  - 修复：
    - home 弹窗（`/midday-style?embed=1`）：恢复 GitHub / 微信 / 备案号超链接可直接打开，其它链接仍跳转到登录/注册页。
    - AI DeepResearch 弹窗：取消 embed 模式下的点击跳转登录拦截，卡片点击直接在弹窗内打开原有 HTML 页面（`/assets/html/*.html`）。
  - 改进：
    - 桌面图标文案：`Docs` 改为 `Knowledge (DeepResearch)`；`AI DeepResearch` 改为 `AI DeepResearch (Web)`。
  - 改进：
    - header：进一步降低高度；缩小语言切换与 `Get started — free` 按钮内边距，并为按钮增加阴影。
    - 示例流程图：JSON 下载按钮颜色与 `Get started — free` 保持一致。
    - Flow Components：在 OpenFlow 弹窗中，点击画布节点/连线或节点工具栏项会弹出使用说明气泡（含 i18n）。
    - 桌面图标：新增「Simulation Panel」图标，弹窗展示 `SimulationActionPlate`；新增 `33_dashboard.png` 图标，弹窗展示 `ASM1SlimAnalyzer` 结果看板。
    - 多终端：手机端隐藏桌面两张装饰图片；当弹窗窗口过小时，窗口内容区域允许滚动。
    - 多终端：header 固定；桌面内容区独立滚动（高度不足时出现垂直滚动条）；手机端恢复显示图标。
