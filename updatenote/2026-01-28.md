日期：2026-01-28

内容：
  - 新增功能：
    - 新增 PostHog 风格新首页 Demo 路由：`/posthog-demo`（保留现有 `/` 首页不变）
    - Demo 中实现可拖拽/可最大化/可关闭的 Windows 风格窗口
    - 桌面图标点击在弹窗内打开对应内容（OpenFlow/Docs/AI DeepReseach/Calculators 等）
    - 新增 `/docs`：知识库在弹窗内展示，带可折叠目录（移动端抽屉）
  - 改进：
    - 顶部导航复用原首页 Logo 与渐变标题，保留 CTA（Get started）跳转到 `/signup`
  - 修复：
    - 修复 `frontend/tests/user-settings.spec.ts` TypeScript 未使用导入导致的 `tsc --noEmit` 报错

