日期：2026-02-14

内容：
  - 新增功能：
    - 新增独立 Hybrid 配置页面（`/hybrid`），支持创建、编辑、删除、应用已保存配置。
    - 新增 UDM Hybrid 配置后端 CRUD API（`/api/v1/udm-hybrid-configs`）与数据库表。
    - 在左下角 FiMenu 的 Hybrid 配置弹窗中新增“已保存配置”选择并支持直接应用。
  - 改进：
    - Hybrid 配置应用流程抽取为公共工具（模型快照、焦集变量、参数归一化等）。
    - 应用 Hybrid 配置时，若节点绑定模型不在当前 Hybrid 选择集内，自动清理旧绑定字段，避免脏数据。
  - 修复：
    - 修复新建 `/hybrid` 路由后未被路由类型系统识别的问题（重新生成 `routeTree.gen.ts`）。
