# AutoWaterSimu Next Current State

> Snapshot date: 2026-06-14.

This document summarizes long-lived facts from README files, `.ai/decisions/`, release gate scripts, and the Certainty/Elegance quality plan. It does not replace source code, tests, OpenAPI, contracts, or `.ai/changes/`.

## Landed

- README First protocol is active through `AGENTS.md`, `README_First.md`, directory README files, `.ai/changes/`, and `.ai/decisions/`.
- Next monorepo surfaces exist beside legacy `frontend/` and `backend/`: `contracts/`, `apps/api/`, `services/simulation-worker/`, `apps/desktop/`, and `simulation_core/`.
- `contracts/` contains versioned JSON Schemas, a registry, a codegen/validation policy manifest, valid/invalid examples, and contract tests.
- `contracts/desktop_project_package.v1.json` and `contracts/desktop_support_bundle.v1.json` define the Desktop project package and support bundle wire shapes with valid/invalid fixtures.
- `ontology/` contains the first Water Ontology registries for objects, actions, links, and policies; `docs/architecture/ontology-model.md` records their purpose and runtime enforcement boundary.
- `scripts/check-ontology.ps1` validates Water Ontology schema versions, unique keys, object/action/link/policy references, and required descriptive fields.
- `apps/api/` exposes the Go Compute API with job lifecycle, worker lifecycle, artifacts, model governance, evidence, simulation checks, result explanation workflow, production readiness, metrics, retention, and archive boundaries.
- `apps/api/internal/compute/http.go` keeps the HTTP server entrypoint and unchanged route registration block, while route handlers are split by group across `http_jobs.go`, `http_artifacts.go`, model governance `http_model_*.go` / `http_benchmark_runs.go`, `http_contracts.go`, `http_simulation.go`, `http_workers.go`, and `http_metrics.go`.
- `apps/api/internal/compute/store_interfaces.go` expresses the aggregate metadata `Store` as 13 embedded domain/audit store interfaces, including `MutationAuditStore`, while `memory_*.go` files keep the in-memory `MemoryStore` implementation split by domain in the same package; model governance in-memory persistence is further split across model run, benchmark run, and model catalog files, non-job-scoped mutation audit persistence lives in `memory_mutation_audit.go`, and draft/result explanation persistence is split across draft confirmation and result explanation files.
- `apps/api/internal/compute/postgres.go` keeps the PostgreSQL store entrypoint and migration apply/check helpers, while `postgres_*.go` files keep PostgreSQL metadata persistence split by domain in the same package; model governance persistence is further split across model run, benchmark run, and model catalog files, non-job-scoped mutation audit persistence lives in `postgres_mutation_audit.go`, and draft/result explanation persistence is split across draft confirmation and result explanation files.
- `apps/api/internal/compute/artifact_lifecycle.go` keeps `ArtifactLifecycleService` struct/constructor/store/object-store wiring, while same-package `artifact_lifecycle_*.go` files group artifact upload, listing/metadata lookup/download fallback, retention sweep, and archive copy/checksum/delete workflows.
- `apps/api/internal/domain/agent` owns stable draft confirmation envelope validation, draft confirmation record data projection including tenant/project/site metadata, constraint application advisory plan assembly, and Agent draft proposed request extraction; `compute` keeps schema file lookup, JSON Schema validation, compute record mapping, approval/schema gating, promotion job creation, persistence, and HTTP mapping.
- `apps/api/internal/domain/artifacts` owns stable artifact retention policy parsing, upload/archive metadata record projection, compact audit state projection, candidate checks, and sweep action planning; `compute` keeps object storage, archive execution, checksum verification, metadata persistence, selected audit envelope/call site/persistence decisions, hot object deletion, download fallback, and HTTP mapping.
- `apps/api/internal/domain/evidence` owns stable evidence package input reference extraction, evidence ref parsing, evidence object scope matching, stored result summary risk projection, result explanation evidence ref extraction, result explanation record data projection, result explanation compact audit state projection, embedded simulation input payload lookup, `risk_findings` extraction/summary rules, and production-readiness policy evaluation; `compute` keeps job completion persistence, result explanation schema validation/job availability/evidence resolution/record mapping/persistence/review-publish mutations, selected audit envelope/call site/persistence decisions, store-backed evidence governance, response DTOs, job-scoped model catalog/artifact/process graph callbacks, compute record-to-evidence-scope adapters, and HTTP mapping.
- `apps/api/internal/compute/simulation_inputs.go` now keeps `SimulationInputService` struct/constructor/store wiring, while same-package `simulation_inputs_*.go` files group simulation input registry/read, process graph registry/projection, `simulation_request.input_ref` resolution, and model-run replay workflows.
- `apps/api/internal/compute/draft_workflows.go` now keeps `DraftWorkflowService` struct/constructor/callback wiring, while same-package `draft_workflows_*.go` files group draft confirmation validation/persistence/read, compact mutation audit, constraint application planning, and explicit simulation-check promotion workflows; draft confirmation envelope validation and record data projection use `apps/api/internal/domain/agent`.
- `apps/api/internal/compute/result_explanations.go` keeps `ResultExplanationService` constructor/callback wiring while `result_explanations_*.go` groups submit/read/review-publish workflows behind domain store interfaces; result explanation evidence ref extraction, record data projection, and compact audit state projection now use `apps/api/internal/domain/evidence`.
- `apps/api/internal/compute/model_governance.go` now keeps `ModelGovernanceService` struct/constructor/store wiring, while same-package `model_governance_*.go` files group model catalog, default parameter set promotion planning, benchmark case queueing, benchmark run history, and model run lookup workflows behind domain store interfaces.
- `apps/api/internal/domain/jobs` owns stable compute job status constants, terminal/worker-result status invariants, create idempotency duplicate/conflict decision, queued job create record projection / create-event plan, failed-worker fallback result construction, worker result completion status/error extraction, worker claim capability/contract-version matching, worker claim candidate ordering/running mutation planning, worker heartbeat lease refresh mutation planning, and cancel/timeout state lifecycle mutation planning; `compute` keeps compatibility aliases, MemoryStore/PostgresStore projection calls, worker claim queue scanning/locking, worker table heartbeat updates, job payload hashing/idempotency store lookup/error mapping, concrete state persistence, audit envelope assembly, snapshot assembly, and HTTP wiring.
- `apps/api/internal/domain/models` owns stable built-in `model_catalog.v1` document shape, persisted model catalog snapshot record data projection, `benchmark_run.v1` record data projection, default parameter set status transition document mutation projection, compact model governance audit state projection, benchmark case schedule-run `compute_job.v1` document shape, `compute_result.v1.runtime_audit.model_runs` extraction/precheck, `model_run.v1` raw identity/evidence refs/warnings/parameter_hash extraction, model-run identity/hash checking, `benchmark_run.v1` evidence refs extraction, benchmark case scheduling / benchmark run admission gates, single benchmark case promotion evidence/readiness, default parameter set status transition invariants, promotion gate policy, and model_run production governance gate; `compute` keeps material-balance default parameter hash generation, typed catalog DTO conversion, model_run schema validation/persistence, model catalog governance workflow, catalog selection/scope gates, catalog store/audit persistence, selected audit envelope/call site/persistence decisions, benchmark workflow orchestration, benchmark_run validation/orchestration/persistence/audit, benchmark query orchestration, benchmark schedule-run catalog lookup/gate/error mapping/execution profile lookup/simulation input resolution/createJob, promotion plan DTO mapping, persistence implementation, evidence/simulation wiring, DTOs, and HTTP mapping.
- `apps/api/internal/domain/simulation` owns stable simulation job type execution profile / worker capability mapping, pure simulation check `compute_job.v1` document assembly, material-balance process graph validation/projection rules, and simulation input/process graph record data projection used by simulation-check creation, benchmark case queueing, process graph registration validation, ProcessGraph-to-SimulationInput resolution, and simulation registry record assembly; `compute` keeps simulation request schema validation, input_ref resolution, simulation input/process graph metadata persistence, compute record mapping, schema/store orchestration, HTTP, and model governance wiring.
- `apps/api/internal/compute/worker_lifecycle.go` adapts existing `WorkerStore` and job records into the workers domain package, including tenant/project/site claim-scope forwarding for scoped worker tokens.
- `apps/api/internal/domain/workers` owns worker register / claim / heartbeat domain behavior while `compute` keeps only the adapter to existing stores and job state projections.
- `apps/api/internal/compute/evidence_governance.go` now keeps `EvidenceGovernanceService` struct/constructor/store wiring, while same-package `evidence_governance_*.go` files group result read, evidence package export, production readiness, and evidence-ref resolution workflows behind job/model-run/process-graph stores and artifact/model-catalog callbacks; evidence package and production-readiness governance pass the source job tenant/project/site into model catalog read selection, and process graph evidence-ref resolution maps compute records into `domain/evidence.ObjectScope` before rejecting stored objects whose tenant/project/site conflicts with the source job.
- `apps/api/internal/compute/job_lifecycle.go` keeps `JobLifecycleService` constructor/callback wiring while `job_lifecycle_*.go` groups job create, read/events/snapshot, cancel/timeout adapter, complete/fail, and model-run persistence behind job/model-run stores and artifact listing callback; create maps the `domain/jobs` idempotency decision to existing conflict/reused-snapshot behavior, maps queued job projection / event plans into compute records, worker claim and heartbeat persistence apply `domain/jobs` mutation plans, and cancel/timeout delegates through `domain/jobs.JobStateService`.
- `apps/api/internal/platform/audit` owns selected mutation audit envelope field shape, HTTP request audit context projection, and event JSON attachment without importing compute domain types; `apps/api/internal/compute/audit.go` keeps compatibility wrappers and selected audit call sites. Job-scoped selected audit events live in `compute_job_events.event_json.audit` for job create/queue, job cancel/timeout, worker claim/heartbeat/artifact upload/succeed completion/fail completion, draft promotion / benchmark schedule-run job create/queue, artifact retention delete/archive, and result explanation submit/review/publish. Non-job-scoped selected audit events live in `mutation_audit_events.event_json.audit` for worker registration, draft confirmation records, model catalog registration, default parameter set status/promote, benchmark_run registration, process_graph registration, and simulation_input registration. This is a partial audit slice, not full all-mutation/data-scope enforcement.
- Static token config can carry tenant/project/site scope; HTTP job list/get, process_graph get, simulation_input get, draft confirmation get / constraint plan / promotion, persisted model catalog root/model/snapshot reads, model_run get / job-filtered list, benchmark_run get / job-filtered list, and artifact download deny or filter cross-scope reads for scoped tokens. Evidence package and production-readiness governance now use the source job tenant/project/site to select the matching persisted model catalog before falling back, and evidence-ref process graph dereference returns not found when the stored process graph scope conflicts with the source job. Direct job create/cancel, worker claim/heartbeat/artifact/succeed/fail completion, confirm-draft record persistence, result explanation submit/review/publish, direct simulation-check, draft promotion job create, benchmark schedule-run job create, model catalog registration/status/promote-approved, benchmark_run registration, and explicit process_graph/simulation_input registry POST reject or skip scoped writes outside token tenant/project/site scope. Scoped model catalog default-parameter-set promotion plan and promote-approved require an authorized `job_id` and filter benchmark/model_run evidence to that job; unfiltered promotion evidence remains global-token-only.
- `apps/api/internal/compute/metrics.go` now keeps only compatibility aliases for the platform metrics collector.
- `apps/api/internal/platform/audit` owns selected mutation audit envelope helpers, `apps/api/internal/platform/auth` owns static bearer token config/principal parsing, `apps/api/internal/platform/config` owns the command/runtime `Config` shape, `apps/api/internal/platform/contracts` owns JSON Schema loading, schema_version mapping, reusable contract validation, and base contract document validation response, `apps/api/internal/platform/httpx` owns JSON response writing and loopback-only local CORS, and `apps/api/internal/platform/metrics` owns metrics snapshot shape, read-only metrics collector, and Prometheus rendering without importing compute domain types.
- `apps/api/cmd/compute-api` rejects production startup when static token config is empty or contains default development token values; token config can come from inline `COMPUTE_API_TOKENS_JSON` or file-mounted `COMPUTE_API_TOKENS_FILE`, but not both.
- `scripts/audit-compute-api-boundary.ps1` verifies the current Compute API Store/domain interface shape, internal domain/platform service constructor boundaries, internal package boundary, domain/platform reverse-import boundaries, resolved Store-call source files, and writes evidence under `tmp/architecture-evidence/compute-api-boundary.json`. `scripts/ci/pr-fast.ps1` now runs this audit in the default PR fast lane and records `compute_boundary_audit` plus `tmp/ci-evidence/compute-boundary/compute-api-boundary.json`.
- `scripts/audit-simulation-core-boundary.ps1` now provides the P3 simulation_core / worker / legacy backend boundary audit. It writes `tmp/architecture-evidence/simulation-core-boundary.json`, separates hard runtime violations from open packaging/test/oracle gaps, verifies that simulation_core and worker runtime do not import legacy `app.*`, verifies that the backend material_balance exception leaf is a dependency-backed core re-export, verifies that the backend calculator result model leaf comes from simulation_core and ASM/UDM services do not annotate against the legacy result model, verifies that the backend material_balance utility helper leaf is a dependency-backed core re-export, runs a core-only import smoke with `PYTHONPATH` limited to `simulation_core/python`, and treats backend/core duplicate material-balance implementations as controlled only when the calculator itself is thin-shell-like or an explicit parity drift guard is present.
- `scripts/audit-simulation-core-input-contract.ps1` now provides the simulation_core input contract audit. It writes `tmp/architecture-evidence/simulation-core-input-contract.json`, verifies `simulation_input.v1` top-level unknown-field closure, verifies explicit node/edge item schemas, verifies direct `NodeData` / `EdgeData` runtime construction rejects unknown fields, and runs a runtime probe across adapter `compat`, `warn`, and `strict` unknown-field modes.
- `scripts/audit-simulation-core-correctness-freeze.ps1` now provides the simulation_core correctness-freeze audit. It writes `tmp/architecture-evidence/simulation-core-correctness-freeze.json`, verifies the current `_run_hours` mutually exclusive branch order (`asm1slim` before `asm1` before `asm3` before `udm` before default), verifies ASM/UDM branches clamp solver output while the default branch remains in its current commented-clamp state, and verifies core-only tests freeze branch precedence, clamp policy, and `compute_mask` derivative masking.
- `simulation_core/python` now has `autowatersimu-simulation-core` packaging metadata with PEP 440 version `0.1.0+phase2b`, and `contracts/python` now has `autowatersimu-contracts` packaging metadata with the same phase version. Both packages can be installed in editable mode with `uv pip install --python backend\.venv\Scripts\python.exe -e <path> --no-deps`.
- `services/simulation-worker/` now prefers installed `autowatersimu-simulation-core` / `autowatersimu-contracts` imports before any repo path mutation. The remaining `simulation_core/python` / `contracts/python` `sys.path` mutation is a deprecated compatibility fallback gated behind missing installed package imports, and the P3 boundary audit fails unsafe or direct fallback usage.
- `simulation_core/tests/test_material_balance_core_boundary.py` now holds core-only import boundary, adapter behavior, and `_run_hours` correctness-freeze coverage, including default-compatible unknown-field ignoring, opt-in warn/strict assertions, branch precedence, clamp policy, and `compute_mask` derivative masking, without importing legacy backend. `simulation_core/tests/test_material_balance_core.py` remains the backend parity/oracle file and declares `BACKEND_CORE_DRIFT_GUARD_CASES` for material-balance minimal, ASM1Slim model-bound, independent ASM1Slim/ASM1/ASM3, and UDM parity coverage.
- The P3 simulation_core boundary audit now reports `passed` with 0 hard violations and 0 open gaps. The first backend thin-shell leaves are complete: `backend/app/material_balance/exceptions.py` re-exports `autowatersimu_simulation_core.material_balance.exceptions`, backend declares the local `autowatersimu-simulation-core` dependency, `backend/app/material_balance/core.py` constructs the simulation_core `MaterialBalanceResult`, ASM/UDM services annotate against that core result model, `backend/app/material_balance/utils.py` re-exports `autowatersimu_simulation_core.material_balance.utils`, and focused backend tests guard exception class identity, result class identity, and utility helper object identity. Legacy backend and simulation_core still duplicate material-balance runtime files, but the duplicate implementation state is guarded by backend/core parity tests that compare status, total steps, timestamps, node fields, and numeric series across the required drift guard cases before broader backend thin-shell migration.
- `services/simulation-worker/tests/test_worker_cli.py` now holds worker runtime, CLI, JSON-RPC, API bridge, artifact, model_run, and dependency-boundary coverage without statically adding legacy `backend/app`; `test_worker_backend_oracle.py` isolates old-vs-worker backend oracle numerical baselines. The P3 boundary audit treats backend oracle imports as acceptable only in that dedicated file.
- Compute service-test split is structurally closed for the current P0 list: job read/list scope, job create/cancel mutation scope, worker claim filters, worker claim/heartbeat audit, worker artifact/succeed completion audit, worker fail audit, artifact retention HTTP/scope, model catalog read/mutation/promotion scope, result explanation audit/scope, evidence governance scope, and process graph evidence scope all have focused files. Future service-test splits should only happen when a test still mixes security/data-scope with happy path behavior, combines future performance hot paths such as claim/list/heartbeat, risks losing security-smoke coverage, or makes failures ambiguous across scope/audit/method/persistence/DTO concerns; line count alone is not a reason to keep splitting.
- `services/simulation-worker/` supports self-check, direct job execution, API once mode, API loop mode, and Desktop sidecar JSON-RPC mode.
- `frontend/` has generated Compute client isolation under `frontend/src/client/compute`; `frontend/src/shared/api/computeApiClient.ts` owns Compute client base URL/token configuration, `frontend/src/shared/api/computeTypes.ts` owns shared UI-facing Compute wrapper types, `frontend/src/features/compute-jobs`, `features/lifecycle`, `features/model-governance`, and `features/contracts` own feature API wrappers plus route-facing query/mutation options, the Compute Jobs/Lifecycle/Model Governance routes use `features/*/queries.ts` instead of `computeJobsService`, `computeJobsService.ts` remains a compatibility facade over those wrappers, and `check-deps` rejects direct generated Compute client imports outside `frontend/src/shared/api`, `frontend/src/features`, and the generated client directory while also rejecting Compute route bypasses of the feature query boundary.
- `apps/desktop/` has Tauri runtime, local project package/export/import, worker sidecar management, support bundle and packaging smoke boundaries.
- New Desktop project exports use `desktop_project_package.v1`; the runtime still imports legacy `desktop_project_export.v1` files for backward compatibility.
- `scripts/release/next-release-gates.ps1` generates local/CI release gate evidence under `tmp/release-evidence`.
- `scripts/release/smoke-release-artifact-download.ps1` generates temporary release artifact fixtures, verifies the download validator success path, verifies the missing-installer rejection path, and writes `tmp/release-evidence/release-artifact-download-smoke.json`.
- Root `Justfile`, `scripts/doctor.ps1`, and `scripts/check-deps.ps1` now provide the first Phase 0 task graph and dependency check entry. `check-deps` also guards the new API platform/domain boundary by preventing `apps/api/internal/platform` and `apps/api/internal/domain` from importing the compute compatibility package.
- `docker-compose.dev.yml` now provides a source-mounted Next dev stack candidate for PostgreSQL, MinIO, Go Compute API, Python worker loop, and Vite frontend.
- `scripts/ci/pr-fast.ps1` and `.github/workflows/next-pr-fast.yml` define a PR fast lane that writes `tmp/ci-evidence/pr-fast.json` and includes dependency boundary, Compute API boundary audit, simulation_core correctness-freeze audit, README path, ontology, contracts, Go, frontend, and Desktop checks.
- `scripts/ci/integration-smoke.ps1` defines an opt-in local integration smoke that starts an isolated Compose project for PostgreSQL + MinIO + Compute API, runs the host Python worker API once, verifies job/result/model_run/artifact/evidence/retention dry-run/metrics, and writes `tmp/ci-evidence/integration-smoke.json`.
- `.github/workflows/next-integration-smoke.yml` exposes the integration smoke as a manual/reusable hosted workflow that uploads `next-integration-smoke-evidence`.
- `scripts/ci/security-smoke.ps1` defines an opt-in security smoke for production token guard, file-mounted token config source, static token revocation, scope-denial, artifact admin-scope checks, selected mutation audit envelopes including job create/cancel/timeout, worker registration/claim/heartbeat/artifact/succeed completion/fail completion, draft confirmation, draft promotion, direct simulation-check, model governance and simulation registry global audit, tenant/project/site read-scope checks for job list/get, process_graph get, simulation_input get, draft confirmation get / constraint plan / promotion, persisted model catalog root/model/snapshot reads, model_run get / job-filtered list, benchmark_run get / job-filtered list, artifact download, evidence governance model catalog selection, and process graph evidence-ref object scope, plus job collection method/no-write, direct job create/cancel, artifact retention sweep candidate filtering and archive cross-scope no-write, worker claim/heartbeat/artifact/succeed/fail completion, confirm-draft record persistence, result explanation submit/review/publish, direct simulation-check, draft promotion job create, benchmark schedule-run job create, model catalog registration/status/promote, benchmark_run registration, and explicit process_graph/simulation_input registry POST mutation data-scope checks, writing `tmp/ci-evidence/security-smoke.json`.
- `.github/workflows/next-security-smoke.yml` exposes the security smoke as a manual/reusable hosted workflow that uploads `next-security-smoke-evidence`.
- `docs/architecture/compute-mutation-scope-matrix.md` records the P2 selected-mutation method guard, static-token auth scope, tenant/project/site mutation scope, no-write denial, audit envelope, `security-smoke` selection, and focused-test evidence per mutation route. Its previous red/yellow rows for route-specific audit smoke alignment, collection-route method proof, direct simulation-check audit proof, and archive-specific no-write proof are now closed; the still-future work remains full all-mutation audit, complete object-level data scope, OIDC/RBAC, and ontology-backed policy enforcement.
- `scripts/ci/browser-smoke.ps1` defines an opt-in mock-backed Playwright browser smoke for Compute Jobs/current-flow/result/evidence, contract validation, Model governance, and lifecycle retention, writing `tmp/ci-evidence/browser-smoke.json`.
- `scripts/ci/live-backend-browser-smoke.ps1` defines an opt-in live backend browser smoke that prepares a PostgreSQL/MinIO/worker-backed succeeded job through integration smoke, keeps the Compute API running, and runs Playwright against Compute Jobs without Compute API route mocks, writing `tmp/ci-evidence/live-backend-browser-smoke.json`.
- `scripts/ci/current-flow-live-smoke.ps1` defines an opt-in current-flow live browser smoke that starts an isolated Compute API/PostgreSQL/MinIO stack plus bounded host Python worker loop, submits current flow through the browser, waits for the real worker result, then verifies UI evidence package download and evidence ref resolution, writing `tmp/ci-evidence/current-flow-live-smoke.json`.
- `.github/workflows/next-browser-smoke.yml` exposes the browser smoke as a manual/reusable hosted workflow that uploads `next-browser-smoke-evidence`.
- `.github/workflows/next-live-backend-browser-smoke.yml` exposes the live backend browser smoke as a manual/reusable hosted workflow that uploads `next-live-backend-browser-smoke-evidence`.
- `.github/workflows/next-current-flow-live-smoke.yml` exposes the current-flow live smoke as a manual/reusable hosted workflow that uploads `next-current-flow-live-smoke-evidence`.
- `scripts/ci/desktop-package-smoke.ps1` defines an opt-in Desktop package smoke for Desktop package/support bundle fixtures, clean-runtime project package export/import, support bundle redaction, and Desktop typecheck, writing `tmp/ci-evidence/desktop-package-smoke.json`.
- `scripts/ci/desktop-release-artifacts-smoke.ps1` defines an opt-in heavy Desktop release artifact smoke that builds a real unsigned PyInstaller sidecar and NSIS installer, runs packaged worker CLI/runtime/installed-sidecar smokes, verifies a local unsigned artifact bundle, runs release gate `Mode=release` with real artifact paths, and writes `tmp/ci-evidence/desktop-release-artifacts-smoke.json`.
- `.github/workflows/next-desktop-package-smoke.yml` exposes the Desktop package smoke as a manual/reusable hosted workflow that uploads `next-desktop-package-smoke-evidence`.
- `.github/workflows/next-nightly.yml` schedules and manually exposes a nightly evidence orchestrator for pr-fast, integration, browser, live backend browser, current-flow live, security, and Desktop package smoke lanes, plus `next-nightly-summary-evidence`.
- `scripts/ci/golden-scenarios.ps1` defines a local golden scenario evidence summary that reads existing CI/release evidence, checks whether lane evidence matches the current HEAD, derives PostgreSQL migration coverage only from integration smoke or a passed `postgres migration up/down smoke` release-gate step, and writes `tmp/ci-evidence/golden-scenarios.json`. Its default mode is summary-only; `-RefreshLocalEvidence` refreshes non-Docker local lanes first (`pr-fast`, browser, security, Desktop package, release artifact download smoke, and merge release gate with `-SkipLong`) and records refresh step outcomes in the same evidence file. `just golden-scenarios-refresh-integration` adds explicit Docker-backed integration smoke through `-RunIntegrationSmoke`; `just golden-scenarios-refresh-live` adds live backend browser read evidence through `-RunLiveBackendBrowserSmoke`; `just golden-scenarios-refresh-current-flow-live` adds UI current-flow submit to live worker/evidence proof through `-RunCurrentFlowLiveSmoke`.
- Local CI smoke evidence keeps backward-compatible `is_dirty_*` / `dirty_files_*` fields and also records `has_tracked_changes_*`, `tracked_changes_*`, and `untracked_files_*` so local untracked documents can be distinguished from uncommitted tracked edits.
- `scripts/check-contracts.ps1` validates `contracts/registry.json`, runs contract tests, regenerates the Compute TS client, and checks OpenAPI/client drift.

## Accepted Current Gaps

- `apps/api/internal/compute` is still a large compatibility wiring package. The aggregate `Store` resolves to 42 methods across 13 embedded domain/audit metadata interfaces, the in-memory `MemoryStore` implementation is split across same-package `memory_*.go` domain files, with model governance persistence further split across `memory_model_runs.go`, `memory_benchmark_runs.go`, and `memory_model_catalog.go`, mutation audit persistence in `memory_mutation_audit.go`, and draft/result explanation persistence split across `memory_draft_confirmations.go` and `memory_result_explanations.go`; HTTP handlers are split across same-package route-group files while preserving `Routes`, and model governance HTTP mapping is further split across catalog, parameter set, benchmark case, benchmark run, and model run files. PostgreSQL persistence is split across same-package `postgres_*.go` domain files, with model governance persistence further split across `postgres_model_runs.go`, `postgres_benchmark_runs.go`, and `postgres_model_catalog.go`, mutation audit persistence in `postgres_mutation_audit.go`, and draft/result explanation persistence split across `postgres_draft_confirmations.go` and `postgres_result_explanations.go`; the public `Service` compatibility delegates are split across same-package `service_*.go` domain files while `service.go` keeps only struct and constructor wiring. Job lifecycle workflow methods are split across same-package `job_lifecycle_*.go` files while `job_lifecycle.go` keeps constructor/callback wiring, create maps `domain/jobs` idempotency decision plus queued job projection / event plans into compute behavior and records, worker claim and heartbeat apply `domain/jobs` mutation plans in MemoryStore/PostgresStore persistence, and cancel/timeout delegates through `domain/jobs.JobStateService`; artifact lifecycle workflow methods are split across same-package `artifact_lifecycle_*.go` files while `artifact_lifecycle.go` keeps constructor/store/object-store wiring; model governance workflow methods are split across same-package `model_governance_*.go` files with compact audit construction in `model_governance_audit.go` while `model_governance.go` keeps constructor/store/callback wiring; evidence governance workflow methods are split across same-package `evidence_governance_*.go` files while `evidence_governance.go` keeps constructor/callback wiring; simulation input/process graph workflow methods are split across same-package `simulation_inputs_*.go` files while `simulation_inputs.go` keeps constructor/store wiring; draft workflow methods are split across same-package `draft_workflows_*.go` files while `draft_workflows.go` keeps constructor/callback wiring; result explanation workflow methods are split across same-package `result_explanations_*.go` files while `result_explanations.go` keeps constructor/callback wiring. Artifact lifecycle including upload/listing, simulation input/process graph, draft workflow, result explanation, model governance, evidence governance, job lifecycle, and metrics paths now have narrowed constructors. Draft confirmation envelope validation, draft confirmation record data projection, constraint application advisory plan assembly, and Agent draft proposed request extraction have moved to `apps/api/internal/domain/agent`; artifact retention policy helpers, sweep action planner, upload/archive metadata record projection, and compact audit state projection have moved to `apps/api/internal/domain/artifacts`; evidence input/ref/risk parsing, evidence object-scope matching, stored result summary risk projection, result explanation evidence ref extraction, result explanation record data projection, result explanation compact audit state projection, and readiness policy helpers have moved to `apps/api/internal/domain/evidence`; job status, create idempotency decision, queued job create record projection / create-event plan, failed-worker fallback result construction, worker result completion, worker claim matching invariants, worker claim running mutation planning, worker heartbeat lease refresh mutation planning, and cancel/timeout state lifecycle mutation planning have moved to `apps/api/internal/domain/jobs`; stable built-in `model_catalog.v1` document shape, persisted model catalog snapshot record data projection, `benchmark_run.v1` record data projection, default parameter set status transition document mutation projection, compact model governance audit state projection, benchmark case schedule-run `compute_job.v1` document shape, `compute_result.v1.runtime_audit.model_runs` extraction/precheck, `model_run.v1` raw parsing, model-run identity/hash checking, `benchmark_run.v1` evidence ref parsing, benchmark workflow gates, benchmark case promotion evidence/readiness, default parameter set status transition invariants, promotion gate policy, and model_run production governance gate have moved to `apps/api/internal/domain/models`; simulation execution profile, simulation check job document assembly, material-balance process graph validation/projection helpers, and simulation input/process graph record data projection have moved to `apps/api/internal/domain/simulation`; and worker register/claim/heartbeat has moved to `apps/api/internal/domain/workers` behind a compute adapter. The architecture audit fails if internal domain/platform service constructors accept aggregate `Store` or expose more than three store-like parameters. Platform audit/auth/config/contracts/HTTP/metrics helpers, the base contract document validation response, and the read-only metrics collector moved to `apps/api/internal/platform/audit`, `apps/api/internal/platform/auth`, `apps/api/internal/platform/config`, `apps/api/internal/platform/contracts`, `apps/api/internal/platform/httpx`, and `apps/api/internal/platform/metrics`, but full domain workflows such as full artifact lifecycle, full models governance, full evidence governance, full simulation input/process graph metadata service handling, full agent draft workflow, full result explanation workflow, and full jobs lifecycle have not moved yet. Package-level wiring, public `NewService` / `NewServiceWithArchive` signatures, and some handler-facing behavior still remain in the compatibility package.
- Multi-language generated contract type output for Go, Python, and Rust is not yet implemented; `contracts/codegen/manifest.json` records the current decision to use runtime schema validation or hand-written boundary DTOs for those targets. Desktop package/support-bundle contracts now exist, but Rust generated DTO adoption is still deferred.
- The Next dev compose stack now has initial API runtime smoke evidence for PostgreSQL + MinIO + Compute API + host Python worker. A hosted/manual integration workflow file exists, and nightly now calls it through `workflow_call`, but a real GitHub Actions green run is still pending. Hosted browser workflow entries now exist for mock-backed, live backend read, and current-flow live lanes, but actual hosted green runs, packaged worker first-start timing, and full compose `simulation-worker` container behavior still need dedicated evidence.
- Local `pr-fast` and smoke evidence records dirty worktree state and separates tracked changes from untracked files; a clean HEAD proof still requires running the same lane after tracked changes are committed and without relevant untracked local files, or using hosted CI evidence.
- Full production OIDC/RBAC/data-scope policy is not implemented. P0 static tokens remain the current auth model, but production startup now blocks empty token config and default development token values, supports mounted token config files through `COMPUTE_API_TOKENS_FILE`, and security smoke covers token file source, token revocation, scope denial, admin-only artifact retention, selected mutation audit envelopes for job create, job cancel/timeout, worker registration, worker claim/heartbeat/artifact upload/succeed completion/fail completion, draft confirmation records, draft promotion / benchmark schedule-run job create, direct simulation-check job create, artifact retention delete/archive, result explanation submit/review/publish, compact model governance mutations, simulation registry registrations, tenant/project/site read-scope on job list/get, process_graph get, simulation_input get, draft confirmation get / constraint plan / promotion, persisted model catalog root/model/snapshot reads, model_run get / job-filtered list, benchmark_run get / job-filtered list, artifact download, process graph evidence-ref object-scope, job collection method/no-write, direct job create/cancel, artifact retention sweep candidate filtering and archive cross-scope no-write, worker claim/heartbeat/artifact/succeed/fail completion, confirm-draft record persistence, result explanation submit/review/publish, direct simulation-check, draft promotion job create, benchmark schedule-run job create, model catalog registration/status/promote, benchmark_run registration, and explicit process_graph/simulation_input registry POST mutation data-scope. `compute-mutation-scope-matrix.md` now aligns selected smoke evidence with focused route proof for the selected mutation slice, but full object-level data scope, remaining mutation data-scope, and complete all-mutation audit remain incomplete. A hosted/manual security workflow exists, but a real GitHub Actions green run is still pending.
- Browser smoke is split into mock-backed route orchestration, an opt-in live backend browser read lane, and an opt-in current-flow live submit lane. The mock-backed lane covers Compute Jobs current-flow submission, job result/readiness reads, evidence package download, evidence ref lookup, contract validation, Model governance, and lifecycle retention. The live backend read lane covers Compute Jobs reads of a real PostgreSQL/MinIO/worker-backed job/result/evidence/ref without Compute API route mocks. The current-flow live lane submits current flow through the browser to a live Compute API, waits for a real worker completion, downloads evidence, and resolves the model_run ref. All three browser lanes now have hosted/manual workflow files, but real GitHub Actions green evidence and a full legacy authenticated backend session are still pending.
- Desktop package smoke is source-mode runtime focused, and `desktop-release-artifacts-smoke.ps1` now provides local real unsigned sidecar/NSIS/release-gate evidence. A hosted/manual Desktop package workflow file exists, but a real GitHub Actions green run is still pending. The release artifact download verifier now has fixture-backed smoke coverage plus local real-artifact bundle verification, but a real hosted unsigned artifact upload/download round trip is still pending. This does not prove signing, auto update, or GitHub Release publication.
- Water Ontology has a first registry-backed semantic layer, but it is not yet a runtime policy enforcement layer. Full OIDC/RBAC/ABAC/data-scope and all-mutation audit enforcement remain future work.
- The 8 golden scenarios are not complete. A local summary/refresh script now maps existing lane evidence to scenario-level `partial` / `missing` / `blocked` status and can refresh non-Docker local lanes, Docker-backed integration, live backend browser reads, and current-flow live submit evidence before summarizing, but existing release gate, worker, contract, Go, frontend, and desktop checks still cover many single-lane validations rather than every scenario end to end. A generic release gate pass is not treated as PostgreSQL migration evidence unless the migration step ran, and hosted/release artifact evidence still needs separate proof.
- Legacy backend and simulation_core still keep duplicate material-balance implementations until the broader backend thin-shell migration. The exception leaf and `utils.py` have moved to backend compatibility re-exports, and the calculator now returns the simulation_core `MaterialBalanceResult`, but legacy `core.py` still contains the calculator implementation and `models.py`, ASM helpers, and UDM helpers remain duplicates or migration candidates. This is no longer an open P3 audit gap because `simulation_core/tests/test_material_balance_core.py` now carries the explicit backend/core parity drift guard and `scripts/audit-simulation-core-boundary.ps1` verifies the drift guard plus exception/result/utils thin-shell leaves. Broader calculator thin-shell migration, old input model/helper cleanup, stricter worker default validation, and performance hot-path optimization remain future work.
- The simulation_core input contract audit is currently `passed` with 0 hard violations and 0 open gaps: `simulation_input.v1` closes unknown top-level fields and node/edge item unknown fields through explicit canonical snake_case field schemas; direct `NodeData` and `EdgeData` runtime construction rejects unknown fields. The adapter keeps default `compat` behavior for worker/backward compatibility, while opt-in `warn` writes unknown top-level/node/edge field details to `MaterialBalanceInput.contract_warnings` and opt-in `strict` rejects those fields before calculation. Worker default strict mode, backend thin-shell migration, and performance hot-path optimization remain future work.
- The simulation_core correctness-freeze audit is currently `passed` with 0 hard violations and 0 open gaps. It intentionally records current `_run_hours` behavior as a pre-performance baseline, not as the desired final mixed ASM/UDM model semantics. Changing branch precedence, default clamp behavior, or `compute_mask` derivative masking should update the tests, audit, and ADR in the same change.

## Immediate Quality Direction

The next low-risk increments are:

1. Keep `scripts/audit-simulation-core-boundary.ps1`, `scripts/audit-simulation-core-input-contract.ps1`, and `scripts/audit-simulation-core-correctness-freeze.ps1` green as P3 baselines; next P3 work should continue backend thin-shell planning from old input models, explicit adapter validation, remaining helper candidates, or calculator delegation before worker/default strictness or hot-path optimization.
2. Continue production security after the selected mutation matrix closeout; full issuer/JWKS/RBAC, complete object data-scope, and all-mutation audit remain later governance work.
3. Keep the P2 selected mutation matrix closed by adding new mutation routes to `compute-mutation-scope-matrix.md` and `security-smoke` in the same change that adds the route.
4. Trigger hosted integration/browser/live backend browser/current-flow live smoke workflows only when the next step can produce actual green GitHub Actions evidence, then record the run as evidence.
5. Expand `check-deps` or `audit-compute-api-boundary.ps1` only after existing code is brought into compliance with each new rule.
6. Keep Go package movement focused on concrete security/correctness/performance blockers; do not continue same-package service-test splitting or wrapper extraction for line-count reasons alone.
7. Add focused browser coverage for the feature-query backed Compute routes before deeper route/component decomposition when that coverage unlocks a concrete route split.
8. Expand scenario-level smoke evidence beyond the current material-balance job path to browser, packaged Desktop worker/installer, security, release, and golden scenario lanes.
9. Connect Water Ontology policies to real authorization/data-scope/audit implementation only after the runtime security model is explicit and test-backed.
